# PROJECT MASTER PLAN — Offline Multi-Agent Decision Transformer for Traffic Signal Control

**Version:** 1.1 · **Last updated:** 2026-08-12 · **Maintained in:** Master Coordination Chat
**Mentor:** Paweł Gora (Quantum AI Foundation) · **Target:** arXiv → IEEE ITSC / IEEE T-ITS / TRB (Q2/Q1)

---

## 1. Headline Contribution (FROZEN — do not renegotiate in sub-chats)

> **"Offline Multi-Agent Decision Transformer (MADT) for traffic signal control, trained purely from logged trajectories, evaluated on a rigorous multi-simulator platform — with cross-backend transfer (CityFlow → SUMO) and rare-event robustness as the headline evaluation axes."**

Three claims, each independently defensible (hardened 2026-07-10 after external critique review):

| # | Claim | Why it survives review |
|---|-------|------------------------|
| C1 | **Data-quality study:** MADT performance as a function of a D4RL-style dataset ladder (random / fixed-time / max-pressure / medium-MAPPO / expert-MAPPO / mixed), trained with **no online exploration** from logged operational data. Ladder released publicly. | Fixes the circularity trap (expert data → trivial imitation; weak data → probably fails). Positioned vs DTLight's DTRL (2 policies, SUMO-only, independent agents): ours has 6 tiers, multi-agent local rewards, **dual-backend paired**. "No online exploration" replaces the rhetorically weak "zero simulator interaction." **Load-bearing.** |
| C2 | **Pre-registered RQ:** does offline sequence modeling degrade more gracefully under scenario shift (demand shocks, incidents, sensor dropout)? Answered via a **2×2 design** ({nominal, shift-augmented} training data × {MADT, MAPPO}) + mechanism ablations (context length, data diversity). | The 2×2 with domain-randomized MAPPO removes the data/architecture confound (the single most likely rejection reason). Known DT risk under shift (ESPER/infeasible-RTG) is addressed by calibrated prompting — and plausibly explains DataLight's negative result. Negative/mixed answer still publishes. **Load-bearing.** |
| C3 | **Dynamics-shift study:** transfer *curve* of CityFlow-trained MADT on SUMO — zero-shot → few-shot (k ∈ {5, 20, 100} episodes, collected by a MaxPressure probe, spanning scenario variants) → full retrain — with within-backend-normalized metrics (vs fixed-time AND max-pressure anchors) and calibrated RTG prompting. Interface documented precisely; one alternative-state-encoding robustness ablation. | No systematic cross-simulator dynamics-shift study for TSC exists (cross-city ≠ cross-backend; X-Light took cross-city). Every curve outcome is publishable. k=1 dropped (statistically meaningless; CityFlow determinism makes same-scenario episodes near-duplicates). Paper stands on C1+C2 if C3 lands negative. |

**⚠️ CLAIM CONSTRAINT — C1's CONCEPT IS 2021 PRIOR ART IN ANOTHER DOMAIN (verified 2026-08-12).**
`arXiv:2112.02845` (Meng et al., peer-reviewed in *Machine Intelligence Research*) introduces **"the
first offline MARL dataset with diverse quality levels"** on StarCraft II, proposes **MADT**, and
studies **offline pre-training → online fine-tuning with few-shot and zero-shot transfer**. That is
C1's ladder concept **and** C3's transfer-curve concept, as one package, five years before us.
**C1 survives; the sentence "we introduce the study of offline MARL performance as a function of data
quality" does not, and must never be written.** What survives is the **TSC instantiation**: 6 tiers
against their 3, multi-agent *local* rewards, dual-backend paired scenarios, the method × tier grid
(P4.6), F3's selection-mechanism decomposition, and the pre-registration. §1's existing wording is
already hedged this way ("ours has 6 tiers, multi-agent local rewards, dual-backend paired") and is
correct as it stands. **Binding: 2112.02845 is cited where C1 is INTRODUCED, not only in the prior-art
list** — a referee who knows it will look for it there first.

**⚠️ CLAIM CONSTRAINT — action masking (measured 2026-08-07, before the claim was written).** The
corpus is collected under `control_mode: acyclic`, whose `available_actions()` returns **every** green
phase, so **`avail_mask` never binds anywhere in our data: 460/460 per-intersection streams are
entirely `True`.** This is binding on what C1/C2/C3 may say. Masking stays in the architecture — the
env raises on illegal actions, masks bind under cyclic and RESCO-cyclic modes, and P6's lane closures
are designed to make them bind — but on **this** corpus a no-mask ablation measures nothing, and no
paper sentence may credit masked-action modelling as a learned capability or as a source of our
performance. Making masking a *result* requires a corpus collected under a mode where it binds: a new
collection, not a re-analysis. Found by the P3 implementer in pre-flight, which is the only cheap time
to find it — after the claim is drafted it costs a retraction.

**Named method component:** *probe-calibrated return prompting* — target RTG expressed relative to a cheap
in-domain probe policy (MaxPressure) return distribution (e.g., quantile-space). One mechanism serving
both C2 (infeasible-RTG under scenario shift) and C3 (reward-scale shift across backends), with its own
ablation. ⚠️ **NOVELTY NARROWED 2026-08-01:** DTLight already sets RTG as `max_offline_return × scale`
(γ_eval = 0.2, γ_online = 0.3) *with its own ablation figure*, explicitly to avoid hand-setting RTG per
agent. So "scale-factor RTG conditioning **within** a domain is taken." Our defensible remainder: RTG
calibrated **in the target domain** from a probe policy run there, for the cross-backend case DTLight
never faces (it never transfers between simulators). Position precisely against their Fig. 4 or a
reviewer will call it a re-run.

**Controversy framing (the paper's hook) — SHARPENED 2026-08-01 after reading both primary sources:**
DataLight (2303.10828) states DT "cannot be applied for TSC", but its Appendix A.5 shows the negative
result rests on a configuration that is not a fair test of sequence modeling: **context length K ∈ {1, 2}
only** (K=1 is *no* sequence modeling), and **a single hardcoded RTG of −351** for all agents and
scenarios. Their DT scores 779–1295 s ATT against ~240 s for good methods, and "DT performs better
under K=1 than K=2" — consistent with a model that never had enough context to model anything.
DTLight (2312.07795) with a proper DT (GPT-2, 6 layers/8 heads teacher) beats all online baselines on
5–6 of 8 scenarios **purely offline**. So the contradiction is real but *explicable*, and our two
mechanisms are exactly the two DataLight omitted: context length (P5.3 ablation) and calibrated RTG
(P4.3). **Be scrupulously fair in the paper**: state that DataLight reported the failure under the
configurations it tested, and present our K-sweep as the test of their stated hypothesis, not as a
refutation of their integrity. DataLight's own diagnosis — "offline datasets mixed with trajectories
from multiple agents may make DT agents confused" — is a hypothesis our per-intersection sequence
design directly tests.

**Non-negotiable baselines (offline-RL venue standard):** BC and %BC on the expert slice (if BC matches MADT, sequence modeling adds nothing — must be tested), IQL (independent per-intersection), CQL optional; plus online MAPPO/IPPO/DQN, MaxPressure, fixed-time, random; plus **domain-randomized MAPPO** for C2.

**Unified narrative:** *Offline MADT for TSC under two axes of distribution shift — scenario shift (C2) and dynamics shift (C3) — with the data-quality ladder (C1) as the foundation.* Enabled by the repo's **6** genuinely paired scenarios (4× hangzhou_1x1 + cologne1 + cologne3 have flow.json AND .rou.xml AND .net.xml). **CORRECTION 2026-07-27: grid4x4 is NOT paired** — its `.sumocfg` references a `grid4x4.rou.xml` that does not exist in the repo, and there is no `.net.xml`. C3 transfer therefore runs on hangzhou_1x1 and cologne1/3; grid4x4 stays a CityFlow-only coordination scenario unless someone generates its SUMO files.

**Closest prior art (cite prominently or die):** ⚠️ **arXiv:2112.02845 — Meng et al., "Offline Pre-trained Multi-Agent Decision Transformer", model named MADT, PEER-REVIEWED in *Machine Intelligence Research* (s11633-022-1383-7). Our exact method class — offline + multi-agent + decision transformer — on SMAC rather than TSC. ADDED 2026-08-12: it was absent from this list and from every project document, which is a citation gap a referee finds in one search, and it is where the name "MADT" actually comes from.** · arXiv:2602.02903 (Su, Sun & Deng, 2 Feb 2026, *"Spatiotemporal Decision Transformer for Traffic Coordination"* — **verified 2026-08-12: it names its own model MADT, in our exact domain**; architecture overlap, no transfer/ladder); DTLight 2312.07795 + DTRL datasets; DataLight 2303.10828 (negative DT result — our hook); X-Light (cross-city transfer); OffLight; TransformerLight; arXiv:2603.22315 (offline DT for emergency-vehicle corridors, Mar 2026 — overlaps C2's EV perturbation; ours is a robustness study, theirs an EV method); iLLM-TSC; CoLLMLight; LATS. Space is moving monthly — speed on P1–P5 matters.

---

## 2. Hardware & Compute Budget

- **GPU:** NVIDIA RTX 5080 (ample VRAM for DT training; 7–8B local LLM in 4-bit for the optional teacher extension)
- **RAM:** 64 GB — sized for large offline trajectory corpora held in memory during training
- **CPU:** high-end multi-core — the actual bottleneck is simulation; data collection parallelizes across CPU workers (one env per process)
- **Strategy:** simulators are CPU-bound → generate the offline corpus in parallel overnight; DT training is pure supervised learning → GPU-efficient, fast iteration. This asymmetry is *the reason* the offline approach fits our hardware.

---

## 3. Baseline Platform (rltraffic) — verified facts

Repo: `github.com/RusieckiFilip/rltraffic` — the June 2026 UW bachelor's thesis of Bibrowski, Bublik, Pisula & Woliński (sup. G. Grudziński), lineage RESCO → RESCO TensorCell → this framework. Verified by direct code inspection 2026-07-08; thesis PDF reviewed 2026-07-09. Thesis-documented facts: ~3× training speedup vs TensorCell baseline (libsumo in-process + metric caching; ~200k simulator calls/episode on cologne3, only ~2% advance the clock); backend qualitative consistency SUMO↔CityFlow (smoothed-reward corr. 0.93) but **absolute metrics not interchangeable** (CityFlow jumpier, occasional gridlock episodes); IDQN(CNN) most sample-efficient on grid scenarios (spatial inductive bias, Fig. 5.9 ablation); IPPO/MAPPO unconverged at 200 eps on hangzhou_4x4 (Fig. 5.4); thesis experiments use the **PressLight** reward. Our paper cites this as the platform paper; ours is the method paper.

- **Three simulator backends, one API:** CityFlow (vendored, patched for Python 3.12+), SUMO (traci/libsumo), MOSS (GPU). Agents/rewards/metrics/states are backend-neutral.
- **Agents present:** DQN, DQNAgentPFRL (RESCO parity), IPPO, MAPPO (centralized critic + running normalization). Baselines: MaxPressure, random. *(Corrected 2026-08-05: this line previously read "fixed-time via phase-control modes" — false. The phase-control modes supply timing **semantics** (clearance, min/max green), never a controller that emits a schedule. No fixed-time controller existed until P2.5 built one.)*
- **Safety semantics:** 4 phase-control modes (acyclic, bounded, cyclic, RESCO-cyclic); enforced yellow/all-red clearance, min/max green; env raises on illegal actions.
- **Scenarios available:** hangzhou_1x1 (×4 variants), hangzhou_4x4_gudang, hangzhou_4x4_hetero, grid4x4, manhattan_28x7, cologne1, cologne3, aigen_1x1, bb5b.
- **Experiment harness:** JSON-config `env × agent × seed` matrix; paired-seed eval; outputs `results.json`, `summary.csv`, plots.
- **Gap we fill:** no offline-RL data path exists — no trajectory logger, no offline dataset format, no sequence-model agent. That is our contribution surface.

### 3.1 P0.2 Baseline anchors (2026-07-09, 3 seeds, 60 train episodes, avg travel time in s)

| Scenario | MaxPressure | Random | MAPPO@60ep |
|---|---|---|---|
| cf_hz1x1 | **160.56 ± 0.00** | 307.53 ± 0.60 | 197.91 ± 1.78 |
| cf_grid4x4 | **141.65 ± 0.00** | 207.26 ± 1.93 | 632.95 ± 51.63 (gridlock, 1040 veh stuck) |

🚨🚨 **NOT SETTLED (2026-08-07) — EVERY MAPPO-vs-MaxPressure ORDERING IN THIS PLAN IS PROVISIONAL, AND
TWO OF THEM ARE INSIDE THE ERROR BAR OF THE METRIC ITSELF.** P2.6 measured, on one real corpus episode
using the new v1.1 field, `att_horizon = 178.90` against `att_per_step.mean() = 127.88` — **a 40 %
gap between the registered metric and the legacy one, on our own data.** Every MAPPO-vs-MaxPressure
comparison we have is in `att_running_mean` (`run.py` prints what the frozen runner computes). The
margins being defended:

| comparison | hz1x1 | grid4x4 | cologne3 |
|---|---|---|---|
| MAPPO@1000 vs MP (2026-08-06 row) | MAPPO +43.6 % | MAPPO **+2.5 %** | MAPPO **+5.8 %** |
| MAPPO@500 vs MP (2026-08-06 row) | MAPPO +40.6 % | MAPPO **−8.5 %** | MAPPO **−12.0 %** |

**The flip condition is computable and small.** On cf_grid4x4 the measured horizon/running-mean ratio
is MaxPressure **1.19**, Random 1.28, MAPPO@60 **2.33** (2026-08-06 row) — a spread of 1.96× *between
policies on one scenario*. MAPPO@1000 leads by 2.5 % there, so **the ordering reverses the moment
MAPPO@1000's ratio exceeds ~1.22**, which is barely above MaxPressure's own and far below MAPPO@60's.
cologne3 reverses above a 5.8 % ratio difference. **hz1x1's 43.6 % margin would require a ratio gap
larger than any we have measured, so it is the most likely to survive — but "most likely" is not
settled either.**

**Binding precondition: none of these cells may enter a figure, a table or a claim until recomputed in
`att_horizon`.** ⚠️ **UPDATE 2026-08-07 — the randomised-draw recomputation is DONE and both findings reproduce with their signs intact** (see the Decisions Log row of that date): MAPPO@500 gives MaxPressure +15.1 % on grid4x4 and +17.4 % on cologne3, against the legacy −8.5 % and −12.0 %. **The metric-artifact hypothesis is now substantially disfavoured.** These cells stay NOT SETTLED because they are nominal draw-0 by definition and the recomputation was on draws 1-200, but the outstanding run is now a confirmation rather than an open question. In particular the finding *"the ladder's expert tier is MAPPO on hz1x1 and MaxPressure
on the other two"* is **suspended**, not held — it is currently doing work in P4/P5 planning and it may
be a metric artifact.

**What is NOT affected, stated so nobody redoes sound work:** the **C1 ladder's normalised returns**
(hz1x1 0→155, grid4x4 −1416→113, cologne3 −29→114) are computed from the logged **reward**, not from
ATT, and §5 defines tiers by measured normalised return. That axis stands. By that registered
definition MAPPO@1000 is above MaxPressure on all three; the open question is whether **ATT**, the
registered *reporting* metric, agrees — and a disagreement between reward-ranking and ATT-ranking would
itself be a finding worth the paper.

**Recomputation is arithmetic, not a re-collection** — `att_per_step[-1]` per episode, once
`datasets_v11/` exists. ⚠️ **One trap to not walk into:** the corpus is draws **1–200** while the
figures above are **nominal (draw 0)** from `run.py`. Recomputing over the corpus answers the
*randomised-draw* question and does **not** settle the nominal one; conflating them would repeat the
A2 error of quoting numbers from a different pipeline. Report them as the two distinct quantities D4
already says they are.

🚨 **AGGREGATION WARNING (2026-08-05): every number in the table above is the EPISODE-MEAN OF A RUNNING AVERAGE, not average travel time at the episode horizon.** `experiments/runner.py:168,175` samples `info["average_travel_time"]` each step and reports `_mean()` of those samples. The registered primary metric is the **horizon** value (`PREREGISTRATION.md` amendment A1, tag `v0.2-prereg-a1`). Measured 2026-08-05: MaxPressure on cf_hz1x1 is **160.56 episode-mean vs 247.75 horizon** (ratio 1.54); Random **307.5346 vs 413.53** (ratio 1.345) — the distortion is **policy-dependent**, so it moves effect sizes, not just levels. *(Corrected 2026-08-07: this line carried **317.46 vs 429.67**, the non-canonical ad-hoc-rollout figures that amendment **A2** retired on 2026-08-06. A2 fixed the pre-registration and nobody fixed §3.1, so retired numbers survived in the one section §7 makes the sanity anchor for every later phase. Found by running the bidirectional-propagation rule below once over the existing amendments — the first thing it was pointed at.)* **These anchors must be re-derived under A1 (task P8.0) before any reported result is compared against them.** Until then treat the table as *running-mean ATT*, a legacy harness statistic.

⚠️ **Corrected 2026-08-03: `docs/p0_baseline_numbers.md` does not exist and never has** (0 commits have
ever touched that path — `git log --all`). This row cited it from 2026-07-09 onward. The numbers in the
table above are the surviving record; the full run output lives in
`output/experiments/p0_baselines/{results.json,summary.csv}`, which is **untracked and gitignored**, so
a fresh clone does not have it and these sanity anchors are currently unreproducible from the repo
alone. Committing that output is folded into **P0.6**. Two standing observations:
1. ⚠️ **CityFlow demand is deterministic on hz1x1 and grid4x4 ONLY** (MaxPressure σ=0 across engine seeds; **corrected 2026-08-06 — cf_cologne3 measures σ=3.32 over 5 seeds, mechanism unidentified; QUANTIFIED 2026-08-07 — it reaches bit-level trajectory divergence on 7.7 % of draws (3/39) under a fully deterministic MaxPressure policy, identical code, two runs one day apart, against a grid4x4 control of 0/6. Tier-level means are nevertheless stable to 0.080 %**) → seed-CIs capture policy stochasticity only; environment stochasticity must come from P6 perturbations / flow randomization. Affects P8 statistics design.
2. **MAPPO@60ep < random on grid4x4** → keep as motivating evidence (online MARL sample-inefficiency at 16 intersections); corroborated independently by thesis Fig. 5.4 (IPPO/MAPPO unconverged at 200 eps on hangzhou_4x4). Reproduce as a "performance vs. interaction budget" motivation figure. P2.1 convergence budget: ≥500 episodes on grid4x4, with explicit convergence verification.

---

## 4. Interface Contracts (FROZEN — paste into every sub-chat brief)

### 4.1 Environment API (non-standard Gym — do not "fix" it)
```python
info = env.reset(seed=42)                                (# returns info ONLY, no obs)
reward, terminated, truncated, info = env.step(action)   # reward FIRST
```
`action` is an `np.ndarray`, one action per intersection, **ordered by `[ix.id for ix in env.intersections]`**.

### 4.2 The `info` dict contract
```python
info = {
    "sim_time": float, "vehicle_count": int, "step": int,
    "average_travel_time": float,
    "lane_vehicle_count": {lane_id: int}, "lane_waiting_vehicle_count": {lane_id: int},
    "metrics": {metric_name: float},                # requested global metrics
    "intersections": {
        ix_id: {
            "state": [...],                          # per-intersection obs vector
            "avail_actions": [0, 2],                 # legal actions NOW (masking!)
            "current_phase": int, "time_in_phase": int,
            "action_applied": bool,
            "metrics": {name: float},
            "reward": float,                         # ONLY if local_reward_fn set
        }, ...
    },
}
```

### 4.3 Agent API (mirror `agent/MAPPOAgent.py`)
```python
class MyAgent(BaseAgent):
    def __init__(self, gym_env, ..., device=None, seed=None)   # reads env.intersections
    def act(self, info, explore=True, update_memory=True) -> np.ndarray
    def observe(self, next_info, reward, terminated, truncated=False) -> dict
    def save(self, path); def load(self, path)
```
Helper functions in `agent/utils/utils.py` (USE THESE, do not reimplement):
`Utils.infer_action_counts(env.action_space, intersections)`, `Utils.extract_per_intersection_info(info, ids)`, `Utils.state_from_info(ix_payload)`, `Utils.extract_valid_actions(ix_payload, n_actions)`, `Utils.scalar_reward(reward)`, `Utils.reward_for_intersection(...)`, `Utils.resolve_device(device)`, `Utils.seed_everything(seed)`.

### 4.4 Coding standards
Python ≥3.12, PyTorch, type hints (`from __future__ import annotations`), numpy `float32` for stored arrays, docstrings in the repo's style, no new heavy deps without a Decisions-Log entry, `pytest` tests for everything, no modifications to `envs/base_traffic_env.py` unless a task explicitly says so.

---

## 5. Offline Data Design (agreed v0.1 — refine in P1/P3)

- **Format:** one `.npz` (compressed) per episode + one `manifest.json` per collection run.
- **Per step, per intersection:** `state (float32)`, `action (int64)`, `avail_actions mask (bool, width = n_actions)`, `local_reward (float32, NaN if unset)`, `current_phase`, `time_in_phase`.
- **Per step, global:** `global_reward`, `vehicle_count`, `sim_time`, selected `metrics`.
- **Episode metadata:** scenario id, backend, engine seed, `delta_time`, `max_steps`, `state_features` list, reward fn names, phase-control mode, behavior-policy id + checkpoint hash, repo git hash, timestamp.
- **Dataset ladder (C1's experimental axis; released publicly as a contribution, positioned vs DTRL):** tiers are defined by **measured normalized return** (random = 0, MaxPressure = 100, computed per scenario post-collection), NOT by policy names — our own P0.2 data shows MaxPressure > MAPPO@60ep, so policy names imply a false quality ordering (critique 2026-07-11b). Source policies (= dataset provenance metadata): random, fixed-time, MaxPressure, MAPPO checkpoints (60/200/converged), ε-noised MAPPO (ε ∈ {0.1, 0.3}), DQN(CNN), mixed. The paper's ladder table reports the measured return distribution of each dataset. If no learned policy beats MaxPressure after convergence, the top slice IS MaxPressure — which sharpens, not weakens, the "does MADT exceed its data" question.
- **Demand randomization is mandatory for every collected episode (P2.0):** with deterministic CityFlow flows, N episodes of a deterministic policy = 1 trajectory × N; effective corpus size of deterministic tiers ≈ 0 (critique 2026-07-11a). Each episode = (scenario, flow-draw seed); nominal flow = draw 0. Randomizer operates at the vehicle-list level (verified: flow.json = per-vehicle insertions with routes) so draws render to BOTH backends (flow.json ↔ .rou.xml) — preserves C3's paired scenarios and fixes P8's environment-stochasticity problem at the source. Logger convention: flow-draw id goes into run_metadata (no logger code change needed).

---

## 6. Task Checklist

> **The checklist IS the state. Answering the question directly, because it was asked after
> reading it and believing it (2026-08-07):** §8 is *why* a thing was decided, §10 is *what is in
> flight today*, and **§6 is what is done and what is not.** A checkbox is inherently a state
> claim — there is no way to write `[ ]` that means "look elsewhere" — so it is maintained, not
> annotated with a disclaimer. **A stale checklist is a false claim in the most-read part of this
> document.**
>
> **Binding rule: the merge commit that lands a task ticks its boxes IN THE SAME COMMIT.** A
> follow-up step is a step that gets skipped; that is exactly how P3.1–P3.4 sat unticked while
> `offline/dataset.py` was merged, reviewed and on `main`.
>
> **Mechanical drift check — every Return Packet on `main` must have a ticked box:**
> ```bash
> for f in $(git ls-tree --name-only main docs/returns/ | grep -v TEMPLATE); do
>   id=$(basename "$f" .md); base=${id%%-*}
>   git show main:docs/PROJECT_PLAN.md | grep -qE "^- \[x\] \*\*${base}" \
>     || echo "DRIFT: $id has a Return Packet and no ticked box"
> done
> ```
> It found three violations on 2026-08-07 (P2.2-draws, P2.6, P3) and is the check to run before
> trusting this section. Automating it as a test is `DEFERRED.md` row 26.
>
> ⚠️ **WHAT THIS CHECK CANNOT SEE, so nobody reads a green invariant as a verified plan.** It
> catches *"merged but unticked"*. It cannot catch **"ticked but the artifact does not do what the
> item says"** — which is the **P0.3 mis-tick**, ticked from a commit message that actually
> recorded a *failure*, and it survived **weeks**. That failure mode is unreachable by any
> structural check, because the box and the Return Packet both exist and both look right; only
> reading the code catches it. **A green invariant means the bookkeeping agrees, never that the
> work is what it claims to be.**

### Phase P0 — Platform verification  ✅ CLOSED 2026-08-03
- [x] Clone + inspect repo structure, docs, agent/env/reward/state contracts *(Master chat, 2026-07-08)*
- [x] **P0.0** Dev environment: WSL2 Ubuntu 24.04 + torch cu128 (capability (12,0) confirmed) + CityFlow build + SUMO + VS Code WSL Remote *(Return Packet 2026-07-09)*
- [x] **P0.1** Smoke test: PASS *(2026-07-09)*
- [x] **P0.2** Baseline numbers recorded (see §3.1) → `docs/p0_baseline_numbers.md` *(2026-07-09; run sequentially due to fork deadlock)*
- [x] **P0.3** ✅ 2026-07-27 diagnosis (`docs/notes/P0.3_spawn_attempt.md`, `38b3d4a`), **PARTIALLY CORRECTED 2026-08-03**. We concluded "never a deadlock, just oversubscription; not reproducible; cause unknown, most likely environmental." **That was wrong.** The P0.3-fix reviewer produced real hangs on demand (`exit=124`, killed at 120 s and 150 s) and isolated the mechanism: `fork()` + libgomp — a child entering an OpenMP parallel region with `nthreads > 1` waits forever on team threads `fork()` never duplicated. Our own benchmark's `workers=6 unpinned ≥1200 s / cap` was almost certainly that hang, which we recorded as "slow." The July report was right about the symptom; our refutation over-reached from "we did not reproduce it in 20 min" to "it does not happen." What WAS correctly refuted: the spawn patch as the fix, and all four stated mechanisms. Real fix = P0.3-fix
- [x] **P0.3-fix** ✅ DONE 2026-08-03, merged (`64800fb`, one +45-line authorised delta in `experiments/runner.py`; 249 tests). **THE PIN IS A LIVENESS FIX, NOT A PERFORMANCE FIX** — the reviewer's inert-mutant experiment shows the fork+libgomp hang IS reachable against unpinned code (`exit=124` at 120 s / 150 s) and unreachable against the committed code (14 passed, 10/10 runs). `torch.set_num_threads(1)` before the first parallel region makes the child run serially in the master thread, so there is nothing to wait on. Speed (199.2 s → 50.2 s, ≈3.97× at workers=6, measured in one session from a clean shell) is a **side effect**. Evidence: `docs/reviews/P0.3-fix.md`, `docs/returns/P0.3-fix.md`
- [x] **P0.5** ✅ **DONE 2026-08-04** (merged `e01b0d1`, Authorisation B, comments/docstrings only — proven by an AST-identity check with a live mutation control, recomputed independently by the coordinator). Original item: ⚡ **Document the liveness role in `experiments/runner.py`** — reviewer finding N1, highest-value item on the list. Nothing on disk says the pin prevents an unbounded hang; docstring, plan and packet all justified it on speed alone. A maintainer could reasonably make it conditional on `workers > 1` and silently reintroduce a hang with no failure message (`run_matrix` uses `as_completed` + `future.result()` with no timeout). **Needs a NEW frozen-file authorisation — the P0.3-fix authorisation covered only "limiting per-worker torch thread counts" and is spent**
- [x] **P0.6** ✅ **DONE 2026-08-04** (merged `e01b0d1`). `experiments/configs/p0_threading_bench.json` committed and run: **182.9 s → 48.3 s, ≈3.79× at workers=6**, measured in one session from a clean shell. **Deliberately NOT reconciled** with the recorded 199.2/50.2 — different machine state is expected, and quietly adopting the older pair would recreate the cross-session error the ban exists for. §3.1 anchor data now tracked at `docs/data/p0_baselines/` with `docs/p0_baseline_numbers.md` and a double-compute test that passed first run. Original item: Commit the benchmark config to `experiments/configs/` — reviewer N5. The 199.2 s / 50.2 s headline numbers rest on a config that lives only in a session scratchpad. A repo whose output is a paper cannot have its timing evidence be unreproducible. No authorisation needed (a *new* config is explicitly permitted). **Extended 2026-08-03:** also commit the **P0.2 baseline run output** (`output/experiments/p0_baselines/results.json` + `summary.csv`) under a tracked path. §7 makes §3.1 the sanity anchor every later phase is compared against, and its backing data is currently gitignored — an anchor that a fresh clone cannot reproduce is not an anchor. Same defect class as N5, found the same way
- [x] **P0.7** ✅ **DONE 2026-08-04** (merged `e01b0d1`). N7 bound by running `run_matrix` in a forked child with a queue timeout + `terminate()` — **not** `signal.alarm`, which cannot bound it: the alarm unwinds through `ProcessPoolExecutor.__exit__` into `shutdown(wait=True)` and re-hangs on the wedged workers. N8 and N3 closed; retired ratios swept. Original item: Test hardening in `tests/test_runner_threading.py` (not frozen, ~20 lines) — N7: T2 is an unbounded wait by construction, bound it with `signal.alarm` or a child-with-join-timeout; N8: T2 and T3 do not assert the parent-forcing took, so they are one torch behaviour change away from being vacuous. Note T4 is near-theatre (asserts a constant and a non-empty docstring) — do not count it as coverage
- [x] **P0.4** ✅ **DONE 2026-08-03.** `PREREGISTRATION.md` written in full, committed and tagged `v0.1-prereg` (annotated; carries the file's sha256, since a file cannot contain its own hash). Registered while the repo contains **no corpus and no trained model** — verified, not assumed. It is deliberately more than a restatement of this plan: it closes four researcher-degree-of-freedom gaps that existed nowhere else (§5 train/eval draw split, §6 no-online-model-selection + frozen-tuning rules, §3.1 primary metric pinned to an exact registry name, §8 multiplicity + no-optional-stopping), and §10 registers what we publish under **every** outcome including MADT collapsing on grid4x4. ⚠️ **One human action remains: push the tag.** A local tag is not a third-party timestamp — the push event is the only externally checkable act, and §0 of the document says so rather than overclaiming
- [x] **P0.8** ✅ **DONE 2026-08-04** (merged `e01b0d1`, Authorisation A). **The guard has tests for the first time in its life** — `tests/test_claude_guard.py`, 13 rows against the real script in a hermetic scratch repo. 3 rows failed pre-patch, 13 passed post-patch. Original item: Fix guard defect `G1` (found 2026-08-03 by on-repo falsification, `docs/notes/D3_falsification.md`): `git status --porcelain` collapses a wholly-untracked directory to one entry, so `experiments/newpkg/foo.py` is reported as `experiments/newpkg/` and slips past `FROZEN_PATTERNS` — whose `experiments/.*\.py$` clause is the only one anchored on a file extension instead of a directory prefix. Unreachable via Edit/Write (permission layer denies those); **reachable via a Bash heredoc, which is the exact route the guard exists to cover.** Fix drafted in the note (prefix clause + `experiments/configs/` moved into `FROZEN_EXCEPTIONS`); verified first that runtime artifacts land in `output/experiments/` (`config.py:355`) so a prefix rule cannot misfire. Ships as a patch under `docs/patches/` — `scripts/` is frozen and a session must not apply it

- [ ] **P0.9** Fix the `check_english.sh` o-acute false positive. It now fires on **every** edit to `scripts/claude_guard.sh` — the guard's own comment *about* the false positive contains the character, so the check blocks the file that documents it, and it echoes again into any patch file carrying that line as context. Cost is recurring and will be paid on every future guard change. **Design ruled 2026-08-04:** treat a single character delimited by backticks as a code literal and blank it before matching (same mechanism as `ALLOWED_NAMES`), rather than removing o-acute from the class (it is common in genuine Polish prose) or excluding the guard wholesale (that would stop checking a real file). `check_english.sh` is a guard-level `FROZEN_EXCEPTIONS` entry but is **denied at permission level**, so this ships as a patch under `docs/patches/`

### Phase P1 — Trajectory Logger  ✅ MERGED to main 2026-07-27 (`00c9d42`)
- [x] **P1.1** `offline/trajectory_logger.py`: episode recorder wrapping the act/step/observe loop (wrapper/callback pattern, zero env modifications)
- [x] **P1.2** `offline/collect.py`: CLI — behavior policy × scenario × episodes × seeds → dataset dir
- [x] **P1.3** `tests/test_trajectory_logger.py`: shape/roundtrip/determinism tests
- [x] **P1.4** Return Packet reviewed in Master chat; plan updated
- [x] **P1.4b** ✅ **DONE 2026-08-06**, folded into `BRIEF_09` §2 and verified there on main: key by intersection ID (never index), `local_reward` is composite, episode ends are truncations not terminals. All three are carried in the merged loader
- [x] **P1.5** Independent review sub-chat (critical path): diff + brief + contracts → discrepancy report; merge `task/p1-logger` only after pass

### Phase P2 — Offline corpus generation
- [x] **P2.0** ✅ DONE 2026-07-31, merged after 3 review rounds (245 tests, 13 commits). **11 of 13 sim configs randomisable** (refused: pb_compare + aigen_1x1, genuinely aggregate — correct refusal). Large maps unblocked: hangzhou_4x4 (16 ix), hangzhou_4x4_hetero, manhattan_28x7. ⚠️ **For P3/P4:** `draw(0)` preserves source order (byte identity demands it) while `draw(k>0)` sorts globally, so on cityflow1x3 (7 `startTime` inversions) and hangzhou_4x4 (8, verified) the nominal control differs from randomised draws in **ordering as well as demand**. Pinned by a test (`task/p2-flow-randomizer`, 220 tests green, 30 P1 tests untouched). Acceptance met on a real engine: nominal 1/3 distinct `episode_sha256`, with draws 3/6; draw 0 byte-identical to source end-to-end. `render_sumo` shipped REAL (edge-id mapping is the identity: hangzhou 8/8, cologne1 10/10, cologne3 48/48). ~~`offline/flow_randomizer.py`~~ (BEFORE any collection): seeded demand draws — departure-time jitter, Bernoulli thinning, volume scaling — at the vehicle-list level, rendering to both flow.json and .rou.xml; nominal = draw 0; unit tests incl. dual-backend render equivalence *(Sub-chat Brief #2a)*
- [x] **P2.0b** ✅ Compute budget computed 2026-07-27 from measured P0.2 timings + a live 7-scenario peak-load run. **Result (CORRECTED 2026-07-27): compute IS a real constraint, because parallelism does not work.** Corpus collection (9 policies × 200 eps × all 7 scenarios) = **4.6 h sequential**. (~~0.6 h on 8 workers~~ **withdrawn 2026-08-03: that was 4.6 h ÷ the never-measured 8×.** Worse, `collect.py` parallelises via N shell processes, *not* through `runner.py`'s pool, so the measured ≈3.97× does not transfer to it either. **No parallel figure for collection has ever been measured. Budget collection at 4.6 h sequential** until someone measures the shell-process path.) Online baselines (4 algs × 5 seeds × 3 headline scenarios) = 15 h @500 eps, 60 h @2000 eps sequential → **measured**: 15–60 h sequential; with P0.3-fix thread-pinning → **~4–15 h**. Worst case ×3: 194 h sequential; **at the measured ≈3.97× ≈ 48 h** (CORRECTED 2026-08-03 — the earlier 5.80×/≈33 h came from cross-session arithmetic; only 199.2 s and 50.2 s, measured together, are trustworthy). **Plan P5 at ~4×, never 5.8× and never 8×.** Measured s/episode (rollout, 360 steps, thread_num=1): hz1x1 0.59–0.83 · cologne1 0.61 · cologne3 0.97 · grid4x4 1.22. Training s/episode: hz1x1 1.04 · grid4x4 2.81
- [x] **P2.0c** ✅ Scope frozen 2026-07-27, **cost re-derived 2026-08-03**: **collect on all 7 CityFlow scenarios**. (The original justification "costs 0.6 h parallel" rested on the fabricated 8×; the honest figure is **4.6 h sequential, parallel unmeasured**. **The decision does not move** — it now rests on the sequential number, and 4.6 h is cheap enough that restricting scope would buy nothing, while a richer corpus strengthens the public dataset contribution.) **Headline online-baseline suite on 3:** hz1x1#1 bc-tyc (single-intersection, heaviest 1×1 at 212 peak, P4 validation gate) + grid4x4 (16 signals, coordination) + cologne3 (3 signals, real network, RESCO-comparable, heaviest load at 536 peak). Appendix: hz1x1 #2/#3/#4 + cologne1. Manhattan 28×7 stays out (P11.3)
- [x] **P2.1** ✅ **DONE 2026-08-06.** 60 checkpoints on disk at budgets 60/200/500/1000 × 5 seeds × 3 scenarios; prefix-equivalence measured, so three budgets cost one run. The registered convergence criterion **failed at B=1000 and was NOT weakened** — the tier is labelled *MAPPO@1000*, "the best budget we ran", because §5 defines tiers by measured return and never by a convergence claim. MAPPO@1000 exceeds MaxPressure on all three scenarios in `att_horizon` (`docs/data/att_ladder_v11.json`). Original item: Train MAPPO to convergence on headline scenarios (≥500 eps grid4x4, convergence verified); save checkpoints at 60/200/converged; verify whether converged MAPPO actually exceeds MaxPressure (determines the top ladder slice)
- [x] **P2.2** ✅ **DONE 2026-08-07.** Two full campaigns collected: `datasets/` (v1.0) and `datasets_v11/` (v1.1), **4800 episodes each**, 7 tiers × 3 headline scenarios, 200 distinct draws per tier, `base_seed=1000`. The v1.1 re-collection passed its validation gate (44 tiers bit-identical, 22 cologne3 tiers inside the measured envelope, 3 fixedtime exempt). Draw materialisation shipped separately as **P2.2-draws** (`docs/returns/P2.2-draws.md`). ⚠️ **SIZE BY DRAWS, NOT EPISODES — except cf_cologne3, where repeated episodes of a deterministic policy ARE distinct samples (7.7 % of draws diverge under identical code; measured 2026-08-07 with a grid4x4 control of 0/6).** With demand fixed, the engine seed changes nothing observable, so `--episodes 20 --flow-draw 0` is one trajectory twenty times. Rule: **deterministic policies (maxpressure, fixedtime, greedy mappo, dqn) → 1 episode per draw**; **stochastic policies (random, mappo_eps) → multiple episodes per draw are legitimate** (the policy RNG advances across episodes within a run). Budget is unchanged (N distinct trajectories still costs N episodes), only the CLI invocation changes. P2.4's linter should enforce this by flagging duplicate `episode_sha256` within a run. Collection campaign: source policies × headline scenarios × episodes-with-flow-draws × 5 seeds (parallel CPU workers, spawn ctx)
- [x] **P2.5** ✅ **DONE 2026-08-05**, merged `3af41e4` (299 tests, +29). Ships `k=4` on the **structural** criterion — clearance overhead 12.50% vs the shipped plan's 14.29%, against k=3's 16.67% — which is aggregation-independent and computable before any simulation. **The brief's own ATT-closeness tiebreaker was rejected by the coordinator as outcome-driven configuration**, the exact researcher degree of freedom D7 registers against. Uses the shipped `signal_plan_template.txt` where present. **grid4x4 ladder inversion recorded as a finding, not a defect:** uncoordinated zero-offset fixed-time measures *worse* than random (232.23 vs 207.26, running-mean ATT); per-intersection offsets declined as a new degree of freedom, and tiers are defined by **measured** return precisely so provenance names need not order as intuition suggests. Original item: `offline/policies/fixed_time.py`: fixed-time controller (NOT in repo — verified 2026-07-25, zero matches for `fixed.time|FixedTime`). Needs its own design decisions: cycle length, phase split (equal vs scenario-tuned), interaction with phase-control clearance semantics. Registers into `collect.py`'s policy registry. Ladder Tier 1 depends on it
- [ ] **P2.4** ⚠️ **Three requirements now BINDING on this brief, each recorded elsewhere and easy to lose:** the alignment check must be the **paired** form, never the exact identity (which has 1.8× power on cologne3); the exact identity to assert instead is `Σⱼ local_reward[t] == −Σ lane_waiting[t+1]`, **hz1x1 and grid4x4 only**; and the duplicate-`episode_sha256` detector **must exempt cf_cologne3 by name** (7.7 % of its draws diverge under identical code, so non-duplication is not evidence of distinct draws). See `DEFERRED.md` rows 20 and 25. Original item: `offline/corpus_lint.py`: post-collection validator — manifests, shapes, NaNs, logged-vs-recomputed reward equality, return distributions, episode-hash duplicate detector *(critical path → gets review sub-chat)*
- [ ] **P2.3** Dataset stats notebook: measured normalized-return distribution per dataset (defines tier labels), state coverage, corpus size; **scenario license audit** (Cologne CC terms, Hangzhou/CityFlow provenance) — redistribution rights must be confirmed before the public-release claim stands; **reward-metric correlation latch** (Spearman of queue- vs pressure-returns against avg travel time; confirms or reverses the queue_length decision before P4)

- [x] **P2.6** ⚠️ **DONE 2026-08-07, merged `3c9c21d` — BUT MERGED WITHOUT THE MANDATED INDEPENDENT REVIEW.** §7 names the **trajectory logger** in its critical-path list and P2.6 modified it; it also touched a frozen file under authorisation and produced the corpus P4 trains on. **Retro-review DONE 2026-08-08** — `docs/reviews/P2.6.md`, PASS-WITH-NOTES: the ladder's arithmetic verified clean by independent recomputation (0/147 mismatches, byte-identical regeneration) and all 60 migrated checkpoints verified against their backup, but **one comparison the plan drew from it did not survive a paired analysis (D1)** and **seed is confounded with demand block (D16)**. Test-strength defects parked as `DEFERRED` 27. The clause stays so the tick never reads as a gate that was passed at the time. Corpus **format v1.1** (`att_per_step`, a T+1 observation row, named so that `.mean()` cannot masquerade as the registered metric) + **contract C8 made mechanical** in `agent/MAPPOAgent.py` under AUTHORISATION C (human-applied, content verified twice independently) + 60 checkpoints migrated with a verified backup + the re-collection campaign, its validation gate, the format linter and the `att_horizon` ladder emitter. *(Added to this checklist 2026-08-07: it had a brief, a branch, a Return Packet and a merge, and **no checklist entry at all** — found by the sweep below.)*

### Phase P3 — Offline dataset loader  ✅ MERGED to main 2026-08-07 (`3f9721f`)
- [x] **P3.1** ✅ `offline/dataset.py`: PyTorch `Dataset` — episode files → per-intersection sequences with **returns-to-go**, context-window slicing (K steps), padding + attention masks
- [x] **P3.2** ✅ Normalization stats computed from corpus (frozen at train time) — **and fitted on the TRAINING split only**, recording the draw ids they were fitted over, per the D3 ruling; statistics fitted on held-out data are leakage in its quietest form
- [x] **P3.3** ✅ Loader unit tests incl. RTG correctness verified by an INDEPENDENT computation path (`np.cumsum` on raw arrays) against the loader's own — double-computation rule
- [x] **P3.4** ✅ **DONE 2026-08-07** — `docs/reviews/P3.md`, PASS-WITH-FIXES: 3 blockers from the reviewer, 2 more promoted by the coordinator, all five fixed with mutation proofs and the three original blockers **re-falsified by the coordinator** before merge

### Phase P4 — Single-intersection Decision Transformer (validation gate)  ✅ **GATE PASSED 2026-08-08**
- [x] **P4.1** ✅ `agent/DTAgent.py`: causal GPT-style DT over (RTG, state, action) tokens; action masking from `avail_actions`; `BaseAgent`-compatible `act()`
- [x] **P4.2** ✅ **PASS 2026-08-08**, on all 100 registered held-out draws `1000-1099`, every arm paired on the identical draw set. `ATT_MADT = 104.9558` against MaxPressure **176.8912** and a threshold of **110.8611** (= 1.05 × MAPPO@1000's 105.5820). Wilcoxon paired over 100 draws: vs MaxPressure and vs MAPPO@500 **w₊ = 0, i.e. the DT wins on 100/100 draws** (p = 3.96e-18); vs MAPPO@1000 p = 6.34e-07, z = −4.98. **The DT slightly exceeds the policy whose data it trained on** — 0.6 %, which is C1's question answered positively and is P4.4's to attribute. Reported at **40,000** steps against a declared 20,000 plus the one pre-declared raise; **the plateau criterion failed 0/5 at both**, so the gate passed with an under-trained model. Independent review `docs/reviews/P4.md`: PASS-WITH-NOTES, five MAJOR defects fixed, **no reported number changed** (both artifacts regenerated and diffed bit-identical). ⚠️ **QUALIFIED 2026-08-12 (see the Decisions Log row of that date): every behaviour-policy comparison on this line is against a SEED-AVERAGE.** MAPPO@1000's per-seed held-out means are `103.6087 / 103.5286 / 107.7980 / 105.9976 / 106.9773`, so the DT's 104.9558 is ahead of the **5-seed mixture** whose pooled data it trained on, but **1.4272 ATT behind the best single checkpoint**, and **2 of the 5 beat it** (MAPPO@500's seed 101, 104.6655, also does). **The gate passes under both readings** — strict threshold `1.05 × 103.5286 = 108.7050` — so the ✅ stands; the *sentence* is what needed the qualifier. Original item: Train on hangzhou_1x1 corpus; **GATE (re-anchored, critique 2026-07-11c):** DT-offline ≥ MaxPressure **AND** within 5% of the best available online policy on avg-travel-time (5 seeds, CIs) — being within 5% of a weak MAPPO while losing to a 1970s heuristic is not a pass → if failed, diagnose before proceeding
- [ ] **P4.3** RTG-conditioning sweep + **probe-calibrated return prompting** v1 (MaxPressure-relative / quantile-space targets) — the named method component; ablated here first on 1×1
- [x] **P4.4** ✅ **DONE 2026-08-12**, merged after an independent review (`docs/reviews/P4.4.md`, **PASS-WITH-NOTES**) and a mandated fix round (`BRIEF_12`). On the `mappo1000` tier of `cf_hz1x1`, 100 registered held-out draws, 5 seeds, 40,000 steps, one evaluation function: **BC 105.1635 — *matches* the DT** (paired −0.2077, CI [−0.4690, +0.0536] wholly inside ±δ, **recovered fraction 0.668**); **%BC 103.1627** and **IQL 103.4764** both **BEAT it decisively**, landing in A6's unregistered fourth branch (p = 5.1e−17 and 7.1e−15; recovered fractions 3.863 and 3.362) against MADT's 104.9558. **Registered forecasts: primary HELD, secondary FAILED.** The reviewer retrained 3 of 15 checkpoints, re-rolled 16 cells, rebuilt the 72,000-row transition table by an independent route and regenerated the artifact byte-identically — **no number moved**; I recomputed every headline figure myself from the raw records (≤1e−11) and ran the suite first-hand (**612 passed, 1 skipped**). Fix round: **14 tests, 11 mutations executed**, including the `gamma=0.0`-at-the-call-site mutation that had survived all 58 tests. **The mechanism is the result:** the top-10 % filter performed **checkpoint selection** — `{202:10, 101:9, 505:1, 303:0, 404:0}` of the top 20, exact p = 0.007225300, training return predicting held-out ATT at **r = −0.991 over disjoint draw sets** — so %BC's edge is at least partly *"clone the best 2 of 5 behaviour policies"*, which **sharpens C1 at checkpoint granularity**. Decisive test registered as **P4.5**, not run here. ⚠️ **Constraint on the paper (`docs/reviews/P4.4.md` §8.6): no DT-versus-baseline sentence enters it until P4.3 has run** — the DT is prompted at a target P4's own review showed is not its best. Original item: Offline baselines on the same corpus: **BC, %BC (top-10% return filter), IQL** (independent per intersection; CQL optional) — non-negotiable comparators; if BC-on-expert matches MADT, report honestly and pivot the story to the ladder/shift findings
- [ ] **P4.5** ⭐ **NEW 2026-08-12 · BRIEF ISSUED (`BRIEF_13`) — the decisive test of %BC's mechanism.** ⚠️ **The obvious arm is CONFOUNDED and the brief refuses it:** %BC trains on **20** streams while seeds 101+202 hold **80**, so "BC on the two best seeds" differs in *which seeds* **and** *how much data*, and either outcome would be unattributable. **Four arms with size held fixed** — `bc_top10` (20, top-return, measured 103.1627), **`bc_best2_20`** (20, random from seeds 101+202), **`bc_any_20`** (20, random from all five), `bc_best2_all` (80, secondary). **The decisive comparison is `bc_best2_20` vs `bc_any_20`: identical training-set size, differing only in which checkpoints produced the data.** Subset draws come from the training seed's RNG so the five-seed spread averages over five subsets. Registered prediction, in the plan file before the first gradient step: `bc_best2_20` within δ of `bc_top10`, and `bc_any_20` worse by ≈ the 2.05 ATT behaviour gap. **If it fails, F3's reading is incomplete and I correct three documents** — the trade this apparatus exists to make. Original item: **train BC on MAPPO seeds 101+202 ONLY (the two strongest checkpoints) and compare against %BC.** P4.4 established that the top-10 % return filter selected **19 of 20 streams from those two seeds**, that per-seed training return predicts per-seed held-out ATT at **r = −0.991 (n = 5, disjoint draw sets)**, and that demand is excluded as the cause (equal arrivals 443.6 vs 446.1; only 4/20 overlap with MaxPressure's difficulty ranking). **The filter is doing CHECKPOINT selection, and right now we can only deduce that — this measures it.** If BC-on-best-two ≈ %BC (103.16), %BC's advantage over BC is checkpoint selection; if %BC still wins, a within-policy episode-quality effect exists on top. **Either outcome is publishable and the hypothesis is registered here BEFORE the run** (A8(a)'s discipline). Cheap: the machinery exists — `filter_stacked_to_streams` already filters to an arbitrary stream set, so this is a selection predicate plus tests, ~550 s training × 5 seeds and ~9 min evaluation, **no new corpus and no new rollout protocol**. **Gate:** report paired against %BC on the same 100 held-out draws with the same δ machinery. **Why it is critical path and not P11:** the paper now states a mechanism for the arm that beats our headline method, and *"we inferred it from a correlation with n = 5"* is a materially weaker sentence than *"we trained it and it reproduced"*.
- [ ] **P4.6** ⭐ **NEW 2026-08-12 — C1's actual experiment: the METHOD × TIER grid.** BC, %BC, IQL and the DT across **random / fixedtime / maxpressure / mappo500 / mappo1000**. P4.4 filled **one column of twenty**; C1 claims a data-quality ladder and currently has comparators at a single rung. `evaluate_arm` already takes arbitrary arms, so this is a config loop: measured costs give ≈1.2 h training + ≈35 min evaluation **per tier**, so ≈9 h total, no new corpus. ⚠️ **Two design constraints, both registered here before it runs.** **(a) OUR LADDER CONFOUNDS QUALITY WITH STATE COVERAGE** — `mappo1000` is high-quality and narrow, `random` is low-quality and broad — whereas a mixture corpus varies quality at roughly constant coverage. A ranking that moves along our ladder therefore cannot be attributed to *quality* alone, and no claim may do so. **(b) The fix is nearly free and we already own the data:** mixture tiers (e.g. 25/50/75 % expert) are a **resampling of the corpus we have collected**, not a new campaign, so the grid can carry both axes — policy-identity *and* expert-fraction — for one config loop. The second axis is what makes us directly comparable to OffLight's design. **Gate:** per-tier δ must be re-derived (A6's δ is `mappo1000`-specific) and the derivation declared before any number. **Sequencing: after P4.3**, because the DT arm must use the calibrated prompt or the grid measures the un-calibrated DT five times instead of once. ⭐ **THE GRID'S REAL TARGET, registered 2026-08-12 (the user's framing, sharpened): %BC is THREE DIFFERENT MECHANISMS under one method name, and the grid should be built to separate them, not to draw a performance curve.** `pure expert → the filter selects the best CHECKPOINTS` (**measured**: F3, `{202:10, 101:9, 505:1, 303:0, 404:0}`, r = −0.991); `mixture → it selects the EXPERT FRACTION` (classic filtered-BC, and the only regime OffLight's data speaks to); `pure random → it selects the EASIEST DEMAND DRAWS and the luckiest rollouts`, which should not transfer. **The distinguishing feature is not quality-versus-coverage but that a mixture always contains some expert fraction while `random` contains none.** ✅ **And the instrument to separate them already exists and costs no training:** the per-tier version of the two checks that settled F3 — arrival volume of the kept episodes against the rest, and overlap of the kept set with MaxPressure's own difficulty ranking, MaxPressure being deterministic and therefore a policy-independent difficulty probe. **Report, for every tier, WHAT the top-decile filter selected.** Three mechanisms named and measured under one method label is a stronger result than a curve, and it is not available to anyone who reports only the curve.

### Phase P5 — Multi-Agent DT (the paper's core model)
- [ ] **P5.1** Spatial mixing layer across intersections (graph attention over road-network adjacency from `RoadnetInfo`) interleaved with temporal causal attention
- [ ] **P5.2** Train + evaluate on grid4x4, hangzhou_4x4 **per ladder tier**; compare vs online MAPPO/IPPO/DQN, MaxPressure, fixed-time, random, **and offline BC/%BC/IQL**
- [ ] **P5.3** Ablations: no-spatial-mixing, no-RTG, context-length K

### Phase P6 — OOD / robustness suite (claim C2)
- [ ] **P6.1** Scenario perturbation tools: demand surges, lane/approach closures, sensor dropout (state masking), emergency-vehicle flows (cite/differentiate arXiv:2603.22315)
- [ ] **P6.2** Train the **domain-randomized MAPPO** baseline (same shift-augmented scenario distribution the MADT sees) — removes the data/architecture confound
- [ ] **P6.3** The **2×2 benchmark**: {nominal, shift-augmented} training data × {MADT, MAPPO} × perturbations, 5 seeds, significance tests; mechanism ablations: context length (in-context adaptation) and dataset diversity; calibrated vs naive RTG prompting under shift

### Phase P7 — Dynamics-shift study (claim C3, reframed 2026-07-09)
- [ ] **P7.0** ⚡ EARLY GATE (~1 day, runs right after P1, parallel to P2): log MaxPressure trajectories on one paired hangzhou_1x1 in BOTH backends; compare state-feature distributions (per-feature KS/overlap) and MaxPressure-normalized returns. Pathological shift → descope C3 to a limitations study, minimal sunk cost
- [ ] **P7.1** Feature-space alignment: freeze the backend-neutral feature set (no backend-specific structured states) **+ freeze the normalized transfer metric** (improvement over within-backend MaxPressure; Decisions Log 2026-07-09)
- [ ] **P7.2** RTG calibration protocol for cross-domain prompting (target return relative to in-backend MaxPressure return, or quantile-space prompting) — novel contribution candidate
- [ ] **P7.3** Transfer curve: CityFlow-trained MADT on SUMO at zero-shot → few-shot (**k ∈ {5, 20, 100}**, collected by the MaxPressure probe, spanning scenario variants — same-scenario episodes are near-duplicates under CityFlow determinism) → full retrain anchor; many seeds + dataset draws; small SUMO corpora only (main corpus stays CityFlow)
- [ ] **P7.4** Alternative-state-encoding robustness ablation (waiting-based vs count-based features) — guards the \"transfer gap = interface mismatch\" objection

### Phase P8 — Statistics & reporting hardening
- [x] **P8.0** ✅ **MERGED 2026-08-06** (`2153e2a`, 320 passed / 2 skipped). Merge gate held **exactly**: `att_running_mean` reproduces every committed baseline anchor bit-for-bit (160.5584 / 307.5346 / 141.6547 / 207.2605), so the horizon values from the same run are trustworthy by construction. **Registered A1 anchors: cf_hz1x1 MaxPressure 247.75 / Random 413.53; cf_grid4x4 169.05 / 265.75.** Naming ruling enforced by an AST guard over `offline/`. ⏳ **The two MAPPO cells are PENDING the user's `tmux` re-run** — absent from the data rather than placeheld, with two reasoned skips pointing at `PROVENANCE`. **They must land before any P4+ comparison against the anchors.** Original item: 🚨 **BLOCKING for every reported number, including the P4.2 gate.** Implement the registered primary metric (`PREREGISTRATION.md` A1): `average_travel_time` at the **episode horizon**, in our own code — `experiments/runner.py` is frozen and computes the running mean, and it does not need changing. Re-derive the §3.1 anchors under A1 (cheap: 3 seeds × 2 scenarios × 3 policies), publish them alongside the legacy running-mean figures under distinct names, and update `tests/test_p0_baseline_anchors.py` to assert both. Note the committed `docs/data/p0_baselines/` stores only the aggregate, so the horizon value **cannot** be recomputed from it — a re-run is required. Also make `offline/policies/plan_replay.py` report both aggregations
- [ ] **P8.1** ≥5 seeds everywhere, mean ± 95% CI, paired tests vs strongest baseline; extend `experiments/report.py`
- [ ] **P8.2** Compute/latency reporting: train-time, inference ms/decision, param counts (reviewers ask)

### Phase P9 — *(moved)*
LLM-teacher extension relocated to **P11.4** (stretch goals) on 2026-07-13 — it was already optional, and grouping all "only if time remains" items in one place prevents it from silently competing with load-bearing work.

### Phase P10 — Paper & release
- [ ] **P10.0** Dataset release package: datasheet (Gebru et al. template), license (per P2.3 audit), Zenodo DOI, versioned archive + loading code — reviewers check this when a dataset is claimed as a contribution
- [ ] **P10.1** Outline + related-work matrix (vs the 2024–26 papers in §1)
- [ ] **P10.2** Full draft → mentor review → arXiv → venue submission

### Phase P11 — Stretch goals (ONLY if time remains after P10; never blocks the paper)
Candidates parked here deliberately: each is scientifically attractive but competes with the project's top risk (time exhaustion). Revisit only once P10.2 is submitted.
- [ ] **P11.1** **Transfer triangle** (CityFlow ↔ SUMO ↔ MOSS): promote the C3 transfer *curve* into a three-domain study. Prereqs: convert headline scenarios to MOSS format (backend `envs/moss_env.py` exists, but **zero MOSS configs/scenarios in repo** — verified 2026-07-13) + validate conversion fidelity (conversion errors would contaminate the measured dynamics gap) + third evaluation sweep. Payoff: dynamics shift measured across three independent engines instead of two — substantially strengthens the sim-to-real proxy argument
- [ ] **P11.2** **Reverse-direction transfer ablation** (SUMO → CityFlow): is the transfer gap asymmetric? Would indicate which engine's dynamics are "richer." Cost: a second full MADT training run
- [ ] **P11.3** **City-scale demo** (Manhattan 28×7) on MOSS: MOSS's GPU parallelism only pays off at scale (thesis measured ~3× on hangzhou 4×4, far from the paper's 100× claim, because the network is small). Caveat: MOSS competes with DT training for the same RTX 5080 — schedule sim and training runs separately, never concurrently
- [ ] **P11.5** **Conversion-artifact bound** (methodological, NOT a way to grow C3): `CityFlow/tools/converter/converter_v2.py --typ c2s` is fully implemented and grid4x4 has the data it needs (32 intersections, 16 phases, 12 roadLinks). Convert grid4x4 to SUMO, run the P7.0 protocol on it, and measure **how much gap the converter itself introduces** on a scenario whose topology is identical by construction. Payoff: an upper bound on conversion artifact that strengthens the real C3 interpretation ("conversion gap ≤ X, measured dynamics gap = Y ≫ X"). **Do NOT add converted scenarios to the C3 transfer curve** — the converter hardcodes yellow time to 5 s (`converter_v2.py:1062 TODO`), marks direction as "falsely defined" (`:464 TODO`), and flags phase-to-lane mapping as uncertain for irregular phase counts (`:1129 TODO`) — and grid4x4 has exactly 16 phases, the flagged case. A converted pairing would inject OUR artifact into the measurement C3 exists to make, and P7.4's defence against "the gap is just interface mismatch" rests on the paired scenarios being natively authored, not self-generated
- [ ] **P11.6** **Evaluate MADT on DTLight's DTRL datasets** (16 offline datasets, 8 scenarios, published baseline numbers). Training on *their* data and comparing against *their* table removes the data confound entirely — the cleanest external anchor available to us. Costs a format adapter plus evaluation runs; **their metric is average delay, not ATT**, so the comparison needs the same care C3's cross-backend normalisation does. **Journal version, not September.**
- [ ] **P11.7** **`dqn_pfrl` (RESCO-parity IDQN) as an online baseline and ladder tier.** Motivation is strong — every learned baseline we have is this platform's own implementation, so *"we beat MAPPO"* invites *"your MAPPO was weak"*, whereas RESCO-parity IDQN points at a published NeurIPS D&B benchmark that DTLight also reports on Cologne and Grid 4×4. **But it is NOT "a config, not code" (checked 2026-08-06):** `pfrl` is **not installed** (`ModuleNotFoundError`), so `agent/DQNAgentPFRL.py` cannot import, and `experiments/registry.py` — **frozen** — offers only `dqn`/`ippo`/`mappo`. It therefore needs a new dependency (CLAUDE.md rule 3) **and** an authorised patch to a frozen file. Deferred; the repo's own `dqn` gives most of the "second learned algorithm" value for a config alone.
> **NON-GOAL (recorded 2026-08-06): do NOT re-implement CoLight, MPLight or PressLight.** A self-implemented baseline that underperforms is a straw man a reviewer catches faster than a missing baseline, and each expects its own state representation — importing exactly the confound C3's whole defence is built to avoid.
- [ ] **P11.4** **LLM-teacher extension** (was P9): local Mistral-7B/Llama-3.1-8B (4-bit) for OOD trajectory labeling / perturbation curricula — training-time only, never in the control loop

---

## 7. Sub-chat Protocol (hardened 2026-07-12 — designed for AI-written code)

Threat model: the dominant failure mode of AI-heavy development is *confident code with a silent semantic bug that nobody independently checked*. Every rule below exists to catch that.

**Execution modes.** Coding tasks preferably run in **Claude Code** on the user's terminal inside the repo (reads real files, runs real tests); chat sub-chats remain the fallback. Briefs carry a mode header: in Claude Code the "attach files" list becomes "read these repo paths", and these guardrails apply: work ONLY on a task branch (`task/<id>-<name>`), NO edits to existing repo files unless the brief says so, commit before finishing, Return Packet includes the actual `pytest` output and `git diff --stat`.

**Lifecycle per task:**
1. Master chat issues the **Brief** (self-contained; embeds §4 contracts; Definition of Done; ≤ ~2 source files + tests — larger tasks get split).
2. Implementation sub-chat (Claude Code preferred) writes code + tests on the task branch, ends with the **Return Packet**.
3. **Critical-path tasks get an independent review sub-chat** (fresh context; receives brief + contracts + diff; finds discrepancies ONLY, writes no features; may run in Claude Code read-only). Critical path = anything the paper's data flows through: trajectory logger, flow randomizer, dataset/RTG loader, DT agent, corpus linter, statistics harness. Merge to main only after review passes.
4. User pastes Return Packet(s) back → Master chat updates checkboxes, issues next brief.
5. **Phase-boundary review:** before freezing any phase, Master chat writes down what that phase assumes about later phases (this caught determinism↔corpus and reward↔RTG couplings late; do it early now).

**Stopping rule for review rounds (added 2026-07-25).** A review round is worth running while it can
still find *load-bearing* defects — things that would force rework (a wrong data format, a missing
baseline, a confounded experiment). Once a round returns only style and preference items, stop and ship:
further rounds trade real progress for churn. Planning documents get **at most one** external review
round, then they freeze. From P1 onward the review function moves into the pipeline (P1.5-style
independent review of an actual code diff against a frozen spec), which is bounded by construction —
reviewing code against a fixed contract terminates; reviewing prose against opinions does not.

**One writer per working tree (added 2026-08-07).** While an implementer session is live in the same
directory, **the coordinator does not commit.** `git checkout -b task/...` switches the branch for
*every* session sharing the tree, so a coordinator commit silently lands on the implementer's branch.
**Check `git rev-parse --abbrev-ref HEAD` before any commit.** If both must write, use a worktree.
⚠️ **And `git push origin main` from a task branch is a no-op that reports success** — it pushes the
unmoved `main` ref, not your work. *Recorded after three coordinator commits landed on
`task/p2.6-format-v11` and three "pushed" confirmations were false; the §10 handoff the user had been
told to restart against was neither on main nor on the remote.*

**End every Master-chat turn with exactly one decision line (added 2026-08-07).** One of two forms,
verbatim, as the final line of the message:

```
DECISION NEEDED: <what> | my recommendation: <what> | reversible: yes/no
NO DECISION NEEDED — continuing with <next thing>.
```

**Reserve `DECISION NEEDED` for choices that are irreversible or that touch the paper's claims**;
everything else is decided and reported. The `THINGS YOU NEED TO DO:` block goes immediately before
this line. *Recorded 2026-08-07 after discovering the format existed **only in the conversation** —
the rule that references "the closing decision line" had been written down while the line itself
never was, so a fresh session would have produced the block and not the line.*

**Surface every human action in a fixed end-of-turn block (added 2026-08-06).** The Master chat ends
every turn with `THINGS YOU NEED TO DO:` — numbered, copy-pasteable — immediately before the decision
line, and writes `nothing` when there is nothing, so its absence is never ambiguous. Anything the user
must run, apply or restart appears there in full even if it was already named earlier. **A required
action mentioned only in prose is an action that does not happen** — the agent-definition patch sat
unapplied for a day because it was introduced mid-message.

**A brief's acceptance criteria are claims too (added 2026-08-07).** Any acceptance criterion in a
brief that asserts a **property of real data** must either **cite the measurement that established
it** — with its sample — or be **marked unverified and handed to the implementer to check before it
becomes a test**. BRIEF_08 specified "`average_travel_time` is monotone non-decreasing early in an
episode"; that was reasoning presented as fact, and 407 of 407 real episodes falsify it (the metric
averages over completed *plus currently-active* vehicles, so an entering vehicle pulls the mean
down). **`scripts/check_test_hygiene.sh` cannot catch this: it greps tests, and this was upstream of
any test existing.** The guard has a blind side exactly where specifications enter, and this is the
project's signature error one layer earlier in the pipeline than we had been watching.

**A fixture that satisfies the assertion by construction is a tautology, not a fixture (added
2026-08-07).** `FakeTrafficEnv` hardcodes `average_travel_time: 12.5`, and a constant satisfies
"monotone non-decreasing" — so the falsified criterion would have shipped **green**. Any fixture
backing a load-bearing test must be built so the **wrong implementation is detectable**:
non-constant where the property is about change, and differing values where the property is about
*which key was read*.

**Campaign scripts must abort and must self-verify (added 2026-08-06).** Every multi-command
collection or training script gets `set -e` **and** a final assertion that completed collections
equal collections requested. The 2026-08-06 ATT campaign had neither: one tier was refused by the
out-dir guard, fifteen-plus raised `ValueError`, and the loop ran on to a clean-looking end. Run
overnight it would have reported success with half the corpus missing.

**Check the output directory before issuing a collection command (added 2026-08-06).** The
populated-`out_dir` guard exists and we designed it; a command that trips it wastes the slot.

**An inertness or equivalence check must enumerate its cases before it runs (added 2026-08-06).**
State which cases exist and which were tested, in the message that reports the result. "Adding a
metric does not change trajectories" is a claim about seven behaviour policies; it was tested on
one and was false for four of them.

**Verify runtime state before asserting it (added 2026-08-06).** Before any instruction that assumes
a job is running or finished, check `ps` or the file count in its out-dir. The coordinator twice
reported on the state of a run it was not watching, and planned two turns of work around a campaign
that had died an hour earlier.

**A corrected definition propagates in BOTH directions (added 2026-08-07).** When an amendment changes
what a quantity *means*, it must be propagated **forward** to future measurements **and backward to
every conclusion already recorded under the old meaning**. Concretely, and mechanically: **any
`PREREGISTRATION.md` amendment that changes a quantity's definition requires, in the same turn, a grep
of this plan for conclusions resting on the old meaning, and each hit is either re-derived or marked
`NOT SETTLED`.** A1 was propagated forward — P8.0 re-derived the P0.2 anchors — and never backward,
and the expensive casualty was not a number in a table but a **finding steering P4/P5**: "the ladder's
expert tier is scenario-dependent", whose grid4x4 and cologne3 margins are 2.5 % and 5.8 % against a
40 % measured legacy-vs-horizon gap. Fixing a metric is not fixing the results computed with it.
*Applied to the existing amendments the moment it was written, which is the only honest way to add it —
otherwise the rule would itself have been propagated forward and not backward. Two hits: (1) **A2**
retired two illustrative figures on 2026-08-06 and §3.1 still carried them a day later, in the section
every phase is anchored against; (2) **A4** makes `vehicle_count` at the horizon a mandatory companion
to every ATT cell and **no recorded ATT cell carries one** — folded into queue item 0, since both are
`[-1]` of arrays the corpus already stores.*

**An unticked box after a merge means the task is NOT DONE (added 2026-08-07).** This is the
version of the ownership answer that survives its author: not *"the coordinator should remember to
tick"* but *"a merge that does not tick its box is incomplete work"*. It belongs in the Definition
of Done, and it is now in `CLAUDE.md` §6 as a checklist item like any other, so an implementer's own
DoD fails without it rather than depending on a coordinator's habit.

**PASTE THE SWEEP, NEVER REPORT A COUNT (added 2026-08-11).** When fixing a class, **paste the full stem-grep output into the message, one line per hit, each labelled `LIVE` / `DATED-ANNOTATED` / `DIFFERENT-CLAIM`.** Never write *"five sites"*. **A count is a description of the sweep; the enumerated list IS the sweep.** Three consecutive sweeps came up short — `429.67 (Random)` vs `429.67 Random`, then `byte-identical` vs `byte-identity`, then a site the user could not check because I had reported a number — and **all three failed at the CLASSIFICATION step, not the search step.** The 2026-08-07 rule already says classify every hit; it kept failing because the classification happened in my head, where nothing can check it. Pasting moves it somewhere the other seat can audit. *Corollary learned the same day: a crude classifier is not a substitute for reading each line — an automated labeller flagged 40+ hits as defects that were a different claim entirely, and acting on it would have been the over-application failure of 2026-08-07.*

**Check a worktree's IGNORED artifacts before retiring it (added 2026-08-11).** `git worktree remove` deletes gitignored state without warning, and `output/` is gitignored by design. Before removing any worktree, run `git -C <wt> status --porcelain --ignored`, and for each ignored path ask whether a **merged** artifact references it. P4's worktree held the five checkpoints backing the reported gate; they are now copied and sha256-verified against `docs/data/p4_gate.json`.

**Tick the checklist in the merge commit (added 2026-08-07).** §6 is the project's completion state, and a checkbox is a claim like any other. **The merge that lands a task ticks its boxes in the same commit** — never as a follow-up, because a follow-up is a step that gets skipped. This has now failed in **both directions**: on 2026-08-06 §10 read as progress that had not happened, and on 2026-08-07 §6 read as absence of work that had. Both make the section unusable as the thing it exists to be. The drift check is in §6's header and is one command.

**Deleting a test is permitted ONLY when its specification was withdrawn (added 2026-08-07).** This project had never sanctioned a test deletion before P2.6 removed three that asserted amendment A4's now-withdrawn validity condition. It is the right call — **keeping a test for a retired specification is worse than deleting it**, because it pins a withdrawn rule into the suite and the next session will "fix" correct code to satisfy it. But *"the spec changed"* is also the most available excuse for deleting an inconvenient test, so the permission is fenced mechanically. **All three conditions, every time:** (a) the withdrawn property must be retired by a **written, dated authority** — an amendment, a ruling — **cited by name in the commit message AND the Return Packet, never left to a diff**; (b) the net test count must not fall; (c) **the withdrawal must itself be pinned** — add an assertion that the retired thing is *absent from the module surface*, so a partial revert cannot reinstate it through a docstring edit or a stray constant. Condition (c) is P2.6's invention (`test_no_validity_threshold_survives_anywhere`), offered unprompted, and it is the part that makes deletion safe rather than merely defensible.

**A rule has a SCOPE, and applying it outside that scope is a failure of the same kind as ignoring it inside (added 2026-08-07).** Every prior instance in this log is **under**-application — a rule not applied where it should have been. P2.6's refusal to rebase its own branch is the first **over**-application, and it matters because **from the inside the two are indistinguishable**: both feel like caution and refusal to act unilaterally, and over-application is the harder of the two to catch because it looks like diligence and nobody challenges it. **Diagnostic, one question: name the party this rule protects, and check that party is present in the case at hand.** The one-writer rule protects *other sessions sharing a tree*; a session rewriting its own branch as sole writer was never in scope. Cost of getting it wrong in this direction is real and invisible — here it would have forced a hard-conflicting merge across four duplicated commits instead of a clean rebase. **The fix for over-application is the opposite of the fix for under-application**, which is why the two are named separately rather than filed together as "apply rules carefully".

**A check must report its DISCRIMINATING POWER, not only its pass rate (added 2026-08-07).** Any
identity or invariant used to **certify data** must state, beside its pass rate, the distance between
the correct answer and a deliberately wrong one — the same quantity on the misaligned, mis-keyed or
mis-scaled variant it exists to reject. §7 already forbids tests that *cannot fail*; it had nothing to
say about checks whose *force* was never measured. The corpus-alignment identity separated right from
wrong by **108×** on grid4x4 and by **1.8×** on cologne3: rigorous in form, nearly empty in force, and
on cologne3 it would have certified the corpus while being unable to detect the error it was written
for. A pass rate answers "did it agree"; only the margin answers "would it have noticed". Report both,
or the check is decoration.

**Verify by EFFECT, not by STATUS (added 2026-08-07).** A command whose success output is identical
whether or not it did anything must be verified by its effect on the artifact, never by its exit code
or its report. `git push origin main` from a task branch pushes the unmoved `main` ref and reports
success — three false confirmations, indistinguishable from true ones. The fix generalises: compare
the refs, not the exit code. Same shape as `claude_guard.sh` exiting 0 after a commit (its verdict is
evidence about `git status`, not about the working tree), as the ten inert `Write(...)` permission
rules that looked protective, and as a green suite whose corpus tests all skipped. **Where success and
no-op are observationally identical, the status line is not evidence.**

**Grep the plan before escalating a decision (added 2026-08-06).** Before marking anything
`DECISION NEEDED`, search `docs/PROJECT_PLAN.md` for an existing answer — the frozen claims in §1 and
the task checklist in §6 most often hold one. If escalating anyway, say where you looked: *"I checked
§1 and §6; there is no answer there."* This is the same discipline as verifying the artifact instead
of its description, applied to our own plan. Re-litigating a settled design spends the one resource
the coordinator cannot regenerate — the user's attention — and risks a fresh answer that silently
contradicts a frozen one.

**Fix the class, not the sentence (added 2026-07-31).** When a false claim is found, the correction
must be applied to every claim of the same shape, not only the one that was caught. This project has
now produced five instances of the same failure — a claim measured on a sample and stated about the
population: Master chat's "≥7 paired scenarios" (from a `.sumocfg` referencing a missing file),
Master chat ticking off P0.3 (from a commit message rather than the code), Master chat's fabricated
"8× parallel speedup", the sub-chat's "8305/8305 entries, every flow file in this repo" (four files),
and — after writing the retrospective about that very error — "startTime is sorted ascending in every
file checked" (false for two files) and "interval is int in hangzhou" (hangzhou_4x4 is float).
**Rule: a measured claim must state its sample, or measure the population.** When correcting one,
grep for its siblings in the same document before declaring it fixed.

**Suppressed-assertion guard (added 2026-07-31, after `assert X or True` shipped undisclosed).** A
mechanical grep must run over `tests/**` in the guard hook and reject always-true assertion patterns:
`or True`, `assert True`, `assert 1`, `if False:`, bare `pass` in an `except`, `pytest.skip` without a
reason, `pytest.raises` without `match=` on a test whose name claims a specific error. Rationale: the
CLAUDE.md §0 rule ("never edit a test to make it pass — stop and say so") worked twice in the same task
and failed once, silently. A rule that depends on remembering is weaker than a grep. This is the same
philosophy as the frozen-file guard: mechanical enforcement beats good intentions.

**Filesystem-mutation barrier (added 2026-07-31, after the pattern recurred).** In any collection or
data-producing tool, **every filesystem mutation — writes AND deletions — must happen after all
validation, never before**. This bug has now appeared twice in the same shape: P1's NB2 (`--overwrite`
deleted the previous corpus before constructor validation, so a failed construction destroyed data and
only then raised) and P2.0 (both draw materialisation and `_clear_stale_draw_files` ran before the
logger's populated-`out_dir` check, so a refused run could write files and delete a previous run's
demand). The fix in both cases was ordering, not new logic. Reviews of P2.5, P3 and P2.4 must check
this explicitly.

**TIGHTENING A PROTOCOL BEFORE MEASUREMENT IS ALWAYS SAFE; LOOSENING ONE AFTER A RESULT NEVER IS (added 2026-08-12, proposed by the user).**
This asymmetry decides every *should we change the protocol?* question, and it is more portable than
any of the rulings it has produced. A constraint registered **before** the number exists can only make
a claim **harder** to make, so it needs no permission and carries no risk — which is why
`PREREGISTRATION.md` **A8(a)** (P4.3's calibration rule must be a declared probe-return quantile, the
sweep an ablation around it and never the mechanism that picks it) was simply registered. A criterion
changed **after** an experiment has run and lost needs an argument that survives being written into
the paper, and *"we changed the budget rule after our method lost"* does not, however defensible the
rule is in the abstract — which is why **A8(b)** became a declared-ceiling **secondary** that cannot
replace the primary. **The test to apply: write the change as a sentence in the methods section, with
its date relative to the result. If that sentence embarrasses you, it is loosening.** The mirror-image
check is the same tool: we refused a DT budget raise on 2026-08-08 when the DT had **passed**, so
granting one on 2026-08-12 because it had **lost** was refused on sight.

**A REGISTERED CRITERION CAN SILENTLY MEASURE SOMETHING OTHER THAN WHAT IT WAS NAMED FOR — CHECK WHAT IT TRACKS BEFORE LETTING IT ARBITRATE (added 2026-08-12).**
The plateau criterion (2000-step windows, tolerance 0.05) was registered to mean *has this arm
finished learning*, and an external critique used it to argue the DT had been treated unfairly.
Measured on our own committed artifacts: **`madt` 0/5, `bc` 0/5, `iql` 0/5, `bc_top10` 4/5.** The only
arm that reaches it is the one whose training set is **10× smaller** — 7,200 windows against 72,000,
**355 epochs**, final loss **0.0011–0.0032** against BC's **0.0139–0.0189** on the same architecture,
optimiser and budget. **On this corpus "plateaued" tracks TRAINING-SET SIZE, not convergence.** It
therefore cannot arbitrate fairness between arms whose training sets differ, and any argument of the
form *"arm X plateaued and arm Y did not, so the comparison is unfair"* is measuring the filter rather
than the learning. **Binding on P4.3 and P5:** the criterion stays a reported **diagnostic** and may
not justify a budget change for any single arm. **The general form — a criterion is a measurement and
has to be validated like one** — is why this sits here and not only in the Decisions Log.

**A VALIDATOR TAKES ITS REFERENCE FROM THE DECLARATION, NEVER FROM ITS ARGUMENT (added 2026-08-12, after the class recurred ONE TASK after we fixed it).**
P4.4's F5 was `_run_report` deriving its "requested runs" from the very episodes it then checked for
completeness — a tautology. P4.5 fixed that, and then wrote `assert_selection_design`, whose leakage
check reads the held-out pool **from the payload it is validating**
(`held_out = {int(d) for d in payload["held_out_draws"]}`). The reviewer emptied that field, planted a
real leak, and **the suite stayed green.** The same function also takes `declared_count` from the
block under test and `reference_seeds` from the first arm's own record: **three instances of one class
in one function**, under a docstring promising *"no held-out draw enters training"*.
**The rule is not "check `_run_report`".** A check whose expected value comes from its input can only
ever verify internal consistency, and internal consistency is exactly what a corrupted artifact has.
**Every guard names the constant it validates against** — `HELD_OUT_DRAWS`, `TRAINING_SEEDS`,
`SELECTION_ARMS` — **and cross-checks the payload's own copy against it, so a disagreement raises
instead of being adopted.** Related and already in force: *a fixture that satisfies the assertion by
construction is a tautology*, and *an inertness check must enumerate its cases before it runs*.

**ANY TIMING THAT ENTERS AN ARTIFACT RECORDS ITS THREAD REGIME BESIDE IT (added 2026-08-12).**
Not only suite runs. **Measured:** `docs/data/p4_4_training.json` carries **15 per-run `seconds`**
values and records `torch_num_threads = 1` — while **`OMP_NUM_THREADS` and `MKL_NUM_THREADS` appear
nowhere in it, and nothing under `offline/` sets or reads them.** Those env vars are a **different
knob** from `torch.set_num_threads()`, and they are the ones that stopped the P4.5 suite hanging. So
the recorded `torch_num_threads = 1` does **not** establish which regime produced `bc seed 101 =
103.31 s`. **Suite timings are not results; training seconds and evaluation wall time are the kind of
number that reaches a reproducibility section**, and two figures from different regimes look
comparable and are not. Record `OMP_NUM_THREADS`, `MKL_NUM_THREADS` and `torch.get_num_threads()`,
**read at run time, never assumed**. The general fix to `runtime_provenance()` is `DEFERRED` 39
(P4.3's, because that function lives in a merged module).

**THE TABLE SHAPE CHECK, IN ITS CORRECT FORM (added 2026-08-12, after the first two versions were both wrong).**
Run it after **any** structured edit, over the whole file, not the row you touched:

```python
# group contiguous '|' lines into blocks; every row in a block must have the SAME pipe count.
# sys.exit(1) on failure -- see the rule below.  Do NOT hard-code an expected count: a 3-column
# table has 4 pipes and a 4-column table has 5, and a fixed expectation reports false positives
# (it did, on BRIEF_13, and a false alarm trains you to ignore the alarm).
```
**The invariant is uniformity WITHIN a block, not a global number.** Version 1 hard-coded 4 pipes and
missed nothing but could only check one table; version 2 allowed `{3, 5}` and flagged nine correct
rows of a three-column table. **A check that cries wolf is worse than no check, because the next real
failure is read as another false positive.**

**A CHECK THAT PRINTS ITS FAILURE INSTEAD OF EXITING NON-ZERO IS NOT A CONTROL (added 2026-08-12, after it let a broken table through in the same turn it detected it).**
I ran the table shape check, it **correctly printed `MALFORMED 617`**, and the commit went through
anyway — because the check `print(...)`ed and the shell chained on `&&`, so a detection with no exit
code is a log line, not a gate. The row it caught was my own: I pasted OffLight's ATT figures using
`|` as a group separator **inside a markdown table cell**, turning a 4-pipe row into an 18-pipe one.
**Two rules, and the second is the general one.** (1) **Never put a literal `|` inside a table cell** —
use `/`. (2) **Any verification wired into a command chain must `sys.exit(1)` on failure**, or it does
not participate in the outcome it appears to guard. This is `VERIFY BY EFFECT, NOT BY STATUS` turned
on the verifier itself: I checked the artifact, saw the defect, and still shipped it, because nothing
connected seeing to stopping. ⚠️ **And the repair failed on its first attempt too** — a regex over
backtick spans ate a structural pipe and produced a 3-pipe row — which is why the second attempt used
**exact enumerated spans with an assertion per span**, and asserted the final pipe count before
writing. **Fix a structured document by exact match, never by pattern, and assert the shape before
the write rather than after.**

**A MONITOR THAT GREPS FOR A PROCESS NAME MATCHES ITSELF (added 2026-08-12).**
`until [ -f out ] && ! pgrep -f "job.py"; do sleep 20; done` **can never exit**: the monitor's own
command line contains the literal `job.py`, so `pgrep -f` matches the monitor. One such loop leaked
from the P4.4 review, ran indefinitely after its job had finished, and had to be killed by PID. It was
demonstrated live in the same session — the diagnostic command sent to *check* for it also appeared in
its own output, for exactly the same reason. **Use a sentinel file the job writes on exit, or
`pgrep -f "[j]ob.py"`, or match a PID captured at launch — never a name the monitor itself spells
out.** P4.3 and P5 both run long jobs; without this it recurs. **And the wider point: the leaked
process was invisible from inside the session that created it**, so *"the job finished"* and *"nothing
is still running"* are two claims, and the second needs its own command.

**Redundancy rules (non-negotiable):**
- **Independence of ROUTE is not independence of ASSUMPTION (added 2026-08-12).** Two computations
  that share a model agree because the arithmetic is right, not because the model is. F3's
  concentration p-value was computed twice — exact enumeration and a 20,000-draw Monte Carlo — and
  agreed to every digit, **while both assumed the same multivariate-hypergeometric null**. The
  agreement confirms the calculation and leaves the assumption untested. **So: a double computation
  discharges the arithmetic risk only, and any reported statistic must NAME ITS NULL (or its model) in
  the same sentence as its value**, because that is the only thing that carries the untested half to
  the reader. Raised by the user against a cross-check I had already accepted as sufficient.
- **Double computation of critical quantities:** RTG in the loader verified by an independent `np.cumsum` test path; logged rewards vs rewards recomputed from per-lane counts — equality asserted on real episodes; randomizer dual-backend renders checked for equivalence.
- **Corpus linter (P2.4) after every collection run:** manifests, shapes, NaNs, reward-recompute equality, return distributions, **episode-hash duplicate detector** (residual guard on the determinism failure mode).
- **Sanity anchors:** any phase producing numbers compares against §3.1 (P0.2) before results are accepted.

```markdown
## RETURN PACKET — [Task ID]
**Status:** DONE / PARTIAL / BLOCKED
**Branch + diff stat:** (task branch name; `git diff --stat` vs main)
**Files produced:** (paths + one-line description each)
**Tests:** (actual pytest output summary; run locally/in Claude Code)
**Self-review checklist:** contracts honored? / tests green? / zero edits to existing files? / zero new deps? (Y/N each)
**Deviations from brief:** (any, with justification)
**Open questions / risks for Master chat:** (bullets)
```

**Sub-chats may NOT:** change the headline contribution, alter §4 contracts, add heavy dependencies, or modify `envs/base_traffic_env.py` — such needs go in the Return Packet as an open question.

---

## 8. Decisions Log

| Date | Decision | Rationale |
|------|----------|-----------|
| 2026-07-08 | Headline = Offline MADT + cross-backend transfer (Track B / H3-core) | Least saturated axis; hardware-optimal (sim on CPU, supervised DT on GPU); differentiates vs arXiv:2602.02903 via C3 |
| 2026-07-08 | LLM demoted to optional training-time teacher (P9) | Avoids latency & trustworthiness critiques dominating 2025–26 reviews |
| 2026-07-08 | One `.npz` per episode + run manifest | Simple, appendable, parallel-writer-safe, no new deps (HDF5 rejected for now) |
| 2026-07-08 | Wrapper/callback logger; zero env modifications | Keeps platform contribution separable from method contribution in the paper |
| 2026-07-08 | Validation gate at P4 (1×1 offline ≈ online) before multi-agent scaling | Cheap early falsification of the core hypothesis |
| 2026-07-08 | Dev stack = WSL2 Ubuntu 24.04 + VS Code WSL Remote; repo lives in Linux FS (never /mnt/c) | CityFlow/SUMO need Linux toolchain; cross-OS I/O is 10–50× slower |
| 2026-07-08 | PyTorch from cu128 wheel index; **no** CUDA toolkit, **no** Linux NVIDIA driver in WSL | RTX 5080 = Blackwell sm_120 (needs cu128 kernels); Windows driver projects libcuda into WSL; wheels bundle CUDA runtime |
| 2026-07-09 | ~~Worker pool uses `spawn` start method (P0.3 patch)~~ — **SUPERSEDED 2026-08-03, NEVER IMPLEMENTED.** The spawn patch was written, failed, and was reverted; the real fix was `torch.set_num_threads(1)` (P0.3-fix). Verified 2026-08-03: `experiments/runner.py:450` still constructs `ProcessPoolExecutor(max_workers=...)` with **no `mp_context`**, i.e. default `fork`. (The row's original "runner.py:405" is the same statement — the pin patch inserted 45 lines above it.) See the 2026-07-27 and 2026-08-03 rows below | *Belief at the time:* Linux `fork` clones torch/CityFlow C++ lock state → deadlock (observed: frozen terminal, 10% CPU). Half-right: it IS a `fork` hazard, but the mechanism is libgomp team threads the child never inherits, not C++ lock state. **Left in place rather than deleted — a reader must be able to see that this row stood uncorrected for 25 days, because that is the argument for distrusting undated plan rows** |
| 2026-07-09 | grid4x4 MAPPO@60ep result retained as motivation-figure material | Online MARL below-random at low interaction budget = the paper's sample-inefficiency argument, empirically on our own platform |
| 2026-07-09 | C3 transfer reported as **within-backend-normalized** improvement (policy vs MaxPressure evaluated in the same backend), not raw travel times | Thesis §5.2: SUMO/CityFlow absolute metrics not interchangeable (corr. 0.93, CityFlow 8.4× jumpier); raw cross-backend numbers would be rejected on the thesis's own evidence |
| 2026-07-09→12 | **RESOLVED (Master chat decision, mentor free hand): primary reward = queue_length; PressLight = mandatory robustness appendix; empirical safety latch in P2.3** (Spearman corr. of queue- vs pressure-returns with avg travel time across the corpus — if pressure clearly wins, reverse before P4 at zero corpus cost) | RTG must rank trajectories consistently with the headline metric (Σqueue ≈ total delay ≈ travel time); queue is backend-neutral (waiting counts only) while pressure imports turn-topology differences into exactly the C3 interface confound; cross-paper comparability lives in metrics, not rewards; Addendum A makes the corpus reward-agnostic anyway |
| 2026-07-09 | OPTION: MOSS backend for corpus acceleration (thesis: ~3× vs SUMO on 4090, agent-bound) | Only if scenarios exist in MOSS format; not a dependency |
| 2026-07-09 | ACTION: clarify thesis-credit/co-authorship conventions with mentor (thesis is fresh, June 2026) | Platform paper (theirs) vs method paper (ours) split must be agreed early |
| 2026-07-09 | **C3 reframed:** binary "transfer works" → measured transfer curve (zero-shot → few-shot → retrain) under unified "distribution shift" narrative with C2; P7.0 early gate added; RTG calibration (P7.2) promoted to contribution candidate | Thesis §5.2 kills raw-metric comparison, not transfer itself; action semantics backend-neutral by repo design; MaxPressure transfers by construction (existence proof); Δt=10s decisions aggregate over microscopic dynamics differences; ≥7 paired scenarios verified; every curve outcome is publishable — C3 can no longer waste more than ~1 day (the P7.0 gate) |
| 2026-07-10 | **All three claims hardened after external critique** (verified sound on nearly all points): C1 → dataset-ladder study, "no online exploration" phrasing; C2 → pre-registered RQ + 2×2 with domain-randomized MAPPO + mechanism ablations; C3 → k ∈ {5,20,100} with MaxPressure probe; BC/%BC/IQL baselines added as non-negotiable; probe-calibrated return prompting named as the method component serving both C2 and C3 | Circularity, missing-BC, and data/architecture confound were genuine rejection-level flaws; verified prior art: DTLight+DTRL (2312.07795), DataLight (2303.10828), X-Light, arXiv:2603.22315 (EV-DT, Mar 2026) |
| 2026-07-11 | **P2.0 FlowRandomizer mandatory before corpus; episodes keyed (scenario, flow-draw)** — critique (a) accepted; deterministic demand kills deterministic-tier corpus content, not just CIs; randomizer must render dual-backend (vehicle-list level, verified against flow.json structure) | Regenerating the corpus later would cost weeks; fix costs one small tool |
| 2026-07-11 | **Ladder tiers = measured normalized return** (random=0, MaxPressure=100), policy names demoted to provenance — critique (b) accepted; own P0.2 data contradicts the name-implied ordering | Reviewer would catch it before us |
| 2026-07-11 | **P4.2 gate re-anchored:** ≥ MaxPressure AND within 5% of best online — critique (c) accepted | Old gate passable while losing to a 1970s heuristic |
| 2026-07-11 | **Compute budget sheet (P2.0b) gates P2; time exhaustion promoted to top risk** — critique (d) accepted | Hundreds of sim-hours on one workstation; headline/appendix split must precede spending |
| 2026-07-11 | **Pre-registration made real (P0.4):** git tag + doc hash within 24 h of reward decision — critique (e) accepted with one scheduling correction (register AFTER reward is fixed, not today) | Registering a protocol with an OPEN reward metric would undermine the registration |
| 2026-07-11 | **Reward decision de-blocked technically:** Brief #1 Addendum A adds per-lane count logging → any reward (queue/PressLight/pressure) recomputable offline from the corpus; mentor's answer now selects the headline metric only — critique (f) partially superseded by a better fix; mentor email still goes out this week (co-authorship blocks arXiv regardless) | Kilobytes per episode buy immunity to the reward decision |
| 2026-07-13 | **Transfer direction (CityFlow → SUMO) and MOSS exclusion have scientific rationale, not just convenience — write it into the paper** | Direction: (i) corpus economics (20× faster source engine is what makes the project fit one workstation), (ii) mirrors sim-to-real (train cheap/approximate, deploy expensive/accurate — the reverse has no real-world analogue), (iii) argumentative asymmetry (surviving a *fidelity upgrade* is a strong result; downgrade is not). MOSS excluded: GPU contention with DT training (our compute strategy is sim-on-CPU + training-on-GPU), zero MOSS scenarios in repo (conversion errors would contaminate the measured dynamics gap), weaker literature comparability, and its scale advantage doesn't materialize on headline networks. Reviewers will ask "why this direction?" — answer belongs in the paper, not the rebuttal |
| 2026-08-03 | **D3 falsified ON-REPO at both layers; ruling unchanged.** Full record in `docs/notes/D3_falsification.md`. Against the real `.claude/settings.json` (verified byte-identical to HEAD + the reviewed patch, sha256 `c37aece4…`, so nothing rode in alongside it): F1 `Write scripts/zz_perm_probe.sh`, a NEW file with no individual rule, DENIED and absent from disk; F2 `Edit scripts/check_english.sh` DENIED, sha256 unchanged; F3 control `Write docs/notes/_f3_control.tmp` ALLOWED. Guard layer tested end-to-end in a scratch repo with a byte-identical guard copy and a real `git status`, not by re-reading its regexes: both exceptions PERMITTED, `brand_new.sh` / `.bak` / `.claude/settings.json` BLOCKED, control PERMITTED | The prior evidence established what the permission engine does with *a* rule set; it never established that this repo runs *that* rule set. F1 is the only evidence the glob covers a script that did not exist when the rule was written. **F3 is what makes F1 and F2 mean anything** — without a control in the same session and permission state, "denied" and "the tool was broken" are indistinguishable |
| 2026-08-03 | **Master-chat harness errors, twice in one falsification run, both caught only by the control row.** (1) First guard harness returned BLOCKED for every path including the control, because the copied guard was itself untracked and dirtied `git status`. (2) Second harness cleaned up with `git checkout -- .`, which does not remove untracked files, so one probe's leftovers blocked the next three — results I had already read as confirmations. After fixing cleanup, one probe **reversed its verdict**, which is how G1 below was found | **A confirmation produced by a broken harness is indistinguishable from a real one, except by a row whose expected answer is the opposite of the others.** Recorded because the lesson is procedural and general: every falsification table carries a control whose predicted answer differs, or it certifies nothing. Same family as the suite-that-certifies-nothing problem the test-hygiene grep exists for |
| 2026-08-03 | **DEFECT G1 in `claude_guard.sh` → new task P0.8.** `git status --porcelain` collapses a wholly-untracked directory into a single entry, so `experiments/newpkg/foo.py` arrives as `experiments/newpkg/` and escapes `FROZEN_PATTERNS`. Cause: the `experiments/.*\.py$` clause is the **only** frozen pattern anchored on a file extension; the others are either directory prefixes, which survive the collapse (`envs/newpkg/` still matches `^envs/`), or four exact file paths (`agent/base.py`, `agent/utils/utils.py`, `agent/MAPPOAgent.py`, `rewards.py`), which cannot be created inside a new subdirectory and sit in tracked directories. Only the `experiments/` clause both matches many paths and needs the full path to do so. Not reachable via Edit/Write, which the permission layer denies; **reachable via Bash heredoc/cp/tee** | The guard's own header states it must fire after Bash *"because a heredoc is not an Edit/Write tool call and would otherwise go unnoticed"* — so the hole is in precisely the half of the defence the permission layer cannot cover. Exposure is prospective, like D3's own: no such directory exists today. Found by falsification, not inspection; the regexes had been read several times without anyone noticing, because reading a regex tells you what it matches, not what git will hand it |
| 2026-08-03 | **P0.4 CLOSED — `PREREGISTRATION.md` frozen and tagged `v0.1-prereg`**, while the repo demonstrably contains no corpus and no trained model. Hash lives in the annotated tag, not in the file (a file cannot contain its own hash). §0 states plainly what a local tag proves (content integrity, ordering within history) and what it does not (absolute time — a git date is written by the machine that made it), and names pushing to the public remote as the only externally checkable act | Registering after data exists would be misconduct, and this was the one open item whose value decays with time. **The honesty of §0 is the point**: a registration that overclaims its own timestamp is worse than none, because it invites a reviewer to check and find the overclaim |
| 2026-08-03 | **D4 — train/evaluation flow-draw split registered before collection.** Draw `0` = nominal control, reported separately and never pooled; draws `1–999` = training-corpus pool; draws `1000–1099` = **held-out evaluation pool**, never entering any training corpus for any method including baselines. P2.4's linter enforces it from manifests | Grepped 2026-08-03: no held-out split, train/test split or leakage rule existed anywhere in the plan or the old prereg. Once the corpus is collected the split can no longer be chosen innocently — whatever ids happen to be in it become the training set by default, and evaluating on them is testing on training data. Draw 0 is excluded from **both** pools for a structural reason, not a statistical one: draw 0 preserves source vehicle order while draws k>0 sort globally, so pooling it mixes two generative processes into one CI |
| 2026-08-03 | **D5 — offline-RL leakage rules registered: no online model selection** (every reported model is the checkpoint at a fixed pre-declared step count, never one chosen by evaluation score); **hyperparameters tuned only on hangzhou_1x1 #1 then frozen** across scenarios, tiers, perturbations and backends; **baselines get the same tuning budget**, and an untuned baseline is reported as untuned | A method tuned against online returns is not an offline method, and this is the standard on which offline-RL venues reject papers. No tuning or checkpoint-selection protocol existed anywhere before this (grepped). Registering it costs nothing now and is unbuyable later |
| 2026-08-03 | **D6 — primary metric pinned to the exact registry name `average_travel_time`, and its look-alike explicitly excluded.** Verified in source this session across all three implementations (`metrics/cityflow.py`, `metrics/sumo.py`, CityFlow `engine.cpp:682`): it averages over every vehicle that **entered** the network, counting en-route vehicles at elapsed time, so it is survivorship-free. `average_time_of_journey`, three functions away in the same file with a near-identical name, averages **completed trips only**. Every ATT cell must co-report throughput and entered-count, and a comparison whose entered-counts differ by >5% is reported invalid rather than as a win *(⚠️ superseded: relaxed by A4 under contract C8, then the >5 % clause WITHDRAWN by A5 on 2026-08-07 — it was sound for `entered`, a population size, and unsound for `vehicle_count`, a control outcome. Shared draw ids now carry the guarantee.)* | Under congestion the two metrics diverge in the flattering direction: a policy that gridlocks the network lets only the lucky few finish and posts a better completed-trips average. The plan already knew cologne3 ends with 532 of 536 vehicles still in the network — on that scenario the wrong metric would have been computed over a handful of cars. The suspicion that CityFlow's variant was survivorship-biased turned out to be **wrong** (`count += 1` is present), but checking it is what surfaced the look-alike |
| 2026-08-03 | **D7 — analysis discipline registered:** four confirmatory families only (H1 ladder monotonicity, H2 2×2 interaction, H3 zero-shot vs anchor, H4 context-length trend), Holm–Bonferroni within family, everything else exploratory and labelled; ≥5 training seeds × ≥20 held-out draws fixed in advance with **no optional stopping**; paired Wilcoxon over shared draws; effect sizes mandatory beside every p-value; failed and gridlocked episodes included, never dropped | With three claims and many cells, an uncorrected garden of forking paths would be the easiest thing for a reviewer to attack. H4 is registered as **confirmatory on purpose** so the paper's answer to the DataLight controversy cannot be assembled after the fact. Dropping gridlocked episodes would bias exactly the comparison C1 exists to make |
| 2026-08-07 | **Master-chat error: I merged P2.6 without the mandated independent review, HOURS after commissioning a retro-review of P8.0 for exactly that omission.** The user asked whether P3 and P2.6 had been reviewed; verified from disk rather than memory: **P3 yes** (`docs/reviews/P3.md`, PASS-WITH-FIXES, five blockers, three re-falsified by me before merge), **P2.6 no** — the only occurrence of "review" in its merge message is *"the reviewed **patch**"*, which was me reading a 161-line diff, not an independent review of the task. P2.6 is critical path on §7's own terms: it modified `offline/trajectory_logger.py`, named explicitly in that list, touched a frozen file under authorisation, and produced the corpus P4 trains on | **The failure is not that the gate was forgotten — it is that I diagnosed the exact class that morning and then committed a fresh instance of it that afternoon.** The P8.0 retro-review existed *because* merging critical-path code unreviewed is how the statistics harness reached `main`; I wrote the row, parked the remedy, and then merged P2.6 the same way. **Rules I enforce on others are not self-executing on me, and the review gate is the one step in §7's lifecycle with no mechanical trigger** — every other gate in this project fires from a hook, a test or a guard, while this one fires only from the coordinator remembering. **That asymmetry is the root cause and it is now visible rather than latent.** Scoped retro-review queued as item 0c, deliberately not a whole-task round: the v1.1 logger delta and the C8 guard carry the strongest evidence in the repo (four mutations; an acceptance criterion falsified with its assertion removed), while `att_ladder.py` — whose output is already cited three times here and is destined to be the C1 figure — has none, and the D3 tooling's tests were written after its code by the author's own disclosure. **P4 is unaffected**, because today's separate ruling moved its gate to live paired measurement on held-out draws; what is blocked is citing the C1 ladder as a result |
| 2026-08-11 | **Master-chat error inside the amendment written to remove exactly this latitude: I claimed δ was "derived, not chosen" when only its reference quantity is derived.** The **multiplier of 1.0** is a choice — half the margin, or the margin's own CI width, are equally derivable and give a different verdict on the same data. The user also identified the branch where it bites: **BC landing 0.5 ATT worse than the DT sits inside ±δ and returns "matches", having recovered only 20.2 %** of the DT's margin over its behaviour policy, which A6's own registered reading (*"whatever the DT gained BC gained too"*) is not true of. **Fixed by reporting `recovered_fraction = (MAPPO@1000 − BC) / (MAPPO@1000 − DT)` UNCONDITIONALLY beside the verdict**, rather than gating it on `δ/2` — a gate would introduce a second chosen threshold to defend, and one number is not clutter. A6 annotated in place, before any P4.4 number existed; the `v0.7-prereg-a6` tag predates the clause and a reader following it sees A6 without it | **Registering a threshold does not make it principled — it makes it fixed, which is a different and lesser property, and I conflated the two while writing the document whose whole purpose is that distinction.** The remedy is better than tightening δ would have been: a reported *recovered fraction* is a continuous quantity that carries the information the binary verdict compresses away, so no reader depends on where the line was drawn. **Second ruling the same turn, and structurally the same defect one level up:** the IQL sweep was authorised as *optional, implementer's choice*, which makes the **decision** outcome-driven even though the **selection inside** the sweep is clean — identical in shape to the budget raise. Now required in the plan file **before the first gradient step**, with its reason; either answer is defensible and only the timing matters. **Both were caught by the user in the same message, in a document I had just published as the fix for this class.** |
| 2026-08-11 | **RESOLVED: the "byte-identical weights" claim was tensor-level throughout, and the file-level comparison was STRUCTURALLY IMPOSSIBLE — not failed.** The user asked which of two readings held, since `p4_gate.json` uses a per-checkpoint sha256 as identity. Measured: `recheck_505.pt` vs `dt_seed505.pt` differ at file level (2606827 vs 2607605 B) with **all 48 tensors identical**. ⚠️ **Two causes there, not one** — the `provenance` blocks differ *and* the filenames differ, so the zip root prefix differs too; conflating them would be imprecise in an exchange whose whole subject is that distinction. Then bounded, per the user's suggested method: unzipping the 500-step CUDA pair gives **54 members each, 0 of 54 with a differing payload** — every member's content byte-identical — while the name lists differ **only in their root prefix**, because `torch.save` names the zip root after the output filename (`a_cuda/…` vs `b_cuda/…`). **Two checkpoints saved under different names can never be byte-identical, however deterministic the training.** | **No measurement in the repo contradicts another; the wording was wrong in five places and the mechanism is now a sentence the paper can carry: tensor payloads are deterministic, the archive wrapper is named after its file.** ⚠️ **Corollary that outlives this task, and it qualifies a verification I performed two days ago:** `p4_gate.json`'s per-checkpoint sha256 is a valid identity check **only while each checkpoint keeps its filename**. My 5/5 verification held because `cp` preserved names; a rename, or a re-save under a new name, changes the hash with identical weights. **Any future reproducibility claim must compare tensors, or compare hashes with the filename pinned and said so.** Five dated records annotated rather than rewritten, per the 2026-08-03 live-vs-dated rule — and **one site deliberately left alone**: `docs/returns/P4.md:306` says the 100 held-out *draws* are byte-identical, which is a different claim, is about files that keep their names, and which I verified true myself the day before |
| 2026-08-11 | **Three self-corrections from the user's audit, one of which makes my own sweep look worse than reported.** **(1) I annotated nothing in `docs/returns/P4.md`** — I had recorded the correction only in the plan and believed I had swept. **(2) My grep was too narrow**: searching `byte-identical` missed `byte-identity` at line 317, the primary claim. Grepping the class needs the *stem*, not one spelling — **five sites, not three**. **(3) My worktree size table did not close** (13 + 13 + 250 against `du` 475) because I measured `scenarios/draws` while the worktree's whole `scenarios/` is **440 MB**, and my `find` matched only `*.pt`/`*.pth`/`*.ckpt`. Enumerating properly: `output/` holds `p4_dt`, `p4_probe`, and two disposable CityFlow runtime files. **(4) I committed to `main` without checking `git rev-parse --abbrev-ref HEAD` first**, as §7 requires before *any* commit | **(2) is the sharpest and it is the sweep rule failing on its own author again**: *"fix the class"* is only as good as the pattern, and I chose a spelling instead of a stem — the same shape as replacing the formatted string `"429.67 (Random)"` and missing `429.67 Random` five days ago, which I logged and then repeated. **(3) is the worktree rule failing to be applied to itself** in the very message that introduced it: I described ignored artifacts by what a filtered `find` happened to match rather than by listing them. **(4) did no harm — HEAD was `main` and no implementer was live, and I did verify by effect afterwards, which is the more important half — but the rule exists precisely for the case where nothing looks different and an implementer IS live.** Recorded because a control that only runs when it feels necessary is not a control |
| 2026-08-11 | **Retiring a worktree deletes untracked evidence, and `git` will not warn you — I handed over a `git worktree remove` without asking what it held.** The user checked first and found `output/p4_dt/` (the **five DT checkpoints backing the merged, reported gate**) and `output/p4_probe/` (the determinism-proof artifacts my conditional CUDA ruling rested on). Both secured into the main tree and verified: the five gate checkpoints **5/5 sha256 match** `docs/data/p4_gate.json`; the probe set hashed into `SHA256SUMS.txt`. The 250 MB of `scenarios/draws/` was let go deliberately — proven byte-regenerable the same day. **Rule: before `git worktree remove`, list its ignored artifacts and check each against what the merged results reference.** | **`output/` is gitignored precisely so it stays out of history, which is exactly what makes `git worktree remove` treat it as disposable and say nothing.** The class is general and it had already bitten silently: P2.6's worktree was retired the same way and I did not check it either. Note what made the recovery *checkable* rather than hopeful — `p4_gate.json` records a per-checkpoint sha256, so "these are the models that produced 104.9558" is a verified statement rather than an assumption. **A verified copy beats a determinism argument**: the checkpoints were regenerable in principle, but only at the cost of a retrain and only if determinism held on the same device |
| 2026-08-11 | **Two verifications that closed standing doubts, both prompted by the user's spot-checks rather than by a plan item.** **(1) The in-process and materialised demand routes agree byte-for-byte across the pool boundary.** P4 evaluated on 100 held-out draws while only five are materialised, so 95 were generated in-process and the two routes had never been compared. Measured: draws **0, 1, 1000, 1001, 1004 all MATCH** on `flow.json` sha256, and the source file's own hash still equals what the 2026-08-06 materialisation recorded. **(2) The P4 determinism proof holds at tensor level.** All **48 tensors identical** in both the CPU and the CUDA double-train pairs, and every provenance field equal — while the serialised **file bytes differ**, mechanism unidentified and not guessed at. | **(1) retires the worry behind `DEFERRED` 10 and the P2.6 review's D7** — three demand parameters relayed by module default rather than derived — by demonstrating the defaults produce the recorded bytes on real draws, instead of arguing it from a provenance field. It also confirmed the corpus and the materialised draws point at the **same** hangzhou variant (`18041610`), a mismatch that would have been invisible and fatal. **(2) is a correction to `docs/returns/P4.md` §6.4, which claims "identical losses, tensors and bytes" — the last word is wrong.** ⚠️ **And it is a near-miss of mine worth recording: I inferred what `a_cpu`/`b_cpu`/`a_cuda`/`b_cuda` were from their FILENAMES, hashed the files, saw a mismatch, and was one step from reporting that the CUDA determinism proof had failed.** Reading the packet and comparing tensors is the only reason I did not. **A file hash is evidence about a container; the claim was about weights** — the same distinction as *verify the artifact, not its description*, one layer below where I usually apply it |
| 2026-08-12 | **Every "behaviour policy" number in this project so far is a SEED-AVERAGE, and the DT is beaten by 2 of the 5 behaviour checkpoints it learned from.** Found while reviewing P4.4 for merge; enumerated from the **merged, reviewed** P4 artifacts (`docs/data/p4_gate.json` + `p4_heldout_thresholds.json`), so it depends on no P4.4 code. Per-seed held-out means — **MAPPO@1000** `101:103.6087  202:103.5286  303:107.7980  404:105.9976  505:106.9773` (mean **105.5820**); **MAPPO@500** `101:104.6655  202:108.5122  303:106.7061  404:105.9553  505:114.6189` (mean **108.0916**). The DT's **104.9558** is **ahead of the 5-seed mixture** — the correct reference for *does the model exceed its data*, since it trained on all five seeds' data pooled — and **1.4272 ATT behind the best single behaviour checkpoint** (MAPPO@1000 seed 202, 103.5286), which MAPPO@500's seed 101 also beats it by. **No measurement is wrong and no verdict moves:** the registered gate `ATT_MADT ≤ 1.05 × ATT_best_online` passes under **both** readings (strict threshold `1.05 × 103.5286 = 108.7050` ≥ 104.9558), and **δ = 0.6263 stands exactly as A6 fixed it**, because A6's reference *is* the pooled mixture the DT trained on. What changes is the **sentence**: *"the DT exceeds the policy whose data it learned from"* is true of the mixture and **false of the best member**, and *"wins 100/100 draws vs MAPPO@500"* is a win over a seed-average. **Ruling: every reported behaviour-policy comparison names its reference class from now on, and the paper's tables carry the best-instance column beside the mean.** | **This is the project's signature error one level up — a claim true of an AGGREGATE stated as if true of the POPULATION — and I wrote it myself on 2026-08-08, four days after logging the same class about flow files.** It survived an independent review because the review checked the arithmetic, which was right, and not the reference class, which was never named. **It is also a reviewer-facing vulnerability we would not have survived being asked about**: an offline-RL referee reading *"our DT beats its behaviour policy by 0.6 %"* asks *"which behaviour policy?"*, and *"the mean of five, two of which beat us"* is a materially weaker claim discovered by them rather than disclosed by us. Disclosing both numbers is **strictly better than either sentence alone**, because the interesting result is the pattern it exposes: on multi-modal behaviour data the DT and BC land near the **mixture**, while %BC and IQL recover the **best mode** — which is what P4.4 measured and what makes the mechanism testable rather than a post-hoc story. Corrected at all sites in the same turn, per the backward-propagation rule of 2026-08-07 |
| 2026-08-12 | **A9 REGISTERED (tag `v1.0-prereg-a9`): we stop naming the architecture. And with `poppler-utils` now installed I read OffLight's DATASET COMPOSITION and Fig. 8 first-hand — which corrects a second thing.** **A9:** the model is described functionally as *"an offline multi-agent Decision Transformer for TSC"*; the only named contribution is **probe-calibrated return prompting**; both prior MADTs are cited **where the model is introduced**; `madt` stays an internal arm key under `docs/CONTRACTS.md` **C9**, and **no artifact is regenerated for a string**. **Now the paper, read rather than relayed.** ⚠️ **Table II's dataset is a SIX-CONTROLLER heterogeneous mixture** — verbatim: *"rule-based methods (Fixed Time, Greedy, Max Pressure, SOTL), expert RL controllers (MAPPO), and random policies for suboptimal behavior"* — and **Low/Med/High are demand levels** (*"Each scenario simulates low, medium, and high demand conditions"*). **Their offline algorithms train for 20k timesteps; ours for 40k.** ⚠️ **Fig. 8, read: the mixtures are EXPERT + RANDOM at 33/50/67 %, and the prose names only OffLight, TD3+BC and CQL** — *"OffLight exhibits a more gradual performance decline compared to TD3+BC and CQL"*. **BC is not named in Fig. 8's discussion, so "BC is weakest on 33–67 % expert mixtures" is NOT supported by that figure**; what IS supported, from Table II, is that **BC is weakest among offline methods on a six-controller heterogeneous mixture**. ⚠️ **Third finding, mine, and it cuts AGAINST the inference we were about to draw: their MaxPressure is WORSE THAN THEIR FIXED TIME** in every Jinan and Hangzhou cell (`386.43/409.47/434.91` against `354.95/380.26/407.25`). A MaxPressure that loses to fixed-time is anomalous and points at their implementation or action interval, **so "their Greedy beat their MaxPressure" is NOT evidence that our MaxPressure anchor is weak** | **Verification changed the meaning three times in two turns, and each time in a direction that would have put a wrong sentence in the paper.** First the columns (demand, not composition), then Fig. 8's method set (no BC), now the anchor inference. **The composition finding is the one that pays:** our `mappo1000` tier is a **single-controller pure-expert** corpus and theirs is a **six-controller heterogeneous** one, so the two sit at opposite ends of a **policy-heterogeneity** axis — which is literally what OffLight's method addresses — and P4.6's mixture tiers land on exactly that axis. ⭐ **It also SIMPLIFIES the %BC claim rather than complicating it: the three "different mechanisms" are one mechanism with three instantiations — the top-decile filter selects the best behaviour MODE available, whatever a mode is in that corpus.** Pure expert → the best *seeds* (measured, F3); six-controller mixture → the best *controller*; pure random → the easiest *demand draws*. **One sentence, three testable instantiations, and F3 already measured the first** — a stronger claim than three unrelated mechanisms, and it is what our own data showed. **On the anchor question: adding a stronger heuristic is still right, but not for the reason offered.** Their MaxPressure anomaly kills that argument; the honest reason is that **a referee will ask, it is deterministic, it needs no training and it costs one evaluation pass** — and per §7, tightening before measurement is always safe |
| 2026-08-12 | **OffLight's Table II READ FIRST-HAND, and it does two things at once: it confirms every relayed figure to the cent, and it corrects what the columns MEAN.** Source `arxiv.org/html/2411.06601v3` (the PDF route failed — no `poppler-utils`, no `pymupdf`; recorded so the next reader knows the working route). **Offline rows, ATT, Jinan / Hangzhou / Manhattan × Low / Med / High:** BC `364.50 388.18 409.36 / 382.59 393.38 408.46 / 561.07 595.22 617.59`; TD3+BC `321.87 356.51 364.26 / 324.18 338.52 364.97 / 489.78 495.13 545.11`; CQL `326.06 337.74 372.41 / 369.63 350.06 367.33 / 495.29 524.25 582.87`; OffLight(TD3+BC) `292.90 317.29 316.91 / 288.52 299.59 313.87 / 440.80 423.34 441.54`; OffLight(CQL) `303.24 307.34 324.00 / 325.27 304.55 308.56 / 439.82 456.10 483.78`; **online MAPPO** `263.87 282.86 301.49 / 272.57 280.82 292.72 / 322.99 367.83 402.99`. Reward `−Σ_{l∈L_i} q_l(t)`, action interval **5 s**, episodes **6 min**, networks **12 / 16 / 196** intersections. **BC is worst among the offline methods in all nine cells, and online MAPPO beats every offline method in all nine — confirmed.** ⚠️ **CORRECTION to the framing I was given: Table II's Low/Med/High are DEMAND levels, not expert fractions.** Every method degrades monotonically Low→High, including `Random` (`411.97 → 450.61 → 470.82`), which is a demand signature, not a data-quality one. **So Table II is ONE data-composition point measured across 3 networks × 3 demand levels, not three composition points**; the 33/50/67 % expert axis is in their Fig. 8, which I have **not** read. ⚠️ **And two rule-based rows nobody flagged: `Greedy` scores `287.10 310.08 337.60 / 303.02 307.41 314.72 / 484.01 527.01 575.03` — it beats BC, TD3+BC AND CQL in 7 of 9 cells, and beats OffLight's own best in 1 of 9. Their MaxPressure (`386.43 … 684.59`) is far weaker than their Greedy.** | **Verifying it changed two things and would have changed a third if we had built on the version I was given.** The numbers were right; the **columns were not what they were said to be**, and a registered prediction phrased as *"BC is weakest on 33–67 % expert mixtures, per Table II"* would have cited the wrong table for its own basis — the exact defect this project polices, arriving through a channel that had already been right four times. **The `Greedy` row is the more useful find and it cuts toward humility, not advantage:** in the closest published comparison, **a rule-based heuristic beats the three standard offline baselines in 7 of 9 settings and online MAPPO beats everything everywhere**, so *"offline TSC is not at parity with online"* is the field's current state and our own framing must not imply otherwise. It also **raises the bar on our own anchors**: their Greedy dominates their MaxPressure by 25–100 ATT, and MaxPressure is *our* headline heuristic anchor, so a reviewer may ask why we did not include a stronger one. **Recorded as external evidence, not as our measurement** — none of it enters an artifact, and the route that worked is named so the next reader does not repeat the failed one |
| 2026-08-12 | 🚨 **"MADT" IS NOT AVAILABLE, AND IT WAS TAKEN TWICE BEFORE WE USED IT — one of them peer-reviewed, in our exact method class, since 2021.** Raised by the user from an external analysis; **I verified both, and the situation is worse than the analysis said.** **(1) arXiv:2112.02845, Meng et al., *"Offline Pre-trained Multi-Agent Decision Transformer: One Big Sequence Model Tackles All SMAC Tasks"* — names its model MADT, and is PUBLISHED in *Machine Intelligence Research* (s11633-022-1383-7). Offline + multi-agent + decision transformer is our method class exactly; only the domain differs.** **(2) arXiv:2602.02903, Su, Sun & Deng, 2 Feb 2026 — fetched and confirmed verbatim: *"We introduce MADT (Multi-Agent Decision Transformer)…"*, in TRAFFIC SIGNAL CONTROL.** So the acronym is occupied in our method class **and** in our method class ∩ our domain. ⚠️ **Second finding, independent of the name and arguably worse: 2112.02845 is cited NOWHERE in this repo** — not in §1's prior-art list, not in `PREREGISTRATION.md`, nowhere — while we propose an offline multi-agent decision transformer. Added to §1 today. **Embedding measured before deciding anything:** `madt` appears in 5 source/test files, **3 committed artifacts** (`p4_gate.json`, `p4_secondary_training_draws.json`, `p4_4_baselines.json`), 29× in this plan, 20× in `PREREGISTRATION.md`, and in `CLAUDE.md`'s title | **This stopped being a branding question the moment the 2021 paper turned up: publishing "our MADT" would not be a clash of taste but a plain error, and the kind a referee finds in one search.** ⚠️ **My recommendation is stronger than "rename": stop naming the architecture at all.** Our own §1 already concedes the architecture is **not** the contribution — it records "architecture overlap" with 2602.02903 and DTLight, and the novelty was narrowed on 2026-08-01 to the **target-domain probe**. **Branding an architecture we have already said is not novel invites exactly the criticism we anticipated**, whereas naming the *mechanism* aligns the name with the claim we can defend. **What I do NOT recommend: renaming the artifact keys.** `madt` as a JSON key is an internal identifier in three merged, reviewed artifacts; regenerating them to change a string risks numbers for zero scientific gain, and 2026-08-12's provenance findings showed regeneration is not free. **A documented alias in `docs/CONTRACTS.md` costs one line and carries the same information** — and the distinction between *what a key is called* and *what a model is called* is one this project is already good at. `PREREGISTRATION.md` gets an amendment rather than an edit, so the registered name stays visible |
| 2026-08-12 | **P4.4 MERGED. The fix round closed all seven blocking findings with 14 tests and 11 executed mutations, and no reported number moved — verified by me from the artifact, not from the packet.** Independent checks before merging: every cell mean, paired difference and recovered fraction recomputed from the committed `p4_4_baselines.json` and equal to my pre-round values to **≤1e−11**; suite **612 passed, 1 skipped** run first-hand; `claude_guard.sh --frozen-only`, `check_english.sh`, `check_test_hygiene.sh` all 0; `git diff main...HEAD` over the four merged modules **empty**; and **every number in F3's composition block equal to my own independent computation of the same quantities** — the seed counts, the five per-seed held-out ATTs, `r = −0.9909741850`, and the exact p-value, which I enumerated separately as `0.007225300`. **The mutation that mattered:** `gamma=0.0` at the `train_iql` call site had survived all 58 tests and now fails with *"the loop passed gamma=0.0, so the declared discount is not the one training used"*. **Two disclosures I want on the record because they were volunteered:** the implementer's declared expected-difference list was **right as a set and wrong as a spelling** for the training document, and it **proved mechanically that the 38 added paths were identical between the two documents before correcting the prefix** — so a spelling was fixed rather than a difference excused; and its **own three-point correlation floor refused the first draft of its own F3 test**, which was reported in §12.10 rather than quietly repaired. **Ledger reconciled: 599 → 613 collected, +14 against a plan of 7** — F7 split in two and F3's six were never enumerated in the plan's table | **The round was worth running and the stopping rule now says stop.** It bought a test that kills a mutation which had survived the entire suite and which moves the winning arm by 3.8 × δ; it converted a false docstring into a true one **naming a test I confirmed exists**; and it turned F3 from provenance into a stated result. **A further round would return preference, not defects.** ⚠️ **What I am NOT claiming by merging:** that the DT-versus-baseline comparison is ready for the paper. It is not, and `docs/reviews/P4.4.md` §8.6 binds: **the DT is prompted at a target its own P4 review showed is not its best**, so the sentence *"%BC and IQL beat MADT"* stays out of the manuscript until P4.3 has run. **Merging an artifact and licensing a claim are different acts**, and conflating them is how a provisional number becomes a headline. **The two volunteered disclosures are the reason this merged today rather than after a third round:** a session that reports its own guard refusing its own test has earned the presumption that the things it did not flag are also clean — which is the only currency that makes review cheap enough to keep doing |
| 2026-08-12 | **An artifact that records `git rev-parse HEAD` at write time can never be committed at the commit it records, and ours is additionally UNDER-SPECIFIED for the campaign that produced it.** `BRIEF_12`'s stage-1 byte-identity check **failed**, and the implementer stopped and diagnosed instead of reasoning past it. Recursive comparison over **18,170 leaf paths: 0 removed, 0 added, 1 changed** — `.runtime.git_commit`, `c13aaa9` → `13732f3`, because the packet commit landed between writing and regenerating. It then built the instrument my ruling actually needed — **pre-fix module vs post-fix module at identical cwd, HEAD and inputs → byte-identical `5289e3ba…`** — which isolates the only question stage 1 existed to answer: **this round's source edits move nothing.** ⚠️ **Two things I verified myself and one the implementer did not say.** Verified: the committed artifacts do record `c13aaa9`, and the test the corrected F1 docstring names exists at `tests/test_offline_baselines.py:939`, so that docstring's claim is now true of the artifact. **Not said, and sharper than the fixed-point problem:** `output/p4_4/gate_a.json` carries **`738884b`** while the three `eval_*.json` carry **`c13aaa9`**, so **no single commit produced all the measurements** and the recorded value means *"when the report was assembled"*. **Rulings: the corrected stage-1 instrument is accepted; stage 2 proceeds with a LITERAL DECLARED expected-difference list checked before it runs, and the "only `git_commit` differs" claim must name its sample (this machine, this session); the schema split is `DEFERRED` 39 for P4.3; and §12 must state the input provenance measured from the eval JSONs rather than remembered.** | **My instrument was wrong in a nameable way and it is the SECOND time this class has cost a step: I specified an identity check on a CONTAINER whose contents include a write-time quantity** — precisely `DEFERRED` 29's finding (`torch.save` names the zip root after the output file) one layer up, and the implementer named the connection unprompted. **The general lesson is not about git: any artifact that embeds a description of its own creation cannot be compared for identity across creations, and the comparison must be over CONTENT with the self-describing fields declared as exclusions IN ADVANCE.** In advance is the whole discipline — an expected difference named before the comparison is a specification, the same difference explained afterwards is an excuse, and a packet cannot distinguish them unless the order is visible. **What makes this a good outcome rather than a lost hour:** a check that fails for a reason nobody predicted is worth more than one that passes, because it produced a stronger instrument, a registered schema fix, and a provenance defect (`738884b` vs `c13aaa9`) that no one was looking for and that would have quietly mislabelled every future regeneration |
| 2026-08-12 | **"Was the DT given a fair run?" — ruled, after checking the three facts the question rests on. One of them is FALSE and one is an incommensurable comparison; the third is right and is now registered as A8.** External critique (webchat), relayed by the user. ⚠️ **FACT 1, *"the DT is the only arm known not to have finished learning"*, is FALSE, and the artifact that refutes it is the one the critique is about.** Read from `p4_training.json` and `p4_4_training.json`: **`madt` plateaued 0/5** (final loss 0.0132–0.0244), **`bc` 0/5** (0.0139–0.0189), **`iql` 0/5** (0.8192–1.9324), **`bc_top10` 4/5** (0.0011–0.0032). **Three of the four arms did not plateau; the only one that did is the winner**, and it did so because it trains on 7,200 windows for 355 epochs — on this corpus *plateaued* tracks **training-set size**, not convergence. BC, which *matches* the DT, sits in the same final-loss regime with the same 0/5. ⚠️ **FACT 2's direction is right and its arithmetic is not:** *"at target 0 the DT lands at 102.05, ahead of %BC's 103.16"* compares **one episode to a 500-episode mean**. On that episode `(101, 1000)` the DT's declared-prompt score is **106.4562 against its own arm mean of 104.9558** — the episode is **1.50 ATT worse than typical for the DT** — while MAPPO@1000 scores 102.1594 there against a 105.5820 mean, **3.42 ATT better than typical**. Draw 1000 flatters MAPPO and punishes the DT; extrapolating a 4.4 ATT prompt effect from it to the arm mean is not supported. **The underlying point survives intact and is what matters:** the prompt lever is large, unexplored, and documented before P4.4 existed. **FACT 3** (K=20/3L/1H/d=128 never ablated) is true and is P5.3's registered work. **RULINGS. (a) ACCEPTED and sharpened into `PREREGISTRATION.md` A8(a)** — P4.3's calibration rule must be a declared quantile of the **in-domain probe** return distribution, fixed before any target-domain number, with the sweep as the **ablation around it and never the mechanism that picks it**. **This is the single most valuable sentence in the critique**, and without it P4.3 is a leak wearing a mechanism's name. **(b) REJECTED as stated, replaced by A8(b).** Its premise is the false Fact 1; and "train each arm to its own plateau under a ceiling" **degenerates into a DT-only budget raise in symmetric costume**, because the DT has plateaued at no budget we have tried. A DT raise was refused on 2026-08-08 when the DT had **passed**; granting one now it has **lost** is the mirror image. Registered instead: a **declared-ceiling budget-sensitivity ablation over all four arms as a SECONDARY**, with the 40,000-step results retained as the primary. **(c) ACCEPTED, with the scope sentence corrected** — not *"at a budget where the DT had not plateaued"* but **"at 40,000 steps, at which three of the four arms including BC and IQL had not plateaued, and only %BC had"**, which is the honest version and the one *less* favourable to the DT-was-robbed reading. **A7 written as the user approved it; A7 and A8 are published in the SAME commit, so no tag exists at which A7 is present and A8 absent** | **The critique was right about the thing that mattered and wrong about the thing that would have moved the protocol, which is exactly why every fact got checked instead of weighed.** Fact 1 is the project's own error class arriving from outside — a plausible claim about a population, checkable in one command, believed because it fitted. Had it gone unchecked it would have justified a budget change that a referee could read as *the DT lost, so we retrained it*. **What makes (a) different from (b) is not that one flatters us:** (a) constrains a future measurement and can only make our claim harder to make, so registering it needs no permission and carries no risk; (b) would have altered the criterion of an experiment already run and lost, which needs an argument that survives being stated in the paper — and *"we changed the budget rule after our method lost"* does not, however defensible the rule is in the abstract. **Registering a tighter protocol before measurement is always safe; loosening one after a result never is**, and that asymmetry is the whole decision. **On the DT's standing: it is not broken and nothing here says it is** — it passed its gate, beat its behaviour mixture on 76/100 draws, reached CE 0.013–0.024, and had its sequence alignment independently audited. **Undertrained and mis-prompted is a different claim from broken, and it is measurable**, which is why it becomes P4.3 and an ablation rather than a hedge in the text |
| 2026-08-12 | **P4.4 DELIVERS, AND TWO OF THE THREE BASELINES BEAT OUR DECISION TRANSFORMER. Review PASS-WITH-NOTES; ruled FIX-FIRST on seven items, then merge.** On the `mappo1000` tier of `cf_hz1x1`, 100 registered held-out draws, 5 seeds, 40,000 steps, one evaluation function: **BC 105.1635 — *matches*** (paired diff −0.2077, CI [−0.4690, +0.0536] entirely inside ±δ, p = 0.096, **recovered fraction 0.668**); **%BC 103.1627** and **IQL 103.4764** both land in **A6's unregistered FOURTH branch** (CI entirely *above* +δ), p = 5.1e−17 and 7.1e−15, rank-biserial +0.966 and +0.897, **recovered fractions 3.863 and 3.362**. I recomputed every one of these from the raw records by my own route (≤1e−14). The reviewer **retrained 3 of the 15 checkpoints**, re-rolled 16 cells, rebuilt the 72,000-row transition table by a route sharing no code, and regenerated the artifact **byte-identically — it could not move a single number**. ⚠️ **What it did find is a false COVERAGE claim**: `iql_targets`'s docstring says the bootstrap test *"guards the path training takes"*, and the same mutation at the `train_iql` call site survives all 58 tests; retrained in full it moves IQL by **+2.398 ATT**, and a reversed τ by **+1.081** — 3.8× and 1.7× δ, enough to flip the verdict. **Ruled: F1, F2, F3, F4, F7, F8, F9 blocking (`BRIEF_12`); F5, F6, F10–F12 and MU5 queued as `DEFERRED` 32–37; no reported number may change in the fix round.** **Mechanism, confirmed twice:** %BC's top decile is a **behaviour-seed** selection, not a demand selection — `{202:10, 101:9, 505:1, 303:0, 404:0}`, permutation p = 0.0072, while arrivals are equal (443.6 vs 446.1) and only 4/20 overlap MaxPressure's difficulty ranking; training return predicts held-out ATT at **r = −0.991 across disjoint draw sets**. **Registered forecasts scored:** primary HELD, secondary FAILED | **The pre-registration did its job, and the proof is in the timestamps, not in anyone's word:** `3cf3344` declares δ, the forecast, the IQL sweep refusal *and* the unregistered fourth branch at 22:11; the first number lands at 10:36 the next morning. A result that beats us, produced by a design we fixed twelve hours before seeing it, is worth more than a favourable one we could have steered. **The sweep refusal is the best thing in the packet** — it is argued *analytically* (β appears in no value loss, so the authorised criterion is exactly insensitive to it; selecting β on the policy loss drives β→0, at which point IQL's extraction *is* BC and the arm collapses into its comparator), which is a stronger reason than the budget one I would have accepted. **Why FIX-FIRST rather than merge-and-queue, against the P2.6 precedent:** P2.6's defects were missing tests over correct artifacts; **F1 is a false statement on disk**, in a docstring, about the one constraint `PREREGISTRATION.md` §7 singles out because getting it wrong *"would hand the MADT an unearned win over its own baselines"*. Merging it puts on `main` precisely the artifact-versus-description error this project exists to catch, and P4.3 will edit that file. **F2 is blocking for a different reason:** the arm that beats our headline method is protected by reading. **And the standard for the round is mutation, not passage** — the code is correct, so a new test passes on arrival and proves nothing. **F13 is mine, and it is a constraint on the paper: no DT-versus-baseline sentence enters it until P4.3 has run**, because the DT is prompted at a target P4's own review already showed is not its best (106.46 declared vs 102.05 at target 0, n=1) and that swing is 2.5× the effect we would be reporting. **The result is not a setback — it is the registered pivot arriving with a mechanism attached**: on multi-modal behaviour data BC and the DT land near the *mixture* while %BC and IQL recover the *best mode*, which is one sentence explaining all four arms and which makes P4.3's hypothesis falsifiable rather than decorative |
| 2026-08-08 | **THE P4 GATE PASSES, and the DT slightly exceeds the policy whose data it learned from.** ⚠️ **QUALIFIED 2026-08-12 — see the row above: "the policy whose data it learned from" is the 5-seed MIXTURE, and 2 of those 5 checkpoints beat the DT. The gate verdict is unaffected under either reading.** Verified by the coordinator from `docs/data/p4_gate.json`, not from the report. All 100 registered held-out draws `1000-1099`, every arm paired on the identical draw set: **ATT_MADT 104.9558** against MaxPressure **176.8912** and a threshold of **110.8611** (1.05 × MAPPO@1000's 105.5820); MAPPO@500 108.0916. Wilcoxon paired over the 100 draws: against MaxPressure and MAPPO@500 the statistic is **0.0 with w₊ = 0**, i.e. **the DT wins on 100 of 100 draws** (p = 3.96e-18); against MAPPO@1000 p = 6.34e-07, z = −4.98. Reported at **40,000** steps after the plateau criterion failed 0/5 at the declared 20,000 **and again** after the single pre-declared raise | **C1's central question — does the model exceed its data — is answered positively at 0.6 %, and the claim is stated at that size and no larger.** It trained on the `mappo1000` tier and beat it, on demand it never saw; but **P4.4's BC baseline is what attributes that to sequence modelling rather than to averaging**, and until then the honest reading is *matches its behaviour policy and edges past it*. Two things make the result more credible rather than less: the gate passed with an **under-trained** model (the loss had not plateaued), so it is a lower bound; and the reviewer showed the reported number is **not RTG-optimised** — a better target exists (102.05 at target 0 against the declared 106.46) and was not used. **The registered gate was not renegotiated at any point**, and the brief's original 110.73 sits in the packet as the labelled draws-1-200 secondary exactly as ruled |
| 2026-08-08 | **Three P4 rulings: no second budget raise; RTG-out-of-support is P4.3's material; §6.3 closed by inspection.** **(1) Budget.** The plateau criterion failed 0/5 at 20k, the one pre-declared raise to 40k fired on the training curve alone, and it fails again at 40k. **Ruled: report at 40k and stop.** **(2) RTG.** The reviewer found the model is conditioned **outside its training support for 20.8 % of every episode** — all training RTG are non-positive and evaluation goes positive — and that the declared target is not the best one (**106.46 declared vs 102.05 at target 0**). **(3) §6.3.** `MaxPressureAgent.__init__(self, env)` takes **no tunable parameters** and the module defines no constants; the baseline is parameter-free and is reported as untuned because there is nothing to tune | **(1) It PASSED. Raising a budget after a pass, to improve a passing number, is outcome-driven configuration — the same shape as widening the cologne3 tolerance because the data approached it, which I refused two days ago.** A3 is the precedent: the MAPPO criterion failed twice, we labelled the budget honestly and moved on. And *"the gate passed before the loss plateaued"* is a **stronger** sentence than a plateaued number, because it is a lower bound on the method. **(2) The out-of-support finding is a gift, not a defect**: it is direct evidence, on our own data and before the mechanism exists, that naive return conditioning sits off-distribution *and* leaves performance on the table — which is exactly the motivation for probe-calibrated return prompting, the paper's named method component. Discovering it in P4 rather than asserting it in P4.3 is worth more than the mechanism itself. **(3) Closed by reading the constructor** rather than by scheduling a tuning run — the honest answer to *"was the baseline tuned?"* is sometimes that the question does not apply, and §6.3 requires saying so rather than leaving it blank |
| 2026-08-08 | **The independent review found that P4's ENTIRE ONLINE INFERENCE PATH was unprotected, and each surviving mutation moved the reported number.** Three mutations survived all 58 tests: dropping state normalisation (**+32.5 ATT**), omitting the RTG scale (**+3.8 — most of the margin to the threshold**), zeroing timesteps (+0.7). **No test had ever constructed a `DTAgent` with `stats=`, so the normalisation branch executed zero times.** Closed by a test asserting that what `act()` feeds the model equals what training fed it, exactly, for every step of a real episode; all three mutations now die. All five MAJOR findings fixed, each guarded by a test that fails when the fix is reverted, and **both evaluation artifacts regenerated and diffed bit-identical** — all cells, all 1500 episode records, the gate object and every Wilcoxon result unchanged | **The code was right and the guard did not exist. That is precisely the shape where a plausible wrong number reaches a table, and it was one `stats=` argument away from doing so.** Note what the mutation magnitudes mean: the RTG-scale bug alone would have consumed most of the 5.9-point margin to the threshold, so a gate that passed could have passed for the wrong reason and nothing in 58 tests would have said. **This is the strongest single argument the project has produced for the review gate**, and it lands two days after I merged P2.6 without one — the reviews of P3, P8.0, P2.6 and now P4 have each found something no author self-check did, and P4's find is the first that would have changed a headline number rather than a claim about it |
| 2026-08-08 | **The numbers were right and MY INFERENCE was wrong — and P2.6 had warned me in writing before I drew it.** The P2.6 retro-review recomputed the whole ladder independently (**0 mismatches over 147 cell-fields**, byte-identical regeneration) and then attacked the *comparisons*. `att_ladder.py` reports **unpaired per-cell** CIs, while every claim built on it is a **difference of two cells measured on identical draws** — a paired design. Recomputed paired: cf_hz1x1 both comparisons solid (−71.04 and −69.00, 0 sign flips); cf_grid4x4 mappo1000 −7.16 ± 0.26 solid, mappo500 **+25.21 ± 3.46** solid (±16.38 block-level, 0 flips); cf_cologne3 mappo1000 −22.34 ± 16.06 **marginal** (1/5 blocks flip); **cf_cologne3 mappo500 +16.89 ± 21.94, straddling zero, 3 of 5 blocks flipping sign.** | **The 2026-08-07 row above says the scenario-dependence finding reproduces with "same sign on all three, larger margins". One of the three is noise.** Corrected finding: it holds on **grid4x4**, is **UNDETERMINED on cologne3**, and hz1x1 is solid throughout. **What makes this mine rather than the reviewer's catch is that P2.6 told me first**: its Return Packet said, unprompted, *"non-overlap is suggestive, not a significance test — P8's harness should own the real one"*. I recorded that caveat approvingly in the same turn and then wrote the directional conclusion anyway, into the plan and into `BRIEF_10`. **A disclosed caveat that is filed and then not applied is worse than an undisclosed one, because the paper trail shows we knew.** New shape for this log: not a wrong number and not a wrong quantifier, but **the right numbers with the wrong test** — non-overlapping unpaired intervals read as a result. The registered primary test has been the paired Wilcoxon since 2026-08-03 (§8) and I did not run it before concluding |
| 2026-08-08 | **Seed is PERFECTLY CONFOUNDED with demand block in the corpus, so §8's "both sources of variance are reported" is not satisfiable for any MAPPO tier.** Measured across every tier: the five MAPPO seeds occupy **disjoint contiguous 40-draw blocks** (seed101→1–40 … seed505→161–200). The seed variance component is therefore **not estimable from this corpus at all**, and — the operational half — **MAPPO cells' `std` mixes seed and demand variance while the deterministic baselines' `std` is pure demand variance, so the printed ± is not comparable row to row.** | **RULING: the ladder is DESCRIPTIVE CORPUS STATISTICS; every reported policy comparison comes from the held-out crossed design.** That is not a workaround — it is what `PREREGISTRATION.md` §8 already registers (unit of replication = held-out flow draw, paired across methods, both variance sources reported) and what the 2026-08-07 P4 ruling already installed: all training seeds evaluated on **the same** 100 held-out draws, so seed and draw are **crossed rather than nested**. **P4 is unaffected and the C1 figure survives**, provided it presents the ladder as *what the corpus contains* and never as a policy-ordering result with commensurable error bars. Fixing the confound in the corpus itself would require re-collection with crossed seed × draw, which buys nothing the held-out protocol does not already give. **Recorded as a collection-design lesson for any future campaign: assigning each seed its own draw block makes the two factors inseparable, and it costs nothing at collection time to interleave them** |
| 2026-08-07 | **My P4 brief handed the implementer a gate threshold measured on the TRAINING pool, and the pre-registration had already closed the question.** `BRIEF_10` computed `ATT ≤ 110.73` from `att_ladder_v11.json`, which is **draws 1-200** — the training pool — while `PREREGISTRATION.md` §8 (registered 2026-08-03) fixes the unit of replication as the **held-out flow draw**, paired across methods, ≥20 per cell, and §5/D4 reserves `1000-1099` for exactly that. Caught by the P4 implementer in pre-flight, before a line was written, and escalated as three options. **Ruled: the gate is evaluated on all 100 held-out draws, with DT, MaxPressure and MAPPO@1000 measured live and paired there; both thresholds committed before the first gradient step; 110.73 retained in the packet as the draws-1-200 secondary and explicitly not the verdict.** | **The implementer offered option A — evaluate on 1-200 against the committed ladder — as a live choice, and it was never available: §8 had closed it three days earlier.** That is the *grep-the-plan* rule failing on the person who wrote it, in the artifact where it mattered most, and it is the same shape as the A4 error: **I took a number that existed and used it for a purpose its measurement conditions did not support.** The ladder was built to answer the randomised-draw question and says so in its own `scope` field, which I quoted approvingly two days ago and then read past. **What makes this cheap instead of expensive is that the gate had not been run**: a threshold measured on training demand would have produced a PASS that a reviewer could dismantle in one sentence, and it would have been in the paper. The implementer's own safeguard — measure and commit both thresholds *before* the first gradient step — is adopted verbatim and is stronger than the ruling it protects, because it removes the choice rather than making it |
| 2026-08-07 | **The checklist contradicted `main`, in the opposite direction to the failure I fixed two days earlier — and I caused both.** §6 still showed P3.1–P3.4 as `[ ]` while `offline/dataset.py` was merged, reviewed and on `main`, in a commit whose own subject was *"merge recorded"*. **Swept for the class rather than the four boxes, and it was worse than reported:** P1.4b, P2.1 and P2.2 were also stale-unchecked; **P2.6 had no checklist entry at all** despite a brief, a branch, a Return Packet and a merge; and the 2026-08-06 NON-GOAL was wearing a `- [ ]` checkbox it can never shed, reading as pending work. A mechanical drift check — every Return Packet on `main` must have a ticked box — found exactly three violations (P2.2-draws, P2.6, P3) and is now in §6's header. | **Both failures make §6 unusable as the thing it exists to be, and they are the same defect with opposite signs**: on 2026-08-06 §10 claimed progress that had not happened; here §6 denied work that had. The user read it and believed it, which is the correct thing to do with a checklist and the reason a stale one is a false claim in the most-read part of the document. **Answered plainly in the document rather than in conversation: §6 IS the state.** §8 is why, §10 is what is in flight, §6 is what is done — and a checkbox is inherently a state claim, since there is no way to write `[ ]` that means *"look elsewhere"*. Annotating it with a disclaimer would have been the worse repair. **The fix is ordering, not diligence:** the merge commit ticks the boxes in the same commit, because a follow-up step is a step that gets skipped — which is precisely how this happened while I was writing the Decisions Log row about the very merge. Automation parked as `DEFERRED.md` row 26; until then the one-liner in §6's header is the check |
| 2026-08-07 | **P3 MERGED — the loader every number in the paper flows through, reviewed and re-falsified before merge.** Independent review returned PASS-WITH-FIXES with three blockers; the coordinator promoted two more (P3-04 thin RTG margin, P3-10 untested zero-std guard against 191/640 constant grid4x4 features). All five fixed with mutation proofs, and the three original BLOCKERs **re-verified by the coordinator by mutation** in a provenance-asserted scratch tree rather than read from the packet: P3-01 → 1 failed, P3-02 → 2 failed, P3-03 → 2 failed; scratch restored byte-identical, sha `bbe9d441acc9`. Suite on the merged tree **478 passed**, run by the coordinator with both corpora reachable. | **P3-03 is the finding worth carrying forward, because it was a prediction and the future has now arrived.** The review's fear was that the mixed-version test would begin *passing* at the exact moment v1.1 landed, since the fall-through error message would still satisfy its `match=`. v1.1 has landed and the test still goes red when the guard is deleted. **A falsifiable prediction about a test's future behaviour, checked after that future arrived, is stronger evidence than any static review of the same code.** Second: the rebase onto the v1.1 logger required **no line change in P3** — the supported-versions helper picked up both versions and the ATT accessor picked up the real array, with all 55 existing tests passing unmodified. **That is the C-3 ruling (do not write a second reader) paying out exactly as argued**, and it is among the cheapest wins in the project: a refusal to duplicate forty lines removed an entire class of divergence between two readers of one format |
| 2026-08-07 | **Master-chat error: I took a correct measurement and relayed it with a wider quantifier; the implementer caught it with a matrix.** I told P3 that `att_per_step` is float32 while `att_horizon` computes in float64, so *"any consumer comparing without casting will see a correct corpus fail `==`"*. P2.6's underlying measurement was right; my generalisation was not. Measured on numpy 2.5.1 by P3 and re-verified by me: comparing the stored float32 against a **plain Python float** returns **True**, because NEP 50 treats it as a *weak* scalar and casts it down; whereas passing the stored value through `float()`, through `np.float64`, or into any float64 array comparison returns **False**. P3's first version of that test asserted the raw comparison fails, and **failed against a perfectly correct loader** | **The real shape is more dangerous than the one I described: a consumer is safe by accident on the bare scalar comparison and unsafe the moment the value passes through a cast or an array — so the safe case is the one people try first.** The correction is now a table in the module docstring with a test asserting **every row**, so a future NEP-50 change surfaces as a failure instead of leaving stale guidance. **Nineteenth instance, and a new sub-shape: not a claim measured on a sample and stated of a population, but a claim measured CORRECTLY and then re-stated with a wider quantifier while being relayed between sessions.** The relay is a place where claims mutate and nothing was watching it. Two fixture defects fell out of the same investigation, both found by P3: its ATT sequence ended in a **dyadic** value, hence float32-exact, so the trap could not fire on the horizon element at all; and the premise asserted that *some* value was lossy where **the specific element** mattered — the *any*-versus-*the* quantifier error, inside a fixture built to catch a quantifier-sensitive bug |
| 2026-08-07 | **THE RANDOMISED-DRAW ATT LADDER: both suspended findings REPRODUCE under the registered metric — and the summary that said otherwise was reading the wrong tier.** `docs/data/att_ladder_v11.json`, `att_horizon` over draws 1-200, all 63 within-scenario pairs sharing an identical draw set so every comparison is valid under A5 by construction. **MAPPO@500 vs MaxPressure — the tier the scenario-dependence finding was actually about:** hz1x1 107.50 vs 176.50 (**MAPPO +39.1 %**), grid4x4 192.70 vs 167.49 (**MaxPressure +15.1 %**), cologne3 113.95 vs 97.06 (**MaxPressure +17.4 %**) — against the legacy running-mean's +40.6 / −8.5 / −12.0. **Same sign on all three, larger margins.** ⚠️ **CORRECTED 2026-08-08 — one of the three is noise.** Paired on the same data: grid4x4 mappo500 **+25.21 ± 3.46** holds, but cologne3 mappo500 is **+16.89 ± 21.94, straddling zero with 3 of 5 seed-blocks flipping sign**. The finding holds on grid4x4, is **UNDETERMINED on cologne3**, and hz1x1 is solid. See the 2026-08-08 row. **MAPPO@1000:** 105.46 / 160.33 / 74.73 against 176.50 / 167.49 / 97.06 — MAPPO better by 40.2 / 4.3 / 23.0 %. | **P2.6 reported "MAPPO@1000 wins on all three, so the scenario-dependence finding does not reproduce here", and the relay dropped the "here".** That is true of @1000 and is not an answer about the finding, which was never about @1000 — **a claim about one tier presented as a verdict on a conclusion drawn from another**, i.e. the signature error in its selection form. Corrected before it reached a figure. **What the measurement actually buys is stronger than what was claimed:** two variables changed at once — the aggregation (running mean → horizon) *and* the demand distribution (nominal → randomised 1-200) — and every ordering held its sign. **The metric-artifact hypothesis that motivated the suspension is therefore substantially disfavoured, measured rather than argued.** The NOT SETTLED cells remain open because they are nominal draw-0 *by definition*, but the re-run is now a confirmation rather than a question. Four further readings from the same table: the ladder is **monotone in training budget on all three scenarios** (H1 has support); the **tuned** fixedtime now sits above random everywhere (229.18 vs 266.81 on cologne3, 206.93 vs 257.73 on grid4x4), retiring the untuned-k artifact; **mappo060 remains below random** on grid4x4 (1370 vs 258) and cologne3 (296 vs 267), preserving the sample-inefficiency motivation figure; and the CI widths differ by **87×** between grid4x4 MaxPressure (±0.15) and cologne3 MaxPressure (±13.06), which is the non-determinism above showing up in the statistics exactly where it should |
| 2026-08-07 | **The cologne3 envelope is half spent on its first real run, and the number is recorded now so a future breach reads as expected rather than as a surprise.** Worst observed on the binding condition: tier-mean `total_global_reward` **0.562 %** against the **1 %** tolerance — **56 % of budget**, and **7× the 0.080 % self-noise the tolerance was derived from**. The differing-episode fraction used 7.5 % of 20 %. | **The derivation was sound and the practice is tighter than it implied**, because a single self-comparison measures one draw from a distribution and the gate compares 22 tiers. Not a reason to widen the tolerance — widening a bound because the data approached it is the outcome-driven configuration D7 exists to prevent — but a reason to **record the budget consumed on every run**, so the first breach is diagnosed against a known baseline instead of being argued about. Flagged unprompted by the implementer, in a table, which is why it is here rather than discovered later |
| 2026-08-07 | **Third self-caught instance in three days of the same shape: protective-looking code that could never speak.** P2.6's `envelope_check` ran only when `compare_run` reported no structural error, so condition (i)'s branch — the draw-id match — was **unreachable**, surfacing instead as the generic "filenames differ". Found by a failing test, not by reading. Siblings: the inert `conftest` that removed 0 of 5 meta-path entries while every run reported success, and P3's atomicity spy that patched `builtins.open` while `Path.write_text` reached `io.open` directly — so the test passed under the very mutant it existed to catch | **All three were found by RUNNING the thing rather than trusting the green, and all three were disclosed by their own author.** The family is now well enough attested to name: **a guard whose failure path is unreachable is indistinguishable from a working guard, in every artifact except an executed mutation.** Reading cannot find it, coverage cannot find it, and a passing suite is positive evidence *for* it. This is why mutation is the review standard here and why "the test passes" is not evidence — and it is worth noting that the discipline is now producing self-reports rather than reviewer-catches, which is the cheaper of the two by an order of magnitude |
| 2026-08-07 | **cf_cologne3 is NOT BIT-REPRODUCIBLE, measured with a control — and the v1.1 gate failure is entirely inside that envelope.** `compare_corpora` failed 15/69 tiers and **every failure was cologne3**; hz1x1 22/22 and grid4x4 22/22 bit-identical. The user's reading was that this is engine non-determinism rather than a logger defect, and required it be verified rather than accepted. **Discriminating experiment, same command, same code, twice on one day:** cologne3 **3/6** targeted draws differ, grid4x4 control **0/6**. **Unbiased re-measurement** over draws 1-40: cologne3 self-difference **3/39 = 7.7 %** against the gate's observed **6/200 = 3.0 %**. **The v1.0-vs-v1.1 difference is SMALLER than cologne3's difference from itself.** Crucially the *distribution* is stable where individual episodes are not: tier-mean return **0.080 %** apart, std 21496.4 vs 21499.5, worst single draw 5.7 % | **A logger defect cannot be scenario-selective, and cannot appear between two runs of identical code — so v1.1 is exonerated by construction, not by argument.** The result also converts a known unknown into a quantity: the 2026-08-06 row recorded cologne3's σ=3.32 with *mechanism unidentified*, and we now know it reaches **bit-level trajectory divergence on ~8 % of draws under a fully deterministic policy**. **Consequence for the gate: bit-identity is the wrong acceptance criterion for a scenario that is not bit-reproducible, and passing it by waiver would be worse than failing it.** Replaced by a measured envelope rather than a round number — see the ruling below. **Consequence for C1, and it is good news: the ladder is safe on cologne3**, because tier means move by 0.080 % while episodes move by up to 5.7 %; it is the per-episode analyses, not the rungs, that inherit the noise |
| 2026-08-07 | **P2.6's mixed-version linter test is NOT the P3-03 shape — falsified, not accepted — and the reason generalises.** P2.6 asked, unprompted, whether its D4 test shared the defect the P3 review found, noting honestly that it only knew otherwise because it had mutation-checked rather than designed against P3-03, which it had not read. Verified by the coordinator in a scratch copy with module provenance asserted: **dropping `or self.mixed_versions` from `ok` → RED; neutralising the `mixed_versions` property → RED.** | **The difference is architectural, and that is why it is worth recording.** P3-03 asserted `pytest.raises(match="format version")` — an **error-message shape**, satisfiable by *any* error carrying that substring, which is why it survived the guard's deletion and would have survived v1.1 support. The D4 test asserts a **named structured field** plus exact counts plus the exit code, and no other failure route can satisfy it. **Rule that follows: assert on a named structured outcome, never on the shape of an error message** — a message is a *description* of what happened, a field is the *artifact*, which is this project's founding distinction one layer down. P2.6's linter is strong because it returns a report object instead of raising; **strength by architecture transfers to the next editor, strength by vigilance does not.** |
| 2026-08-07 | **P8.0 RETRO-REVIEW: the quantity is right, the instrument is not.** Commissioned because P8.0 — the statistics harness, named in §7's critical-path list — merged **unreviewed**, and queue item 0 would settle a suspended finding through it. **Extraction VERIFIED by three independent routes:** a live env loop written by the reviewer reproduces `att_horizon`, `att_running_mean` and `final_vehicle_count` on **all 12** committed baseline cells with **0 mismatches**; the shipped harness agrees; and a three-way check against v1.1 `att_per_step[-1]` agrees at 104.375. **Harness FAILS as the settling instrument:** **B1** `rederive_anchors.py` always retrains and never loads a checkpoint, and retraining perturbs ATT by **±5.1 %** against the **+2.5 %** margin it would settle; **B2** `final_completed` (last episode) and `final_vehicle_count` (mean over episodes) are mixed in the `entered` formula, dormant at `episodes=1` and armed at the anchors' own `eval_episodes=3`; **B3** A4's co-report stops at the dataclass and the >5 % rule is implemented nowhere. Mutation: `[-2]` and `np.max` each killed by **one** assertion; "last non-NaN" **survives** and is a proven *equivalent* mutant (CityFlow's metric cannot be NaN) | **The review's own finding is the lesson: the number was right and the net around it was a single assertion behind a `skipif`.** On a machine without CityFlow the suite goes green with **zero** coverage of the paper's primary metric, and the 24 anchor tests are structurally incapable of catching a reader bug — they verify `_aggregate` against a committed file, and all three mutants pass them. Worse, the `np.max` kill is a **fixture coincidence**: on the 360-step production configuration `max == last` on 3 of the 4 reported cells, so the mutant dies only against a 40-step fixture no reported number uses. **This is the discriminating-power rule finding a case one layer below where the rule was written** — not a test that cannot fail, but a test whose ability to fail depends on a fixture shape nobody chose for that reason. **Settling the suspended cells through this harness as it stands would have reproduced the exact error the suspension exists to prevent, with a reviewer-proof paper trail saying we had checked.** |
| 2026-08-07 | **Master-chat error, twice in one hour: the A2 backward sweep I declared "Fixed" was incomplete, and my fix for it was incomplete in the same way.** The P8.0 reviewer found `429.67` — retired by amendment A2 in favour of `413.53` — still **live** in two plan rows, inside brackets presented as the *correct* consistent-aggregation ordering. My sweep had fixed §3.1 line 130 and stopped. Repairing it, I replaced the string `"429.67 (Random)"` and caught **one** of the two, because the other row writes `429.67 Random` without parentheses. Caught only by re-grepping the **number** and classifying every hit | **Fourth instance of a corrective sweep coming up short, and the first where the shortfall recurred inside the correction itself.** The mechanism is now clear enough to state as a procedure: **sweep on the quantity, never on a formatted string, and classify every hit as live-or-dated rather than replacing matches.** A formatted string is a description of the number; the number is the artifact. That is the same distinction the whole verification doctrine rests on, and I violated it while enforcing it — one session after writing the rule that a corrected definition must propagate backward. Recorded because the failure is procedural and general: a sweep that pattern-matches is a sample, and a sample stated as a population is this project's signature error |
| 2026-08-07 | **RULING (user) — a corrected definition must propagate BACKWARD as well as forward, and running the rule once on our own amendments found two live gaps immediately.** New §7 rule: any `PREREGISTRATION.md` amendment that changes what a quantity means requires, in the same turn, a grep of the plan for conclusions resting on the old meaning, each then re-derived or marked `NOT SETTLED`. **Swept the four existing amendments on the spot** — the only honest way to add it, since otherwise the rule would itself have been propagated forward and not backward. **A1:** the backward sweep is what tonight's NOT SETTLED marks are; complete. **A2:** ⚠️ §3.1's aggregation warning still quoted the **retired non-canonical** Random figures (317.46 / 429.67) that A2 replaced with 307.5346 / 413.53 on 2026-08-06 — a day live, in the section §7 designates as the sanity anchor for every later phase. Fixed. **A3:** a budget change, not a definition change; no backward obligation. **A4:** ⚠️ it makes `vehicle_count` at the horizon a **mandatory** companion to every ATT cell with a >5 % validity condition, and **no recorded ATT cell in the plan carries one** — folded into queue item 0 | **The general shape is that we habitually fix the instrument and not the readings already taken with it.** A1 got a re-derivation task (P8.0) and a §3.1 annotation, and both are *forward* acts; nobody asked which recorded conclusions were computed with the old definition, and the costly one was not sitting in a table awaiting re-run but silently steering which scenario gets which expert tier. **A2's survival is the sharper evidence** — an amendment written specifically to retire two numbers, and the numbers stayed in the plan's most load-bearing section, because the amendment edited `PREREGISTRATION.md` and the conclusion lived elsewhere. Amendments and conclusions live in different files, so nothing mechanical connects them; this rule is that connection, and it costs one grep. A4's gap is the cheap kind — the data is already stored, only the reporting was never wired — but an ATT comparison lacking its co-reported count is **invalid by our own registration**, so shipping one would have been a self-inflicted reviewer finding |
| 2026-08-07 | **A conclusion that is currently steering P4/P5 planning may be a metric artifact, and BOTH of us walked past the number that says so.** P2.6 reported `att_horizon` 178.90 against `att_per_step.mean()` 127.88 on one real corpus episode — **40 %**, our own data, our own new field. In the same message I relayed its *smaller* sibling (the float32/float64 casting trap) to P3 and let the 40 % go by; the user caught it. Every MAPPO-vs-MaxPressure comparison we hold is `att_running_mean`, and the margins are **+2.5 % / +5.8 %** (MAPPO@1000) and **−8.5 % / −12.0 %** (MAPPO@500) on grid4x4 and cologne3. Flip condition computed: grid4x4 reverses once MAPPO@1000's horizon/running-mean ratio passes **~1.22**, against MaxPressure's measured **1.19** and MAPPO@60's **2.33**. **All such cells marked NOT SETTLED in §3.1 and at both source rows; recomputation in `att_horizon` is now a precondition for any of them entering a figure, a table or a claim** | **A1 was registered on 2026-08-05 and its consequence was never propagated to the results that already existed.** We treated the aggregation defect as a *metric* problem to be fixed by P8.0's re-derivation of the P0.2 anchors, and did not ask which *already-recorded conclusions* rested on the legacy quantity. The scenario-dependence finding is the expensive one, because it is not sitting in a table waiting to be re-run — it is shaping which scenario gets which expert tier in P4/P5. **The recomputation is cheap and that is exactly why the omission is bad**: `att_per_step[-1]` over `datasets_v11/` is arithmetic, so the only thing that kept this open was nobody asking. Two precisions recorded to stop the correction being over- or under-applied: the **reward-based** normalised-return ladder is a different quantity and stands; and the corpus is draws **1–200** while these figures are **nominal draw 0**, so recomputing over the corpus answers the randomised-draw question and not the nominal one — conflating them would repeat A2 |
| 2026-08-07 | **RULING (user) — pace is now the binding constraint, not correctness, and the evidence is in our own log: nine commits today, zero lines of production code.** Verified on `main` @ `5744387`: `FORMAT_VERSION` still `"1.0"`, `att_per_step` absent from `offline/`, last commit touching `offline/` or `tests/` is `29ab244` (P2.2-draws). Every rule that landed was justified and several caught real defects — the `att_per_step` collision, the false alignment identity, two tautological acceptance criteria — but they landed **while both implementers sat at their gates**. Two §7 rules added tonight at the user's request (**discriminating power**; **verify by effect, not status**), and with them a **moratorium: no further protocol commits until `offline/` moves.** §10 now opens with what has *not* been delivered | **The failure mode is specific and it is mine: process work is legible, feels productive, and is always available, so it silently outcompetes the thing the process exists to protect.** The corpus has existed since 2026-08-06 and the loader is the last artifact between it and a trained DT; every hour of rule-writing is an hour that critical path does not move. Recorded as a Decisions Log row rather than a passing remark because the log is where this project's recurring failures live, and "the coordinator generated protocol instead of product" is now one of them. **The standing filter is sharpened from "does it produce corpus data" to "does the next commit move `offline/` forward", which is checkable in one command rather than arguable.** Note the asymmetry that makes this hard to self-correct: a wrong number gets caught by a guard, a wasted day does not |
| 2026-08-07 | **The test-hygiene guard scans whole CHANGED files, so the first task to touch a pre-guard test file inherits its entire history — P2.6 is being billed for 11 lines it did not write.** `scripts/check_test_hygiene.sh` landed 2026-08-01 (`6729807`); `tests/test_trajectory_logger.py` last landed 2026-07-27 (`535e362`, P1), so it was never scanned. P2.6 touched it to add the v1.1 tests and TH006 fired on **11 pre-existing** `pytest.raises` without `match=` — all 11 present on `main`, none written by P2.6 (verified by counting on `main`, not by reading the diff). Repo-wide exposure measured the same session: **28 such lines across 6 files** (`test_cityflow_env_phase_constraints` 8/8, `test_trajectory_logger` 11/11, `test_sumo_env_phase_constraints` 5/5, `test_phase_control` 2/4, `test_base_traffic_env` 1/2, `test_flow_randomizer` 1/14) | **Ruling: fix, never waive — and the fix is a strengthening, so it does not touch the "never edit a test to make it pass" rule.** Adding `match=` makes each test *able* to distinguish the error it claims to test; the waiver comment would leave it unable to. Ships as its own commit ahead of the D1 tests so the review can see the two apart. **Each regex must come from the message the test actually raises (run it and read it) and must name the specific condition — a key, a field — not merely re-state the error class**, which is the discriminating-power rule added the same night applied to `match=` itself. The remaining 17 lines are parked, to be fixed opportunistically when their file is next touched, rather than swept now: a sweep is exactly the process work the moratorium above exists to stop |
| 2026-08-07 | **SEVENTEENTH instance, and it was the sentence certifying that the corpus is correctly aligned.** §10 and BRIEF_09 §2 both stated `global_reward == -lane_waiting_vehicle_count[1:].sum(axis=1)` **exactly**, "max abs diff 0.0000", as the end-to-end proof of contract C6 on real data. Found by the P3 implementer in pre-flight. Re-measured by the coordinator over **138 episodes, 2 per dataset dir, all 69 dirs, all 7 tiers**: cf_hz1x1 **46/46 exact**, cf_grid4x4 23/46 (max 3.0), cf_cologne3 **0/46** (max 26.0). The original was an **hz1x1-only** measurement — its own "misaligned differs by 16" is hz1x1's signature — stated of the corpus. **Mechanism verified in source, not hypothesised:** `metrics/cityflow.py::_halting_vehicles` counts *every vehicle in the simulation* by speed with no lane filter, while the lane arrays count only vehicles **on lanes**; vehicles inside junctions belong to one population and not the other. Prediction registered before measuring — the residual must be ≤ 0 everywhere, one population being a superset — **confirmed, 0 violations in 49,680 rows** | **C6 survives, but the proof had to be rebuilt, and the replacement is stronger where it matters.** (1) Alignment is now established by a **paired** test rather than an identity: in **138/138 episodes** the aligned residual beats the misaligned one. On cologne3 the identity form has **1.8× discriminating power on the mean** — i.e. effectively none — so an exact-identity check there would have been a test that certifies nothing while looking rigorous. (2) A genuinely exact identity exists and is about the quantity MADT actually consumes: `Σ_j ix{j}_local_reward[t] == -Σ lane_waiting[t+1]` is **bit-exact on 92/92 hz1x1+grid4x4 episodes**, failing on cologne3 for a structural reason (three controlled intersections do not cover every lane of a real network). RTG is built from local rewards, so the replacement check is closer to the risk than the one it replaces. **The general lesson is the one the log keeps re-learning at higher stakes: a verification is itself a claim, and this one was the verification.** Both new forms go to P2.4, which must state its sample or run the population |
| 2026-08-07 | **Action masking never binds anywhere in the corpus — measured, and it changes what P4/P5 may claim.** The P3 implementer measured all 460 per-intersection `avail_mask` streams across 69 sampled episodes: **entirely `True`**. Cause found in source: the corpus was collected under `control_mode: acyclic`, whose `available_actions()` returns every green phase (`envs/phase_control.py:583`), and the env expands any action into a clearance-plus-green plan that fits `delta_time` exactly — so every phase is always legal. BRIEF_09's specified test ("the mask never excludes the taken action") is therefore **satisfied by construction**: §7's tautology rule fires. Replaced by a synthetic mixed-mask fixture for the loader plus the falsifiable population claim *"every stored mask is all-True"*, to be measured over all 4800 episodes | **Masking stays in the model but must not be sold as a result.** It is load-bearing at deployment — the env raises on illegal actions, masks bind under cyclic and RESCO-cyclic modes, and P6's lane-closure perturbations are designed to make them bind — but on *this* corpus a no-mask ablation would measure exactly nothing, and a paper sentence crediting masked-action modelling would be unsupported by our own data. **Second time in two days that a specified acceptance criterion turned out to be a tautology on real data** (the first was BRIEF_08's ATT monotonicity), and both were caught by an implementer in pre-flight rather than by the guard — because `check_test_hygiene.sh` greps tests and both defects lived upstream of any test existing |
| 2026-08-07 | **Master-chat error: I committed three times onto an implementer's branch, and my stated mitigation did not address the mechanism.** After finding the shared working tree I wrote the rule as "no checkout, no `commit -a`, explicit paths only" — which controls *what* is committed and says nothing about *where*. `git checkout -b` by the implementer had already moved HEAD for every session in the directory, so my commits landed on `task/p2.6-format-v11`. **Compounding failure: `git push origin main` from a task branch is a no-op that reports success**, so a push confirmation was indistinguishable from a real one, and the §10 handoff a fresh session was told to restart against was on neither `main` nor the remote. Recovered by the user by cherry-picking onto `main` (`c3d58c5`) through a throwaway worktree, leaving the shared tree untouched | **The correct rule is the one now in §7: while an implementer is live in the tree, the coordinator does not commit; when it must, it does so in a throwaway worktree.** Logged as my error rather than as a git footnote because of the shape: I diagnosed the hazard correctly, wrote a mitigation that felt responsive, and never checked that the mitigation acted on the cause. That is the same defect as citing a `.sumocfg` for a file it references — a control verified by its description instead of by what it does. The push no-op deserves separate emphasis: it is the rarest and worst class we have hit, **a false confirmation that is byte-identical to a true one**, and the only defence is checking the ref rather than the exit code |
| 2026-08-07 | **Two artifacts I own contradicted each other mechanically, and the format was days from being frozen into 4800 files.** BRIEF_08 §2 told the P2.6 implementer to store the v1.1 field as `average_travel_time`; `tests/test_offline_naming_guard.py`, merged by P8.0, is an AST scan over `offline/**` that rejects that name as an attribute, a bound name, a keyword arg or a dict-literal key. **Falsified, not inspected:** running the guard's own `_violations()` against exactly the four shapes in `docs/plans/p2.6.md` §1.1 returns **4 violations**, and the renamed control — still reading `info["average_travel_time"]` — returns **0**. **Ruling: rename to `att_per_step`; the guard is not edited and gets no exception.** Swept the same turn into BRIEF_09 (P3), which had inherited the name | **The guard is not in the way, it is right, and the reason is specific to this array.** `ep.average_travel_time` would be a `(T+1,)` series whose most likely misuse is `.mean()` — which *is* `att_running_mean`, the exact quantity amendment A1 exists to retire, wearing the registered metric's name. That is the P8.0 defect moved one layer down, out of a helper function and into the on-disk format, where a public release and 4800 files would have frozen it. **The implementer would have hit this at its own commit step 3 with two exits: rename, or weaken a merged guard** — and the second is the one a session under pressure takes. A brief that makes weakening a guard the path of least resistance is a defect in the brief. Note the shape: not a false claim this time but an **unchecked interaction between two of my own artifacts**, and the cheap general defence is the same as the 2026-08-03 backticked-path sweep — run the repo's own mechanical checks against a brief's specified code *before* issuing it |
| 2026-08-07 | **The signature error found one layer earlier: in a BRIEF's acceptance criteria, where no guard was looking.** BRIEF_08 specified that `average_travel_time` is "monotone non-decreasing early in an episode". Reasoning presented as fact; **407/407 real episodes falsify it** (earliest drop at row 4 of 361). And it would have shipped **green**, because `FakeTrafficEnv` hardcodes `12.5` and a constant satisfies the property. `check_test_hygiene.sh` greps *tests* and was structurally blind to it. Two §7 rules added: a brief criterion asserting a property of real data must cite its measurement **with its sample** or be marked unverified; and a fixture that satisfies the assertion by construction is a tautology, not a fixture | **Sixteenth instance, and the first upstream of any code.** Every previous instance was caught in an implementation, a commit message or a plan row — all downstream of a specification. This one shows specifications are the same kind of artifact and need the same discipline. **Swept immediately per fix-the-class:** BRIEF_09 carried the identical shape at line 80 — "`avail_mask` never masks the action actually taken", verified by me on **one episode of one tier** and stated of all seven — now cited with its sample and marked for verification. Found by the implementer, in pre-flight, before writing a line |
| 2026-08-06 | **AUTHORISATION C granted (user-confirmed): `agent/MAPPOAgent.py` may persist `_global_metric_keys` in `save()` and assert set-equality in `load()`.** Chosen over asserting in `collect.py` because both consumers converge on `agent.load()` — `runner.py:241` is the `--from-checkpoint` **reporting** path and `collect.py:155` is collection — so an assertion in the collector alone would leave open exactly the path where a silent semantic error becomes a number in a table. Absence-warns / presence-asserts makes it safe mid-phase; the 60 existing checkpoints are migrated by **our** script (a checkpoint is a plain dict, so no frozen edit is needed for that half) | **Documentation does not catch a silent failure — C8 needed to become mechanical.** Three requirements added by the user, each closing a way the fix could be hollow: the error must print the **symmetric difference** so the failure ends an investigation rather than starting one months later; the same-width key-swap case is the **acceptance criterion**, not one test among many, and must **fail with the assertion removed** (a guard that passes its own test when deleted is what `check_test_hygiene.sh` exists for); and the **migration must itself be verified**, since a migration that writes the field wrongly would satisfy the presence check while recording nonsense. The checkpoint witnesses its own migration: `learner["global_feature_dim"]` is stored (=3), so `len(keys) == dim - 2` is checkable with no external source of truth |
| 2026-08-06 | **CONTRACT C8 DISCOVERED THE HARD WAY: a MAPPO checkpoint freezes the env's global metric set.** `_build_global_features` (`MAPPOAgent.py:619-638`) feeds the centralised critic from **every** key in `info["metrics"]`, so `global_feature_dim = 2 + len(metrics)`. Adding `average_travel_time` + `count_of_vehicles_completing_journey` took it 3 → 5 and every MAPPO collection raised. **`act()` builds it too (`:682`), so evaluation breaks identically** — which kills the obvious workaround of a separate ATT evaluation pass. Worse, the guard checks only the **width**: a same-width metric *swap* is silent, and `_global_metric_keys` is not in the checkpoint, so a loaded agent re-freezes from whatever env it is handed | **The metric set is part of MAPPO's MDP, not observability sugar, and nobody had written that down.** Now C8 in `docs/CONTRACTS.md`. The escape is that `info["average_travel_time"]` is **top-level and independent of the requested metric set** (verified live), so ATT can enter the corpus as a first-class logger field without perturbing the critic. **Cost of not knowing: 2400 of 4800 episodes and the entire top of the ladder**, caught only because the user watched a run I had stopped watching |
| 2026-08-06 | **Master-chat error: an inertness check that tested one policy and was generalised to seven.** I verified that adding a metric leaves trajectories byte-identical — on **`random`**, a policy with no network — and concluded "the metric is inert, the new corpus is a pure superset". It is inert for network-free policies (`maxpressure` hz1x1 returned **−18546.510** under both metric sets, identical) and **fatal for MAPPO**. The check was correct and **under-scoped**. The user endorsed it without asking which policies were covered, and asked for that to be logged too | **Fifteenth instance of the signature error, and the first inside a check written specifically to prevent one.** An equivalence or inertness claim is a claim about a *population of cases*; it must enumerate the cases it covers **before** it is run and state which were tested. Sample tested: 1 of 7 behaviour policies. Population claimed: all 7. **New §7 rule.** The endorsement is logged because a second reader who does not ask "which cases?" is not redundancy — both seats made the same omission |
| 2026-08-06 | **RULING (user, option b) — fixed-time gets the tuning budget D5 requires, and tuning OVERTURNS a corpus finding.** Tier 1 was `equal_split` with an untuned `k=4` on grid4x4 and cologne3, while we described it with DataLight's "what a city can hand you" framing — but a city hands you a *tuned* plan. Swept `k` on **draws 1–5 (training pool, never held-out)**, objective `att_horizon`: **cf_hz1x1 k=6** (480 s, interior optimum — k=8 265.5, k=14 291.5), **cf_grid4x4 k=1** (80 s, optimum at the platform's minimum k), **cf_cologne3 k=3** (90 s, sharp interior optimum: k=2 235.3, **k=3 95.7**, k=4 204.2). Cost was minutes, not the projected overnight run | **The untuned tier was a straw man and tuning reverses a headline reading.** On grid4x4 untuned k=4 scores ≈290, *worse than Random* (265.75); tuned k=1 scores **206.09**, properly between MaxPressure (169.05) and Random. **So "uncoordinated fixed-time loses to random on a grid" was a tuning artifact, not a coordination effect** — the paper sentence I was about to write would have been wrong, and the user predicted exactly this. On cologne3 tuning is worth 2× (204→96). **Declared limitation:** we tune cycle length only, not per-phase green splits, so this is the best *equal-split* fixed-time, not the best possible fixed-time. Note also the optima are driven by clearance overhead (`5/(10k)`) under an 8-phase one-green-at-a-time model, which is why hz1x1's optimum is an unrealistically long 480 s cycle |
| 2026-08-06 | **CORPUS COLLECTED — 69 datasets, 4800 episodes, 0.24 GB, 4800/4800 distinct `episode_sha256`.** Seven tiers × three scenarios, 200 distinct draws per tier, `base_seed=1000` throughout so draws match the materialised tree. Reward double-compute verified against raw lane counts by an independent route (max abs diff 0.0000; the misaligned control differs by 16, confirming C6). ⚠️ **CORRECTED 2026-08-07: that verification ran on hz1x1 only and was stated of the corpus — see the 2026-08-07 row. The identity is exact on 46/46 hz1x1 episodes, 23/46 grid4x4, 0/46 cologne3. C6 holds; the proof is now a paired test (138/138) plus an exact local-reward identity (92/92 on hz1x1+grid4x4). Row left standing rather than rewritten, because it is the dated record of what was believed.** **C1 ladder, measured normalised return** (random=0, MaxPressure=100): cf_hz1x1 random 0 → mappo060 37.6 → fixedtime 43.0 → **MP 100** → mappo200 138.9 → mappo500 153.1 → mappo1000 154.7; cf_grid4x4 mappo060 **−1416** → mappo200 −423 → fixedtime −55 → random 0 → mappo500 71 → **MP 100** → mappo1000 113; cf_cologne3 mappo060 −29 → random 0 → mappo200 13 → fixedtime 34 → mappo500 92 → **MP 100** → mappo1000 114 | **The 2026-07-11 decision to define tiers by *measured* return rather than policy name FIRED, exactly as designed:** mappo060 sits below random on two of three scenarios, and a name-based ladder would have shipped a catastrophic tier labelled "medium quality". **P3/P4/P5 are unblocked.** Two caveats to carry into the paper: hz1x1's top two rungs are ~1% apart (153.1 vs 154.7), so **C1 has no expert headroom there** and the three ladders must not be presented as equivalent; and `mappo1000` is **"the best budget we ran", not a converged expert** — the criterion was tested at B=1000 and failed (cologne3 0/5, grid4x4 3/5, hz1x1 4/5) |
| 2026-08-06 | **The ladder is TWO-dimensional: data quality and data coverage are not the same axis, and on grid4x4 they anticorrelate.** Measured state coverage per tier (8 episodes, per-intersection, distinct discretised states per 1k steps): cf_grid4x4 random **775.0** > fixedtime 657.6 > mappo200 640.9 > MaxPressure 584.6 > mappo500 576.0 > mappo1000 543.8 > **mappo060 483.9**. Mean per-feature std runs the other way for the worst tier (mappo060 **1.387** vs mappo1000 0.247) | **Gridlock is high-magnitude, low-diversity data**: everything stuck at high queue, repetitively — so mappo060's 200 draws overstate its information content, and episode count is as misleading for *coverage* as it was for *demand* ("size by draws, not episodes", one axis over). Diversity is **non-monotone in quality**: random explores broadest, and both extremes narrow — gridlock into a congested regime, good policies into an uncongested one. Directly relevant to offline RL, where an expert-only corpus gives a DT a narrow state distribution to fail off. ⚠️ **Coordinator error: I called this "refuted" after reading only the std column, before the distinct-state column existed.** The user's hypothesis was right |
| 2026-08-06 | **`fixedtime`'s schedule source is scenario-dependent, and the corpus provenance is what proved it.** Manifests record `fixed_time_schedule_source=shipped_plan` for cf_hz1x1 (sha `1b0aa65adf80`) and **`equal_split` for cf_grid4x4 and cf_cologne3** — neither ships a `signal_plan_template.txt`. So the "fixed-time is not a straw man" argument holds *as written* only on hangzhou; on grid4x4, where fixedtime measures below random, the plan is **our** equal split with k=4 | A reviewer will assume fixed-time was configured to lose. The honest answer differs per scenario and must be written that way: on hangzhou it is the city's own 30 s/5 s plan; elsewhere it is an equal split across greens, declared, with the platform's enforced clearance. **This is what the P2.5 provenance fields were added for**, and they answered a question three weeks later that no one could otherwise have settled |
| 2026-08-06 | 🚨 **NOT SETTLED — annotated 2026-08-07. The grid4x4 (+2.5 %) and cologne3 (+5.8 %) orderings in this row are inside the metric's own error bar** and may not survive recomputation in `att_horizon`; grid4x4 reverses if MAPPO@1000's horizon/running-mean ratio exceeds ~1.22, against MaxPressure's measured 1.19 and MAPPO@60's 2.33. See §3.1. The row stands as the dated record of what was measured in `att_running_mean`; it may not be cited as a result. **MAPPO@1000 beats MaxPressure on ALL THREE headline scenarios, and is still improving — so the ladder gains a rung instead of a delay.** Running-mean metric, 5 seeds: cf_hz1x1 **90.63** vs 160.56, cf_grid4x4 **138.14** vs 141.65, cf_cologne3 **71.45** vs 75.81. The registered convergence criterion **fails again at B=1000** (cologne3 0/5, grid4x4 3/5, hz1x1 4/5), and correctly so: evaluation improved 5–16% from B=500, so this is genuine continued learning, not window noise. **Criterion NOT weakened.** | **Resolved without spending compute, because §5 defines tiers by *measured normalised return*, not by policy name — "converged" was always provenance metadata, never a claim.** The tier is labelled **MAPPO@1000** and collection proceeds; the ladder now spans four budgets (60/200/500/1000) rather than three, which is strictly better for C1's data-quality axis. Reported honestly, the fact that MAPPO is *still climbing at 1000 episodes* strengthens rather than weakens the paper's sample-inefficiency motivation. Under the 8-week constraint, a further 4.5 h of training buys a label we do not need |
| 2026-08-06 | **Draws PROVEN deterministic: regeneration is byte-identical, so `git_dirty: true` in provenance is now irrelevant rather than merely tolerable.** Re-materialised all 33 draws with `--force` on a clean tree: **all 33 `flow.json` and all 22 `routes.rou.xml` byte-identical**. Draws are therefore a pure function of `(source, base_seed, draw_id)` — the property D4's held-out split entirely rests on. **Two corrections fall out.** (1) The Return Packet's suggested cleanup — re-run with `--force` to obtain `git_dirty: false` — **cannot work**: `--force` reports *kept*, because idempotence compares identity fields and `git_commit`/`git_dirty` are deliberately excluded (`_NON_IDENTITY_FIELDS`, `materialise_draws.py:180`), so a new commit does not invalidate every draw. That exclusion is correct design. (2) My ruling to run it accepted that claim untested | **The flag records the tree state when a draw was *created*, which is what provenance is for**, and it should not be refreshed on read. It said `true` only because the `.gitignore` line was uncommitted at write time — a file that cannot influence demand. **The determinism proof is strictly stronger than a clean flag would have been:** a clean `git_dirty` asserts the commit hash describes the code; the proof shows the *data* is reproducible from the recorded inputs regardless. Ruling: leave provenance untouched, cite the proof. Recorded as a fourteenth verify-the-artifact instance — a trivial-looking cosmetic cleanup was the thing that turned out to be wrong |
| 2026-08-06 | 🚨 **NOT SETTLED — annotated 2026-08-07. The scenario-dependence conclusion this row draws is SUSPENDED**: its grid4x4 (−8.5 %) and cologne3 (−12.0 %) margins are in `att_running_mean`, against a 40 % legacy-vs-horizon gap measured on one real episode (`att_horizon` 178.90 vs `att_per_step.mean()` 127.88, P2.6, 2026-08-07) and a 1.96× policy spread in the ratio on grid4x4. **"The ladder's expert tier is MAPPO on hz1x1 and MaxPressure on the other two" may be a metric artifact and is currently doing work in P4/P5 planning — recompute before building on it.** See §3.1. **HEADLINE RESULT — converged-ish MAPPO BEATS MaxPressure on hangzhou_1x1, and the answer is scenario-dependent.** P2.1 asked whether a learned policy ever exceeds MaxPressure, because §5 makes that the definition of the ladder's top slice. Measured (running-mean metric, 5 seeds, nominal demand), MAPPO across budgets 60 → 200 → 500: cf_hz1x1 **197.48 → 111.72 → 95.41** against MaxPressure **160.56**; cf_grid4x4 **592.25 → 307.34 → 153.69** against **141.65**; cf_cologne3 **144.70 → 126.03 → 84.89** against **75.81** | **The ladder's expert tier is MAPPO on hz1x1 and MaxPressure on the other two** — so §5's fallback ("if no learned policy beats MaxPressure, the top slice IS MaxPressure") applies per scenario rather than globally. Two further gifts: the grid4x4 curve 592 → 154 is the "performance vs interaction budget" motivation figure the plan wants, measured on our own platform at 5 seeds, and it shows the MAPPO@60-below-random result is a *budget* effect rather than a defect. ⚠️ **All figures here are `att_running_mean`, not the registered metric** — `run.py` prints what the frozen runner computes, and the horizon re-evaluation is still owed |
| 2026-08-06 | **The registered convergence criterion FAILS at B = 500 on every scenario; B raised once to 1000 per the pre-declared response** (`PREREGISTRATION.md` amendment **A3**, tag `v0.4-prereg-a3`). Per-seed relative change between consecutive 50-episode training-return windows: cf_cologne3 **0/5** converged (worst 0.95), cf_grid4x4 **0/5** (0.06–0.20), cf_hz1x1 **4/5** (seed 303 at 0.0509). **My own §4 was ambiguous** — it defined the criterion per seed and never said how a scenario aggregates them; resolved strictly (all seeds must converge) | **The value of pre-declaring the response is visible here**: the criterion failed, and the reaction was already written down, so no judgement was exercised after seeing the numbers. The raise reads only training returns — nothing about `att_horizon`, MaxPressure or ladder position entered it, which is what keeps it outside D5. The ambiguity is logged rather than silently resolved because the loose reading would have declared hz1x1 converged and stopped; the strict reading buys more training, which is the conservative error |
| 2026-08-06 | **CityFlow determinism is SCENARIO-DEPENDENT, and our registered claim was measured on a sample.** §3.1 observation 1 states "CityFlow demand is deterministic (MaxPressure σ=0 across engine seeds)". Measured on 5 seeds, 2026-08-06: cf_hz1x1 σ=**0.0000**, cf_grid4x4 σ=**0.0000**, **cf_cologne3 σ=3.3239** (per-seed 72.82 / 73.81 / 73.97 / 82.01 / 76.44). Within a single seed the three eval episodes also differ (non-integer `final_vehicle_count` means). **Mechanism not yet identified** — `laneChange` was the obvious suspect and is REFUTED (`CityFlow/src/engine/engine.cpp:53` defaults it to `false`, and cologne3 merely omits the key). The flow randomiser is not involved: it has zero references in `experiments/**` | **This invalidates a load-bearing operating rule for one scenario.** "Corpus sizing is by DRAWS, not episodes" (2026-07-31) rests on N episodes of a deterministic policy being one trajectory repeated N times — **true on hz1x1 and grid4x4, false on cologne3**, where repeated episodes are genuinely distinct samples. P2.2 must size cologne3 differently, and P2.4's duplicate-`episode_sha256` detector must not treat non-duplication there as a bug. The original claim came from P0.2, which only ever ran hz1x1 and grid4x4 — **thirteenth instance of a claim measured on a sample and stated of the population, this time inside our own §3.1** |
| 2026-08-06 | **A Decisions Log figure is contradicted by a reproducible measurement: cologne3 DOES clear.** The 2026-07-27 row states "cologne3 does not clear within the episode (532 of 536 peak vehicles still in network at t=3600 s), measured under MaxPressure". The 2026-08-06 run, same config (`configs/sim/cityflow_cologne3.json`), same `max_steps=360` × `delta_time=10` = 3600 s, same policy, gives **`final_vehicle_count` ≈ 42–50 across five seeds** — roughly a tenth of the recorded figure | **The old number is not reproducible and the new one is**: the 2026-07-27 measurement lives only in a session scratchpad (never committed — the N5 defect), while this one is in `output/experiments/p2_1_mappo_nominal_060/results.json` from a committed config. Not silently overwritten: the discrepancy is recorded and the old row is **flagged, not deleted**, pending one deliberate re-measurement of peak-concurrent and final counts after the P2.1 runs free the CPU. **Consequence if the new figure stands: cologne3's "RTG conditions on a partially unresolved state" caveat for P3/P4 weakens or disappears**, which changes why cologne3 is in the headline three |
| 2026-08-06 | **Per-episode draw cycling is impossible through the frozen training path, and that threatens C2's 2×2 with a hidden confound.** `_train_agent` constructs one env *before* the episode loop, and CityFlow reads the flow file at engine construction (`engine.cpp:65`; `Engine::reset()` only re-runs the parsed in-memory vector), so a new draw requires a new engine. `mappo_dr` therefore needs a trainer in `offline/` — but then the 2×2's two MAPPO cells would be trained by *different code paths*, confounding the demand axis with an implementation axis | **Resolved the same way P8.0 earned trust in its metric: prove the new path reproduces the old one exactly, then use the new path for both cells.** The draw-cycling trainer must reproduce `_train_agent`'s per-episode returns **bit-exactly on draw 0** before it is used for anything. Tonight's frozen-path `mappo_nominal` run is thereby promoted from "the nominal cell" to "the equivalence reference", so its compute is not wasted either way. Recorded now because the constraint shapes BRIEF_07's output layout — pre-constructing one env per draw and rotating beats ≈11,400 engine constructions across the matrix |
| 2026-08-06 | **N2 CONFIRMED — the float-reduction boundary is real and materially large.** The P8.0 re-run crossed the thread-pin boundary for the first time on record. MaxPressure and Random reproduce the pre-pin 2026-07-09 anchors **bit-exactly** (delta 0.0000, no torch); **MAPPO does not**: cf_hz1x1 197.9104 → **207.9382** (+10.03, +5.1%), cf_grid4x4 632.9525 → **600.7043** (−32.25, −5.1%). BRIEF_06 §3.2 pre-registered that MaxPressure and Random must reproduce **exactly** while **"MAPPO may not"**, and named the exact matmul shape — ⚠️ **but see the correction below: that is a disjunction, not a directional prediction** | **The pre-pin MAPPO anchors are historical and are not reproducible on current code.** A ~5% swing from float reduction order alone, compounded through 60 episodes of training, is far larger than the 5.6e-4 per-matmul difference N2 measured — training amplifies it. Consequence for the paper's reproducibility section: every reported run must be post-pin, and the 2026-07-09 MAPPO figures may be cited only as the historical record that motivated the pin. ⚠️ **CORRECTED 2026-08-07 (P8.0 retro-review, finding on the coordinator): this row claimed the prediction was "confirmed in both sign and magnitude". It was not, and could not be.** BRIEF_06 line 82 reads *"**exactly**; **MAPPO may not**. If MAPPO differs, that is N2 materialising… If MAPPO *does* reproduce exactly, that is equally informative"* — a **disjunction covering both outcomes**, which predicts neither sign nor magnitude and cannot be confirmed by either. The two scenarios then moved in **opposite** directions (+5.1 % hz1x1, −5.1 % grid4x4), which no prediction anticipated. **What survives is real and worth keeping**: the brief did pre-register the exact matmul shape and did declare both outcomes informative before the run, which is good practice. What was false is the later gloss claiming a confirmed directional prediction. **Eighteenth instance of the signature error, and the first about our own claim to have done good science.** |
| 2026-08-06 | **A1's strongest evidence yet: the running mean hides gridlock, and hides it worst on the worst policy.** On cf_grid4x4 the horizon/running-mean ratio is MaxPressure **1.19**, Random **1.28**, MAPPO@60 **2.33**. So MAPPO@60's badness against MaxPressure reads **4.24×** under the running mean and **8.27×** at the horizon | The distortion scales with how *unresolved* the network is at the horizon, and MAPPO@60 is the gridlock case (1040 vehicles stuck). The running mean averages over the early episode when the queue is still filling, so it systematically **compresses the gap between adequate and catastrophic policies** — the exact axis C1's ladder exists to measure. Had we reported the running mean, the headline "online MARL below random at low interaction budget" motivation figure would have understated the effect by roughly half |
| 2026-08-06 | **MAPPO training is PREFIX-EQUIVALENT, measured rather than assumed.** Trained cf_hz1x1 at seed 0 for 60 and for 120 episodes; the first 60 per-episode returns are **exactly equal**. `MAPPOAgent` carries no budget-coupled schedule (its only `epsilon` is a 1e-4 numerical-stability constant in running normalisation; the `epsilon_decay_to_budget` path is DQN's). **Consequence: P2.1's 60/200/converged cadence needs no code** — three runs at one seed give one policy at three budgets, costing 760 episodes instead of 500 | The user's condition was explicit: if prefix equivalence fails, the ladder's three checkpoints are three *unrelated* policies and every C1 "data quality" claim is confounded with run-to-run variance — a claim-level risk, not plumbing. It holds, so the cheap route is also the correct one. **The test was chosen to need no new code:** comparing the returned per-episode training returns of a 60-run against the first 60 of a 120-run tests the parameter trajectory without a snapshot hook |
| 2026-08-06 | **RULING (user) — MAPPO has TWO roles with opposite requirements, and both are built.** **Role A, behaviour policy for ladder tiers 3–4:** trained across ~5 draws from the 1–999 pool, because a single-demand expert yields a corpus with narrow state coverage and MADT cannot learn a regime its data never visited. **Role B, online baseline:** C2's registered 2×2 is {nominal, shift-augmented} × {MADT, MAPPO}, so **MAPPO-nominal (draw 0) is a required CELL, not an alternative.** Training only the multi-draw variant would delete a cell from a design pre-registered three days earlier. Both ship: `mappo_nominal` and `mappo_dr` | **Master-chat error: I posed this as one either/or decision when §1 had already answered half of it.** The 2×2 is frozen in the headline-contribution section and I did not check it before escalating. The draw count and the specific draw ids are themselves now registered quantities — after collection nobody can distinguish "5 was chosen" from "5 was settled on" — so they go in the plan with reasoning before P2.1 runs |
| 2026-08-06 | **PROTOCOL (§7) — grep the plan before escalating a decision.** Before marking anything `DECISION NEEDED`, search `PROJECT_PLAN.md` for an existing answer. If escalating anyway, the message must state where it was checked, e.g. *"I checked §1 and §6; there is no answer there."* | This is *"verify the artifact, not the description"* turned on our own plan. The failure mode is asymmetric and expensive in the user's time rather than the session's: a settled design re-litigated wastes the one resource the coordinator cannot regenerate, and worse, invites a fresh answer that may silently contradict a frozen one. Added after the MAPPO two-roles escalation, where the 2×2 in §1 had settled half the question for three days |
| 2026-08-06 | **A1 confirmed on canonical numbers, and the merge gate held exactly.** P8.0's re-derivation reproduces all four committed baseline anchors **bit-for-bit** under the legacy aggregation, which is what makes the horizon values from the same run trustworthy by construction rather than by assertion. Registered anchors now exist: cf_hz1x1 MaxPressure **247.75** / Random **413.53**; cf_grid4x4 **169.05** / **265.75**. MaxPressure's advantage over Random is **47.8%** (running mean) against **40.1%** (horizon) | A 7.7-point swing produced by nothing but the aggregation, on the two policies that anchor C1's normalisation. **The re-run strategy is what earned the trust:** measuring fresh would have produced horizon numbers with no way to tell a metric change from a pipeline change, whereas reproducing the legacy quantity bit-exactly isolates the one variable that moved |
| 2026-08-06 | **Master-chat error: the illustrative Random figures I put into pre-registration amendment A1 were not canonical.** They came from my own ad-hoc rollout sampling actions from a hand-rolled RNG instead of `experiments.runner._baseline_chooser` — a different realisation of the random policy. Canonical is 307.5346 → 413.53, not 317.46 → 429.67; the stated advantage becomes 47.8% / 40.1% rather than 49.4% / 42.3%. Corrected by **amendment A2**, tagged `v0.3-prereg-a2`; A1's specification is untouched and its documented swing grows from 7.1 to 7.7 points | Twelfth instance, and the sharpest yet on the "different pipeline" axis: **I wrote the ruling that a measurement path must be validated against the reference pipeline before its numbers are used, and then failed to apply it to my own rollout — inside the pre-registration amendment written to prevent exactly that.** MaxPressure was deterministic and therefore accidentally canonical, which is why the error hid: half the table was right. **Standing rule that follows: an illustrative number in a registered document must come from the canonical harness, never from a convenience script** |
| 2026-08-06 | **A1's blast radius reached code already merged, and was found by the implementer who wrote it.** P2.5's `offline/policies/plan_replay.py` returns the episode-mean in a field named `average_travel_time`, and its comment (line 120) calls that *"the canonical measure"* — correctly computed, wrongly labelled, and false the moment A1 landed. The §9 gate asserts on the same quantity. Folded into P8.0 rather than patched separately, with a **naming ruling** that outlives it: no episode-level field may be named bare `average_travel_time`; it is `att_horizon` (registered) or `att_running_mean` (legacy), and a mechanical grep enforces it | **The defect's root cause was a name collision** — `average_travel_time` is simultaneously the frozen registry name of a *per-step* metric and the obvious name for an *episode* number. Renaming is therefore not tidying, it is the fix. Note the failure mode was latent, not active: nothing computed a wrong value, but any later use of `replay_plan` for a reported figure would have silently mixed aggregations. **Caught because the author of the merged code re-read it against a ruling issued after the merge, rather than treating merge as the end of responsibility** |
| 2026-08-06 | **Master-chat error corrected: my own k-ruling row illustrated its point with a number produced by the very mixing A1 forbids.** The row claimed that had k=3 shipped, "Tier 1 would be reported as falling below Random" — from comparing a **horizon** k=3 (309.60) against an **episode-mean** Random anchor (307.53). Under one consistent aggregation Tier 1 is inside the bracket either way: horizon 247.75 < 285.42 (k4) < 300.91 (shipped) < 309.60 (k3) < 413.53 (Random). Corrected in place; the structural ruling is untouched | Eleventh instance of the project's signature error, and the most pointed: **it was committed inside the very Decisions Log row announcing the aggregation defect.** The principle the clause illustrated — that the configuration choice must not follow the outcome — survives; the illustration was wrong, for exactly the reason the amendment exists. Recorded rather than silently repaired, because a reader needs to see that the author of the correction made the same mistake while writing it |
| 2026-08-05 | **The primary metric had an unregistered aggregation, and it distorts POLICY-DEPENDENTLY.** `experiments/runner.py:168,175` samples `info["average_travel_time"]` per step and reports `_mean()` of the samples, so every P0.2 anchor is the episode-mean of a running average rather than average travel time at the horizon. Coordinator's own rollout 2026-08-05: MaxPressure 160.56 episode-mean vs 247.75 horizon (1.54×); Random 317.46 vs 429.67 (1.35×) *(superseded by A2: the canonical Random figures are 307.5346 / 413.53; these came from an ad-hoc rollout, kept as the dated record of the discovery)*. MaxPressure's advantage over Random reads **49.4% or 42.3%** depending purely on the aggregation. Registered as `PREREGISTRATION.md` amendment **A1**, tagged `v0.2-prereg-a1`; §3.1 annotated; re-derivation scheduled as **P8.0** | **A rescaling would have been harmless; a policy-dependent distortion is not, because C1's normalised return, C2's 2×2 interaction and C3's within-backend transfer ratio are all differences-of-differences on this quantity.** The horizon value is additionally the standard TSC reading and the one comparable to DataLight / DTLight / RESCO; the running mean has no standard interpretation and is dominated by the early transient when few vehicles have accumulated travel time. **Found because the P2.5 implementer cross-checked a measurement against the P0.2 anchors, saw a 35% disagreement, and root-caused it instead of adjusting to fit** — the sanity-anchor rule in §7 working exactly as intended, on a defect nobody was looking for |
| 2026-08-05 | **RULING — P2.5 ships `k=4`, and the brief's own tiebreaker was rejected as outcome-driven.** BRIEF_05 said to pick the `k` closer to the shipped plan's ATT. That selects k=3, and it is the wrong criterion: ATT closeness is an **outcome**, and choosing a configuration on an outcome is precisely the degree of freedom D7 registers against. Decided instead on the structural quantity that actually differs and drives the result — **clearance overhead `5/(10k)`**: k=3 16.67%, shipped 14.29%, k=4 12.50%, so k=4 is closer (1.79 pp vs 2.38 pp). Computable before any simulation | The mechanism is clean and was verified monotone: ATT tracks overhead exactly (309.60 / 300.91 / 285.42 on the original readings) while being *anti*-monotone in cycle length, which rules out cycle length as the driver. **The asymmetry is recorded deliberately: had the structural criterion selected k=3, k=3 would ship and Tier 1 would be reported wherever it landed.** *(Corrected 2026-08-06: this clause originally read "would be reported as falling below Random", which was itself an artifact of the very mixing A1 describes — it compared a **horizon** k=3 (309.60) against an **episode-mean** Random anchor (307.53). Under one consistent aggregation Tier 1 sits inside the bracket both ways: horizon 247.75 MaxPressure < 285.42 k4 < 300.91 shipped < 309.60 k3 < 413.53 Random. The principle stands untouched; the illustration was wrong, and it was wrong for exactly the reason the amendment exists.)* "We chose the configuration that made the ladder look like a ladder" is a fair suspicion for a reviewer to hold and a fatal thing to have actually done |
| 2026-08-05 | **The P2.5 validation gate fired on a real defect before any conclusion rested on it.** The §9 gate required the replay harness and the env to agree on a degenerate plan both could express. They disagreed 662.36 vs 1051.25 while in-network vehicle counts matched 250 = 250 — identical dynamics, different **metric definitions**: the engine's native survivorship-biased `get_average_travel_time()` against the env's unbiased `CityFlowMetrics`. Fixed by driving the same metric class over the raw replay engine; agreement is now byte-exact | Without the gate, `k` would have been selected by comparing numbers from two measurement pipelines, and the resulting Tier 1 timing would have been wrong for a reason invisible in every artifact. **This is the double-computation rule applied to a measurement *path* rather than a quantity, and it is the second time in two tasks that a required cross-check caught something nobody suspected** — the first being the P0.2 anchor cross-check that exposed the aggregation gap above |
| 2026-08-04 | **Every hangzhou scenario already ships a real fixed-time plan, and nothing in the repo has ever read it.** `signal_plan_template.txt`, 3600 rows of per-second phase index, **zero references from any Python in the repo** (grepped). Measured structure, identical across variants: phase 0 = 5 s clearance, phases 1–8 = 30 s greens, so **8 × (30 + 5) = a 280 s cycle with an equal split**. P2.5 therefore does not invent a cycle length or a phase split — the scenario supplies the canonical one, which is what makes Tier 1 *deployment-realistic* rather than merely the weakest rung | Found while writing the P2.5 brief, by listing the scenario directory instead of trusting the plan's description of what a fixed-time task would need. **The design decisions the plan flagged as P2.5's hard part — "cycle length, phase split (equal vs scenario-tuned)" — were already answered on disk for 4 of 7 scenarios.** The remaining real difficulty is different and sharper: see the next row |
| 2026-08-04 | **The shipped fixed-time plan cannot be reproduced on the decision grid, and the fix is NOT to change `delta_time`.** `delta_time = 10` in all six `experiments/configs/` files and in `collect.py`'s default; the plan is 30 s green + 5 s clearance = **35 s per phase**, and 35 mod 10 = 5. Holding a green for `k` decision steps under `acyclic` renders `5 s clearance + (10k − 5) s green`, so k=3 gives 25 + 5 (240 s cycle) and k=4 gives 35 + 5 (320 s) — the shipped 30 + 5 / 280 s is unreachable. **k is chosen by measurement** (k=3 and k=4 are equidistant from the shipped plan on both green time and cycle length, so no arithmetic argument settles it), with a `delta_time = 5` faithful run as a sensitivity reference that does **not** enter the corpus | **A tier collected at a different `delta_time` would silently break the ladder**: C1 compares datasets against each other, the DT's context length K counts *decision steps*, and a tier whose step is 5 s instead of 10 s changes the wall-clock horizon K represents without changing any number a reader can see. Constant `delta_time` across the ladder is therefore a corpus invariant, and Tier 1 is documented as an *approximation of* the shipped plan with the deviation stated |
| 2026-08-04 | **RULING (user) — C3 enforces vehicle-parameter PARITY on every parameter both engines expose: `tau`/`headwayTime`, `accel`, `decel`, `maxSpeed`, `length`, `minGap`. Shipped-config runs become a sensitivity check, not the headline.** Three binding conditions: (1) the unmatchable parameters are declared in the paper **by name and in both directions** — CityFlow's `usualPosAcc`/`usualNegAcc` have no SUMO counterpart, SUMO's `sigma` and `speedFactor` distribution have none in CityFlow — and the method section states that C3 measures *"the residual dynamics gap after matching every parameter both engines expose"*, never "the dynamics gap"; (2) cologne gets its own investigation and ruling, not an assumption that one policy fits both; (3) the parity values ship as a **committed config artifact** naming the source of each value, so P7.3 cannot re-derive them from a note | An overclaimed C3 is worse than a narrow one. Unmatched parameters make the measured gap partly a configuration artifact — which is exactly the *"the gap is just interface mismatch"* objection P7.4 exists to answer, so leaving it open would undercut the defence we already planned. Condition 3 is the N5 defect class again: a number whose provenance lives only in a session scratchpad is not reproducible, and a parity table is precisely such a number |
| 2026-08-04 | **RULING — parity is achieved by configuring the SUMO side to match CityFlow, on BOTH hangzhou and cologne. Same policy, but NOT for the naive reason.** Investigated per condition 2: the engines' authorship runs in opposite directions (hangzhou is CityFlow-native with SUMO derived from it; cologne is a natively-authored SUMO scenario whose CityFlow `flow.json` was derived from it — `length` 4.3 and `minGap` 1.5 match to the digit and `maxPosAcc` 2.6 equals SUMO's default `accel`). The deciding fact is **not** authorship: **CityFlow is the training domain and the substrate of the entire C1/C2 corpus, so it must not move.** Changing CityFlow's vehicle parameters to suit C3 would ripple through the ladder and the shift study; SUMO is the target domain and is configured per experiment anyway | **The cost falls on cologne and is declared, not hidden:** matching there overrides a natively-calibrated scenario — SUMO vehicles get capped at CityFlow's 13.39 m/s although cologne's own lanes allow 13.89 and 19.44, and `speedFactor` heterogeneity is switched off (CityFlow has exactly **1** distinct vehicle parameter set across all 2011/2800 entries, so it cannot represent any). Consequence to state in the paper: our parity-run cologne numbers are **not** comparable with published SUMO cologne results; the shipped-config sensitivity run is what restores that comparison. Note also that CityFlow's 13.39 cap sits *below its own lane limits*, so it binds on every road — a conversion artifact, not a modelling choice |
| 2026-08-04 | **RULING — match parameters, never models.** `speedFactor` is a sampling distribution over an already-matched parameter, so parity sets it to deterministic 1.0 (cologne1 ships `normc(1.00,0.10,0.20,2.00)`, cologne3 `speedDev=0.1`). `sigma` (Krauss driver imperfection, SUMO default 0.5) is a **model** parameter with no CityFlow counterpart and is left at its native value and declared unmatchable | Setting `sigma=0` would not align the two engines; it would replace SUMO's car-following model with a different one, and the residual we are trying to measure IS the car-following difference. Removing a sampling distribution over a matched parameter is parity; removing a model term is sabotage of the measurement |
| 2026-08-04 | **C3 landmine corrected by measurement: the prescribed fix was a 0.6% effect, and the real confound is `tau` (headway), which that fix does not touch.** Measured on hangzhou bc-tyc, SUMO 1.27.1, identical network and TLS: as-shipped mean TT 276.45 s → bind `pkw` 278.17 s (**+0.6%**) → bind + `tau=2.0` to match CityFlow's `headwayTime` **415.04 s (+49%, arrivals −23%)**. The shipped `pkw` vType **does not set `tau` either**, so "bind the vType" leaves SUMO at tau 1.0 against CityFlow's 2.0. Also reversed: the **speed** mismatch is a **cologne** problem (lanes to 27.78 m/s vs CityFlow's 13.39 cap, and cologne1 draws `speedFactor` from `normc(...,2.00)`), not a hangzhou one — hangzhou is where speed already agrees | **Had P7.0 run as the plan prescribed, we would have bound the vType, declared the prerequisite satisfied, and carried a ~50% travel-time confound into the headline C3 measurement.** The 2026-07-31 row was right that something was broken and wrong about what; it reasoned from a parameter's presence in a file instead of from what the simulator does with it. Note the magnitudes above are `tripinfo` means over *arrived* vehicles, so by `PREREGISTRATION.md` §3.1's own >5%-population rule they establish direction, not size — the registered rule bit its author's own measurement within a day of being registered |
| 2026-08-04 | **Master-chat spec defect, caught by the implementer with measurement: the G1 fix as I specified it would have reopened the same hole one directory over.** BRIEF_03 told them to carve out `experiments/configs/` as a bare prefix. Measured: `experiments/configs/evil.py` was BLOCKED before the patch and would have been PERMITTED after it — a heredoc `.py` into the configs directory. Tightened to `experiments/configs/[^/]*\.json$`, which additionally makes a new nested config directory fail closed | I designed the G1 fix and introduced a G1-shaped hole in the same clause while doing it. **The general lesson is about carve-outs, not this regex:** an exception written as a bare directory prefix inherits everything the prefix covers, including file types the frozen rule existed to stop. Every future exception is specified by what it *permits*, never by where it lives |
| 2026-08-04 | **Master-chat error: I said two rows would flip against the unpatched guard; three do.** The two G1 rows plus `config_subdir_fail_closed` — a nested config was permitted-by-accident because the old extension anchor never matched the collapsed `experiments/configs/sub/`. I asserted it from reading the regex; the implementer ran it | Tenth instance of the signature error and the cheapest yet to catch — reading a regex tells you what it matches, not what `git status` hands it. **Recorded because the correction came upward:** the implementing seat measured and contradicted the coordinating seat, which is the behaviour the two-session split exists to produce |
| 2026-08-04 | **P0.6 benchmark re-measured: 182.9 s → 48.3 s, ≈3.79× at workers=6** (single session, clean shell, both runs pinned, `experiments/configs/p0_threading_bench.json`). **Deliberately NOT reconciled** with the recorded 199.2 s → 50.2 s ≈3.97×; the docstrings still cite the recorded pair. No unpinned run was attempted — that is the failure P0.5 documents, not a measurement to take | Two honest single-session pairs that disagree by ~5% is what machine-state variation looks like; averaging them, or silently adopting the older one, would recreate exactly the cross-session arithmetic the ban exists for. The timing evidence is now reproducible from the repo, which was the point of N5 |
| 2026-08-04 | **`scripts/claude_guard.sh` has tests for the first time in its life** — `tests/test_claude_guard.py`, 13 rows exercising the real script in a hermetic scratch repo (fresh repo per case, so cross-contamination is impossible by construction rather than by cleanup discipline). 3 rows failed pre-patch, 13 passed post-patch | **The defect was found the moment coverage existed, having survived several close readings of the same regexes.** The mechanism protecting the entire frozen set had zero coverage for its whole life while enforcing coverage discipline on everything else. Standing rule: an enforcement mechanism is code, and untested code is untested regardless of how carefully it was read |
| 2026-08-04 | **RULING — the `experiments/configs/` carve-out is PERMANENT**, unlike the two `FROZEN_EXCEPTIONS` above it, which both carry deletion schedules ("delete once it settles"). New runs need new configs; that need does not expire. **No patch round-trip spent on labelling it**, deliberately: if a future cleanup deletes it, legitimate config creation starts being BLOCKED — loudly, immediately, and fail-closed. A noisy reversible failure does not justify touching a frozen file | Proportionality applied to the project's own stopping rule. The two temporary exceptions are the ones whose deletion is *silent* (they widen access); this one's deletion narrows access and announces itself. Whoever eventually deletes the first two must keep the third — recorded here because that is the moment of risk |
| 2026-08-04 | **Master-chat error: a merge commit message was silently corrupted between authoring and landing.** `git merge -m "..."` with backticks let bash execute two regex fragments as command substitution and drop them from the text. Amended, with the corruption recorded in the amended message rather than quietly repaired | New shape, worth naming: **verification has to extend to the artifact as it lands, not stop at the artifact as authored.** Every commit message in this project carries reasoning that later readers rely on, so a message is an artifact too. Caught only because bash printed its errors above the merge output. Standing fix: compose every non-trivial commit message through a quoted heredoc, never `-m` |
| 2026-08-03 | **Review finding N1 corrected before it could be frozen into a docstring.** N1 warns that a maintainer might make the pin "conditional on `workers > 1` … and would silently reintroduce an unbounded hang". Read from source instead of copied: `run_matrix` builds the `ProcessPoolExecutor` only under `if workers and workers > 1:` (`runner.py:447`), so the fork — and therefore the libgomp hazard — exists **only** on the pooled path. Such a condition would keep the pin exactly where the hazard is. What actually reintroduces the hang is removing the call, moving it later, or adding child-side work ahead of it. BRIEF_03 §5.1 specifies the accurate wording and tells the implementer not to "restore" N1's version | A review is evidence, not an oracle — the same rule this project applies to commit messages and docstrings. Had the imprecise sentence been copied into `runner.py`, it would have become the on-disk explanation of a liveness property, i.e. exactly the artifact future readers trust most. The review itself stays unedited: it is the record of what the reviewer said, and rewriting it would destroy the audit trail |
| 2026-08-03 | **The cross-session-ratio ban was swept incompletely — two files still used 5.80× / 1.37× as LIVE justification.** The 2026-08-03 sweep covered this plan and `docs/patches/README.md` and declared the class fixed. Re-grepped repo-wide: `experiments/runner.py:41-49` (the inline benchmark table) and `tests/test_runner_threading.py:5-9, 184` still present them as the reason the pin exists. Folded into P0.5 and P0.7. `docs/notes/P0.3_spawn_attempt.md` and `docs/plans/P0.3-fix.md` also carry them but are **dated session records** — those get one annotation each, not a rewrite | **Ninth instance, and the first where the corrective sweep itself was the thing that came up short.** "Fix the class, not the sentence" is only as good as the grep's scope: the original sweep looked where the number was *discussed* and not where it was *used*. Rule sharpened: sweep the whole repo, and distinguish a live justification (fix it) from a dated record (annotate it) |
| 2026-08-03 | **§3.1 cited a file that never existed — `docs/p0_baseline_numbers.md`, 0 commits touching that path in `git log --all`.** Cited since 2026-07-09. Found by mechanically checking **every** repo path the plan references, not by reading the sentence. The inline table survives, but the full P0.2 output lives only in `output/experiments/p0_baselines/`, which is **gitignored** — so §7's sanity anchor, the thing every later phase's numbers are compared against, cannot be reproduced from a fresh clone. Folded into P0.6. The same sweep confirmed the three missing `offline/*` paths are correctly described as future work | Eighth instance of the project's signature error, and a new sub-shape: not "a claim from a commit message" but **a claim from a filename**. A path in backticks reads like evidence and is not. Cheap general defence adopted: when touching the plan, resolve every backticked repo path mechanically — it takes one command and it caught this in a document that had been read closely a dozen times |
| 2026-08-03 | **P0.3's real mechanism found, and our own 2026-07-27 conclusion partially retracted.** The fork+libgomp deadlock IS real and reproducible on demand (inert-mutant test: `exit=124` at 120 s / 150 s); the pin prevents it. We had written "not a deadlock / not reproducible / most likely environmental" after failing to reproduce it in one 20-minute window — an over-reach from "not observed" to "does not happen", **the same shape as every other error in this log**. Our own benchmark's `workers=6 unpinned ≥1200 s cap` was probably the hang, mis-recorded as slowness | Seventh instance of sample-vs-population reasoning in this project; this time the sample was one time window |
| 2026-08-03 | **`docs/reviews/` endorsed as a convention.** §7 mandated independent review sub-chats but never gave their output a home, and the P0.3-fix review was one usage-limit event away from being lost permanently (recovered from a session transcript). Reviews are now first-class artifacts: `docs/reviews/<task>.md`, committed | A mandated process step with no durable output is a process step that evaporates |
| 2026-08-03 | **Cross-session ratios are banned from the plan and the paper.** 5.80× / 1.37× (2026-07-27) divided a baseline measured on a different day-state. Corrected to the measured ≈3.97×; worst-case budget ~33 h → **~48 h**. Per "fix the class, not the sentence", sweep **every** ratio in the plan, not only this one | Sixth instance of a number measured once and quoted onward as settled |
| 2026-08-03 | **Reproducibility boundary recorded (reviewer N2):** pinning changes torch's float reduction order — bitwise-different at exactly (128,128)@(128,128), which is MAPPO's `minibatch_size`×`hidden_dim` in `p0_baselines.json`; max abs diff 5.6e-4 at larger shapes. Clean today because nothing recorded predates the pin (no manifests in git). **It stops being clean the moment any run is recorded from an unpinned path.** One sentence owed in P8's reproducibility section and in `PREREGISTRATION.md` | Determinism is a pre-registered property |
| 2026-08-03 | **Claude Code permission semantics established by experiment, not by documentation.** Three facts, each observed as *file contents on disk after the attempt* (never the nested agent's self-report), in an isolated `/tmp` workspace via `claude -p --settings`: (1) **`deny` beats `allow` regardless of specificity** — an explicit `allow: Edit(scripts/check_english.sh)` did NOT survive `deny: Edit(scripts/**)`; the file stayed unmodified while a no-rule control was modified, proving the allow array had loaded. (2) **`Write(...)` rules are inert**, per the CLI's own warning: *"Write(...) is not matched by file permission checks — only Edit(path) rules are"*. (3) **`Edit(path)` rules DO govern the Write tool** — a Write-tool call succeeded under an `Edit(...)` allow and was blocked by an `Edit(...)` deny. `Read(...)` denies are effective (tested separately; not inert). | The entire frozen-set defence rests on these three, and all three were previously assumed. **Fact (3) is load-bearing**: had `Edit(X)` not covered the Write tool, the "cosmetic" dedupe of the ten `Write(...)` rules would have opened ten real holes in the frozen set. First design consequence: a glob deny can never coexist with a permission-level exception |
| 2026-08-03 | **`scripts/` is the least-protected part of the frozen set, and its only protection is circular.** Found by the implementer session. CLAUDE.md freezes `scripts/**`, but the permission system denies exactly one path in it — `Edit(scripts/claude_guard.sh)`. Everything else in `scripts/` is governed solely by `claude_guard.sh`, a script living inside the directory it protects. Compounding fact already on record: the guard reads `git status`, so a clean exit after a commit proves nothing about what was committed. Today's exposure is **prospective, not actual** — `scripts/` contains exactly three files (verified 2026-08-03) and the other two are the intended `FROZEN_EXCEPTIONS`, so no unprotected frozen script exists *right now*; the hole opens the moment a fourth script is added | Circularity is not defence-in-depth. **Remedy ruled the same day — D3, see the next row.** The options were: name each frozen script individually (a no-op today, relies on humans remembering to extend the list) vs a glob (prospective cover, at the cost of making the two tunable scripts human-edit-only). The experiment above proves there is no third option that keeps both |
| 2026-08-03 | **RULING — D3: glob-deny `Edit(scripts/**)`, with NO permission-level exceptions.** Ships as `docs/patches/settings_scripts_glob_deny.patch` (a session cannot apply it; `Edit(.claude/**)` is denied). Same patch removes the ten inert `Write(...)` rules. After it, `check_english.sh` and `check_test_hygiene.sh` are **denied at permission level and permitted at guard level** — intentional, and documented in CLAUDE.md rule 1 together with the escape route (write a patch into `docs/patches/`, never a heredoc). Falsified before shipping, not just reasoned: with this exact deny list, `scripts/brand_new.sh` — a file with no individual rule — was DENIED and unmodified on disk, while a no-rule control was modified in the same run | **The asymmetry is the reason, and it is the same trade §7 already made when it chose a mechanical grep over a remembered rule.** D3 buys *prospective, mechanical* cover on every script that does not exist yet; it pays in *bounded, self-terminating* friction on two temporary files — those scripts have needed editing about twice in the project's life, at ~2 min per patch round-trip, and the guard's own comments already schedule both exceptions for deletion once they settle. D2's gap is unbounded and opens at the exact moment someone is focused on something else. **Counter-argument weighed and rejected as low, but made visible rather than trusted: a check that stops improving because of friction is worse than the friction — so if either script goes untouched-but-wanting-a-rule *because* of the patch route, that is a signal to reopen this choice, not to absorb it** |
| 2026-08-03 | **Master-chat error: the "Write→Edit" ruling closed zero holes.** I ruled that "paths with only a `Write` rule were never protected" and ordered a fix. Re-derived from the actual list: all ten `Write(...)` deny rules duplicate an existing `Edit(...)` rule, **the Write-only set is empty**, and the four Edit-only paths (`agent/base.py`, `agent/utils/utils.py`, `agent/MAPPOAgent.py`, `rewards.py`) were protected all along, because `Edit(path)` governs the Write tool. I reasoned from the CLI's warning text instead of reading the list the warning was about | Same shape as every other error in this log: a claim about a population (*the rules*) asserted from a description (*the warning*) instead of the artifact (*the list*). The dedupe is still worth doing — ten inert rules that look protective are a trap for the next reader — but it is **cosmetic**, and shipping it as a security fix would have been false |
| 2026-08-01 | **Reward choice (queue_length) empirically vindicated by DataLight's own ablation.** Their Table 3, offline TSC, ATT lower-is-better: pressure reward 369.63 / 373.04 / 355.23 vs queue-length reward 251.48 / 230.65 / 312.07 (JN1/JN2/HZ2, ROD). Pressure is dramatically worse as an *offline* reward. Note DTLight uses pressure (r = −p) and is the one that collapses on Grid 4×4. Cite Table 3 when justifying our primary reward — the decision now has external empirical support, not just our RTG-alignment argument | Read from source 2026-08-01 |
| 2026-08-01 | **Fixed-time (P2.5) has real-world justification, not just ladder completeness.** DataLight builds an entire case study (COD, "cyclical offline data") on FixedTime precisely because "in real-world TSC scenarios, FixedTime stands out as the most commonly employed method" — i.e. cyclical logs are what an actual city can hand you. Their offline models trained on COD beat BC/BCQ/CQL by 8.5–26.7%. Our Tier 1 should be framed the same way: the most deployment-realistic data source, not the weakest rung | Strengthens the P2.5 brief's motivation |
| 2026-08-01 | **DTLight's stated future work is our P5.1.** They close with: "we plan to explore better coordination between multiple DTLight agents in offline settings" — their multi-agent setup is *independent* DT agents plus discounted neighbour states, no learned spatial mixing. Our graph-attention layer over the road network occupies exactly the gap they named. Quote that sentence in related work | Positioning gift; also confirms 2602.02903 is the competitor to watch, not DTLight |
| 2026-07-31 | ~~**C3 LANDMINE — hangzhou vType is not bound in the shipped `.rou.xml`.**~~ — **SUPERSEDED 2026-08-04 BY MEASUREMENT. Kept, not deleted: the wrong version is the argument for measuring a prerequisite instead of reasoning about it.** Its `<vehicle>` elements carry no `type` attribute, so SUMO runs them under `DEFAULT_VEHTYPE` (maxSpeed 55.55 m/s) while CityFlow uses the flow file's 11.11 m/s. A naive CityFlow→SUMO comparison on hangzhou would therefore measure a **5× speed-limit difference and attribute it to the simulator dynamics gap**. `render_sumo` faithfully mirrors the shipped file (correct behaviour), so this is not a P2.0 defect — it is a P7 prerequisite: **before any transfer experiment, bind vType explicitly and verify effective vehicle parameters match across backends** | Found by the P2.0 reviewer 2026-07-31. ⚠️ **PARTIALLY REFUTED AND SUPERSEDED 2026-08-04 — see the next row and `docs/notes/P7.0_vtype_investigation.md`.** The premise (vType defined, bound to 0 of 2021 vehicles) is correct and was re-verified through traci; the *consequence* is not — measured, binding the vType moves travel time by **0.6%**, because every hangzhou lane is capped at or below CityFlow's 11.11 m/s and `speedFactor` is exactly 1.0, so a 55.55 ceiling can never bind |
| 2026-07-31 | **Corpus sizing is by DRAWS, not episodes** — with demand fixed the engine seed changes nothing observable (root cause proven in CityFlow C++: `engine.cpp:65` reads the flow file in the constructor; `Engine::reset()` only calls `flow.reset()` on the parsed in-memory vector). Deterministic policies need one episode per draw; stochastic ones may reuse a draw | Measured end-to-end 2026-07-31: nominal 3 episodes → 1 distinct hash; 3 draws → 3 distinct hashes |
| 2026-07-31 | **cologne's shipped `flow.json` and `.rou.xml` are NOT index-aligned** (cologne1 2011 vs 2015 vehicles, cologne3 2800 vs 2856, plus the 25200 s offset). Harmless for us — we render OUR drawn demand to both formats, so C3 compares identical demand — but any comparison against published/shipped cologne numbers must not assume the two files describe the same vehicle population. hangzhou IS 1:1 (2021/2021, routes identical by index), which is why the identity-mapping test uses it | Found during P2.0; recorded so C3 does not trip over it |
| 2026-07-27 | **Scenario pairing corrected: 6 paired, not 7. grid4x4 is CityFlow-only** (`.sumocfg` points at a non-existent `grid4x4.rou.xml`; no `.net.xml`). C3's dynamics-shift study runs on hangzhou_1x1 ×4 + cologne1 + cologne3. grid4x4 remains the 16-intersection coordination scenario for C1/C2 | Verified by file inventory 2026-07-27; the earlier "≥7 paired" claim came from the presence of a `.sumocfg`, not from checking the files it references |
| 2026-07-27 | **P0.3 root cause found — it was never a deadlock.** Benchmark: unpinned workers=3 = 0.29× (3.4× slower than sequential); pinned workers=6 = 5.80×; pinned workers=1 = 1.37×. The July "frozen terminal, 10% CPU" was torch thread oversubscription (48–96 OMP threads on 16 cores), not a fork lock. faulthandler dump showed workers mid forward/backward, not blocked. Fix is `torch.set_num_threads(1)`, a one-liner, not the spawn patch. My fabricated "8× parallel" budget assumption was wrong in both magnitude AND direction (unpinned parallelism is *slower*) | Found only because the user asked "are you sure parallel even works?" — the single most valuable question of the session |
| 2026-07-27 | **Master-chat error corrected: P0.3 was wrongly ticked off.** It was marked done from the commit message `aa4a2c4` ("record failed spawn attempt") without checking the code — the commit recorded a FAILURE. `runner.py:405` still uses default `fork`. Parallel execution has never worked. Lesson applied: verify the artifact, never the description of the artifact — the same rule already imposed on sub-chats applies to the Master chat | Found because the user questioned whether the fix had actually landed |
| 2026-07-27 | **Free demand-level axis discovered in the four hz1x1 variants:** trips 743 / 1417 / 1671 / 2021, peak concurrent 33 / 55 / 136 / 212 — genuinely different intersections AND demand levels, not seed re-rolls. Gives C2 a natural train-on-one-demand / test-on-another axis at zero extra cost | Measured 2026-07-27; not previously noticed |
| 2026-07-27 | **cologne3 does not clear within the episode** (532 of 536 peak vehicles still in network at t=3600s); hz1x1#1 and #4 likewise (208/212, 132/136). Episode boundaries there fall mid-congestion, so RTG on those scenarios conditions on a partially-unresolved state — note for P3/P4, and a reason cologne3 is the interesting stress case rather than a defect | Measured 2026-07-27 under MaxPressure |
| 2026-07-27 | **DETERMINISM EMPIRICALLY CONFIRMED on real CityFlow.** First live collection run: engine seeds 1000 and 1001 produced **byte-identical trajectories**, identical `episode_sha256`, identical return (−502.000). Predicted 2026-07-11 from P0.2's σ=0; now observed. **P2.0 FlowRandomizer is therefore blocking, not a refinement** — without it N episodes of a deterministic policy is one trajectory × N, and the manifest truthfully reports N. Also makes out-dir collision traceless (an overwritten file is identical) | The `episode_sha256` alarm specified in Brief #1 §8 fired on its first real run, exactly as designed |
| 2026-07-27 | **P2 collection config decided:** collect with `--local-reward-fn queue_length --global-reward-weight 0.0`. At the default weight 1.0 every intersection's `local_reward` silently carries the FULL global reward (`base_traffic_env.py:214`) — the composite double-counting trap. `global_reward` is unaffected by the weight, so both clean signals get logged | Surfaced by the first real collection run: `local_reward` came back all-NaN because `local_reward_fn` defaults to None. MADT needs per-intersection RTG |
| 2026-07-27 | **Mutation testing adopted as the review standard for critical-path code** (§7) | The P1 re-review ran 13 code mutations against an isolated copy: mutating `ix_ids` to `sorted()` killed 5 tests (previously 0), and reverting the float64 accumulator made a previously-passing test fail by 3.31. Falsification proved test strength in a way reading never could |
| 2026-07-27 | **SUMO lane-array concern resolved:** `SumoEnv._create_metrics()` always returns a `SumoMetrics` object (never `None`), same for CityFlow, so the empty-lane branch is unreachable there. `_require_lane_arrays` retained as defence-in-depth (MOSS may differ). **P7.0 SUMO collection needs no special metrics handling** | Verified in source 2026-07-27; reviewer's reading was right, Master chat's earlier reading was half-right |
| 2026-07-26 | **`terminated` is hardcoded `False`** (`base_traffic_env.py:604`); every episode ends by time-limit truncation at `max_steps`. Consequence for P4.4: value-based offline baselines (IQL/CQL) **must bootstrap through the boundary**, never treat it as absorbing — treating a timeout as terminal causes systematic value underestimation near episode end and would hand the MADT an unearned win over its own baselines. DT/RTG is unaffected (no bootstrapping, and all episodes share one horizon, so RTG is comparable across the corpus) | Verified 2026-07-26 from the P1 plan's open question; a fair-baseline defect here would be a rejection-level flaw in C1 |
| 2026-07-26 | **`local_reward` in `info` is composite, not purely local:** `_get_local_rewards()` returns `global_reward_weight * global_reward + local_reward_fn(local_metrics[j])`. P3/P4 must not condition on both a per-intersection RTG and a separate global RTG without accounting for the overlap, or the global signal is double-counted | Found in Master-chat verification of the P1 plan; not flagged by the sub-chat |
| 2026-07-26 | **`ix_ids` stored in `env.intersections` order, NOT lexicographic** (Brief #1 v2 said sorted — that was wrong and is hereby corrected). C1 orders the action vector by `env.intersections`, so native order gives `ix{i}_action ≡ action[i]` with no remap table. **P3 loader must key by intersection ID, never by positional index** — CityFlow and SUMO may enumerate in different orders, and positional pairing would silently mismatch intersections in C3 | Eliminating a remap eliminates a whole off-by-one class; `lane_ids` stays lexicographic (frozen at reset) |
| 2026-07-26 | **`collect.py` hard-fails when `L == 0`** (lane arrays empty). SUMO/MOSS return `{}` lane dicts when the metrics pipeline is off (`sumo_env.py:301`, `moss_env.py:653`); CityFlow falls back to a direct engine query and is always populated. The logger records `L=0` honestly (correct layer); the collector refuses to build a corpus that silently violates C6 reward-agnosticism | P7.0 needs SUMO collection specifically; discovering an unusable corpus at P3 costs an overnight run |
| 2026-07-26 | **`utils/` discovered as a frozen dependency of `envs/` (imported by all four backend env files) and added to the frozen set, along with `scripts/**` and `.claude/**` for the same reason — a guard must not be able to unfreeze itself.** One dated exception: `scripts/check_english.sh` stays writable while being tuned, encoded as `FROZEN_EXCEPTIONS` in the guard rather than left as a silent gap | Not enumerated in the original brief; found via `/init`'s repo scan and a grep of the import graph before P1 started |
| 2026-07-25 | **Brief #1 consolidated into a single v2 document**; Addendum A, ADDENDUM_A_PATCH and BRIEF_01_DELTA all superseded and retired | Four documents that disagree in places are how an off-by-one gets frozen into a data format; one source of truth per task is now the standard for every brief |
| 2026-07-25 | **Format alignment fixed and promoted into contract C6 (v1.1):** observations T+1 rows, decisions/outcomes T rows; `local_reward` read at `on_step_result` (post-step), never bundled with the pre-decision observation; uniform "every info recorded once on arrival" write rule replaces any terminal-row special case | Verified in `envs/base_traffic_env.py::step()`: simulation advances BEFORE reward and info are built, so `r_t` is recoverable only from row `t+1` and `info[...]["reward"]` from `step t` is `r_t`. External critique caught the missing final row; the `local_reward` off-by-one (worse — it shifts *every* per-intersection RTG, not just the last) was found in Master-chat verification |
| 2026-07-25 | **Fixed-time split out as P2.5** rather than added to Brief #1 (external critique flagged it as a BLOCKER for Brief #1 — accepted as a gap, rejected as scope) | No fixed-time controller exists in the repo, so this is a policy implementation with real design decisions, not a CLI flag; adding it would break §7's ≤2-file brief limit. `--policy` becomes a registry now so P2.5 needs no CLI change |
| 2026-07-25 | **Claude Code guard hook split into `--frozen-only` (fires after Bash too) and `--tests-only` (file edits only)** | Original matcher covered only the file-edit tools, so a bash heredoc write to a frozen file would go undetected until the next file edit; adding Bash to the single hook would have run pytest after every `ls`, since the test gate cannot early-exit while a task has changed files |
| 2026-07-13 | **P11 stretch-goals phase created** (transfer triangle w/ MOSS, reverse-direction ablation, city-scale demo, LLM-teacher); P9 folded into it | Attractive ideas kept visible but firewalled from the critical path — top risk is time exhaustion, so "nice to have" must never silently compete with load-bearing work |
| 2026-07-12 | Mentor gave free hand on positioning; controversy framing retained as *scientific* motivation (a genuine open question in the literature), not marketing; credit/citation deferred to P10 by mentor (non-blocking) | "Good science first" is compatible with adjudicating a real contradiction |
| 2026-07-12 | **Protocol hardened for AI-written code:** task branches, two-Claude rule (independent review sub-chats on critical path), double computation of critical quantities, corpus linter with duplicate detection, self-review checklist, ≤2-file brief limit, Claude Code as preferred coding mode | Dominant risk of AI-heavy development = silent semantic bug in confident code; redundancy is cheaper than a retracted result |
| 2026-07-11 | **P10.0 release package added** (datasheet, license, Zenodo DOI) + license audit pulled into P2.3 — critique (g) accepted, extended with source-scenario redistribution audit | A promised public dataset without redistribution rights is worse than no promise |
| 2026-07-10 | **Paper hook = adjudicating the literature controversy:** DataLight says DT cannot work for TSC; DTLight and 2602.02903 say it does. Our ladder + shift + calibration explain when and why. | A published negative result on our core method is a framing gift, not a threat — provided we engage it head-on |

---

## 9. Risk Register

| Risk | Likelihood | Mitigation |
|------|-----------|------------|
| DT underperforms online MAPPO even on 1×1 | Med | P4 gate; remedies: richer behavior mix, longer context K, %BC-style filtering of top-return trajectories |
| CityFlow/SUMO state features misaligned → C3 collapses | Med | P7.0 early gate (1 day, right after P1) measures the shift before any transfer investment; feature-set discipline in P7.1; C3 reframed as transfer curve → no binary failure mode |
| RTG prompting miscalibrated across backends (reward-scale shift) | Med | P7.2 calibration protocol (MaxPressure-relative or quantile-space prompting); if unsolvable, becomes the characterized-limitation finding |
| **Pure-offline DT collapses on 16-intersection grids — PRIOR EVIDENCE, not speculation.** DTLight's own Table 1: DTLight-s (offline-only) on Grid 4×4 scores **446.8 ± 128.0** from EMP data and **61.2 ± 27.3** from IDQN data, against behaviour policies at 48.39 and 37.81 — i.e. far *worse* than the data it learned from, with enormous variance. Only online fine-tuning recovers it (49.6, 26.7). Same pattern on 8-Inter with weak data (66.1 ± 39.3 vs 28.2). **This is precisely our C1 target scenario (grid4x4, 16 ix) and our headline claim's hardest case** | **High** | This is why the ladder (C1) and the spatial mixing layer (P5.1) exist — the failure correlates with data quality and with intersection count, both of which we vary deliberately. P4.2's gate is on 1×1 where DT demonstrably works; expect the real fight at P5.2. If MADT also collapses on grid4x4, the honest paper reports *when* offline sequence modeling stops working and why — still publishable, and pre-registered as an RQ rather than a promise |
| ESPER/infeasible-RTG failure under scenario shift (return-conditioning erratic in stochastic regimes) — plausibly why DataLight's DT failed | Med-High | Probe-calibrated prompting (P4.3) tested under shift in P6.3; naive-vs-calibrated ablation makes even the failure mode a finding |
| BC-on-expert matches MADT → sequence modeling adds nothing | Med | Tested early at P4.4 on 1×1; if true, pivot headline weight to ladder + shift + calibration findings (which BC cannot deliver) rather than architecture |
| **Unbounded hang if any future code path reaches `run_cell`'s work without passing the pin** — it does not get a slow run, it gets a silent wedge with no failure message, freezing both the suite and the PostToolUse guard. `run_matrix` has no timeout and `experiments/` is frozen | Med | P0.5 documents the liveness role in the file itself; P0.7 bounds the test. Any P2.2/P5 script importing `experiments.runner` inherits a process-global pin as a side effect of calling a function — brief them explicitly |
| **A Claude Code upgrade silently changes permission semantics.** The entire frozen-set defence rests on three facts established **by experiment on v2.1.220** (Decisions Log 2026-08-03): deny beats allow regardless of specificity, `Write(...)` rules are inert, and `Edit(path)` governs the Write tool. None is documented behaviour we can rely on across versions, and the repo already has form here — `OPERATING_GUIDE.md` records that the `/agents` wizard was removed in this very version. `tests/test_claude_guard.py` covers the **guard** layer; the **permission** layer has no automated test and cannot easily have one, since it lives outside the repo | Med | **Do not upgrade mid-phase.** On any upgrade, re-run the three permission probes in `docs/notes/D3_falsification.md` §1 (F1 deny-a-new-script, F2 deny-an-exception-file, F3 control) before the next task, and record the version. Auto-update is currently failing (npm prefix `/usr/local` is root-owned, user is uid 1000) — **that failure is pinning us to a known-good version, so it is load-bearing until someone deliberately upgrades** |
| **A frozen file is modified without anyone noticing.** Two layers exist and neither is complete on its own: the permission layer cannot see Bash writes at all, and the guard sees only what `git status` reports — which, per defect G1, collapses untracked directories and drops the path the `experiments/` pattern needs. A silent edit to `envs/`, `metrics/` or `rewards.py` does not crash; it changes what every recorded number means, retroactively | ~~Low-Med~~ **Low** (2026-08-04) | **G1 fixed and merged 2026-08-04** (`e01b0d1`), and the guard now has a 13-row regression test — the first tests it has ever had, which is what turned this from "read carefully and hope" into a checked property. Structural mitigations already in place: the corpus manifest records the repo git hash per episode, so a contaminated run is at least *identifiable* after the fact. **Standing rule: the guard's verdict is evidence about `git status`, never about the working tree** — a clean exit after a commit proves nothing about what was committed |
| **Time exhaustion — TOP RISK, downgraded to Med 2026-07-27 on measured data** | Med | Root cause found: not a deadlock, thread oversubscription. P0.3-fix (`set_num_threads(1)`) restores parallelism: **≈3.97× at workers=6** (199.2 s → 50.2 s, one session, clean shell), cutting the ×3 worst case from 194 h to **~48 h**. More importantly the pin is a LIVENESS fix: unpinned parallelism can hang unboundedly, not merely run slowly. Corpus collection needs no runner fix at all (`collect.py` parallelises via N shell processes). Blocking prerequisite for P5 (online baselines through `runner.py`), NOT for P2 |
| Scoop cadence (DTLight→DataLight→2602.02903→2603.22315: one relevant paper roughly every 1–3 months) | High | Speed on P1–P5; arXiv early; our unique assets (paired dual-backend scenarios, ladder, calibration) are the hardest to replicate |
| Scooped by follow-ups to arXiv:2602.02903 | Med | Speed on P1–P5; C2+C3 remain differentiators even if core overlaps |
| Manhattan 28×7 too heavy for full sweeps | Low | Headline results on grid4x4 + hangzhou_4x4; Manhattan as scale demo only |
| RESCO/state parity issues across backends | Low | Repo already reproduces RESCO `drq_norm`; reuse it |
| Deterministic CityFlow demand | ~~Med~~ RESOLVED 2026-07-11 | Was mis-scheduled as a P8 statistics issue; actually kills deterministic-tier corpus content. Fixed at the source: P2.0 flow randomization mandatory for every episode; also supplies environment stochasticity for P8 CIs |

---

## 10. Current Status

**Rewritten in full 2026-08-07.** ⚠️ **REPLACED, never appended to.** §8 holds history; §10 holds only
what is true today. **A fresh Master session should read, in order: this section → §7 (protocol) →
`docs/notes/DEFERRED.md` → `docs/CONTRACTS.md` → `PREREGISTRATION.md` §12 (amendments).** That is the
whole handoff; nothing load-bearing lives in a chat log.

### ✅ REPLACED 2026-08-12. The block here since 2026-08-07 said *"NOTHING HAS BEEN DELIVERED"*; that is now false, and it had been false for four days.
It is kept in `§8`'s history, not here — **`§10` holds only what is true today, and a stale `§10` is
the same artifact-versus-description error this section exists to prevent, applied to itself.**

**Verified first-hand on `main` @ `a740ec3`, 2026-08-12, by running the commands, not by reading a
checklist:** `offline/trajectory_logger.py` reads `FORMAT_VERSION = "1.1"`; `offline/` holds **16
modules**; the suite is **541 passed in 46.71 s**. Delivered and merged since: **P2.6** (format v1.1
+ the C8 guard), **P3.1–P3.4** (the dataset loader), **P4.1–P4.2** (the DT and its **passing**
pre-registered gate).

**The Decision Transformer exists, it trained, and it passed its gate — and P4.4 has now shown that
%BC and IQL beat it on that corpus.** That is the registered pivot, not a setback, and it arrived
with a mechanism: on multi-modal behaviour data, BC and the DT land near the **mixture** while %BC
and IQL recover the **best mode**. The standing filter still applies — *before the next commit, ask
whether it moves `offline/` forward* — and it has been satisfied continuously since 2026-08-07.

### The governing constraint (added 2026-08-06)
**A defensible, presentable result by end of September 2026** — eight weeks from 2026-08-06. Not the
finished paper. **Guaranteed target: C1 ladder + P4** (single-intersection DT, pre-registered gate,
BC/%BC/IQL). **Stretch: P5.2** on grid4x4. C2 and C3 are October.
**Filter, applied before starting anything:** *does it produce corpus data, or unblock something that
does?* If neither, it waits — and the deferral goes in `DEFERRED.md` **in the same turn**, or it is
forgotten rather than parked.

### What exists (verified 2026-08-07)
- **THE CORPUS EXISTS.** `datasets/`, **69 datasets / 4800 episodes**, 7 tiers × 3 headline
  scenarios, 200 distinct draws per tier, `base_seed=1000`, **4800/4800 distinct `episode_sha256`**.
  **Rewards: C6 alignment verified, but by a PAIRED test — the exact-identity claim that stood here
  until 2026-08-07 was hz1x1-only** (see the Decisions Log row of that date). Aligned residual beats
  misaligned in **138/138 episodes** sampled across all 69 dirs and all 7 tiers; the exact identity
  `Σ_j local_reward[t] == -Σ lane_waiting[t+1]` holds bit-exactly on **92/92** hz1x1+grid4x4 episodes
  and structurally not on cologne3. **Untracked** (`datasets*/`).
  **Size, corrected 2026-08-07 — the earlier "0.24 GB" was the `.npz` files only, stated of the
  directory.** Measured: 13,269 files, **3.73 GiB** on disk, of which the 4800 `.npz` are **0.228 GiB**
  and 8,400 are per-dataset copies of the materialised demand (`flows/flow_drawN.json` +
  `cityflow_drawN.json`, ~3.5 GiB, regenerable — draws are a proven pure function of
  `(source, base_seed, draw_id)`). Not a defect: the copies are what make each dataset self-contained.
  It matters twice — **budget ~4 GiB per corpus re-collection** (P2.6 makes a second one), and
  **P10.0 must not ship 3.5 GiB of duplicated regenerable JSON** as if it were trajectory data.
  Disk headroom checked the same day: 877 GB free, so this constrains nothing today.
- **The C1 ladder is measured** (normalised return, random=0 / MaxPressure=100): hz1x1 spans 0→155,
  grid4x4 **−1416→113**, cologne3 −29→114. `mappo1000` exceeds MaxPressure on all three. ⚠️ The
  `fixedtime` rungs and the state-coverage table are **stale** — measured before the k retune.
- **60 MAPPO checkpoints** at budgets 60/200/500/1000 × 5 seeds × 3 scenarios. Note the 1000-budget
  files live under `.../p2_1_mappo_nominal_500/p2_1_mappo_nominal_1000/` (a `sed` error of mine).
- **Suite: 343 passed, run by the coordinator on main 2026-08-06** — first-hand, not hearsay.
- P0 closed except P0.9 · P1 · P2.0/b/c · P2.5 · P8.0 · P2.2-draws.

### In flight right now (verified 2026-08-12, by `git worktree list` and `git log`)
- ✅ **P4.4 IS MERGED** (2026-08-12), after `docs/reviews/P4.4.md` (**PASS-WITH-NOTES**) and the
  mandated fix round `BRIEF_12` — 14 tests, **11 mutations executed**, and **no reported number
  moved**, which I verified myself from the committed artifact rather than from the packet (≤1e−11 on
  every cell mean, paired difference and recovered fraction), along with the suite (**612 passed, 1
  skipped**), all three guards and the untouched merged modules. §6's box is ticked **in the merge
  commit**.
- **Nothing is in flight.** `task/p4.4-offline-baselines` is merged; the worktree
  `/home/filip/rltraffic-p44` is retained **only** for its gitignored `output/p4_4/` (15 checkpoints
  and the four raw evaluation JSONs) — see the retirement rule below.
- **Next: P4.5** (BC on MAPPO seeds 101+202 only — the decisive test of %BC's mechanism, hypothesis
  registered before the run), then **P4.3** (RTG calibration, constrained by `PREREGISTRATION.md`
  **A8(a)**: the rule must be a declared probe-return quantile, the sweep an ablation around it).
  ⚠️ **Binding until P4.3 lands: no DT-versus-baseline sentence enters the paper.**
- **Two worktrees, and they are properly separated** — `/home/filip/rltraffic` on `main` (mine) and
  `/home/filip/rltraffic-p44` on the task branch (the implementer's). The 2026-08-07 *"one tree, two
  sessions"* hazard is retired. **The coordinator commits only from the `main` tree and checks
  `git rev-parse --abbrev-ref HEAD` first**; when an implementer is live on a branch I need to touch,
  the route is a throwaway worktree, `git -C` for every command, and never `cd` into it before removal.
- **`/home/filip/rltraffic-p44/output/p4_4/` holds the 15 checkpoints and the four raw evaluation
  JSONs backing every P4.4 number, and it is gitignored.** Per the 2026-08-11 rule, **that worktree is
  not to be retired until those artifacts are secured** — the fix round regenerates the committed
  artifact from them.
- **No simulation or training jobs running.** The reviewer's scratch worktree was removed and
  `/home/filip/rltraffic-p44` was left clean (`git status --porcelain` empty, checkpoint mtimes
  unchanged) — checked, because a review that mutates the thing it reviews is worse than no review.
- **RULING 2026-08-07 — the v1.1 ATT field is `att_per_step`, not `average_travel_time`** (BRIEF_08
  §9). BRIEF_08 as issued specified code that `tests/test_offline_naming_guard.py` rejects in four
  distinct AST shapes; falsified against the guard's own scanner, 4 violations vs 0 for the rename.
  Swept into BRIEF_09 the same turn. **The guard is not edited and gets no exception.**

### Immediate queue, in order
0. 🚨 **ATT RECOMPUTATION — blocking for claims, not for code.** ⚠️ **Three P8.0 findings gate this (review 2026-08-07, `docs/reviews/P8.0.md`): (B1)** `offline/rederive_anchors.py` can only *retrain* MAPPO, never evaluate the existing checkpoints — and retraining moves reported ATT by **±5.1 %** against the **+2.5 %** margin it would be settling; it must be taught the frozen `from_checkpoint` path first. **(B2)** `final_completed` is a **last-episode** value while `final_vehicle_count` is a **mean over episodes**, and the documented `entered` formula mixes them — dormant only at `episodes=1`, and it arms at the anchors' own `eval_episodes=3`. **(B3)** the A4 co-report stops at the dataclass and the >5 % condition exists nowhere in code. **Also: this reader has NEVER been run on cf_cologne3**, one of the two suspended cells and the one scenario with non-deterministic demand. The moment `datasets_v11/` exists,
   recompute every tier's `att_horizon` (= `att_per_step[-1]`, arithmetic over the corpus) per scenario
   and settle the MAPPO-vs-MaxPressure orderings marked NOT SETTLED in §3.1. **Until then no ATT cell
   enters a figure, a table or a claim, and the "expert tier is scenario-dependent" finding is
   suspended.** Report the draw-1–200 answer and the nominal draw-0 answer as the two distinct
   quantities D4 makes them; do not pool. **Also emit `vehicle_count` at the horizon beside every ATT
   cell** — amendment **A5** (2026-08-07) makes the co-report **unconditional** and **withdraws** A4's >5 % invalidation, which mistook a control outcome for a population size. **What replaces it is registered, not prose: every reported comparison must be over SHARED DRAW IDS, reported with the cell; a comparison that cannot be is void.** ⚠️ **Qualification measured 2026-08-07: shared draws fix the DEMAND, not the ENGINE.** On cf_cologne3 two runs of the same draw diverge bit-wise 7.7 % of the time, so a shared-draw comparison there still carries engine variance and needs seeds and CIs to absorb it; on hz1x1 and grid4x4 a shared draw fixes the trajectory outright. A5 is unaffected — it registers which comparisons are *valid*, not which are *noise-free*. No recorded ATT
   cell currently carries one (swept 2026-08-07), and it is the same `[-1]` arithmetic on an array the
   corpus already stores, so it costs nothing to add and invalidates comparisons if omitted.
0c. 🔎 **RETRO-REVIEW P2.6 — merged without the mandated independent review (found by the user, 2026-08-07).** **Scoped, not a whole-task round**, because the evidence is very uneven across what it shipped. **Review these three:** (i) `offline/att_ladder.py` — **its output is already cited three times in this plan and is destined to be the C1 ladder figure**, and it is the highest-exposure unreviewed artifact in the repo; (ii) `offline/recollect_v11.py` and `offline/compare_corpora.py` — **their tests were written AFTER the code by the implementer's own disclosure**, so they are the weakest evidence in the delivery and were produced by someone who had already seen the implementation; (iii) `offline/migrate_mappo_checkpoints.py` — it rewrote all 60 checkpoints **in place**. **Explicitly OUT of scope, because their evidence is the strongest we have:** the v1.1 logger delta (one array, four executed mutations) and the C8 guard (acceptance criterion falsified with the assertion removed). **P4 is NOT blocked by this** — today's ruling moved its gate to live paired measurement on held-out draws, so the gate no longer depends on `att_ladder.py`. What *is* blocked is citing the C1 ladder as a result.

0a. 🔓 **RULING 2026-08-07 — `compare_corpora` exempts cf_cologne3 from BIT-IDENTITY, and the replacement is a MEASURED envelope, not a waiver.** cologne3 is not bit-reproducible (7.7 % of draws diverge under identical code), so bit-identity cannot be its acceptance criterion. **Replacement, all three required:** (i) episode counts and draw ids must match **exactly** — those are bookkeeping and are reproducible; (ii) the tier-level mean of `total_global_reward` must agree within **1 %** (measured self-noise **0.080 %**, so ≈12× headroom); (iii) the fraction of differing episodes must be **≤ 20 %** (measured self-noise **7.7 %**, ≈2.6× headroom). **The tolerances are recorded with the measurement that produced them** so a later reader sees where they came from rather than finding round numbers. hz1x1 and grid4x4 keep strict bit-identity — they earned it, 44/44 tiers. **The exemption is recorded in the tool's output the way `fixedtime`'s is, naming cologne3 and the reason, so a passing gate never hides it.**
0b. 🔒 **B1 MUST BLOCK BY CONSTRUCTION, AND TODAY IT DOES NOT — stated plainly rather than claimed.** The user required written confirmation that `rederive_anchors.py` cannot be run against the NOT SETTLED cells until it can load existing checkpoints, *because a blocker that depends on someone remembering it is the same class as the A2 sweep that recurred inside its own fix*. **Honest status: right now the block is CONVENTIONAL.** The coordinator does not write source, so the mechanism does not exist until an implementer builds it, and claiming otherwise would be the exact overclaim this plan keeps logging. **The registered mechanism, which is the first commit of whoever takes queue item 0:** (i) every cell `rederive_anchors.py` emits carries a machine-readable `policy_source` of `"retrained"` or `"checkpoint"`; (ii) the tool **exits non-zero** rather than emitting a cell for a checkpoint-backed tier while in retrain mode; (iii) any consumer that settles a NOT SETTLED cell **asserts `policy_source == "checkpoint"`** for every cell it uses. **Acceptance is mechanical: the refusal test must FAIL when the refusal is deleted** — a guard that passes its own test when removed is what `check_test_hygiene.sh` exists for. Until (i)-(iii) land, no number from that tool may settle anything, and that sentence is the whole of the protection.
1. **P2.6** — format v1.1 + the C8 guard (`BRIEF_08`). Carries AUTHORISATION C.
2. **P3** — dataset loader (`BRIEF_09`). **Critical path; independent review before merge.**
3. **P2.4** — corpus linter. Checks already specified by the ad-hoc verification of 2026-08-06.
4. **P4** — single-intersection DT + the pre-registered gate, then BC/%BC/IQL.

**⚠️ Scheduling flag — P7.0 has never been run.** It is scoped as the ~1-day early kill-switch for C3
and was to run "right after P1, parallel to P2"; P1 closed 2026-07-27. Whoever writes its brief **must
bind hangzhou's vType first** (Decisions Log 2026-07-31): the shipped `.rou.xml` binds no vType, so
SUMO would run 55.55 m/s against CityFlow's 11.11, and the gate would measure a 5× speed-limit
artifact. Descoping C3 on that number would be descoping it for the wrong reason.

**✅ Closed 2026-08-03:** the D3 patch was applied by the human, the session restarted, and the owed
on-repo falsification was run at **both** layers — see `docs/notes/D3_falsification.md` and the three
Decisions Log rows. The applied `settings.json` was verified byte-identical to HEAD + the reviewed
patch before being committed, so "the human applied it" is not taken on report. Ruling unchanged;
one defect found (G1 → P0.8).

**✅ Registration externally anchored 2026-08-03.** `main` and `v0.1-prereg` pushed to
`github.com/RusieckiFilip/rltraffic`. Verified **from a fresh clone of the public remote**, not from
this working copy: tag object `1e18676e`, commit `35e05ff`, annotation sha256
`0aec96ea…` equal to the file recomputed from the tag, commit timestamp `2026-08-03T18:36:57+02:00`.
The same clone confirms the document's own factual claim — **0 files matching `*.npz` / `*.pt` /
`*.pth` / `*.ckpt` / `manifest.json` in the registered tree**, and `offline/` holding only
`__init__.py`, `collect.py`, `flow_randomizer.py`, `trajectory_logger.py`. So "registered before any
corpus or model existed" is checkable by a reviewer rather than asserted by us. Remaining optional
anchor: mirror to OSF and cite the DOI (a push proves a third party saw it; an OSF DOI additionally
timestamps it under an institution's control).

**Inherited by every later task:** `experiments/runner.py` is no longer byte-identical to main's frozen
copy (one authorised +45-line delta, `docs/patches/runner_thread_pinning.patch`) — any brief saying
"the frozen set is untouched" is working from a false premise; parallelism is safe *because of* the
pin, and a code path reaching `run_cell`'s work without it hangs unboundedly rather than running slow;
corpus numerics are on the pinned side of a float-reduction boundary (N2); importing
`experiments.runner` and running a cell pins the *calling* process to one torch thread as a
process-global side effect, `workers=1` included.

**Carried notes for P3:** key by intersection ID, never positional index · `local_reward` is composite
(`global_weight × global + local_fn`) · episode ends are truncations, not terminals · format v1.0
stores global metrics only · `draw(0)` preserves source order while `draw(k>0)` sorts globally, so on
cityflow1x3 and hangzhou_4x4 the nominal control differs from randomised draws in ordering as well as
demand.
**Carried notes for P2:** `--overwrite` deletes rather than appends → one `out_dir` per
(policy, scenario, seed-block) · size by **draws**, not episodes · collect with
`--local-reward-fn queue_length --global-reward-weight 0.0` · **⚠️ REGISTERED DRAW-ID SPLIT (D4,
`PREREGISTRATION.md` §5) — binding on every collection run:** draw `0` = nominal control, reported
separately and never pooled with randomised draws (it preserves source vehicle order while k>0 sorts
globally); draws **`1–999` only** for the training corpus; draws **`1000–1099` are the held-out
evaluation pool and must never appear in any training corpus, for any method including baselines**.
P2.4's linter enforces this from the manifests. Collecting into the wrong range is not recoverable by
re-labelling — it means the model was trained on its own test set.
