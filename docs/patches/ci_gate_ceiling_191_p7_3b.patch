diff --git a/.github/ci/ci_baseline.json b/.github/ci/ci_baseline.json
index e033034..a09b2f2 100644
--- a/.github/ci/ci_baseline.json
+++ b/.github/ci/ci_baseline.json
@@ -3,125 +3,136 @@
   "written_by": "P0.10 (BRIEF_23), 2026-08-17, branch task/p0.10-reproducibility",
   "read_by": ".github/ci/ci_gate.py -- see its module docstring for the format contract",
   "pytest": {
-    "skip_ceiling": 189,
+    "skip_ceiling": 191,
     "skip_breakdown": {
-      "corpus_or_checkpoint": 93,
+      "corpus_or_checkpoint": 94,
       "cityflow": 42,
       "sumo_traci": 15,
       "matplotlib": 2,
       "campaign_output": 31,
       "grid4x4_resco_candidates": 4,
-      "main_tree_interpreter": 1,
+      "main_tree_interpreter": 2,
       "shallow_checkout": 1
     },
-    "ceiling_if_cityflow_were_built": 147,
+    "ceiling_if_cityflow_were_built": 149,
     "superseded_chain_is_uniformly_named": "2026-08-24. Every link of measured.superseded.* is now keyed 'superseded', at every depth. It used to be 'superseded' at depth 1 and 'its_own_superseded' below, which is TWO NAMES FOR ONE RELATION and is exactly why the chain could only be pinned one depth at a time: a generic walk stops at the first name it does not recognise, and the coordinator's first attempt at this patch did precisely that, reading the chain as (104, 98) and stopping. Caught by running the test, not by reading it. tests/test_ci_gate.py now asserts the legacy key is absent, so the shape cannot come back.",
     "breakdown_and_alternative_added": "2026-08-24. The breakdown existed only as prose inside measured.breakdown and the alternative only as prose inside ceiling_is_a_consequence_of, so NOTHING checked that the parts summed to the ceiling -- the same defect as the guards' by_file-versus-total gap recorded in guards.english.by_file_note, one block higher in this same file. tests/test_ci_gate.py now closes both by arithmetic. NOTE the coincidence, because it will confuse someone: ceiling_if_cityflow_were_built is 72, which is also the ceiling this file carried on 2026-08-17. Unrelated -- 104 - 32 = 72.",
     "provisional": true,
     "measured": {
-      "value": 189,
-      "condition": "GitHub Actions run 35338316324, BOTH jobs 'suite-and-guards (ubuntu-24.04)' and '(ubuntu-22.04)', on main at d92947e -- the merge of task/p7.3a-ci-shallow, which is P7.3a's merge b480c7b plus one tests-only commit. Workflow unchanged.",
-      "result": "2174 tests collected, 189 skipped, 0 failures, 0 errors on BOTH legs, with IDENTICAL counts and an IDENTICAL 48-message skip multiset compared element by element. The preceding run 35335268248 at 09c4aac carried 188 skips AND ONE REAL FAILURE (test_j1_code_changed_since_reads_real_git_history assumes full git history while actions/checkout@v4 checks out shallow); that failure was fixed on its own branch BEFORE this ceiling was measured, so no ceiling here was ever committed over a red suite.",
-      "breakdown": "COUNTED from each leg's junit.xml and classified BY INSPECTION, never by regex: the 178 run's artifacts (35085215720 at 164b519) were downloaded and diffed DIRECTLY against this run's. 8 message texts are new and nothing was removed. +7 campaign_output (3 on draw 5's parity configuration, 2 on draw 201's, 1 on the CityFlow held-out draws 1000 and 1001, 1 on draw 1000's parity), +1 corpus_or_checkpoint (the two subjects' checkpoints), +1 sumo_traci (a compound predicate naming SUMO+traci first, then draw 5's parity and dt_seed101 -- assigned to the first condition it names, this file's convention since 156), +1 main_tree_interpreter and +1 shallow_checkout, both NEW families rather than forced into an existing one. THE CLASSIFIER WAS CROSS-CHECKED BY REPRODUCING THE DECLARED 178 BREAKDOWN EXACTLY (92/42/14/2/24/4) from the 178 run's own messages before it was applied here.",
-      "how_it_was_obtained": "`gh run download 35338316324 -D ...` for both legs and `gh run download 35085215720` for the comparison, then every <skipped> message extracted from junit.xml with xml.etree, the file:line prefix stripped, and the multisets compared leg against leg and run against run.",
+      "value": 191,
+      "condition": "GitHub Actions run 35459798196, BOTH jobs 'suite-and-guards (ubuntu-24.04)' and '(ubuntu-22.04)', on main at 708f157 -- the merge of task/p7.3b-ci-interp (P7.3b's CI fix) on top of P7.3b's merge 732f378. Workflow unchanged.",
+      "result": "2230 tests collected, 191 skipped, 0 failures, 0 errors on BOTH legs, with IDENTICAL counts and an IDENTICAL 50-distinct-message skip multiset compared element by element (distinct-text count by output/ci_runs/skips.py, which folds the file:line prefix only; the 189 entry's own tool counted 48 distinct texts on the same run -- a variant-folding convention, the totals and the leg identity are unaffected). The two runs before this one on the merged tree were NOT usable: 35451103010 carried 190 skips AND ONE REAL FAILURE (test_the_anchor_driver_refuses_a_bogus_stage_and_creates_nothing assumed this laptop's interpreter path), and the run on the true merge 732f378 was cancelled by the workflow's cancel-in-progress.",
+      "breakdown": "COUNTED from each leg's junit.xml and classified BY INSPECTION, never by regex: the 189 run's artifacts (35338316324) were downloaded and diffed DIRECTLY against this run's. 2 message texts are new and nothing was removed: +1 corpus_or_checkpoint (needs the real anchor corpus and P4's checkpoints: datasets_sumo_v11/hz1x1_sumo_maxpressur...); +1 main_tree_interpreter (needs the main tree's interpreter at /home/filip/rltraffic/.venv/bin/python...).",
+      "how_it_was_obtained": "`gh run download 35459798196 -D ...` for both legs and `gh run download 35338316324` for the comparison, then every <skipped> message extracted from junit.xml with xml.etree (output/ci_runs/skips.py), the file:line prefix stripped, and the multisets compared leg against leg and run against run; the classifier was first falsified against the 189 run (reproducing 189/189/identical legs) and refuses an unfinished run.",
       "measured_on_a_github_runner": true,
-      "tree": "main at d92947e (P7.3a's merge b480c7b, the brief pointer 09c4aac, the CI classification 523a5c1, and the shallow-checkout fix)",
-      "date": "2026-09-18",
+      "tree": "main at 708f157 (P7.3b's merge 732f378, its CI fix a790ad1, and the coordinator's docs commits between)",
+      "date": "2026-09-19",
       "superseded": {
-        "value": 178,
-        "condition": "GitHub Actions run 35085215720, BOTH jobs 'suite-and-guards (ubuntu-24.04)' and '(ubuntu-22.04)', on main at 164b519 -- workflow unchanged (pinned threads, dev extra only, CityFlow not built, no corpus, no SUMO binary). READ THIS BEFORE CITING THE TREE: Amendment G's merge commit is 8c83fd8 and its OWN run, 35085007974, was CANCELLED about seven minutes in when the plan commit (164b519) superseded it -- the third time this exact shape has occurred, and the second caused by the coordinator's own follow-up push. Its ubuntu-22.04 leg had uploaded a complete junit.xml before the cancel (2054 collected, 178 skipped, 0 failures); its ubuntu-24.04 leg uploaded NO artifact, checked against the API rather than assumed. That single leg's 42-message skip multiset is IDENTICAL element-by-element to the classified run's on both legs. The classified run is 35085215720 at 164b519, the first COMPLETED run of the merged tree; the two trees differ by `git diff --stat 8c83fd8 164b519` = 5 lines in docs/PROJECT_PLAN.md and docs/notes/HANDOFF_2026-09-15.md: no test file, no conftest, no gating path. Recorded rather than glossed, as before.",
-        "result": "1876 passed, 178 skipped, 0 failures, 0 errors, 2054 tests collected. BOTH legs report identical counts AND an identical 42-message skip multiset (compared element-by-element). The suite step PASSED on both legs; only this ceiling gate failed (178 > 158). The delta CLOSES exactly against P7.2b's own test count: collected +66 = passed +46 plus skipped +20, and 66 = the 65 tests P7.2b's merge added in tests/test_transfer_calibration.py and tests/test_aligned_env.py plus the 1 CI-runnable test Amendment G added. 46 passed / 20 skipped is, to the number, what Amendment G's route-A reproduction produced on the author's machine with the two hardcoded roots hidden -- the first time a local reproduction of CI's condition existed for these files.",
-        "breakdown": "COUNTED from each leg's junit.xml and classified BY INSPECTION, never by regex: the 158 run's artifacts (34782398552 at 155989a) were diffed DIRECTLY against this run's -- 3 NEW messages carrying all 20 new skips, 0 removed, 0 count changes. 'P4 checkpoints are not in this tree' x16 -> corpus_or_checkpoint (76 -> 92): the P7.2b tests that load the two subjects' seed-101 checkpoints from output/. 'P7.2a's parity configuration for draw 5 is not present' x3 and 'P7.2a's parity configuration for probe-band draw 201 is not present' x1 -> campaign_output (20 -> 24): P7.2a's materialised parity draws are a gitignored campaign product under scenarios/draws/, the same gate shape as 'P7.0's episodes are not present' already filed there. cityflow 42, sumo_traci 14, matplotlib 2, grid4x4_resco_candidates 4 unchanged. NOTE: sumo_traci did not move although P7.2b's engine tests ARE SUMO-gated -- their parity-configuration gate is the one that fires on a runner and is the message recorded, so the family follows the message, not the intent. 92+42+14+2+24+4 = 178.",
-        "how_it_was_obtained": "`gh run download 35085215720 -D ...` for both legs (and 35085007974's single uploaded leg for the cross-check), every <skipped> message extracted from junit.xml with xml.etree and tallied as a Counter per leg; legs compared to each other, to the cancelled leg, and to the 158 run's Counter. pytest.txt's tail read on both legs as the second parser; junit's own arithmetic (passed + skipped == collected) as the third.",
-        "tree": "main at 164b519 (Amendment G's merge 8c83fd8 plus a plan and handoff note, docs only)",
-        "date": "2026-09-16",
+        "value": 189,
+        "condition": "GitHub Actions run 35338316324, BOTH jobs 'suite-and-guards (ubuntu-24.04)' and '(ubuntu-22.04)', on main at d92947e -- the merge of task/p7.3a-ci-shallow, which is P7.3a's merge b480c7b plus one tests-only commit. Workflow unchanged.",
+        "result": "2174 tests collected, 189 skipped, 0 failures, 0 errors on BOTH legs, with IDENTICAL counts and an IDENTICAL 48-message skip multiset compared element by element. The preceding run 35335268248 at 09c4aac carried 188 skips AND ONE REAL FAILURE (test_j1_code_changed_since_reads_real_git_history assumes full git history while actions/checkout@v4 checks out shallow); that failure was fixed on its own branch BEFORE this ceiling was measured, so no ceiling here was ever committed over a red suite.",
+        "breakdown": "COUNTED from each leg's junit.xml and classified BY INSPECTION, never by regex: the 178 run's artifacts (35085215720 at 164b519) were downloaded and diffed DIRECTLY against this run's. 8 message texts are new and nothing was removed. +7 campaign_output (3 on draw 5's parity configuration, 2 on draw 201's, 1 on the CityFlow held-out draws 1000 and 1001, 1 on draw 1000's parity), +1 corpus_or_checkpoint (the two subjects' checkpoints), +1 sumo_traci (a compound predicate naming SUMO+traci first, then draw 5's parity and dt_seed101 -- assigned to the first condition it names, this file's convention since 156), +1 main_tree_interpreter and +1 shallow_checkout, both NEW families rather than forced into an existing one. THE CLASSIFIER WAS CROSS-CHECKED BY REPRODUCING THE DECLARED 178 BREAKDOWN EXACTLY (92/42/14/2/24/4) from the 178 run's own messages before it was applied here.",
+        "how_it_was_obtained": "`gh run download 35338316324 -D ...` for both legs and `gh run download 35085215720` for the comparison, then every <skipped> message extracted from junit.xml with xml.etree, the file:line prefix stripped, and the multisets compared leg against leg and run against run.",
         "measured_on_a_github_runner": true,
+        "tree": "main at d92947e (P7.3a's merge b480c7b, the brief pointer 09c4aac, the CI classification 523a5c1, and the shallow-checkout fix)",
+        "date": "2026-09-18",
         "superseded": {
-          "value": 158,
-          "condition": "GitHub Actions run 34782398552, BOTH jobs 'suite-and-guards (ubuntu-24.04)' and '(ubuntu-22.04)', on main at 155989a -- workflow unchanged (pinned threads, dev extra only, CityFlow not built, no corpus, no SUMO binary). READ THIS BEFORE CITING THE TREE: P7.2a's merge commit is 5d57474 and its OWN run, 34781997686, was CANCELLED 8 min in when the BRIEF_36 commit (155989a) superseded it -- the same shape as the 156 move -- but unlike that case BOTH of its suite legs had already uploaded complete junit.xml (1830 passed, 158 skipped, 1988 collected), and their 39-message skip multisets are IDENTICAL element-by-element to the classified run's on both legs (checked by parsing both, not assumed). The classified run is 34782398552 at 155989a, the first COMPLETED run of the merged tree; the two trees differ by `git diff --stat 5d57474 155989a` = 114 added lines in docs/briefs/BRIEF_36_p7.2b_transfer_calibration.md: no test file, no conftest, no gating path. Recorded rather than glossed, as before.",
-          "result": "1830 passed, 158 skipped, 0 failures, 0 errors, 1988 tests collected. BOTH legs report identical counts AND an identical 39-message skip multiset (compared element-by-element). The suite step PASSED on both legs; only this ceiling gate failed (158 > 156). The delta CLOSES exactly against P7.2a's own test count: collected +32 = passed +30 plus skipped +2, and 32 = tests/test_materialise_parity.py (28, new) + tests/test_parity_vtype.py (+4).",
-          "breakdown": "COUNTED from each leg's junit.xml and classified BY INSPECTION, never by regex: the previous run's artifacts (34780605940 at 535de12, the 156 tree) were diffed DIRECTLY against this run's -- 0 new messages, 0 removed, 2 changed in count: 'SUMO/traci not available' 13 -> 14 and 'cityflow not installed' 31 -> 32, the two P7.2a tests named in the superseded link's why_it_was_wrong. Every byte-identical message keeps its recorded family, so sumo_traci 13 -> 14, cityflow 41 -> 42, and corpus_or_checkpoint 76, matplotlib 2, campaign_output 20, grid4x4_resco_candidates 4 are unchanged. 76+42+14+2+20+4 = 158.",
-          "how_it_was_obtained": "`gh run download 34782398552 -D ...` for both legs (and 34781997686's for the cross-check), then every <skipped> message extracted from junit.xml with xml.etree and tallied as a Counter per leg; legs compared to each other and to the previous run's Counter; the two runs' Counters compared to each other. pytest.txt's tail read on both legs as the second parser.",
-          "tree": "main at 155989a (P7.2a's merge 5d57474 plus BRIEF_36, docs only)",
-          "date": "2026-09-13 (runs, UTC evening); classified and committed 2026-09-14",
+          "value": 178,
+          "condition": "GitHub Actions run 35085215720, BOTH jobs 'suite-and-guards (ubuntu-24.04)' and '(ubuntu-22.04)', on main at 164b519 -- workflow unchanged (pinned threads, dev extra only, CityFlow not built, no corpus, no SUMO binary). READ THIS BEFORE CITING THE TREE: Amendment G's merge commit is 8c83fd8 and its OWN run, 35085007974, was CANCELLED about seven minutes in when the plan commit (164b519) superseded it -- the third time this exact shape has occurred, and the second caused by the coordinator's own follow-up push. Its ubuntu-22.04 leg had uploaded a complete junit.xml before the cancel (2054 collected, 178 skipped, 0 failures); its ubuntu-24.04 leg uploaded NO artifact, checked against the API rather than assumed. That single leg's 42-message skip multiset is IDENTICAL element-by-element to the classified run's on both legs. The classified run is 35085215720 at 164b519, the first COMPLETED run of the merged tree; the two trees differ by `git diff --stat 8c83fd8 164b519` = 5 lines in docs/PROJECT_PLAN.md and docs/notes/HANDOFF_2026-09-15.md: no test file, no conftest, no gating path. Recorded rather than glossed, as before.",
+          "result": "1876 passed, 178 skipped, 0 failures, 0 errors, 2054 tests collected. BOTH legs report identical counts AND an identical 42-message skip multiset (compared element-by-element). The suite step PASSED on both legs; only this ceiling gate failed (178 > 158). The delta CLOSES exactly against P7.2b's own test count: collected +66 = passed +46 plus skipped +20, and 66 = the 65 tests P7.2b's merge added in tests/test_transfer_calibration.py and tests/test_aligned_env.py plus the 1 CI-runnable test Amendment G added. 46 passed / 20 skipped is, to the number, what Amendment G's route-A reproduction produced on the author's machine with the two hardcoded roots hidden -- the first time a local reproduction of CI's condition existed for these files.",
+          "breakdown": "COUNTED from each leg's junit.xml and classified BY INSPECTION, never by regex: the 158 run's artifacts (34782398552 at 155989a) were diffed DIRECTLY against this run's -- 3 NEW messages carrying all 20 new skips, 0 removed, 0 count changes. 'P4 checkpoints are not in this tree' x16 -> corpus_or_checkpoint (76 -> 92): the P7.2b tests that load the two subjects' seed-101 checkpoints from output/. 'P7.2a's parity configuration for draw 5 is not present' x3 and 'P7.2a's parity configuration for probe-band draw 201 is not present' x1 -> campaign_output (20 -> 24): P7.2a's materialised parity draws are a gitignored campaign product under scenarios/draws/, the same gate shape as 'P7.0's episodes are not present' already filed there. cityflow 42, sumo_traci 14, matplotlib 2, grid4x4_resco_candidates 4 unchanged. NOTE: sumo_traci did not move although P7.2b's engine tests ARE SUMO-gated -- their parity-configuration gate is the one that fires on a runner and is the message recorded, so the family follows the message, not the intent. 92+42+14+2+24+4 = 178.",
+          "how_it_was_obtained": "`gh run download 35085215720 -D ...` for both legs (and 35085007974's single uploaded leg for the cross-check), every <skipped> message extracted from junit.xml with xml.etree and tallied as a Counter per leg; legs compared to each other, to the cancelled leg, and to the 158 run's Counter. pytest.txt's tail read on both legs as the second parser; junit's own arithmetic (passed + skipped == collected) as the third.",
+          "tree": "main at 164b519 (Amendment G's merge 8c83fd8 plus a plan and handoff note, docs only)",
+          "date": "2026-09-16",
           "measured_on_a_github_runner": true,
           "superseded": {
-            "value": 156,
-            "condition": "GitHub Actions run 34708726985, BOTH jobs 'suite-and-guards (ubuntu-24.04)' and '(ubuntu-22.04)', on main at 64c2265 -- workflow unchanged (pinned threads, dev extra only, CityFlow not built, no corpus, no SUMO binary). READ THIS BEFORE CITING THE TREE: the instruction for this move named 2bce838 (P7.1's merge commit), and 2bce838's OWN run, 34708703166, CANNOT be classified -- it was cancelled 30 s in when the handoff commit superseded it (both suite legs 'cancelled' at 17:36:27-28, started 17:35:58) and it uploaded NO artifacts, which was checked against the API rather than assumed. 34708726985 at 64c2265 is the first run of P7.1's merged tree that completed and kept its junit.xml. The two trees differ by `git diff --stat 2bce838 64c2265` = 2 added lines in docs/notes/HANDOFF_2026-09-12.md: no test file, no conftest, no gating path. The substitution is recorded rather than glossed because 'the run named in the instruction' and 'the run measured' must never silently differ in this file.",
-            "result": "1800 passed, 156 skipped, 0 failures, 0 errors, 1956 tests collected. BOTH legs report identical counts AND an identical 39-message skip multiset (compared element-by-element, not by total), which is the cross-check. The suite step PASSED on both legs; only this ceiling gate failed. The delta CLOSES exactly against P7.1's own test count: collected +177 = passed +165 plus skipped +12, and 177 is the number of tests in P7.1's three new files as its Return Packet reports them.",
-            "breakdown": "COUNTED from each leg's junit.xml and classified BY INSPECTION of all 39 distinct skip messages, not by regex (BRIEF_30 D5): 76 corpus/checkpoint + 41 cityflow + 13 SUMO/traci + 2 matplotlib + 20 campaign-output + 4 grid4x4 candidates-root = 156. Zero messages were unclassified on either leg. METHOD, and it matters: the previous run's artifacts (34587670145, 9dd224e) were still retained, so the 144 and 156 multisets were diffed DIRECTLY -- 0 messages removed, 0 changed in text, 1 changed in count ('SUMO/traci not available' 10 -> 13) and 4 new messages carrying 9 skips. Every message that is byte-identical to one classified on 2026-09-11 KEEPS its recorded family; only the delta was classified afresh. That is what makes 76/41/2 carried values rather than re-litigated ones. THE DELTA: sumo_traci +3, all of it P7.1's own new SUMO-gated tests (test_backend_alignment 0 -> 2, test_sumo_att_reference 0 -> 1; test_sumo_env_phase_constraints stays at 8 and test_transfer_gate and test_dqn_queue_length_reward at 1 each). campaign_output +5, all of it 'P7.0's episodes are not present at .../output/p7_0: set RLTRAFFIC_OUTPUT_ROOT ...' (3 in test_sumo_att_reference, 2 in test_backend_alignment) -- the same gate shape and the same environment variable as the p8_4b_rederivation messages already filed there. grid4x4_resco_candidates 4 is NEW and is not any existing family: RLTRAFFIC_GRID4X4_RESCO points at a read-only, gitignored, CC BY-NC-SA candidates root that is neither the corpus, nor a campaign output tree, nor an engine, nor matplotlib (3 in test_conversion_audit, 1 in test_backend_alignment).",
-            "how_it_was_obtained": "`gh run download 34708726985 -D ...` for both legs, then every <skipped> message extracted from junit.xml with xml.etree, the file:line prefix stripped, and the multisets compared leg against leg (identical) and run against run. pytest.txt's terminal tail is the independent second parser and agreed at '1800 passed, 156 skipped' on both legs, so the number 156 was read four times by two parsers. junit's own arithmetic was checked as a third route: passed + skipped == collected (1800 + 156 == 1956).",
-            "an_independent_inspection_re_derived_THE_SAME_boundary_disagreement": "Before carrying the families forward, all 39 messages were classified from scratch by a reader who did not consult the recorded split. That pass returns corpus_or_checkpoint 79 / campaign_output 17 where this file records 76 / 20; cityflow 41, sumo_traci 13 and matplotlib 2 agree exactly and so does the total. Run against the 144 data it returns 79 / 12 against the record's 76 / 15 -- the SAME three skips, the same direction, the same magnitude as the disagreement the 2026-09-11 entry documented under 'an_honest_disagreement_about_the_CORPUS_versus_CAMPAIGN_boundary'. It is therefore the known ambiguity re-derived independently, not a new error, and the recorded split stands: it was made by inspection over byte-identical data, which is the method the protocol mandates. Only the TOTAL gates.",
-            "tree": "main at 64c2265dbd2433a3fa4a4431b2ec4fbdc3ed0439 (P7.1's merge 2bce838 plus a 2-line handoff note)",
-            "date": "2026-09-12",
+            "value": 158,
+            "condition": "GitHub Actions run 34782398552, BOTH jobs 'suite-and-guards (ubuntu-24.04)' and '(ubuntu-22.04)', on main at 155989a -- workflow unchanged (pinned threads, dev extra only, CityFlow not built, no corpus, no SUMO binary). READ THIS BEFORE CITING THE TREE: P7.2a's merge commit is 5d57474 and its OWN run, 34781997686, was CANCELLED 8 min in when the BRIEF_36 commit (155989a) superseded it -- the same shape as the 156 move -- but unlike that case BOTH of its suite legs had already uploaded complete junit.xml (1830 passed, 158 skipped, 1988 collected), and their 39-message skip multisets are IDENTICAL element-by-element to the classified run's on both legs (checked by parsing both, not assumed). The classified run is 34782398552 at 155989a, the first COMPLETED run of the merged tree; the two trees differ by `git diff --stat 5d57474 155989a` = 114 added lines in docs/briefs/BRIEF_36_p7.2b_transfer_calibration.md: no test file, no conftest, no gating path. Recorded rather than glossed, as before.",
+            "result": "1830 passed, 158 skipped, 0 failures, 0 errors, 1988 tests collected. BOTH legs report identical counts AND an identical 39-message skip multiset (compared element-by-element). The suite step PASSED on both legs; only this ceiling gate failed (158 > 156). The delta CLOSES exactly against P7.2a's own test count: collected +32 = passed +30 plus skipped +2, and 32 = tests/test_materialise_parity.py (28, new) + tests/test_parity_vtype.py (+4).",
+            "breakdown": "COUNTED from each leg's junit.xml and classified BY INSPECTION, never by regex: the previous run's artifacts (34780605940 at 535de12, the 156 tree) were diffed DIRECTLY against this run's -- 0 new messages, 0 removed, 2 changed in count: 'SUMO/traci not available' 13 -> 14 and 'cityflow not installed' 31 -> 32, the two P7.2a tests named in the superseded link's why_it_was_wrong. Every byte-identical message keeps its recorded family, so sumo_traci 13 -> 14, cityflow 41 -> 42, and corpus_or_checkpoint 76, matplotlib 2, campaign_output 20, grid4x4_resco_candidates 4 are unchanged. 76+42+14+2+20+4 = 158.",
+            "how_it_was_obtained": "`gh run download 34782398552 -D ...` for both legs (and 34781997686's for the cross-check), then every <skipped> message extracted from junit.xml with xml.etree and tallied as a Counter per leg; legs compared to each other and to the previous run's Counter; the two runs' Counters compared to each other. pytest.txt's tail read on both legs as the second parser.",
+            "tree": "main at 155989a (P7.2a's merge 5d57474 plus BRIEF_36, docs only)",
+            "date": "2026-09-13 (runs, UTC evening); classified and committed 2026-09-14",
             "measured_on_a_github_runner": true,
             "superseded": {
-              "value": 144,
-              "condition": "GitHub Actions run 34486928659, BOTH jobs 'suite-and-guards (ubuntu-24.04)' and '(ubuntu-22.04)', on task/p5.3b-nortg-campaign at afb9062 after P5.3b's campaign artifact landed; workflow unchanged (pinned threads, dev extra only, CityFlow not built, no corpus, no SUMO binary)",
-              "result": "1555 passed, 144 skipped, 0 failures, 0 errors, 1699 tests collected. BOTH legs report identical counts AND an identical 35-message skip breakdown, which is the cross-check. The suite step PASSED on both legs; only this ceiling gate failed.",
-              "breakdown": "COUNTED from each run's junit.xml and classified BY INSPECTION of all 35 distinct skip messages, not by regex (BRIEF_30 D5): 76 corpus/checkpoint + 41 cityflow + 10 SUMO/traci + 2 matplotlib + 15 campaign-output = 144. Zero messages were unclassified on either leg. Delta from 139 is +5, entirely in two families: campaign_output 11 -> 15 (+4) and corpus_or_checkpoint 75 -> 76 (+1). cityflow, SUMO and matplotlib are unchanged at 41/10/2, which is what P5.3b should do -- it added corpus-, checkpoint- and campaign-output-gated tests and no engine-gated ones. TWO of the +4 are P5.3b's own (\"P8.4b's re-derived cells are not present ...\", tests/test_nortg_campaign.py); the other three arrived with P8.4b and were already in the tree at 139.",
-              "how_it_was_obtained": "`gh run download 34486928659 -D ...` for both legs, then every <skipped> message enumerated and assigned to a category by reading it. junit.xml is the instrument of record; pytest.txt's terminal tail is the independent second parser and agreed at 144 on both legs.",
-              "tree": "task/p5.3b-nortg-campaign at afb9062",
-              "date": "2026-09-10",
+              "value": 156,
+              "condition": "GitHub Actions run 34708726985, BOTH jobs 'suite-and-guards (ubuntu-24.04)' and '(ubuntu-22.04)', on main at 64c2265 -- workflow unchanged (pinned threads, dev extra only, CityFlow not built, no corpus, no SUMO binary). READ THIS BEFORE CITING THE TREE: the instruction for this move named 2bce838 (P7.1's merge commit), and 2bce838's OWN run, 34708703166, CANNOT be classified -- it was cancelled 30 s in when the handoff commit superseded it (both suite legs 'cancelled' at 17:36:27-28, started 17:35:58) and it uploaded NO artifacts, which was checked against the API rather than assumed. 34708726985 at 64c2265 is the first run of P7.1's merged tree that completed and kept its junit.xml. The two trees differ by `git diff --stat 2bce838 64c2265` = 2 added lines in docs/notes/HANDOFF_2026-09-12.md: no test file, no conftest, no gating path. The substitution is recorded rather than glossed because 'the run named in the instruction' and 'the run measured' must never silently differ in this file.",
+              "result": "1800 passed, 156 skipped, 0 failures, 0 errors, 1956 tests collected. BOTH legs report identical counts AND an identical 39-message skip multiset (compared element-by-element, not by total), which is the cross-check. The suite step PASSED on both legs; only this ceiling gate failed. The delta CLOSES exactly against P7.1's own test count: collected +177 = passed +165 plus skipped +12, and 177 is the number of tests in P7.1's three new files as its Return Packet reports them.",
+              "breakdown": "COUNTED from each leg's junit.xml and classified BY INSPECTION of all 39 distinct skip messages, not by regex (BRIEF_30 D5): 76 corpus/checkpoint + 41 cityflow + 13 SUMO/traci + 2 matplotlib + 20 campaign-output + 4 grid4x4 candidates-root = 156. Zero messages were unclassified on either leg. METHOD, and it matters: the previous run's artifacts (34587670145, 9dd224e) were still retained, so the 144 and 156 multisets were diffed DIRECTLY -- 0 messages removed, 0 changed in text, 1 changed in count ('SUMO/traci not available' 10 -> 13) and 4 new messages carrying 9 skips. Every message that is byte-identical to one classified on 2026-09-11 KEEPS its recorded family; only the delta was classified afresh. That is what makes 76/41/2 carried values rather than re-litigated ones. THE DELTA: sumo_traci +3, all of it P7.1's own new SUMO-gated tests (test_backend_alignment 0 -> 2, test_sumo_att_reference 0 -> 1; test_sumo_env_phase_constraints stays at 8 and test_transfer_gate and test_dqn_queue_length_reward at 1 each). campaign_output +5, all of it 'P7.0's episodes are not present at .../output/p7_0: set RLTRAFFIC_OUTPUT_ROOT ...' (3 in test_sumo_att_reference, 2 in test_backend_alignment) -- the same gate shape and the same environment variable as the p8_4b_rederivation messages already filed there. grid4x4_resco_candidates 4 is NEW and is not any existing family: RLTRAFFIC_GRID4X4_RESCO points at a read-only, gitignored, CC BY-NC-SA candidates root that is neither the corpus, nor a campaign output tree, nor an engine, nor matplotlib (3 in test_conversion_audit, 1 in test_backend_alignment).",
+              "how_it_was_obtained": "`gh run download 34708726985 -D ...` for both legs, then every <skipped> message extracted from junit.xml with xml.etree, the file:line prefix stripped, and the multisets compared leg against leg (identical) and run against run. pytest.txt's terminal tail is the independent second parser and agreed at '1800 passed, 156 skipped' on both legs, so the number 156 was read four times by two parsers. junit's own arithmetic was checked as a third route: passed + skipped == collected (1800 + 156 == 1956).",
+              "an_independent_inspection_re_derived_THE_SAME_boundary_disagreement": "Before carrying the families forward, all 39 messages were classified from scratch by a reader who did not consult the recorded split. That pass returns corpus_or_checkpoint 79 / campaign_output 17 where this file records 76 / 20; cityflow 41, sumo_traci 13 and matplotlib 2 agree exactly and so does the total. Run against the 144 data it returns 79 / 12 against the record's 76 / 15 -- the SAME three skips, the same direction, the same magnitude as the disagreement the 2026-09-11 entry documented under 'an_honest_disagreement_about_the_CORPUS_versus_CAMPAIGN_boundary'. It is therefore the known ambiguity re-derived independently, not a new error, and the recorded split stands: it was made by inspection over byte-identical data, which is the method the protocol mandates. Only the TOTAL gates.",
+              "tree": "main at 64c2265dbd2433a3fa4a4431b2ec4fbdc3ed0439 (P7.1's merge 2bce838 plus a 2-line handoff note)",
+              "date": "2026-09-12",
               "measured_on_a_github_runner": true,
-              "reconfirmed": [
-                {
-                  "value": 144,
-                  "condition": "GitHub Actions run 34587670145, BOTH jobs 'suite-and-guards (ubuntu-24.04)' and '(ubuntu-22.04)', on main at 9dd224e after P5.3b-fix merged; workflow unchanged (pinned threads, dev extra only, CityFlow not built, no corpus, no SUMO binary).",
-                  "result": "1635 passed, 144 skipped, 0 failures, 0 errors, 1779 tests collected. BOTH legs report identical counts and an identical 35-message skip breakdown. The skip-ceiling gate PASSED on both legs -- the first time this protocol has run without the ceiling breaching.",
-                  "why_the_value_did_not_move": "P5.3b-fix added +80 collected tests (1699 -> 1779) and ZERO environment-gated ones: its new tests/test_nortg_decomposition.py is entirely synthetic (tmp_path fixtures, no corpus, no checkpoint, no engine) and its additions to tests/test_p5_3b_artifact.py read committed JSON under docs/data/. So the count of environment-gated skips is unchanged. ⭐ This is the first DIRECT evidence that the ceiling tracks the runner's environment rather than drifting with the suite's size, which is the property the gate was designed to have and which every previous move was consistent with but none demonstrated.",
-                  "how_it_was_obtained": "`gh run download 34587670145` for both legs, then every <skipped> message extracted from junit.xml with xml.etree, the file:line prefix stripped, and the multiset compared -- BOTH legs identical. pytest.txt's terminal tail is the independent second parser and agreed at '1635 passed, 144 skipped' on both legs.",
-                  "the_breakdown_is_UNCHANGED_and_this_was_verified_not_assumed": "The previous run's artifacts (34486928659, afb9062) were still retained, so the two runs' skip-message multisets were compared DIRECTLY rather than the classification being redone from memory: 35 distinct messages, identical counts, zero messages added, zero removed, zero changed. The recorded breakdown therefore describes exactly this data and is carried forward untouched. This is the check that distinguishes 'the number is the same' from 'the number is the same for the same reasons'.",
-                  "an_honest_disagreement_about_the_CORPUS_versus_CAMPAIGN_boundary": "A keyword classifier over the same 144 messages returns corpus_or_checkpoint 79 / campaign_output 12 where this file records 76 / 15; cityflow 41, sumo_traci 10 and matplotlib 2 agree exactly, and the total agrees exactly at 144. The disagreement is entirely about which side of the corpus/campaign line three messages sit on -- the clearest being 'the corpus, draws and campaign artifacts are not all present' (tests/test_att_rederivation.py), which names both families in one sentence. The recorded split is retained because it was made BY INSPECTION over byte-identical data, which is the method the protocol mandates, and a keyword rule is explicitly what it forbids. Recorded here so the next auditor meets the ambiguity instead of rediscovering it, and so nobody 'corrects' the breakdown to a regex's answer. Only the TOTAL gates.",
-                  "tree": "main at 9dd224e171286cd1dddc13f3b62610f97e8af645",
-                  "date": "2026-09-11",
-                  "measured_on_a_github_runner": true
-                }
-              ],
               "superseded": {
-                "value": 139,
-                "condition": "GitHub Actions run 33502850557, BOTH jobs 'suite-and-guards (ubuntu-24.04)' and '(ubuntu-22.04)', on main at 075a477 after P8.4b merged; workflow unchanged (pinned threads, dev extra only, CityFlow not built, no corpus, no SUMO binary). The ONLY failing step in that run is 'Gate - counts, cross-check and skip ceiling'; the suite, all three guards and the wheel are green, so this is the previous ceiling's own re_measure_required_at firing on the merged tree, not a defect.",
-                "result": "1445 passed, 139 skipped, 0 failures, 0 errors, 1584 tests collected. BOTH legs report identical counts, which is the cross-check: ubuntu-22.04 and ubuntu-24.04 each give tests=1584 skipped=139 failures=0 errors=0.",
-                "breakdown": "COUNTED, not derived, by extracting every <skipped> message from junit.xml, stripping the file:line prefix and classifying all 33 DISTINCT messages BY INSPECTION rather than by regex. cityflow 41 = 31 'cityflow not installed' + 9 'the CityFlow engine is not installed, so no episode can be rolled' + 1 'cityflow engine is not importable here'. sumo_traci 10. matplotlib 2. corpus_or_checkpoint 75 = 52 'format v1.1 corpus not found' across seven call sites + 7 'offline corpus not found' + 4 'the corpus is not present on this machine' + 6 'checkpoint not present in this tree' across four sites + 1 each for 'the corpus is not present at datasets_v11', 'datasets/ is not present', 'RLTRAFFIC_CORPUS_V11 is not set', 'RLTRAFFIC_P4_DT_CHECKPOINTS is not set', 'probe draw 201 is not materialised', 'surviving draw 1000 is absent'. campaign_output 11 = 3 're-derivation campaign output is not present' + 8 single distinct campaign-artifact messages. 41+10+2+75+11 = 139.",
-                "how_it_was_obtained": "gh run download 33502850557, then junit.xml parsed with xml.etree and the skips tallied by message. Not read off the job summary. Written by the coordinator at the author's explicit instruction; tests/** is normally outside the coordinator's role and the crossing is disclosed in the Decisions Log entry of 2026-09-01.",
-                "tree": "main at 075a477d968119f8740986711f752281cdb7ee2a",
-                "date": "2026-09-01",
+                "value": 144,
+                "condition": "GitHub Actions run 34486928659, BOTH jobs 'suite-and-guards (ubuntu-24.04)' and '(ubuntu-22.04)', on task/p5.3b-nortg-campaign at afb9062 after P5.3b's campaign artifact landed; workflow unchanged (pinned threads, dev extra only, CityFlow not built, no corpus, no SUMO binary)",
+                "result": "1555 passed, 144 skipped, 0 failures, 0 errors, 1699 tests collected. BOTH legs report identical counts AND an identical 35-message skip breakdown, which is the cross-check. The suite step PASSED on both legs; only this ceiling gate failed.",
+                "breakdown": "COUNTED from each run's junit.xml and classified BY INSPECTION of all 35 distinct skip messages, not by regex (BRIEF_30 D5): 76 corpus/checkpoint + 41 cityflow + 10 SUMO/traci + 2 matplotlib + 15 campaign-output = 144. Zero messages were unclassified on either leg. Delta from 139 is +5, entirely in two families: campaign_output 11 -> 15 (+4) and corpus_or_checkpoint 75 -> 76 (+1). cityflow, SUMO and matplotlib are unchanged at 41/10/2, which is what P5.3b should do -- it added corpus-, checkpoint- and campaign-output-gated tests and no engine-gated ones. TWO of the +4 are P5.3b's own (\"P8.4b's re-derived cells are not present ...\", tests/test_nortg_campaign.py); the other three arrived with P8.4b and were already in the tree at 139.",
+                "how_it_was_obtained": "`gh run download 34486928659 -D ...` for both legs, then every <skipped> message enumerated and assigned to a category by reading it. junit.xml is the instrument of record; pytest.txt's terminal tail is the independent second parser and agreed at 144 on both legs.",
+                "tree": "task/p5.3b-nortg-campaign at afb9062",
+                "date": "2026-09-10",
                 "measured_on_a_github_runner": true,
-                "the_delta_is_entirely_P8_4b": "+16 against 123, and every one is P8.4b's own new tests: cityflow 34 -> 41 (+7) and campaign_output 2 -> 11 (+9). corpus_or_checkpoint, sumo_traci and matplotlib are UNCHANGED. tests/test_engine_att_reference.py needs the CityFlow engine and tests/test_att_rederivation.py needs output/p8_4b_rederivation and the other campaign trees -- exactly what the superseded entry's expiry named before the merge happened.",
-                "coincidence_worth_naming": "ceiling_if_cityflow_were_built is now 98, which is ALSO a historical ceiling in the chain. Unrelated arithmetic -- 139 - 41 = 98 -- and it is named for the same reason the 72 coincidence was named one move ago: a reader meeting the number twice will otherwise assume a link that does not exist.",
+                "reconfirmed": [
+                  {
+                    "value": 144,
+                    "condition": "GitHub Actions run 34587670145, BOTH jobs 'suite-and-guards (ubuntu-24.04)' and '(ubuntu-22.04)', on main at 9dd224e after P5.3b-fix merged; workflow unchanged (pinned threads, dev extra only, CityFlow not built, no corpus, no SUMO binary).",
+                    "result": "1635 passed, 144 skipped, 0 failures, 0 errors, 1779 tests collected. BOTH legs report identical counts and an identical 35-message skip breakdown. The skip-ceiling gate PASSED on both legs -- the first time this protocol has run without the ceiling breaching.",
+                    "why_the_value_did_not_move": "P5.3b-fix added +80 collected tests (1699 -> 1779) and ZERO environment-gated ones: its new tests/test_nortg_decomposition.py is entirely synthetic (tmp_path fixtures, no corpus, no checkpoint, no engine) and its additions to tests/test_p5_3b_artifact.py read committed JSON under docs/data/. So the count of environment-gated skips is unchanged. ⭐ This is the first DIRECT evidence that the ceiling tracks the runner's environment rather than drifting with the suite's size, which is the property the gate was designed to have and which every previous move was consistent with but none demonstrated.",
+                    "how_it_was_obtained": "`gh run download 34587670145` for both legs, then every <skipped> message extracted from junit.xml with xml.etree, the file:line prefix stripped, and the multiset compared -- BOTH legs identical. pytest.txt's terminal tail is the independent second parser and agreed at '1635 passed, 144 skipped' on both legs.",
+                    "the_breakdown_is_UNCHANGED_and_this_was_verified_not_assumed": "The previous run's artifacts (34486928659, afb9062) were still retained, so the two runs' skip-message multisets were compared DIRECTLY rather than the classification being redone from memory: 35 distinct messages, identical counts, zero messages added, zero removed, zero changed. The recorded breakdown therefore describes exactly this data and is carried forward untouched. This is the check that distinguishes 'the number is the same' from 'the number is the same for the same reasons'.",
+                    "an_honest_disagreement_about_the_CORPUS_versus_CAMPAIGN_boundary": "A keyword classifier over the same 144 messages returns corpus_or_checkpoint 79 / campaign_output 12 where this file records 76 / 15; cityflow 41, sumo_traci 10 and matplotlib 2 agree exactly, and the total agrees exactly at 144. The disagreement is entirely about which side of the corpus/campaign line three messages sit on -- the clearest being 'the corpus, draws and campaign artifacts are not all present' (tests/test_att_rederivation.py), which names both families in one sentence. The recorded split is retained because it was made BY INSPECTION over byte-identical data, which is the method the protocol mandates, and a keyword rule is explicitly what it forbids. Recorded here so the next auditor meets the ambiguity instead of rediscovering it, and so nobody 'corrects' the breakdown to a regex's answer. Only the TOTAL gates.",
+                    "tree": "main at 9dd224e171286cd1dddc13f3b62610f97e8af645",
+                    "date": "2026-09-11",
+                    "measured_on_a_github_runner": true
+                  }
+                ],
                 "superseded": {
-                  "value": 123,
-                  "why_it_was_wrong": "Not wrong -- EXPIRED AS DECLARED, and this is the cleanest expiry in the chain because the entry named its own successor event before the merge that triggered it. The 2026-09-01 entry declared 're_measure_required_at: merge of P8.4b -- this move's OWN next expiry, because P8.4b adds tests/test_engine_att_reference.py and tests/test_att_rederivation.py, both carrying corpus-, draws- and CityFlow-gated tests'. It then DECLINED TO PREDICT THE DELTA for the fourth time running, and the no-prediction protocol was right again: observed delta +16, split cityflow +7 and campaign_output +9, which no one had forecast.",
+                  "value": 139,
+                  "condition": "GitHub Actions run 33502850557, BOTH jobs 'suite-and-guards (ubuntu-24.04)' and '(ubuntu-22.04)', on main at 075a477 after P8.4b merged; workflow unchanged (pinned threads, dev extra only, CityFlow not built, no corpus, no SUMO binary). The ONLY failing step in that run is 'Gate - counts, cross-check and skip ceiling'; the suite, all three guards and the wheel are green, so this is the previous ceiling's own re_measure_required_at firing on the merged tree, not a defect.",
+                  "result": "1445 passed, 139 skipped, 0 failures, 0 errors, 1584 tests collected. BOTH legs report identical counts, which is the cross-check: ubuntu-22.04 and ubuntu-24.04 each give tests=1584 skipped=139 failures=0 errors=0.",
+                  "breakdown": "COUNTED, not derived, by extracting every <skipped> message from junit.xml, stripping the file:line prefix and classifying all 33 DISTINCT messages BY INSPECTION rather than by regex. cityflow 41 = 31 'cityflow not installed' + 9 'the CityFlow engine is not installed, so no episode can be rolled' + 1 'cityflow engine is not importable here'. sumo_traci 10. matplotlib 2. corpus_or_checkpoint 75 = 52 'format v1.1 corpus not found' across seven call sites + 7 'offline corpus not found' + 4 'the corpus is not present on this machine' + 6 'checkpoint not present in this tree' across four sites + 1 each for 'the corpus is not present at datasets_v11', 'datasets/ is not present', 'RLTRAFFIC_CORPUS_V11 is not set', 'RLTRAFFIC_P4_DT_CHECKPOINTS is not set', 'probe draw 201 is not materialised', 'surviving draw 1000 is absent'. campaign_output 11 = 3 're-derivation campaign output is not present' + 8 single distinct campaign-artifact messages. 41+10+2+75+11 = 139.",
+                  "how_it_was_obtained": "gh run download 33502850557, then junit.xml parsed with xml.etree and the skips tallied by message. Not read off the job summary. Written by the coordinator at the author's explicit instruction; tests/** is normally outside the coordinator's role and the crossing is disclosed in the Decisions Log entry of 2026-09-01.",
+                  "tree": "main at 075a477d968119f8740986711f752281cdb7ee2a",
+                  "date": "2026-09-01",
+                  "measured_on_a_github_runner": true,
+                  "the_delta_is_entirely_P8_4b": "+16 against 123, and every one is P8.4b's own new tests: cityflow 34 -> 41 (+7) and campaign_output 2 -> 11 (+9). corpus_or_checkpoint, sumo_traci and matplotlib are UNCHANGED. tests/test_engine_att_reference.py needs the CityFlow engine and tests/test_att_rederivation.py needs output/p8_4b_rederivation and the other campaign trees -- exactly what the superseded entry's expiry named before the merge happened.",
+                  "coincidence_worth_naming": "ceiling_if_cityflow_were_built is now 98, which is ALSO a historical ceiling in the chain. Unrelated arithmetic -- 139 - 41 = 98 -- and it is named for the same reason the 72 coincidence was named one move ago: a reader meeting the number twice will otherwise assume a link that does not exist.",
                   "superseded": {
-                    "value": 121,
-                    "why_it_was_wrong": "Not wrong -- EXPIRED AS DECLARED. The 2026-08-26 entry named 'any task adding corpus-, checkpoint-, CityFlow-, SUMO- or matplotlib-gated tests' as its expiry. Observed delta +2, and it is NOT one category growing: cityflow +2 and campaign_output +1 against corpus_or_checkpoint -1, so part of the move is a RECLASSIFICATION of messages the previous count filed elsewhere. No prediction was made and none was needed.",
+                    "value": 123,
+                    "why_it_was_wrong": "Not wrong -- EXPIRED AS DECLARED, and this is the cleanest expiry in the chain because the entry named its own successor event before the merge that triggered it. The 2026-09-01 entry declared 're_measure_required_at: merge of P8.4b -- this move's OWN next expiry, because P8.4b adds tests/test_engine_att_reference.py and tests/test_att_rederivation.py, both carrying corpus-, draws- and CityFlow-gated tests'. It then DECLINED TO PREDICT THE DELTA for the fourth time running, and the no-prediction protocol was right again: observed delta +16, split cityflow +7 and campaign_output +9, which no one had forecast.",
                     "superseded": {
-                      "value": 104,
-                      "why_it_was_wrong": "Not wrong -- EXPIRED AS DECLARED, and this is the FIRST expiry under the walked-chain design. The 2026-08-24 entry named 'merge of P5.3' as its expiry and deliberately declined to predict the delta, on the record that two coordinator predictions of it had already been wrong. Observed delta +17, all of it in one category. No prediction was made and none was needed: the protocol -- merge, let the gate go red, classify junit.xml, commit the observed value with its breakdown -- produced the number in minutes.",
+                      "value": 121,
+                      "why_it_was_wrong": "Not wrong -- EXPIRED AS DECLARED. The 2026-08-26 entry named 'any task adding corpus-, checkpoint-, CityFlow-, SUMO- or matplotlib-gated tests' as its expiry. Observed delta +2, and it is NOT one category growing: cityflow +2 and campaign_output +1 against corpus_or_checkpoint -1, so part of the move is a RECLASSIFICATION of messages the previous count filed elsewhere. No prediction was made and none was needed.",
                       "superseded": {
-                        "value": 98,
-                        "why_it_was_wrong": "Not wrong -- EXPIRED AS DECLARED. The 2026-08-19 entry named 'merge of P5.2' as its expiry. Observed delta +6. The coordinator's own predictions of that delta were wrong TWICE and are recorded because the pattern matters: '+1' on 2026-08-19 (read from a test file that then grew from 89 to 146 tests) and '>=4' on 2026-08-23 (counting skip SITES, which undercounts because one is parametrised). The protocol -- merge, read the observed number, commit it -- got it right both times without a prediction.",
+                        "value": 104,
+                        "why_it_was_wrong": "Not wrong -- EXPIRED AS DECLARED, and this is the FIRST expiry under the walked-chain design. The 2026-08-24 entry named 'merge of P5.3' as its expiry and deliberately declined to predict the delta, on the record that two coordinator predictions of it had already been wrong. Observed delta +17, all of it in one category. No prediction was made and none was needed: the protocol -- merge, let the gate go red, classify junit.xml, commit the observed value with its breakdown -- produced the number in minutes.",
                         "superseded": {
-                          "value": 72,
-                          "why_it_was_wrong": "Not wrong -- EXPIRED EXACTLY AS PREDICTED. The 2026-08-17 entry declared 're_measure_required_at: merge of P5.1' and forecast 'roughly +25 skips ... this ceiling WILL breach on that merge, and it is meant to'. Observed delta: +26. The mechanism worked; the number was stale.",
+                          "value": 98,
+                          "why_it_was_wrong": "Not wrong -- EXPIRED AS DECLARED. The 2026-08-19 entry named 'merge of P5.2' as its expiry. Observed delta +6. The coordinator's own predictions of that delta were wrong TWICE and are recorded because the pattern matters: '+1' on 2026-08-19 (read from a test file that then grew from 89 to 146 tests) and '>=4' on 2026-08-23 (counting skip SITES, which undercounts because one is parametrised). The protocol -- merge, read the observed number, commit it -- got it right both times without a prediction.",
                           "superseded": {
-                            "value": 62,
-                            "why_it_was_wrong": "It came from a simulated runner on the dev machine, and the simulation was defeated by usrmerge: the harness removed `sumo` from a symlink copy of /usr/bin but left /bin on PATH, and /bin IS /usr/bin on this system, so shutil.which('sumo') kept finding it and 10 SUMO-gated tests ran that a runner skips. The error was invisible locally and was caught by the first real run -- which is the reason the ceiling was declared provisional and the branch was pushed."
+                            "value": 72,
+                            "why_it_was_wrong": "Not wrong -- EXPIRED EXACTLY AS PREDICTED. The 2026-08-17 entry declared 're_measure_required_at: merge of P5.1' and forecast 'roughly +25 skips ... this ceiling WILL breach on that merge, and it is meant to'. Observed delta: +26. The mechanism worked; the number was stale.",
+                            "superseded": {
+                              "value": 62,
+                              "why_it_was_wrong": "It came from a simulated runner on the dev machine, and the simulation was defeated by usrmerge: the harness removed `sumo` from a symlink copy of /usr/bin but left /bin on PATH, and /bin IS /usr/bin on this system, so shutil.which('sumo') kept finding it and 10 SUMO-gated tests ran that a runner skips. The error was invisible locally and was caught by the first real run -- which is the reason the ceiling was declared provisional and the branch was pushed."
+                            }
                           }
                         }
                       }
                     }
-                  }
+                  },
+                  "why_it_was_wrong": "Not wrong -- EXPIRED AS DECLARED. The 2026-09-01 entry named 'merge of P5.3b' as its expiry and deliberately declined to predict the delta. Observed delta +5, confined to two families: campaign_output 11 -> 15 and corpus_or_checkpoint 75 -> 76, with cityflow, SUMO and matplotlib unchanged at 41/10/2. No prediction was made and none was needed: the protocol -- merge, let the gate go red, classify junit.xml, commit the observed value with its breakdown -- produced the number in minutes."
                 },
-                "why_it_was_wrong": "Not wrong -- EXPIRED AS DECLARED. The 2026-09-01 entry named 'merge of P5.3b' as its expiry and deliberately declined to predict the delta. Observed delta +5, confined to two families: campaign_output 11 -> 15 and corpus_or_checkpoint 75 -> 76, with cityflow, SUMO and matplotlib unchanged at 41/10/2. No prediction was made and none was needed: the protocol -- merge, let the gate go red, classify junit.xml, commit the observed value with its breakdown -- produced the number in minutes."
+                "why_it_was_wrong": "Not wrong -- EXPIRED AS DECLARED, for the sixth time, and the expiry clause named the trigger class that fired: 'any task adding corpus-, checkpoint-, campaign-output-, CityFlow-, SUMO- or matplotlib-gated tests'. P7.1's merge added tests gated on output/p7_0 and on SUMO. It ALSO added a gate class the clause did not model -- an external, read-only, CC BY-NC-SA candidates root (RLTRAFFIC_GRID4X4_RESCO) -- so the clause was right about the event and incomplete about the families, and it now names six. Observed delta +12: campaign_output 15 -> 20, sumo_traci 10 -> 13, and a new grid4x4_resco_candidates 4. corpus_or_checkpoint, cityflow and matplotlib did not move. No prediction was made, for the fifth time, and none was needed."
               },
-              "why_it_was_wrong": "Not wrong -- EXPIRED AS DECLARED, for the sixth time, and the expiry clause named the trigger class that fired: 'any task adding corpus-, checkpoint-, campaign-output-, CityFlow-, SUMO- or matplotlib-gated tests'. P7.1's merge added tests gated on output/p7_0 and on SUMO. It ALSO added a gate class the clause did not model -- an external, read-only, CC BY-NC-SA candidates root (RLTRAFFIC_GRID4X4_RESCO) -- so the clause was right about the event and incomplete about the families, and it now names six. Observed delta +12: campaign_output 15 -> 20, sumo_traci 10 -> 13, and a new grid4x4_resco_candidates 4. corpus_or_checkpoint, cityflow and matplotlib did not move. No prediction was made, for the fifth time, and none was needed."
+              "why_it_was_wrong": "Not wrong -- EXPIRED AS DECLARED. re_measure_required_at named 'any task adding corpus-, checkpoint-, campaign-output-, CityFlow-, SUMO-, matplotlib- or candidates-root-gated tests', and P7.2a's merge (5d57474, 2026-09-13, SUMO draws under the parity contract) added exactly two: one SUMO-gated (tests/test_materialise_parity.py::test_the_parity_type_and_the_teleport_regime_are_what_the_engine_actually_runs -- cf_parity and time-to-teleport read out of the running engine) and one CityFlow-gated (::test_regenerated_draw_201_reproduces_p4_3s_recorded_episode -- the P4.3 band gate). 156 -> 158, both on messages that already existed; no new family."
             },
-            "why_it_was_wrong": "Not wrong -- EXPIRED AS DECLARED. re_measure_required_at named 'any task adding corpus-, checkpoint-, campaign-output-, CityFlow-, SUMO-, matplotlib- or candidates-root-gated tests', and P7.2a's merge (5d57474, 2026-09-13, SUMO draws under the parity contract) added exactly two: one SUMO-gated (tests/test_materialise_parity.py::test_the_parity_type_and_the_teleport_regime_are_what_the_engine_actually_runs -- cf_parity and time-to-teleport read out of the running engine) and one CityFlow-gated (::test_regenerated_draw_201_reproduces_p4_3s_recorded_episode -- the P4.3 band gate). 156 -> 158, both on messages that already existed; no new family."
+            "why_it_was_wrong": "Not wrong -- EXPIRED AS DECLARED. re_measure_required_at named 'merge of P7.2b (the SUMO probe, the aligned-env wrapper and the fenced smoke add SUMO- and checkpoint-gated tests)', and P7.2b's merge (ad7fa1a, 2026-09-15) added 65 tests, 19 of which skip on a runner; then Amendment G (8c83fd8, 2026-09-16) converted the ONE ungated test -- it had put CI red with a real FileNotFoundError (probe draw 201's parity configuration is a gitignored P7.2a product), NOT a ceiling breach, which is why the bump waited for it -- into a gated skip and added one CI-runnable test. 20 new skips, all checkpoint- or parity-configuration-gated: exactly the families the clause named. The CI red between ad7fa1a and 8c83fd8 was therefore a failure and a ceiling breach at once; this file records only the ceiling, and the failure is in PROJECT_PLAN's Decisions Log of 2026-09-16."
           },
-          "why_it_was_wrong": "Not wrong -- EXPIRED AS DECLARED. re_measure_required_at named 'merge of P7.2b (the SUMO probe, the aligned-env wrapper and the fenced smoke add SUMO- and checkpoint-gated tests)', and P7.2b's merge (ad7fa1a, 2026-09-15) added 65 tests, 19 of which skip on a runner; then Amendment G (8c83fd8, 2026-09-16) converted the ONE ungated test -- it had put CI red with a real FileNotFoundError (probe draw 201's parity configuration is a gitignored P7.2a product), NOT a ceiling breach, which is why the bump waited for it -- into a gated skip and added one CI-runnable test. 20 new skips, all checkpoint- or parity-configuration-gated: exactly the families the clause named. The CI red between ad7fa1a and 8c83fd8 was therefore a failure and a ceiling breach at once; this file records only the ceiling, and the failure is in PROJECT_PLAN's Decisions Log of 2026-09-16."
+          "why_it_was_wrong": "Not wrong -- EXPIRED AS DECLARED. Its own re_measure_required_at named the merge of P7.3, and this is that event: P7.3a's merge (b480c7b) added 10 gated skips -- SUMO-, checkpoint- and draw-gated tests of the zero-shot campaign -- and the shallow-checkout fix that followed added 1 more, taking 178 to 189 on run 35338316324."
         },
-        "why_it_was_wrong": "Not wrong -- EXPIRED AS DECLARED. Its own re_measure_required_at named the merge of P7.3, and this is that event: P7.3a's merge (b480c7b) added 10 gated skips -- SUMO-, checkpoint- and draw-gated tests of the zero-shot campaign -- and the shallow-checkout fix that followed added 1 more, taking 178 to 189 on run 35338316324."
+        "why_it_was_wrong": "Not wrong -- EXPIRED AS DECLARED. Its own re_measure_required_at named the merge of P7.3b, and this is that event: P7.3b's merge (732f378) added the anchor-corpus gate of tests/test_anchor_training.py, and the CI fix that followed (a790ad1, merged as 708f157) made test_the_anchor_driver_refuses_a_bogus_stage_and_creates_nothing skip where the main tree's interpreter is absent -- both classified by inspection of the two new junit messages. Measured on run 35459798196 at 708f157: 189 -> 191."
       }
     },
     "ceiling_is_a_consequence_of": [
@@ -135,7 +146,7 @@
       "shallow_checkout (1): actions/checkout@v4 checks out with depth 1, so `git rev-list --max-parents=0 HEAD` returns HEAD itself and the test that proves `code_changed_since` reads real history has no root commit to compare against. It skips on `git rev-parse --is-shallow-repository`, naming that condition. Taking fetch-depth 0 for one test was weighed and refused: it would clone this repository's whole history on every run."
     ],
     "re_measure_required_at": {
-      "event": "merge of P7.3b (BRIEF_38: the A18(a) full-retrain anchor -- a SUMO-trained DT on 200 MaxPressure probe episodes, which adds corpus-, checkpoint- and campaign-output-gated tests), or any task adding corpus-, checkpoint-, campaign-output-, CityFlow-, SUMO-, matplotlib- or candidates-gated tests",
+      "event": "merge of P7.3d (BRIEF_39: the grid4x4 zero-shot point -- SUMO-, RESCO-candidates-, checkpoint- and campaign-output-gated tests), or any task adding corpus-, checkpoint-, campaign-output-, CityFlow-, SUMO-, matplotlib- or candidates-gated tests",
       "mandated_by": "coordinator ruling on BRIEF_23 Q1, 2026-08-17 -- 'a ceiling whose expiry lives only in a chat is a ceiling that silently breaches'.",
       "predicted_delta": "UNKNOWN until measured. P7.3a's own delta was +11 over 178 (10 gated tests plus the shallow-checkout skip). A prediction is never committed: this file records only what a run reported.",
       "reason": "The ceiling is a COUNT of environment-gated skips, not a policy, so every task that adds a corpus-, CityFlow-, SUMO- or matplotlib-gated test moves it: it is stale by construction after any such merge, and a stale ceiling stops gating without saying so. RESTORED 2026-08-24 -- this field was dropped in 69680fa while ci_gate.py:39 still declares it part of the format and ci_gate.py:308 still reads it through a .get(..., '') default, so its absence printed an empty clause into every job summary instead of failing. tests/test_ci_gate.py:687 caught it; the gate could not.",
diff --git a/tests/test_ci_gate.py b/tests/test_ci_gate.py
index 1f79be6..3679209 100644
--- a/tests/test_ci_gate.py
+++ b/tests/test_ci_gate.py
@@ -728,7 +728,7 @@ def test_committed_baseline_records_why_the_ceiling_has_the_value_it_has() -> No
     #: The declared ceiling and every value it supersedes, newest first.  ONE literal, walked
     #: rather than addressed by depth -- see the docstring for why the depth-addressed form let
     #: the root drop out of the record.
-    CEILING_CHAIN = (189, 178, 158, 156, 144, 139, 123, 121, 104, 98, 72, 62)
+    CEILING_CHAIN = (191, 189, 178, 158, 156, 144, 139, 123, 121, 104, 98, 72, 62)
 
     baseline = json.loads(BASELINE_FILE.read_text(encoding="utf-8"))
     block = baseline["pytest"]
