--- a/.github/ci/ci_baseline.json
+++ b/.github/ci/ci_baseline.json
@@ -3,51 +3,55 @@
   "written_by": "P0.10 (BRIEF_23), 2026-08-17, branch task/p0.10-reproducibility",
   "read_by": ".github/ci/ci_gate.py -- see its module docstring for the format contract",
   "pytest": {
-    "skip_ceiling": 104,
+    "skip_ceiling": 121,
     "skip_breakdown": {
-      "corpus_or_checkpoint": 59,
+      "corpus_or_checkpoint": 76,
       "cityflow": 32,
       "sumo_traci": 10,
       "matplotlib": 2,
       "campaign_output": 1
     },
-    "ceiling_if_cityflow_were_built": 72,
+    "ceiling_if_cityflow_were_built": 89,
     "superseded_chain_is_uniformly_named": "2026-08-24. Every link of measured.superseded.* is now keyed 'superseded', at every depth. It used to be 'superseded' at depth 1 and 'its_own_superseded' below, which is TWO NAMES FOR ONE RELATION and is exactly why the chain could only be pinned one depth at a time: a generic walk stops at the first name it does not recognise, and the coordinator's first attempt at this patch did precisely that, reading the chain as (104, 98) and stopping. Caught by running the test, not by reading it. tests/test_ci_gate.py now asserts the legacy key is absent, so the shape cannot come back.",
     "breakdown_and_alternative_added": "2026-08-24. The breakdown existed only as prose inside measured.breakdown and the alternative only as prose inside ceiling_is_a_consequence_of, so NOTHING checked that the parts summed to the ceiling -- the same defect as the guards' by_file-versus-total gap recorded in guards.english.by_file_note, one block higher in this same file. tests/test_ci_gate.py now closes both by arithmetic. NOTE the coincidence, because it will confuse someone: ceiling_if_cityflow_were_built is 72, which is also the ceiling this file carried on 2026-08-17. Unrelated -- 104 - 32 = 72.",
     "provisional": true,
     "measured": {
-      "value": 104,
-      "condition": "GitHub Actions run 32753659114, job 'suite-and-guards (ubuntu-24.04)', on main at 63e384e after P5.2 merged; workflow unchanged (pinned threads, dev extra only, CityFlow not built, no corpus, no SUMO binary)",
-      "result": "1240 passed, 104 skipped, read from the run's own pytest.txt artifact. Both matrix legs failed this gate and nothing else; libc-matrix passed, and all three guards passed.",
-      "breakdown": "COUNTED, not derived, from the run's junit.xml by classifying every <skipped> message: 59 corpus/checkpoint + 32 cityflow + 10 SUMO/traci + 2 matplotlib + 1 'the campaign output is not present on this machine' = 104. Only the corpus family moved (54 -> 59, +5) plus the one new campaign-output gate, because P5.2 added corpus-gated tests and no cityflow-, SUMO- or matplotlib-gated ones.",
-      "how_it_was_obtained": "`gh run view --log` returned ZERO lines for both jobs, so the log route was unavailable. The artifacts were: `gh run download <id> -n ci-artifacts-ubuntu-24.04`, which carries pytest.txt, junit.xml, english.out and hygiene.out. junit.xml is the better instrument anyway -- it classifies each skip by its own message instead of relying on a grep over aggregated `SKIPPED [N]` lines, which is what defeated the 2026-08-19 attempt to recount 98 from a log.",
-      "tree": "main at 63e384e",
-      "date": "2026-08-24",
+      "value": 121,
+      "condition": "GitHub Actions run 33011650275, BOTH jobs 'suite-and-guards (ubuntu-24.04)' and '(ubuntu-22.04)', on main at f4a73d3 after P5.3a merged; workflow unchanged (pinned threads, dev extra only, CityFlow not built, no corpus, no SUMO binary)",
+      "result": "1297 passed, 121 skipped. FOUR independent readings agree: junit.xml AND pytest.txt on EACH of the two legs, all four giving 121 and the same five-category breakdown. Only the ceiling gate failed -- the suite step itself PASSED on both legs, as did all three guards, the wheel gate and libc-matrix.",
+      "breakdown": "COUNTED, not derived, from each run's junit.xml by classifying every <skipped> message: 76 corpus/checkpoint + 32 cityflow + 10 SUMO/traci + 2 matplotlib + 1 campaign-output = 121. THE ENTIRE +17 IS THE CORPUS/CHECKPOINT FAMILY (59 -> 76); cityflow, SUMO, matplotlib and campaign-output are unchanged at 32/10/2/1, which is exactly what P5.3a should do -- it added corpus- and checkpoint-gated tests in tests/test_rtg_mode_identity.py, tests/test_rtg_probe_second_route.py and tests/test_rtg_ablation.py, and no engine-gated ones. Six of the +17 carry a NEW message shape, 'checkpoint not present in this tree: <path>'; two more are 'surviving draw 1000 is absent' and 'no integrity manifests in this tree'.",
+      "how_it_was_obtained": "`gh run download 33011650275 -n ci-artifacts-ubuntu-24.04` (and -22.04), which carry pytest.txt, junit.xml, english.out and hygiene.out. Unlike 2026-08-24 the log route was not needed. junit.xml remains the instrument of record because it classifies each skip by its own message; pytest.txt's terminal tail is the independent second parser and agreed at 121 on both legs.",
+      "tree": "main at f4a73d3",
+      "date": "2026-08-26",
       "measured_on_a_github_runner": true,
       "superseded": {
-        "value": 98,
-        "why_it_was_wrong": "Not wrong -- EXPIRED AS DECLARED. The 2026-08-19 entry named 'merge of P5.2' as its expiry. Observed delta +6. The coordinator's own predictions of that delta were wrong TWICE and are recorded because the pattern matters: '+1' on 2026-08-19 (read from a test file that then grew from 89 to 146 tests) and '>=4' on 2026-08-23 (counting skip SITES, which undercounts because one is parametrised). The protocol -- merge, read the observed number, commit it -- got it right both times without a prediction.",
+        "value": 104,
+        "why_it_was_wrong": "Not wrong -- EXPIRED AS DECLARED, and this is the FIRST expiry under the walked-chain design. The 2026-08-24 entry named 'merge of P5.3' as its expiry and deliberately declined to predict the delta, on the record that two coordinator predictions of it had already been wrong. Observed delta +17, all of it in one category. No prediction was made and none was needed: the protocol -- merge, let the gate go red, classify junit.xml, commit the observed value with its breakdown -- produced the number in minutes.",
         "superseded": {
-          "value": 72,
-          "why_it_was_wrong": "Not wrong -- EXPIRED EXACTLY AS PREDICTED. The 2026-08-17 entry declared 're_measure_required_at: merge of P5.1' and forecast 'roughly +25 skips ... this ceiling WILL breach on that merge, and it is meant to'. Observed delta: +26. The mechanism worked; the number was stale.",
+          "value": 98,
+          "why_it_was_wrong": "Not wrong -- EXPIRED AS DECLARED. The 2026-08-19 entry named 'merge of P5.2' as its expiry. Observed delta +6. The coordinator's own predictions of that delta were wrong TWICE and are recorded because the pattern matters: '+1' on 2026-08-19 (read from a test file that then grew from 89 to 146 tests) and '>=4' on 2026-08-23 (counting skip SITES, which undercounts because one is parametrised). The protocol -- merge, read the observed number, commit it -- got it right both times without a prediction.",
           "superseded": {
-            "value": 62,
-            "why_it_was_wrong": "It came from a simulated runner on the dev machine, and the simulation was defeated by usrmerge: the harness removed `sumo` from a symlink copy of /usr/bin but left /bin on PATH, and /bin IS /usr/bin on this system, so shutil.which('sumo') kept finding it and 10 SUMO-gated tests ran that a runner skips. The error was invisible locally and was caught by the first real run -- which is the reason the ceiling was declared provisional and the branch was pushed."
+            "value": 72,
+            "why_it_was_wrong": "Not wrong -- EXPIRED EXACTLY AS PREDICTED. The 2026-08-17 entry declared 're_measure_required_at: merge of P5.1' and forecast 'roughly +25 skips ... this ceiling WILL breach on that merge, and it is meant to'. Observed delta: +26. The mechanism worked; the number was stale.",
+            "superseded": {
+              "value": 62,
+              "why_it_was_wrong": "It came from a simulated runner on the dev machine, and the simulation was defeated by usrmerge: the harness removed `sumo` from a symlink copy of /usr/bin but left /bin on PATH, and /bin IS /usr/bin on this system, so shutil.which('sumo') kept finding it and 10 SUMO-gated tests ran that a runner skips. The error was invisible locally and was caught by the first real run -- which is the reason the ceiling was declared provisional and the branch was pushed."
+            }
           }
         }
       }
     },
     "ceiling_is_a_consequence_of": [
-      "the workflow does NOT build the vendored CityFlow (coordinator ruling 2026-08-17: an unverified C++ build step is a new failure surface on the one task whose purpose is reliability). 32 of these 104 skips are cityflow-gated, so building it would move the ceiling from 104 to 72. CORRECTED 2026-08-24: this entry read '32 of these 72 ... from 72 to 40' and had survived TWO ceiling moves (72 -> 98 -> 104) unedited, because the only assertion over it was a substring grep, assert '40' in json.dumps(block), and that assertion EXPIRED rather than having always been empty. Measured 2026-08-24 on both eras: with the alternative deleted the expression is False on the 98-era file (4866d52) and True on this one, because the 104 measurement itself introduced the string '1240 passed' into measured.result. A substring assertion's discriminating power is a function of data it does not name, so it can stop discriminating with nobody editing it. The arithmetic is now pinned to ceiling_if_cityflow_were_built instead.",
-      "no corpus is available on a runner: datasets*/ is gitignored and ~189 MB, so 59 corpus- and checkpoint-gated tests skip. CORRECTED 2026-08-24 from 28, which was the 2026-08-17 measurement and had also survived two ceiling moves unedited; the 28 that once reproduced the coordinator's 2026-08-16 corpus-absent count is history, and P4.7, P5.1 and P5.2 each added corpus-gated tests since.",
+      "the workflow does NOT build the vendored CityFlow (coordinator ruling 2026-08-17: an unverified C++ build step is a new failure surface on the one task whose purpose is reliability). 32 of these 121 skips are cityflow-gated, so building it would move the ceiling from 121 to 89. The arithmetic is pinned to ceiling_if_cityflow_were_built and checked by tests/test_ci_gate.py; the prose figure is a reading aid, and it went two ceiling generations stale in 2026-08 precisely because a substring grep was all that guarded it.",
+      "no corpus is available on a runner: datasets*/ is gitignored and ~189 MB, so 76 corpus- and checkpoint-gated tests skip. This family absorbed the ENTIRE +17 of the P5.3a move (59 -> 76) and the entire +5 of the P5.2 move before it. It is the only family that has moved since 2026-08-17 and it is the one to expect to move again.",
       "SUMO is not installed on the runner, so _sumo_available() is False and 10 SUMO-backed tests skip cleanly. sumolib IS present -- traci declares sumolib>=1.27.1 and pip resolved it -- so the gate here is the missing binary, not the missing module.",
       "the workflow installs the dev extra only, so the 2 matplotlib-gated tests skip.",
       "P5.2's campaign output lives in the gitignored output/p5_2, absent on a runner, so 1 further test skips. This category did not exist before 2026-08-24 and was missing from this list while being counted in measured.breakdown."
     ],
     "re_measure_required_at": {
-      "event": "merge of P5.3 (the no-RTG / context-length ablations), or any task adding corpus-gated tests",
+      "event": "merge of P5.3b (the dt_nortg campaign) or P5.3c (context-length K), or any task adding corpus-, checkpoint-, CityFlow-, SUMO- or matplotlib-gated tests",
       "reason": "The ceiling is a COUNT of environment-gated skips, not a policy, so every task that adds a corpus-, CityFlow-, SUMO- or matplotlib-gated test moves it: it is stale by construction after any such merge, and a stale ceiling stops gating without saying so. RESTORED 2026-08-24 -- this field was dropped in 69680fa while ci_gate.py:39 still declares it part of the format and ci_gate.py:308 still reads it through a .get(..., '') default, so its absence printed an empty clause into every job summary instead of failing. tests/test_ci_gate.py:687 caught it; the gate could not.",
-      "predicted_delta": "NOT PREDICTED -- deliberately. Two coordinator predictions of this delta have been wrong; the protocol below does not need one.",
+      "predicted_delta": "NOT PREDICTED -- deliberately, for the third time. Two coordinator predictions of this delta were wrong; the third expiry was handled with no prediction at all and cost minutes. The protocol does not need one.",
       "what_to_do": "Merge, let this gate go red, download `ci-artifacts-<leg>` and classify junit.xml's <skipped> messages, then commit the observed value with its breakdown. Do NOT pre-bump and do NOT widen with slack: slack is what blinds the gate.",
       "mandated_by": "coordinator ruling on BRIEF_23 Q1, 2026-08-17 -- 'a ceiling whose expiry lives only in a chat is a ceiling that silently breaches'."
     },
--- a/tests/test_ci_gate.py
+++ b/tests/test_ci_gate.py
@@ -704,7 +704,7 @@
     A future reader must be able to see that the number is a consequence of a choice, and
     what the number would be under the other choice.
 
-    The chain is **104 -> 98 -> 72 -> 62**, newest first.  Only the root was ever WRONG: 62 came
+    The chain is **121 -> 104 -> 98 -> 72 -> 62**, newest first.  Only the root was ever WRONG: 62 came
     from a simulated runner (see section "harness errors" of ``docs/returns/P0.10.md``).  72 and 98
     EXPIRED EXACTLY AS THEIR OWN ENTRIES SAID THEY WOULD, on the merges of P5.1 and P5.2.  The chain
     is pinned here, in a second file, precisely so that widening it is a visible edit in a diff
@@ -718,11 +718,17 @@
     happen.  ``DEFERRED`` 54, third instance.  The walk below reaches the root at whatever depth it
     lands.  *(If a skip CATEGORY moves rather than the total, ``skip_breakdown`` in the baseline
     moves too and the arithmetic below fails until it does -- that is intended.)*
+
+    ✅ **MEASURED 2026-08-26, the first move under this design: it really was ONE edit.**  The
+    104 -> 121 move (P5.3a's merge, +17 skips, all of them corpus/checkpoint-gated) was made by
+    changing ``CEILING_CHAIN`` and nothing else in this file, and the 34 tests passed.  Under the
+    previous depth-addressed form the same move needed four edits and the fourth was masked by the
+    first (``DEFERRED`` 54).  The claim is now checked rather than argued.
     """
     #: The declared ceiling and every value it supersedes, newest first.  ONE literal, walked
     #: rather than addressed by depth -- see the docstring for why the depth-addressed form let
     #: the root drop out of the record.
-    CEILING_CHAIN = (104, 98, 72, 62)
+    CEILING_CHAIN = (121, 104, 98, 72, 62)
 
     baseline = json.loads(BASELINE_FILE.read_text(encoding="utf-8"))
     block = baseline["pytest"]
