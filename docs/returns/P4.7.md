# RETURN PACKET — P4.7 (the mixture tiers)
**Date:** 2026-08-15 · **Mode:** Claude Code · **Contracts:** v1.1

**Status: PARTIAL — everything except the 12 training cells is complete and verified.**
The campaign script is written, its preconditions all pass, and **it has not been launched**:
`output/p4_7/checkpoints` holds **0** files, measured at the time of writing. `BRIEF_17` §12
condition 1 gives the launch to the user, and I neither launch nor poll it. **Q1 and Q3 are already
scored** — both cost zero compute — so only **Q2, Gate P1's execution and the final artifact** wait
on the run.

---

## 1. Branch and diff

```
task/p4.7-mixture-tiers
$ git diff --stat main...HEAD
 docs/data/p4_7_declaration.json           | 10218 ++++++++++++++++++++++++++++
 docs/data/p4_7_selection_diagnostics.json |   502 ++
 docs/plans/p4.7.md                        |   590 ++
 offline/campaigns/p4_7_phase2.sh          |   213 +
 offline/method_tier_grid.py               |   101 +-
 offline/mixture_tiers.py                  |  1318 ++++
 tests/test_dt_agent.py                    |     6 +-
 tests/test_dt_gate.py                     |    12 +-
 tests/test_flow_randomizer.py             |     2 +-
 tests/test_method_tier_grid.py            |     6 +-
 tests/test_mixture_tiers.py               |   447 ++
 tests/test_offline_dataset.py             |     6 +-
 tests/test_p4_7_constructed_reference.py  |   321 +
 tests/test_p4_7_gate_d.py                 |   262 +
 tests/test_p4_7_predictions.py            |   494 ++
 tests/test_rtg_calibration_guards.py      |   326 +
 16 files changed, 14794 insertions(+), 30 deletions(-)
```

**Zero frozen files touched.** `bash scripts/claude_guard.sh --frozen-only` (no arguments) → **0**.
The only non-new source file changed is `offline/method_tier_grid.py`, which is ours, and both
changes were authorised: RULING 1 (three `TierSpec` declaration lines) and RULING 3
(`--artifact-prefix`, additive).

## 2. What each commit established

| commit | what it proves |
|---|---|
| `7df6d97` | the plan, with Q1–Q3 registered **before any P4.7 number existed** |
| `c8bf51d` | the mixture path **executed** on the real corpus; its declaration was **unrunnable** |
| `e536265` | **Gate D** — the demand behind every merged P4.6 number reproduces |
| `639f467` | **Gate G PASS** in this session; the declaration; the campaign script |
| `969675f` | RULING 1's amendment on the scale formula, and what it would have cost |
| `3c640af` | the **constructed** behaviour reference, and the variance the ruling protects |
| `e9c0d8e` | Gate P1, the Q1–Q3 scorers, the report path |
| `b1b8f10` | **`DEFERRED` 44** closed — 6/6 mutations killed |
| `ad7508a` | **`DEFERRED` 43**'s remainder — 28 ambiguous → 11 |
| `26685a1` | FINDING 1's **declaration audit** |
| `9111ba2` | **Q1 and Q3 scored** — and why Q1 could not have failed |

## 3. ⭐ The findings, in the order they matter

### 3.1 The mixture declaration was UNRUNNABLE, and it went unnoticed because the path never ran

All three mixture `TierSpec`s declared `target_rtg = −5762.0, rtg_scale = 40294.0` — the **union's**
600-episode maximum and largest absolute return, from `docs/plans/p4.6.md` §8. But
`assert_declaration_matches_corpus` computes both over the **composed 200-stream training set**
(`BRIEF_17` §11 A4). **Measured: neither the union's best expert stream (`−5762.0`,
`ep000013_seed1000_draw14.npz`) nor its worst random stream (`−40294.0`,
`ep000221_seed1001_draw111.npz`) is drawn into ANY of the three mixtures.** So all six values were
wrong and `train --tier mix33` would have raised before its first gradient step.

**Corrected by RULE, not by number** (RULING 1), recomputed from the raw `.npz` by an independent
route (`load_episode` + `math.fsum`, never `stream_returns`):

| tier | `target_rtg` | `rtg_scale` | superseded |
|---|---|---|---|
| `mix33` | **−5994.0** | **40223.0** | −5762.0 / 40294.0 |
| `mix50` | **−5959.0** | **40223.0** | −5762.0 / 40294.0 |
| `mix67` | **−5959.0** | **40123.0** | −5762.0 / 40294.0 |

### 3.2 ⚠️ RULING 1's original `max − min` wording would have confounded the comparison

Applied literally it gives **34229.0 / 34264.0 / 34164.0** — about **15 % below** the repo's
`max(|min|, |max|)` — so the three mixture tiers would have been RTG-normalised on a different scale
from the five phase-1 tiers they are compared with. **The DT would have been trained and prompted
under a different normalisation inside the comparison the task exists to make**, and nothing would
have crashed. I implemented the repo's form and flagged it (CLAUDE.md §2); the coordinator verified
5/5 phase-1 tiers on the repo's form, 0/5 on the other, and amended the ruling.

### 3.3 Gate D — the demand behind every merged P4.6 number had never been checked

Of the 100 held-out draws, **5 directories survive** anywhere on this machine. The other 95 existed
only as `materialise_draws` plus a seed.

```
draw 1000: byte-identical ['flow.json', 'routes.rou.xml']  path-normalised ['cityflow.json']
draw 1001: byte-identical ['flow.json', 'routes.rou.xml']  path-normalised ['cityflow.json']
draw 1002: byte-identical ['flow.json', 'routes.rou.xml']  path-normalised ['cityflow.json']
draw 1003: byte-identical ['flow.json', 'routes.rou.xml']  path-normalised ['cityflow.json']
draw 1004: byte-identical ['flow.json', 'routes.rou.xml']  path-normalised ['cityflow.json']
GATE D PASS: 5 of the 5 surviving held-out draws reproduce
```

`cityflow.json` differs only in `dir` — the absolute scenario path, which a second checkout moves —
and both values are proved to resolve to **byte-identical roadnets** before that exemption is
allowed. Vehicle counts 1821/1828/1813/1827/1822 match the survivors' provenance.

⚠️ **My first attempt regenerated into `/tmp` and the gate REFUSED it, correctly**: `flowFile` is
stored **relative to the scenario dir**, so a draw tree is only byte-reproducible in its conventional
position. The plan's "generate to scratch, then compare" procedure was wrong and is corrected in the
commit message and here. **The recipe was never in doubt; my procedure was.**

### 3.4 Gate G — this session's instrument reproduces the re-used column

```
checkpoint identity verified: 20 checkpoints
GATE G PASS: 100 cells reproduce exactly, 20 checkpoints identical
```

Run **in this worktree, on freshly regenerated demand**. A gate passed in a retired worktree is not
this session's instrument.

### 3.5 Three further independent reproductions of P4.6, none of them asked for

- The P4.7 **declaration** reproduces P4.6's `mappo1000` and `random` entries **exactly** — scalars,
  the full 200-stream selections and the top deciles, leaf for leaf. So the one-per-draw subsample
  RNG is stable here, which is what makes the mixtures' random component the same data P4.6 measured.
- The P4.7 **diagnostics** reproduce P4.6's committed `mappo1000` and `random` entries on **every
  leaf** (`json.dumps(sort_keys=True)` equality).
- The **18 phase-1 cells** verify against `output/SHA256SUMS_p4_6.txt` (19/19 OK, run from `output/`
  itself), and the copies re-verify against the same digests.

### 3.6 The constructed behaviour reference, and the variance the ruling protects

```
behaviour@mix33 CONSTRUCTED: att_horizon 322.2417 +/- 13.3492  n=500  33 expert draws per seed
behaviour@mix50 CONSTRUCTED: att_horizon 267.2651 +/- 14.1931  n=500  50 expert draws per seed
behaviour@mix67 CONSTRUCTED: att_horizon 212.2675 +/- 13.3562  n=500  67 expert draws per seed
  bracket on all three: mappo1000 105.5820 < constructed < random 428.8839
```

**Why the realisation and not the expectation, quantified:**

| tier | realisation | expectation `f·A+(1−f)·B` | mean difference | CI95 realisation | CI95 expectation | ratio |
|---|---|---|---|---|---|---|
| `mix33` | 322.2417 | 322.1943 | +0.0474 | 13.3492 | 0.5009 | **26.65×** |
| `mix50` | 267.2651 | 267.2330 | +0.0321 | 14.1931 | 0.4093 | **34.68×** |
| `mix67` | 212.2675 | 212.2717 | −0.0042 | 13.3562 | 0.3293 | **40.56×** |

The forbidden construction is **indistinguishable by its mean** and reports paired CIs roughly
**thirty times too narrow** — which would turn "does not resolve" into "resolves" across the mixture
rows. That is the overstated precision `BRIEF_19` §3 constraint 2 exists to prevent, measured.

Each component is read from the committed grid **and** from its primary artifact
(`p4_heldout_thresholds.json`, `output/p4_6/eval_random_behaviour.json`) with every value required to
agree exactly — which also proves the secured raw evidence is faithful to the merged artifact.

### 3.7 🚨 Q1 HELD — and it could not have failed

```
tier     kept expert   of  fraction  floor  expected  hypergeom p
mix33             20   20     1.000   0.90      6.60   2.5199e-11
mix50             20   20     1.000   0.90     10.00   3.3217e-07
mix67             20   20     1.000   0.90     13.40   1.9959e-04
```

**And the disclosure registered in §4.1 before the number existed says this is hollow.** The two
components' return distributions **do not overlap at all**: common-language effect size **1.0000** on
all three tiers, **zero** random streams above the expert minimum, expert returns in
**[−8789, −5994]** against random in **[−40223, −38369]**. The worst expert stream beats the best
random stream by ≈**30,000 return units**. With no overlap and ≥20 expert streams in every mixture,
**the top decile by return is all-expert with probability 1**, so Q1's floor of 18/20 could not have
been missed. **The prediction was not falsifiable on this corpus.**

> **This bears directly on §1b's R2.** The mixture was billed as *"the one corpus where what the
> filter should select is known by construction"*. It is known **so completely that the measurement
> is a tautology**, so **R2's open question is not answered by Q1**. What is informative is A5's
> second axis (§3.8).

### 3.8 Q3 HELD — one half circular, one half a real measurement

| tier | composition p | null applicable? | volume CI95 | difficulty p | ρ(return, difficulty) | demand signature |
|---|---|---|---|---|---|---|
| `mix33` | 2.52e−11 | **no** | [−9.039, +2.973] | 0.6218 | −0.0455 | **absent** |
| `mix50` | 3.32e−07 | **no** | [−11.811, +1.038] | 0.6218 | −0.0203 | **absent** |
| `mix67` | 2.00e−04 | **no** | [−9.767, +1.873] | 0.3213 | −0.0760 | **absent** |

The **composition half inherits §3.7's circularity** — it is P4.6's `maxpressure` problem in a new
place. The **demand half does not**: ρ between stream return and own-tier difficulty is −0.02 to
−0.08, against `maxpressure`'s **−0.9967**, so those checks are genuinely null rather than
tautologically so. **That half is a real measurement and it supports "the filter does not select
difficulty".**

**⭐ What actually carries information here is `BRIEF_17` §11 A5's second axis**, and it is not a
tautology — the kept top decile concentrates on **3–4 of the 5 expert seeds**:

```
mix33 {101:8, 202:8, 505:4}      mix50 {101:7, 202:6, 404:3, 505:4}      mix67 {101:9, 202:10, 505:1}
```

Within the expert component the filter is selecting **checkpoints** — P4.5's measured effect — not a
composition. A5 exists precisely so this could not be confounded with "the filter selects the expert
fraction", and it is carrying the result the composition axis cannot.

## 4. Tests — real output

```
$ OMP_NUM_THREADS=1 MKL_NUM_THREADS=1 RLTRAFFIC_CORPUS_V11=/home/filip/rltraffic/datasets_v11 \
  RLTRAFFIC_CORPUS=/home/filip/rltraffic/datasets .venv/bin/python -m pytest -q
844 passed, 3 skipped, 12 warnings in 91.99s (0:01:31)
```

**Pinned, with both corpus variables set.** Baseline at the branch point was **769 passed, 3 skipped**;
the delta is exactly this task's 75 new tests. ⚠️ **Without the two corpus variables the same suite
reports `755 passed, 17 skipped`** — 14 corpus-backed tests skip silently, so an unset variable makes
the suite *quieter*, not red.

**Red-first evidence** (each new module was reduced to signature-only skeletons and the tests run):

```
$ pytest tests/test_p4_7_gate_d.py -q --tb=line          # against the skeletons
10 failed in 0.09s      (every failure NotImplementedError at the API surface)

$ pytest tests/test_p4_7_constructed_reference.py -q      # against the skeletons
13 failed, 1 passed in 0.91s

$ pytest tests/test_mixture_tiers.py -q                   # against main's declaration
E           assert -5762.0 == -5994.0
2 failed, 10 passed in 4.93s
```

⚠️ **Disclosed honestly:** for `offline/mixture_tiers.py` I drafted the Gate D implementation
**before** writing its tests, then reverted the module to skeletons to produce the red evidence
above. That is the real order of events; the red output is genuine but it was produced by reverting,
not by writing tests first. Every later block of that module (constructed reference, scorers,
Gate P1) was written tests-first without reverting.

## 5. Mutations — 29 executed, 29 killed, three survivors found and closed

| # | mutation | killed by |
|---|---|---|
| M1 | swap `mixture_training_streams`' component arguments | **SURVIVED first pass** → fixture rebuilt through the production entry point |
| M2 | draw with replacement | `..._holds_exactly_two_hundred_distinct_streams` |
| M3 | invert the expert count | `..._expert_count_is_the_declared_rounded_fraction` |
| M4 | shift `MIXTURE_RNG_BASE` | `..._drawn_by_the_declared_rng...` |
| M5 | drop the component sort | `..._decided_by_the_sort_key_not_by_load_order` |
| M6 | flip `--artifact-prefix`'s default | `..._reproduces_every_pre_p4_7_filename` |
| D1–D5 | Gate D: tolerate a demand difference · stop comparing config keys · drop the empty refusal · skip a missing draw · stop checking the roadnet | 5/5 killed |
| Q1a–c | relax the floor · lower it to 85 % · count experts by the composed fraction | 3/3 killed |
| Q2a–c | accept a tie as decreasing · flip the sign · drop the reversed-pair refusal | **Q2a SURVIVED** → `ordering_holds` now asserted on the tie path |
| Q3a–c | hold on either half · let a withdrawn check count · always claim the null applies | **Q3b SURVIVED** → withdrawn-check test added |
| P1a–c | loosen Gate P1 to `isclose` · stop comparing behaviour comparisons · skip a lost cell | 3/3 killed |
| 44-F3a/b, 44-F4a–d | the four `DEFERRED` 44 sites | 6/6 killed |
| 43-A/B/C | raise a different message the OLD token still matched | 3/3 killed |

**The three survivors were the useful ones.** M1 — swapping the two components inside
`training_streams`, which turns `mix33` into `mix67`'s composition **under `mix33`'s name** — passed
all 14 tests because my fixture called `mixture_training_streams` directly and bypassed the dispatch.
Q2a survived because the tie branch is evaluated first, so the *outcome* was protected while the
reported `ordering_holds` field was not — P4.6 finding M4's class exactly. Q3b survived because no
fixture set `withdrawn: True`.

## 6. `DEFERRED` 44 — closed

Six mutations on P4.3's four sites, **including the exact ×4.0 that survived P4.3's own 48-test
suite**:

```
44-F3a  ci95_half_width x 4.0 ......... FAILED the independent-route test
44-F3b  every adjacent step resolves .. FAILED the interval-consistency test
44-F4a  drop Gate A's exactly-one ..... FAILED the two-gates/no-gate test
44-F4b  drop Gate A's status refusal .. FAILED the failed-gate test
44-F4c  disable probe disjointness .... FAILED both leak tests
44-F4d  break the in-support partition. FAILED the partition test
```

`offline/rtg_calibration.py` is **unmodified** (`git diff --name-only offline/rtg_calibration.py` →
empty). The cover lives in a new file so a reader can see which assertions were added afterwards.

## 7. `DEFERRED` 43 — measured against the current tree, and the row is partly stale

⚠️ **The row's enumeration is out of date.** It lists 73 sites by (file, line, token); several of
those tokens no longer exist — `test_offline_selection.py:323 'behaviour seeds'` now reads
*"the declared behaviour seeds are"*, `:327 'count'` now reads *"a matched-size design is the whole
experiment"*, `test_offline_baseline_agents.py:545 'statistics'` now reads *"carries no statistics"*.
Following the row literally would have "fixed" already-fixed sites and missed sites it never listed.

**Measured on the current tree** (excluding the two files already discharged): **77 fragile-shaped
sites, 28 ambiguous, 49 unique** → after the change, **71 / 11 / 60**.

Ground truth rather than guesswork: the whole suite was run under a plugin recording the **actual**
message every `match=` matched (**267 captured**), then each ambiguous token was extended —
word-aligned, regex-safe, shortest first — until it appeared in exactly one raise in that test's own
search space.

| site | old token | hits | new token | hits |
|---|---|---|---|---|
| `test_dt_agent.py:473` | `reward` | 2 | `'reward'` | 1 |
| `test_dt_agent.py:487` | `n_actions` | 5 | `but n_actions` | 1 |
| `test_dt_agent.py:640` | `n_actions` | 5 | `checkpoint n_actions` | 1 |
| `test_dt_gate.py:274` | `window` | 6 | `windows of` | 1 |
| `test_dt_gate.py:285` | `three` | 2 | `least three` | 1 |
| `test_dt_gate.py:352` | `groups` | 3 | `pass group=` | 1 |
| `test_dt_gate.py:429` | `gradient steps` | 2 | `gradient steps but` | 1 |
| `test_dt_gate.py:480` | `declared budget` | 2 | `a declared budget` | 1 |
| `test_dt_gate.py:552` | `local_reward_fn` | 2 | `with local_reward_fn` | 1 |
| `test_flow_randomizer.py:865` | `cityflow` | 3 | `'cityflow'` | 1 |
| `test_method_tier_grid.py:529` | `env settings` | 2 | `of env settings` | 1 |
| `test_method_tier_grid.py:1002` | `not requested` | 2 | `not requested for` | 1 |
| `test_method_tier_grid.py:1029` | `different designs` | 2 | `describe different designs` | 1 |
| `test_offline_dataset.py:685` | `split` | 8 | `unknown split` | 1 |
| `test_offline_dataset.py:738` | `flow_draw` | 2 | `manifest flow_draw` | 1 |
| `test_offline_dataset.py:745` | `NaN` | 2 | `has NaN` | 1 |
| `test_p4_7_constructed_reference.py:320` | `no episodes` | 5 | `artifact carries no episodes` | 1 |

**11 left unchanged, each with a reason.** The sharpest: `test_method_tier_grid.py:710 'one arm'`
**cannot be disambiguated at all**, because `dt_gate.py` and `method_tier_grid.py` contain the
**identical** raise `"a cell must describe one arm, got {arms}"` — the two-identical-messages limit
P4.6 already recorded. The others are messages beginning with a tmp path, or whose distinguishing
part is a runtime-formatted value no source token can reach.

**One token was chosen by hand, disclosed:** the mechanical chooser picked `" groups"` (a leading
space) — unique but unreadable, which is the same defect in a new form. Replaced with `pass group=`,
also unique in the same space.

**Proved the fix bites** — three mutations changing the module to raise a **different** message the
**old** token still matched: all three now FAIL, and under the old tokens all three would have passed.

## 8. FINDING 1's declaration audit — run, result recorded in the plan §8

**40 module-level declarations** across the two modules: every one has a consumer executed by a test
or a real run. Three were flagged by my first pass and **all three are false positives of my own
script** (module-scope consumers inside the `TIERS` dict), verified individually against the
executed-line set. **21 constants the plan declares** checked against the code that consumes them:
**0 mismatched**.

**Two stale statements found in `docs/plans/p4.6.md` §8**, both about phase 2: the target/scale
declaration (falsified, §3.1) and *"phase 2 adds 12 cells and 3 tiers to the same artifact"*
(superseded by RULING 3). A third claim in the same paragraph — the per-tier `phase` field — checked
and true of the declaration.

**What the audit does not establish:** it checks declarations against consumers and values, **not**
that every prose sentence is backed by an execution. The mixture-target failure was caught because a
**guard** existed to refuse it.

## 9. Guards — each reading names its corpus

| guard | invocation | exit | findings |
|---|---|---|---|
| `claude_guard.sh --frozen-only` | no arguments | **0** | 0 frozen paths |
| `check_test_hygiene.sh` | **no arguments — all of `tests/**.py`** | **1** | **16**, `grep -c TH006` on the full output: `test_cityflow_env_phase_constraints` 8 · `test_sumo_env_phase_constraints` 5 · `test_phase_control` 2 · `test_base_traffic_env` 1. **All pre-existing, `DEFERRED` 45, outside this fence.** |
| `check_test_hygiene.sh` | this task's 10 changed/added test files | **0** | 0 |
| `check_english.sh` | `offline tests docs` | **1** | **1**: `docs/patches/claude_guard_hygiene.patch:4`, pre-existing |
| `check_english.sh` | this task's changed files | **0** | 0 |

⚠️ **Correction of my own earlier report, and of the mechanism the coordinator inferred.** I first
reported **2** hygiene findings. The guard ran **with no arguments and emitted all 16**; `grep -c
TH006` on the very output file I wrote at the time returns **16**. **The truncation was `tail -12` in
my reporting step** — a full run reported from its last twelve lines, i.e. a partial **view**, not a
partial corpus. Naming the invocation form alone would not have caught it; taking the count from the
full output does.

## 10. Self-review checklist

- Contracts in `docs/CONTRACTS.md` honored, including the non-standard env API? **Y** — no env call
  is made by new code; evaluation goes through the frozen `evaluate_arm`.
- All tests actually executed and green? **Y** — 844 passed, 3 skipped, pasted above.
- Zero modifications to frozen files? **Y** — guard 0, and the diff touches no frozen prefix.
- Zero new dependencies? **Y** — `coverage` was used as an already-installed audit tool and is
  imported by no delivered code.
- Every number in this packet produced by a command run in this session? **Y.**
- Tests written before implementation? **PARTIAL — N for Gate D**, disclosed in §4.
- Every mutation executed and its failure pasted? **Y** — 29 executed.
- §6's checkbox in `PROJECT_PLAN` left unticked? **Y.**
- Campaign launched by the user in `tmux`, never by me? **Y — and it has not been launched yet.**

## 11. Tests I changed, in full

1. **`tests/test_p4_7_gate_d.py::test_a_missing_regenerated_draw_is_refused`** — my own new test.
   It asked for a draw absent from **both** trees, so it exercised the missing-**survivor** branch
   while asserting the missing-**regenerated** message. **The assertion was right and the fixture was
   wrong**; the fixture now creates the case it names and the other branch got its own test. No
   assertion weakened.
2. **`tests/test_p4_7_predictions.py::test_q2_carries_the_paired_interval_for_each_mixture`** — my
   own new test. I had written `assert "verdict" not in str(result).lower()`, which fails on the
   companion note saying *"no equivalence verdict anywhere"* — it forbade the word inside the
   sentence forbidding the thing. Replaced with the repo's canonical `assert_no_verdicts`, which is
   **strictly stronger** and is the mechanism `BRIEF_17` §4 is actually enforced by.
3. **17 `match=` tokens lengthened** across 6 pre-existing test files (§7). **No assertion was
   weakened or removed**; each token became strictly more specific, and three mutations prove the
   new tokens catch what the old ones missed.
4. **`tests/test_rtg_calibration_guards.py`** — three fixture shapes corrected against the real API
   after I had inferred them (`adjacent_comparisons` not `adjacent_steps`; `SupportCounts` built via
   `in_support_counts`; `training_rtg_range` reads `stats["rtg"][scenario][ix_id]`). CLAUDE.md rule 6
   — read before writing — and the suite caught all three.

**No pre-existing assertion was changed to make anything pass.**

## 12. Deviations from the brief

1. **Plan committed before the §2 verification**, where §7's DoD lists the verification first. Both
   of the brief's binding constraints are satisfied (*"before any training"*, *"before the first
   gradient step"*), and this order is **strictly stronger for A8(a)**: Q1–Q3 were registered before
   any measurement whatsoever existed.
2. **Gate D regenerates in place, not into a scratch root** — the plan's procedure was wrong (§3.3).
3. **`docs/data/p4_7_training.json`** rather than an isolated directory, per RULING 3.

## 13. Conflicts found between brief and repo (repo wins)

1. **`rtg_scale = max − min`** (RULING 1, as first worded) against the repo's `max(|min|, |max|)`.
   Implemented the repo's; the coordinator verified 5/5 phase-1 tiers and amended the ruling. **This
   was material, not editorial** (§3.2).
2. **`BRIEF_19` §2 asserts the diagnostic "already splits by dataset directory and by behaviour
   seed, which is §11.A5's both-axes requirement already implemented."** True of
   `kept_composition`, and it is what produced §3.8's seed histograms.

## 14. Open questions for the coordinator

1. **Q1 is a tautology on this corpus and the packet says so.** Does R2's open question now need a
   different instrument — e.g. a mixture whose components' return distributions **overlap**, which
   would make the filter's selection informative rather than forced? That is a design question, not
   a fix, and it is outside this brief's fence.
2. **`DEFERRED` 43's enumeration is stale** (§7) and `DEFERRED` 45's count is confirmed at 16. Both
   are the coordinator's files; I have edited neither.
3. **`output/p4_7/`** must be secured into the main tree with a `SHA256SUMS_p4_7.txt` before this
   worktree is retired — it will hold 60 checkpoints plus the gate and cell artifacts.
4. **The identical raise message** in `dt_gate.py` and `method_tier_grid.py` (*"a cell must describe
   one arm"*) makes one test unfixable by token and means that test would pass against the wrong
   module's function. Worth a rename in whichever task next opens either file.

## 15. What the next step assumes about this one

The campaign assumes: Gate G and Gate D PASS on disk, `docs/data/p4_7_declaration.json` exists, 100
materialised draws exist, and `--artifact-prefix p4_7` keeps every write out of P4.6's records. It
asserts all four before training and exits non-zero otherwise. After it completes, `report` needs
only the 12 `eval_mix*_<method>.json` files; everything else it consumes is already committed.

## 16. AI-assistance record (CLAUDE.md §8)

1. **Tool and version** — Claude Code, Opus 4.6, 2026-08-14 → 2026-08-15.
2. **Authorship per file** — AI-authored: `offline/mixture_tiers.py`,
   `offline/campaigns/p4_7_phase2.sh`, `tests/test_mixture_tiers.py`, `tests/test_p4_7_gate_d.py`,
   `tests/test_p4_7_constructed_reference.py`, `tests/test_p4_7_predictions.py`,
   `tests/test_rtg_calibration_guards.py`, `docs/plans/p4.7.md`, this packet, and the two
   `docs/data/p4_7_*.json` artifacts (generated by the CLI). AI-edited from a human-authored
   original: `offline/method_tier_grid.py` (3 declaration lines + `--artifact-prefix`), and 17
   `match=` tokens across 6 existing test files. Human-authored: nothing in this diff.
3. **Human verification performed** — the coordinator independently verified, from the corpus and
   from `main`, that all three mixture specs carried `−5762.0 / 40294.0` (FINDING 1); that all five
   phase-1 tiers' `rtg_scale` equals `max(|min|, |max|)` and none equals `max − min` (FINDING 2's
   amendment); that the hygiene guard reports 16 in both trees (FINDING 3); and that 11 draw
   directories survive with held-out 1000–1004 only. **The implementer ran** every test, every one of
   the 29 mutations, both gates and all three guards.
4. **Who made the research decisions** — **coordinator rulings**, all dated 2026-08-15 and quoted in
   §12 of the plan: RULING 1 (fix the specs, declare the rule, record the amendment), RULING 2
   (report both scorings, no rescue), RULING 3 (`--artifact-prefix`). **Pre-registration**: Q1–Q3 and
   the constructed-reference RNG were fixed in `docs/plans/p4.7.md` at commit `7df6d97`, **before any
   P4.7 number existed**; the brief `BRIEF_19` was committed on `main` at `109f9bd` before that. The
   implementer chose implementation and test design only; no scoring rule was set or altered after a
   number was seen, and the one field added after seeing a number (`composition_null_applicable`)
   changes no rule and reports a disclosure that was registered in advance.
