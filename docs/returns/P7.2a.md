# RETURN PACKET — P7.2a (SUMO draws under the parity contract)
**Date:** 2026-09-13 · **Mode:** Claude Code · **Contracts:** v1.1
**Written against `BRIEF_35` + Amendment A** (plan approved at `be937be`; A1–A7 and the A6 reorder
are all implemented and each has a test).

**Status: PARTIAL — gate 2 is complete and green; gates 3–6 are NOT RUN by design.**
Amendment A5 makes the pre-flight (gate 3) block the campaign (gate 4), and the coordinator spawns
it on the commit where the tests are green — which is `b43ac39`, this packet's commit. **No draw has
been materialised outside `tmp_path`: the main tree's `scenarios/draws/` is untouched and still holds
its 106 directories.** The P4.3 band gate has been run on **one** draw (201) inside `tmp_path`, not on
the band.

---

## 1. Branch + diff stat

```
task/p7.2a-sumo-draws
$ git diff --stat main...HEAD
 docs/plans/p7.2a.md              | 380 +++++++++++++++++
 offline/materialise_draws.py     | 883 ++++++++++++++++++++++++++++++++++++++-
 offline/parity.py                |  58 ++-
 tests/test_materialise_parity.py | 797 +++++++++++++++++++++++++++++++++++
 tests/test_parity_vtype.py       |  73 ++++
 5 files changed, 2181 insertions(+), 10 deletions(-)
```

**Zero frozen files.** `git diff --name-only main...HEAD` filtered against the frozen set
(`envs/ agent/base agent/utils agent/MAPPOAgent algorithms/ rewards.py states/ metrics/ utils/
CityFlow/ experiments/**.py scripts/ .claude/ .github/`) returns **NONE**. The only deletions in the
stat are 10 lines: 5 of an outdated module-docstring paragraph (§3 below) and 5 rewritten lines of
`render_parity_sumocfg_text`'s return statement.

## 2. Files produced

| path | what |
|---|---|
| `offline/materialise_draws.py` | the parity phase: `materialise_parity`, `verify_p4_3_probe`, `parity_dir` / `parity_sumocfg_path` / `load_parity_provenance`, the `--parity` / `--verify-p4-3-probe` / `--allow-worktree` / `--report` CLI, and the guards |
| `offline/parity.py` | `render_parity_sumocfg_text(..., *, time_to_teleport=None)` and `NOTELEPORT_SUMOCFG` |
| `tests/test_materialise_parity.py` | NEW, **24 tests** |
| `tests/test_parity_vtype.py` | **+4 tests** (22 → 26) |
| `docs/plans/p7.2a.md` | the plan (committed at the gate, `be937be`) |

**One docstring paragraph was deleted, not edited**: `materialise_draws.py`'s SUMO section said
"**No `.sumocfg` is generated** -- that is P7.3's". That is now false — this task generates one per
draw — so it was replaced by the new `THE PARITY SUBDIRECTORY` section, which declares the format
version `materialised-draw-parity/1.0` beside `materialised-draw/1.0` and restates the alignment
convention (§3.1(i)).

## 3. Tests — real output

### 3.1 Red first, and every test reached the real API surface

```
$ pytest tests/test_materialise_parity.py tests/test_parity_vtype.py -q -p no:randomly --tb=line
26 failed, 24 passed in 4.22s
```
Failure reasons, tallied from that run — **not one shared import error**:

| n | reason |
|---|---|
| 20 | `offline/materialise_draws.py:490: NotImplementedError` (`materialise_parity`) |
| 2 | `offline/parity.py:360: NotImplementedError` (the new `time_to_teleport` parameter) |
| 1 | `materialise_draws.py:458` (`parity_dir`) · 1 `:1016` (the CLI path) |
| 2 | `AttributeError: no attribute '_checked_parity_target'` / `'_render_bound_routes'` (the two internal seams) |

The 24 that passed red are the pre-existing `test_parity_vtype.py` tests plus the two new ones that
are **controls on committed artifacts** (the byte-identical default, and `NOTELEPORT_SUMOCFG` naming
the file A15(c) registered) — they must pass before and after, and they do.

### 3.2 Green

```
$ pytest tests/test_materialise_parity.py tests/test_parity_vtype.py -q -p no:randomly
50 passed, 1 warning in 11.41s

$ pytest tests/test_materialise_parity.py tests/test_parity_vtype.py tests/test_materialise_draws.py -q -p no:randomly
81 passed, 1 warning in 17.75s

$ pytest -q -p no:randomly                      # the whole suite, in the worktree
1891 passed, 93 skipped, 20 warnings in 144.91s (0:02:24)
```

**Test totals:** `test_materialise_parity.py` **24** (new) · `test_parity_vtype.py` 22 → **26** ·
`test_materialise_draws.py` **21 `def test_` / 31 collected**, unchanged (not edited). All four
counts were measured in this session by `--collect-only`. 24 + 4 = **28 added**, and `1891 − 28 =
1863`, which is what `docs/returns/P7.1.md` recorded for this worktree before the task — that prior
figure is quoted from the packet, **not** re-measured here. The single warning is SUMO's own
`getAllProgramLogics` deprecation, raised from frozen `utils/sumo_utils.py`.

⚠️ **93 skipped is the WORKTREE's number, not a runner's and not the main tree's.** The same suite in
the main tree earlier today reported 1949 passed / **7** skipped, because `output/` and
`datasets_v11/` exist there. Nothing was inferred from the skip count.

### 3.3 Guards

```
$ bash scripts/check_test_hygiene.sh tests/test_materialise_parity.py   -> exit 0
$ bash scripts/check_english.sh <the 5 changed/new files>               -> exit 0
$ bash scripts/claude_guard.sh --frozen-only                            -> exit 0
```
The hygiene guard **caught a real weakness in my own test** on its first run: `pytest.raises(ValueError)`
with no `match=` in the commit-guard loop (TH006). It was **strengthened, not waived** — each of the
five bad target shapes now asserts its own message, so the guard can no longer be satisfied by a
`ValueError` raised for an unrelated reason.

**Tree-wide hygiene, now measured** (it had not finished when this packet was first written, where it
was recorded as `NOT RUN`):

```
$ bash scripts/check_test_hygiene.sh        -> exit 1
findings: 16      tests/test_cityflow_env_phase_constraints.py 8
                  tests/test_sumo_env_phase_constraints.py     5
                  tests/test_phase_control.py                  2
                  tests/test_base_traffic_env.py               1
tests/test_materialise_parity.py: 0 occurrences in the whole report
```

**16 and exit 1 are exactly what `.github/ci/ci_baseline.json` records** (`guards.hygiene.total` 16,
`expected_exit_code` 1, and the same four-file split) — `DEFERRED` 45's pre-existing findings,
unchanged by this task.

⚠️ **A counting trap, recorded so the next person does not report 17.** `grep -c 'TH00'` over the
guard's output returns **17**, because the script's own trailer prints the waiver instruction
`# hygiene: allow TH00N - <reason, 10+ chars>`, which contains the string. The findings are the lines
that start with a test path: `grep -c '^tests/.*TH00'` → **16**. I hit this myself and checked the
17th line rather than reporting the larger number.

## 4. Mutations — 9 executed, 9 killed

Seven code mutations were applied one at a time to an **isolated sandbox copy** (`/tmp/p72a_mut`,
real `offline/` and `tests/`, the read-only trees symlinked), run, and reverted; the sandbox was
verified green (50 passed) before mutating and the file was restored afterwards (asserted). The repo
tree was never left mutated.

| # | mutation | verdict | killed by |
|---|---|---|---|
| M1 | the `os.replace` target guard returns unconditionally | **KILLED** (2) | the planted-wrong-path test and the direct guard test |
| M2 | `parity_dir` returns the **draw** directory itself | **KILLED** (21) | nearly the whole file, starting with the parent-untouched snapshot |
| M3 | the phase validates the **bound** text and writes the **unbound** one | **KILLED** (3) | ⭐ the **runtime SUMO test**, plus the binding and equivalence tests |
| M4 | the CAP(E) multiset refusal is dropped | **KILLED** (1) | the lost-vehicle barrier test |
| M5 | `--force` is forwarded to the parent `materialise()` | **KILLED** (1) | the A4 spy on the kwarg |
| M6 | A6's reorder undone (existing parents not validated first) | **KILLED** (3) | including the A6 test: draw 201 got materialised |
| M7 | the config is written with `time_to_teleport=None` | **KILLED** (3) | ⭐ the **runtime** teleport read-back, and two file-level tests |
| M8 | **`base_seed` 1001 instead of 1000** (the brief's T4 mutation) | **KILLED** | the band gate |
| M-T1b | a file written **beside** the parent's four | **KILLED** | `materialise()`'s own no-op check |

Two of these are worth reading rather than counting:

**M3 and M7 are killed by the running engine, which is the point of T2.** A route file can name
`cf_parity` on every vehicle and still be run by an engine that resolved something else. M3 writes a
file whose vehicles are unbound while every in-memory check passed on the bound text — a file-level
suite would go green. `getTypeID` does not.

**M8's number is itself evidence.** Under `base_seed 1001`, draw 201 reproduces
`local_return −17409.0`, `att_horizon 170.42777777777778`, `horizon_vehicle_count 95.0` — which is
**P4.3's recorded row for draw 202**, exactly. Seeds are `base_seed + draw_id`, so a base-seed slip
of one is a draw slip of one, and the gate lands on the neighbouring row rather than on noise. That
is `DEFERRED` 55's "materialisation is a proven pure function of `(source, base_seed, draw_id)`"
observed from the other side.

```
$ python /tmp/p72a_m8.py
recorded (p4_3_probe.json episodes[0]):
    {'att_horizon': 191.80088251516824, 'decisions': 360, 'horizon_vehicle_count': 116.0, 'local_return': -20966.0, 'local_return_from_lanes': -20966.0}
observed under base_seed 1001:
    {'att_horizon': 170.42777777777778, 'decisions': 360, 'horizon_vehicle_count': 95.0, 'local_return': -17409.0, 'local_return_from_lanes': -17409.0}
matches: False | differing fields: ('local_return', 'local_return_from_lanes', 'att_horizon', 'horizon_vehicle_count')
```

## 5. 🔒 A test of mine was wrong. Here it is in full, with the diagnosis

**T5 failed on its first green run.** I did not adjust it until I had diagnosed why, and the
diagnosis is that **my test was wrong, not the code**.

*What it did:* compared the `(depart, route)` multisets of draw 0's generated parity routes and the
committed parity route file by reading `vehicle.get("depart")` as a **string**.

*Why that is wrong:* the depart is a time. The committed parity file inherits the shipped
`.rou.xml`'s integer strings (`"0"`, `"3"`, `"8"`); a drawn rendering carries
`flow_randomizer.render_sumo`'s two-decimal form (`"0.00"`, `"3.00"`). Measured:

```
committed parity           n= 2021 first departs (raw strings)=['0', '3', '8', '11']
draw 0 parent rendering    n= 2021 first departs (raw strings)=['0.00', '3.00', '8.00', '11.00']
float multiset equal: True | index-aligned: 2021 / 2021
```

So the demand is identical and my assertion was comparing **file formatting**.

*The change:* the comparison now goes through `conversion_audit.demand_from_route_file` — the same
extractor CAP(E) uses and the one A15(g)'s `2021/2021` verdict was made with — and it additionally
asserts, positively, that the two raw string forms **do** differ. The test is strictly stronger than
before: it now checks demand equality, order equality, the count, and the formatting difference that
motivates comparing semantically. **No assertion was removed.**

*Scope of the error:* the test file only. No shipped code changed because of it, and the artifact was
correct throughout — T3's CAP(E) check already compared floats.

## 6. Self-review checklist

| question | answer |
|---|---|
| Contracts honoured, including the non-standard env API (`reset` → info only, reward first)? | **Y** — the SUMO test uses `info = env.reset(seed=1000)` and `_reward, terminated, truncated, info = env.step(...)` |
| All tests actually executed and green? | **Y** — output pasted in §3, not summarised |
| Zero modifications to frozen files? | **Y** — proved by the filtered diff in §1 |
| Zero new dependencies? | **Y** — stdlib + the repo's own modules only |
| Every number in this packet produced by a command run in this session? | **Y** |
| Tests written before the implementation and red for their own reasons? | **Y** — §3.1, with the per-reason tally |
| Every load-bearing claim mutation-tested? | **Y** — §4, 9/9 killed |
| Did you change a test? | **Y — one, mine, disclosed in full in §5** |
| Anything written outside `tmp_path` and `/tmp`? | **N** — the main tree's `scenarios/draws/` is byte-untouched; verified it still holds 106 draw directories and no `parity/` |

## 7. Deviations from the brief

1. **T5 lives in `tests/test_materialise_parity.py`, not in `tests/test_parity_vtype.py`.** The brief
   groups it with the parity tests; it needs a materialised draw, so it sits with the phase that
   makes one. The four tests that genuinely belong to the generator (T6 and the reference pin) are in
   `test_parity_vtype.py`.
2. **`ParityResult` gained a `planned_action` field** beyond the brief's sketch. `action` is
   `"planned"` for every draw under `dry_run` — `MaterialisedDraw`'s existing convention — which would
   have made §3.1(vii)'s four-way dry-run vocabulary unreportable. `planned_action` carries the
   decision; `action` keeps the convention.
3. **`demand_audit` gained `depart_offset`** (flagged at the plan gate, §3.2 of the plan). The shift
   is the one step that could make a mismatch look like a match, and it is not re-derivable from the
   other fields. Measured **0.0** on this scenario.
4. **T3's mutation is shipped as a test, not only as an executed mutation.** "Drop a vehicle before
   binding" is a planted fault that proves the barrier; as a test it runs on every future change. The
   code mutation that removes the check it exercises (M4) is also executed.

## 8. Conflicts between brief and repo (repo wins)

Both were found at the plan gate and **confirmed by the coordinator's own commands** in Amendment A,
which records that the brief was wrong on both.

1. **`COLLECT_SETTINGS` cannot be handed to `make_env`** — it is a flat CLI argv tuple
   (`transfer_gate.py:1028`), while `make_env` needs a resolved settings dict. Implemented per A1
   through `collect_style_args(...)` → `_build_env_spec(...)` → `make_env(...)`, the path
   `tests/test_backend_alignment.py` already exercises on SUMO. **Verified in this session,
   independently of the amendment: 15 common keys with `docs/data/p4_3_probe.json:env_settings`,
   zero differing, `compare_with` the only extra.** The test asserts this equality.
2. **`reset(seed=1000)` does not seed SUMO with 1000.** `BaseTrafficEnv.reset` reseeds the env RNG
   and draws `_engine_seed` from it (`envs/base_traffic_env.py:629-631`); `envs/sumo_env.py:182-183`
   passes *that* as `--seed`, and the `__init__` boot passes no `--seed` at all. Per A7 the packet
   says what actually happened rather than repeating the brief's wording: **the env RNG was seeded
   with 1000 and the value it drew is what reached SUMO**; the test asserts `env._engine_seed` is an
   int rather than pretending to know it. T2's assertions do not depend on the engine seed.
3. **Minor, mine:** `env._sumo` is the `traci` **module**, not a connection (`envs/sumo_env.py:130`),
   so TraCI is process-global and T2 holds exactly one SUMO env at a time.

## 9. What has NOT been run, and what would have to be true before it is

| gate | status | note |
|---|---|---|
| 3 — pre-flight review | **NOT RUN** | the coordinator spawns it; A5 makes it block gate 4 only. The commit to review is `b43ac39` |
| 4 — the 206-draw campaign in the MAIN tree | **NOT RUN** | needs the CLEAR. The run is 3 invocations, cwd `/home/filip/rltraffic`, `PYTHONPATH` to this worktree, `-P` |
| 5 — the P4.3 band gate, 100/100 | **NOT RUN on the band** | run on **draw 201 only**, inside `tmp_path`, where it reproduced all four numbers exactly |
| 6 — the SUMO smoke on draw 1 in the main tree | **NOT RUN** | the equivalent assertions ran inside the test suite against a `tmp_path` draw |

⚠️ **A recommendation for gate 4, found while producing an example artifact:** the parity provenance
records `git_dirty`, and it was `true` in my scratch run because the worktree had uncommitted changes
at that moment. The campaign should be launched from a **committed** tree so that all 206 records
carry `git_dirty: false` and a single commit hash.

## 10. Open questions / risks for the coordinator

1. **`net.reference` is out_root-dependent and is an identity field.** Re-running the phase with a
   different `--out-root` produces a different relative path to the shipped network, so an existing
   `parity/` would be **refused** as differing — the same fail-closed behaviour the parent already has
   for `cityflow.json`'s absolute `dir` (module docstring, *WARNING*). Deliberate and documented; flagging
   it because it means the 206 artifacts are tied to the main tree's layout.
2. **The CI skip ceiling will move by the two environment-gated tests I added** (1 SUMO-gated, 1
   CityFlow-gated; counted, not predicted). I have **not** pre-bumped `156` — the registered protocol
   is classify `junit.xml` and commit the observed value, and it is the coordinator's at merge.
3. **`verify_p4_3_probe` rolls CityFlow through `run_probe`, which is single-intersection by
   construction** (`rtg_calibration.py:396-401`). That is fine for `cityflow1x1` and would refuse on a
   multi-intersection scenario — relevant when `DEFERRED` 75(c) is scheduled.
4. The sandbox used for mutations could not run the CityFlow test through symlinked scenario
   directories (`cannot open roadnet file`), so **M8 was run in the worktree instead**, against a
   `tmp_path` out-root. Recorded so the method is reproducible.

## 11. AI-assistance record

- **Tool and version:** Claude Code, Opus 4.5 (1M context), 2026-09-13, one session (explore → plan →
  gate → code → mutations → packet).
- **Authorship by path:** `offline/materialise_draws.py` and `offline/parity.py` are **AI-edited from
  a human original** — both are pre-existing modules; every change is additive except the one
  docstring paragraph named in §2. `tests/test_materialise_parity.py` and `docs/plans/p7.2a.md` are
  **AI-authored**. `tests/test_parity_vtype.py` is **AI-edited from a human original** (+4 tests, one
  import; no existing test touched). Three read-only survey subagents (same tool) gathered the test
  shapes, the registered rows and the API surfaces; **every fact they reported that entered this
  packet was re-verified by me by running a command** — including A1's 15-key settings equality,
  which I recomputed rather than taking from the amendment.
- **Human verification performed:** the **author** approved the plan at the gate and issued
  Amendment A; the **coordinator** independently confirmed both brief-vs-repo conflicts with its own
  commands before ruling. In this session I ran: the red suite, the green suites, the whole suite, the
  three guards, nine mutations in an isolated sandbox, and eleven fact-checks of the brief's §0 (§2 of
  the plan). No number here is reported from memory or from a subagent's summary.
- **Who made the research decisions:** the **coordinator**, by written ruling — A1 (the settings
  route), A2 (`--verify-p4-3-probe` in the tool, with its four conditions), A3 (foreign subdirectories
  refuse), A4 (the force spy), A5 (pre-flight blocks the campaign only), A6 (the phase reorder), A7
  (write `-1` only; the digest-twin rule; how to word the seed). **Pre-registered before any of it:**
  A14(E) (demand exact, parity vehicle parameters), A15(c) (the teleport-free regime), A15(g) (hz1x1
  bc-tyc ADMITTED). I decided only *how to implement and how to test*, and every such choice is in
  `docs/plans/p7.2a.md` or in §7 above.

## 12. What P7.2b will assume about this one

- **`parity_sumocfg_path(scenario_key, draw_id, out_root=...)` is a pure path function** — no I/O, no
  directory scan — so a probe can build its whole rotation before anything exists. Tested.
- **After gate 4 (not yet run), every draw in 0–5, 201–300 and 1000–1099 has
  `parity/noteleport.sumocfg`.** Today **none** does outside `tmp_path`. P7.2b must not assume the
  band exists until the campaign has run and its 100/100 gate is in a packet.
- **What that config guarantees when it does exist:** every vehicle bound to `cf_parity` with the
  parity attributes (asserted in the running engine, not only in the file), `<time-to-teleport
  value="-1"/>` (asserted from `simulation.getOption`), the **shipped** network by relative reference
  (never a copy), `begin 0` / `end 4000`, and a `provenance.json` carrying CAP(E) for that draw plus
  the parent's three digests.
- **The demand is the parent's demand**, element for element: ids `0..n-1` in `startTime` order,
  `depart = startTime + begin` at two decimals. A probe comparing SUMO departs with CityFlow
  `startTime` must parse them as floats — the two files agree as times, not as strings (§5).
- **The parent's `routes.rou.xml` is still unbound and must never feed a transfer measurement.** It is
  the input of the derivation and nothing else.
