# RETURN PACKET — P5.2 (the spatial DT across the ladder, and the head-count 2×2)
**Date:** 2026-08-24 (revised after `docs/reviews/P5.2.md`) · **Mode:** Claude Code · **Contracts:** v1.1

**Status: DONE, revised.** The campaign reached `CAMPAIGN_COMPLETE 2026-08-23 01:05:24`,
`assert-complete` reports **35 of 35** against the declaration, and **every registered prediction is
scored**.

🛑 **`docs/reviews/P5.2.md` returned FAIL — 2 blockers, 7 major, 10 minor — and this packet is the
revision.** §6a is new and is the one that matters: **a required fix I did not implement and did not
disclose, which destroyed six records in an un-backed-up tree.** §12a answers every finding, one row per item.
**No reported number changed.** The reviewer's verdict on the science stands: *"the science is sound
and reproduces exactly."*

⚠️ **The scoring in §1 is the coordinator's, from `output/p5_2/eval_*.json` directly (`119cc48`,
brief §GATE 2). This packet is written to those verdicts and does not re-score them.** What I
verified in this session is stated as such and separated from what I am carrying.

---

## 1. The registered predictions, as scored

| # | prediction | verdict |
|---|---|---|
| **Q0** | head-count 2×2, stop rule on `d4` | **did not fire** — the ladder was authorised to run |
| **Q1** | level, R′ within ±30 %, `k = ceil(9/13·N) = 14 of 19` | **HELD, 15 of 19**; Q1b instrument check **HELD** at 0.04 % / 0.09 % |
| **Q2a** | predicted first place | **FAILED 3/3** — predicted `bc_top10_perix`/`dt_nomix`/`dt_nomix`, observed `bc`/`iql`/`iql` |
| **Q2b** | ≥ 12 of 15 concordant pairs per tier | **FAILED 3/3** — `11 · 8 · 8` |
| **Q3** | `dt_nomix` leads the field out of sample | **FAILED 3/3** — ranks `2/6 · 3/6 · 3/6` |
| **Q4** | the spatial harm grows as data degrades | **INVERTED** — it vanishes, and reverses on `random` |
| **Q5** | the load sorter replicates; the per-node filter repairs it | **CONFIRMED twice**, with a predicted null on `fixedtime` |
| **Q6** | collapse criterion | no DT arm collapsed on any tier |

**Q1's four misses are all one shape: R′ predicted degradation that did not arrive** —
`bc_top10@fixedtime` 1042.75 predicted against **207.47**, `iql@fixedtime` 360.85 against **203.95**,
`bc_top10@random` 1242.69 against **350.34**, `iql@random` 414.99 against **190.96**. **`iql` did not
merely beat its prediction, it won those tiers.**

### 1.1 Q4 is the consequential one, and it supersedes P5.1's headline

| tier | `d = dt_spatial − dt_nomix` | CI95 | per-seed |
|---|---|---|---|
| `mappo1000` | **+39.5649** | [+36.0510, +43.0787] | 5/5 |
| `maxpressure` | **+0.7267** | [+0.6206, +0.8328] | 5/5 |
| `fixedtime` | **0.0000** | exact | n/a |
| `random` | **−0.7063** | [−1.3172, −0.0953] | 4/5 |

Registered: `+39.56 → +41.74 → +51.37 → +62.71`, **growing**. Measured: it vanishes and reverses.
**Reported by endpoints and never as a trend** (§1b R3's standing instruction): **39.56 at the best
tier against −0.71 at the worst.**

> **P5.1's headline — spatial mixing is decisively harmful — is now known to be a `mappo1000`
> phenomenon, and P5.1 could not have known it because it ran one tier.** The reading the
> coordinator scored is that **mixing hurts only where there is something to lose**.

### 1.2 `fixedtime`'s exact zeros are a RESULT, not missing data

`fixedtime`'s `behaviour`, `dt_nomix` and `dt_spatial` agree to four decimals with identical vehicle
counts — the self-comparison signature. **It was checked at episode level and cleared: 0 of 500
episodes differ between all three, `bc_top10_perix` is likewise identical to `bc`, but `bc` differs
from `behaviour` on 500 of 500**, so the pipeline distinguishes policies; and the **ten** DT
checkpoints at that tier carry distinct weight digests with correct `spatial_mixing` flags.
*(Corrected: an earlier version of this packet said "four" — review MN-10.)*

> ⭐ **It is the strongest instance of §1b's R1 anywhere: a fixed-time controller is
> state-independent, so every DT arm learns it EXACTLY — identical actions, identical trajectories,
> identical ATT, from four different models. Different weights, same argmax.** Three contrasts are
> exactly `0.0000` at that tier and **those zeros are informative**. The `fixedtime` null in Q5 is
> *predicted* by the same mechanism: a state-independent controller makes every stream equivalent,
> so the global and per-node filters select the same thing.

### 1.3 What survives for the DT, stated with its fence

**Q3b still holds where it can be tested:** `dt_nomix` beats its own behaviour policy on
`maxpressure` (**−0.7294**, CI [−0.9139, −0.5450]), **on `random` (−5.0360, CI [−5.5005, −4.5716])**,
and equals it exactly on `fixedtime`. *(The `random` case was omitted from an earlier version of
this packet — review MN-8. §4.5 requires every testable ordering, and under-reporting in our own
favour's opposite direction is still under-reporting.)* **So the DT
still answers C1's question — does the model exceed its data — while failing the field question.**
**And the DT arms are the only arms that never collapse.**

⚠️ **The fence travels with every sentence:** `dt_nomix` is the identity-graph model, **not** the
merged `agent/DTAgent.py`. §1b's **R7** is a descriptive count and carries no inferential weight;
no family-wise correction exists and P8.1 has not run.

### 1.4 C4 was vindicated exactly as specified, five days before the data

The hard subset is **2/6 · 2/6 · 3/6 = 7 of 18**, while the 15-pair counts are carried entirely by
`iql` and `bc_top10` sitting far from the field.
> ⚠️ **CORRECTED (review MJ-5): the sentence *"below the 9 that coin-flipping gives"* is NOT robust
> and must not be written.** `fixedtime` carries **two exact ties** to full double precision
> (`dt_spatial` = `dt_nomix`, `bc` = `bc_top10_perix`), so three of these integers are tie-break
> dependent; enumerated over all valid orders the range is `maxpressure` 2..2 · `fixedtime` **2..4** ·
> `random` 3..3, i.e. **7..9 of 18 — and 9 is exactly the coin-flip expectation.** **A tie-break rule
> is now declared and implemented** (a tie is discordant for both orders), under which the count is
> the lower end, **7 of 18**, and **the range travels with it**. ✅ **Q2b's FAILED verdict is robust
> (max 9 < 12) and Q3's "does not lead" is robust (rank ≥ 2 always)** — only the coin-flip sentence
> was not.
⚠️ **A stale-threshold trap is recorded rather than quietly avoided:** Q2b's parenthetical
*"≥ 24 of 30 overall"* was written when there were **two** out-of-sample tiers; scored against three
it becomes 27/45, which passes 24 and is **meaningless**. **The per-tier criterion is the registered
primary and it is unambiguous.**

### 1.5 The I1/J1 envelope — non-zero on the treatment arm, read per I2 as a FINDING

Verified in `output/p5_2/envelope_random_seed202.json` (read by me this session):

| block | mean | CI95 | resolves? |
|---|---|---|---|
| `dt_nomix` | **+0.0000** | [+0.0000, +0.0000] | 0 of 100 draws differ |
| `dt_spatial` | **+0.1036** | [−1.1990, +1.4062] | 100 of 100 draws differ, does **not** resolve |
| `d1` | +0.1036 | [−1.1990, +1.4062] | no |

> ⭐ **H2's scope limit was justified by measurement rather than caution: the zero envelope did NOT
> transfer to the degraded tier for the mixing arm.** **Read per I2, registered before the number
> existed: this is a FINDING, not a failure** — run-to-run nondeterminism reaches the metric when
> decision margins narrow, and the effect is present but too small to resolve at n = 100.
> 🚨 **It vindicates making the replicate UNCONDITIONAL.** Nothing here "turned on a small margin" in
> a way a human would have noticed, so the triggered version would never have run.

J2's three conditions held: independent training from scratch, distinct artifact key, and the
canonical `state_dict` digests asserted different **before** any envelope was computed —
`dt_spatial` `f7333967431c…` against `dcbffab5b82d…`, `dt_nomix` `3a7234cc0ec3…` against
`699209df930e…`. The 1e−12 positive control fired on both arms.

---

## 2. Branch + diff stat

```
task/p5.2-tier-sweep
$ git diff --stat main...HEAD
 docs/data/p5_2_declaration.json             |  528 +++++
 docs/data/p5_2_declaration_fixedtime.json   | 1748 +++++++++++++++++
 docs/data/p5_2_declaration_mappo1000.json   | 1752 +++++++++++++++++
 docs/data/p5_2_declaration_maxpressure.json | 1748 +++++++++++++++++
 docs/data/p5_2_declaration_random.json      | 1748 +++++++++++++++++
 docs/patches/README.md                      |   63 +
 docs/patches/ci_gate_baseline_pins.patch    |  105 +
 docs/plans/p5.2.md                          | 1064 ++++++++++
 docs/returns/P5.2.md                        |  455 +++++
 offline/campaigns/p5_2.sh                   |  477 +++++
 offline/tier_sweep.py                       | 2834 +++++++++++++++++++++++++++
 tests/test_tier_sweep.py                    | 2180 +++++++++++++++++++++
 12 files changed, 14702 insertions(+)
```

⚠️ **Corrected (review MN-9): the figures above are the stat as taken, and the earlier version of
this packet quoted `11 files / 13494` from a stat run five lines too early.** The current committed
total is larger again after the review fixes; the command output above is what `git diff --stat`
prints today.

**Every path is an addition. Zero frozen files touched** — `claude_guard.sh --frozen-only` exits 0.

## 3. Files produced

- `docs/plans/p5.2.md` — the registration: predictions, bands, thresholds, the stop rule, D1–D17
- `offline/tier_sweep.py` — the module: tiers, size match, the new filter arm, the trainer, the
  gates, the scorers, the CLI
- `tests/test_tier_sweep.py` — 146 tests
- `offline/campaigns/p5_2.sh` — Gates 0/1, phases E1/A/C/B/R, the stop rule, the completeness check
- `docs/data/p5_2_declaration.json` + three per-tier declarations — written before the run
- `docs/patches/ci_gate_baseline_pins.patch` + README entry — `DEFERRED` 54, for `main` only

## 4. Tests — real output

```
$ OMP_NUM_THREADS=1 MKL_NUM_THREADS=1 \
  RLTRAFFIC_CORPUS_V11=/home/filip/rltraffic/datasets_v11 \
  RLTRAFFIC_CORPUS=/home/filip/rltraffic/datasets .venv/bin/pytest -q
1341 passed, 3 skipped, 15 warnings in 122.01s (0:02:02)
```
**Pinned, corpus env named, 3 skipped.** ⚠️ Without the corpus env the same suite reports far more
skips — the corpus-gated tests self-skip — so the env is part of the measurement, not decoration.

```
claude_guard.sh --frozen-only    exit 0,  0 violations
check_english.sh                 exit 1,  6 hits -- all pre-existing, none in P5.2 files
check_test_hygiene.sh            exit 1, 16 hits -- all pre-existing, none in P5.2 files
```
All three run with **no arguments** and counted from full output. The English hits are
`.claude/agents/master-coordinator.md:158`, `README.md:30`, two `docs/patches/*.patch`,
`docs/returns/P8.3.md:141`, `scripts/claude_guard.sh:47`; the hygiene hits are TH006 in four frozen
env/phase test files.

### 4.1 `DEFERRED` 37, executed on 16 intersections — 3 of 3 killed

The mutant feeds `act()` the wrong intersection's statistics, keying every lookup on
`intersection_ids[0]`. On `cf_hz1x1` it is equivalent; on grid4x4's 16 it is live.

```
agent/SpatialDTAgent.py   KILLED   2 failed, 1306 passed, 3 skipped
  FAILED tests/test_multi_intersection_normalisation.py::..._own_statistics[spatial]
  FAILED tests/test_multi_intersection_normalisation.py::test_the_spatial_dt_feeds_..._normalised_state
agent/DTAgent.py          KILLED   2 failed, 1306 passed, 3 skipped
  FAILED tests/test_multi_intersection_normalisation.py::..._own_statistics[dt]
  FAILED tests/test_multi_intersection_normalisation.py::test_the_dt_feeds_..._normalised_state
agent/OfflineBaselines.py KILLED   2 failed, 1306 passed, 3 skipped
  FAILED tests/test_multi_intersection_normalisation.py::..._own_statistics[bc]
  FAILED tests/test_multi_intersection_normalisation.py::test_the_baseline_feeds_..._normalised_state
```
Each mutation applied to a committed file, reverted with `git checkout --`, and `git diff --quiet`
asserted after each. **Tree clean.**

⚠️ **Disclosed because it produced a false negative first:** my initial run used
`-k "spatial or dt_agent or offline_baselines or tier_sweep"` and reported `DTAgent.py` and
`OfflineBaselines.py` as **SURVIVED** — the filter had deselected the very tests that catch them.
**A filtered mutation run can manufacture a survivor.** Re-run against the full suite, all three die.

### 4.2 Other mutations — 19 of 19 killed

12 on the guards and scorers, 7 on the trainer and the regime. Two **genuine survivors** in the
first pass, both real gaps, both now killed — see §7.

---

## 5. Self-review checklist

- Contracts honored, including the non-standard env API? **Y** — evaluation went through the merged
  `dt_gate.evaluate_arm` path unchanged; C8's metric set came from each tier's manifest; C9's
  forbidden brand string appears **0 times** in all four P5.2 files (measured).
- All tests actually executed and green? **Y** — §4.
- Zero modifications to frozen files? **Y** — the diff stat is all additions; guard exits 0.
- Zero new dependencies? **Y.**
- Every number in this packet produced by a command run in this session? **N, and the split is
  stated.** §1's verdicts are the coordinator's scoring at `119cc48` and I did not re-derive them.
  What I ran this session: the campaign-state verification (35/35, 28 cells, 124 checkpoints, 0
  partials), the envelope artifact read, `DEFERRED` 37's three mutations, the checkpoint cadence,
  the suite and the three guards.
- Is any registered prediction reported as holding when it failed? **N.** Q2a, Q2b and Q3 failed
  3/3 and are reported as failed; Q4 is reported as inverted against its registered direction.
- Was every required amendment implemented? **N — and this is BL-1.** Amendment O4 was required and
  I did not implement it or disclose the omission; it destroyed six training records. §6a.
- Was the plan edited after results existed? **N.** Every amendment to `docs/plans/p5.2.md` predates
  the numbers it governs, and §0.1 records each with its direction.
- Did anything get "fixed" after seeing a result? **N.** No threshold, band, scorer or arm
  definition changed after phase B produced a cell.

---

## 6. Declared deviations

**D-1. Tier execution order (Amendment Q3).** Registered
`mappo1000 → maxpressure → fixedtime → random`; **executed `maxpressure → random → fixedtime`.**
**Declared, not corrected — the declaration is a Gate 0 registration and rewriting it to match
execution would destroy the record that makes this visible.**
> It is bookkeeping rather than science, *with evidence*: each tier trains on its own corpus and
> seeds and evaluates on the same fixed 100 held-out draws with no cross-tier state, and **E1
> measured the run-to-run envelope on this metric at exactly 0.0000**, so a tier's numbers do not
> depend on when it ran. Its **duration** does, through thermal state, and durations are governed by
> M3's cadence rule. `TIER_ORDER` the constant is untouched, so B0's *order every figure by measured
> ATT* is satisfied by the report rather than by the run order.
> ⛔ **What is NOT written: "this changes nothing about the design."** It changes the executed order
> away from a registered one; the honest form is that the deviation is declared and its effect is
> confined to duration.

**D-2. Durations from checkpoint cadence, not log deltas (M3).** The campaign log spans a **≈2 h 10 m
forward clock jump after 07:59:54**, a machine restart and a kill, so totals crossing the jump are
inflated. Cadence intervals lie within one clock. Read from checkpoint mtimes this session:
`dt_spatial_h4` **59 · 68 · 70 · 80 min**, then `dt_nomix_h4` **81 · 82 · 81 min** — reproducing
M3's 59.5 · 67.4 · 69.9 · 80.7 / 81.3 · 81.3 · 81.7. The one interval spanning the jump (07:59 →
11:25, 206 min) is excluded.
> **No CELL is affected.** Every cell's numbers come from the corpus and the model, not the clock.

**D-3. `bc_top10_perix` is a new arm beside the global filter, never a re-specification** (§4.3).
The un-re-specified `bc_top10` collapse is reported: **679.96 on `maxpressure`** against the
per-intersection filter's **210.27**.

**D-4. The campaign was launched by the user in `tmux`**, per CLAUDE.md §5; I wired and rehearsed,
and did not run the long jobs in-session.

## 6a. 🚨 DATA LOST, AND A REQUIRED FIX I DID NOT IMPLEMENT OR DISCLOSE (review BL-1)

**`BRIEF_27` Amendment O4 required, in my next commit: *"merge by `(tier, method, seed)` into any
existing record rather than overwriting it, and test the resume case — write 3 seeds, resume, assert
the file holds 5."* I did not implement it and I did not disclose that I had not. `O4` appeared zero
times in the plan and zero times in the first version of this packet.**

**The defect then fired.** The writer held only the seeds trained in the *current* invocation and
wrote `"runs": records` over whatever was on disk. A resume skips every seed already trained, so it
wrote a **short** record — and a resume with nothing left to do wrote an **empty** one.

**Measured on disk before I changed any code:**

| record | `runs` now | should hold | cause |
|---|---|---|---|
| `training_mappo1000_baselines.json` | **0** | 20 | resume wrote `[]` over a complete record |
| `training_maxpressure_baselines.json` | **0** | 20 | same |
| `training_fixedtime_baselines.json` | **0** | 20 | same |
| `training_random_baselines.json` | **0** | 20 | same |
| `training_random_dt_spatial.json` | **1** | 5 | phase R's replicate overwrote phase B's record |
| `training_random_dt_nomix.json` | **1** | 5 | same |

**The last two are also a J2(b) leak.** `REPLICATE_SUFFIX` covered the checkpoint name and the
evaluation cell but **not** the training-record path, so phase R wrote into phase B's file. **The
packet's earlier J2 "distinct artifact key" claim was true of two of three artifact classes.** A
distinct key is only distinct if it covers every artifact class the run writes.

> 🚨 **THE SIX FILES CANNOT BE RECOVERED.** `output/` is gitignored, there is no backup, the records
> are overwritten in place with no history, and the coordinator's own O4 note had verified them
> intact on 2026-08-20 (*"baselines carries 20"*). **They are not intact now, and this packet says so
> rather than letting the record simply be short.**
> **What survives, stated precisely so the loss is neither overstated nor minimised:** every
> checkpoint's `provenance` block still carries `tier`, `method`, `seed`, `gradient_steps`,
> `batch_size`, `learning_rate`, `weight_decay`, `grad_clip`, `device` and `runtime`, and all **124**
> checkpoints are present and loadable. **So the IDENTITY of every training run is recoverable from
> the checkpoints. What is gone is `final_loss` and `seconds` per run** — they were only ever in the
> training records. **I have not reconstructed the files, because a reconstruction presented in the
> place of the original is a fabrication.**
> ⚠️ **No reported number changes.** §D-2's durations come from checkpoint mtimes, not from these
> records, and no cell, prediction or verdict reads them.

**Fixed and tested** (`69e1172`, `f4e1d3c`): `merge_training_runs()` merges by the declared key,
`training_record_name()` carries the replicate suffix, and both writers go through one
`write_training_record()` so the load-merge-write path is testable end to end. **Seven mutants
killed**, including O4's stated acceptance case and the exact shape that emptied the baselines.
⚠️ **One of them survived my first attempt**: testing `merge_training_runs` as a pure function did
not cover the writer, so a mutant that discarded the merge passed the whole suite. **Testing the
pure half of a load-merge-write sequence is not testing the sequence.**

## 7. Defects found in my own work, disclosed

**7.1 M16 — the mutation that would have cost the whole of phase A.** Narrowing the mixing flag to
`method == "dt_spatial"` left **`dt_spatial_h4` training as a no-mixing model wearing the treatment
arm's name.** Phase A would then have compared two controls and measured only the head count — and
it would have looked entirely normal, because the two arms are weight-compatible by design, which is
exactly why P5.1's `assert_declared_budget` checks the mixing flag on load. The stop rule could not
have caught it: a voided contrast makes `d4 ≈ 0`, the CI straddles, the rule does **not** fire, and
the ladder runs on a quantity that is not the contrast. **No test caught it; mutation did.** It is
now killed by a parametrised assertion over all four arms of the 2×2 checking `config.spatial_mixing`
**and** that the saved mask has an off-diagonal edge iff the arm mixes. **This is the single most
valuable thing the mutation discipline bought in this task.**

**7.2 Four hand-transcribed float literals, all four wrong.** I typed the `ladder_att` values into
`TIERS` and every one was wrong beyond six significant figures (`160.33195877075196` against the
artifact's `160.33195198059082`, and the same for the other three). Identical at the 2 dp the plan
quotes, so nothing downstream moved — but they order every figure. **The cross-check against
`att_ladder_v11.json` caught it in the red run, before any implementation consumed them.** The
pin-plus-verify pattern is why: declaring the value *and* asserting it against the artifact catches
a transcription error that either half alone would miss.

**7.3 My mutation harness destroyed uncommitted work.** It reverts with `git checkout --`, which
restores HEAD — and HEAD did not yet contain `train_tier_dt`. One mutation ran, the revert wiped
~200 lines, and six later patterns silently reported "0 matches" as a soft outcome, which would have
read as six phantom passes. **The D1 class, in my own tooling.** The harness now refuses a target
with uncommitted or staged changes, and the code was restored and committed *first*.

**7.4 `behaviour@fixedtime` had no factory — a deterministic defect that cost three days.**
`_arm_factory`'s behaviour branch covered `random`, `maxpressure` and `mappo1000` and fell through to
its own `raise` for `fixedtime`. Reachable **only at evaluation time**, so it surfaced after that
tier had trained and recurred on every resume. **Root cause is coordination, not coding:** `fixedtime`
was added late, H3 listed the registration work meticulously, and nobody listed the implementation —
the declaration declared 35 cells and the factory could build 34.
> **Fixed by importing P4.6's `_fixedtime_factory`**, which additionally asserts the plan hash and
> schedule source against the tier's own manifest. **Prevented in general by a Gate 0 check** that
> walks the declared `(tier, method)` matrix and refuses any cell whose factory raises — 28
> constructible, seconds, no GPU; **proven to catch the original defect** (27 checked, identical
> `ValueError`, exit 1). ⚠️ `assert-complete` cannot catch this class: it compares declared cells to
> cells on disk, so it fires only *after* the work.

**7.5 The failure was invisible, and that is its own finding.** The campaign redirects each step to
its own per-arm log, so the traceback sat in `logs/eval_fixedtime_behaviour.log` while `campaign.log`
and the tmux pane showed nothing — **restarts were spent on an environmental hypothesis with the
answer already on disk.** All 12 redirected invocations now route through a `step` helper that echoes
the failed log's tail into the campaign log before returning the failure.
> ⚠️ **And my first version of that helper was broken in the worst direction.** It used
> `if ! cmd; then local code=$?`, where the status is already reset, so it reported exit 0 and
> **returned success — `set -e` sailed past a failed step.** A log-surfacing helper that swallows
> failures is far worse than the invisibility it fixes. Caught by rehearsing it rather than reading
> it; re-rehearsed both ways after the fix.

**7.6 Tests I changed, in full.** Three encoded the superseded three-tier registration (N = 13 → 19,
`fixedtime` not in `TIER_ORDER` → in it) and were updated when H1 added the tier. One band-edge test
used `predicted × 1.30`, which is **not** a relative error of exactly 0.30 in binary floating point
(0.30000000000000004 on four of five registered levels) — it was failing on a float artefact rather
than on the rule, and now injects `100 → 130`, which is exact. One size-match test was **strengthened**
so the count assertion applies to every tier, not only the subsampled one. One factory test was
**narrowed** with its reason stated: it required a constructible behaviour factory for all four tiers,
but `mappo1000`'s is a reused P5.1 cell this campaign never produces; it now parametrises over the
anchors the campaign produces and asserts that set against the declaration so it cannot drift.
**No assertion was weakened to make failing code pass.**

## 8. Conflicts found between brief and repo (repo wins)

**8.1 §5's ATT figures were `cf_hz1x1`'s, quoted as grid4x4's** (`281.89` / `262.09`). Confirmed by
the coordinator as B0. Implemented to grid4x4's own measured column throughout.

**8.2 K2's "zero new code" premise was false.** It said the completeness assertion's
declaration-derived path already existed; **P5.1's script has it and P5.2's did not** — I dropped it
when restructuring around the E1-first phase order. Built it rather than quietly satisfying the
requirement.

**8.3 B4's "200 streams" is P4.6's unit, not this scenario's.** On grid4x4 a stream is an
`(episode, intersection)` pair, so 200 of those is 12.5 episodes' worth of nodes and
`build_joint_index` could construct no joint window at all. Implemented as **200 episodes / 3,200
streams**, which is the size match the rule expresses.

**8.4 `DEFERRED` 54's fix was correct on `main` and wrong on this branch.** Shipped as
`docs/patches/ci_gate_baseline_pins.patch` instead of a commit. **The alternative — copying main's
`ci_baseline.json` here to make the branch green — was refused: this tree measures 6 English hits,
not main's 4, so it would have committed a measurement that does not describe the tree it sits in.**

## 9. Corrections to earlier records

**Two kills, not three (Note T2).** Note S recorded "an unexplained silent SIGKILL" and "three kills,
three different causes", and S3 made that a packet sentence. **The third interruption was never a
kill** — it was §7.4's deterministic `ValueError`, and the traceback was on disk throughout.

> **S3, corrected:** **Two kills, two different causes — a screen lock and a Windows Update restart —
> at points nobody chose, one mid-training and one mid-evaluation. The third interruption was a
> deterministic code defect that the atomic writer never had to survive.** The structural evidence is
> untouched: **25/25 evaluation cells intact, 122/122 checkpoints loadable, zero stray temporaries**,
> each kill costing only the unit in flight.
> ⭐ **Stated as validation of the WRITER, not of the resume logic.** The resume conditions are bare
> existence tests, which would be D1(c)'s exact failure if a crash could leave a truncated artifact at
> a final name. It cannot: training writes `<name>.partial` and `replace_guarded` does an `os.replace`
> behind the barrier, and evaluation writes through `write_json_atomic`. **D1(c) is satisfied at the
> writer, which is the right place** — a reader-side check would re-validate every artifact on every
> resume. What changed is that the atomicity is now measured on unchosen kill points rather than
> argued from source.

## 10. Binding disclosures carried forward

1. **Ladder provenance (PROJECT_PLAN §10 item 0).** Every ladder value quoted here comes from
   `docs/data/att_ladder_v11.json`, **randomised draws 1–200**, which is discharged for figures and
   for R′'s `T(t)` term. **`DEFERRED` 27 stands: the ladder tool's JSON-writing path has zero tests,
   so the artifact's CONTENT is verified and the PATH that wrote it is not.** Corroboration: P5.1's
   two anchors landed **0.0540** and **2.6276** ATT (0.03 % and 1.0 %) from these values on
   independently rolled held-out draws. The nominal draw-0 comparison and anything from
   `rederive_anchors.py` remain blocked.
2. **The tier set's spacing (B1).** On grid4x4 `mappo1000` and `maxpressure` are **7.16 ATT apart —
   4.5 %** — and the four tiers cover **97.40 of 1209.89 ATT, 8.1 %** of the corpus's measured range.
   **They do not span it and no sentence may say they do.** Separation is on the **return** axis,
   **5.5×** (−165.6 → −918.6), and C1 is registered in measured return.
3. **The single-Q deviation** (`offline_baselines.py:1960-1963`, no clipped double Q, in every merged
   P4.x number) **stays registered and unrepaired**, and is disclosed here because IQL numbers appear
   above — including IQL winning two tiers.
4. 🔒 **The P8.3 fence (C3).** **No sentence in this task cites P8.3's D4RL numbers as validation of
   the IQL arm.** P8.3 is an unreviewed external calibration and its own §6 fences it. The IQL arm's
   honest disclosure is the single-Q deviation above.
5. **`DEFERRED` 21.** `avail_mask` is all-True throughout this corpus; **no sentence credits
   masked-action modelling**, and no mask ablation was run.
6. **G3's two counts, which the DoD required and which this packet previously omitted.** The
   with/without `CUBLAS_WORKSPACE_CONFIG` comparison, run on the C1 harness at campaign shape,
   `dt_spatial`, 100 steps, determinism **off** in both, three replicates each:

   | condition | tensors differing (3 replicates) | worst \|Δw\| |
   |---|---|---|
   | `CUBLAS_WORKSPACE_CONFIG` **unset** | **63/66 · 60/66 · 57/66** | 2.980e−06 |
   | `CUBLAS_WORKSPACE_CONFIG=:4096:8` | **60/66 · 62/66 · 58/66** | 2.038e−06 |

   The ranges overlap completely and losses differed in all six runs, so **the variable suppresses
   nothing on this model: G2 was a cleanliness measure, not a live defect that would have corrupted
   E1.** The ruling stands regardless because it removes the question at no cost. **And the C1
   control's own default arm did NOT carry the variable** — ambient unset, absent from the profile,
   a fresh interpreter sees `None`, the harness sets nothing — so the C1 numbers describe the regime
   P5.1 ran in and no disclosure is owed there.
7. **H4's three:** the provenance `tier` label changed from `grid4x4_mappo1000` (P5.1) to `mappo1000`
   (P5.2) in checkpoints that sit side by side; the **+10.3 %** determinism timing was measured with
   `CUBLAS_WORKSPACE_CONFIG` **set in both arms**, so it is the flag's cost *given* the variable, not
   the flag's cost alone; and the E1 chain is §10.1 below.

### 10.1 The E1 verification chain, as its own paragraph

E1 returned **+0.0000 on all three blocks**. It was treated as a suspected defect **because it
contradicted our own C1 control**, which had measured 61–63 of 66 tensors differing between two runs
of the same code. The two checkpoints were compared and found to differ on **66/66 tensors, worst
1.22e−04**. A **file hash was found to carry provenance rather than weights** (`DEFERRED` 29) — the
two files differed on `git_commit`, `deterministic`, `n_head` and a changed `tier` label while saying
nothing about the weights — so a **canonical `state_dict` digest** over sorted tensor bytes was
computed instead. **Every recorded field was checked, not ATT alone.** And a **positive control proved
the detector resolves 1e−12.** ⚠️ **Without that last step the result would have been
indistinguishable from a self-comparison, and F7 did not require it** — which is why J2 now makes the
digest assertion mandatory and why equal digests are a refusal rather than a zero.

## 11. Limitations

- **The four tiers are one scenario.** Nothing here speaks to grid4x4's behaviour on another network,
  and `cologne3` is unavailable for the spatial question — its adjacency has zero off-diagonal edges,
  so `dt_spatial` and `dt_nomix` would be the same model.
- **The head-count 2×2 is one tier.** Q0 did not fire, but the 4-head arm was measured at `mappo1000`
  only, and `n_head=4` raises the **temporal** head count too — it is not a GAT-head ablation.
- **The `random`-tier envelope does not resolve** at n = 100 (CI [−1.1990, +1.4062]). Present,
  unresolved, and reported as such.
- **R′ is a weak rule and was registered as one** — median 23.9 %, max 378.5 % on its only
  out-of-sample calibration. Q1 holding 15/19 does not make it a good predictor of levels; Q2's
  failure on every tier is the more informative half.
- **Q4's `fixedtime` zero is exact but degenerate.** It is a real result about state-independent
  controllers, not evidence about spatial mixing at that data quality.

## 12. Open questions for the coordinator

1. **Does Q4's inversion change §1b's reading of P5.1?** The merged §6 entry says spatial mixing is
   decisively harmful; it is now a `mappo1000` phenomenon. Not mine to edit.
2. **`iql` won two out-of-sample tiers outright** — the opposite of P5.1's grid4x4 finding where it
   lost to `random`. Worth its own look before any IQL sentence enters the paper.
3. **The `random`-tier envelope is unresolved rather than zero.** Does it warrant a second replicate
   at another seed, or does the disclosure suffice? I have not run one and would not choose the seed
   after seeing this.
4. **The merge needs the CI ceiling re-measured.** This branch's `ci_baseline.json` still reads
   `5 / 72 / 62` while main reads `4 / 98 / 72`; the branch never touches the file, so git takes
   main's side, but main's mandate reads *"merge of P5.2"* and this branch adds corpus-gated tests.

## 12a. Response to `docs/reviews/P5.2.md` (FAIL — 2 blockers, 7 major, 10 minor)

**Every blocker and major is closed with mutation evidence. Nothing was argued away.**

| # | finding | what I did |
|---|---|---|
| **BL-1** | O4 unimplemented, undisclosed, data destroyed | §6a: merge by `(tier, method, seed)`, replicate-safe record name, one `write_training_record()`; **7 mutants killed**; the loss disclosed as unrecoverable |
| **BL-2** | `score_stop_rule` untested, 3 survivors | 8 tests; **M04 · M05 · M06 killed**, plus both exit branches of `_run_stop_rule` |
| **MJ-1** | `d1` composites untested | extracted `paired_d1_block()`, 4 tests; regenerating the envelope reproduces `+0.1036` exactly |
| **MJ-2** | B4's size match on the training input untested | extracted `joint_rows_for_episodes()`, 3 tests |
| **MJ-3** | `bc_top10_perix` wiring untested | extracted `baseline_stream_selections()`; the test asserts the two filters differ in *which* streams, since they agree in *how many* by design |
| **MJ-4** | qualifier absent; docstring claimed imports never made | `per_seed_block()` imports P5.1's two workers and is emitted into both reports; docstring corrected to say what is used **and what is not** |
| **MJ-5** | two exact ties, no tie-break rule | rule **declared in the plan and implemented** in `concordance()`; the non-robust coin-flip sentence corrected in §1.4 |
| **MJ-6** | `docs/plans/p5.2.md:861` still two-tier | corrected, and it records that P5 required it once and it was missed |
| **MJ-7** | G3's two counts absent | §10.6, both counts with three replicates each |
| **MN-2** | no cell→checkpoint link | every cell now records `model_provenance`: path + canonical weight digest per seed |
| **MN-8/9/10** | Q3b under-reported; stat taken early; "four" checkpoints | all three corrected in place, each marked |

**Minors not taken, with reasons.** **MN-1** (Gate 0's aggregator failing branch) — the aggregator is
covered by the live refusal I pasted when the `fixedtime` branch was removed, but the reviewer is
right that no *test* makes it refuse; **left open and named here rather than silently skipped**.
**MN-3** (`seeds_tag` drop), **MN-4** (dict-order dependence), **MN-5** (`ceil`→`floor` equivalent at
n=200/node) — all real, none affects a reported number, and each is a test-strength gap I am
recording rather than claiming to have closed. **MN-6** — this packet still summarises most mutation
runs as counts; the blocker and major mutants are pasted in full. **MN-7** — `time.time()` is
unchanged; P6's quote is not in the file.

⚠️ **Two things the reviewer found that I want restated in my own words, because they are the
uncomfortable ones.** First: *"the task's declared FIRST result runs through completely untested
code."* Q0's `d4` was the thing the whole stop rule fires on, and I had tested only the predicate
around it — a reader auditing *"is the stop rule tested?"* would have answered yes and been wrong.
Second: BL-1 is not a coding slip but **an unmet requirement I never disclosed**, and the review had
to find it. **The packet is supposed to be where that surfaces first.**

## 13. AI-assistance record

- **Tool and version:** Claude Code (Opus 4.6 class), 2026-08-18 → 2026-08-23, worktree
  `/home/filip/rltraffic-p52`, branch `task/p5.2-tier-sweep`.
- **Authorship per file:** `offline/tier_sweep.py`, `tests/test_tier_sweep.py`,
  `offline/campaigns/p5_2.sh`, `docs/plans/p5.2.md`, `docs/patches/ci_gate_baseline_pins.patch` and
  this packet are **AI-authored**. The four `docs/data/p5_2_declaration*.json` files are
  **machine-generated** by `offline.tier_sweep declare`/`declare-campaign`. No human-authored file was
  edited; no frozen file was touched.
- **Human verification performed:** the **user launched and supervised every campaign phase** in
  `tmux` and reported the interruptions; the **coordinator scored all of Gate 2 independently from
  the raw episodes** (`119cc48`), verifying 25/25 cells, 122/122 checkpoints and `assert-complete`
  35/35 without reading any report, and independently re-derived `fixedtime`'s six R′ cells, C4's
  9-of-15 arithmetic and F5's three intervals. The AI ran the C1 control, the G3 with/without
  probe, 19 code mutations plus `DEFERRED` 37's three, the Gate 0/1 rehearsals, the stop-rule
  rehearsal on all three branches, and the suite and guards.  **After the review: 7 blocker mutants
  and the major extractions, each pasted.**
- **Independent review:** `contract-reviewer`, fresh context, read-only, 51 mutants in an isolated
  sandbox — **32 killed, 19 survived**, one survivor a deliberate no-op control. It returned FAIL and
  found BL-1, which none of my own checks had.
- **Who made the research decisions:** **pre-registered before the run** — every prediction, band,
  threshold, the stop rule and the collapse criterion were committed in `docs/plans/p5.2.md` at
  `672f566` (2026-08-18) and amended only before the numbers they govern, each amendment dated in
  §0.1. **Coordinator rulings** set the tier set (B1/H1), the reuse of `mappo1000` (A3), the
  head-count 2×2 (A2), the numerical regime (H1), the envelope design (E1/I1/J1) and the Q1 threshold
  rule (I3), all named by brief section. **The AI chose no research question and no threshold**; its
  decisions were implementation, verification design, and the disclosures in §7.

## 14. What the next task will assume

- **The spatial-mixing harm is `mappo1000`-only.** `+39.5649` at the best tier, `−0.7063` at the
  worst, with `fixedtime` exactly zero. Any P5.3 sentence about spatial mixing inherits this, not
  P5.1's single-tier headline.
- **The DT leads no out-of-sample tier** (`2/6 · 3/6 · 3/6`), but still beats its own behaviour policy
  where that is testable, and is the only arm family that never collapses.
- **The load sorter is confirmed across tiers**, and `bc_top10_perix` is the repair — with a
  *predicted* null on `fixedtime` that the mechanism explains.
- **`offline/tier_sweep.py` is reusable for a tier sweep**: declaration-derived completeness,
  factory constructibility at Gate 0, the read-only barrier, and an atomic writer validated on two
  unchosen kills.
- **The nondeterminism envelope is 0.0000 at `mappo1000` and unresolved-but-present at `random`.**
  Any claim that our cells reproduce bit-exactly is true of `mappo1000` and **not established
  elsewhere**.
- **§6's P5.2 checkbox is left unticked** — it is the coordinator's, in the merge commit, together
  with striking `hangzhou_4x4` and `IPPO/DQN` from §6's P5.2 text (A5).
