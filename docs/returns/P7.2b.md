# RETURN PACKET — P7.2b (the target-domain SUMO probe and A17's cross-domain return-prompt calibration)
**Date:** 2026-09-15 · **Mode:** Claude Code · **Contracts:** v1

**Status:** DONE

**Written against:** `BRIEF_36` §§1–8 + Amendment **A** (2026-09-14, plan gate) · **B** (2026-09-14,
pre-flight) · **C** (2026-09-15, CLEAR + token) · **D** (2026-09-15, post-campaign) · **E**
(2026-09-15, the sampling-path defect) · the **E1 confirmation block with E1.1 and E1.2**
(2026-09-15, later session) · the **E1.1 + E1.2 confirmation block with E1.3** (~15:55) · the
**run-3 block with E1.4** (~16:30) · the **correction from disk** (~17:00) · the **E1.3 + E1.4
confirmation block** (~18:00) · the **run-4 acceptance block** (~19:05).

**This task evaluates nothing.** No held-out draw was rolled, no ρ was computed, and no ATT or
return of any DT is reported anywhere in this packet or in the artifact — see §5.

**The interim packet `docs/returns/P7.2b-e1.3-e1.4.md` covers Amendments E1.3 and E1.4 in full**
(tests, the 14-mutant round, the two survivors found and fixed). It is **referenced here, not
duplicated**.

---

## 1. Branch + diff stat

```
task/p7.2b-calibration   (worktree /home/filip/rltraffic-p53b)

$ git diff --stat main...HEAD
 docs/plans/p7.2b.md                    |  352 ++++++++
 docs/plans/p7.2b_e1.3_e1.4.md          |  242 +++++
 docs/plans/p7.2b_state_2026-09-15.md   |  155 ++++
 docs/returns/P7.2b-e1.3-e1.4.md        |  394 +++++++++
 offline/aligned_env.py                 |  179 ++++
 offline/campaigns/p7_2b_calibration.sh |  247 ++++++
 offline/transfer_calibration.py        | 1522 ++++++++++++++++++++++++++++++++
 tests/test_aligned_env.py              |  192 ++++
 tests/test_transfer_calibration.py     | 1261 ++++++++++++++++++++++++++
 9 files changed, 4544 insertions(+)
```

(This packet and `docs/data/p7_2b_calibration.json` are added by the commit that carries it, so they
are not in the stat above.) `bash scripts/claude_guard.sh --frozen-only` → **exit 0**; nothing under
`envs/ agent/ algorithms/ rewards.py states/ metrics/ utils/ CityFlow/ experiments/**.py scripts/
.claude/` appears in the diff.

## 2. Files produced

| path | what |
|---|---|
| `offline/aligned_env.py` | A16's door — the only way a CityFlow-trained DT sees a SUMO observation |
| `offline/transfer_calibration.py` | the probe, the A17 arithmetic, the fenced smoke, `report`, the canary |
| `offline/campaigns/p7_2b_calibration.sh` | the driver (token, trap, group-leader check, canary gate, stages, manifest) |
| `tests/test_aligned_env.py`, `tests/test_transfer_calibration.py` | T1–T9 + every amendment's tests |
| **`docs/data/p7_2b_calibration.json`** | **the committed artifact — run 4's, sha256 `92b1592de637cee187c56b988ce320611d89706c8f9f02c34fe7ebbe81658d86`** |
| `docs/plans/p7.2b.md`, `docs/plans/p7.2b_e1.3_e1.4.md`, `docs/plans/p7.2b_state_2026-09-15.md` | the plan gate, the E1.3/E1.4 plan, the mid-task state note |
| `docs/returns/P7.2b-e1.3-e1.4.md` | the interim packet for E1.3 + E1.4 |

Campaign outputs (gitignored, manifested): `output/p7_2b/` — 105 entries; `output/SHA256SUMS_p7_2b.txt`
— **108 lines, `sha256sum -c` gives 0 non-OK lines** (run in this session). Archive of the four runs:
`output/p7_2b_runs/`.

## 3. Tests — REAL OUTPUT

```
$ .venv/bin/python -m pytest tests/test_transfer_calibration.py tests/test_aligned_env.py -q
62 passed, 5 warnings in 22.28s

$ .venv/bin/python -m pytest -q
1957 passed, 93 skipped, 25 warnings in 173.51s (0:02:53)
```

Counts by amendment, each measured when it landed: gate 2 **39** (24 red first, 7/7 mutations) →
E1.2 **46** → E1.3 + E1.4 **62**. Mutations: 7 (E1) + 3 (B) + 4 (D) + 3 (E1) + 6 (E1.2) + **14
(E1.3/E1.4, 13 killed one-to-one, one equivalent mutant disclosed)**, plus the coordinator's
independent rounds (7, then 10 with two survivors, then 11 with one equivalent). Detail for the last
round is in the interim packet.

## 4. The probe table (§8, item 1)

### 4.1 What the 100 episodes are, verified from the chunks in this session

`output/p7_2b/probe_draw_0201.json … 0300.json`, one MaxPressure episode per draw on SUMO at
`reset(seed=1000)`:

| property | measured |
|---|---|
| chunks on disk | 100, draw ids 201..300 contiguous — no gap, no duplicate |
| two-route equality `local_return == local_return_from_lanes` | **100 / 100** |
| teleports, summed over all 100 | **0** |
| vehicle types seen (union, read from the engine) | `['cf_parity']` |
| `time-to-teleport` regime reported by the engine | `-1` (all 100) |
| decisions per episode | 360 (all 100) |
| `engine_seed_requested` | 1000 (all 100); 1 distinct `engine_seed_drawn` |
| per-episode wall clock | min 10.238 s · mean 11.188 s · max 12.763 s · **sum 1118.8 s** |
| `git_commit` across the chunks | one value, `f31973ebb770597152807e536dcf3f83cd9cb47e`, `git_dirty false` |

### 4.2 Per-k statistics — the target domain recomputed, the source domain read

The six **source** statistics are **read** from `docs/data/p4_3_probe.json` and never recomputed
(A17(b)); its sha256 `dc12f8b7e791bdf1a2097fc372a0e8918f67b40b8bf531f7bbde802f07b12a74` was
recomputed in this session and equals the one the artifact records. Read verbatim from
`budgets/k{5,20,100}/statistics/{mean,max}`, compared to the artifact under `==`: **all six equal**.

The six **target** statistics were recomputed in this session straight from the 100 chunk
`local_return` values — by `math.fsum(...)/k` **and** by plain `sum(...)/k`, two independent
accumulations — and compared under `==`, never `allclose`: **all six reproduce exactly.**

| k | draws | source mean (CityFlow, read) | source max (read) | target mean (SUMO, recomputed) | target max (recomputed) |
|---|---|---|---|---|---|
| 5 | 201–205 | −18692.4 | −15495.0 | **−23386.4** | **−22917.0** |
| 20 | 201–220 | −18186.65 | −12532.0 | **−23131.65** | **−21659.0** |
| 100 | 201–300 | −18600.59 | −12532.0 | **−23195.39** | **−20809.0** |

The budgets are **nested prefixes**: k=5 is draws 201–205, k=20 the first 20, k=100 all of them.

### 4.3 The eight targets per subject, with the registered one marked, under BOTH bounds (A1)

`in_support` in the artifact is computed against `support_range_over_the_split` — the checkpoint's
`stats.rtg` range, which is what **A17(c) registered**. Beside it, A1 requires the training-set bound
`training_set_return_min = −rtg_scale`. **Both columns below were recomputed in this session** by
calling `in_support_position` on each target against each range; every artifact verdict reproduced.

**`mappo1000`** — best source return −5762.0 · rtg_scale 9991.0 · split range [−9991.0, −6.0]
(n_rows 72,000) · training-set bound −9991.0. *The two bounds coincide for this subject.*

| rule | statistic | k | target RTG | vs split bound | vs training-set bound |
|---|---|---|---|---|---|
| rule_b | max | 5 | −8521.958954501451 | inside (0.0) | inside (0.0) |
| rule_b | mean | 5 | −7208.94250069547 | inside (0.0) | inside (0.0) |
| rule_b | max | 20 | −9958.439036067668 | inside (0.0) | inside (0.0) |
| rule_b | mean | 20 | −7328.703598518693 | inside (0.0) | inside (0.0) |
| rule_b | max | 100 | −9567.623523779126 | inside (0.0) | inside (0.0) |
| **rule_b** | **mean** | **100** | **−7185.354721543778** | **inside (0.0)** | **inside (0.0)** ⭐ **REGISTERED PROMPT** |
| rule_a | q1.0 | 100 | −20809.0 | **below (−10818.0)** | **below (−10818.0)** |
| naive | none | — | −5762.0 | inside (0.0) | inside (0.0) |

**`mix50`** — best source return −5959.0 · rtg_scale 40223.0 · split range [−40294.0, −6.0]
(n_rows 216,000) · training-set bound −40223.0. *The two differ by 71 on a 40,000 scale.*

| rule | statistic | k | target RTG | vs split bound | vs training-set bound |
|---|---|---|---|---|---|
| rule_b | max | 5 | −8813.320619554695 | inside (0.0) | inside (0.0) |
| rule_b | mean | 5 | −7455.412766685926 | inside (0.0) | inside (0.0) |
| rule_b | max | 20 | −10298.913262049155 | inside (0.0) | inside (0.0) |
| rule_b | mean | 20 | −7579.268438662425 | inside (0.0) | inside (0.0) |
| rule_b | max | 100 | −9894.735955952761 | inside (0.0) | inside (0.0) |
| **rule_b** | **mean** | **100** | **−7431.0185327454665** | **inside (0.0)** | **inside (0.0)** ⭐ **REGISTERED PROMPT** |
| rule_a | q1.0 | 100 | −20809.0 | inside (0.0) | inside (0.0) |
| naive | none | — | −5959.0 | inside (0.0) | inside (0.0) |

**The one out-of-support target is `mappo1000`'s Rule A q1.0**, 10,818 below the range the checkpoint
was trained over — and it is an **ablation**, not the registered prompt. The same value is *inside*
`mix50`'s range, which is four times wider. **The diagnostic never selects and no claim rests on
which bound is used**; on this data the two bounds agree on every one of the sixteen verdicts.

Rule A's secondary quantiles, identical for both subjects because they are functions of the probe
returns alone: q0.5 = −23163.5 · q0.75 = −22617.75 · q0.9 = −22153.5.

### 4.4 Disjointness — the three sources plus the held-out pool

`disjoint: true`, union size 300, probe band 201–300, **overlap 0 against each source**:

| source | n | path |
|---|---|---|
| `mappo1000:provenance.training_draw_ids` | 200 | `output/p4_dt/dt_seed101.pt` |
| `mix50:provenance.training_draw_ids` | 200 | `output/p4_7/checkpoints/mix50_dt_seed101.pt` |
| `p4_7_declaration:tiers.mix50.training_draws` | 152 | `docs/data/p4_7_declaration.json` |
| `held_out_pool` (1000–1099) | 100 | `PREREGISTRATION §5` |

The trap A7.1 names is recorded in the artifact: **both checkpoints' `provenance.training_draw_ids`
are 1..200**; the `mix50` field records the *candidate pool*, not the 152 draws its tier trained on,
so the assertion runs against the **union** — the safe direction.

## 5. The smoke — mechanics only (§8, item 2)

> **ATT deliberately not reported.** `horizon_rollout` returns `att_horizon` and `episode_reward`,
> and the RTG series ends at `rtg_last = target − Σ rewards`, which *is* the episode return in
> disguise. All four — `att_horizon`, `episode_reward`, `rtg_last` and the 360-entry per-decision
> RTG series — are written to the smoke chunk under `fenced_do_not_report` and **`report` refuses to
> copy that key into `docs/data/`** (Amendment A3). Verified in this session on the committed
> artifact's bytes: `fenced_do_not_report`, `episode_reward`, `rtg_last`, `rtg_series` — **all four
> absent**; `att_horizon` absent from the smoke block. The zero-shot point is P7.3's.

What the smoke proves: a CityFlow-trained DT drives a SUMO env through `AlignedEnv`, the prompt takes
effect, the RTG advances on every decision, and every action is in range.

| run | subject | decisions | `rtg_advanced_every_decision` | `n_decisions_in_support` | `actions_in_range` | episode seconds | `rtg_first` = target |
|---|---|---|---|---|---|---|---|
| 1 | mappo1000 | 360 | **false** (D1: diagnostic off by one) | 271 | true | 8.758 | −7185.354721543778 |
| 1 | mix50 | 360 | **false** (D1) | 256 | true | 9.770 | −7431.0185327454665 |
| 2 | mappo1000 | 360 | true | 231 | true | 9.411 | −7185.354721543778 |
| 2 | mix50 | 360 | true | 230 | true | 10.557 | −7431.0185327454665 |
| 3 | mappo1000 | 360 | true | **230** | true | 10.166 | −7185.354721543778 |
| 3 | mix50 | 360 | true | **269** | true | 9.801 | −7431.0185327454665 |
| **4** | **mappo1000** | **360** | **true** | **230** | **true** | **9.054** | **−7185.354721543778** |
| **4** | **mix50** | **360** | **true** | **269** | **true** | **9.302** | **−7431.0185327454665** |

Runs 1 and 2 ran the **sampling** path (`DTAgent.act`'s default `explore=True`, an unseeded
`torch.multinomial`) — the defect Amendment E was written about. Their counts are shown for the
record and **no claim rests on them**. Runs 3 and 4 ran `agent.act(info, explore=False,
update_memory=True)`, the declared evaluation path that the other fifteen call sites use.

**E3, answered by measurement.** Comparing run 3's and run 4's smoke chunks field by field in this
session, **the only differing field is `seconds`** — for both subjects. `n_decisions_in_support` is
230 (`mappo1000`) and 269 (`mix50`) in *both* runs, at different commits (`8e70d77` vs `ded56a0`) and
different canary times (0.79 s vs 0.68 s). **The smoke is deterministic under `explore=False`; there
is no argmax-tie finding to report.** Engine reads on both: `vehicle_types_seen ['cf_parity']`,
`time_to_teleport_option '-1'`, draw 5 (the fenced draw, never a probe-band draw).

## 6. The canary of every run (§8, item 3)

All four lines, quoted verbatim from the file that holds each, with that file's sha256 — every one
read by a command in this session.

**Run 1** — `output/p7_2b_runs/tmp_capture_p72b_run.log`,
sha256 `944395f082ef47ff78d4af246a8c2d7010992e0140cce0c4e442205d7b061e39`:
```
canary 0.89 s {'decisions': 360, 'local_return': -32648.0, 'att_horizon': 247.75089149261333, 'two_routes_agree': True}
```

**Run 2** — `output/p7_2b_runs/tmp_capture_p72b_reroll.log`,
sha256 `5606ce4a2dec52b3…` (first 16 bytes printed by this session's `sha256sum`):
```
canary 0.69 s {'decisions': 360, 'local_return': -32648.0, 'att_horizon': 247.75089149261333, 'two_routes_agree': True}
```

**Run 3** — `output/p7_2b_runs/tmp_capture_p72b_run3.log`, sha256 `db8573ec61b92501…`. Quoted exactly
as the bytes read, **including the splice**: this is the `tee /dev/stderr` `O_TRUNC` damage that
Amendment E1.3 item 3 fixed — the header was overwritten mid-line and the complete line follows it:
```
canary 0.79 s {'decisions': 360, 'local_return': -32648.0, 'att_hcanary 0.79 s {'decisions': 360, 'local_return': -32648.0, 'att_horizon': 247.75089149261333, 'two_routes_agree': True}
```
The same run's line is also intact in `output/p7_2b/logs/canary.log`
(sha256 `615be9c30b4718c43858b92529f18c3317423aa423be08784a3746b622d1658a`, in the manifest).

**Run 4** — `output/p7_2b/canary.json`,
sha256 `effcca5ee64e2fe4bb85298957b3c19db4be7b4bad8ee54350f2fd02fd408097`, in the manifest, secured as
`output/p7_2b_runs/run4_canary_effcca5e.json`:
```
canary 0.68 s {"att_horizon": 247.75089149261333, "decisions": 360, "local_return": -32648.0, "two_routes_agree": true}
```

**All four runs observed the same engine facts** — `local_return −32648.0`, `att_horizon
247.75089149261333`, `two_routes_agree` true, 360 decisions — and all four were under the 2.0 s
threshold. The references are tied to two committed artifacts:
`p7_1_metric_freeze.json:cells.cityflow__maxpressure.att_env_mean` and
`p0_baselines/results.json:cells[0].policies.MaxPressure.metrics.episode_reward`, compared under `==`.

**The two sentences the brief requires, in its words:**

> *The probe table's rate basis is run 1's canary, 0.89 s, whose correctness half is recovered from
> the run-1 driver capture (`944395f0…`) — not from the chunks, which do not carry it; run 4's canary
> was checked at the driver and re-verified by `report`.*

> *In any artifact `report` can write, `observed` equals `checked_against` by construction; the
> independent evidence of what the engine answered is the manifested `canary.json` and `canary.log`.*

The second sentence is the coordinator's finding on mutant **M-F** and it **withdraws** a claim made
in the interim packet: that `observed` beside `checked_against` is "the stronger form" overstated it.
`report` refuses any record whose facts differ from the references, so the two are necessarily equal
in every artifact it can produce. The artifact's canary block therefore carries a `source` field
naming the two manifested files where the independent evidence actually lives.

## 7. Both clocks per stage (§8, item 4)

Two clocks throughout: the **wall clock** of the stage and the **canary** under which it was
measured. No rate is quoted from a run whose canary exceeded 2.0 s; none did.

**Run 4** (the committed run), derived in this session from the stage-log `run at` headers
(one-second resolution), `canary.json`'s mtime, and `COMPLETE`:

| stage | wall clock | canary (rate basis) | note |
|---|---|---|---|
| canary | 0.68 s (measured by the stage itself) | — | before the token; threshold 2.0 s |
| `record-canary` | < 1 s (`canary.json` mtime 18:49:32.997) | 0.68 s | after the token (Amendment B1) |
| probe | ≈ 1 s (18:49:33 → 18:49:34) | 0.68 s | 100 chunks **reused by content**, none re-rolled |
| smoke mappo1000 | ≈ 12 s (18:49:34 → 18:49:46) | 0.68 s | episode itself 9.054 s |
| smoke mix50 | ≈ 12 s (18:49:46 → 18:49:58) | 0.68 s | episode itself 9.302 s |
| report + manifest | ≈ 5 s (18:49:58 → 18:50:03.54) | 0.68 s | |
| **total** | **`CAMPAIGN COMPLETE in 30s`** | 0.68 s | token written 18:49:12 |

**The probe band's own clock is run 1's**, because runs 2–4 reused its chunks: 100 episodes,
**1118.8 s summed**, mean **11.188 s** per episode (min 10.238, max 12.763), at canary **0.89 s**.
For comparison, P7.1's registered bare-MaxPressure rate is 11.84 s (n = 5). Run 1's total campaign
clock was `COMPLETE in 1150s` (19 min); runs 2, 3 and 4 took 31 s, 30 s and 30 s because the probe
was reused.

## 8. Where each driver ran (§8, item 5)

**Runs 1, 2 and 3 — detached tmux sessions launched from a Claude Code Bash tool call, with nobody
attached.** The implementer's exact command, identical for all three:

```bash
tmux new-session -d -s p72b -c /home/filip/rltraffic-p53b \
  "exec bash offline/campaigns/p7_2b_calibration.sh > /tmp/p72b_<run>.log 2>&1"
```

The `exec` is what made the driver its own **process-group leader** (run 3: pid 1478536, pgid
1478536), which is the mechanical condition Amendment B3's check enforces — so B3 passed. **The
operator condition behind "tmux foreground pane" was not met in any of the three**: created with
`-d`, never attached, output redirected to a file, no human able to Ctrl-C or read the scrollback,
and the pane gone the moment the driver exited. That is exactly why runs 1 and 2's canary lines
were nearly lost, and why `canary.log` (E1.2) and `canary.json` (E1.4) exist.

**Run 4 — by the author, attended, in a tmux foreground pane**, through a pipe so the driver stayed
its own group leader and `tee -a` so nothing was truncated:

```bash
cd /home/filip/rltraffic-p53b && bash offline/campaigns/p7_2b_calibration.sh 2>&1 \
  | tee -a /home/filip/rltraffic/output/p7_2b_runs/run4_driver_stdout.txt
```

The capture is 1,249 bytes with no NULs and no truncation
(`output/p7_2b_runs/run4_driver_stdout.txt`, sha256 `12fcfb1d2dd48c1c…`). **The operator condition
was met for the first time on run 4.**

## 9. The four-way diff (§8, item 6)

Computed in this session at key granularity over the four archived artifacts.

**Byte-identical across runs 1 → 2 → 3 → 4:** `disjointness`, `format_version`, `probe`,
`registered_in`, `rule_a_secondary_values`, `scenario_key`, `statistics`, `subjects`, `targets`,
`what_this_does_not_say`.

**Differ:** `canary`, `git_commit`, `git_dirty`, `smoke` — and nothing else.

| | run 1 | run 2 | run 3 | **run 4** |
|---|---|---|---|---|
| artifact sha256 | `f683e1e9…` | `8b8987d8…` | `cfdf3829…` | **`92b1592d…`** |
| `git_commit` | `f31973e` | `556c5b6` | `8e70d77` | **`ded56a0`** |
| `git_dirty` | false | **true** | false | **false** |
| `canary` block | **absent** (pre-D2) | 0.89 s | 0.89 s | **0.68 s** |
| smoke `in_support` | 271 / 256 | 231 / 230 | 230 / 269 | **230 / 269** |

Run 2's `git_dirty: true` was caused **only** by run 1's untracked artifact sitting in `docs/data/`
when `report` computed provenance — not by modified code. Runs 3 and 4 are clean. **Run 4's artifact
is the one committed**: the first whose provenance is clean *and* whose canary block reports the
reporting run rather than the run that rolled the chunks. The three earlier artifacts stay in
`output/p7_2b_runs/` with their shas, so this diff is reproducible by anyone.

**The `canary.seconds` of runs 2 and 3 read 0.89 — run 1's**, carried in the reused probe chunks.
That is the defect Amendment E1.4 removed; run 4's block reads its own 0.68 s at the top level and
records the chunks' 0.89 s at `f31973e` under `probe_chunks_canary`, with `correctness_half: "not
recorded in these chunks"`. `checked_against` occurs **exactly once** in the whole artifact
(counted in this session).

**Byte-identical regeneration (DoD) — and the one condition on it, measured.** With the artifact
moved out of the worktree so `git status --porcelain` printed nothing, and with `HEAD` at `ded56a0`
(the commit the artifact records), `report` was re-run against `output/p7_2b/` into a scratch
directory: the regenerated file's sha256 is
`92b1592de637cee187c56b988ce320611d89706c8f9f02c34fe7ebbe81658d86` — **identical** to the committed
one. The artifact was then restored and its sha re-verified.

⚠️ **A reviewer regenerating it at a later commit will get a different sha, and that is correct, not
a defect.** `_git_provenance()` records `git rev-parse HEAD` at the moment `report` runs, so the
top-level `git_commit` tracks whatever commit the regeneration happens at. **Measured after the
merge**, at `HEAD = 9c7c611`: the regenerated artifact differs from the committed one in **exactly
one key — `git_commit`** (`ded56a0…` committed vs `9c7c611…` regenerated) and in nothing else. Every
number, the whole probe table, the targets, the smoke and the canary block — including that block's
own nested `git_commit`, which comes from `canary.json` and stays `ded56a0` — are identical. The
check to run is therefore: *regenerate, and assert the diff is confined to the top-level
`git_commit`.*

## 10. Losses (§8, item 7)

**Run 1's smoke chunks were overwritten at 14:08 and their fenced block is lost.** Run 2's smoke
stages ran at 14:08:15 and 14:08:27 (stage-log headers), and a smoke is an atomic overwrite with no
move-aside, so run 1's two chunks were replaced before anyone copied them. The artifact's published
subset survives (`run1_f683e1e9.json`, carrying `n_decisions_in_support` 271 / 256), but `report`
strips `fenced_do_not_report` by design, so run 1's `att_horizon`, `episode_reward`, `rtg_last` and
360-entry RTG series exist nowhere.

**The search, stated by name and by content, re-run in this session over `/home/filip` and `/tmp`:**
by name — `smoke_mappo1000*`, `smoke_mix50*`, `*smoke*mappo*`, `*smoke*mix50*`; by content —
`"n_decisions_in_support": (271|256)` over every `*.json`. The name search returned 14 paths: the two
live run-4 chunks, the two stage logs, run 2's, run 3's and run 4's secured copies, one pre-flight
fixture and three pytest temporaries (all with `in_support` 360). The content search returned **two
files, both artifact-level copies of run 1's published output**, neither a chunk. **No file anywhere
carries run 1's fenced block.**

**Nothing else is lost.** The three earlier claims of loss were withdrawn by the correction of
~17:00 after the files were found on disk: runs 1 and 2's canary lines and run 3's driver stdout
were preserved by the implementer's `> /tmp/p72b_*.log 2>&1` redirect and are now secured in
`output/p7_2b_runs/` (shas in §6). `/tmp` on this machine is **tmpfs** — RAM, cleared at reboot — so
each was one reboot from gone, which is what `PROJECT_PLAN` §7's rule calls *not a record*.

## 11. Observations recorded rather than fixed (§8, item 8)

* **`canary.log` holds two line formats.** It is appended and never truncated, so it carries run 3's
  Python-`repr` line and run 4's JSON line, in that order. The format changed at `5dfaaa2` because a
  `repr` cannot be parsed back by anything — which is precisely why the artifact had to fall back on
  the chunks before Amendment E1.4. Both lines are true records of their runs. Anything that parses
  this file in future must handle both.
* **Two `Retrying in 1 seconds` lines at every SUMO start.** Counted in this session, per run, per
  subject: `[2, 2, 2, 2]` in each of the two smoke logs — traci connection retries, benign, reported
  as observed.
* **The interim packet `docs/returns/P7.2b-e1.3-e1.4.md`** carries E1.3 and E1.4's red-first output,
  the 14-mutant table, the two survivors (ME2, MD1b) with their diagnoses, and the equivalent mutant
  MD1c. Referenced, not duplicated.
* **`_read_canary_record`'s `parse_canary_line` permits fact names beyond the four** it requires.
  Deliberate; accepted as disclosed by the coordinator's E1.3/E1.4 confirmation block.

## 12. Self-review checklist (Y/N)

- Contracts in `docs/CONTRACTS.md` honored, including the non-standard env API (`reset` → info only;
  `step` → reward first)? **Y** — `AlignedEnv` forwards both unchanged and `tests/test_aligned_env.py`
  asserts the outcomes are untouched.
- All tests actually executed and green? **Y** — 62 and 1957, pasted in §3.
- Zero modifications to frozen files? **Y** — `claude_guard.sh --frozen-only` exit 0; the diff in §1.
- Zero new dependencies? **Y** — stdlib + numpy + torch + pytest only.
- Every number in this packet produced by a command run in this session? **Y**, with one stated
  exception: the historical timings of runs 1–3 (`COMPLETE in 1150s`, 31 s, 30 s) and run 4's launch
  details come from the archived logs and captures, which I read in this session — the runs
  themselves were not re-executed.
- Critical quantities double-computed? **Y** — the six target statistics by two accumulations under
  `==`; every in-support verdict recomputed against both bounds; the six source statistics compared
  to `p4_3_probe.json` under `==`; the artifact regenerated byte-identically.
- The fence honoured — no ATT, no episode return, no RTG series in `docs/data/`? **Y**, asserted over
  the committed artifact's bytes in this session.
- 100/100 two-route equality, 0 teleports, `cf_parity` from the engine, disjointness against both
  subjects? **Y** — §4.1, §4.4.
- The canary value beside every timing? **Y** — §7.
- Did I run run 4 myself? **N** — the author ran it, attended. I read it from disk.
- Did I run the merge reviews? **N** — they follow this commit.

## 13. Deviations from the brief

1. **The canary block's layout** (E1.4 items 3–4 fix truth-conditions, not a shape). Implemented as:
   the block's own top level *is* the this-run record, with the chunks' canary as one named sub-block
   `probe_chunks_canary`. Confirmed by the coordinator at `5dfaaa2`.
2. **No `re_checked_by_report` flag**, and the "stronger form" justification for `observed` beside
   `checked_against` is **withdrawn** here on the coordinator's M-F finding — see §6.
3. **`record_canary` does not call `check_canary`**; `report` is the checker. Recording a failing
   canary's observed values is E1.2's point.
4. **The driver's diff since `8e70d77` is not only `tee` → `tee -a`** — E1.4 item 1, written later,
   also mandates the `record-canary` invocation. The later amendment supersedes E1.3's closing
   sentence; raised rather than resolved silently.
5. **Runs 1–3 were not "tmux foreground panes"** in the operator sense the brief assumed; §8 states
   what they actually were, in the implementer's own words.

## 14. Conflicts found between brief and repo

1. **E1.3's "exactly `tee` → `tee -a`" vs E1.4 item 1** — deviation 4.
2. **The state note said E1.3 "goes in with the packet commit"**; E1.4 and the RUN 4 block require
   both to be committed *before* run 4, since run 4's provenance must be a commit that already
   exists. Implemented that way.
3. Nothing else. Where the brief specified a repo path, a function or a field, it matched.

## 15. Open questions / risks

1. **The branch has no upstream.** `git push -u origin task/p7.2b-calibration` failed offline on
   2026-09-15 at 14:41 and is still outstanding for the author. It must be pushed before the merge.
2. **`§6`'s P7.2b checkbox in `PROJECT_PLAN`** is to be ticked **in the merge commit** (CLAUDE.md §6);
   this packet does not tick it.
3. **The two merge reviewers** named in the brief's final block have not run: (1) code + mutations
   over the module, the door, the driver and the tests; (2) the artifact recomputed from the raw
   chunks. §4 of this packet performs the arithmetic half of (2) — the reviewer should treat that as
   the implementer's own recomputation, not as the independent one.
4. **`output/p7_2b/` is not in git** (gitignored). The artifact is reproducible from it byte for byte
   today; if that directory is ever cleared, the chunks are gone and only the archived artifacts
   remain. The manifest is the integrity record.

## 16. AI-assistance record

- **Tool and version:** Claude Code (Anthropic), model identified by the harness as *Claude Opus 5
  (1M context)*, 2026-09-13 → 2026-09-15. Stated as the harness reports it; a session cannot verify
  its own model identifier.
- **Authorship per file:**
  - `offline/aligned_env.py`, `offline/transfer_calibration.py`,
    `offline/campaigns/p7_2b_calibration.sh`, `tests/test_aligned_env.py`,
    `tests/test_transfer_calibration.py` — **AI-authored**, across several Claude Code sessions on
    this branch, each against a coordinator brief or amendment.
  - `docs/plans/p7.2b.md`, `docs/plans/p7.2b_e1.3_e1.4.md`, `docs/plans/p7.2b_state_2026-09-15.md`,
    `docs/returns/P7.2b-e1.3-e1.4.md`, `docs/returns/P7.2b.md` — **AI-authored**.
  - `docs/data/p7_2b_calibration.json` — **machine-generated** by `offline.transfer_calibration
    report` during run 4; not written by hand by anyone.
  - `docs/briefs/BRIEF_36…md`, `docs/PROJECT_PLAN.md` — the coordinator's, not this session's.
- **Human verification performed:**
  - **The author ran run 4 himself**, attended, in a tmux foreground pane, on mains power, after
    writing the one-shot token by hand — the campaign that produced the committed artifact. He also
    wrote the tokens for runs 1–3 and ruled on the *lost*-vs-recovered question of ~15:25.
  - **The coordinator verified every gate independently, in throwaway worktrees**, and its rulings
    are Decisions-Log rows of 2026-09-14 and 2026-09-15: *P7.2b PLAN APPROVED at the gate* (09-14) ·
    *P7.2b PRE-FLIGHT RULED: CLEAR WITH CONDITIONS* (09-14) · *P7.2b PRE-FLIGHT CLEAR on `d284e47`*
    · *P7.2b's CAMPAIGN HAS RUN* · *the re-rolled smoke … exposed a THIRD defect* · *P7.2b E1
    CONFIRMED by mutation at `7347da4`* · *P7.2b E2 CORRECTED IN TWO PLACES* · *THE CANARY IS HALF A
    CANARY* · *the canary correctness half of runs 1 and 2 is LOST* (later corrected) · *P7.2b E1.1 +
    E1.2 CONFIRMED at `8e70d77`* · *P7.2b RUN 3 VERIFIED FROM DISK* · *P7.2b E1.3 + E1.4 CONFIRMED at
    code tip `5dfaaa2`* · *P7.2b RUN 4 VERIFIED FROM DISK AND ACCEPTED* (all 2026-09-15). Three of
    those rounds killed mutants the implementer had not written, and two found surviving assertions.
  - **This session** recomputed, from disk: the six target statistics by two independent
    accumulations under `==`; the six source statistics against `p4_3_probe.json`; all sixteen
    in-support verdicts against both bounds; the four-way diff; the fence; the manifest
    (`sha256sum -c`, 0 non-OK of 108); the byte-identical regeneration; the loss search by name and
    by content.
- **Who made the research decisions:** **pre-registered before any target-domain number existed.**
  `PREREGISTRATION` **A17** (2026-09-13, tag `v1.7-prereg-a17`, commit `cc620…` per the Decisions
  Log) fixed Rule B, the registered statistic `S = mean`, the probe design (MaxPressure, SUMO, draws
  201–300, one episode per draw at `reset(seed=1000)`), the two subjects, the H3 arm's prompt (Rule
  B, mean, k = 100) and the exploratory status of the calibrated-vs-naive contrast. **A15** and
  **A18** fixed the admitted scenario set and the C3 anchor. **Nothing in this task chose any of
  that**; the module executes it. Every subsequent design question was ruled by the **coordinator**
  in a named `BRIEF_36` amendment (A1–A7, B1–B5, C1–C3, D1–D3, E1–E5, E1.1–E1.4), and the *lost vs
  recovered* ruling of ~15:25 was the **author's**. The AI sessions chose implementation shape only.

## 17. What P7.3 will assume about this task

* **`docs/data/p7_2b_calibration.json:targets[subject]`** carries the eight targets per subject;
  the one with `role: "registered_prompt"` is the number the H3 zero-shot arm conditions on —
  **−7185.354721543778** for `mappo1000`, **−7431.0185327454665** for `mix50`. The other seven are
  `role: "ablation"`. **P7.3 reads them; it does not recompute them.**
* **`probe[draw_id].local_return`** (float64) is what P7.3's logged k-shot episode on the same draw
  must reproduce bit-for-bit (A17(f)); `engine_seed_drawn` is recorded so the logger can assert the
  same seed. All 100 rows carry `engine_seed_requested: 1000` and one distinct drawn seed.
* **`AlignedEnv` is the only way a DT sees SUMO.** P7.3 wraps every evaluation env with it and
  generalises `dt_gate.evaluate_arm` with an env factory (a `BRIEF_37` item), keeping the CityFlow
  path byte-identical so P4's artifacts still regenerate.
* **Every DT evaluation on SUMO uses `agent.act(info, explore=False, update_memory=True)`** — the
  same shape as P4.3's `evaluate_point`. `BRIEF_37` states this as a contract rather than leaving it
  to a default. Runs 1 and 2 of this campaign are what that rule is for.
* **The held-out SUMO evaluation pool** is `scenarios/draws/cityflow1x1/draw_1000–1099/parity/
  noteleport.sumocfg`, untouched by this task; its regeneration gate is `DEFERRED` 80.
* **The in-support diagnostic is a diagnostic.** `mappo1000`'s Rule A q1.0 target sits 10,818 below
  the trained range; it is an ablation and nothing selects on it.
* **`report` refuses a work directory without `canary.json`** from `5dfaaa2` on. Any future re-run of
  `report` over an old work directory will refuse until one is written by `record-canary`.
