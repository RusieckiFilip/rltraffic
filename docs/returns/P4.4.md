# RETURN PACKET — P4.4 (BC, %BC and IQL against the P4 Decision Transformer)
**Date:** 2026-08-12 · **Mode:** Claude Code · **Contracts:** v1.1

**Status:** DONE

---

## 0. The result, first, before any interpretation

> ### BC **matches** the DT. **%BC and IQL both beat it**, decisively.
>
> All on the same corpus, the same 100 registered held-out draws, the same five seeds, the same
> 40,000-step budget and the same evaluation function that produced P4's gate.
>
> | arm | `att_horizon` ± 95 % CI | veh | n | verdict vs the DT (A6, δ = 0.6263) | recovered fraction |
> |---|---|---|---|---|---|
> | **bc** | **105.1635 ± 0.2017** | 44.27 | 500 | **matches** | **0.668** |
> | **bc_top10 (%BC)** | **103.1627 ± 0.1701** | 43.57 | 500 | **baseline genuinely better** | **3.863** |
> | **iql** | **103.4764 ± 0.1873** | 43.95 | 500 | **baseline genuinely better** | **3.362** |
> | madt (P4's model, re-rolled here) | 104.9558 ± 0.2021 | 44.14 | 500 | — | 1.000 |
> | mappo1000 (the behaviour policy) | 105.5820 ± 0.2515 | 44.35 | 500 | — | 0.000 |
> | maxpressure | 176.8912 ± 2.7963 | 111.92 | 100 | — | — |
>
> | paired comparison | mean diff (DT − arm) | 95 % CI | width | p | rank-biserial | wins/losses/ties |
> |---|---|---|---|---|---|---|
> | madt vs **bc** | **−0.2077** | [−0.4690, +0.0536] | 0.5226 | 9.574e−02 | −0.192 | 61/39/0 |
> | madt vs **bc_top10** | **+1.7931** | [+1.5423, +2.0438] | 0.5015 | 5.100e−17 | +0.966 | 8/92/0 |
> | madt vs **iql** | **+1.4794** | [+1.2194, +1.7393] | 0.5200 | 7.102e−15 | +0.897 | 14/86/0 |

**The plain sentence the brief asks for (§6.1):** *on the `mappo1000` tier of `cf_hz1x1`, over the
100 registered held-out draws, behaviour cloning is statistically indistinguishable from the
Decision Transformer — the paired difference is 0.21 ATT in the DT's favour with a 95 % CI of
[−0.47, +0.05] that lies entirely inside the registered equivalence margin δ = 0.6263, p = 0.096,
rank-biserial −0.192 — and a %BC model trained on the top 10 % of trajectories beats the DT by
1.79 ATT (p = 5.1e−17, rank-biserial +0.966), as does an untuned IQL by 1.48 ATT.*

**Read the recovered fraction next to it, which is exactly why A6 made it unconditional.** "BC
matches" compresses away that BC recovered **66.8 %** of the DT's margin over its own behaviour
policy, not all of it. %BC recovered **386 %** of that margin and IQL **336 %**.

**What this does NOT say.** DT − baseline is a **combined** difference — attention/context **plus**
return-to-go conditioning **plus** the timestep embedding — so none of it decomposes into "sequence
modelling adds X"; that is P4.3's (RTG) and P5.3's (no-RTG, K) registered work. And: **MADT is
outperformed by an untuned IQL configured from published D4RL-locomotion values; this is not
evidence about IQL in general** (the reverse of §8.3(a)'s sentence, which anticipated the other
outcome — the caveat binds in both directions and is carried in the artifact verbatim).

---

## 1. Branch and diff

```
task/p4.4-offline-baselines   (based on main @ 6875dd2)

$ git diff --stat main...HEAD
 agent/OfflineBaselines.py             |   647 +
 docs/data/p4_4_baselines.json         | 23619 ++++++++++++++++++++++++++++++++
 docs/data/p4_4_training.json          |  1049 ++
 docs/plans/p4.4.md                    |   394 +
 offline/offline_baselines.py          |  1958 +++
 tests/test_offline_baseline_agents.py |   555 +
 tests/test_offline_baselines.py       |   815 ++
 7 files changed, 29037 insertions(+)
```
(This packet's own file is not in that stat: it is the commit being written.)

**Zero frozen files touched** — `bash scripts/claude_guard.sh --frozen-only` exits 0, and the diff
contains no path under `envs/`, `agent/base.py`, `agent/utils/utils.py`, `agent/MAPPOAgent.py`,
`algorithms/`, `rewards.py`, `states/`, `metrics/`, `utils/`, `CityFlow/`, `experiments/`,
`scripts/`, `.claude/`. `agent/OfflineBaselines.py` is a **new** file in `agent/`, which the brief
§3 permits and which I re-verified at the guard layer (`scripts/claude_guard.sh:44` names only the
three `agent/` files above; there is no `agent/**` glob).

**No existing module was modified** — `offline/dataset.py`, `offline/dt_gate.py` and
`agent/DTAgent.py` are imported and reused, never edited.

Commits, in the order the plan fixed:

```
3cf3344 plan(P4.4)     delta, the forecast and the IQL sweep decision -- before the first gradient step
933eb54 tests(P4.4)    red: 51 failed, 1 passed, 102 NotImplementedError and nothing else
738884b feat(P4.4)     the two source files; 20 mutations run
c13aaa9 gate(P4.4)     GATE A PASS: 1100 of P4's episodes reproduce exactly   <-- precedes training
66fdc30 result(P4.4)   training, evaluation, the artifacts and the verdicts
```

---

## 2. Files produced

- `agent/OfflineBaselines.py` — `BCAgent` (BC and %BC), `IQLAgent`, the shared `MLPTrunk` (the DT's
  feed-forward stack with attention removed) and `canonical_state_dict_digest` (`DEFERRED` 29).
  Checkpoint formats `bc-checkpoint/1.0` and `iql-checkpoint/1.0`.
- `offline/offline_baselines.py` — the %BC filter, IQL's transition table, training, the A6 verdict,
  effect sizes, the artifact and the CLI (`gate`, `train`, `evaluate`, `report`). Artifact format
  `p4.4-baselines/1.0`.
- `tests/test_offline_baseline_agents.py` (22 tests) · `tests/test_offline_baselines.py` (36 tests).
- `docs/plans/p4.4.md` · `docs/data/p4_4_training.json` · `docs/data/p4_4_baselines.json`
  (2600 per-episode records: the aggregate cannot be re-derived from a mean, P8.0 finding N7).
- Gitignored, regenerable: `output/p4_4/{gate_a,eval_bc,eval_bc_top10,eval_iql}.json` and 15
  checkpoints under `output/p4_4/checkpoints/`.

---

## 3. Tests — real output

```
$ RLTRAFFIC_CORPUS_V11=/home/filip/rltraffic/datasets_v11 \
  RLTRAFFIC_CORPUS=/home/filip/rltraffic/datasets \
  /home/filip/rltraffic/.venv/bin/pytest -q
598 passed, 1 skipped, 12 warnings in 43.33s

$ /home/filip/rltraffic/.venv/bin/pytest -q          # no corpus variables, i.e. a fresh clone
588 passed, 11 skipped, 12 warnings in 42.17s
```

Against the **541** baseline I measured on this branch's base before writing anything
(`540 passed, 1 skipped, 45.62 s`): **+58 net new passing tests**, 599 collected vs 541. The one
skip in the full-corpus run is `test_corpus_format_check.py: datasets/ is not present in this
working tree`; the extra 10 skips without the variables are the corpus-gated tests, one of which is
this task's own `test_the_real_tier_filters_and_builds_transitions_consistently` — **it ran** in the
first invocation (0.66 s), which is stated because a green suite in which it silently skipped is the
failure mode, not the safeguard.

**Red-first evidence** (commit `933eb54`, run before any implementation existed):

```
$ pytest tests/test_offline_baseline_agents.py tests/test_offline_baselines.py -q --tb=line
51 failed, 1 passed in 3.66s
# 102 NotImplementedError in the tracebacks; ZERO AttributeError/TypeError/ImportError,
# so every test reached the real API surface rather than a missing name.
```
The single pass was legitimate and was named in that commit:
`test_the_held_out_pool_cannot_enter_a_training_dataset` constrains the **already-merged** loader,
and it lives in this file so that "the leakage guard stays in one place" is asserted by the task
that would otherwise be tempted to add a second one.

### 3.1 Mutation proofs — 21 run, 20 killed, 1 survived and was then killed on the fixture

| # | mutation | result |
|---|---|---|
| M1 | `_normalise_state` returns the raw state (both agents) | **2 failed — KILLED** |
| M2 | IQL treats the horizon as terminal (`done = 1` on the last transition) | **1 failed — KILLED** |
| M3 | `next_state` reads observation row `T−1` instead of row `T` | **1 failed — KILLED** |
| M4 | drop the final transition of every stream | **1 failed — KILLED** |
| M5 | %BC keeps the **bottom** decile | **2 failed — KILLED** |
| M6 | %BC floors instead of ceilings the fraction | **1 failed — KILLED** |
| M7 | canonical digest iterates in insertion order | **1 failed — KILLED** |
| M8 | canonical digest omits key, shape and dtype | **1 failed — KILLED** |
| M9 | verdict uses `<=` where A6 says "entirely below −δ" | **1 failed — KILLED** |
| M10 | the unregistered fourth branch folded into "inconclusive" | **1 failed — KILLED** |
| M11 | recovered fraction inverted | **2 failed — KILLED** |
| M12 | rank-biserial sign flipped | **1 failed — KILLED** |
| M13 | wins/losses swapped (lower ATT is better) | **1 failed — KILLED** |
| M14 | campaign integrity: the missing-runs check deleted | **1 failed — KILLED** |
| M15 | IQL acts from `Q` instead of the extracted policy | **2 failed — KILLED** |
| M16 | `act()` stops masking illegal actions | **4 failed — KILLED** |
| M17 | `load()` adopts a checkpoint from another action space | **1 failed — KILLED** |
| M18 | **BC training stops masking** | **30 passed — SURVIVED**, then killed (below) |
| M19 | filesystem barrier moved after training | **2 failed — KILLED** |
| M20 | declared-step guard deleted | **1 failed — KILLED** |
| M21 | `merge_training_runs` drops the earlier chunk's runs | **1 failed — KILLED** |

**M3 is the one the brief predicted, and it landed exactly where §8.4 said it would.** Reading
`next_state` from row `T−1` fails **exactly one** test out of 33 —
`test_the_last_transitions_next_state_is_observation_row_T_read_by_a_different_path`, which reads
row `T` through plain `np.load` — while the loader cross-check over all `t ∈ [0, T−1]` stays green,
because the loader can never yield row `T`. **Without the §8.4 correction that mutation would have
survived the entire suite and every IQL bootstrap target would have been one row early.** The
coordinator's own guard-review caught a defect my test plan had; that is recorded here rather than
smoothed over.

**M18 survived, and I am reporting it as an equivalent mutant rather than describing a guard.**
Dropping the availability mask from BC's *training* loss left all 30 tests green. On this corpus it
**cannot** move a gradient or a number: every stored `avail_mask` is all-`True` (P3 review,
32000/32000 streams). I added `test_bc_training_applies_the_availability_mask_the_way_the_dt_does`,
which kills it on the fixture (whose masks bind), so the intent is pinned where it is testable.
Same class as P4's diagonal-guard finding.

---

## 4. Task-specific answers

### 4.1 Gate A — the instrument reproduces P4 exactly

```
GATE A PASS: 1100 episodes reproduced exactly, 0 mismatches
madt         re-rolled 104.95575898180847  committed 104.95575898180847  delta 0.000e+00
mappo1000    re-rolled 105.58203462874322  committed 105.58203462874322  delta 0.000e+00
maxpressure  re-rolled 176.89119451638254  committed 176.89119451638254  delta 0.000e+00
```
Every `(arm, seed, draw)` cell was compared for **exact equality** on `att_horizon`,
`horizon_vehicle_count` **and** `episode_reward` before any baseline number existed; the aggregate
lines above are a second, independent check computed from the re-rolled records. **The re-rolled
records — not the cited ones — are what `p4_4_baselines.json` reports**, so every number in this
packet is first-hand.

### 4.2 The declared budget, and confirmation the reported models are those checkpoints

**40,000 gradient steps**, declared in `docs/plans/p4.4.md` §3.1 (commit `3cf3344`) **before the
first gradient step** and matching the DT's *reported* budget, with **no raise available**. All 15
runs record `gradient_steps = 40000`; `load_baseline_checkpoint` refuses any other count
(mutation-proved, M20). The plateau criterion is recorded as a diagnostic only: it fails on 5/5 BC
seeds and 5/5 IQL seeds and holds on 4/5 %BC seeds. **The DT was also reported un-plateaued (0/5)**,
so all four arms are reported under the same rule.

Batch sizes as declared: BC and %BC 64 windows (identical to the DT's, and for the same seed BC
consumes the **identical batch index sequence** the DT consumed); IQL 1280 transitions, equalising
`(s, a)` pairs per gradient step. As the ruling asked me to say: IQL's 1280 transitions are
**independent** while the DT's and BC's 1280 positions are correlated within 64 episodes, so this
equalisation slightly favours IQL — the conservative direction for our claim, and IQL won.

### 4.3 Was each baseline tuned, and with what budget (§6.3)

**None of the three was tuned. All three are reported as untuned**, and so was the DT:

| arm | tuning budget | what fixed the configuration |
|---|---|---|
| BC | **zero sweeps** | the DT's own optimiser recipe and batch; architecture = the DT's stack minus attention |
| %BC | **zero sweeps** | identical to BC; only the training subset differs |
| IQL | **zero sweeps** | published D4RL-locomotion values (τ 0.7, β 3.0, γ 0.99, Polyak 0.005, clip 100) + IQL's own published reward normalisation `1000/(max−min)` = **0.23646252069047055**, fitted on the training split |
| MADT (P4) | **zero sweeps** | published DT-Gym configuration |
| MaxPressure | **none exists** | no parameters to tune |

### 4.4 The IQL sweep decision, made before the first gradient step (§9.2)

**No sweep.** Written into `docs/plans/p4.4.md` §3.5 at commit `3cf3344`, with a reason that is
analytic rather than budgetary and that does not depend on any P4.4 number:

1. **β appears in no value loss at all**, so the authorised criterion (training-split expectile/TD
   loss) is *exactly insensitive* to it.
2. **Selecting β on the policy loss is provably degenerate**: the AWR weights shrink toward 1 as
   β → 0, so the loss falls monotonically and the criterion picks the smallest β in any grid — at
   which point IQL's policy extraction **is** plain BC, and the sweep collapses the arm into the one
   it exists to be compared against.
3. **τ defines the expectile loss**, so two τ values are values of two different objectives;
   substituting Q's TD error is worse, because a smaller τ makes V easier to fit and biases the
   criterion toward the SARSA-like regime IQL exists to escape.

**What shipped instead of a sweep**, computed on training data only and therefore selecting nothing
— the degeneracy diagnostic, which shows the transplant did **not** degenerate:

| diagnostic (5 seeds) | value |
|---|---|
| AWR weight median | 0.009 – 0.077 |
| AWR weight q90 | 31.5 – 100.0 |
| fraction clipped at 100 | 7.7 % – 11.4 % |
| fraction below 0.01 | 37.8 % – 50.5 % |
| effective sample size | 11.3 % – 17.0 % |
| advantage std | 2.78 – 2.92 |
| agreement with the behaviour action | **94.6 % – 95.0 %** |

Against BC's **99.75 % – 99.84 %** and %BC's **99.84 % – 99.87 %**: IQL departs from the behaviour
policy on about 5 % of states, and those departures are what buy its 2.11 ATT over MAPPO@1000.

### 4.5 The registered forecasts, scored mechanically

| forecast (registered in `docs/plans/p4.4.md` §4, before training) | outcome |
|---|---|
| **Primary:** BC lands within δ = 0.6263 of the DT on the paired held-out mean | **HELD** (−0.2077) |
| *Secondary, labelled:* %BC lands within δ of BC | **FAILED** (2.0008) |

The primary forecast's reasoning was the DT's final training cross-entropy of 0.013–0.024, i.e. it
had nearly memorised the behaviour policy. BC's final loss is **0.0139–0.0189** — the same regime —
and BC reproduces the logged action on **99.8 %** of training positions. **The confirmed prior is
itself the finding:** the DT's held-out margin over its behaviour policy is not explained by better
imitation, because BC imitates at least as well and lands in the same place.

### 4.6 Determinism, claimed on weights and proved by canonical digest

`DEFERRED` 29 shipped and is used for every determinism claim here. Retraining seed 101 of **BC** and
of **IQL** from scratch on the reporting device (CUDA):

```
bc  seed 101  canonical digest be4be9745a936e90...  == reported  -> MATCH
              file sha256      cc9468763954d7ba... vs 6bb158865a7b...  -> differ, as they must
              final loss 0.01390801 == 0.01390801
iql seed 101  canonical digest b2725a297b583b95...  == reported  -> MATCH
              file sha256      5c1c55d52e03fcca... vs 64cb9516d3e5...  -> differ, as they must
              final loss 0.93346500 == 0.93346500
```
The file hashes differ **because the filenames differ** — the structural fact DEFERRED 29 exists
for, demonstrated rather than asserted. All 15 reported checkpoints carry distinct canonical digests
and their file sha256 beside them.

The whole reported artifact also regenerates **bit-identically**: re-running `report` from the same
inputs produced sha256 `e887f298677f38e7878103c7e2c274e1c759ad2c406e776de480695b81163de6`, equal to
the committed file.

### 4.7 Independent recomputation of every reported number

All 2600 per-episode records were re-aggregated by a second route (own per-draw means with
`math.fsum`, own CI, own signed-rank statistic computed by **counting** ranks rather than
`argsort`), with no import of the module under test:

| quantity | agreement |
|---|---|
| six cell means, six CIs, six vehicle-count means | ≤ **1.4e−14** (largest: one cell's float summation order) |
| three paired mean differences | ≤ 2.7e−15 |
| three CI widths | ≤ 1.3e−15 |
| three `W+`/`W−` pairs | **exact** (2040/3010, 4964/86, 4789/261) |
| three rank-biserial values | **exact** |
| three recovered fractions | ≤ 6.5e−14 |
| three verdicts | **identical** |
| Gate A, all 1100 cells against P4's committed records | **0 mismatches** |

The recovered fraction is additionally double-computed *inside* the artifact by the identity
`1 − mean(arm − DT)/δ`, and the builder **refuses to emit** if the two routes disagree by more than
1e−9 (that would mean the seed-by-draw design is unbalanced).

---

## 5. Self-review checklist (Y/N — an honest N is worth more than a false Y)

- Contracts in `docs/CONTRACTS.md` honored, including the non-standard env API? **Y** — every
  rollout goes through `dt_gate.evaluate_arm` → `horizon_rollout`, which uses `reset(seed=)`
  returning info only and `step()` returning reward first; actions are ordered by
  `[ix.id for ix in env.intersections]` and masking is mandatory.
- All tests actually executed and green? **Y** — output pasted in §3, run in this session.
- Zero modifications to frozen files? **Y** — guard exits 0; the diff stat proves it.
- Zero new dependencies? **Y** — numpy/torch/stdlib only; the Wilcoxon is **reused** from
  `offline/dt_gate.py` rather than reimplemented, and scipy is still absent.
- Every number in this packet produced by a command run in this session? **Y.**
- Helpers reused rather than reimplemented (CLAUDE.md rule 5)? **Y** — `Utils.*`, and from the
  merged modules `evaluate_arm`, `env_settings_from_manifest`, `build_training_dataset`,
  `stack_dataset`, `mean_ci95`, `wilcoxon_signed_rank`, `_cell`, `_paired`, `write_json_atomic`,
  `runtime_provenance`, plus `action_loss` and `masked_action_logits` from `agent/DTAgent.py`.
- Critical quantities double-computed by an independent route? **Y** — §4.7.
- Did I change any test after seeing it fail? **N** — no assertion was weakened or edited. **Six
  tests were ADDED after the implementation** *(corrected 2026-08-12 from "four", finding F9)*, each
  disclosed in its own docstring and each backed by an executed mutation (§6.1).
- Is the reported model the declared checkpoint and not a better one? **Y** — 40,000 steps for all
  15 runs, and the loader refuses any other count (M20).
- Determinism proved on the actual reporting device? **Y** — §4.6, by canonical digest.
- Is every claim in the plan still true? **N** — one is corrected: the plan said each job would stay
  under ~30 minutes; Gate A's first attempt ran 25 minutes and then **deadlocked** (§6.2).

---

## 6. Deviations, disclosures and findings

### 6.1 Six tests written after the implementation, all disclosed in their own docstrings

> ⚠️ **CORRECTED 2026-08-12 (review finding F9): this section said FOUR and the number was wrong — it is SIX.** The two `merge_training_runs` tests below are also absent at the red commit `933eb54`; verified by name against `git show 933eb54:tests/...`, which carries 30 + 22 test definitions against 36 + 22 on the branch. The substance is unchanged — both shipped in the same commit as the code they guard — and the four entries below are left exactly as written, with the two missing ones added as 5 and 6.

1. `test_bc_training_applies_the_availability_mask_the_way_the_dt_does` — added because M18
   survived. Kills it on the fixture; on the real corpus the mutant is equivalent (all-`True`
   masks), which is stated in the test itself.
2. `test_the_checkpoint_directory_is_checked_before_the_first_gradient_step` — added because the
   original barrier test is **weaker than it looks**: `torch.save` creates no directory either, so
   a run that trained first and only then refused would satisfy it. The new one replaces the loss
   with a sentinel, so reaching training at all is the failure. (M19 fails both.)
3. `test_a_checkpoint_saved_at_another_step_count_cannot_be_evaluated` — the "no online model
   selection" guard, mutation-proved (M20).
4. `test_the_thread_pin_is_applied_by_the_cli_and_refuses_a_nonsense_count` — added after a real
   hang, not a hypothesis (§6.2). *(Superseded in the fix round: it tested the helper and not
   `main`, which is review finding F8 — see §12.)*
5. `test_a_second_training_run_keeps_the_first_ones_methods` — added with `merge_training_runs`.
6. `test_merging_two_training_runs_from_different_designs_is_refused` — likewise.

No existing assertion was changed. *(Corrected 2026-08-12: this paragraph said the merge tests were
"extended with new cases … and are red-first". They are not — they are entries 5 and 6 above, added
after the implementation in the same commit as it. The claim they were red-first was wrong and is
the whole of finding F9.)*

### 6.2 ⚠️ A REAL DEADLOCK IN THE EVALUATION PATH — and it is P4's path too

Gate A's first attempt **hung**: after roughly 600 rollouts the process stopped consuming CPU
entirely (`utime` frozen across a 5-second sample) with **all 32 OS threads parked in
`futex_do_wait`**, while wall clock advanced. It ran 25 minutes and would have run forever.

This is the liveness class `experiments/runner.py` pins against (P0.3-fix: "**the pin is a liveness
fix, not a performance fix**"), and **`offline/dt_gate.py` deliberately does not import that module**
— by design, since importing it pins the calling process as a global side effect — so nothing was
pinning this path. **P4 ran the same code unpinned on the same machine and did not hang**, which is
what makes this a latent hazard rather than a broken build.

Before adopting the fix I checked the thing that would have made it unusable — whether one thread
changes any number. **It does not**: six cells across MADT, MAPPO@1000 and MaxPressure reproduced
bit-identically at one thread, and then all 1100 did. It is also **~5× faster** (1.05 s/episode
against ~5 s). `pin_torch_threads` is applied in `main()` only, never at import, and
`torch_num_threads = 1` is recorded in every artifact this task emits.

**For the coordinator:** P4's Return Packet §6.4 records seed 505 training for **14,018 s against
202–356 s** for the other four, attributed to "almost certainly a laptop suspend". I cannot
diagnose that from here and I am not claiming it was this — that run was CUDA training, not CPU
rollout — but the same machine has now demonstrably produced an unbounded torch-thread hang, so the
suspend hypothesis is no longer the only one on the table.

### 6.3 One deviation from the brief, approved in advance, and its guard

**IQL's transitions are built with `offline.trajectory_logger.load_episode`**, not from
`TrajectoryWindowDataset` alone, because the loader structurally never yields observation row `T`
and that row is the final transition's bootstrap target. Approved as Q1 (brief §7). Episode
selection, the draw-split leakage guard and the normalisation statistics all stay in the loader.
Guarded by the loader cross-check **and** by the raw-`np.load` row-`T` test the §8.4 correction
required — and M3 shows only the second one has any power there.

### 6.4 One defect I found in my own code before committing it

`_run_report` originally built its "requested runs" list **from the very episodes it then checked**
for completeness, making the campaign-integrity assertion a tautology. It now derives the expected
set from the declared design (cited arms, `TRAINING_SEEDS`, `HELD_OUT_DRAWS`) and never from the
data under test. Found by re-reading the function, not by a test — which is itself worth recording.

### 6.5 Smaller notes

- The evaluation ran as four jobs (Gate A, then one per method) so no single job exceeded ~25
  minutes; **each ends with `assert_campaign_complete`**, and `report` re-asserts the union against
  the declared design. Training ran as two chunks (BC+%BC, then IQL) — 40,000 steps × 3 methods
  does not fit one job — which is why `merge_training_runs` exists and refuses to merge two
  artifacts that disagree on budget, draws or group.
- Total compute: training 2,939 s (BC 549, %BC 580, IQL 1,811); evaluation 2,600 rollouts at
  ≈1.05 s each.
- The demand behind the evaluation was verified rather than assumed: the 5 draws that overlap with
  the main checkout's independently materialised pool are **byte-identical** on `flow.json` and
  `routes.rou.xml`, and the five DT checkpoints match `p4_gate.json`'s recorded sha256 **5/5**
  (valid because their filenames were preserved — the 2026-08-11 ruling).
- `experiments.runner` is never imported. `final_completed` is never read (`episodes=1`, so P8.0's
  finding B2 cannot arise).

---

## 7. Conflicts found between brief and repo — the repo wins

1. **"Do not write a second loader" vs "IQL must bootstrap through truncation" (§3.1 vs §3.2).**
   The repo settles it: `offline/dataset.py`'s docstring says "observation row T is never an
   input", so the two instructions cannot both be honoured through the window dataset. Raised
   before writing code, ruled in §7 Q1, implemented as §6.3 describes. **The repo won.**
2. **The brief's §5 "541 baseline"** matches this worktree exactly (`540 passed, 1 skipped`). No
   conflict; recorded because I checked rather than assumed.
3. **A6 names three verdict branches; four are arithmetically possible.** Two of my three
   comparisons landed in the fourth (CI entirely **above** +δ). I report it as
   `baseline_genuinely_better` rather than folding it into "inconclusive", which A6's literal
   wording would do — that would report a decisive result in the other direction as an absence of
   one. Flagged in the plan **before** any number existed, and it is open question #1.
4. **No venv in this worktree.** The interpreter is `/home/filip/rltraffic/.venv/bin/python` and
   the corpus is `/home/filip/rltraffic/datasets_v11`; CLAUDE.md §5's `.venv/bin/pytest` is not
   literally runnable here. Same as P4.

---

## 8. Limitations of what I shipped

1. **The comparison is one scenario, one backend, one intersection, one tier** — `cf_hz1x1`,
   CityFlow, `mappo1000`. Expert-only data has the narrowest state coverage in the ladder; nothing
   here bears on off-distribution behaviour, and nothing here is a ladder result.
2. **DT − baseline is a combined difference** (§0). No decomposition is offered or implied.
3. **The reported models have not converged** on the declared budget: the plateau criterion fails
   5/5 for BC and IQL and holds 4/5 for %BC. The DT was reported the same way (0/5). A longer budget
   could move any of these numbers; the registered design forbids me from choosing a better point.
4. **IQL is untuned by design and by ruling.** Its *winning* is not weakened by that, but no claim
   about IQL-in-general follows from a single untuned configuration, in either direction.
5. **%BC's advantage may be a filter effect on a narrow tier.** The top-10 % slice is 20 streams of
   200 spanning −5762 to −6419, and %BC trains 40,000 steps on 7,200 windows — 355 epochs. It
   reaches 0.001 training loss and 4/5 plateau, so some of its margin may be memorisation of an
   easier subset rather than a return-filter effect that generalises. **I did not test that**; a
   %BC-at-other-fractions sweep would, and it is out of this task's fence.
6. **The CIs over 500 episodes pool two variance sources** (5 seeds × 100 draws). Per-seed means are
   in §9 so the seed component is visible; the paired tests correctly use per-draw means.
7. **Action masking is untested as a capability** on this corpus (all-`True`), and M18 documents
   that BC's training mask is an equivalent mutant here.
8. **One thread is now the reported configuration** for evaluation. It reproduces P4's 16-thread
   numbers exactly on 1100 cells, so nothing is at risk today, but a future comparison against a
   16-thread number should re-check rather than assume.

---

## 9. Per-seed means (so the seed component is visible)

| arm | 101 | 202 | 303 | 404 | 505 |
|---|---|---|---|---|---|
| madt | 105.4091 | 104.9929 | 104.9024 | 104.4499 | 105.0246 |
| bc | 105.1239 | 105.2574 | 104.7173 | 105.7108 | 105.0080 |
| bc_top10 | 103.3389 | 103.0586 | 103.2399 | 103.3322 | 102.8439 |
| iql | 103.4743 | 103.3362 | 103.7252 | 103.2281 | 103.6182 |

Every %BC and IQL seed beats every MADT seed; BC's seeds interleave with the DT's, which is what
"matches" looks like at the seed level.

---

## 10. Open questions for the coordinator

1. **A6's fourth branch.** Two of three comparisons landed in a branch A6 does not name (CI entirely
   above +δ). I report it separately as `baseline_genuinely_better`. Does A6 want amending to name
   it, given it is now the *observed* outcome rather than a hypothetical?
2. **What the paper does with this.** `PREREGISTRATION.md` §10 registers what to publish if BC
   *matches*. It does not register the case where **%BC and IQL beat the DT**. The registered pivot
   ("weight moves to the ladder, shift and calibration findings") still applies, but the sentence
   the paper writes is stronger than the registered one and is yours to fix, not mine.
3. **Is P4.3 still the right next task?** Its motive was that the DT sits outside its training RTG
   support 20.8 % of the time and that the declared target is not the best one (106.46 vs 102.05 at
   target 0). That now reads differently: **RTG calibration would be tuning the one arm that is
   currently third of four.** A calibrated DT beating %BC would be a real result; a calibrated DT
   still losing would be a stronger one.
4. **The deadlock (§6.2).** Should `offline/dt_gate.py` get the same pin, so P4's own path cannot
   hang for the next task that re-runs it? That is a change to a merged module and is not mine.
5. **`%BC`'s fraction is a single point.** 10 % is the registered value; the result would be much
   better characterised by 5/10/25/50 %. Worth a follow-up task?
6. **§6 checkbox.** P4.4's box in `PROJECT_PLAN.md` §6 is still `[ ]`. Per the binding rule it is
   ticked **in the merge commit**, which is yours; I deliberately did not tick it on an unmerged
   branch.
7. **The independent review is yours** (brief §7 Q9). I did not commission one.

---

## 11. What the next task will assume about this one

- `agent.OfflineBaselines` exposes `BCAgent`, `IQLAgent`, `MLPTrunk`, `TrunkConfig` and
  **`canonical_state_dict_digest`** — the last is `DEFERRED` 29 and **every future reproducibility
  claim in this project should use it instead of a file hash.**
- `offline.offline_baselines` exposes reusable pieces: `paired_comparison` (Wilcoxon + rank-biserial
  + CI width + wins/losses/ties), `equivalence_verdict` (A6's branches, including the fourth),
  `recovered_fraction`, `assert_campaign_complete`, `merge_training_runs`, `pin_torch_threads`,
  `build_transitions` and `top_return_streams`.
- **`docs/data/p4_4_baselines.json` carries all 2600 per-episode records** on the same 100 held-out
  draws as `p4_gate.json`, so P5 and P4.3 can pair against BC, %BC and IQL without re-running them.
- **The baselines to beat on `cf_hz1x1` are now 103.1627 (%BC) and 103.4764 (IQL), not 104.9558.**
  Any later claim that a model "exceeds its behaviour policy" must be read against those.
- The 100 held-out draws are materialised in this worktree's gitignored `scenarios/draws/` and are
  byte-identical to the main checkout's pool on the 5 overlapping draws.

---

## 12. Review-fix round (BRIEF_12, 2026-08-12)

`docs/reviews/P4.4.md` returned **PASS-WITH-NOTES**: the reviewer retrained 3 of the 15
checkpoints, re-rolled 16 cells, rebuilt the 72,000-row transition table by a route sharing no
code with mine, and regenerated `p4_4_baselines.json` byte-identically — **and could not move a
single reported number.** This round fixes what it did find: a coverage claim on disk that was
false, and an arm with no test power over its own learning rule.

**No reported number changed.** Proved twice, below.

### 12.1 What this round shipped

| finding | what changed | tests | mutation executed |
|---|---|---|---|
| **F1** | the false coverage claim corrected at **both** sites (`iql_targets`, `TransitionTable.select`) — the guarantee is relocated to the call site and the docstring now names the test that provides it | 1 | 2 |
| **F2a–d** | no source change: the learning rule was correct | 3 (+F2c shares F1's) | 4 |
| **F3** | `top_return_composition` + a `compose` subcommand; the block recorded in `p4_4_training.json` | 6 | 3 |
| **F4** | no source change | 1 | 1 |
| **F7** | the promised assertion added (`DELTA_PROXIMITY_TOLERANCE = 1e-3`) | 2 | 1 |
| **F8** | no source change | 1 | 1 |
| **F9** | packet §6.1 corrected in place, dated | — | — |
| | | **14** | **11** |

### 12.2 These are characterisation tests, and that is a weaker guarantee than red-first

**Stated in my own words rather than borrowed from §6.1, which is about the original round.** The
code under test here was already correct, so every one of these 14 tests passed the moment it was
written. **A passing test proves nothing about code that already works** — what it proves is that
the behaviour is *pinned*, and the only evidence for that is the mutation. So each one is delivered
with its named mutation applied to the source, run, and its failure message pasted. Eleven were run;
the three tests without one are `test_the_committed_comparisons_are_far_from_the_margin_so_the_guard_never_fired`,
`test_the_committed_composition_is_the_one_the_reported_filter_selected` (both assert facts about the
**committed artifacts**, so the thing that would falsify them is a changed artifact, not changed
code) and `test_the_composition_reports_concentration_and_the_correlation_it_rests_on` (covered by
the enumeration and identity mutations below it).

### 12.3 The eleven mutations, with the failures they produced

```
F1/F2c  gamma=IQL_GAMMA -> gamma=0.0 AT THE CALL SITE   (survived all 58 tests before this round)
  E  AssertionError: step 0: the loop passed gamma=0.0, so the declared discount is not the one
                     training used
F1      next_value zeroed at the call site
  E  AssertionError: step 0: every bootstrap value is zero, which is a terminal horizon by
                     another name
F2a     the V (expectile) loss reads the online q instead of q_target
  E  AssertionError: V=2.7182 tracks the ONLINE q (expectile 4.0768) rather than the frozen
                     target network (expectile 0.4717)
F2b     the tau asymmetry reversed
  E  AssertionError: V=-5.7010 does not lean toward the upper tail; tau=0.7 puts the fixed point
                     at +4.0 and the reversed asymmetry at -4.0
F2d     the AWR advantage sign flipped
  E  AssertionError: the extracted policy prefers the -10 action: logits [-18.42, 18.59]; the
                     advantage weighting is up-weighting the worse action
F4      explore=False -> explore=True in _baseline_factory
  E  AssertionError: the declared evaluation path is greedy, but the factory passed explore=True
F7      the delta-proximity assertion deleted
  E  Failed: DID NOT RAISE ValueError
F8      pin_torch_threads deleted from main()
  E  AssertionError: main() did not pin this process, so the evaluation path runs unpinned and
                     can deadlock as it did on 2026-08-11
F3      the enumeration counts strictly-greater instead of at-least
  E  assert 0.16666666666666666 == 0.3333333333333333 +- 3.3e-07
  E  assert 0.006030682999753447 == 0.0072253 +- 5.0e-09
F3      the block-identity cross-check against the manifest deleted
  E  Failed: DID NOT RAISE ValueError
F3      the three-point floor on the correlation removed
  E  Failed: DID NOT RAISE ValueError
```

All reverted; the source is byte-identical to its pre-mutation state (`diff -q`), and regenerating
the artifact after the surgery reproduces the working-tree file exactly.

### 12.4 The F2 instrument, and why it can fail for exactly one reason

One state, two transitions worth **+10** and **−10**, self-loops, `gamma` patched to 0 so the Q
fixed point **is** the reward. The τ-expectile of `{+10, −10}` is `20τ − 10`: **+4 at τ = 0.7 and
−4 reversed.** No corpus, no simulator, ~2 s, and the measured separations are:

| instrument | shipped code | mutant |
|---|---|---|
| F2a `V(s₀)` with Polyak 0 (target frozen) | **+0.4843**, against the frozen target's own expectile of **+0.4717** | **+2.7182** |
| F2b `V(s₀)` | **+2.5243** | **−5.7010** |
| F2d `argmax π` | **0** (the +10 action) | **1** (the −10 action) |

**F2a is behavioural rather than a call-spy, per §7.3.** Polyak is patched to 0 so `q_target` stays
at initialisation while `q` learns the rewards; the two then differ by an order of magnitude and V's
fixed point separates them. The assertion is that V sits closer to the frozen target's expectile
than to the online one — *"using the wrong network changes the answer"*, not *"the target network
was consulted"*.

**F1/F2c is a call-spy rather than behavioural, per §7.7, and the two are the same rule.** That
claim is **contractual** — is the declared bootstrap the one the training path takes? — so the
assertion is on what the loop passes. F4 is the same shape: `explore=False` **is** the declared
quantity, and the behavioural alternative would have been the wrong instrument, because BC
reproduces the logged action on 99.8 % of training positions and a near-deterministic policy can
make sampling and argmax agree. **Assert at the level the claim is made at.**

### 12.5 No reported number changed — stage 1

**Stage 1 as specified FAILED, I stopped, and the coordinator ruled the instrument at fault.**
Regenerating gave `5289e3ba…` against the committed `e887f298…`. Recursive comparison over the full
JSON, **18,170 leaf paths: 0 removed, 0 added, exactly 1 changed** —

```
CHANGED .runtime.git_commit: 'c13aaa983b9da2ad86d3288748e0fef08c736f63'
                          -> '13732f3afc92b677cc348b06e5052ff2e8103d33'
```

`runtime_provenance()` records `git rev-parse HEAD`, so the file's hash is a function of **when and
where** it was written. Byte-identity is therefore unavailable to any artifact that records its own
writing commit on a branch that has moved — **the same structural class as `DEFERRED` 29**, an
identity claim resting on a container property rather than on content.

**The corrected instrument, accepted in §7.6, does isolate what stage 1 existed to isolate:**

```
pre-fix module (git show 13732f3:offline/offline_baselines.py), same cwd, same HEAD, same inputs
  -> 5289e3ba91d152a5d8c0c20cbc73604bf747445a69870f0af7d83f960a54c21f
post-fix module, same cwd, same HEAD, same inputs
  -> 5289e3ba91d152a5d8c0c20cbc73604bf747445a69870f0af7d83f960a54c21f
BYTE-IDENTICAL -- this round's source edits move nothing
```
The pre-fix module was verified in use (`ob.__file__`) and verified to lack this round's new
constant, so the swap was real.

### 12.6 No reported number changed — stage 2, against a list declared before it ran

The declared expected-difference set, written into `/tmp/stage2_compare.py` **before** the
comparison was run, with the comparison failing on anything not in it:

```python
EXPECTED_CHANGED: set[str] = {".runtime.git_commit"}
EXPECTED_ADDED_PREFIX: str = ".training.top_return_filter.composition"
EXPECTED_REMOVED: set[str] = set()
```

**Result — `p4_4_baselines.json`:**
```
paths: 18170 -> 18208   removed 0  added 38  changed 1
  declared change  .runtime.git_commit: 'c13aaa98…' -> '13732f3a…'
  added under .training.top_return_filter.composition: 38
STAGE 2 PASS
```

**Result — `p4_4_training.json`:** `899 -> 937 paths, 0 removed, 38 added, 0 changed`. Its own
`runtime.git_commit` is **preserved at `c13aaa9`**, because `compose` patches the record rather than
regenerating it.

⚠️ **Disclosed, because the order is what makes a declaration a specification.** The declared list
was correct as a **set** and wrong as a **spelling** for the second document: the training artifact
nests the same block at `.top_return_filter.composition`, one level higher than the baselines
artifact's `.training.top_return_filter.composition`, so the first run of comparison A reported all
38 additions as undeclared. Before re-running with the corrected prefix I verified mechanically that
nothing else was going on: **the training document's 38 added paths, lifted under `.training`, are
the same 38 keys as the baselines document's, with equal values at every one.** So a path spelling
was corrected; no difference was discovered afterwards and then excused.

**The sample behind "only `git_commit` differs".** All 8 fields of the `runtime` block were compared
by the recursive walk, on **this machine, in this session**: `cuda_available`, `cuda_device_name`
(`NVIDIA GeForce RTX 5080 Laptop GPU`), `numpy_version` (2.5.1), `python_version` (3.12.13),
`torch_cuda_version` (12.8), `torch_num_threads` (1) and `torch_version` (2.11.0+cu128) were
**equal**; `git_commit` differed. Four of those seven would differ on other hardware or in another
environment, so the claim is scoped to this one.

**Artifact hashes, old and new:**

| file | at `13732f3` | now |
|---|---|---|
| `p4_4_baselines.json` | `e887f298677f38e7…` | `af8ad2bdb5263d60…` |
| `p4_4_training.json` | `f38ea9ef45662c77…` | `9f2c792d6ec2225f…` |

### 12.7 ⚠️ Which commit produced which measurement — measured, not remembered

`runtime.git_commit` records **the commit at which the file was WRITTEN, not the commit whose code
produced the numbers.** For a chunked campaign no single commit can express the latter, and this
campaign already demonstrates it. Read from the run artifacts themselves:

| input | `runtime.git_commit` | what it produced |
|---|---|---|
| `output/p4_4/gate_a.json` | **`738884b`** | Gate A's 1100 re-rolled episodes |
| `output/p4_4/eval_bc.json` | **`c13aaa9`** | BC's 500 episodes |
| `output/p4_4/eval_bc_top10.json` | **`c13aaa9`** | %BC's 500 episodes |
| `output/p4_4/eval_iql.json` | **`c13aaa9`** | IQL's 500 episodes |
| `docs/data/p4_4_training.json` | `c13aaa9` | the 15 checkpoints (written by `train`) |
| `docs/data/p4_4_baselines.json` | `13732f3` (was `c13aaa9`) | **nothing** — it is the assembly |

**So the regenerated `13732f3` in the reported artifact is the commit the file was written at.
There is no single commit whose code produced all the measurements**, and a reader who takes that
field for measurement provenance is wrong. The schema split that would fix it is `DEFERRED` 39 and
belongs to P4.3, which writes new artifacts and can adopt it cleanly.

### 12.8 F3 — what the top-return filter actually selected, as a result

**%BC's advantage is at least partly checkpoint selection, not episode filtering.** §8.5 of this
packet disclosed *"may partly be memorisation of an easier subset — I did not test that"*. It is now
tested, and the answer is **neither** option that sentence offered.

1. Per-seed **training** return ranks the five behaviour checkpoints
   `101 −6711.0, 202 −6751.8, 404 −7199.2, 505 −7293.2, 303 −7570.2`; per-seed **held-out** ATT
   ranks them `202 103.53, 101 103.61, 404 106.00, 505 106.98, 303 107.80`.
   **Pearson r = −0.9910, n = 5, on DISJOINT draw sets** (1–200 against 1000–1099), so it cannot be
   produced by shared draw difficulty.
2. The filter ranks streams **by training return, deterministically**. Given (1) it therefore
   selects the strongest checkpoints — **a consequence of the selection rule, not a coincidence
   needing a significance test.**
3. The observed composition is what (2) looks like from outside: `{202: 10, 101: 9, 505: 1,
   303: 0, 404: 0}` of the top 20. **Exact p = 0.007225300 under the multivariate hypergeometric
   null of drawing 20 of 200 streams uniformly without replacement from five blocks of 40,
   P(max block count ≥ 10)** — enumerated, so no RNG seed enters a reported quantity.
   Cross-checked by 20,000 permutations at seed 20260812: **0.007750 ± 0.000620**, 0.85 σ away.
   ⚠️ **Two routes under one null test the arithmetic and leave the null untested.**
4. D16 (seed identical to demand block) is what makes "seed" and "block" the same thing here.

⚠️ **The p-value tests CONCENTRATION — that some block holds ≥ 10 of the top 20 — and NOT that the
concentrated blocks are the best-performing ones.** The identity claim rests on (1) and (2).

**What this licenses:** *on this corpus the top-10 % return filter performed checkpoint selection,
so %BC's advantage over BC is at least partly an effect of training on the two strongest behaviour
policies rather than of filtering episode quality within a policy.*
**What it does not:** that return filtering does checkpoint selection in general — **n = 5 seeds,
one tier, one scenario, one backend; a correlation this strong over this few points is a strong hint
and a weak law.**

**This sharpens C1 rather than threatening it:** C1 says measured data quality decides the outcome,
and this is C1 operating one level finer than the ladder's tiers — **composition within a tier, at
checkpoint granularity, measured rather than asserted.** It is a better result than the margin it
explains.

**The decisive test is not run here and is not mine.** BC trained on seeds 101 + 202 only, against
%BC, is registered as **P4.5**; it needs a training run and §5 of the brief forbids one in this
round.

**Why patching a committed record was acceptable:** `compose` recomputes the kept streams and
**refuses** unless they are exactly the ones the training artifact records. Provoked with one
dataset directory instead of five it raises — *"the streams recomputed … are not the streams …
records as kept (4 against 20 …); this composition would describe a different run and is refused"* —
and the artifact was verified byte-unchanged afterwards. Everything is validated before the first
byte is written.

### 12.9 The test count, reconciled

**599 collected before this round → 613 now: +14, 0 removed.** My plan listed 7. Measured by name
against `git show 13732f3:tests/…`:

| item | planned | shipped | why the difference |
|---|---|---|---|
| F1 + F2a–d | 4 | **4** | F2c shares F1's test, as the brief allowed |
| F4 | 1 | **1** | |
| F7 | 1 | **2** | **split**: one proves the guard *fires*; the second proves it **never fired** on the committed result, so the guard cannot be silently load-bearing on a reported verdict |
| F8 | 1 | **1** | |
| F3 | *(not enumerated in the plan's table)* | **6** | the plan listed F3 under source changes and named "a test with a mutation" in prose without a count; it needed six — the enumeration against a hand-computed case, the permutation cross-check, the block-identity double-read, the composition itself, the correlation floor, and the committed-artifact check |
| | **7** | **14** | |

### 12.10 One test-setup defect my own guard caught

The first draft of `test_the_composition_reports_concentration_and_the_correlation_it_rests_on`
used **two** seed blocks, and `top_return_composition` refused it: *"only 2 block(s) have a held-out
ATT, which is too few to correlate"*. **The guard was right and my setup was wrong** — a correlation
over two points is a line. The test now uses three blocks of different sizes with an ATT that is
worse exactly where the training return is worse, so `r = −1.0` by construction. Recorded here
rather than quietly repaired.

### 12.11 Self-review for this round (Y/N)

- No reported number changed? **Y** — §12.5 and §12.6, two stages, recursive over every path and type.
- Every test's named mutation executed and pasted? **Y** — 11 of 14; the other 3 assert facts about
  committed artifacts and are named in §12.2.
- All mutations reverted and the source verified pristine? **Y** — `diff -q`, and the artifact
  regenerates identically afterwards.
- Suite green and count up? **Y** — **612 passed, 1 skipped** (613 collected) with corpus variables;
  **602 passed, 11 skipped** without. Up from 599.
- Guards clean? **Y** — `claude_guard.sh --frozen-only`, `check_test_hygiene.sh`, `check_english.sh`
  all exit 0.
- Frozen paths or merged-module edits? **N, none** — `git diff --stat 13732f3` touches five files:
  `offline/offline_baselines.py`, `tests/test_offline_baselines.py`, the two `docs/data/` artifacts
  and this packet.
- Anything from the do-not-fix list touched? **N** — F5, F6, F10, F11, F12 are untouched; F11's
  staleness and F12's missing `warmup_steps` are still true of the code.
- §6 checkbox ticked? **N** — deliberately; it is the coordinator's, in the merge commit.

### 12.12 Open questions from this round

1. **`git_commit` is under-specified for a chunked campaign** (§12.7). `DEFERRED` 39 is the forward
   fix; this packet's table is the disclosure that buys most of its value meanwhile.
2. **`p4_4_baselines.json` now carries a hash that differs from the one `docs/reviews/P4.4.md`
   quotes** (`e887f298…` → `af8ad2bd…`). §7.1 anticipated this and said the review would be
   annotated, dated, rather than rewritten.
3. **P4.5 is the decisive test of §12.8** and is registered, not run here.
4. **F13 stands and is yours:** the DT arm is prompted at a target P4's own review showed is not its
   best, so no DT-versus-baseline sentence should enter the paper until P4.3 has run.
