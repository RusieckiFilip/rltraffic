# RETURN PACKET — P1 (trajectory logger)
**Date:** 2026-07-26, amended 2026-07-27 after contract review · **Mode:** Claude Code · **Contracts:** v1.1

**Status:** DONE

**Branch + diff stat**

Measured with this packet excluded. The first version of this section quoted a whole-branch stat that
went stale the moment the packet was committed, because the packet is part of the diff it reports
(reviewer finding 11). Excluding it keeps the number stable under further packet edits:
```
task/p1-logger
$ git diff --stat main...HEAD -- . ':!docs/returns/P1.md'
 docs/plans/P1.md                | 234 ++++++++++++
 offline/__init__.py             |  13 +
 offline/collect.py              | 367 ++++++++++++++++++
 offline/trajectory_logger.py    | 771 ++++++++++++++++++++++++++++++++++++++
 tests/test_trajectory_logger.py | 802 ++++++++++++++++++++++++++++++++++++++++
 5 files changed, 2187 insertions(+)
```
All entries are new files; no frozen path appears in the diff (checked explicitly, see the
self-review checklist). Including this packet the branch is 6 files; run
`git diff --stat main...HEAD` for the whole-branch figure.

**Files produced**
- `docs/plans/P1.md` — the GATE-2-approved plan, committed before any implementation existed
- `offline/__init__.py` — package marker so `python -m offline.collect` imports
- `offline/trajectory_logger.py` — `TrajectoryLogger`, `Episode`, `IntersectionEpisode`, `load_episode`, `LoggerStateError`
- `offline/collect.py` — collection CLI + four-entry policy registry
- `tests/test_trajectory_logger.py` — 27 tests, `FakeTrafficEnv`, no simulator required

**Tests — REAL OUTPUT, not a summary you believe is true**
```
$ .venv/bin/pytest tests/test_trajectory_logger.py -q
...........................                                              [100%]
27 passed in 1.16s
```
```
$ .venv/bin/pytest -q
-- Docs: https://docs.pytest.org/en/stable/how-to/capture-warnings.html
177 passed, 9 warnings in 24.66s
```
```
$ bash scripts/claude_guard.sh --frozen-only ; echo "exit=$?"
exit=0
$ bash scripts/check_english.sh docs/returns/P1.md offline/ tests/test_trajectory_logger.py ; echo "exit=$?"
exit=0
$ .venv/bin/python -m offline.collect --help >/dev/null ; echo "exit=$?"
exit=0
```
The 9 warnings are pre-existing SUMO deprecation warnings from `utils/sumo_utils.py:56`,
unrelated to this task.

**Smoke run against a real simulator — CityFlow, 2026-07-27**
```
$ .venv/bin/python -m offline.collect --backend cityflow \
    --env-config configs/sim/cityflow1x1.json --policy maxpressure \
    --episodes 2 --base-seed 1000 --max-steps 30 --delta-time 10 --out-dir /tmp/p1_smoke
episode 1/2  seed=1000  steps=30  return=-502.000  -> ep000000_seed1000.npz
episode 2/2  seed=1001  steps=30  return=-502.000  -> ep000001_seed1001.npz
done: 2 episodes, 60 steps, mean return -502.000, manifest /tmp/p1_smoke/manifest.json
```
1x1 CityFlow, MaxPressure, 16 lanes, 8 phases, `T = 30`. Reloaded through `load_episode`:
31 observation rows / 30 decisions / 30 outcomes. Alignment checked on real data, both halves:
```
exact == on t+1 : True
exact == on t   : False   <- must be False
rows where the shift differs: 23 / 30
```
dtypes as C6 requires (`global_reward` float32, lane arrays int32, mask bool, action int64);
`terminated=False, truncated=True`; `flow_draw=-1`; `local_reward` all-NaN, correct because
`--local-reward-fn` defaulted to `None`. This closes the "no simulator was exercised" risk that the
first version of this packet recorded.

**Determinism finding — the most valuable output of this task**
The two episodes above have **identical `episode_sha256`** despite different engine seeds:
```
engine_seed: 1000 vs 1001
global_reward identical : True
lane counts  identical  : True
actions      identical  : True
state        identical  : True
```
Seeds 1000 and 1001 produced byte-identical trajectories; both returned exactly `-502.000`.
`configs/sim/cityflow1x1.json` drives demand from a flow file, so the fresh engine seed each
`reset()` draws changes nothing about vehicle arrivals, and MaxPressure is deterministic given
identical arrivals.

This is **not a defect** — it is the alarm brief §8 specified ("it turns the deterministic-demand
failure mode into an automatic alarm") firing as designed on its first real run, and it confirms the
Master chat's 2026-07-11 prediction empirically rather than by argument.

Consequences:
- On this scenario `engine_seed = base_seed + i` diversifies nothing. A 20-episode run would write
  20 copies of one trajectory and a manifest truthfully reporting 20 episodes.
- **P2.0 (flow randomiser) is blocking, not a refinement.** Without it there is no corpus, only a
  replicated single trajectory.
- P2.4's duplicate detector has a real corpus-killing case to catch, not a hypothetical one.
- It strengthens reviewer finding 3: with deterministic demand an overwritten episode file is
  *identical* to the one it replaces, so an `out_dir` collision corrupts a corpus **with no trace**.
  That is why the fix below refuses rather than warns.

**Post-review fixes applied (2026-07-27)**
Contract review returned PASS with 11 non-blocking findings. The Master chat ruled three blocking
and three fix-now; all six are applied, and the suite grew from 20 to 27 tests.
- **Finding 1 (blocking).** The one deliberate contract deviation had zero coverage: the fixture's
  intersection ids were already lexicographically sorted, so `ix_ids` passed under either
  convention. Ids are now `("ix_zulu", "ix_alpha")` in native order, a module-level assert pins that
  env order differs from sorted order, and `test_ix_ids_follow_env_order_not_sorted` asserts
  `ep.ix_ids == env order`, `ep.ix_ids != sorted(...)`, and the `ix{i}_action == action[i]` identity.
- **Finding 3 (blocking).** `TrajectoryLogger.__init__` now refuses a populated `out_dir` with
  `FileExistsError`; `--overwrite` is the explicit opt-in and removes only the previous run's
  `manifest.json` and `ep*.npz`. Verified end-to-end against CityFlow: the refusal leaves the earlier
  run intact, and `--overwrite` restarts the counter cleanly.
- **Finding 11 (blocking).** The diff stat above is corrected, and its *cause* removed rather than
  its symptom: the stat now excludes this packet, so it no longer reports a number that its own
  commit invalidates.
- **Finding 2.** `lane_vehicle_count`, `metrics` (with its column-to-`metric_keys` correspondence),
  `vehicle_count`, `sim_time`, `step`, `current_phase` and `time_in_phase` are now content-checked
  against recorded env output, not merely shape-checked. Three new tests.
- **Finding 7 (second half).** `total_global_reward` is summed with `dtype=np.float64`; float32
  accumulation costs ~1e-2 at 360 steps and that number may be quoted in the paper.
- **Finding 8.** `run_metadata` is `json.dumps`-checked in `__init__`, so bad metadata fails before
  any simulator time instead of after an `.npz` is already on disk.
- **Finding 6 (optional, applied).** The anti-degeneracy guard now asserts the mismatch *count*
  (`>= T-1`) rather than mere inequality, so it stays meaningful if the fake env's dynamics change.
- **Findings 9 and 10 (documentation only).** Both unreachable with today's backends; recorded in the
  module docstring under "Two caveats for P3" — the `avail_mask` all-True-on-empty inheritance from
  `Utils.extract_valid_actions`, and the potential `state` aliasing if a backend ever returns a live
  buffer instead of `.tolist()`.
- Also fixed: the `_require_lane_arrays` message suggested `--metrics queue_length`, but
  `queue_length` is a **reward** function, not a registered metric. It now names
  `average_travel_time` and says so explicitly.

Findings 4 and 5 were ruled not-now: the two GATE-2 strengthenings and most of `collect.py` remain
untested, and `_build_env_spec` bypasses `experiments/config.py`'s private `_validate_settings`.

**Self-review checklist** (Y/N — an honest N is worth more than a false Y)
- Contracts in `docs/CONTRACTS.md` honored, including the non-standard env API? **Y** — `reset(seed=)`
  returns info only; `step` returns reward first; the logger never calls `env.step` itself.
- All tests actually executed and green? **Y** — output pasted above, run in this session.
- Zero modifications to frozen files (proved by the diff stat above)? **Y** — `git diff --name-only
  main...HEAD` filtered against the frozen set from CLAUDE.md §1 returns nothing; guard exit 0.
- Zero new dependencies? **Y** — numpy + stdlib only in `offline/`; `git diff main...HEAD` touches no
  dependency manifest (0 lines).
- Every number in this packet produced by a command run in this session? **Y**

**Deviations from the brief** (each with a justification)
- **`ix_ids` is stored in `env.intersections` order, not sorted.** The brief said "sorted once from
  `[ix.id for ix in env.intersections]`", but C1 says the action vector is ordered by
  `env.intersections`. Sorting would break the identity `ix{i}_action == action[i]` and require a
  remap table at every write site. Raised at GATE 1; the Master chat ruled native order correct and
  the brief wrong. Stated explicitly in the module docstring.
- **`Utils.reward_for_intersection` deliberately not used** (rule-5 exception). Its fallback chain
  (`agent/utils/utils.py:69-75`) substitutes the *global* scalar reward when `payload["reward"]` is
  absent — exactly the case the format requires to be `np.nan`. Using it would fabricate
  per-intersection rewards for envs with no `local_reward_fn`. `local_reward` is read directly from
  `next_info["intersections"][id]["reward"]`, key-absence → NaN. Verified and approved at GATE 2.
- **Two strengthenings beyond the brief**, both approved at GATE 2: `format_version` is stored inside
  each `.npz` (not only in the manifest) so the P2.4 linter can hard-fail per episode as C6:123
  requires; and an episode whose `lane_ids` disagree with the run-level `lane_ids_sha256` raises at
  `on_reset` rather than being left for the linter.
- **Episode digests use explicit little-endian views** (`"<i8"` / `"<f4"`) before `.tobytes()`. Native
  byte order would make `episode_sha256` platform-dependent and the P2.4 duplicate detector would
  disagree across machines.
- **`collect.py` exposes more flags than the brief listed** (`--control-mode`, `--global-reward-fn`,
  `--local-reward-fn`, `--global-reward-weight`, `--state-features`, `--metrics`, `--thread-num`,
  `--device`, `--libsumo`, `--map-file`, `--person-file`). The brief requires `run_metadata` to record
  the state features, reward-fn names and phase-control mode, which is only honest if they are
  settable. Defaults come from `experiments.config.SETTING_DEFAULTS` so collector and harness cannot
  drift.
- **Empty-lane-set guard added to `collect.py`** (GATE-2 amendment 1): `_require_lane_arrays` hard-fails
  after the first `on_reset` when `len(lane_ids) == 0`, with a message naming `--metrics`. The guard is
  in the collector, not the logger — the logger records `L = 0` honestly, because refusing to log is a
  collection-policy decision, not a format decision.

**Limitations of the format as shipped**
- **Reward-agnosticism is narrower than "any reward".** It covers rewards computable from the stored
  lane counts plus the scenario topology the manifest references — queue-family and PressLight-family
  both qualify. A reward that reads per-intersection metrics from
  `info["intersections"][j]["metrics"]` (available at `envs/base_traffic_env.py:478` but **not** stored)
  would require format v1.1. Master-chat ruling 2026-07-26: keep v1.0 global-metrics-only, record the
  narrowing explicitly. It is stated in the module docstring's layout section as well as here.
- **On SUMO and MOSS the lane dicts are `{}` when the metrics pipeline is off**
  (`envs/sumo_env.py:301`, `envs/moss_env.py:653`); CityFlow falls back to a direct engine query. The
  collector-side guard above turns this into a hard failure at collection time instead of a silently
  useless corpus.

**Conflicts found between brief and repo** (repo wins — what did you implement instead?)
- Brief/C2 present `info["metrics"]` as unconditional; both the global and the per-intersection forms
  are gated on the metrics pipeline (`envs/cityflow_env.py:279`, `envs/base_traffic_env.py:477`).
  Implemented with `.get("metrics", {})`; `metric_keys` freezes as `sorted(...)` at `on_reset`
  (mirroring `agent/MAPPOAgent.py:629`), an empty freeze is legal, and a mid-episode change raises.
- Brief describes `avail_mask` as coming from `Utils.extract_valid_actions`, which actually returns a
  *list of indices*, not a mask (`agent/utils/utils.py:78,101`). The logger builds the `np.bool_` mask
  itself, width = that intersection's `n_actions`, `True` = legal.
- `Utils.infer_action_counts` returns a positional `list[int]` and needs `env.action_space`, which only
  exists after `_setup_spaces()` (`envs/base_traffic_env.py:309`, called from `reset()`). It is
  therefore called in `on_reset`, not `__init__`, and via `getattr(env, "action_space", None)` so the
  `ix.num_phases` fallback path stays reachable.
- C6 stores `time_in_phase` as float32; the source is an `int` (`envs/phase_control.py:176`). Cast on
  write, documented.
- C3 sketches `MaxPressureAgent` under the `BaseAgent` API; it is not a `BaseAgent` subclass and its
  `act` takes only `info` (`algorithms/max_pressure.py:11,24,28`). The registry therefore holds
  per-policy adapter factories rather than one uniform call.
- **Amendment-4 check:** both `CLAUDE.md` §0 (`CLAUDE.md:12`) and `scripts/check_english.sh` **exist**.
  Nothing was removed from the verification list and nothing was created to justify it.

**A test was fixed during Phase 3 — disclosed in full**
`test_lane_id_order_preserved` failed after implementation. Per CLAUDE.md §0 I stopped instead of
editing it. Diagnosis: the test re-derived expected lane counts by constructing a fresh
`FakeTrafficEnv` and setting `_step_count`, but `FakeTrafficEnv._waiting()` also depends on
`_last_action.sum()`, which was `0, 2, 2, 2, 1` across the real run — so from row 2 the replay computed
a demand the run never produced. The logger had stored exactly what the env emitted, correctly
reindexed to lexicographic lane order. The Master chat verified the diagnosis and approved the fix; the
test now compares against `env.emitted_lane_waiting`, the dicts the env actually emitted, which removes
the risk that test and logger share a formula bug. Exact `==`, the `lane_ids == SORTED_LANES`
assertion and the column-order check are all retained; an additional `ep.lane_ids != RAW_LANES`
assertion was added. No assertion was weakened.

**Open questions / risks for the Master chat**
- All three open questions from the plan were closed at GATE 2 and are recorded in the master plan:
  `terminated` hardcoded `False` (RTG at episode boundaries — P4), the `{}` lane dicts on SUMO/MOSS,
  and C2's wording on conditional `metrics`.
- `envs/base_traffic_env.py:604` hardcodes `terminated = False`; only `truncated` at `max_steps` ends an
  episode. Every corpus episode therefore carries `terminated == False`. Both scalars are recorded
  faithfully. **P4 must not use `terminated` to distinguish a genuine absorbing state from a time
  limit** — the module docstring states this.
- `local_reward` is COMPOSITE: `global_reward_weight * global_reward + local_reward_fn(local_metrics[j])`
  (`envs/base_traffic_env.py:204-219`, global term `:214`, local term `:217`). Downstream RTG code must
  not double-count the global part by summing `global_reward` and `local_reward`. Stated in the module
  docstring.
- Exercised against a real simulator on 2026-07-27 (see the smoke-run section above). Not exercised
  against SUMO or MOSS, and not with `--policy mappo` / a checkpoint; those paths remain unverified.

**What the next task will assume about this one** (phase-boundary check)
- `load_episode(path) -> Episode` is the reload contract; P3's dataset loader builds on it.
  Per-intersection data is keyed by real intersection id, `ix_ids` is in `env.intersections` order,
  `lane_ids` is lexicographic, `metric_keys` is sorted.
- Observations have `T+1` rows, decisions and outcomes `T`. `row t+1` is the post-step state that
  `r_t` was computed from.
- `episode_sha256` (manifest) is sha256 over the per-intersection `action` arrays in intersection
  order followed by `global_reward`, all viewed little-endian. P2.4's duplicate detector consumes it
  and must hard-fail on `format_version != "1.0"`, which `load_episode` already does.
- `flow_draw` is plumbed but always `None` in P1; the `-1` sentinel means absent in both the npz scalar
  and the manifest entry.
- The policy registry `POLICIES` in `offline/collect.py` has exactly four entries; P2.5 adds
  `fixedtime` and `dqn` there without touching the CLI.
- A populated `--out-dir` is refused unless `--overwrite` is passed. Any P2 script that re-collects
  must either use a fresh directory per run or pass the flag deliberately.

**NEW REQUIREMENT for P2, surfaced by the smoke run — not implemented here**
Corpus collection must run with **`--local-reward-fn queue_length` AND
`--global-reward-weight 0.0`**, so that `local_reward` is *purely* local. At the default weight
`1.0`, `envs/base_traffic_env.py:214` adds the full global reward into every intersection's
`local_reward`, so each per-intersection signal silently carries the global one — the composite
double-counting trap already documented in the module docstring. `global_reward` is computed by
`_get_reward()` and is unaffected by the weight, so setting the weight to zero yields **both** clean
signals in the same corpus: a purely local `local_reward` and an untouched `global_reward`. Collecting
at the default weight would produce a corpus in which the two are not separable after the fact.
