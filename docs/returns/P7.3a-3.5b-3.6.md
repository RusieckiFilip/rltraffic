# RETURN PACKET — P7.3a §3.5b + §3.6 (the cell runner, `report`, and the driver)

> **ADDENDUM, 2026-09-17 — Amendment H's fix round is at the bottom of this file (§14).** Read that
> section for the current state; everything above it describes the branch at `55498ab` and is left
> unedited, including the §1 claim about the push that H's opening paragraph correctly falsified.

**Date:** 2026-09-16 · **Mode:** Claude Code (Opus 4.6) · **Contracts:** v1.1 (C1, C2, C6, C8, C9)

**Status: DONE** for the scope of this session — `BRIEF_37` Amendment **E2**, **E3(a)–(f)** and
**§3.6**, with **G1–G4** applied. Nothing was run that produces a number: no token, no campaign, no
artifact in `docs/data/`. The pre-flight (F1's pilot), the stage-1 checkpoint and the token are next
and are not this session's.

---

## 1. Branch + diff stat

```
task/p7.3a-zero-shot   (pushed: origin/task/p7.3a-zero-shot, upstream set this session)

$ git diff --stat f6f57ec HEAD          # this session only, from the merge of main
 docs/plans/p7.3a_section_3_5b.md     |  369 ++++++++
 offline/aligned_env.py               |   87 +-
 offline/campaigns/p7_3a_zero_shot.sh |  299 +++++++
 offline/transfer_curve.py            | 1603 ++++++++++++++++++++++++++++++++++
 tests/test_transfer_curve.py         | 1468 +++++++++++++++++++++++++++++++
 5 files changed, 3806 insertions(+), 20 deletions(-)

$ git diff --stat main HEAD | tail -1   # the whole branch
 15 files changed, 5993 insertions(+), 57 deletions(-)

$ bash scripts/claude_guard.sh --frozen-only ; echo "exit=$?"
exit=0
```

Commits this session, in order:

| sha | what |
|---|---|
| `f6f57ec` | `git merge main` (G9/E6/C9) — ort, 3 files, 0 conflict hunks |
| `1641198` | §3.5b: the loader, the pins, the cell runner, chunks, `report`; plan file (G8) |
| `4575d67` | §3.6: the driver, the `a17f` CLI stage, T9 |

`git push -u origin task/p7.3a-zero-shot` — new branch on origin, upstream set (E6 closed).

## 2. Files produced

- `docs/plans/p7.3a_section_3_5b.md` — the §3.5b plan, approved at `61783e2` (Amendment G), with
  the three conflicts I raised and how each was ruled, including the one I got wrong.
- `offline/transfer_curve.py` (+1,603) — `load_calibration`, `checkpoint_identity`,
  `demand_identity`, `halting_check_for`, `env_for_cell`, `assert_env_matches_cell`, `dt_choose`,
  `anchor_choose`, `att_env_from_info`, `_StepTap`, `validate_cell_payload`, `run_cell`,
  `chunk_path`/`write_chunk`/`chunk_is_reusable`/`reusable_chunk_at`/`move_aside`, `report` and its
  blocks, `run_stage` + `_worker` (the spawn pool), `build_parser`/`main` with
  `cells | report | a17f | canary | record-canary`.
- `offline/aligned_env.py` (+87/−20) — **G3**: `observer_env_for_draw` (unwrapped) extracted;
  `aligned_observer_env_for_draw` is now that plus the wrap. One construction path, no mirror.
- `offline/campaigns/p7_3a_zero_shot.sh` (new, 299) — the driver, stage as an argument.
- `tests/test_transfer_curve.py` (+1,468, 62 tests total in the file).

## 3. Tests — real output

```
$ .venv/bin/python -m pytest tests/test_transfer_curve.py -q
............................................................ [100%]
62 passed, 1 warning in 4.84s

$ .venv/bin/python -m pytest -q          # whole suite, from the worktree
2037 passed, 94 skipped, 32 warnings in 182.71s (0:03:02)

$ bash scripts/check_test_hygiene.sh tests/test_transfer_curve.py ; echo exit=$?
exit=0
$ bash scripts/check_english.sh offline/transfer_curve.py tests/test_transfer_curve.py \
      offline/campaigns/p7_3a_zero_shot.sh docs/plans/p7.3a_section_3_5b.md ; echo exit=$?
exit=0
```

The red run before implementation (signature-only skeleton, so every test reached the real API
surface rather than one shared `ModuleNotFoundError`): **40 failed, 9 passed**, the failures spread
across 11 distinct `NotImplementedError` sites plus `AttributeError` on `P7_2B_CALIBRATION_SHA256`
and on the not-yet-existing `aligned_env.observer_env_for_draw`. T9's 7 tests were red separately
with `FileNotFoundError` on the driver path.

**T5 runs one real SUMO episode** (20 decisions, `dt_seed101`, draw 5) — the traci deprecation
warning in the run above is attributed to it, which is how I know it did not skip.

## 4. Mutation testing — 27 mutants, 27 killed (3 of them only after work)

Module (17), each applied by script, diff printed, tree restored and verified byte-identical to the
backup afterwards:

| # | mutation | verdict |
|---|---|---|
| M1 | `HALTING_CHECK_DRAW = 1001` | KILLED (E2's test — this is the coordinator's survivor) |
| M2 | loader's digest comparison disabled | KILLED |
| M3 | full digest → 8-char prefix | **survived first**, then KILLED (see below) |
| M4 | `mix50` lookup without `method` | KILLED |
| M5 | `routes_sha256` dropped from the demand check | KILLED |
| M6 | C8's anchor-through-the-door refusal disabled | KILLED |
| M7 | every cell built through the aligned factory | KILLED |
| M8 | `halting_check_for` → always `True` | KILLED |
| M9 | `explore=True` | KILLED (kwargs spy + AST test) |
| M10 | `att_env_from_info` stops refusing a missing key | KILLED |
| M11 | `decisions` hardcoded to 360 | KILLED |
| M12 | `_write_json` moved above the fence scan | KILLED |
| M13 | B2's stage-1 row comparison disabled | KILLED |
| M14 | rho falls back on any draw's anchors | KILLED |
| M15 | `aligned_observer_env_for_draw` stops delegating (G3) | KILLED (`tests/test_aligned_env.py`) |
| M16 | rho's anchors swapped | KILLED |
| M17 | seed mean → first seed | **survived first**, then KILLED (see below) |

Driver (10): token before the canary · `tee` without `-a` · trap moved off INT/TERM · A17(f) deleted
· worker count hardcoded · a log truncated instead of appended · `record-canary` removed · manifest
written non-atomically · the stage-1 citation dropped · a root hardcoded in a command. **All 10
KILLED.**

**The three that needed work, because these are the ones worth reading:**

1. **M17 exposed a real test gap.** Replacing the per-draw seed mean with "the first seed" passed
   every assertion I had written, because my fixture had **one seed per cell** and the mean over one
   value is the identity. The paired unit — *"the per-draw unit is the mean over training seeds, as
   in P4"* — was therefore untested. The fixture now carries **two** seeds whose ρ differ (0.5/0.25
   on draw 1000, 0.75/0.5 on draw 1001, all exact binary fractions), the test recomputes the
   per-draw means and the arm mean by an independent route, and the mutant dies.
2. **M3 is the one I could not kill head-on.** Comparing `digest[:8]` instead of the whole digest
   survived, because in that test the two digests differ from their first character — only a crafted
   32-bit prefix collision (~4·10⁹ hashes) separates the two versions. Instead of weakening the
   claim I inverted the mutation: a **committed record holding a truncated digest** is cheap to
   build, is exactly what a malformed record looks like, and the prefix mutant *accepts* it while
   the delivered code refuses. New test, mutant killed.
3. **M15's first "SURVIVED" was my error, not a finding.** I ran it with `-k` selecting only
   `tests/test_transfer_curve.py`, where the aligned factory is monkeypatched and the mutated line
   never executes. Re-run against `tests/test_aligned_env.py` it dies immediately. Recorded because
   a mutation verdict is only as good as the selector, and I nearly reported a false survivor.

## 5. Self-review checklist

- Contracts honoured, including the non-standard env API (`reset` → info only; `step` → reward
  first)? **Y** — `_StepTap` forwards exactly that shape and nothing else.
- All tests actually executed and green? **Y** — output pasted above, run in this session.
- Zero modifications to frozen files? **Y** — guard exit 0; the only files touched are `offline/**`,
  `tests/**`, `docs/**`.
- Zero new dependencies? **Y** — stdlib + numpy + torch (via existing modules) only.
- Every number in this packet produced by a command run in this session? **Y**, with one explicit
  exception: the **schedule figures in the driver header** are quoted from
  `docs/plans/p7.3a_amendment_a_measurements.md` §A6 (2026-09-16, n = 16 cells), read from disk this
  session but **measured in a previous one**. The header says so, and says they are the *harness*
  pilot's, not F1's pre-flight pilot's.
- Tests written before the implementation and red for their own reasons? **Y** — counts above.
- Did I edit a test to make it pass? **Y, once — disclosed in full in §7.**

## 6. Deviations from the brief, each with its justification

1. **`report` takes a `cells=` parameter** the brief does not mention. Without it a unit test would
   need 4,700 chunks to satisfy the completeness check. The risk it creates (a caller narrowing the
   declaration) is closed mechanically: when `cells` is supplied, `report` **refuses to write into
   `docs/data/`**, and the artifact records `cell_set_source`. The campaign path passes nothing and
   gets `declared_cells(stage)`. Tested both ways.
2. **`anchor_choose` returns `(choose, diagnostics)`**, not a bare callable. I wrote the bare version
   first and it forced `decisions` to be taken from the declared horizon — which would have made
   `validate_cell_payload`'s `decisions == 360` check a tautology. Anchors now count their own
   decisions. (Mutant M11 exists because of this.)
3. **One canary per *stage*, not per campaign.** P7.2b's `report` refuses chunks carrying more than
   one `canary_seconds`. B1 runs two stages under two tokens and re-runs the canary at each start,
   so that rule would refuse every valid two-stage campaign. `report` records
   `chunk_canaries_by_stage` and refuses a chunk with **no** canary; the artifact states why it
   differs from P7.2b.
4. **`a17f` CLI added to `offline.transfer_curve`** rather than to `transfer_calibration`, where the
   gate function lives. The driver had nothing to call (`assert_logged_corpus_matches_probe` has no
   CLI). I put it in P7.3a's own entry point to avoid touching the parser P7.2b's driver depends on.

## 7. The test I changed, in full, with the diagnosis

**`test_t9_a17f_stops_the_driver_before_any_evaluation_cell_runs`.** My first version located the
evaluation stage with `code.index(" cells ")`. That needle matched the **refusal message** *"cells
from another run are still alive"* at character 1,296 — 3,700 characters before the subcommand it
meant — so the ordering assertion failed against a driver whose order was correct
(`run_stage collect offline.collect` at 5,047 < `run_stage a17f` at 5,384). I verified that by
printing the three match positions before changing anything.

The needles are now the command forms (`run_stage collect offline.collect`,
`run_stage a17f offline.transfer_curve`, `cells --stage`), and the second assertion was replaced by
two precise ones: `fail "$label"` exists in `run_stage`, and `run_stage()` is defined before the
A17(f) call so the gate runs *through* it. **The driver was not changed to satisfy the test.** The
docstring records the false-match so the next person does not re-introduce it. Mutation MD4 (A17(f)
deleted) confirms the repaired test still kills what it is for.

Three other test edits, all *strengthening* and all before the first green run, listed for
completeness: the fake env in the `run_cell` test gained `_engine_seed` and `max_steps = 360` (it
must model the real env's contract); `assert len(h3["clauses"]) == 4` was added so a loop over an
empty list cannot pass vacuously; `("canary_seconds", None, "canary")` was added to the parametrised
refusal list.

## 8. Conflicts found between brief and repo (repo wins)

1. **E3(b)'s `SHA256SUMS_p4_6` pin does not exist** — raised at the plan gate, **ruled by G1**, and
   the ruling is now implemented: both subjects pinned against **committed** artifacts.
2. **G1's own lookup key was wrong, and I implemented the artifact instead.** G1 names the `mix50`
   row by `(tier, seed)`. `docs/data/p4_7_training.json` carries **20** `mix50` rows — `bc`,
   `bc_top10`, `iql`, `dt` × 5 seeds — so `(tier, seed)` matches **four**, three of them other
   methods' checkpoints. Implemented as `(tier, method="dt", seed)` with exactly one match required;
   `seed` is an `int` there, not the string G1 wrote. A test builds the three decoy rows explicitly.
3. **`BRIEF_37` §3.5's "the paired-CI helper *and its resampling seed*"** — the repo has no
   resampling estimator (plan §0.4, Amendment A3). The artifact reports `resampling_seed: null` and
   says why in the artifact itself rather than inventing one.

## 9. My own error, recorded because it is the same class the project keeps paying for

At the plan gate I reported that `git merge` and `git push` were **denied** to this session. They are
under **`ask`**. I had run `grep -n "push\|merge\|checkout" .claude/settings*.json`, seen the three
patterns at lines 25–27, and concluded "deny" **without checking which array they were in** — a
claim about a file's content, stated without opening the file, which is exactly the class of the
coordinator's own `SHA256SUMS_p4_6` error that the same plan caught. Re-read with a parser:
`[ask] n=8` holds all three; `[deny] n=17` holds none. G9 corrected it, and I ran both commands
myself with the author approving each prompt. Line numbers are not membership.

## 10. Limitations of what I shipped

1. **The pool has never been executed.** `run_stage`'s `spawn` `Pool` and `_worker` have no test that
   runs them: `spawn` re-imports the module, so a monkeypatched fake cannot reach a worker, and a
   real one costs a SUMO episode per cell. Tested without a simulator: the skip/resume decision (a
   complete work directory starts no worker) and `move_aside`. **First execution is F1's pre-flight
   pilot**, which is what F1 says the pilot is for. Until then, treat the pool as unproven.
2. **`run_cell` has never run end to end on a real episode.** T5 exercises its pieces on real SUMO
   (env factory, `dt_choose`, the real agent, the kwargs spy, the recorder, `att_env_from_info`) for
   20 decisions; the full-episode assembly is exercised only through fake seams. The pre-flight
   pilot closes this too.
3. **The `e_sumo`/`att_env` ρ agreement in the tests is fixture-constructed** (`e_sumo = att_env +
   20` on every cell). The tests prove the arithmetic and the pairing; they say nothing about what
   the two definitions will do on real data. That is the campaign's job.
4. **No stage-1 or final artifact exists.** `docs/data/p7_3a_zero_shot*.json` are not written.
5. **The driver header's schedule is the A6 harness pilot's**, not a pilot through this driver. The
   header states this in its own text and names what must replace it (F1's pilot, then F3's pad
   re-run if the canary moves more than 10 %).
6. `paired_comparison`'s Wilcoxon runs on **2 shared draws** in the tests. It does not crash and the
   block is well-formed; its statistical behaviour at n = 100 is untested here.

## 11. Open questions for the coordinator

1. **G1's lookup key** (§8.2): I implemented `(tier, method, seed)` against the artifact. Confirm, or
   tell me the `bc`/`iql` rows were meant.
2. **One canary per stage** (§6.3): confirm the departure from P7.2b's single-canary rule. It follows
   from B1 mechanically, but it changes a rule a previous amendment wrote down.
3. **`report --stage rest`** is not wired in the driver's final step: the `rest` run calls `report`
   over **all** cells with `--stage1-path`, which is what B2 describes. Confirm that the final
   artifact should cover the whole campaign rather than only stage 2's cells.
4. **The pre-flight pilot (F1)** is the first execution of the pool. Should I run it in the next
   session, or is that the reviewer's?

## 12. What the next task will assume about this one

- `offline.transfer_curve` exposes `cells`, `report`, `a17f`, `canary`, `record-canary`, and every
  root is a parameter with today's path as a default (§0.9); no new absolute path is buried in a call.
- A cell chunk's schema is fixed by `_PUBLISHED_FIELDS` plus the identity digests; **P7.3b's few-shot
  cell is the same cell with a different checkpoint**, which is what §3.5 asked the design to allow —
  `run_cell` takes the checkpoint through `checkpoint_identity`, so a fine-tuned checkpoint needs a
  committed digest record of its own before it can run.
- `declared_cells(stage)` is the only expectation `report` compares against, and the committed
  artifact cannot be produced from any other declaration.
- A17(f) is a CLI stage now, and the driver stops on it.

## 13. AI-assistance record (`CLAUDE.md` §8)

1. **Tool and version.** Claude Code, Opus 4.6, 2026-09-16.
2. **Authorship per file.** AI-authored: `offline/transfer_curve.py` (§3.5b additions),
   `offline/campaigns/p7_3a_zero_shot.sh`, `tests/test_transfer_curve.py` (additions),
   `docs/plans/p7.3a_section_3_5b.md`, this packet. AI-edited from a human-reviewed original:
   `offline/aligned_env.py` (G3's extraction; the existing docstrings were moved, not rewritten).
3. **Human verification performed.** The author approved the merge and push prompts. All test runs,
   the 27 mutations and the red-first runs were executed by the AI in this session and are pasted
   above; the coordinator has **not** yet re-run them. The checkpoint digests were recomputed from
   the ten `.pt` files and compared with `p4_gate.json` and `p4_7_training.json` in this session; the
   `p7_2b_calibration.json` digest was recomputed and equals the pin.
4. **Who made the research decisions.** None were made here. H3, ρ, the arms, the seeds, the pool and
   the stages are registered (`PREREGISTRATION` §2, §3.4, A15–A18) and were fixed before any number
   existed; the checkpoint-pin sources are **Amendment G1**; the halting subset is **C2**; the
   analytic CI is **A3**; the two stages are **B1/B2**; the anchors-unwrapped rule is **A2/C8**. The
   one judgement I made — the `(tier, method, seed)` lookup key — is a reading of an artifact against
   a ruling's prose, is flagged in §8.2, and is the coordinator's to confirm.

---

# 14. ADDENDUM — Amendment H fix round (2026-09-17)

**Status: DONE** for H1–H8 and H9's two handed-over test weaknesses. One commit, as instructed.
**No pilot** — H10 Q4 puts F1's pilot in the next session. Nothing here produces a number.

## 14.1 The push, corrected

H's opening paragraph is right and my §1 was misleading: `origin` was at `f6f57ec`, so the three
commits after the merge were **not** pushed. §1 said "pushed: origin/task/p7.3a-zero-shot, upstream
set this session" — true of the branch ref, false of those commits, and I did not re-check after the
last commit. This session began by merging `main` (`772329b` → merge commit) and pushing:
`f6f57ec..fdfcd8b`, and `git log origin/task/p7.3a-zero-shot..HEAD` is now empty before this commit.

## 14.2 What changed, by amendment item

| item | change |
|---|---|
| **H1** | the `a17f` CLI passes `draw_ids=range(201, 301)` explicitly, asserts `n_checked == n_matching == 100`, prints `A17(f) 100/100`, and calls `load_calibration` first (MIN-7) |
| **H2** | driver stage 1 branches on `[ -d "$CORPUS" ]`: gate-and-skip if the corpus exists, else collect-then-gate. `--overwrite` appears nowhere; a refused corpus is moved aside by hand |
| **H3** | T9 asserts `set -euo pipefail` and refuses `set -eu` |
| **H4** | the reviewer's one-shared-draw fixture, matching the guard's own words |
| **H5** | T5 re-derives `rtg[t-1] − rtg[t] == reward_series[t-1]` by raw arithmetic and by a cumulative sum; the helper is asserted afterwards as the weaker statement |
| **H6** | (a) both directions of `halting_checked` vs C2; (b) a tap whose final info differs from the rollout's sample |
| **H7** | `holds` → `point_estimate_satisfies`, plus `ci95_entirely_satisfies`; format version stays `1.0` |
| **H8** | MIN-1 order pinned by a spy; MIN-3 bound from `Utils.infer_action_counts` (+ `action_space_n` in the chunk); MIN-5 stage filtering by name with `n_chunks_outside_stage`; MIN-6 print-then-check |
| **H9** | `"rest" in code` (matched *restart*) → the case statement's own form; the mis-named anchors test renamed to say it reaches **completeness** |

## 14.3 H1's falsification, run against the artifact that produced it

The coordinator's evidence was the one-draw smoke corpus returning `n_checked: 1, all_match: true,
exit 0`. Re-run against the fixed CLI, same corpus, this session:

```
$ python -m offline.transfer_curve --data-dir docs/data a17f \
    --corpus-dir /home/filip/rltraffic/output/p7_3a_runs/collect_smoke
ValueError: the corpus has no logged episode for draw(s) [202, 203, ..., 300]
exit=1
```

## 14.4 Mutations — 17 run, 15 killed, 2 equivalent (and the equivalence proved, not asserted)

| mutation | verdict |
|---|---|
| H1 band dropped | survived alone → **see below** |
| H1 `100/100` assertion removed | survived alone → **see below** |
| H1 **both removed** | **KILLED** (99-draw test) |
| H1 artifact pin dropped (MIN-7) | KILLED |
| H2 skip-branch removed | KILLED |
| H2 `--overwrite` added | KILLED |
| H3 `pipefail` dropped | KILLED |
| H4 per-draw anchor guard removed | KILLED |
| H5 RTG double-counted | KILLED |
| H6(a) halting/C2 check removed | KILLED (both directions) |
| H6(b) two-route ATT comparison removed | KILLED |
| H7 interval fact computed from the mean | survived first → **KILLED** after the fixture was fixed |
| H8 MIN-1 shape check moved after the rollout | KILLED |
| H8 MIN-3 bound hardcoded to 8 | KILLED |
| H8 MIN-5 other-stage chunks refused again | KILLED |
| H8 MIN-5 outside-stage validation skipped | survived alone → **see below** |
| H8 MIN-5 **both validation calls narrowed** | **KILLED** |
| H8 MIN-6 check-then-print | KILLED |

**The two survivors are redundancy, and I checked rather than assumed it.**

1. **H1's two guards are equivalent on every possible input.** With the band passed, `wanted` is
   always the 100 declared draws and any missing draw raises inside the gate, so `n_checked` can
   only ever be 100 — the count assertion is **unreachable while the band is there**. Conversely the
   count assertion catches every short corpus the band catches, because a corpus of probe-covered
   draws with the right *count* is necessarily the right *set* (the probe has exactly 100 rows,
   201–300). So no single input separates them; **removing both** is what the 99-draw test kills,
   and that is the mutation run above. Both are kept, which is H9's ruling on MIN-2 applied here:
   defence in depth, stated as such.
2. **MIN-5's second validation call is redundant with step 4's.** `report` validates every chunk at
   load time (`validate_cell_payload(payload)`) and again with its declared cell; narrowing either
   one alone leaves the other covering it. Narrowing **both** dies on the new test. The unique
   content of the second call — the chunk-vs-declared-cell identity cross-check — is itself already
   covered by step 4's filename-equals-content refusal.

**H7's first survival was a fixture defect, not redundancy:** on the campaign fixture ρ = 0.5 with a
narrow interval, so `mean > 0` and `ci95_low > 0` are both true and an implementation computing the
interval fact from the mean passed. The new test drives `_h3_block` directly with mean 0.5 and
CI [−0.2, 1.2] — the point estimate satisfies both clauses, the interval satisfies neither — which
is exactly the case the rename exists for.

## 14.5 Tests

```
$ .venv/bin/python -m pytest tests/test_transfer_curve.py -q
79 passed, 1 warning in 5.35s        (62 before this round; T5 still runs one real SUMO episode)

$ .venv/bin/python -m pytest -q
2054 passed, 94 skipped, 32 warnings in 186.16s (0:03:06)

$ git diff --stat HEAD
 offline/campaigns/p7_3a_zero_shot.sh |  46 ++-
 offline/transfer_curve.py            | 114 ++++++-
 tests/test_transfer_curve.py         | 565 +++++++++++++++++++++++++++++++---
 3 files changed, 659 insertions(+), 66 deletions(-)

hygiene exit=0 · English exit=0 · frozen guard exit=0 · bash -n on the driver: ok
```

The red run before implementing: **8 failed, 69 passed** — H1 ×3, H2, H7, H8 MIN-3, MIN-5, MIN-6.
The H-round tests that passed on arrival (H3, H4, H5, H6a, H6b, MIN-1) pin behaviour that already
existed; H asked for the tests, and their mutations above are what show they reach it.

## 14.6 Tests I changed, disclosed

1. **`test_t8_report_states_h3_as_inequalities_and_interprets_nothing`** asserted `clause["holds"]`,
   which H7 renames. Updated to `point_estimate_satisfies`, with a comment pointing at H7's own
   test. This is the rename landing, not an assertion being weakened.
2. **`test_t8_report_refuses_a_cell_whose_anchors_come_from_another_draw`** → renamed
   `test_t8_report_refuses_when_a_declared_anchor_chunk_is_missing`, and its match tightened from
   `"1001|anchor|missing"` to `"declared cell"`. H4/H9: it reaches the **completeness** check, not
   the pairing guard, and its old name claimed otherwise.
3. **`test_t9_the_driver_declares_both_stages…`** used `"rest" in code`, which matches *restart* in
   a refusal message (H9). Now asserts the case statement's own `confirmatory|rest)`.
4. **The MIN-5 fixture** now supplies the whole declaration (`[*_DEMO_CELLS, rest_cell]`) and lets
   `report` filter by stage, which is the real campaign's shape: `declared_cells(None)` is the
   declaration and `declared_cells(stage)` is the slice.
5. **`test_h8_min5_an_undeclared_arm…`** was joined by
   `test_h8_min5_a_broken_chunk_of_the_OTHER_stage_is_still_refused`, because the first is caught by
   the `extra` check and does not pin the validation loop (found by the mutant, not by reading).
6. The `run_cell` fake seams were extracted into `_install_cell_seams` so H6(b), MIN-1 and MIN-3 can
   each reach `run_cell` without a fourth copy of 80 lines of fakes. No assertion was removed.

## 14.7 Still not executed by anyone (unchanged from H12)

The `spawn` pool end to end; a full real episode through `run_cell`; A17(f) on a real 100-draw
corpus (the synthetic 100-draw corpus is tested, the real one is not); stage-1 versus final
byte-identity on real data; the signal handler and the group-leader refusal under a real tmux pane.
**H2's restart path is asserted over the driver's text only** — no restart has been executed.

## 14.8 Open questions for the coordinator

1. **H1's count assertion is unreachable** while the band is passed (§14.4). Keep it as defence in
   depth — my reading of H9's MIN-2 ruling — or drop it as dead code? I kept it.
2. **`action_space_n` is a new published field** in every chunk and in `_PUBLISHED_FIELDS`. Nothing
   is recorded yet so no artifact moves, but it is a schema addition made in a fix round.
3. **H2's `a17f_existing` stage log** is a second log name (`logs/a17f_existing.log`) beside
   `logs/a17f.log`. The manifest covers both; say if you would rather they shared one name.

## 14.9 AI-assistance record for this round

1. **Tool and version.** Claude Code, Opus 4.6, 2026-09-17.
2. **Authorship per file.** AI-authored: this addendum, `docs/plans/p7.3a_amendment_h.md`, and the
   edits to `offline/transfer_curve.py`, `offline/campaigns/p7_3a_zero_shot.sh`,
   `tests/test_transfer_curve.py`.
3. **Human verification performed.** The author approved the merge and push prompts. All test runs
   and all 17 mutations in §14.4 were executed by the AI in this session and are pasted above; H1's
   falsification was re-run against the coordinator's own smoke corpus. The coordinator's and
   reviewer's prior runs are cited from Amendment H, not re-derived here.
4. **Who made the research decisions.** None in this round. Every item is a coordinator ruling
   (H1–H9), H0 answers my open question 1, and H10 answers Q2–Q4. The one judgement I made — keeping
   H1's unreachable count assertion as defence in depth — is §14.8's first open question.

---

# 15. ADDENDUM — F1's pre-flight pilot (2026-09-17)

**Status: DONE.** The pilot route, I3(3)'s log-name change, the driver header's schedule replaced by
the pilot's own rate, and the first execution of the `spawn` pool and of a full real episode through
`run_cell`. One commit, as instructed. `report` was never called on the pilot directory.

## 15.1 What ran

```
$ python -m offline.transfer_curve --work-dir output/p7_3a_runs/preflight_pilot \
    --draws-root … --output-root … --data-dir … pilot --workers 12 \
    --transcript output/p7_3a_runs/preflight_pilot.txt
```

16 DT cells on **draw 5** (P7.2b's fenced smoke draw, deliberately not the held-out pool), both
subjects × all four declared arms × seeds 101/202, 12 workers, `halting_check` OFF (draw 5 is not
C2's declared subset). Transcript at `output/p7_3a_runs/preflight_pilot.txt`, appended.

| | canary | wall (16 cells) | effective s/cell | in-process mean | speed-up | failures |
|---|---|---|---|---|---|---|
| run 1 | 0.92 s | 32.05 s | **2.003** | 14.34 s | 7.16× | 0 |
| run 2 | 0.77 s | 32.55 s | **2.035** | 14.86 s | 7.30× | 0 |

n = 2 runs × 16 cells, 2026-09-17, A11 (thermally constrained, no pad). Run 2 went to a throwaway
`/tmp` work directory so the canonical pilot directory holds one clean run.

## 15.2 What the first real pool run found

1. **The pool works, and nothing had ever executed it.** 32 cells rolled across two runs, 0
   failures, one env per process, `spawn`. This and the item below were §14.7's top two "never
   executed by anyone".
2. **A full real episode through `run_cell` works** — 360 decisions, both subjects, all four arms,
   including `a_q1.0` whose target is out of support for `mappo1000` (E4). Every chunk passed
   `validate_cell_payload` inside `run_cell` before it was written.
3. **Resume by content works on real chunks.** Re-running the pilot on the complete directory:
   `n_reused: 16, n_rolled: 0, n_failed: 0` — no worker started. And the rate guard fired correctly:
   `effective_seconds_per_cell: null` rather than a fabricated 0 s/cell for a run that rolled
   nothing.
4. **Destruction and move-aside work on a real chunk.** I truncated
   `cell_mix50_naive_seed202_draw0005.json` to 120 bytes and re-ran: the chunk was judged unusable,
   **moved to `failed/` with its corrupt bytes intact** (120 bytes, still there as evidence), the
   cell was re-rolled, `n_reused: 15, n_rolled: 1, n_failed: 0`. That is the P5.3b defect class —
   a bad chunk surviving every restart, and a `JSONDecodeError` killing the driver — not happening.
5. **⚠️ TWO `traci` connect retries per SUMO start, systematically.** 32 "Retrying in 1 seconds"
   lines for 16 cells in run 2's full capture — exactly 2 per cell. **It is not contention:** one
   cell at **one** worker also retries twice. This is A10's recorded traci start retry, now
   quantified: `envs/sumo_env.py:196` starts with `numRetries=_TRACI_START_NUM_RETRIES` (= 120), and
   each retry sleeps 1 s, so ≈ 2 s of every cell's ≈ 14.9 s in-process time — about 13 % — is
   sleeping. At 12 workers that is ≈ 0.17 s/cell effective, ≈ 13 min over 4,700 cells. `sumo_env.py`
   is FROZEN and nothing here touches it; recorded as a cost, not a defect of P7.3a's code.
6. **The fence holds, and by two independent routes.** Pointing `report` at the real pilot directory
   exits 1 at the canary record (`this run wrote no canary.json`) and creates nothing — the out-dir
   was not even made. The test covers the other route: a pilot cell is not a declared cell of any
   stage, so completeness refuses it even with a canary present.

## 15.3 The driver header

§6 of `p7_3a_zero_shot.sh` now carries the pilot's rate, the pilot's name, its canary, its date and
its n — replacing the A6 harness figures. **The projection is stated with what it does not cover:**
stage 1 ≈ 40 min and the full campaign ≈ 2.6 h are measured on **DT cells only**; the campaign's 700
anchor cells were not run by this pilot, and A6's in-process anchor times with halting ON (38.65 /
44.66 / 51.24 s) sit above the DT cell's 31.64 s, so those 700 are a lower bound. This replaces C7's
stage 1 ≈ 1 h / full ≈ 3–3.5 h, which came from the harness pilot with the halting check on **every**
cell; under C2 it is on for 47 of 4,700.

## 15.4 I3(3)

Both stage-1 branches now run `run_stage a17f`, so there is one `logs/a17f.log`, and `STAGE_NOTE`
writes the branch taken as the **first line** of that run's entry (`branch: corpus ALREADY PRESENT
…` / `branch: corpus COLLECTED by this run …`). T9 asserts the single name, the two branch lines,
and that the note precedes the stage header inside the entry.

## 15.5 Tests and mutations

```
$ .venv/bin/python -m pytest tests/test_transfer_curve.py -q
83 passed, 1 warning in 5.33s          (79 before this round)

$ .venv/bin/python -m pytest -q
2058 passed, 94 skipped, 32 warnings in 197.65s (0:03:17)

hygiene=0 · english=0 · guard=0 · bash -n ok
```

Red first: the four new tests failed at the real API surface (`AttributeError: … has no attribute
'run_pilot'`, and the driver-text assertions). **8 mutations, 8 killed:** the pilot moved onto the
held-out pool; the pilot shrunk to one subject; `PILOT_STAGE` made a campaign stage; an outcome
field added to the fenced summary; the canary line dropped from the transcript; I3(3)'s branch note
deleted; two log names restored; the note written after the stage header.

## 15.6 Tests I changed, disclosed

1. **`test_t9_a17f_stops_the_driver_before_any_evaluation_cell_runs`** used `code.index("run_stage
   a17f …")`. After I3(3) that label appears **twice** and the first occurrence is the *skip* branch,
   which precedes the collect branch and contains no collection — so the assertion failed against a
   driver whose order is correct. Restated as the invariant that actually matters: the collect
   branch gates what it collected (`index(collect) < rindex(a17f)`), and **every** gate precedes
   **every** evaluation cell (`rindex(a17f) < index(cells --stage)`). Same class as the `" cells "`
   needle in §14.6 — I wrote `index` where the branch made `rindex` the right question.
2. **`test_f1_report_refuses_a_pilot_work_directory`**'s fixture built a draw-5 chunk from the
   draw-1000 payload helper, so it carried `halting_checked: True` and tripped C2's refusal before
   the fence — passing for the wrong reason. The fixture now sets the flag off and the three halting
   fields to `None`, which is what a real pilot chunk carries.
3. **`test_f1_the_pilot_summary_carries_no_outcome`** scanned the whole serialised summary for the
   substring `att`, and the record's own prose says *"no ATT, no rho"* — the test was satisfied, and
   then broken, by its own documentation. It now scans **keys** recursively and asserts the numeric
   fields are clocks and counts. Same trap `_driver_code_text` exists for.

## 15.7 Still not executed by anyone

A17(f) on a **real** 100-draw corpus (the synthetic one is tested; the real collection has not run);
stage-1 versus final byte-identity on real data; the signal handler and the group-leader refusal
under a real tmux pane; **H2's restart branch executed** (its text is asserted, and the pilot
exercised the same `chunk_is_reusable` path it depends on, but the driver branch itself has not run);
the anchors' rate at 12 workers. The reviewer's pre-flight (I5.3) is where the driver-level ones
belong.

## 15.8 Open questions

1. **The traci retry (§15.2.5)** costs ≈ 13 min over the campaign and is systematic. `sumo_env.py`
   is frozen, so this is a note, not a change — unless you want it raised as a separate finding.
2. **Run 2's chunks went to `/tmp`** so the canonical pilot directory holds one clean run plus the
   `failed/` evidence from the destruction probe. Say if you would rather both runs sat under
   `output/p7_3a_runs/`.
3. **The pilot re-ran the canary** on each invocation (0.92 / 0.77 / 0.76 s across three runs today).
   That spread is within A11's expectations but it is also the F3 comparison basis — the pad re-run
   should be compared against which of them?

## 15.9 AI-assistance record for this round

1. **Tool and version.** Claude Code, Opus 4.6, 2026-09-17.
2. **Authorship per file.** AI-authored: the pilot route in `offline/transfer_curve.py`, the driver's
   header and I3(3) change, the new tests, this addendum.
3. **Human verification performed.** The author approved the merge and push prompts. The pilot, its
   two re-runs, the destruction probe, the single-worker retry check, the real `report`-on-pilot
   fence check, all test runs and all 8 mutations were executed by the AI in this session; every
   number in §15.1 and §15.2 comes from a command run here. The coordinator has not re-run them.
4. **Who made the research decisions.** None. The pilot's size, draw, subjects, worker count, work
   directory and fence are F1 and I5(2); the log-name change is I3(3); the header rule is F1/F3. The
   two judgements I made are recorded as open questions: where run 2's chunks live, and which canary
   F3 compares against.

---

# 16. ADDENDUM — Amendment J1–J4 (2026-09-17)

**Status: DONE** for J1, J2, J3 and J4. **J5 was NOT run in this session** (the pilot re-run from
the dedicated worktree at the fix commit), as instructed.

## 16.1 J1 — provenance measured strictly, and checked at every consumer

* **(a)** `transfer_curve._git_provenance` now runs `git rev-parse HEAD` and `git status
  --porcelain` in the module's own tree through `_git`, which **raises** when either fails. It no
  longer imports `materialise_draws._git_commit` — that helper returns `dirty = False` whenever
  `git status` cannot run, so an unmeasured tree read as a clean one and §7's gate would have been
  satisfied by the absence of evidence. `git_dirty` is `True` for **any** porcelain entry,
  untracked files included.
* **(b)** `validate_cell_payload` refuses `git_dirty is not False` and a `git_commit` that is not
  40 hex characters (missing, `"abc123"` and `"z" * 40` all refused).
* **(c)** `code_changed_since(commit)` = `git diff --name-only <commit> HEAD` minus everything under
  `docs/`. `chunk_is_reusable` returns `False` on a non-empty result; `report` re-derives it and
  refuses, naming the paths. A **docs-only** difference stays reusable, so a packet commit does not
  re-roll 1,200 cells. A `git` failure raises out of `chunk_is_reusable` deliberately — it is an
  environment failure, not a verdict about a chunk.
* **(c)** the artifact records `chunk_commits_by_stage`, beside the top-level `git_commit`, which is
  `report`'s own at write time and is routinely a different (docs) commit.
* **(d)** the driver refuses before the token when `git -C "$WORK_TREE" status --porcelain` is
  non-empty, printing the paths. **Executed against this tree, which is dirty:**

```
REFUSING TO START: the worktree /home/filip/rltraffic-p73a is DIRTY
     M offline/campaigns/p7_3a_zero_shot.sh
     M offline/transfer_curve.py
     M tests/test_transfer_curve.py
  … Run the campaign from the dedicated worktree detached at the reviewed commit (J1(e)).
exit=2
```

## 16.2 J2 and J3

* **J2** `trap on_signal INT TERM HUP`. T9 asserts all three.
* **J3** a pre-token refusal when `/proc/$$/status` `SigIgn` has bit 0x2. **Executed**, under
  `setsid --wait bash -c 'trap "" INT; exec bash …'`:
  `REFUSING TO START: SIGINT is IGNORED in this shell (SigIgn 0000000000000006)`, **exit 2**;
  the same command without the `trap` falls through to the next check. Transcript:
  `output/p7_3a_runs/j_round/sigign_refusal.txt`.
* **J3** the header's process-group sentence now says what was measured: a plain `&` from a
  non-interactive shell **did** lead its own group; `set +m` produced the non-leader.
* **J3** a failed canary is caught (`if ! CANARY_LINE=$(…)`), prints its own refusal saying nothing
  has been consumed, and exits **2** — not `fail()`'s 1, which writes `FAILED` and so claims a run
  started.
* **J3** `run_cell` loads the calibration through `data_dir`; `run_pilot` prints the canary line
  before it checks it.

## 16.3 J4 — two statements in §15 corrected against the files on disk

**(min-8) §15.8(3) said "three runs today (0.92 / 0.77 / 0.76)". The transcripts hold FIVE canary
measurements across five pilot invocations:**

| file | entry | date | canary | what that invocation did |
|---|---|---|---|---|
| `preflight_pilot.txt` | 1 (line 4) | 11:04:50 | **0.92 s** | run 1 — rolled 16, reused 0 |
| `preflight_pilot.txt` | 2 (line 18) | 11:05:14 | **0.79 s** | first resume attempt — rolled 0, reused 16 |
| `preflight_pilot_resume_probe.txt` | 1 (line 4) | 11:05:25 | **0.76 s** | resume probe — rolled 0, reused 16 |
| `preflight_pilot_resume_probe.txt` | 2 (line 18) | 11:05:56 | **0.73 s** | destruction probe — rolled 1, reused 15 |
| `preflight_pilot_run2.txt` | 1 (line 4) | 11:06:57 | **0.77 s** | run 2 — rolled 16, reused 0 |

The second entry in `preflight_pilot.txt` is the one I missed: the first resume attempt wrote to the
canonical transcript before I switched the probes to a separate file. §15.1's "n = 2 runs × 16 cells"
remains right for the **rate** — only two invocations rolled all 16 — but the canary count was wrong
and is now taken from the files rather than from memory.

**(min-9) §15.2(5) said "one cell at one worker also retries twice". What the file shows:**
`output/p7_3a_runs/preflight_pilot_single.log` contains **two `Retrying in 1 seconds` lines and one
rolled cell** (`n_rolled: 1`). **The worker count is not in the log** — `workers=1` was an argument
of the command I ran, and nothing recorded it. The claim that survives the file is: *two retries for
one cell in a run that rolled one cell*. That it was a single-worker run rests on my command, not on
the evidence.

**Standing rule adopted:** no evidence of a P7.3a run goes to `/tmp` (tmpfs here). This round's
mutation script, its transcript and the SigIgn capture are under
`output/p7_3a_runs/j_round/`.

## 16.4 Tests and mutations

```
$ .venv/bin/python -m pytest tests/test_transfer_curve.py -q
99 passed, 1 warning in 5.86s        (83 before this round)

$ .venv/bin/python -m pytest -q
2074 passed, 94 skipped, 32 warnings in 199.90s (0:03:19)

hygiene=0 · english=0 · guard=0 · bash -n ok
```

Red first: **16 failed, 83 deselected** — every new test at its own API surface.

**14 mutations, 14 killed** (`output/p7_3a_runs/j_round/mutations.txt`): the fail-open helper
substituted back; the `git` failure no longer raising; the dirty check removed; the commit-shape
check removed; `chunk_is_reusable` ignoring a code change; `report` ignoring it; `docs/` no longer
exempt; the artifact's commit block emptied; the driver's dirty-worktree check removed; `HUP`
dropped from the trap; the SigIgn check removed; the canary failure routed through `fail()`;
`run_cell` ignoring `data_dir`; `run_pilot` checking before printing.

## 16.5 Tests I changed, disclosed

1. **Every fixture chunk now carries a real `HEAD` sha** instead of `"0" * 40`. J1(c) resolves a
   chunk's commit against git; a fabricated sha is an unknown revision, and refusing it is the
   behaviour J1 asks for — so the fixtures had to stop fabricating one.
2. **The LAST-refusal test's carrier moved from `git_commit` to `checkpoint`.** J1(b)'s
   commit-shape refusal sits *above* the fence, so the old carrier stopped reaching it and the test
   would have passed on the wrong refusal. `checkpoint` is published, free text, and not validated
   for content (`report` pins the checkpoint's digest, not its path), so the route still reaches
   the serialised bytes with delivered code and no patching.
3. **`_install_cell_seams` now fakes `_git_provenance`.** The assembly tests call the real
   `run_cell`, which records this tree's provenance — dirty while the round is being written — and
   J1(b) then refuses the cell. Provenance is faked there for the same reason the env and the
   rollout are. That a dirty cell **is** refused is asserted separately against the real function.
4. **`test_j3_the_header_states_what_was_measured`** asserts the corrected claim is present and
   that the old claim *stated as fact* is gone; the driver is allowed to quote the wrong sentence
   in order to correct it, which is how a correction reads.

## 16.6 Still not executed

J5's re-run from the dedicated worktree at the fix commit, and the interrupted-run probe (a signal
while spawn workers are live). **Every chunk currently on disk — the pilot's 16 and the 17 others —
records `git_dirty: true` and commit `a0327ce`, so under J1 they are all refused and not reusable**;
J5's run is what produces chunks this code will accept. A17(f) on a real 100-draw corpus, `cells
--stage` and `report` through the driver, and the manifest step remain the stage-1 run's (J7).

## 16.7 Open questions

1. **J1(e)'s dedicated worktree does not exist yet** (`/home/filip/rltraffic-p73a-run`). Creating it
   is `git worktree add --detach`, which is a repository-level act; say whether I create it in the
   J5 session or you do.
2. **`chunk_commits_by_stage` is a new artifact field.** Nothing is recorded yet, format version
   stays `1.0`.
3. **The pilot's existing chunks are now unusable** by J1(c) (code changed since `a0327ce`). J5
   re-rolls into `preflight_pilot_fix/`; say if you want the old directory moved aside rather than
   left beside it.

## 16.8 AI-assistance record for this round

1. **Tool and version.** Claude Code, Opus 4.6, 2026-09-17.
2. **Authorship per file.** AI-authored: the J1–J3 changes to `offline/transfer_curve.py` and
   `offline/campaigns/p7_3a_zero_shot.sh`, the new tests, the mutation script under
   `output/p7_3a_runs/j_round/`, and this addendum.
3. **Human verification performed.** The author approved the merge and push prompts. The 16 red
   tests, the 99 green, the whole suite, the 14 mutations, and the two executed driver refusals
   (dirty worktree; SIGINT ignored) were run by the AI in this session; the five canary values and
   the single-worker retry count in §16.3 were read from the files on disk, not from memory.
4. **Who made the research decisions.** None. J1–J4 are coordinator rulings; J6 answers the previous
   packet's three questions. The judgements I made are §16.7's three open questions.
