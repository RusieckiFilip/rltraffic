# RETURN PACKET — P4.3 (probe-calibrated return prompting, ablated in-domain)
**Date:** 2026-08-13 · **Mode:** Claude Code · **Contracts:** v1.1

**Status:** PENDING — this file is being written as the campaign runs; the landscape table below is
filled from the committed artifacts once the last point lands.

---

## 0. The verdict, before any interpretation

*(filled at the end)*

---

## 1. Branch and diff

```
task/p4.3-rtg-calibration
$ git diff --stat main...HEAD
```

*(filled at the end)*

---

## 2. Gate A — the instrument check, run before anything was reported

```
dt_g5: target_rtg -5762.0 over 5 seeds x 100 draws
  att_horizon 104.9558 +/- 0.2021  vehicle_count 44.14  in-support 0.8180 (below 0.0, above 65.5)  n=500  992s
  GATE A PASS: 500 cells compared, 0 mismatched, reference mean 104.9557589818 against 104.9557589818
```

The declared grid point `-5762` **is** P4's reported configuration, so its 500 episode records were
compared cell by cell against `docs/data/p4_gate.json` — `att_horizon`, `horizon_vehicle_count` and
`episode_reward`, with `==`, never a cell mean. **500 compared, 0 mismatched.**

This is the strongest single piece of evidence in the packet: the instrument that produced every
number below reproduces P4's committed gate exactly, in a different worktree, from re-materialised
demand, three days later.

---

## 3. The probe, and the two rules

*(filled at the end)*

---

## 4. Tests

*(filled at the end)*

---

## 5. Self-review checklist

*(filled at the end)*
