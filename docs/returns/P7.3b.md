# RETURN PACKET — P7.3b (the full-retrain anchor of the C3 curve, A18(a))
**Date:** 2026-09-19 · **Mode:** Claude Code · **Contracts:** v1

**Status:** DONE

**Amendments and rulings written against:** `BRIEF_38` **A1–A7** (plan gate, 2026-09-18) and **B1–B4**
(the packet task, 2026-09-19); `BRIEF_37` A2, B1/B2, C2, E4, G1–G4, J1; `PREREGISTRATION` H3, §3.4,
A15, A16, A17, A18(a)–(c) and **A19** (`v1.9-prereg-a19` → `443c076`, verified in this session:
`git rev-list -n 1 v1.9-prereg-a19` = `443c0760b7f6595dfcfa97a8b25bce4046bb8b5a`). Decisions-Log
rulings of 2026-09-17 (the exactly-zero env-ATT denominator) and 2026-09-18 (read the driver capture,
not only the logs; one merge review; findings carried with their caveats).

**What this packet is.** The write-up of a campaign that has already run and been verified. This
session rolled no cell, trained no model, consumed no token, and edited no file under `offline/**`,
`tests/**`, `experiments/**` or any frozen path. Every number below was produced by a command run in
**this** session unless the line says otherwise; numbers read from an artifact of another date carry
that date.

---

## 1. Branch + diff stat

```
task/p7.3b-anchor
$ git diff --stat main...HEAD          # measured BEFORE this packet's own commit
 docs/data/p7_3b_anchor.json          | 405639 ++++++++++++++++++++++++++++++++
 docs/data/p7_3b_anchor_training.json |   1979 +
 docs/notes/P7.3b_CORPUS_EVIDENCE.md  |    530 +
 docs/plans/p7.3b.md                  |    597 +
 offline/anchor_training.py           |    762 +
 offline/campaigns/p7_3b_anchor.sh    |    363 +
 offline/transfer_curve.py            |    633 +-
 tests/test_anchor_training.py        |    797 +
 tests/test_transfer_curve.py         |   1201 +-
 9 files changed, 412410 insertions(+), 91 deletions(-)
```

The stat excludes this file, which is the branch's last commit. **No frozen path appears in it** —
checked mechanically:

```
$ git diff --name-only main...HEAD | grep -E "^(envs/|agent/|algorithms/|rewards.py|states/|metrics/|CityFlow/|experiments/|utils/|scripts/|\.claude/)"
(none -- no frozen path in the diff)
```

**Commits added by this session** (three; none amended, every one with named paths):

| sha | what |
|---|---|
| `4eaa01c` | `Merge branch 'main' into task/p7.3b-anchor` (B1) |
| `9248f4e` | the Stage B plan addendum in `docs/plans/p7.3b.md` |
| `86812e7` | `docs/data/p7_3b_anchor.json` at `4cae233c…` |

## 2. Files produced by this session

- `docs/data/p7_3b_anchor.json` — the campaign's committed artifact, copied by hand (B2).
- `docs/plans/p7.3b.md` — a **Stage B** section appended; sections 0–8 untouched.
- `docs/returns/P7.3b.md` — this packet.
- `output/p7_3b_runs/impl_verify/` — scratch evidence, gitignored, outside every manifest:
  `verify_impl.py` (this session's independent checker), `verify_impl.out`, `impl_verify.json`,
  `mutation_control.out`, `mutation_1.out`, `mutation_2.out`, `mutation_3.out`, `regen.out`,
  `regen/p7_3b_anchor.json`, `manifest_check.out`, `diagnostics.out`.

---

## 3. B1 — the merge, with its output

```
$ git -C /home/filip/rltraffic-p73b status --porcelain          # must be EMPTY before the merge
<no output>

$ git -C /home/filip/rltraffic-p73b merge --no-edit main
Merge made by the 'ort' strategy.
 PREREGISTRATION.md                           |   1 +
 docs/PROJECT_PLAN.md                         |  12 +-
 docs/briefs/BRIEF_38_p7.3b_anchor.md         | 102 +++++++++++++
 docs/notes/HANDOFF_2026-09-19.md             | 140 ++++++++++++++++++
 docs/notes/P7.3b_CAMPAIGN_READ_2026-09-19.md | 214 +++++++++++++++++++++++++++
 5 files changed, 466 insertions(+), 3 deletions(-)
 create mode 100644 docs/notes/HANDOFF_2026-09-19.md
 create mode 100644 docs/notes/P7.3b_CAMPAIGN_READ_2026-09-19.md

$ git -C /home/filip/rltraffic-p73b merge-base --is-ancestor 443c076 HEAD && echo "A19 is an ancestor: yes"
A19 is an ancestor: yes
```

**No conflict.** `f13358e` — the commit every one of the 700 chunks records — is still an ancestor
(`git merge-base --is-ancestor f13358e HEAD` → yes), so the merge commit changes nothing about the
campaign's provenance. `core.hooksPath` is `/home/filip/rltraffic/githooks` (absolute, so it binds
this linked worktree); no commit in this session carries an AI trailer.

---

## 4. B2 — the artifact into the branch, by hand

```
$ pgrep -af "python.*offline\.transfer_curve"
(no live runner)

$ cp /home/filip/rltraffic/output/p7_3b_anchor/artifacts/p7_3b_anchor.json /home/filip/rltraffic-p73b/docs/data/
$ sha256sum docs/data/p7_3b_anchor.json
4cae233ce480a5596dc9a65ad53c1ec0cb20c17fa80008d1e1a13341703af1cc  docs/data/p7_3b_anchor.json
```

**The measured digest is the required one.** It is also the digest that
`output/SHA256SUMS_p7_3b_anchor.txt` records for that file and the digest of this session's own
regeneration (§5.7). The destination did not exist before the copy.

`p7_3b_anchor_training.json` was **not** re-copied: the campaign's copy and the committed one are
byte-identical (`cmp` clean, both `edca65f96f060d77a8ef9620deaa2c4a76c4c77eae0f7a9e05c47587a06dd62a`).

The artifact came from the campaign's **work directory**, not from the run worktree's `docs/data/`
that the driver's closing `NEXT` line names — that tree is detached at `f13358e` and carries no
branch (Amendment B2).

---

## 5. B3.1 — the campaign facts, re-measured in this session

The checker is `output/p7_3b_runs/impl_verify/verify_impl.py`, written for this packet. It is **not**
the coordinator's `verify_cells.py` and imports nothing from `offline.transfer_curve`: the cell
declaration is derived by hand from A18(a)/A18(c), ρ is recomputed from the raw chunks by the
registered formula, and every comparison against the artifact is `==` on values both sides parsed
from JSON. Full transcript: `output/p7_3b_runs/impl_verify/verify_impl.out` (exit 0, every check
passed).

### 5.1 700 cells and their composition

| check | result |
|---|---|
| chunk files in the work directory | **700** |
| declaration derived by hand (5 seeds × draws 1000–1099, + `fixedtime`, `maxpressure` × 100) | **700** |
| cell set vs that declaration | **identical**, 0 missing, 0 extra |
| composition | 500 `dt / anchor_k200 / naive_in_domain`, 100 `anchor / fixedtime`, 100 `anchor / maxpressure` |
| duplicate cell identities | 0 |

### 5.2 Stage summary, `COMPLETE`, and the clocks — per stage, each with its own canary

| stage | date | commit | canary | clock |
|---|---|---|---|---|
| train (five seeds × 40,000 steps) | 2026-09-18T21:10:58+02:00 | `b35216c` | **0.77 s** | `TRAINING COMPLETE in 1095s` |
| cells + report (700 cells) | 2026-09-19T11:31:19+02:00 | `f13358e` | **0.87 s** | `CAMPAIGN COMPLETE (anchor) in 1296s` |

`n_declared 700 · n_rolled 700 · n_reused 0 · n_failed 0 · failures []`, read from the JSON summary at
the end of `logs/cells_anchor.log`; `output/p7_3b_anchor/COMPLETE` contains
`CAMPAIGN COMPLETE (anchor) in 1296s`; no `FAILED` file and no `failed/` directory exists.

**Rate, with its canary beside it (A11: thermally constrained laptop, no cooling pad, on mains).**
Cells-stage wall **1288.61 s**, sum of per-cell seconds **15,337.9 s** ⇒ **11.90×** on 12 workers;
per-cell mean 21.91 s (anchor_k200 23.01 s, baselines 19.18 s), min 17.39 s, max 70.33 s.

### 5.3 Provenance and regime, over all 700

One value each: `git_commit f13358eb14d37592d517c75b7cb6c39a75b05cfd` (equal to the branch tip before
this session's commits), `git_dirty false`, `stage anchor`, `n_teleports 0`, `time_to_teleport_option
"-1"`, `vehicle_types_seen ["cf_parity"]`, `decisions 360`, `n_observations 3600`,
`engine_seed_requested 1000`, `engine_seed_drawn 437485271` (A18(c): one seed across all 700),
`canary_seconds 0.87`, `max_abs_depart_clock_deviation 0.0`, `n_vanished_without_arrival 0`,
`n_arrived_never_observed_at_a_boundary 0`.

On the 500 anchor cells: `rtg_first == target_rtg == -20625.0` on **500/500**; one `support_range`
`[-25257.0, -71.0]` with the target **inside** it.

Halting cross-check ON for **exactly 7 cells, all on draw 1000** (Amendment C2's declared subset),
28,800 lane-seconds each, **0 disagreeing lane-seconds, 0 max absolute difference**.

**Provenance chain, closed end to end in this session:** for every seed, the checkpoint digest in the
committed training artifact **==** the sha256 of the file on disk **==** what all 100 of that seed's
cells recorded; and every anchor cell's `anchor_training_sha256` equals the actual sha256 of
`p7_3b_anchor_training.json`.

| seed | checkpoint sha256 | final loss | steps | seconds |
|---|---|---|---|---|
| 101 | `7a717b99526d47a9…` | 0.03604091703891754 | 40000 | 212.77 |
| 202 | `63517323b4ef02ca…` | 0.035134412348270416 | 40000 | 213.33 |
| 303 | `878152759b67a91b…` | 0.022929096594452858 | 40000 | 228.17 |
| 404 | `556255ade9a66553…` | 0.043298762291669846 | 40000 | 227.04 |
| 505 | `ae39174e208514b7…` | 0.04229655861854553 | 40000 | 207.94 |

`declared_gradient_steps: 40000`, `raise_to: null` (A3), 200 episode ids, `state_dim 25`, `n_actions 8`.

### 5.4 ρ under both definitions, recomputed from the raw chunks

**The load-bearing comparison is per cell.** All **1,000** per-cell ratios (500 cells × 2 definitions),
recomputed here as `(ATT_fixedtime − ATT_arm) / (ATT_fixedtime − ATT_maxpressure)` against that draw's
own anchors, are **bit-identical** to the artifact's — 0 differ.

| definition | mean ρ | 95 % CI | n draws | vs artifact |
|---|---|---|---|---|
| `e_sumo` (registered primary) | **0.9829792481916148** | **[0.9701604747851167, 0.9957980215981128]** | 100 | mean, std, both bounds: difference **exactly 0.0** |
| `att_env` (co-reported) | 0.9840242299939405 | [0.921675243158202, 1.0463732168296789] | 100 | difference **exactly 0.0** |

Per-seed ρ on the primary (101…505): **0.9726532764576968 / 0.9858670548205469 / 0.9798954351423358 /
0.9926876589198256 / 0.9837928156176684** — no seed is an outlier.

**Estimator, named:** the per-draw unit is the mean over the five training seeds; the interval is
`dt_gate.mean_ci95`'s analytic `1.96·s/√n` with `ddof=1` over the 100 per-draw values (no resampling,
no seed). The paired comparison's helper is `offline.offline_baselines.paired_comparison`, which pairs
per draw through `dt_gate._per_draw_means` and carries `wilcoxon_signed_rank` beside the analytic CI.

**Denominator diagnostic:** `e_sumo` denominators span **197.9551–299.7228 s**, `att_env` **9.8002–
81.1889 s**; **0 non-positive on either**, so the exactly-zero case the 2026-09-17 ruling provides for
did not arise and **no draw was excluded** from either definition.

### 5.5 Paired ATT against both anchors (primary definition, 100 shared draws)

| against | mean difference | 95 % CI | wins / losses / ties | Wilcoxon p |
|---|---|---|---|---|
| `fixedtime` | **−240.0355018418621 s** | [−242.85279070459723, −237.21821297912697] | 100 / 0 / 0 | 3.9559116088995575e-18 |
| `maxpressure` | **+5.124633990863357 s** | [+1.930806615431012, +8.318461366295702] | 37 / 63 / 0 | 0.003230327177826658 |

Negative means the anchor had the lower travel time. Mean difference, both CI bounds and the
win/loss/tie counts were recomputed here and agree with the artifact **bitwise**. **The Wilcoxon
p-values were obtained by feeding rows built in this session into `paired_comparison`** — the input
path is independent, the statistic is the repository's helper; this is stated rather than implied.

### 5.6 Two further recomputations the brief does not require but the quantities deserve

- **The RTG identity by an independent route:** `rtg_series[1:] == target_rtg − np.cumsum(reward_series)[:-1]`
  holds with **worst absolute difference 0.0** over all 500 anchor cells.
- **The in-support diagnostic, recounted from the chunks:** **166,660 of 180,000** decisions in support
  (**92.59 %**), 13,340 above, **0 below**; the artifact's `total_decisions_in_support` equals the
  recount, and every cell's `in_support_counts` agrees with its own `n_decisions_in_support`.

### 5.7 The artifact's digest, and a regeneration of this session's own

`report --stage anchor` re-run **in the campaign's own worktree** `/home/filip/rltraffic-p73b-run`
(detached at `f13358e`, `git status --porcelain` empty) into
`output/p7_3b_runs/impl_verify/regen/`:

```
wrote .../impl_verify/regen/p7_3b_anchor.json with 700 cells
$ cmp regen/p7_3b_anchor.json /home/filip/rltraffic-p73b/docs/data/p7_3b_anchor.json
cmp: byte-identical
4cae233ce480a5596dc9a65ad53c1ec0cb20c17fa80008d1e1a13341703af1cc  (regeneration)
4cae233ce480a5596dc9a65ad53c1ec0cb20c17fa80008d1e1a13341703af1cc  (committed)
4cae233ce480a5596dc9a65ad53c1ec0cb20c17fa80008d1e1a13341703af1cc  (campaign output)
```

**Why not from this worktree — J1(c) working, recorded as evidence and not as a defect.** Running the
same command in the merged implementer tree refuses:

```
ValueError: cell_anchor_fixedtime_seednone_draw1000.json: it was rolled at f13358eb14d3…, which
differs from HEAD outside docs/ (['PREREGISTRATION.md'], 1 path(s)). Commits may differ only by
documentation; this cell was produced by different code and must be re-rolled
```

`PREREGISTRATION.md` lives at the repository root, so `code_changed_since` — which filters on the
`docs/` prefix — counts A19's registration as a code change. The refusal **created no directory and
no file**. Independently of the refusal, the artifact's top-level `git_commit` is taken at write time,
so a regeneration at the merge commit could not have been byte-identical anyway. Predicted at the plan
gate from the code, then measured; see §11 for the open question it raises.

### 5.8 Integrity of everything the campaign read and wrote

| check | result |
|---|---|
| `sha256sum -c output/SHA256SUMS_p7_3b_anchor.txt` | **713 entries, 713 OK, 0 FAILED** |
| `datasets_sumo_v11/hz1x1_sumo_maxpressure/SHA256SUMS` | 101 entries, **101 OK**, digest-of-digests `def8fb30b408a66221a9fe5062f3a953b2a56c8ae53e2adbad505b0d87941fd7` |
| `datasets_sumo_v11/hz1x1_sumo_maxpressure_301_400/SHA256SUMS` | 101 entries, **101 OK**, digest-of-digests `93f180e08343cb735c26779bc0d71a30f8dbc4a9b56a7369c6876bc81e9debe5` |
| Amendment A2, re-run today: the 201–300 digests vs the coordinator's record of **2026-09-17** (`e81cf687…`, taken before this task touched the tree) | **identical sets**, 101 vs 101 |

Both corpus digests are the ones the committed training artifact records under `corpus.digest_files`.

### 5.9 The 200 re-rolled denominators against P7.3a's — bit-identity, recomputed

200 cells × 3 fields (`e_sumo`, `att_env`, `episode_reward`) = **600 comparisons under `==`,
0 mismatches**, against `docs/data/p7_3a_zero_shot.json`'s `fixedtime` and `maxpressure` rows on draws
1000–1099. P7.3a rolled them at `8bf7eae…`, this campaign at `f13358e`. The re-roll therefore both
satisfied its reason for existing — the denominator must come from the same code as the numerator —
and demonstrated that the code change between the two commits is value-neutral on this path. **ρ from
P7.3a and P7.3b is on one scale and may be compared directly.**

### 5.10 The checker was mutated, to show it is not vacuous

All three mutations were applied to a **copy** of the work directory (`P73B_WORK` override); the
campaign directory was never written to, and the copy was deleted afterwards. Control run on the
unmutated copy: exit 0, every check passed.

| mutation | outcome |
|---|---|
| one anchor cell's `e_sumo` nudged by **one ULP** (395.1040650406504 → …05) | **killed** — `[FAIL] all 1,000 per-cell rho values … 1 differ: [(303, 1042, 'e_sumo')]` |
| one chunk file deleted (`maxpressure`, draw 1077) | **killed** — 3 failed checks (700 → 699, declaration mismatch naming the exact missing cell, 100 → 99) |
| one cell's `n_teleports` set to 1 | **killed** — `[FAIL] n_teleports over all 700 -- observed ['0', '1']` |

**Mutation 1 is the informative one.** With one cell out of 500 corrupted by a single ULP, the
aggregate ρ mean, std, both CI bounds and every per-seed mean remained **bitwise equal** to the
artifact; only the per-cell comparison failed. An aggregate check alone would have passed a corrupted
campaign. That is why §5.4 leads with the 1,000 per-cell comparisons.

---

## 6. Tests — REAL OUTPUT

Run **after** the artifact copy (B2), in this worktree, with `pgrep -f 'python.*offline\.transfer_curve'`
empty immediately before:

```
$ PYTHONPATH=/home/filip/rltraffic-p73b /home/filip/rltraffic/.venv/bin/pytest -q
........................................................................ [ 90%]
........................................................................ [ 93%]
........................................................................ [ 96%]
......................................................................   [100%]
2136 passed, 94 skipped, 32 warnings in 229.09s (0:03:49)
```

**Zero failures.** Against the last two measured runs — coordinator 2,088 / 94 at `0c7d7fd`,
implementer 2,113 / 94 at `b35216c` — the count is accounted for exactly: `git diff b35216c HEAD --
tests/` adds **23** test functions and removes **0**, and 2,113 + 23 = 2,136. **The skip count is
unchanged at 94 because no test reads the committed artifact**: the only three references to
`p7_3b_anchor.json` in `tests/` are a `tmp_path` out-path and two assertions about the driver's source
text (`tests/test_transfer_curve.py:3770, 3891, 3892`). So the copy exercised no new test — measured by
grep, not assumed.

**No test was edited in this session.** No file under `tests/` appears in this session's three commits.

---

## 7. B3.2 — the driver capture, including the refused start

`output/p7_3b_runs/campaign_capture.txt` opens with a refusal that **no log records**:

```
REFUSING TO START: not a process-group leader; run in a tmux foreground pane
  The handler kills the process group, and that is a no-op from a non-leader, so an
  interrupted run would leave the worker pool writing. Nothing consumed.
```

That is the **J3 guard**, firing **before the canary and before the token**. It consumed nothing and
is followed in the same appended capture by the successful tmux run. It demonstrates the refusal path
working on a live machine; **it is not an incident**, and `canary.log` could not have shown it because
by design that file records only starts that reached the canary. The capture then carries, in order:
canary (0.87 s, both halves identical) → `authorised by token written 2026-09-19 11:29:24` → `token
consumed and deleted; a restart needs a new one` → `verify_inputs` (`dry run: nothing trained, nothing
written`) → `cells_anchor` → `report_anchor` (`wrote … with 700 cells`) → `CAMPAIGN COMPLETE (anchor)
in 1296s`.

---

## 8. B3.3 — the anchor beside H3, in A19's registered words

A19 is registered at `v1.9-prereg-a19` → `443c076` (tag resolved in this session), **before any
few-shot point exists** and after both endpoints were known. Quoting the registration rather than
paraphrasing it:

> **(a) H3 CLAUSE 3 IS VOID.** … Clause 1 (better than fixed-time) is the confirmatory test and
> **HOLDS** (P7.3a). Clause 2 (worse than MaxPressure) was **REFUTED** by the same campaign … Clause 3
> presupposes a positive gap to MaxPressure for k = 100 to close. **That gap does not exist, so clause
> 3 is not tested and cannot fail: it is VOID, because its premise is false.**
>
> **(c) …** The k = 200 anchor is **behaviour cloning on MaxPressure demonstrations** … and is
> **pinned near ρ = 1.0 by construction** … **It bounds what target-domain probe data buys the
> architecture, and it bounds nothing else.**

**The artifact's `h3` block is structurally empty in this stage.** It carries the four clause
skeletons (clause text, definition, inequality, the `att_env` caveat) with `mean_rho`, `ci95_low`,
`ci95_high`, `point_estimate_satisfies` and `ci95_entirely_satisfies` all `{}` — the honest result of
running `report` over a work directory that holds no `b_mean_k100` cell. H3's clauses are P7.3a's and
this artifact does not recompute them. `what_this_does_not_say` carries the registered framing,
including A18(a)'s sentence verbatim and A18(b)'s absent online anchor.

**The curve's two endpoints, on one scale** (§5.9). The anchor row is this session's measurement; the
zero-shot rows were read in this session from the committed `docs/data/p7_3a_zero_shot.json`
(P7.3a, 2026-09-18):

| point | ρ (`e_sumo`) | 95 % CI | ATT `e_sumo` | entered / created | pending |
|---|---|---|---|---|---|
| zero-shot `mappo1000 / b_mean_k100` *(P7.3a)* | +1.8895 | [1.8610, 1.9180] | 141.35 | 1813.7 / 1814.3 | 0.7 |
| zero-shot `mix50 / b_mean_k100` *(P7.3a)* | +1.7264 | [1.6862, 1.7666] | 180.99 | 1761.8 / 1814.3 | 52.6 |
| **full-retrain anchor `anchor_k200`** *(this campaign)* | **+0.9830** | [0.9702, 0.9958] | **361.86** | 1714.8 / 1814.3 | 99.5 |

**The anchor clones its demonstrator** — recomputed here from the 700 chunks:

| arm | n | ATT `e_sumo` | entered / created | pending | mean episode return |
|---|---|---|---|---|---|
| `anchor_k200` | 500 | 361.86 | 1714.8 / 1814.3 | 99.5 | −23,311.6 |
| `maxpressure` | 100 | 356.73 | 1717.3 / 1814.3 | 97.0 | −23,036.8 |
| `fixedtime` | 100 | 601.89 | 1419.2 / 1814.3 | 395.2 | −30,490.3 |

Both corpus manifests record `behavior_policy: maxpressure`, `backend: sumo`, `epsilon: None` — pure
MaxPressure, no exploration noise. The 200 episodes have mean return **−23,099.17**, max **−20,625.0**,
largest absolute return **25,257.0**; the anchor lands 212 below its corpus's mean and 2,686.6 short of
the prompt it was given, the ordinary behaviour-cloning shortfall.

**Nothing in this packet interprets past A19's sentence.** The anchor is a measurement reported beside
H3's clauses, not a verdict on P7.3a's number, and A18(b)'s absent online SUMO anchor stands as a
limitation to be named in the paper's C3 section rather than in a footnote.

---

## 9. B3.4 — the two provenance caveats

1. **The pilot's chunks name a commit that no longer exists.**
   `output/p7_3b_runs/preflight_pilot_anchor.txt` records `git_commit:
   5849d59a799400c807f8d3205084e043bf41f9f6` for the four pilot cells; that commit was amended into
   `f13358e` and is now dangling. Checked in this session, read-only: `5849d59` is **not an ancestor of
   HEAD**. **No recovery was attempted.** `HANDOFF_2026-09-19.md` §4.1 records that the only
   `offline/transfer_curve.py` difference between the two commits is **one prose string inside
   `run_pilot`'s fenced record, no logic** — that sentence is quoted from the handoff, not measured
   here, and it cannot be measured now. The four pilot cells live outside the campaign work directory
   and are not among the 700 verified above, every one of which records `f13358e`.
2. **The pilot's rate, with A11's label.** `canary 0.76 s`, 4 cells on the fenced smoke draw, wall
   **19.53 s**, effective 4.883 s/cell, in-process mean 17.02 s/cell, speed-up 3.49×, at
   2026-09-18T22:42:19+02:00. The driver's own header warns that **4 cells do not saturate 12
   workers**, so that speed-up is not the campaign's; the campaign's is §5.2's 11.90×.

---

## 10. B3.5 — findings carried as findings, with their caveats

1. **`run_pilot`'s `int(cell["seed"])` defect**, found by the pilot at the pre-flight: only an anchor
   cell could reach it (`seed = None` on a baseline cell). Fixed then; the fix is in the code this
   campaign ran (`f13358e`) and the pilot transcript above is the one produced after it.
2. **The stage-identity hazard and its closure:** 200 chunk names are shared between the P7.3a and
   P7.3b campaigns, and stage is now part of the cell identity, so the two campaigns' cells cannot
   collide. Verified at the 3.4+3.5 review (2026-09-18) with `declared_cells(None)` still partitioning
   4,700 element for element; **not re-run today**. What this session did measure is the consequence:
   the artifact records `n_chunks_outside_stage: 0` and `cell_set_source: "declared_cells()"`.
3. **The shift-by-one reward/RTG convention (Amendment D1) — and the check that is wrong by design.**
   `run_cell` records `info["intersections"][ix]["reward"]` **before** calling `agent.act`, and by
   contract C6 the `info` in hand at decision *t* describes the state after step *t−1*. So
   `reward_series` is shifted by one: `reward_series[0]` is `-0.0` on **500/500** cells here (measured
   this session), and `episode_reward == sum(reward_series)` is **false by construction** — the final
   step's reward is never in the series. The coordinator's first verification pass asserted exactly
   that relation and reported 500/500 "failures" before diagnosing its own error
   (`P7.3b_CAMPAIGN_READ_2026-09-19.md` §3). The relation that *is* true is the RTG identity, and it
   holds bitwise (§5.6). **A future checker must not re-introduce the wrong assertion.**
4. **1,400 `Retrying in 1 seconds` lines in `cells_anchor.log` — exactly 2 per cell** (counted this
   session). Documented cause, and **two citation corrections to the coordinator's note, made by
   reading the files**: the sentence *"Until SUMO starts, TraCI prints 'Retrying in 1 seconds'"* is at
   **`utils/sumo_utils.py:49`**, not `:56` (line 56 is an unrelated deprecated TraCI call, the one the
   suite's `UserWarning` points at); and `_TRACI_START_NUM_RETRIES = 120` is not in `utils/sumo_utils.py`
   at all — it is **`envs/sumo_env.py:29`**, passed as `numRetries=` at `envs/sumo_env.py:196`. The
   substance is unaffected and was re-measured here: P7.3a's two stage logs carry **2,400** and
   **7,000** retry lines against **1,200** and **3,500** chunks, i.e. **exactly 2.0 per cell** on both
   stages, the same ratio as this campaign's 1,400 / 700. Not a finding, and measured rather than
   inferred from the wording.
5. **A defect in my own checker, disclosed in full** — see §13.

---

## 11. `BRIEF_38` §8's remaining items

**The disjointness assertion's output** (`output/p7_3b_runs/disjointness_201_400.json`, written
2026-09-18; re-read today): `disjoint: true`, 200 probe draws {201…400}, overlap **0** against each of
four sources — `mappo1000`'s checkpoint `training_draw_ids` (200), `mix50`'s (200),
`p4_7_declaration.tiers.mix50.training_draws` (152) and the held-out pool (100), union 300. The
campaign's own `verify_inputs` stage re-asserted it at **2026-09-19T11:31:21**: `draws 201-400
disjoint True 202 files re-verified`.

**The five-draw re-collection check** (2026-09-18, re-read today): draws 301/325/350/375/400 recollected
into a scratch directory, compared on float64 episode return, `engine_seed_drawn`, episode sha256 and
all 23 arrays — **all five identical on all four routes**, returns −22,676 / −23,235 / −22,255 /
−21,748 / −21,063, `engine_seed_requested 1000`, `engine_seed_drawn 437485271`. The `.npz` container
digests differ, as the note records, because `np.savez_compressed` writes wall-clock times into the zip
headers and the scratch run's episode indices differ; the comparison is over content, which is what
determinism claims.

**A17(f) on the 201–300 half after its `SHA256SUMS` existed** (2026-09-18, re-read today):
`A17(f) 100/100`, `all_match: true`, `n_checked 100`, `n_matching 100` — the gate is indifferent to the
new digest file, demonstrated rather than argued from a reading of `dataset.py`.

**Both corpus digest files and the training artifact's per-seed digests:** §5.8 and §5.3.

**Amendment A5's collision, stated plainly.** The proof that the anchor's prompt is its own is the
**route** (`load_calibration` monkeypatched to raise), never a value. On the 201–300 half the naive
in-domain target and A17's Rule A `q = 1.0` are the same number by construction — both are the maximum
of the same 100 SUMO probe returns, **−20,809.0**, which is what `docs/data/p7_2b_calibration.json`
records as `a_q1.0`'s `target_rtg` for **both** subjects. On the full 200 episodes they are **not** the
same: the anchor's target is **−20,625.0**, the maximum of the 301–400 half. Measured this session from
the corpus manifests, a fourth route independent of the module's two and of the coordinator's
`np.cumsum` over the raw episodes: max episode return **−20,625.0** = `target_rtg`, largest absolute
return **25,257.0** = `rtg_scale`.

**Amendment A4:** the `random` arm was **not** re-rolled; ρ's denominator is fixed-time and MaxPressure
only. P7.3a's 500 `random` cells stay where they are, and the artifact says so, so no reader infers a
`random` comparison this stage did not run.

---

## 12. Self-review checklist (Y/N)

- Contracts in `docs/CONTRACTS.md` honored, including the non-standard env API? **Y** — this session
  ran no env; the campaign it writes up was verified against C6 at its own gates, and §10.3 records the
  C6-derived convention this packet relies on.
- All tests actually executed and green? **Y** — 2,136 passed, 94 skipped, 0 failed, after the copy.
- Zero modifications to frozen files (proved by the diff stat above)? **Y** — grep over
  `git diff --name-only main...HEAD` returns no frozen path.
- Zero new dependencies? **Y** — the checker uses stdlib + numpy only.
- Every number in this packet produced by a command run in this session? **Y for every load-bearing
  number**, with these labelled exceptions, all read (not re-run) from files on disk and dated in place:
  the 2026-09-18 disjointness JSON, the five-draw re-collection comparison, the A17(f) log, the pilot
  transcript, the training stage's log lines, and the P7.3a zero-shot ρ values (read this session from
  the committed P7.3a artifact of 2026-09-18).
- Was anything rolled, trained, or token-consuming? **N** — nothing.
- `§6`'s box ticked? **N** — deliberately. The coordinator ticks it in the merge commit (B4).
- English-only rule? **Y** — `bash scripts/check_english.sh` on both files this session touched: **exit 0**
  each. Also run in this session: `bash scripts/check_test_hygiene.sh` → **exit 0**, and
  `bash scripts/claude_guard.sh --frozen-only` → **exit 0**.

---

## 13. A test I changed — none. A checker of my own I changed — disclosed in full

**No repository test was edited, weakened or skipped in this session.** What follows concerns
`output/p7_3b_runs/impl_verify/verify_impl.py`, a scratch script this session wrote, and it is
disclosed because it *failed first* and the diagnosis matters.

**What failed.** On its first run, four of ten per-seed ρ comparisons reported `FAIL`: my recomputed
per-seed means differed from the artifact's in the **last one or two ULP** (e.g. seed 303 `e_sumo`:
mine `0.9798954351423359`, published `0.9798954351423358`).

**The diagnosis — my aggregation route, not the artifact.** Measured, not argued: all **1,000**
per-cell ρ values are bit-identical to the artifact, so the inputs agree exactly. The artifact's
per-seed mean is the one quantity aggregated by Python's builtin `sum` (`transfer_curve.py:1767`,
`float(sum(usable) / len(usable))`) rather than by `dt_gate.mean_ci95`'s numpy route. Since 3.12
`sum` is Neumaier-compensated, so it equals `math.fsum` — the exactly-rounded result — while numpy's
pairwise summation and a naive loop differ in the last ULP. Checked on all ten quantities: **`sum` and
`math.fsum` agree with the published value on 10/10**; the largest disagreement between *any* of the
four routes and the published value is **5.551115123125783e-16**.

**What I changed, and why it is a strengthening rather than a suppression.** The check now asserts
that the **exactly-rounded** route reproduces the published value exactly (10/10), reports all four
routes, and adds a bound asserting the inter-route spread is at the last ULP. And because a
route-tolerant check would have been weak, §5.4's per-cell comparison was **added** — 1,000 exact
equalities — which mutation 1 (§5.10) shows is the only one of the two that catches a single-ULP
corruption. A second, unrelated first-run failure was a `KeyError` from my wrong guess at
`in_support_counts`' key names (`above` / `below` rather than `n_decisions_above` /
`n_decisions_below`); corrected, and the check was extended to compare against the artifact's own
`observed_decisions` block.

**Nothing in the artifact, the chunks or the repository was modified in response to either.**

---

## 14. Deviations from the brief

1. **B1's `git merge` and the Definition of Done's `git push` conflict with this session's own
   operating rules**, which forbid both. Per `CLAUDE.md` §4b the conflict was **raised before the first
   command** and ruled on by the author in this session: merge yes (forward-integration into a task
   branch is not what the rule guards against; the permission layer puts `git merge` under *ask*),
   push yes, and the Stage B plan addendum yes. Recorded in `docs/plans/p7.3b.md` §B.0.
2. **One commit more than B's Definition of Done lists** — the plan addendum (`9248f4e`), authorised in
   the same ruling. `--amend` was never used; `git add -A` was never used.
3. **The regeneration ran in the run worktree, not in this one** (§5.7), on the author's ruling at the
   plan gate, with the merged tree's refusal recorded as the evidence for why.
4. **The Wilcoxon p-values reuse the repository's helper** rather than being re-implemented (§5.5),
   stated explicitly at the point of use.

## 15. Conflicts found between brief and repo (repo wins)

**None that changed an implementation.** One correction of record: Amendment B2 says *"a test anchored
on the committed artifact's presence or content can only be exercised once the file exists"*. In this
repository **no such test exists** — the three references to `p7_3b_anchor.json` in `tests/` are a
`tmp_path` out-path and two source-text assertions (§6). The suite was still run after the copy, as
instructed; the count is unchanged at 94 skips because of this, and the brief's expectation is recorded
here rather than left to be inferred from a number that did not move.

## 16. Limitations of what is shipped

- **Single network.** Every number is `cityflow1x1`, one intersection. The claim constraint of
  2026-08-19 on single-network coordination results applies unchanged. grid4x4 is P7.3d.
- **One engine seed.** `engine_seed_requested 1000` → `engine_seed_drawn 437485271` on all 700 cells
  (A18(c)). Draw-to-draw variation is demand variation, not engine-seed variation.
- **No online SUMO anchor** (A18(b)): the anchor bounds what target-domain probe data buys the
  architecture and nothing else. It is **not** a ceiling on achievable SUMO performance.
- **The co-reported `att_env` definition has a small denominator** (9.80–81.19 s against the primary's
  197.96–299.72 s) and must be read with `denominator_diagnostic` beside it; its CI is correspondingly
  wide ([0.9217, 1.0464]).
- **This packet is not a merge review.** No mutation testing of the campaign's *code* was performed in
  this session; §2's five pins and the stage-identity fix were killed on 2026-09-18 and are in the
  Decisions Log. §5.10's mutations test **my checker**, not the shipped code.
- **The pilot's commit cannot be checked out** (§9.1), so the claim that the runner difference is
  prose-only is quoted, not verified.

## 17. Open questions / risks for the Master chat

1. **`PREREGISTRATION.md` is outside `docs/`, so registering an amendment makes every committed
   artifact non-regenerable from a merged tree** (§5.7). The refusal is correct — J1(c) cannot know
   that a root-level Markdown file is documentation — but it means *"regenerate the artifact at its
   recording commit"* now requires a detached worktree for any campaign whose branch has merged a
   registration. Three options, none taken here: teach `code_changed_since` a documentation allow-list
   beyond the `docs/` prefix; move `PREREGISTRATION.md` under `docs/` (history-visible, affects every
   existing reference); or write the constraint down as a procedure. **This needs a ruling before
   P7.3c/P7.3d, not after.**
2. **Should the per-seed ρ mean be computed by the same helper as every other mean?** It is the only
   aggregate in the artifact that uses Python's `sum` rather than `dt_gate.mean_ci95` (§13). Nothing is
   wrong — `sum` is the *more* accurate route — but two routes in one artifact is a seam a future
   reviewer will trip on, exactly as this session did.
3. **The 27 MB campaign work directory and the 65 MB P7.3a artifact are both load-bearing evidence
   that lives in exactly one place.** `output/` is gitignored. Not a defect of this task; raised
   because §7 of the plan calls an output that exists in one place *not a record*.

## 18. AI-assistance record (`CLAUDE.md` §8)

1. **Tool and version:** Claude Code, Claude Opus 5 (1M context), 2026-09-19.
2. **Authorship per file:** `docs/returns/P7.3b.md` — AI-authored this session. `docs/plans/p7.3b.md` —
   the Stage B section AI-authored this session; sections 0–8 AI-authored on 2026-09-18 and unchanged.
   `docs/data/p7_3b_anchor.json` — **machine-generated**, the campaign's own output, copied unmodified.
   `output/p7_3b_runs/impl_verify/verify_impl.py` — AI-authored this session (scratch, uncommitted).
   No repository source or test file was authored or edited in this session.
3. **Human verification performed:** the author ruled on the three standing-rule conflicts (§14.1)
   before any command ran and approved the plan. The coordinator independently verified the campaign
   from disk on 2026-09-19 (`docs/notes/P7.3b_CAMPAIGN_READ_2026-09-19.md`), including an independent ρ
   recomputation and a byte-identical regeneration. In **this** session, run by the AI and reported
   above: the full test suite (2,136 passed / 94 skipped); 1,000 per-cell ρ equalities; both aggregate
   ρ definitions with CIs; the paired ATT block; the RTG identity by `np.cumsum`; 600 bit-identity
   comparisons against P7.3a; 713 + 101 + 101 digest verifications; a byte-identical regeneration; and
   three mutations of the checker, all killed.
4. **Who made the research decisions:** **pre-registration, before the run** — A18(a) (the anchor's
   recipe, prompt rule, seeds and held-out pool; tagged `v1.8-prereg-a18`, `045e9be`), A18(b)–(c), A15
   (the ρ pair), A16, A17; and **A19** (H3 clause 3 void, the anchor's framing, the few-shot
   expectation), tagged `v1.9-prereg-a19` → `443c076`, registered **2026-09-19 before this packet
   existed**. Coordinator rulings: `BRIEF_38` A1–A7 (plan gate) and B1–B4 (this task). Author rulings:
   the P7.3b/c/d split (2026-09-18), the single merge review, and this session's three (§14.1).
   **No research decision was made by the AI in this session.**

---

## 19. What the next tasks will assume about this one

**P7.3c (the few-shot curve, deferred not cancelled) will assume:**
- **A19's registered expectation**, written before any few-shot point exists: ρ **degrades
  monotonically** with k from the zero-shot +1.89 / +1.73 toward the anchor's **+0.9830**, and the
  curve measures **interference**, not gap-closing. Refutation conditions are registered in A19(b).
- The **200-episode SUMO corpus** it fine-tunes on: `hz1x1_sumo_maxpressure` (`SHA256SUMS`
  `def8fb30…`) + `hz1x1_sumo_maxpressure_301_400` (`93f180e0…`), both verified 101/101 today; pure
  MaxPressure, `epsilon: None`; returns mean −23,099.17, max −20,625.0, largest |return| 25,257.0.
- The **naive in-domain prompt** `target_rtg −20625.0`, `rtg_scale 25257.0`, and that it is **not**
  `p7_2b_calibration.json`'s `a_q1.0` (−20,809.0) — which it *was* by construction on the 201–300 half
  alone (Amendment A5).
- The **five anchor checkpoints** at the digests in `docs/data/p7_3b_anchor_training.json`
  (`edca65f9…`), 40,000 steps each, `raise_to: null`.
- **`f13358e` is the commit every one of the 700 chunks was rolled at**, and §17.1's regeneration
  constraint applies to any artifact it commits.

**P7.3d (grid4x4) will assume:** the ρ machinery, the cell/chunk/stage identity and the refusal set as
they stand at this merge; that the 200 re-rolled denominators put P7.3a's and P7.3b's ρ on one scale
(§5.9); and that every claim here is `cityflow1x1`-only.

**The coordinator's remaining steps** (`BRIEF_38` §5.5, unchanged by this packet): one merge review;
merge with §6's box ticked; both refs pushed; and the CI skip ceiling re-measured by the registered
route, which `re_measure_required_at` names as **this** merge.
