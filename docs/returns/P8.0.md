# RETURN PACKET — P8.0 (horizon metric + anchor re-derivation)
**Date:** 2026-08-06 · **Mode:** Claude Code · **Contracts:** v1.1

**Status:** DONE (in-session half). The MAPPO horizon cells are **PENDING** the user's tmux re-run by
design (Master Ruling 1 / Option A); they are recorded explicitly as pending, never as a silent row.
The merge gate — exact baseline reproduction — holds.

**Branch + diff stat**
```
task/p8.0-horizon-metric
$ git diff --stat main...HEAD
 docs/data/p0_baselines_horizon/PROVENANCE.md |  20 ++
 docs/data/p0_baselines_horizon/results.json  | 204 ++++++++++++++++++
 docs/data/p0_baselines_horizon/summary.csv   |   5 +
 docs/plans/p8.0.md                           | 139 ++++++++++++
 offline/horizon_metric.py                    | 132 ++++++++++++
 offline/policies/plan_replay.py              |  51 +++--
 offline/rederive_anchors.py                  | 310 +++++++++++++++++++++++++++
 offline/rederive_tier1.py                    | 124 +++++++++++
 tests/test_fixed_time_env_mapping.py         |  37 ++--
 tests/test_horizon_metric.py                 | 119 ++++++++++
 tests/test_offline_naming_guard.py           |  85 ++++++++
 tests/test_p0_baseline_anchors.py            | 139 ++++++++++++
 tests/test_rederive_anchors.py               |  31 +++
 13 files changed, 1359 insertions(+), 37 deletions(-)
```
`git diff --stat` shows **no frozen path** (all under `offline/`, `tests/`, `docs/`).
`scripts/claude_guard.sh --frozen-only` → `exit=0`.

**Files produced**
- `offline/horizon_metric.py` — `horizon_rollout(env, choose_action, episodes, seed)`: reports
  `att_horizon` (A1 primary) and `att_running_mean` (legacy) from **one** rollout; loop mirrors the
  frozen `evaluate_policy` line-for-line.
- `offline/rederive_anchors.py` — anchor re-derivation harness. Reuses frozen `experiments.runner`
  helpers (import-only, site comment), refuses the historical dir, writes `results.json` +
  `summary.csv` + `PROVENANCE.md` atomically after computing every cell.
- `offline/rederive_tier1.py` — re-issues P2.5 k=3/k=4/shipped/grid4x4 under `att_horizon`.
- `offline/policies/plan_replay.py` — `ReplayResult.average_travel_time` → `att_horizon` +
  `att_running_mean`; the three false "canonical measure" docstrings corrected.
- `tests/test_offline_naming_guard.py` — AST naming guard + positive control.
- `tests/test_horizon_metric.py` — reader↔env and reader↔`evaluate_policy` agreement (CityFlow).
- `tests/test_rederive_anchors.py` — historical-dir guard (engine-free).
- `tests/test_p0_baseline_anchors.py` — horizon double-compute + baseline reproduction gate +
  mutation; legacy exact double-compute kept untouched.
- `tests/test_fixed_time_env_mapping.py` — §9 gate flipped to `att_horizon` (exact, both quantities).
- `docs/data/p0_baselines_horizon/{results.json,summary.csv,PROVENANCE.md}` — re-derived anchors.
- `docs/plans/p8.0.md` — approved plan.

**Tests — REAL OUTPUT**
```
$ .venv/bin/pytest -q
320 passed, 2 skipped, 12 warnings in 36.42s
```
Count vs the DoD's 299: **+23 tests** (299 → 322 collected; 320 pass, 2 skip). The 2 skips are the
MAPPO horizon double-compute cells — a reasoned skip that auto-arms when the tmux data lands.

Red-before-green was confirmed: before implementation the same set ran **23 failed, 14 passed**, with
the failures being `NotImplementedError` (reader/guard reached the real API), `AttributeError: no
attribute 'att_horizon'` (rename pending), missing-file `AssertionError` (data pending), and the
naming guard catching the real pre-existing violation in `plan_replay.py`.

Load-bearing checks (real output):
```
$ .venv/bin/python -m offline.rederive_anchors --policies random,max_pressure   # 40 s, no training
cf_hz1x1   MaxPressure  att_running_mean=160.5584   (committed anchor 160.5584)   EXACT
cf_hz1x1   Random       att_running_mean=307.5346   (committed anchor 307.5346)   EXACT
cf_grid4x4 MaxPressure  att_running_mean=141.6547   (committed anchor 141.6547)   EXACT
cf_grid4x4 Random       att_running_mean=207.2605   (committed anchor 207.2605)   EXACT
```
Guards: `check_test_hygiene.sh` → exit 0; `check_english.sh` → exit 0; `claude_guard --frozen-only`
→ exit 0.

**Self-review checklist**
- Contracts honored, incl. non-standard env API (`reset`→info only, `step`→reward first)? **Y** — the
  reader only reads `info` and calls the documented `step`/`reset`.
- All tests actually executed and green? **Y** — full tail pasted; 320 passed, 2 skipped (pending
  MAPPO, by design).
- Zero modifications to frozen files (proved by diff stat + `--frozen-only` exit 0)? **Y**.
- Zero new dependencies? **Y** — numpy / stdlib / existing repo modules only.
- Every number in this packet produced by a command run in this session? **Y**.
- Critical quantities double-computed with exact `==`? **Y** — horizon double-compute recomputes
  `np.mean(cells)` vs stored aggregate; reader agreement recomputes from raw samples and against the
  frozen `evaluate_policy`; both exact `==`, mutation-proven.

**Deviations from the brief**
- None material. The brief allowed omitting a data file for §3.5; the Tier 1 numbers are reported here
  (Q4) and reproduced by the committed `offline/rederive_tier1.py`, not a committed JSON.

**Conflicts found between brief and repo** (repo wins)
- Brief wanted the full anchor re-run (incl. MAPPO training) done as one deliverable; CLAUDE.md:203
  routes MAPPO training to the user's tmux. Resolved by Master Ruling 1 (Option A split): baselines
  in-session, MAPPO in tmux. Implemented that split.
- Tests I changed, disclosed in full:
  1. `tests/test_p0_baseline_anchors.py` — **added** horizon assertions; the legacy exact-`==`
     double-compute is byte-for-byte unchanged (kept as immutable history per §3.3).
  2. `tests/test_fixed_time_env_mapping.py` — **flipped** the §9 gate from the episode-mean to
     `att_horizon` (brief §3.4 mandate), keeping **exact** equality and now asserting **both**
     `att_horizon` and `att_running_mean`. Not a weakening: `662.36` held on both sides historically;
     the gate still passes exactly. No assertion was suppressed.

**Task-specific answers (brief §7)**
1. **Did the re-run reproduce each committed anchor?** Baselines: **yes, exactly**, per policy and per
   seed — MaxPressure 160.5584 (std 0) and Random 307.5346 (std 0.6050) on cf_hz1x1; MaxPressure
   141.6547 (std 0) and Random 207.2605 (std 1.9336) on cf_grid4x4. MAPPO: **not run in-session**
   (tmux, Option A).
2. **Did MAPPO cross the N2 boundary?** **UNRESOLVED in-session — pending the tmux re-run.** It is not
   answerable without training MAPPO, which CLAUDE.md:203 routes to tmux. The harness records the two
   MAPPO cells as `pending_policies` and the anchor test skips-with-reason until they land.
3. **Re-derived §3.1 table (both quantities), from `docs/data/p0_baselines_horizon/summary.csv`:**

   | env | policy | att_horizon (A1) | att_running_mean (legacy) |
   |---|---|---|---|
   | cf_hz1x1 | MaxPressure | 247.75 | 160.56 |
   | cf_hz1x1 | Random | 413.53 | 307.53 |
   | cf_hz1x1 | mappo | *pending tmux* | *pending tmux* |
   | cf_grid4x4 | MaxPressure | 169.05 | 141.65 |
   | cf_grid4x4 | Random | 265.75 | 207.26 |
   | cf_grid4x4 | mappo | *pending tmux* | *pending tmux* |

   A1 confirmed in the data: on cf_hz1x1, MaxPressure's advantage over Random reads
   `(413.53−247.75)/413.53 = 40.1%` under the horizon vs `(307.53−160.56)/307.53 = 47.8%` under the
   running mean — the two aggregations move the effect size, as A1 predicted.
4. **P2.5 Tier 1 under `att_horizon`** (from `.venv/bin/python -m offline.rederive_tier1`; single seed
   101 / draw 0, replay seed 0; entered = completed + still-in-network at horizon):

   | env | variant | att_horizon | att_running_mean | entered | completed |
   |---|---|---|---|---|---|
   | cf_hz1x1 | k=3 | 309.60 | 235.90 | 1595 | 1440 |
   | cf_hz1x1 | shipped | 300.91 | 231.50 | 1623 | 1465 |
   | cf_hz1x1 | k=4 | 285.42 | 216.92 | 1661 | 1506 |
   | cf_grid4x4 | k=4 | 292.28 | 232.23 | 1473 | 1429 |

   Every `att_running_mean` and every entered/completed count reproduces P2.5's committed figures
   exactly. The k=4 ruling is **not** reopened. Note: the `att_horizon` column (309.60/300.91/285.42)
   is precisely P2.5's original "preview-gate" numbers, which P2.5 had flagged as the final/horizon
   value — so A1 promotes exactly those to the primary metric. Ordering is preserved under both
   aggregations: hz1x1 `MaxPressure < k4 < shipped < k3 < Random`; grid4x4 stays inverted
   (`MaxPressure < Random < fixedtime-k4`). Entered spread on hz1x1 (1595–1661, ~4%) is within the
   prereg §3.1 5% validity band.
5. **Anything in the naming ruling that did not survive contact with code?** No. The bare name
   `average_travel_time` survives cleanly as the registry-metric string in *read* positions
   (`info["average_travel_time"]`, `get("average_travel_time")`, `metric_names` lists); the AST guard
   allows exactly those and flags attributes/bindings/args/dict-keys. One legitimate internal reach —
   `plan_replay` reading `metrics._episode` for entered/completed — is unchanged (no public accessor
   exists) and is not a naming issue.

**Open questions / risks for the Master chat**
1. **The MAPPO tmux run must land before any phase compares against the anchors (P4+).** Command:
   `.venv/bin/python -m offline.rederive_anchors` (full; overwrites the file with all 3 policies).
   After it lands, `tests/test_p0_baseline_anchors.py::test_mappo_horizon_double_compute_when_present`
   auto-arms (no test edit needed), and the N2 question (Q2) is answerable by comparing the new MAPPO
   `att_running_mean` to the committed 197.91 / 632.95.
2. The Random cf_hz1x1 horizon (413.53) differs from the coordinator's 2026-08-05 rollout figure
   (429.67) because that rollout measured a slightly different setup; the **committed anchor** (307.53
   running mean) is what I reproduced exactly, and 413.53 is its horizon under the identical pipeline.
   Flagging so the 429.67 is not mistaken for a target.
3. `offline/rederive_tier1.py` uses the equal-split cycle (`plan=None`); verified in-session that
   `plan=shipped` gives identical hz1x1 numbers (greens are ascending), so the choice is immaterial.

**What the next task will assume about this one**
- `att_horizon` is the paper's primary metric; `att_running_mean` is legacy-only and never called
  "average travel time". No episode-level artifact in `offline/` is named bare `average_travel_time`
  (mechanically enforced).
- `offline.horizon_metric.horizon_rollout` is the sanctioned reader; its `att_running_mean` is
  byte-identical to the frozen `evaluate_policy`.
- The re-derived baseline anchors in `docs/data/p0_baselines_horizon/` are trustworthy by
  construction (exact running-mean reproduction); the MAPPO rows are pending the tmux run and must
  land before P4+ comparisons.
