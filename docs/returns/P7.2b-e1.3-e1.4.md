# RETURN PACKET — P7.2b Amendments E1.3 + E1.4
**Date:** 2026-09-15 · **Mode:** Claude Code · **Contracts:** v1

**Status:** DONE

⚠️ **This is not the P7.2b packet.** It covers only E1.3 and E1.4. `docs/returns/P7.2b.md` is
written after run 4, against the whole of `BRIEF_36` + Amendments A–E. Run 4 is the author's.

**TIP SHA: `5dfaaa28b9fc134a2f876ebc34669c0f2da5f3be`** — this is what run 4's provenance must read.

---

## Branch + diff stat

```
task/p7.2b-calibration   (checked out at /home/filip/rltraffic-p53b)

$ git log --oneline 8599d2a..HEAD
5dfaaa2 fix(P7.2b E1.3): MD1b -- deleting the canary LINE's own append left the driver test green
b261268 fix(P7.2b E1.3): ME2 SURVIVED -- the missing-canary.json test matched on the file NAME, which ENOENT supplies
9fd6017 fix(P7.2b E1.3+E1.4): two assertions a comment could satisfy, and this run's canary from canary.json
6f3e540 plan(P7.2b E1.3+E1.4): the two prose-satisfied assertions, and this run's canary from a manifested canary.json

$ git diff --stat 8599d2a..HEAD
 docs/plans/p7.2b_e1.3_e1.4.md          | 242 ++++++++++++++++++++++++
 offline/campaigns/p7_2b_calibration.sh |  18 +-
 offline/transfer_calibration.py        | 248 +++++++++++++++++++++---
 tests/test_transfer_calibration.py     | 333 ++++++++++++++++++++++++++++++++-
 4 files changed, 802 insertions(+), 39 deletions(-)

$ git diff --stat main...HEAD          (the whole branch, for context)
 docs/plans/p7.2b.md                    |  352 ++++++++
 docs/plans/p7.2b_e1.3_e1.4.md          |  242 +++++
 docs/plans/p7.2b_state_2026-09-15.md   |  155 ++++
 offline/aligned_env.py                 |  179 ++++
 offline/campaigns/p7_2b_calibration.sh |  247 ++++++
 offline/transfer_calibration.py        | 1522 ++++++++++++++++++++++++++++++++
 tests/test_aligned_env.py              |  192 ++++
 tests/test_transfer_calibration.py     | 1261 ++++++++++++++++++++++++++
 8 files changed, 4150 insertions(+)
```

Frozen set: `git diff --name-only 8599d2a..HEAD` matched none of `envs/ agent/base.py
agent/utils/utils.py agent/MAPPOAgent.py algorithms/ rewards.py states/ metrics/ utils/ CityFlow/
experiments/**.py scripts/ .claude/`. `bash scripts/claude_guard.sh --frozen-only` → **exit 0**.

## Files produced / changed

| path | what |
|---|---|
| `docs/plans/p7.2b_e1.3_e1.4.md` | the plan, first commit of this sub-task |
| `offline/transfer_calibration.py` | `format_canary_line`, `parse_canary_line`, `record_canary`, `_read_canary_record`, `CANARY_FACT_NAMES`, `CANARY_RECORD_NAME`, the `record-canary` subcommand, the rebuilt canary block in `report`, the chunk-commit refusal |
| `offline/campaigns/p7_2b_calibration.sh` | `tee` → `tee -a`; the `record-canary` invocation after the token |
| `tests/test_transfer_calibration.py` | 6 new tests + 12 new parametrised cases; 2 strengthened; 1 fixture extended; 1 assertion changed (disclosed below) |
| `docs/returns/P7.2b-e1.3-e1.4.md` | this packet |

Evidence kept outside the manifest, in `/home/filip/rltraffic/output/p7_2b_runs/` (gitignored,
outside the manifest's `find p7_2b`, survives a reboot — `PROJECT_PLAN` §7's rule):

```
e029037c27b206da8144d983548f5b54df012ed456f6d2a34b7f45ab37ffd5d4  e13e14_verify_session.txt
99db630bdf40f4311b4e168e8924e58155e14387ee0ee0e5c7607c07e9816bfe  e13e14_mutants.py
9bbab782378bb82347339c93fbe641949ed4a6388319a3232dc89c552144aedb  e13e14_mutation_report.json
67731f384b0169eb483bb0f31415ae2cbb5dba3602830e8f6324d3014f8751ce  e13e14_mutation_stdout.txt
7d2d983ccbf6f37b1f2131b731dd10579bc4df342a169a853b8d2c2598253ecf  e13e14_preview_artifact.json
4cd97c52769a5dd5173467eef38b2406b3984a1fef92c2a0e077226d6ce88fe2  e13e14_preview_canary.json
```

## Tests — REAL OUTPUT

**Red first.** After the signature-only skeletons (`raise NotImplementedError`) and before any
implementation, every new test reached the real API surface — no shared `ModuleNotFoundError`:

```
$ .venv/bin/python -m pytest tests/test_transfer_calibration.py -q --tb=line
...
E   NotImplementedError                        offline/transfer_calibration.py:1300   (format_canary_line)
E   NotImplementedError                        offline/transfer_calibration.py:1310   (record_canary, x9)
E   Failed: DID NOT RAISE FileNotFoundError    tests/test_transfer_calibration.py:835
E   Failed: DID NOT RAISE ValueError           tests/test_transfer_calibration.py:863 (x4)
E   Failed: DID NOT RAISE ValueError           tests/test_transfer_calibration.py:878
E   assert 0.9 == 0.5                          tests/test_transfer_calibration.py:904
E   assert 0.9 == 0.5                          tests/test_transfer_calibration.py:993
E   AssertionError: the canary stage's output must be tee'd to stderr, APPENDING: ...
19 failed, 38 passed, 3 warnings in 19.05s
```

`assert 0.9 == 0.5` twice **is** the defect E1.4 exists to remove, stated as a number: `report`
published the probe chunks' canary where the reporting run's belonged.

**Green, at the tip `5dfaaa2`:**

```
$ .venv/bin/python -m pytest tests/test_transfer_calibration.py tests/test_aligned_env.py -q
62 passed, 5 warnings in 21.50s

$ .venv/bin/python -m pytest -q
1957 passed, 93 skipped, 25 warnings in 165.96s (0:02:45)
```

46 → **62** on the two P7.2b files (baseline 46 measured in this session at `8599d2a`).

```
$ bash scripts/check_test_hygiene.sh tests/test_transfer_calibration.py   ; exit 0
$ bash scripts/check_english.sh <the four changed/added files>            ; exit 0
$ bash scripts/claude_guard.sh --frozen-only                              ; exit 0
$ bash -n offline/campaigns/p7_2b_calibration.sh                          ; exit 0
```

## Mutation round — 14 mutants, 13 killed one-to-one, 1 survivor reported

Run in a **throwaway detached worktree** `/home/filip/rltraffic-p72bmut` at the tip, `git checkout
-- .` and a `git status --porcelain` emptiness assertion after every mutant, worktree removed
afterwards. The p53b tree was never mutated. Driver: `output/p7_2b_runs/e13e14_mutants.py`;
stdout: `e13e14_mutation_stdout.txt`; per-mutant detail: `e13e14_mutation_report.json`.

```
BASELINE: 62 passed, 0 failed
MC8   killed    failed= 1  collateral=[]   (check_canary moved above the print in main)
MD2   killed    failed= 1  collateral=[]   (`| tee -a /dev/stderr` deleted from the driver's code line)
ME1   killed    failed= 4  collateral=[]   (the check_canary re-check dropped from report)
ME2   killed    failed= 1  collateral=[]   (the missing-canary.json refusal dropped)
ME3   killed    failed= 1  collateral=[]   (record_canary writes without _git_provenance())
ME4   killed    failed= 2  collateral=[]   (report reads the chunks' canary as if it were this run's)
ME5   killed    failed= 1  collateral=[]   (the chunk git_commit agreement refusal dropped)
ME6   killed    failed= 2  collateral=[]   (parse_canary_line stops requiring the four fact names)
ME7   killed    failed= 8  collateral=[]   (record_canary mkdirs before parsing -- the barrier)
MD1   killed    failed= 1  collateral=[]   (both canary.log writes moved above the token)
MD1b  killed    failed= 1  collateral=[]   (the canary LINE's own append deleted)
MD1c  SURVIVED  failed= 0                  (the timestamp HEADER line removed)
MD3   killed    failed= 1  collateral=[]   (record-canary moved above the token)
MF1   killed    failed= 2  collateral=[]   (the canary line reverts to a Python repr)
SURVIVORS: ['MD1c']
```

`collateral=[]` everywhere: each mutant kills **only** the test that guards it, which is the
one-to-one property the coordinator asked for on E1.2.

**Two mutants survived the first round and both were real findings. Both are now dead.**

* **ME2 SURVIVED at `9fd6017`.** Deleting the explicit `if not path.is_file(): raise
  FileNotFoundError(...)` from `_read_canary_record` left the suite green, because
  `Path.read_bytes()` then raises the operating system's own `FileNotFoundError` whose message is
  `[Errno 2] No such file or directory: '…/canary.json'` — which contains `canary.json` and so
  satisfied `match=tc.CANARY_RECORD_NAME`. Reproduced in isolation before anything was changed
  (the message printed verbatim, `'canary.json' in str(exc) -> True`, `'is not a run' -> False`).
  **Same class as MC8 and MD2: an assertion satisfied by something other than the mechanism it
  names** — there by a comment, here by `ENOENT`. Fixed at `b261268`: the `match` is now the
  explanation `is not a run`, which no OS error can produce, with the file name asserted separately
  on the exception value.
* **MD1b SURVIVED at `b261268`.** The canary.log assertion counted appends to that file and
  required the earliest to follow the token. The log also carries a timestamp header, so deleting
  `echo "$CANARY_LINE" >> "$LOGS/canary.log"` — the write that puts the OBSERVED values on disk,
  the entire substance of E1.2 item 4 — left one matching append behind and the test passed. Fixed
  at `5dfaaa2`: the line itself is asserted as a code line, and the same form was applied to
  `record-canary --line "$CANARY_LINE"`.

**MD1c is reported, not chased.** Removing the `=== canary run at <date -Is>` header leaves the
asserted property intact: the canary line still reaches `canary.log`, appended, after the token.
It is an equivalent mutant *with respect to what the test claims*. The header is a convenience for
a human reading an accumulating log; no number depends on it, and `canary.json` now carries this
run's canary unambiguously with its own provenance. Pinning it would mean asserting a `date -Is`
format string — pinning formatting rather than substance. **Disclosed rather than fixed.**

## Verification on the real engine, not predicted

The mechanism run 4 depends on was **executed**, not reasoned about
(`output/p7_2b_runs/e13e14_verify_session.txt`). A throwaway script reproduced the driver's exact
canary pipeline into a scratch work directory, with stdout and stderr in one file opened `>>` —
the shape that truncated run 3's log:

```
PRIOR LINE 1 -- must survive the tee
PRIOR LINE 2 -- must survive the tee
=== canary, the driver's exact pipeline
canary 0.79 s {"att_horizon": 247.75089149261333, "decisions": 360, "local_return": -32648.0, "two_routes_agree": true}
SECONDS FIELD: 0.79
=== record-canary
wrote .../canary.json
=== a malformed line must refuse and create nothing
transfer_calibration: canary line's facts are not JSON: '{broken}'
refused, exit 1
ls: cannot access '.../refused': No such file or directory
no directory created -- barrier holds
```

* **the two prior lines survived** — `tee -a` fixes the `O_TRUNC` hazard, falsified in the failing
  direction by the same experiment shape that produced it;
* the canary's facts are **exactly** the references — `-32648.0`, `247.75089149261333`, `360`,
  `True` — so the engine still reproduces draw 0 on this machine today, 0.79 s;
* `awk '{print $2}'` still yields `0.79` from the JSON line, so the driver's threshold gate is
  unaffected by the format change;
* a malformed line refuses with exit 1 and **creates no directory at all**.

`scenarios/draws/cityflow1x1/draw_0000/` was byte-identical before and after (file list **and**
mtimes, `diff` of two `stat -c '%n %Y %s'` captures). `output/p7_2b/` still holds its 104 entries
and has **no** `canary.json`: the live work directory was not touched.

## Preview of run 4's `report`, against the real 100 chunks

The 100 live probe chunks and run 3's two smoke chunks were **copied** to a scratch directory and
`report` was run with `--out-dir` pointing at scratch (`e13e14_preview_artifact.json`,
`7d2d983c…`). Nothing under `output/p7_2b/` or `docs/data/` was written.

1. With no `canary.json`, `report` refused and wrote nothing — the scratch out-dir stayed empty.
2. With one parked, the artifact differs from run 3's (`run3_cfdf3829.json`) in exactly
   **`canary`, `git_commit`, `git_dirty`** and nothing else. Byte-identical: `probe`, `statistics`,
   `targets`, `subjects`, `disjointness`, `smoke`, `rule_a_secondary_values`, `format_version`,
   `registered_in`, `scenario_key`, `what_this_does_not_say`.
3. The block, reading the two canaries apart for the first time:

```
run3    canary.seconds = 0.89     <- run 1's, carried in the reused chunks
preview canary.seconds = 0.79     <- the reporting run's, from canary.json
preview canary.probe_chunks_canary = {seconds 0.89, git_commit f31973eb…,
                                      correctness_half "not recorded in these chunks"}
checked_against occurrences in the whole artifact: 1
```

**Double-computed, under `==` and not `allclose`:** `probe_chunks_canary.seconds` and
`.git_commit` were recomputed by an independent route — a direct `set()` over the 100 chunk files
on disk — giving `{0.89}` and `{'f31973ebb770597152807e536dcf3f83cd9cb47e'}`, asserted equal to
what `report` published. Passed.

## Self-review checklist

- Contracts in `docs/CONTRACTS.md` honored, including the non-standard env API? **Y** — no env call
  was added; `canary_seconds()`/`run_probe` were not touched.
- All tests actually executed and green? **Y** — 62 on the two files, 1957 passed / 93 skipped whole suite.
- Zero modifications to frozen files (proved by the diff stat above)? **Y** — guard exit 0.
- Zero new dependencies? **Y** — stdlib `json`/`pathlib`/`subprocess` only; `subprocess` is imported
  inside the one test that needs it.
- Every number in this packet produced by a command run in this session? **Y**.
- Tests written first and red for their own reasons? **Y** — pasted above, 19 distinct reasons.
- Every load-bearing change mutation-tested? **Y** — 14 mutants, table above, survivor disclosed.
- Filesystem-mutation barrier respected? **Y** — `record_canary` parses before touching the path,
  asserted by a test *and* executed for real; `report`'s refusals all precede `_write_json`.
- Any test weakened? **N** — one assertion **changed** because its specification changed; disclosed
  in full below. Two assertions strengthened. No assertion removed or waived.

## Tests changed — disclosed in full, with the diagnosis

1. **`test_report_regenerates_byte_identically_and_carries_no_fenced_quantity`**, one line:
   `assert payload["canary"]["seconds"] == 0.9` → `== THIS_RUN_CANARY_SECONDS` (0.5), **plus a new**
   `assert payload["canary"]["probe_chunks_canary"]["seconds"] == CHUNK_CANARY_SECONDS` (0.9).
   *Diagnosis:* that assertion encoded the semantics E1.4 exists to correct — `canary.seconds` used
   to mean "the chunks' canary" and now means "the reporting run's". Brief authority: **E1.4 item 4**
   ("the top-level `seconds` / `verdict` describe the run that wrote the artifact"). The replacement
   is **strictly stronger**: it distinguishes two numbers the old one conflated, and mutant ME4
   (reading the chunks' value back into the block) kills it.
2. **`test_the_canary_stage_exits_non_zero_on_a_mismatch`** — strengthened per E1.3 item 1. MC8 dies.
3. **`test_the_driver_gates_on_the_canary_before_consuming_the_token`** — strengthened per E1.3
   item 2, plus the ME2/MD1b lessons. MD2, MD1, MD1b, MD3 die.
4. **`test_report_refuses_a_work_directory_without_a_canary_record`** — its `match` changed from the
   file name to the explanation, after ME2 survived. Detailed above.
5. **Fixture, not an assertion:** `_write_band` now also writes a `canary.json`, because `report`
   requires one. Its `line` field is built by a literal f-string in the test rather than by
   `tc.format_canary_line`, so no test that merely needs a work directory depends on the formatter;
   the formatter's exact bytes are pinned against a hand-written literal in the round-trip test.
   `_synthetic_chunk`'s `0.9` became the named `CHUNK_CANARY_SECONDS` — same value, no behaviour change.

## Deviations from the brief

1. **The canary block's shape.** E1.4 item 4 does not fix a layout, and the block says *"a better
   one that meets the conditions is acceptable"*. Implemented: the canary block's **own top level is
   the this-run record** (`seconds`, `verdict`, `threshold_seconds`, `observed`, `checked_against`,
   `source`, `git_commit`, `git_dirty`, `recipe`) with the chunks' canary as one named sub-block
   `probe_chunks_canary`. All four truth-conditions hold and each is asserted by name in
   `test_the_artifact_canary_block_separates_this_run_from_the_probe_chunks`.
2. **No `re_checked_by_report: true` flag.** A boolean saying "I checked" is a self-claim that a
   mutation turns into a lie — exactly what E1.4 item 2 rejects for the chunks. Instead the artifact
   now carries `observed` **and** `checked_against` side by side, so a reader of `docs/data/` alone
   can **redo** the comparison. That is the stronger form, and ME1 proves the re-check is real.
3. **`record_canary` does not call `check_canary`.** Item 2 says `report` **re-runs** it; recording a
   failing canary's observed values is E1.2's whole point, and the driver cannot reach
   `record-canary` with a failing canary anyway (the stage exits non-zero and `pipefail` aborts
   before the token). Stated in the function's docstring.
4. **The driver's diff since `8e70d77` is not *exactly* `tee` → `tee -a`.** E1.3's closing sentence
   asked the packet to state that; **E1.4 item 1, written later, mandates the `record-canary` call in
   the driver**. The real diff is: `tee` → `tee -a` (+ its hazard comment) **and** the eight-line
   `record-canary` block after the token. Pasted in full in the commit `9fd6017` and reproduced in
   §"driver diff" of this session's log. The later amendment supersedes the earlier sentence; flagged
   rather than resolved silently.
5. **Four mutants beyond the brief's three** (ME4–ME7) plus four controls (MD1, MD1b, MD1c, MF1).
   Two of them found real weaknesses.

## Conflicts found between brief and repo

1. **`docs/plans/p7.2b_state_2026-09-15.md` is not on `main`.** The session instruction named it as a
   path to read; it exists only on `task/p7.2b-calibration` at `8599d2a`. Found and read there.
   Not a defect — the brief's §"The implementer session is at 92 % context" says the note is written
   by the implementer, and implementer commits live on the branch.
2. **E1.3's "exactly `tee` → `tee -a`" vs E1.4 item 1.** Deviation 4 above.
3. **`docs/plans/p7.2b_state_2026-09-15.md` §5.1 says E1.3's fixes "go in with the packet commit".**
   They went in as their own commits, before the packet, because E1.4 item 1 says *"before any
   artifact is committed"* and RUN 4 says *"after E1.3 + E1.4 are verified by the coordinator"* —
   run 4's provenance must be a commit that exists before run 4 starts.
4. Nothing else. The brief matched the repo everywhere I checked it.

## Open questions / risks for the coordinator

1. 🚨 **`docs/data/p7_2b_calibration.json` is still untracked in the worktree, and it will make run
   4's provenance read `git_dirty: true`** — the exact defect E2 was written to remove for run 2.
   **Measured, not predicted:** the preview artifact produced in this session reads
   `git_commit 5dfaaa28…, git_dirty True`, and `git status --porcelain` in the worktree prints
   exactly one line, that file. Its sha256 is `cfdf3829c23aac4b73a4ce7c0d83da4b13c070de43c30df37092e0539ca53a86`,
   **identical to the secured `output/p7_2b_runs/run3_cfdf3829.json`** (verified in this session), so
   removing it loses nothing. **I did not move it:** the brief assigns the run-4 procedure to the
   author, and a filesystem mutation outside my fence is not mine to make. It must leave the tree
   before run 4 starts. *Suggested, for the author to run:* `rm /home/filip/rltraffic-p53b/docs/data/p7_2b_calibration.json`
   after re-confirming that sha.
2. **`canary.log` will now hold two formats.** It is appended, never truncated, so run 4's JSON line
   will sit under run 3's Python-repr line. Both are true records of their runs; the format changed
   at `5dfaaa2`. Worth one sentence in the final packet rather than a "fix".
3. **MD1c**, above: an equivalent mutant, disclosed and not chased. If the coordinator disagrees it
   is one line to pin.
4. **`parse_canary_line` allows extra fact names** beyond the four (it requires the four, refuses if
   any is missing). A future fifth fact would flow into `observed` unchallenged. Deliberate — refusing
   unknown names would make adding a fact a breaking change — but it is a choice, not an oversight.
5. **This packet's filename** is `P7.2b-e1.3-e1.4.md`, so it does not collide with the run-4 packet at
   `docs/returns/P7.2b.md`. Rename if the coordinator prefers otherwise.
6. **The branch still has no upstream**; `git push -u origin task/p7.2b-calibration` remains
   outstanding for the author (recorded in the confirmation block, 2026-09-15 14:41). I did not
   attempt it.
7. **§6's checkbox in `PROJECT_PLAN`** is untouched — P7.2b is not done until run 4 and its packet.

## AI-assistance record

- **Tool and version:** Claude Code (Anthropic), model identified by the harness as *Claude Opus 5
  (1M context)*, 2026-09-15. Stated as the harness reports it; the session cannot verify a model
  identifier from inside itself.
- **Authorship per file:**
  - `docs/plans/p7.2b_e1.3_e1.4.md` — AI-authored.
  - `offline/transfer_calibration.py` — AI-edited from an AI-authored original (the E1/E1.1/E1.2
    versions by the previous implementer session); this session added ~180 lines and rewrote the
    canary block in `report`.
  - `offline/campaigns/p7_2b_calibration.sh` — AI-edited from an AI-authored original; 18 lines.
  - `tests/test_transfer_calibration.py` — AI-edited from an AI-authored original; 6 new tests,
    12 new parametrised cases, 3 strengthened, 1 fixture extended.
  - `output/p7_2b_runs/e13e14_mutants.py` — AI-authored, throwaway harness, kept as evidence.
  - `docs/returns/P7.2b-e1.3-e1.4.md` — AI-authored.
- **Human verification performed:** none yet by the author. Everything in this packet was run by the
  AI session in this conversation: 62-test and 1957-test pytest runs; the 14-mutant round in an
  isolated worktree; the real-engine canary execution; the `report` preview against the live chunks;
  the `==` recomputation of the chunks' canary and commit straight off the 100 chunk files; the
  `diff` proving `scenarios/draws/draw_0000` unchanged. **The coordinator's independent round is
  still owed**, as it was for E1, E1.1 and E1.2 — and it found two survivors on E1.2 that the
  implementer's own round did not, which is why it matters. This session's own round found two
  survivors of its own first attempt (ME2, MD1b) and both are recorded above.
- **Who made the research decisions:** the **coordinator**, in `BRIEF_36` Amendment E1.3 (block of
  2026-09-15 ~15:55) and E1.4 (block of ~16:30), with the correction block of ~17:00. The canary
  reference constants were pre-registered earlier (`8e70d77`, E1.2) and are untouched here. The AI
  session decided only implementation shape — the block layout, the parser's refusals, and which
  extra mutants to run — and each such choice is listed under *Deviations*.

## What the next task will assume about this one

* **Run 4 runs the driver at `5dfaaa2`** and its artifact's provenance reads that commit — provided
  the untracked run-3 artifact has left the tree (open question 1), otherwise `git_dirty: true`.
* **`report` now refuses a work directory without `canary.json`.** The live `output/p7_2b/` has none
  today; the driver creates it after the token, so run 4 is fine, but **`report` can no longer be
  re-run by hand against the current work directory** without one.
* **The artifact's `canary.seconds` is the reporting run's**, and the chunks' 0.89 lives under
  `canary.probe_chunks_canary.seconds`. Any prose written about run 3's artifact — which says
  `0.89` at the top level — does not describe run 4's.
* **The canary line is JSON.** Anything parsing `canary.log` must handle both forms across the
  file's history.
* **`probe_chunks_canary.correctness_half` is the fixed string `"not recorded in these chunks"`.**
  The chunks' canary correctness half for run 1 is *recovered*, from
  `output/p7_2b_runs/tmp_capture_p72b_run.log` — per the ~17:00 correction, **not lost** — but it is
  not in the chunks, and the artifact says only what the chunks contain. Both re-verified from disk
  in this session: `sha256sum` reads `944395f082ef47ff78d4af246a8c2d7010992e0140cce0c4e442205d7b061e39`,
  and the file's canary line reads `canary 0.89 s {'decisions': 360, 'local_return': -32648.0,
  'att_horizon': 247.75089149261333, 'two_routes_agree': True}`.
* E1.4 item 6's packet sentence, in the corrected form, belongs in the **run-4 packet**, not here:
  *the probe table's rate basis is run 1's canary, 0.89 s, whose correctness half is recovered from
  the run-1 driver capture (`944395f0…`) — not from the chunks, which do not carry it; the smokes'
  canary is run 4's, checked at the driver and re-verified by `report`.*
