# RETURN PACKET — P4.6 (C1's actual experiment: the method x tier grid)
**Date:** 2026-08-13 · **Mode:** Claude Code · **Contracts:** v1.1
**Brief:** `BRIEF_17` @ `cacf5f8`, **plus its section 11 addendum @ `53da8fd`** (five findings, all
binding, relayed mid-session and folded into the plan before the first gradient step)

**Fix round:** `BRIEF_18` (review `docs/reviews/P4.6.md`, **PASS-WITH-NOTES**, pinned at `0e1b6eb`),
applied 2026-08-14. ⚠️ **`docs/reviews/P4.6.md` is not on disk in either tree and no commit has ever
touched that path** (`git log --all` in the main tree) — this round was therefore executed against
`BRIEF_18`'s own enumeration of the findings, which is detailed and self-contained. **Anything the
review says that `BRIEF_18` does not repeat has not been addressed, because it could not be read.**

**Status: DONE for phase 1.** All 16 new cells, both new behaviour cells and the gated re-use are on
disk; the campaign's own assertion reports `completed == requested`, 16/16, every cell 5 seeds x 100
draws at 40,000 steps. Phase 2 (the three mixture tiers) was not attempted and becomes **P4.7**,
which `BRIEF_17` §11 already rules is the expected outcome.

---

## 0.0 The result, first, before any interpretation

> ### The grid crosses. IQL wins on the three weak tiers by margins that reach 20x the expert-tier spread and are 5.6x it at their weakest; %BC inverts from best to worst; and on `fixedtime` the Decision Transformer reproduces the behaviour policy **exactly**.
>
> `att_horizon` on `cf_hz1x1`, 100 registered held-out draws, 5 training seeds, 40,000 gradient
> steps, one `evaluate_arm`. **Tiers ordered by measured behaviour ATT, never by name** — and the
> `mappo1000` row is P4.4's, re-used under Gate G, not re-run.
>
> ⚠️ **Every number in this table is a mean over held-out draws 1000-1099.** The tier's own corpus
> ATT (training draws 1-200) is a **label**, not a comparator — placing it in this table would be the
> comparison `PREREGISTRATION` A5 makes **void**, which is exactly the defect `BRIEF_18` F2 found in
> the report's own summary print. It is given in a separate column, marked, for orientation only.
>
> | tier | `bc` [1000-1099] | `bc_top10` [1000-1099] | `iql` [1000-1099] | `dt` [1000-1099] | its collecting policy [1000-1099] | *tier label [1-200], not a comparator* |
> |---|---|---|---|---|---|---|
> | `mappo1000` | 105.1635 | **103.1627** | 103.4764 | 104.9558 | 105.5820 | *105.46* |
> | `mappo500` | 107.2780 | **104.7520** | 104.8194 | 106.6556 | 108.0916 | *107.50* |
> | `maxpressure` | 175.3893 | 165.7768 | **158.8801** | 177.5797 | 176.8912 | *176.50* |
> | `fixedtime` | 265.8322 | 275.9991 | **260.3385** | 262.0706 | 262.0706 | *262.09* |
> | `random` | 427.9544 | 432.1852 | **393.3983** | 420.3764 | 428.8839 | *422.52* |
>
> **All three registered predictions FAILED**, scored mechanically by rules fixed before any of these
> numbers existed (§3.6). **P1 failed and is not rescued** (§3.6, rewritten under `BRIEF_18` F1: the
> continuous companion cannot adjudicate it, because four natural forms of it disagree in sign).
> **P3 failed and is inverted** — the demand signature appears on four tiers and not on `random`,
> which is the one tier the prediction was about.
>
> **No equivalence verdict appears anywhere in the artifact**, mechanically enforced and re-checked
> (`assert_no_verdicts` clean over the whole payload).

---

## 0. What is settled, before anything else

> **Gate G PASS.** 20 re-used checkpoints identical (15 canonical digests against
> `p4_4_training.json`, 5 file hashes against `p4_gate.json`), **100 declared cells re-rolled and
> reproduced by exact float equality, 0 mismatches**, and the two DT evaluation paths shown to be
> the same path on 5 cells (0 differences).
>
> **A2's regression gate PASS.** `docs/data/p4_4_baselines.json` regenerated through the generalised
> reporting path: **18,201 leaves outside `runtime`, 0 removed, 0 added, 0 changed** — byte-identical
> — with the `runtime` diff exactly the one declared before the comparison ran.
>
> **P3's two diagnostics are complete for all five tiers** and cost no gradient step. On
> `mappo1000` the difficulty check reproduces the coordinator's ad-hoc **4/20** exactly, from code
> and a definition written here.
>
> **22 mutations executed, 22 killed**, each with its failure pasted (sections 6 and 7).
>
> **Every reported number was recomputed independently.** All **25 cell means** recomputed from the
> raw episode records by `math.fsum`, worst disagreement **5.7e−14**; the four re-used `mappo1000`
> cells **bit-identical** to P4.4's committed values; `assert_no_verdicts` clean over the whole
> payload; 70 method comparisons and 20 behaviour comparisons present as declared.

---

## 1. Status, honestly

| deliverable | state |
|---|---|
| `docs/plans/p4.6.md` committed before any training, with the three predictions verbatim, the mixture construction, the per-tier targets and the subsampling RNG | **DONE** (`622cf2e`, addendum `30073d8`) |
| A2's generalised reporting path + its declared regression gate | **DONE**, gate PASS |
| Gate G (the re-used `mappo1000` column) | **DONE**, PASS |
| The declaration artifact, size-matched and asserted | **DONE** (5 tiers, 200 streams / 72,000 windows each) |
| P3's two diagnostics per tier | **DONE**, reported whichever way they came out |
| `DEFERRED` 42 (four guard branches) | **DONE**, including one branch proved **unreachable by construction** |
| `DEFERRED` 43 (fragile `match=` tokens) | **DONE**: 63 sites measured, 10 fragile, 8 fixed, 4 unfixable by token with the reason recorded |
| The 2 new behaviour cells (A3) | **DONE** — `behaviour@random` 428.8839 ± 0.6946 (n = 500), `behaviour@fixedtime` 262.0706 ± 0.7442 (n = 100) |
| The 16 new method cells | **DONE** — 4 tiers x 4 methods, produced by the user's `tmux` run (§3.4), campaign self-assertion `completed == requested` |
| The reported artifact `docs/data/p4_6_grid.json` | **DONE** — 25 cells, 70 paired comparisons, 20 behaviour comparisons, 3 scored predictions, **11,700** episode records |
| Phase 2 (three mixture tiers) | **NOT ATTEMPTED** — declared and implemented, and `BRIEF_17` section 11 already rules that it becomes P4.7 |
| `BRIEF_18` F1 — P1's rescue withdrawn | **DONE** (§3.6) — and the four continuous readings reproduced first-hand |
| `BRIEF_18` F2 — the wrong table, the rename, the re-enumeration | **DONE** (§5a), artifacts and logs regenerated |
| `BRIEF_18` F3 — check B withdrawn on `maxpressure`, reason in the artifact | **DONE** (§9.6), rho table measured |
| `BRIEF_18` F4 — four unprotected quantities | **DONE** (§11a) — 6 tests, 6 mutations, 6 killed |
| `BRIEF_18` §5 six corrections · §6 four disclosures | **DONE** (§2, §3.1, §3.4, §5b, §11b, §0.0) |
| **No reported number moved** | **PROVEN** (§5b) — 79,349 numeric leaves compared, **0 moved** |

---

## 2. Branch and diff

```
task/p4.6-method-tier-grid   (based on main @ cacf5f8)

$ git diff --stat main...HEAD
 docs/data/p4_6_declaration.json           |  10315 +++
 docs/data/p4_6_grid.json                  | 112994 +++++++++++++++++++++++++++
 docs/data/p4_6_selection_diagnostics.json |    502 +
 docs/data/p4_6_training.json              |   4270 +
 docs/plans/p4.6.md                        |    756 +
 docs/returns/P4.6.md                      |    777 +
 offline/campaigns/p4_6_phase1.sh          |    198 +
 offline/method_tier_grid.py               |   2757 +
 offline/offline_baselines.py              |    150 +-
 tests/test_method_tier_grid.py            |   1360 +
 tests/test_offline_baseline_agents.py     |     10 +-
 tests/test_offline_baselines.py           |     93 +-
 tests/test_offline_selection.py           |    139 +
 13 files changed, 134265 insertions(+), 56 deletions(-)
```

⚠️ **Corrected under `BRIEF_18` §5.** The packet at `0e1b6eb` pasted **9 files / 15,574 insertions**
-- a stat taken mid-task, before the campaign existed. The real one at `0e1b6eb` was **13 files /
133,725**, and the paste omitted `p4_6_grid.json` (112,859 lines), **the file holding every reported
number**. CLAUDE.md §7 mandates the real stat; the conclusion it supported (zero frozen files) was
correct and independently confirmed, but the evidence pasted for it was not the evidence.

**Zero frozen files touched.** `bash scripts/claude_guard.sh --frozen-only` exits 0, and the diff
contains no path under `envs/`, `agent/base.py`, `agent/utils/utils.py`, `agent/MAPPOAgent.py`,
`algorithms/`, `rewards.py`, `states/`, `metrics/`, `utils/`, `CityFlow/`, `experiments/`,
`scripts/`, `.claude/`.

**Merged modules touched, and why each was allowed:**
- `offline/offline_baselines.py` — `BRIEF_17` section 11 finding **A2** orders the reporting path to
  be generalised; the change is additive and its regression gate is in section 5.
- `tests/test_offline_baselines.py`, `tests/test_offline_baseline_agents.py`,
  `tests/test_offline_selection.py` — `DEFERRED` 42 and 43, both named in the brief's section 8.
  **Every changed line is enumerated in sections 7 and 8. No assertion was weakened or removed.**

Commits, in the order the plan fixed:

```
622cf2e plan(P4.6)   the grid, the predictions and the no-verdict ruling, before the first gradient step
126020b tests(P4.6)  red: 34 failed at 34 distinct call sites, 2 declaration tests green on arrival
30073d8 plan(P4.6)   fold in BRIEF_17 section 11 -- five findings, still before the first gradient step
1f35ea3 feat(P4.6)   A2: the generalised reporting path; regression gate PASS
ea839eb test(P4.6)   DEFERRED 42: four guard branches, 6/6 mutations killed
18a4d6d test(P4.6)   DEFERRED 43: 63 sites measured, 10 fragile, 8 lengthened
3782250 feat(P4.6)   the grid module, the declaration and P3's diagnostics; 16/16 mutations killed
```

---

## 3. The campaign: what exists, what is handed off, and how

*(Written from the artifacts on disk, listed by `ls` and read by `json.load`. Nothing here is
predicted; a cell that does not exist is named as missing.)*

### 3.1 On disk now

| artifact | state |
|---|---|
| `output/p4_6/gate.json` | Gate G **PASS** (section 6) |
| `docs/data/p4_6_declaration.json` | 5 tiers, 200 streams and 72,000 windows each, targets and scales asserted against the corpus |
| `docs/data/p4_6_selection_diagnostics.json` | P3's two checks for all 5 tiers (section 3.3) |
| `docs/data/p4_6_training.json` | **20 runs**: the `random` tier, 4 methods x 5 seeds, 40,000 steps each |
| `output/p4_6/checkpoints/` | **20 checkpoints**, `random_{bc,bc_top10,iql,dt}_seed{101..505}.pt` |
| `output/p4_6/eval_random_behaviour.json` | **`behaviour@random` 428.8839 ± 0.6946**, veh 154.34, n = 500 (5 seeds x 100 draws) |
| `output/p4_6/eval_fixedtime_behaviour.json` | **`behaviour@fixedtime` 262.0706 ± 0.7442**, veh 145.96, n = 100 (deterministic, no seed) |
| `output/p4_6/eval_<tier>_<method>.json` | **all 16 method cells**, written by the user's `tmux` run between 11:12 and 15:12 on 2026-08-14 (§3.4) |
| `docs/data/p4_6_grid.json` | **written 15:12:53** — 25 cells, 70 + 20 comparisons, 3 scored predictions, **11,700** episode records |

**How the `fixedtime` behaviour arm is known to be the collecting policy (finding A3).** The
evidence is the **assertion that the factory's resolved plan hashes to
`1b0aa65adf80945f51d7d49a651945a54e9ddaac040c4c72787943e0595ee674`, the value the tier's own manifest
records, with `k = 6` read from that manifest** — an identity check on the schedule itself, which
fails loudly if either differs. Had `PROJECT_PLAN` §6's `k = 4` been assumed, the reference line
would describe a policy that generated none of these episodes.
⚠️ **Corrected under `BRIEF_18` §5:** the packet at `0e1b6eb` presented the held-out cell landing
**0.018 ATT** from the corpus tier's training-draw mean as a validation. **It discriminates almost
nothing** — that cell's own 95 % CI is **±0.7442**, forty times the agreement, so a wrong-`k` policy
could easily land inside it. The ATT line is demoted to a consistency note; the sha256 assertion is
the evidence.

### 3.2 The campaign script, per `BRIEF_17` §12

`offline/campaigns/p4_6_phase1.sh` — **written by me, launched by the user in `tmux`; I never
launched it and never `sleep`-polled it.** It evaluated the already-trained `random` tier, then
trained and evaluated `fixedtime`, `maxpressure` and `mappo500`, then wrote the report.
**Measured wall clock: 4 h 14 m** (10:58:51 to 15:12:53), against the ≈3 h 20 m the script's header
estimated -- the estimate was 21 % optimistic and is recorded here rather than quietly forgotten.

| §12 condition | how this script meets it |
|---|---|
| 1. implementer writes, user launches | the script carries the `tmux` invocation in its header; nothing in this session launches it |
| 2. `set -euo pipefail` + completed == requested, non-zero on mismatch | both; the assertion derives *requested* from the declared tier list, `TRAINING_SEEDS` and `HELD_OUT_DRAWS` — never from the files it checks — and additionally verifies each cell's 500 (seed, draw) pairs and its 40,000-step budget, plus a training record for every (tier, method, seed) |
| 3. pin exported in the shell **and** re-asserted inside | `export OMP_NUM_THREADS=1 MKL_NUM_THREADS=1` inside the script, `--torch-threads 1` on every job, and the header tells the user to export it in the `tmux` shell too |
| 4. resumable, every skip logged by name | a tier with 20 checkpoints is not retrained and a cell with an eval JSON is not re-rolled; both print `SKIP ... : ... already on disk` |
| 5. the per-cell artifact rule stays | unchanged: one JSON per cell, written atomically after `assert_cell_complete` |
| 6. phase 1 and phase 2 are separate scripts | this script is phase 1 only; phase 2 is P4.7's and its code is already written and tested |

**Verified before handing it over**, without running the campaign: `bash -n` clean; the two
preconditions pass on today's state (Gate G `PASS`, both behaviour cells present); the skip logic
reports `random -> 20 checkpoints`, `fixedtime -> 0`; and **the completion assertion was run against
today's state and correctly exits 1** — `requested cells: 16   completed cells: 0`.

### 3.4 The `tmux` run: which run produced which cell

`BRIEF_17` §12's closing requirement. The user launched `offline/campaigns/p4_6_phase1.sh` in `tmux`
at **2026-08-14 10:58:51** and it ran to `PHASE 1 COMPLETE` at **15:12:53**. Read from
`output/p4_6/logs/phase1.log`:

```
[2026-08-14 10:58:51] Gate G: PASS (checkpoint identity + 100 re-rolled cells, verified before this campaign)
[2026-08-14 10:58:51] behaviour cells present: random (n=500) and fixedtime (n=100); neither is re-rolled
[2026-08-14 10:58:51] SKIP training random: 20 checkpoints already on disk
[2026-08-14 11:16:53] TIER random COMPLETE (training + 4 cells)
[2026-08-14 12:16:29] TRAIN fixedtime COMPLETE
[2026-08-14 12:34:51] TIER fixedtime COMPLETE (training + 4 cells)
[2026-08-14 13:37:23] TRAIN maxpressure COMPLETE
[2026-08-14 13:55:14] TIER maxpressure COMPLETE (training + 4 cells)
[2026-08-14 14:55:36] TRAIN mappo500 COMPLETE
[2026-08-14 15:12:50] TIER mappo500 COMPLETE (training + 4 cells)
[2026-08-14 15:12:50] verifying the campaign by its artifacts
requested cells: 16   completed cells: 16
completed == requested, every cell holds 5 seeds x 100 draws at 40,000 steps
[2026-08-14 15:12:53] PHASE 1 COMPLETE
```

**Provenance, by run.** *This session, 2026-08-13*: Gate G (19:58), the `random` tier's 20
checkpoints (19:59-21:00), `eval_fixedtime_behaviour` (21:02), `eval_random_behaviour` (21:08).
*The user's `tmux` run, 2026-08-14*: all 16 method cells and the first report. *This fix round,
2026-08-14*: the regenerated declaration, diagnostics and report — **no cell was re-rolled and no
number moved** (§5b).

**The resumability condition worked as designed**: the `random` tier's 20 checkpoints were not
retrained and neither behaviour cell was re-rolled, each skip logged by name.

⚠️ **`report.log` and the summary table inside `phase1.log` were regenerated in this fix round**
(`BRIEF_18` F2b). `phase1.log` is a transcript of a run that happened, so it is **not rewritten**: a
dated correction is appended to it carrying the corrected table, and the historical lines stand.
**The coordinator must re-secure `output/p4_6/` and rewrite `SHA256SUMS_p4_6.txt`** — I did not touch
the secured copy in the main tree.

### 5a. `BRIEF_18` F2 — the wrong table, and every consumption site re-enumerated

**What was wrong.** `_run_report` printed each tier's **label ATT (training draws 1-200)** beside four
**held-out means (1000-1099)**. Reading across that row is the comparison `PREREGISTRATION` A5 makes
void. The coordinator measured **max substitution error 6.3652 ATT** and **1 statement in 20 flipping
sign** — `bc@random` read **+5.4356 (worse)** against the truth **−0.9296 (better)**. It was on disk
in `report.log` and `phase1.log`, both inside the secured `SHA256SUMS_p4_6.txt`.

**What was done.** (a) the print now reads `behaviour_cells` and **every column carries its draw
range**; (b) `report.log` regenerated and a dated correction appended to `phase1.log` (§3.4);
(c) `behaviour_att` → **`tier_label_att_training_draws`** and `tier_order_by_behaviour_att` →
`tier_order_by_tier_label_att`, in the module, both artifacts and the tests; (d) the packet's §0.0
table now states the draw range in every column header and demotes the label to a marked, separate
column.

**The rename is necessary and not sufficient** — the offending site was a *use* — so all 20
consumption sites are enumerated and classified, as the review did for the pre-fix state:

| site | what it does | class |
|---|---|---|
| `method_tier_grid.py:165` | declares the label constant, docstring states "training draws, never a comparator" | CORRECT |
| `:685` | writes `tier_label_att_training_draws` into the declaration | CORRECT |
| `:746`, `:1883` | tier ORDER derived from the label — an ordering, not a comparison | CORRECT |
| `:1884-1887` | writes the label into the grid under its new name | CORRECT |
| `:1845`, `:1852`, `:1901` | builds and writes `behaviour_cells` (held-out) | CORRECT |
| `:2740` | **the fixed site**: the summary now reads `behaviour_cells` | CORRECT (was WRONG) |
| `:151` | docstring naming `behaviour_cells` as the only lawful reference | CORRECT |
| `tests:300`, `:309` | the constant-versus-ladder test, renamed off `behaviour_att` | CORRECT |
| `tests:1242`, `:1258`, `:1260`, `:1261` | F4.2's guard: asserts the two are different quantities under different names | CORRECT |
| `docs/plans/p4.6.md:132` | the **pre-registration record**, which keeps the old key name | CORRECT — annotated in place with a dated note pointing at the new names, because rewriting a pre-registration is worse than a stale name |
| `docs/returns/P4.6.md` (this file) | §0.0's table, §3.4-§3.6 | CORRECT after (d) |

**0 WRONG and 0 AMBIGUOUS remain**, against the review's pre-fix count of 2 WRONG and 7 AMBIGUOUS.

### 5b. No reported number moved — the regeneration diff, declared before it ran

The expected difference list was written to `/tmp/p46_expected_diff.json` **before** the comparison
(the P4.4 discipline: *"an expected difference named before the comparison is a specification, the
same difference explained afterwards is an excuse"*). Then all three artifacts were regenerated and
compared leaf by leaf, with **renames canonicalised** and **empty containers counted as leaves**:

```
p4_6_declaration.json            leaves 7971 -> 7971    changed 0   removed 0   added 0
p4_6_selection_diagnostics.json  leaves  417 ->  432    changed 0   removed 0   added 15
p4_6_grid.json                   leaves 88215 -> 88342  changed 2*  removed 0   added 126

NUMERIC leaves compared across all three: 79,349
NUMBERS THAT MOVED: 0
```

`*` the two changed leaves are `$.diagnostics.runtime.git_commit` and
`$.diagnostics.runtime.written_at_git_commit` — **the write-time commit of the embedded diagnostics
artifact**, which changed because the diagnostics were regenerated in this round. They are
provenance, not measurement, and they were named in the declared list one level up; the list said
`$.runtime.*` and the embedded copy sits at `$.diagnostics.runtime.*`. **Recorded as a gap in my
declaration rather than waved through.**

The 141 additions are exactly the declared ones: `return_versus_difficulty_rho`, `withdrawn` and
`withdrawn_reason` per tier (15), `effect_size_usable` on all 90 comparisons plus one
`effect_size_caveat`, and four new P3 fields per tier (20).

**P3's outcome is unchanged (FAILED) and no tier's `demand_signature` changed** — `maxpressure`'s
stays `true` because its **volume** interval excludes zero; what changed is that the artifact now
says so (`signature_carried_by: ["volume"]`) instead of letting a circular p-value imply it.

### 3.5 Does each method beat the policy that COLLECTED its tier?

C1's most valuable per-tier sentence, and the reason `BRIEF_17` §11 A3 put two new behaviour cells in
scope. Paired on the same 100 held-out draws; **negative means the method is better**. No verdicts.

| tier | `bc` | `bc_top10` | `iql` | `dt` |
|---|---|---|---|---|
| `mappo1000` | −0.4186 [−0.66, −0.18] | −2.4193 [−2.66, −2.18] | −2.1056 [−2.37, −1.84] | −0.6263 [−0.86, −0.39] |
| `mappo500` | −0.8136 [−1.13, −0.50] | −3.3396 [−3.62, −3.06] | −3.2722 [−3.54, −3.00] | −1.4360 [−1.74, −1.13] |
| `maxpressure` | −1.5019 [−3.81, **+0.81**] | −11.1144 [−13.49, −8.74] | **−18.0110** [−20.29, −15.73] | **+0.6885** [−1.76, +3.14] |
| `fixedtime` | **+3.7616** [+2.55, +4.98] | **+13.9285** [+11.20, +16.65] | −1.7321 [−2.68, −0.78] | **−0.0000** (identity, §3.7) |
| `random` | −0.9296 [−2.05, **+0.19**] | **+3.3013** [+1.09, +5.52] | **−35.4856** [−36.34, −34.63] | −8.5076 [−8.84, −8.18] |

**Read as measured, with no attribution attached:** every method beats its behaviour policy on the
two MAPPO tiers; on the three weak tiers only IQL does so decisively, %BC is **worse than the policy
that produced its data** on `fixedtime` (+13.93) and `random` (+3.30), and the DT does not separate
from the behaviour policy on `maxpressure` (CI spans zero) and is identical to it on `fixedtime`.

⚠️ **What this may NOT be used for** (`BRIEF_17` §10, binding): the single-controller axis varies
**quality and state coverage together**, so no ranking shift here may be attributed to quality alone
— that separation is what the mixture tiers of P4.7 exist for. And the DT's prompt is the **naive**
per-tier target by declaration; **it is not tuned**, no sweep was run, and P4.3 already measured the
in-domain prompt as a weak lever, so no sentence may explain the DT column by prompt calibration.

### 3.6 The three registered predictions, scored mechanically

Scored by `score_p1` / `score_p2` / `score_p3` against the rules in `docs/plans/p4.6.md` §4, fixed
before any cell existed. **All three FAILED**, and how each failed is the informative part.

**P1 — FAILED.** BC's rank among the four methods, in quality order:
`mappo1000` **4** · `mappo500` **4** · `maxpressure` **3** · `fixedtime` **3** · `random` **3**.
The declared primary rule was *BC is worst of four on every tier*; BC is not worst on the three weak
tiers, so **P1 failed. Full stop — there is no rescue and none is offered.**

⚠️ **The packet at `0e1b6eb` argued that P1's hypothesis was right and only its operationalisation
was wrong, on the strength of the declared continuous companion (tau-b = +0.80). That argument is
WITHDRAWN**, and it is withdrawn because it was checked — by the reviewer, precisely because it was
the reading that flattered us. Three things kill it, and I reproduced all three first-hand:

1. **Four natural continuous readings of "BC's rank worsens" disagree in SIGN.** Measured here from
   `p4_6_grid.json`, over the five tiers in quality order:

   | continuous reading | tau-b |
   |---|---|
   | `bc − min(other)`, absolute — **the declared one** | **+0.80** |
   | `(bc − min(other))` normalised by the tier's own ATT | +0.40 |
   | `bc − max(other)`, absolute | **−0.60** |
   | BC's position within the tier's method spread | **−0.53** |

   **A statistic whose sign depends on which of four equally natural forms you pick cannot adjudicate
   anything**, in either direction. That disagreement is the reportable fact, and it is the reason
   this section no longer leans on the companion — not the reviewer's objection to the conclusion.
2. **The mechanism is not BC deteriorating; it is IQL pulling away.** Against each tier's own
   behaviour policy, BC sits at −0.42, −0.81, −1.50, **+3.76**, −0.93 while IQL runs to −2.11, −3.27,
   −18.01, −1.73, **−35.49**. ⚠️ `BRIEF_18` §1 states BC tracks its behaviour policy *"within 1.5 ATT
   on every tier"*; **measured, that holds on four tiers and not on `fixedtime`, where BC is +3.76** —
   the largest deviation is **3.7616**, not 1.5. The mechanism claim survives the correction; the
   number in the brief does not, and it is corrected here rather than repeated.
3. The rank rule and the companion can disagree **by construction**: a rank counts position among
   *all* other methods, the gap measures distance to the *best* one only.

> **The finding that does hold, labelled for what it is — UNREGISTERED AND OBSERVATIONAL:**
> **IQL's advantage over BC grows as data quality falls.** Measured as `bc − iql` in quality order:
> **+1.6871 · +2.4585 · +16.5092 · +5.4937 · +34.5561** (tau-b **+0.80**, and `fixedtime` breaks the
> monotonicity).
> **This is a different claim from P1**, it was **not registered**, no threshold was declared for it,
> and it is reported as an observation about this grid — not as a rescued prediction and not as
> evidence for the hypothesis P1 encoded. Anyone quoting it must quote this sentence with it.

**P2 — partial FAILED, full NOT SCORABLE.** %BC's advantage over BC (`bc − bc_top10`, positive =
%BC better): `mappo1000` **+2.0008** · `mappo500` **+2.5260** · `maxpressure` **+9.6126** ·
`fixedtime` **−10.1669** · `random` **−4.2308**. The declared phase-1 clause was *`random` attains
the minimum among the single-controller tiers*; the minimum is at **`fixedtime`**, so it fails — and
it fails because %BC is *harmful* on **both** weak deterministic tiers, more so on `fixedtime`.
**The mixture half is unscorable without phase 2 and no verdict is issued on it**, exactly as
declared.

**P3 — FAILED, and inverted.** The prediction was a demand signature on `random` and none on
`mappo1000`. Measured: **`random` is the ONLY tier without one** (overlap 3/20, p = 0.3213, volume CI
[−9.7, +0.6] spans zero), while the other four carry one.
⚠️ **Two caveats that materially weaken the four positives, both declared or found here:** on
`mappo1000`, `mappo500` and `fixedtime` the signature comes **only** from the volume check
(difficulty p = 0.1222 on all three), and those volume differences are **6 to 13 vehicles out of
~1818, i.e. 0.3–0.7 %** — resolvable, not large; and `maxpressure`'s check B is **withdrawn** (§9.6).
**The honest summary is that P3's declared OR-rule is carried by a very small volume effect, and that
the one tier the prediction was about shows nothing.**

### 3.7 ⚠️ The finding I nearly reported as a bug: the DT clones fixed-time exactly

`dt@fixedtime` = 262.0706 and `behaviour@fixedtime` = 262.0706 — identical to four decimals, which is
the shape of a report that copied one arm into another's slot. **It is not.** Checked four ways:

- `dt@fixedtime` holds **n = 500** (5 seeds x 100 draws) against the behaviour arm's **n = 100**, so
  they are different objects;
- **all five independently-trained DT seeds produce identical per-draw ATT** — the five models are
  behaviourally indistinguishable;
- the DT's per-draw ATT equals the fixed-time policy's on **100 of 100 draws by exact float
  equality**;
- the two cell means differ **in the last bit** (`262.07062063447944` against `262.0706206344795`),
  which is precisely the residue of averaging 500 values against 100 — a copy would have been
  bit-identical.

**So on a deterministic, periodic behaviour policy the offline sequence model recovers the policy
exactly, and therefore cannot beat it.** The paired comparison reports this correctly but its test
statistics must not be quoted: **98 of the 100 per-draw differences are exactly 0.0** and the other
two are **−2.842e−14**, so Wilcoxon drops 98 zeros, runs on **n = 2**, and returns `p = 0.346`,
`r = −1.000` — **numbers about two float residues, not about policies.** The identity is the result;
the p-value is an artifact of it.

### 3.3 P3's two diagnostics, complete for all five tiers

Training data only; no rollout, no gradient step. Arrivals are rebuilt from each collection's own
`FlowRandomizer` and **every one of the 200 draws is verified against the manifest's recorded
`flow_draw_sha256`** before a single arrival is reported.

| tier | kept arrivals | other arrivals | difference | 95 % CI | overlap with the 20 easiest draws | exact p |
|---|---|---|---|---|---|---|
| `mappo1000` | 1814.9 | 1820.9 | −6.0 | [−11.6, −0.3] | **4/20** (expected 2.0) | 0.1222 |
| `mappo500` | 1809.7 | 1821.4 | −11.7 | [−17.1, −6.4] | 4/20 | 0.1222 |
| `maxpressure` | 1809.0 | 1821.5 | −12.5 | [−18.0, −6.9] | **19/20** | 2.232e−24 |
| `fixedtime` | 1808.7 | 1821.6 | −12.9 | [−18.0, −7.8] | 4/20 | 0.1222 |
| `random` | 1816.2 | 1820.7 | −4.6 | [−9.7, +0.6] | 3/20 | 0.3213 |

**The `mappo1000` control reproduces the coordinator's ad-hoc 4/20 exactly**, from a definition
written here and code that shares nothing with theirs.

⚠️ **Three things a reader must not skip.**
1. **P3's own scoring rule (declared in plan §4.3) is not satisfied on `random`**: p = 0.3213 and the
   volume CI includes zero, so on the evidence now on disk **P3 FAILS**. The formal scoring happens
   in the report, and it will say so.
2. **`maxpressure`'s 19/20 is ALMOST ENTIRELY circular, and check B is now WITHDRAWN there** (§9.6).
3. **On `mappo1000` my volume CI excludes zero** (−11.6, −0.3) where P4.5 concluded "arrivals were
   equal". The difference is **6 vehicles in ~1818, i.e. 0.33 %** — resolvable and negligible, which
   is exactly why the rule reports the interval and not a verdict.

---

## 4. Tests — real output

```
$ OMP_NUM_THREADS=1 MKL_NUM_THREADS=1 \
  RLTRAFFIC_CORPUS_V11=/home/filip/rltraffic/datasets_v11 \
  RLTRAFFIC_CORPUS=/home/filip/rltraffic/datasets \
  /home/filip/rltraffic/.venv/bin/pytest -q
769 passed, 3 skipped, 12 warnings in 89.46s      # exit=0, read from $? with no pipeline
```

**The three guards, each exit code read directly and never through a pipe** (`BRIEF_18` §7, after
this packet reported a pipeline's status as the script's on 2026-08-13):

```
bash scripts/claude_guard.sh --frozen-only   exit=0
bash scripts/check_english.sh <this round's files>   exit=0
bash scripts/check_test_hygiene.sh           exit=1   <- 16 TH006 findings, none in this diff
```

**Pinned**, and the pin is not optional: `DEFERRED` 41 records that this suite deadlocks unpinned in
a fresh worktree. Baseline on this branch's base before anything was written: **710 passed, 3
skipped** — so **+53 net new passing tests**.

### 4.1 Red first, and what was green on arrival

```
$ pytest tests/test_method_tier_grid.py -q          # skeleton raising NotImplementedError
34 failed, 2 passed in 1.60s
... every failure: E       NotImplementedError
```

**34 failures at 34 distinct call sites.** The two that passed on arrival are named rather than
buried: `test_the_behaviour_att_constants_are_the_committed_ladders_and_order_the_tiers` and
`test_the_module_declares_no_equivalence_verdict_anywhere` — both assert facts about **constants and
source text**, which exist in the skeleton by construction. Everything that exercises behaviour was
red.

The tests written *after* the code, and disclosed as such:
- the four `DEFERRED` 42 guard tests and the two `DEFERRED` 43-driven ones (they are regression
  cover for existing code, and the brief asks for exactly that);
- `test_the_training_cli_path_runs_end_to_end_on_a_fixture`, written after `_run_train` failed in
  the real campaign — **and it caught the defect it was written for on its first run** (section 9.1);
- the three `baselines_artifact` generalisation tests, which were red for the right reason first
  (`TypeError: unexpected keyword argument 'behaviour_reference'`).

---

## 5. A2 — the generalised reporting path and its regression gate

`offline/offline_baselines.py` at `cacf5f8` hard-required the `mappo1000` arm, defined
`recovered_fraction` against it by name and emitted an equivalence verdict unconditionally. Two
keyword-only parameters were added, both defaulting to P4.4's behaviour:
`behaviour_reference="mappo1000"` and `emit_verdicts=True`. Under `emit_verdicts=False` the payload
carries no verdict, no `delta`, no margin fields, no decision rule and no delta-scored forecasts, and
records the reporting rule and the reference instead. `_run_report` gained `--behaviour-reference`
and `--no-verdicts`, **read with `getattr`** so that a caller predating them — including a hand-built
`Namespace` — is unaffected.

**The declared regression gate, run after the change:**

```
OUTSIDE runtime: 18201 leaves committed vs 18201 regenerated
  removed 0   added 0   changed 0
BYTE-IDENTICAL outside runtime: True

RUNTIME diff, against the list declared in docs/plans/p4.6.md section 15.2 BEFORE the run:
  CHANGED $.runtime.git_commit
  ADDED   $.runtime.measurement_git_commits          = <empty array>
  ADDED   $.runtime.unreachable_measurement_commits  = <empty array>
  ADDED   $.runtime.written_at_git_commit
  equal   torch_version, torch_cuda_version, cuda_available, cuda_device_name,
          torch_num_threads (1), numpy_version, python_version
```

**The declared expectation and the observed diff match exactly**, and the three added keys are
P4.3's additive extension of `runtime_provenance` at `31153ec`, which post-dates P4.4's artifact.

⚠️ **A defect in my own instrument, found and fixed mid-check.** The first leaf-diff reported only
**one** added `runtime` key instead of three, because it treated an **empty container as no leaf at
all** — so a key added as `[]` was invisible. The byte comparison (which does see empty containers)
was already passing, so no verdict changed; the corrected instrument, which records
`<empty array>` / `<empty object>` as leaves, reproduces the declared list exactly. **Reported
because a comparison instrument blind to a whole class of difference is worth more as a disclosure
than as a silent fix.**

---

## 6. Gate G — the re-used `mappo1000` column

Run **before any training of this task**, against absolute paths into the main tree.

```
checkpoint identity verified: 20 checkpoints
  re-rolled bc over 5 draws x 5 seeds
  re-rolled bc_top10 over 5 draws x 5 seeds
  re-rolled iql over 5 draws x 5 seeds
  re-rolled madt over 5 draws x 5 seeds
GATE G PASS: 100 cells reproduce exactly, 20 checkpoints identical
```

- **Identity:** 15 canonical state-dict digests recomputed from
  `/home/filip/rltraffic/output/p4_4/checkpoints/` equal the ones `p4_4_training.json` records; 5 DT
  file hashes equal the ones `p4_gate.json` records. **The five DT checkpoints' canonical digests are
  recorded for the first time** in `output/p4_6/gate.json` — `p4_training.json` never carried them.
- **Path identity:** the declared sample is **P4.5's own `GATE_B_DRAWS`**, inherited rather than
  chosen here, x 5 seeds x 4 arms = 100 episodes, compared by **exact float equality** on
  `att_horizon` and `horizon_vehicle_count`. 0 mismatches.
- **Cross-path check:** this task evaluates its DTs through `agent_with_target` (which asserts the
  model conditions on exactly the declared target) while P4 used `load_gate_checkpoint`. One seed x
  the 5 gate draws through both paths: **5 cells, 0 differences** — so the new tiers are measured by
  the instrument that produced the re-used column.

---

## 7. `DEFERRED` 42 — four guard branches, and one of them cannot fire

| # | branch | test | mutation | outcome |
|---|---|---|---|---|
| 1 | `assert_selection_design`'s composition-sum check | `test_the_composition_sum_check_fires_when_a_composition_does_not_add_up` | delete the branch | **KILLED** |
| 1b | the same function's composition-versus-stream-list check | `test_the_composition_must_be_the_one_its_own_stream_list_implies` | delete the branch | **KILLED** |
| 2 | `select_arm_streams`'s pool-versus-declared-seeds check | `test_the_pool_versus_declared_seeds_check_is_covered_by_two_earlier_refusals` | delete either upstream refusal | **KILLED** (both) |
| 3 | `selection_artifact`: an arm trained and never evaluated | `test_the_artifact_refuses_an_arm_that_was_trained_and_never_evaluated` | delete the branch | **KILLED** |
| 4 | `selection_artifact`: the re-used reference arm is missing | `test_the_artifact_refuses_to_report_without_the_reused_reference_arm` | delete the branch | **KILLED** |

```
M1 composition-sum branch: KILLED
M2 composition-versus-stream-list branch: KILLED
M3 missing-directory refusal (upstream of guard 2): KILLED
M4 empty-directory refusal (upstream of guard 2): KILLED
M5 trained-but-never-evaluated refusal: KILLED
M6 missing-reused-arm refusal: KILLED

6/6 mutations killed
```

M1's failure, pasted, because it is the one that shows the test pins **that** branch and not a
neighbouring one:

```
E       AssertionError: Regex pattern did not match.
E         Expected regex: 'does not sum to the declared count'
E         Actual message: 'bc_any_20 seed 101: the recorded composition {101: 5, 202: 4, ...} is
E                          not the one its own stream list implies ({101: 4, 202: 4, ...})'
```

> ### ⚠️ Guard 2 **cannot fire**, and that is the finding
>
> Two attempts to provoke `select_arm_streams`'s `observed != set(spec.behaviour_seeds)` branch were
> both caught upstream. The reason is structural: `_dirs_for_behaviour_seeds` maps a directory to a
> seed with `re.search(r"seed(\d+)$")` and refuses a declared seed with no directory;
> `streams_from_datasets` refuses a declared directory that yields no stream; and `_behaviour_seed`
> re-derives the seed from **the same anchored regex on the same directory name**. So whenever the
> two upstream refusals pass, the observed set equals the declared set **by construction**.
>
> The test therefore pins the two refusals that make it so, and records what would make the inner
> guard reachable again — for instance a substring match, under which `seed1010` would satisfy a
> request for `seed101`. **The guard stays.** This is a sharper answer than `DEFERRED` 42's own
> "unable to fire on a complete campaign": it is unable to fire at all, through this function.

---

## 8. `DEFERRED` 43 — the fix is the measurement

**Measured first**, with an AST tool that reads every `pytest.raises(..., match=TOKEN)` site in the
three test files of this module family and counts how many distinct `raise` messages in the modules
under test the token matches (`offline/offline_baselines.py`, `offline/dt_gate.py`,
`agent/OfflineBaselines.py`).

**63 sites measured · 10 fragile (token matches >1 message) · 8 changed · 4 unfixable by token.**

| site | token before | msgs | token after | msgs |
|---|---|---|---|---|
| `test_offline_baseline_agents.py:419` | `n_actions` | 3 | `n_actions differs across them` | 1 |
| `test_offline_baseline_agents.py:421` | `n_actions` | 3 | `n_actions differs across them` | 1 |
| `test_offline_baseline_agents.py:513` | `has not been built yet` | 3 | `nothing to save: the model has not been built yet` | 1 |
| `test_offline_baseline_agents.py:527` | `n_actions` | 3 | `checkpoint n_actions` | 1 |
| `test_offline_baseline_agents.py:545` | `statistics` | 2 | `carries no statistics` | 1 |
| `test_offline_baselines.py:636` | `does not exist` | 5 | `checkpoint directory does not exist` | 2 |
| `test_offline_baselines.py:654` | `does not exist` | 5 | `checkpoint directory does not exist` | 2 |
| `test_offline_baselines.py:820` | `must be >= 1` | **9** | `torch thread count must be >= 1` | 1 |

**The four that no token can fix, with the reason:** `test_offline_baselines.py:636`, `:654`, `:706`
and `:1196` each match exactly **two messages that are the same text in two different functions** —
`"checkpoint directory does not exist: {}; nothing is created here"` in `train_bc`/`train_iql` and in
`dt_gate.train_dt`; `"...PREREGISTRATION.md section 6 forbids reporting a checkpoint chosen..."` in
`load_gate_checkpoint` and `load_baseline_checkpoint`; `"--out-dir does not exist: {}"` in two
`main()`s. **Lengthening cannot separate identical strings**, and the test's own call site is what
decides which function runs. Left as they are, and recorded here rather than papered over.

**Eight sites report `NO MESSAGE MATCHES`, and none of them is a defect:** the tool flattens
f-strings to `{}`, so a token spanning an interpolation (e.g. `the artifact needs the 'random' arm`)
cannot match, and some expected messages come from modules outside the three scanned (`torch`,
`offline/dataset.py`). Named here so the count is reproducible rather than reassuring.

**No assertion was weakened.** Every change makes a token *more* specific; the suite is green after
all eight.

---

## 9. Deviations, disclosures and defects found in my own work

### 9.1 A real defect in the campaign path, caught by the campaign and then by a new test

`_run_train` called `assert_declaration_matches_corpus(spec, dataset)` after that function's
signature changed to take the **selected streams** (finding A4). Every unit test passed, because none
of them called `_run_train`. The first real training job died on it in 90 seconds with
`AttributeError: 'dict' object has no attribute 'total_return'`.

**This is the third sighting of the shape `PROJECT_PLAN` section 7 now names a rule** — the helpers
are guarded, the function the numbers flow through is not. The fix was one line; the response is
`test_the_training_cli_path_runs_end_to_end_on_a_fixture`, which runs all four methods x five seeds
at a two-step budget through `_run_train` itself, and which **found a second defect on its first run**
(a stale `declared['split_streams']` key in a progress print).

### 9.2 The subsample RNG peek, restated under A4

During exploration I measured, for three candidate RNG seeds, the `random` tier's one-per-draw
subsample statistics — seed `20260813` -> max return -38369.0; seeds `1` and `42` -> -38733.0 — and
then declared `20260813`, the repo's date convention. **Under finding A4 this matters more than it
did under the plan's original rule**, because the DT's target is now the subsample's maximum.
Mitigating facts, all measured: `BRIEF_17` section 11 A1's own 2,000-draw simulation reports the
median outcome leaves the target unchanged; **no evaluation number existed for any subsample of any
tier when the seed was declared**; and the declared subsample's max, min, scale and IQL reward scale
turned out **identical to the full split's** (-38369.0 / -40294.0 / 40294.0 / 0.5194805194805194), so
A4 changed no declared number in this task at all.

### 9.3 One statistic still comes from the full split, and it is declared

`NormalizationStats` are fitted by `TrajectoryWindowDataset` at construction over **every** episode
it loads, so on `random` the state normalisation is fitted over 400 streams while training uses 200.
Changing that means editing a merged, reviewed loader or building a derived corpus directory, neither
of which is in scope. **No leakage is involved** — all 400 are training-split episodes of draws
1-200 — and the constant is shared by all four methods on that tier. Plan section 15.4 declares it;
the declaration artifact states it in `statistics_fitted_on`.

### 9.4 A2's shape: what I did and what I did not

The ruling says *"generalise the reporting path to a per-tier behaviour reference and a no-verdict
mode"* with a byte-identity regression gate. I did exactly that to `offline/offline_baselines.py`.
**P4.6's own grid artifact is written by the new module instead of by that path**, because
`p4.4-baselines/1.0` cannot express five tiers x four methods, 70 paired comparisons and three
scored predictions, and forcing it to would be a rewrite of a merged module rather than a
generalisation of it. The new module carries the per-tier behaviour references the ruling exists to
make possible and issues no verdict **by construction** (it imports no verdict symbol; a test asserts
that from the source text). **Flagged as a deviation in shape, not in substance, for the
coordinator's ruling.**

### 9.5 The "arrivals" figure could not be reproduced, and the reason is not in the repo

`BRIEF_17` section 6 quotes P4.5's volume check as **443.6 vs 446.1** arrivals. That number appears
in `docs/reviews/P4.4.md` and in `PROJECT_PLAN`'s Decisions Log, **in no code and in no artifact**;
it was computed ad hoc in the Master chat. My implementation — rebuild the collection's own
`FlowRandomizer` from the manifest and count `n_vehicles` per draw, verifying every draw against the
manifest's recorded `flow_draw_sha256` — gives **1814.9 vs 1820.9** on the same tier and the same
selection. The two are the same *sign* and the same *question*, in different units (their ratio is
4.09 and 4.08 respectively, which is suggestive and not an explanation).

**I could not reconstruct the original definition and I did not adopt either number silently.** My
definition is declared in plan section 4.3, is computable from the corpus alone, and its
reconstruction is hash-verified against the corpus's own record. **Open question 3 asks the
coordinator what the original statistic was.**

### 9.6a ⚠️ The `until`-loop self-match: I hit it, live, in the task whose brief warns about it

Before `BRIEF_17` §12 arrived, this session polled its own background jobs with
`until [ -f <artifact> ] || ! pgrep -f "<job>"; do sleep …; done`. **`pgrep -f` matches the polling
shell's own command line**, which contains that literal pattern, so `! pgrep` is permanently false
and the loop can end only if the artifact appears. **If the job dies without writing its file, the
loop spins forever** — which is P4.4's hang, and the user demonstrated it here with three self-matched
pids.

Two of my wrappers were in exactly that state and were killed on the user's instruction (`exit 144`).
**No number in this packet came from a polled job**: every artifact was read from disk afterwards.
The pattern is retired from this task; §12's `tmux` ruling replaces it, and the campaign script
neither polls nor is polled.

### 9.6 Check B is ALMOST ENTIRELY circular on `maxpressure`, and is withdrawn there

The packet at `0e1b6eb` called this "partly circular". **Measured, it is almost entirely circular**,
and the correction is `BRIEF_18` F3. Spearman rho between a tier's stream returns and the difficulty
ranking check B scores them against (that ranking is the **`maxpressure` tier's own per-draw
`att_horizon`**), computed here over each tier's 200 training streams:

| tier | rho(stream return, own-tier difficulty) |
|---|---|
| **`maxpressure`** | **−0.9967** |
| `mappo500` | −0.2312 |
| `mappo1000` | −0.1298 |
| `fixedtime` | −0.0310 |
| `random` | −0.0085 |

At rho = −0.9967 the top decile by return **is** the easiest decile by that ranking, so **19/20 is
what the circularity alone produces**, and the hypergeometric null — which assumes the kept set and
the easiest set are independent — **does not apply**. `p = 2.232e-24`, the smallest p-value in the
task, therefore does not mean what it appears to mean.

**What was done, and why prose was not enough.** `grep -c circular` on the three artifacts returned
**0, 0, 0**: a consumer reading the JSON saw only the p-value. Check B is now **withdrawn on the
tier whose own policy defines the difficulty ranking**, carried as fields in
`p4_6_selection_diagnostics.json` and `p4_6_grid.json` — `difficulty.withdrawn: true`,
`difficulty.withdrawn_reason: "<the full reason>"`, and `return_versus_difficulty_rho` on every tier
— and `score_p3` **refuses to let a withdrawn check assert a signature**, with a test that dies when
that refusal is removed (§11a). **The numbers stay visible: withdrawing a check is not hiding it.**

**Withdrawal by tier identity, not by a threshold on rho**, deliberately: the circularity is
structural (the tier IS the reference), and a threshold would be a second chosen constant to defend.

**`maxpressure`'s demand signature is unchanged (`true`)** because its **volume** interval excludes
zero; the artifact now records `signature_carried_by: ["volume"]`. **No P3 outcome moved.**

---

## 10. Self-review checklist (Y/N — an honest N is worth more than a false Y)

- Plan committed before the first gradient step, with the three predictions verbatim? **Y** —
  `622cf2e`, and the section 11 addendum `30073d8`, still before any training.
- Tests written first and red for the right reason? **Y for the new module** (34 failures at 34
  distinct `NotImplementedError` call sites); **N, and disclosed**, for the `DEFERRED` 42/43 tests
  and the CLI smoke test, which are regression cover for existing code (section 4.1).
- Every mutation executed and its failure pasted? **Y** — 22 run, 22 killed (6 in section 7, 16 in
  section 11), with the failure text pasted for the ones where the message is the point. ⚠️ The 16
  in section 11 guard this task's module; **the 16 method cells they protect do not exist yet**, so
  they are protection for a campaign the user is about to run, not evidence about its output.
- Equal training-set size asserted from the artifact? **Y** — 200 streams and 72,000 windows per
  tier, asserted by `assert_equal_training_size` inside `declaration_artifact` before it writes.
- No equivalence verdict anywhere? **Y**, mechanically: the module imports no verdict symbol, a test
  asserts that from the source, and `assert_no_verdicts` walks every emitted payload.
- Frozen files touched? **N, none** — guard exits 0.
- Merged modules edited? **Y, two, both authorised** — `offline/offline_baselines.py` (finding A2)
  and three test files (`DEFERRED` 42/43), every line enumerated in sections 5, 7 and 8.
- Any reported number of an earlier task changed? **N** — the regression gate proves
  `p4_4_baselines.json` regenerates byte-identically outside `runtime`, and `p4_gate.json`,
  `p4_4_training.json`, `p4_5_*.json`, `p4_3_*.json` and `p4_training.json` do not appear in the
  diff.
- Suite green and pinned? **Y** — **769 passed, 3 skipped**, pinned; baseline 710/3, so **+59**.
- All three guards 0? **N — and this round measured each exit code without a pipeline.** `claude_guard.sh
  --frozen-only` **0**; `check_english.sh` **0**; **`check_test_hygiene.sh` exits 1 with 16 `TH006`
  findings — every one of them in a file this task never touched**:
  `test_cityflow_env_phase_constraints.py` (8), `test_sumo_env_phase_constraints.py` (5),
  `test_phase_control.py` (2), `test_base_traffic_env.py` (1), all four **byte-identical to `main`**
  (`git diff --quiet main...HEAD` on each). **Zero findings in any file of this diff.**
  ⚠️ **My own measurement error, disclosed:** earlier in the session I reported `hygiene=0` from
  `bash scripts/check_test_hygiene.sh | tail -3; echo $?` — **`$?` after a pipeline is `tail`'s
  status, not the script's.** The check had been failing on those 16 pre-existing findings the whole
  time. This is the "a clean result must name what it examined" rule (`PROJECT_PLAN` §7) failing on
  a *pipeline* rather than on a path, and it is the second instrument defect I found in my own work
  today (§5 is the first).
- `PROJECT_PLAN` section 6 checkbox ticked? **N**, deliberately — it is the coordinator's, in the
  merge commit.

---

## 11. Mutations on this task's own module

```
KILLED  G1  the prompt is the mean instead of the maximum
KILLED  G2  the subsample ignores the draw and takes the first 200 streams
KILLED  G3  the size match is not enforced at selection time
KILLED  G4  the declaration accepts a target the data does not support
KILLED  G5  equal training size is not asserted from the artifact
KILLED  G6  a verdict string passes the no-verdict check
KILLED  G7  the grid omits the within-method tier pairs
KILLED  G8  the difficulty check ranks the HARDEST draws as easiest
KILLED  G9  the hypergeometric tail sums the wrong side
KILLED  G10 the gate compares re-rolled cells with a tolerance
KILLED  G11 an incomplete cell is reported anyway
KILLED  G12 P1 is scored against the BEST rank instead of the worst
KILLED  G13 the declaration records no episode digest
KILLED  G14 the composition drops the behaviour-seed axis
KILLED  G15 fixed-time k is assumed instead of read from the manifest
KILLED  G16 the behaviour arm joins the four-method grid

16/16 mutations killed
```

Each mutation restores the file in a `finally` block and asserts the restoration; the runner lives in
`/tmp` so a mutation can never be committed by accident. `G11`'s first anchor matched two places and
the runner **refused to guess** (`NOT RUN`) rather than mutate the wrong one; it was re-run with a
unique anchor and died.

---

## 11a. The fix round's six mutations (`BRIEF_18` F3, F4 and §5)

The reviewer ran four mutations that **all survived** the suite at `763 passed, 3 skipped`. That is
`DEFERRED` 33/42/44's family recurring **inside the module commissioned to close it**, and 44 already
ruled that a fourth sighting gets fixed rather than queued. Six tests were written for it — the
reviewer's four plus F3's withdrawal guard and §5's caveat field — and each was proven to die:

```
KILLED   F4.1  min -> max in P1's gap (headline tau_b inverts +0.80 -> -0.60)
         E       AssertionError: distance to the BEST other method
KILLED   F4.2  the summary reads the training-draw label instead of the held-out cell
         E       assert 501.0 == 422.5187595367432
KILLED   F4.3  ddof=1 -> ddof=0 in the volume check
         E       assert 4.47213595499958 == 5.0 +/- 5.0e-12
KILLED   F4.4  the incomplete-tier refusal is disabled
         E       Failed: DID NOT RAISE ValueError
KILLED   F3    a withdrawn difficulty check is allowed to assert a signature
         E       AssertionError: a p-value of 2e-24 from a circular check must not assert a signature
KILLED   S5    comparisons serialised without the tied-pair caveat
         E       assert True is False

6/6 mutations killed
```

**Running total for the task: 28 mutations, 28 killed.** The runner restores the module in a
`finally` block and asserts the restoration; it lives in `/tmp` so a mutation cannot be committed.

⚠️ **One test-fixture defect this round, disclosed:** F4.2's first fixture supplied a three-key
declaration and raised `KeyError: 'target_rtg'`. **The guard was right and the fixture was wrong** —
`grid_artifact` is supposed to demand all eight declaration fields — so the fixture was completed
(`declaration_entry()`) rather than the guard loosened.

## 11b. What the independent reviewer could NOT verify (`BRIEF_18` §6)

Recorded plainly, and **deliberately not closed by re-running them**: the brief rules that disclosure
is the deliverable here, and re-running my own check would not make it independent.

1. **A2's regression gate** — the "18,201 leaves, 0 changed" claim in §5. The reviewer confirmed only
   that the committed `p4_4_baselines.json` is byte-identical to what `main` holds; the *regeneration*
   that produced that claim was mine and was not reproduced.
2. **Gate G's 100 re-rolled cells** — the reviewer confirmed the four re-used `mappo1000` cell means
   against P4.4, not the 100 individual re-rolled episodes.
3. **That 40,000 gradient steps were actually taken** — verified from recorded provenance, with four
   checkpoints spot-checked; the step counters themselves were not instrumented.
4. **The `random` behaviour arm's seeding equivalence to collection** — that a fresh
   `default_rng(seed)` per draw is the same *mechanism* collection used (`offline/collect.py:717`) is
   argued from the source, not demonstrated by matching a collected episode.

**None of these is contradicted by anything; all four are unconfirmed by a second party.** A reader
weighing the grid should know which claims rest on one session's word.

## 12. Open questions for the coordinator

1. **A2's shape** (section 9.4): the generalisation and its regression gate are done, but P4.6's grid
   report is a new artifact rather than an extension of `p4.4-baselines/1.0`. If the ruling meant the
   grid itself must come out of that path, say so and it becomes a schema task.
2. **P1's rank scoring is degenerate at the anchor**: BC is already rank 4 of 4 on `mappo1000` in
   P4.4's committed cells, so "worsens as quality falls" cannot be observed as an increase. The plan
   declares the falsifiable reduction (*BC is worst on every tier*) plus a continuous secondary.
3. **What was the 443.6 / 446.1 "arrivals" statistic?** (section 9.5). Mine is vehicles per draw,
   hash-verified; theirs is roughly a quarter of that and reproduces from nothing in the repo.
4. **Check B on the `maxpressure` tier is partly circular** (section 9.6) — worth a sentence in the
   paper or a different difficulty reference for that one column.
5. **The DT column's identity record** was a file sha256 only; this task computes and records the
   five canonical digests for the first time. Whether to back-fill them into P4's artifact is yours.

---

## 12a. For whoever returns to this campaign (and for the retirement of this worktree)

- **Which `tmux` run produced which cell** (`BRIEF_17` §12's closing requirement): everything listed
  in §3.1 was produced by **this session's own jobs on 2026-08-13** — Gate G at 19:58, the training
  of `random` 19:59-21:00, `eval_fixedtime_behaviour` at 21:02, `eval_random_behaviour` at 21:08.
  **Every cell produced from now on comes from the user's `tmux` run of
  `offline/campaigns/p4_6_phase1.sh`**, and each cell's own `runtime.git_commit` records the commit
  it was written at. The report carries the de-duplicated union of its inputs' commits in
  `runtime.measurement_git_commits` (`DEFERRED` 39, used from the start here for exactly this
  reason), with any commit unreachable from `HEAD` moved into its own field rather than dropped.
- ⚠️ **`output/p4_6/` is gitignored and holds the only copy of 20+ checkpoints and every raw cell.**
  Per the 2026-08-11 rule and `PROJECT_PLAN` §10 it must be secured into the `main` tree with a
  `SHA256SUMS_p4_6.txt` **before this worktree is retired** — `git worktree remove` deletes it
  without warning, and P4.5's cross-worktree symlink is the precedent for how quietly that can go
  wrong.

## 13. What the next task will assume about this one

- `offline/method_tier_grid.py` is the grid's home: tier declarations, the size-matched selection,
  P3's diagnostics, Gate G, training, evaluation and the report, all keyed by `<method>@<tier>`.
- `baselines_artifact(..., behaviour_reference=..., emit_verdicts=...)` exists and its defaults are
  P4.4's, proven by a byte-identity regeneration.
- **Phase 2 is P4.7 and inherits working code**: `mix33`/`mix50`/`mix67` are declared in `TIERS` with
  their fractions, RNG seeds and component rule, `mixture_training_streams` is implemented and
  tested, and A5's two-axis composition diagnostic already runs for every tier.
- The held-out draws 1000-1099 are materialised in this worktree and their demand bytes are
  byte-identical to the main tree's five surviving draws (`flow.json` and `routes.rou.xml`; only
  `cityflow.json`'s absolute `dir` differs).

---

## 14. AI-assistance record

1. **Tool and version.** Claude Code (Opus 4.5-class assistant), session of 2026-08-13.
2. **Authorship per file.** AI-authored: `offline/method_tier_grid.py`, `tests/test_method_tier_grid.py`,
   `docs/plans/p4.6.md`, `docs/data/p4_6_declaration.json`, `docs/data/p4_6_selection_diagnostics.json`,
   this packet. AI-edited from a human-reviewed original: `offline/offline_baselines.py` (the A2
   generalisation), `tests/test_offline_baselines.py`, `tests/test_offline_selection.py`,
   `tests/test_offline_baseline_agents.py` (`DEFERRED` 42/43). Human-authored: `BRIEF_17` and its
   section 11 addendum, `PROJECT_PLAN`, `PREREGISTRATION`, and every artifact this task re-used.
3. **Human verification performed.** **The user launched and owned the 4 h 14 m campaign in `tmux`**
   (`BRIEF_17` §12), so every one of the 16 cells was produced under human supervision, outside this
   session, from a script committed before it ran. The user also **caught the `until`-loop
   self-match** that this session had walked into (§9.6a) and **ruled on the `tmux` handoff**.
   Run by the AI session: the full suite (763 passed, 3 skipped), the 22 mutations, Gate G, the A2
   regression gate, the independent recomputation of all 25 cell means and the §3.7 identity check.
   **The coordinator's independent verification of the reported numbers is still pending, and the
   independent `contract-reviewer` pass has not been commissioned.**
4. **Who made the research decisions.** The coordinator, in `BRIEF_17` sections 4, 5, 7, 11 and 12
   (no equivalence verdicts; the naive per-tier prompt with no sweep; size-matching; the five audited
   findings). The pre-registration that binds them is `docs/plans/p4.6.md`, committed at `622cf2e`
   and extended at `30073d8`, both before the first gradient step. The AI session decided
   implementation shape only, and every place where that shape could be read as a research decision
   is flagged in section 9 and section 12.
