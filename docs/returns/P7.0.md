# RETURN PACKET — P7.0 (the C3 early gate, CityFlow → SUMO)
**Date:** 2026-08-16 · **Mode:** Claude Code · **Contracts:** v1.1

**Status: DONE**, after the coordinator's ruling of 2026-08-16 and the fix round that implements it.

*(This packet reported **PARTIAL** at `35ec8d7`, when the gate had returned branch B from an alignment
whose input premise I had just falsified. The coordinator voided that reading, re-registered the
alignment and ruled the verdict. §3 is rewritten to the ruling; the superseded reading is preserved,
labelled, in the artifact and in §3.2.)*

> ## THE RESULT, IN ONE BLOCK
> **BRANCH C.** No B criterion fires. The A conjunction fails on **two independent criteria** —
> **A2** (state distributions, min overlap 0.340720 short of 0.50 by **0.159280**) and **A4**
> (normalised return, ρ gap 6.057427 against 0.5·M = 1.807687, short by **4.249739**, a factor of 3.4).
> **Reported as two because a single-criterion C would read as a near miss, and this is not one.**
>
> **Operationally: C3 is neither licensed nor descoped. CUT 3 of the 2026-08-14 sequencing ruling does
> NOT fire. P5 is next regardless.** ⚠️ `BRIEF_21` §5's Branch C sentence now binds — *a gate that
> returns "unclear" and is read as "proceed" is how a kill-switch stops being one.*

---

## 1. Branch + diff stat

```
task/p7.0-transfer-gate
$ git diff --stat main...HEAD
 docs/data/p7_0_gate.json                           | 1481 +++++
 docs/plans/p7.0.md                                 |  513 ++
 offline/parity.py                                  |  492 ++
 offline/transfer_gate.py                           | 1178 ++++
 .../hangzhou_1x1_bc-tyc_18041610_1h_parity.rou.xml | 6067 ++++++++++++++++++++
 .../hangzhou_1x1_bc-tyc_18041610_1h_parity.sumocfg |   15 +
 tests/test_parity_vtype.py                         |  365 ++
 tests/test_transfer_gate.py                        |  570 ++
 8 files changed, 10681 insertions(+)
```

Six commits: `cf1c950` plan · `ff6b805` the vType binding · `0b58b31` the gate module · `9f389c2`
the run · `35ec8d7` the PARTIAL packet · `a2d023c` the fix round · this packet.

**Zero frozen files touched.** `git diff --name-only main...HEAD` lists eight paths, none matching
`claude_guard.sh`'s `FROZEN_PATTERNS`; the guard agrees (`--frozen-only`, exit 0). `scenarios/` is not
in the frozen set (`scripts/claude_guard.sh:44`) and **no existing scenario file was edited** — the two
files under `scenarios/hangzhou_1x1_bc-tyc_18041610_1h_parity/` are new.

---

## 2. Files produced

- `docs/plans/p7.0.md` — the registered plan; §5 and §6 written before any number existed.
- `offline/parity.py` — the parity contract as a committed artifact, each value's source named, plus
  the renderer and the mechanical binding check.
- `scenarios/hangzhou_1x1_bc-tyc_18041610_1h_parity/*.rou.xml`, `*.sumocfg` — the bound copies.
- `offline/transfer_gate.py` — the statistics, the three alignments with their standing, the
  registered branch rule and its margins, the `DEFERRED` 18 and 23 checks, §5.4's green-action table
  with its control, and the campaign driver.
- `tests/test_parity_vtype.py` (22 tests) · `tests/test_transfer_gate.py` (52 tests).
- `docs/data/p7_0_gate.json` — the gate artifact (39 KB).
- `output/p7_0/` — 30 episodes, 812 KB, **gitignored**, in this worktree only.

---

## 3. The result, and the defect that reshaped it

### 3.1 What happened, in order

1. The gate ran under the registered §5.3 alignment (**by lane id**) and returned **branch B**, firing
   B2 alone at min overlap **0.0554**.
2. Before reporting it I checked why the per-lane means looked *swapped* within each road, and found
   the cause in the scenario files: **the lane-index convention is reversed between the backends.**
3. I reported **PARTIAL**, declared no branch, and handed the ruling up.
4. **The coordinator verified the inversion independently from the roadnet and the `.net.xml` alone,
   with no result in hand**, voided the by-id reading, re-registered the movement-paired alignment,
   and ruled the verdict. `docs/plans/p7.0.md` §11 records it.

### 3.2 The defect: a lane id is not a valid cross-backend key here

Read from the two scenario files — **structural attributes, not outcomes**:

| road (all four incoming) | CityFlow `roadnet.json` lane `_0` | lane `_1` | SUMO `.net.xml` lane `_0` | lane `_1` |
|---|---|---|---|---|
| `road_0_1_0`, `road_1_0_1`, `road_2_1_2`, `road_1_2_3` | `turn_left` | `go_straight` | `dir="s"` | `dir="l"` |

SUMO indexes lanes right-to-left, CityFlow left-to-right. **8 of 8 lane ids denote a different physical
lane.** The by-id table compared a left-turn lane against a through lane, so it **is not a measurement
of dynamics shift at all**.

> ⚠️ **VOID means VOID, not unfavourable.** That reading yields **no branch — not B, and not C either**.
> Branch C is a *valid instrument landing in the middle band*; this was an *invalid instrument*. **B is
> recorded nowhere except as the voided reading**, preserved verbatim in the artifact under
> `voided_per_feature` with its standing attached. Its numbers are byte-identical to the original run's,
> checked.
>
> ⚠️ **DISCLOSURE: all three readings had already been computed when the re-registration was made.**
> The choice is nonetheless not outcome-driven, and the argument is the coordinator's: **the invalidity
> is establishable from the network files with no result whatsoever, and it was established that way.
> The error is a fact about the roadnet, not about the outcome.**

### 3.3 The registered reading: movement-paired, 16 features, never pooled

Each CityFlow incoming lane against the SUMO lane **on the same road serving the same movement set**,
the correspondence derived from the `type`/`dir` attributes alone and refusing anything but a unique
match.

| feature (CityFlow lane → SUMO lane) | turn | KS | OVL | mean_cf | mean_su |
|---|---|---|---|---|---|
| `lane_vehicle_count@road_0_1_0_1` → `road_0_1_0_0` | s | 0.2659 | 0.5573 | 24.2133 | 27.7679 |
| `lane_vehicle_count@road_0_1_0_0` → `road_0_1_0_1` | l | 0.4659 | 0.5119 | 7.8476 | 15.2404 |
| `lane_vehicle_count@road_1_0_1_1` → `road_1_0_1_0` | s | 0.2515 | 0.6343 | 23.3961 | 26.7551 |
| `lane_vehicle_count@road_1_0_1_0` → `road_1_0_1_1` | l | 0.5091 | 0.4698 | 8.3019 | 16.6393 |
| `lane_vehicle_count@road_2_1_2_1` → `road_2_1_2_0` | s | 0.4122 | 0.5468 | 7.5014 | 16.6194 |
| `lane_vehicle_count@road_2_1_2_0` → `road_2_1_2_1` | l | 0.0997 | 0.6105 | 19.2853 | 20.1075 |
| `lane_vehicle_count@road_1_2_3_0` → `road_1_2_3_1` | l | 0.1607 | 0.6698 | 18.3186 | 20.7263 |
| `lane_vehicle_count@road_1_2_3_1` → `road_1_2_3_0` | s | 0.4643 | 0.5296 | 9.0637 | 18.0837 |
| `lane_waiting_vehicle_count@road_0_1_0_1` → `road_0_1_0_0` | s | 0.5745 | 0.4255 | 15.9834 | 5.4665 |
| `lane_waiting_vehicle_count@road_0_1_0_0` → `road_0_1_0_1` | l | 0.2975 | 0.6488 | 6.9307 | 10.6515 |
| `lane_waiting_vehicle_count@road_1_0_1_1` → `road_1_0_1_0` | s | **0.6593** | **0.3407** | 15.5097 | 4.3474 |
| `lane_waiting_vehicle_count@road_1_0_1_0` → `road_1_0_1_1` | l | 0.3452 | 0.5850 | 7.3712 | 11.8006 |
| `lane_waiting_vehicle_count@road_2_1_2_1` → `road_2_1_2_0` | s | 0.1097 | 0.7867 | 3.7839 | 3.0343 |
| `lane_waiting_vehicle_count@road_2_1_2_0` → `road_2_1_2_1` | l | 0.2632 | 0.5850 | 18.4211 | 15.5147 |
| `lane_waiting_vehicle_count@road_1_2_3_0` → `road_1_2_3_1` | l | 0.1607 | 0.6676 | 17.5374 | 16.4327 |
| `lane_waiting_vehicle_count@road_1_2_3_1` → `road_1_2_3_0` | s | 0.2299 | 0.7579 | 4.9003 | 3.0443 |

The three readings side by side, with the first **void** and the third a **retained diagnostic**:

| reading | standing | min OVL | max KS | KS > 0.50 |
|---|---|---|---|---|
| by lane id | **VOID — yields no branch** | 0.0554 | 0.9446 | 8 / 16 |
| **by movement** | **REGISTERED** | **0.3407** | **0.6593** | **3 / 16** |
| road level | diagnostic, not promoted (8 features do not map onto thresholds written for 16) | 0.5313 | 0.4166 | 0 / 8 |

### 3.4 The verdict: BRANCH C, on two independent failures, with every boundary distance

**The thresholds did not move. Not one.** They are exactly as registered at `cf1c950`.

| criterion | statistic | threshold | satisfied when | boundary distance | fired |
|---|---|---|---|---|---|
| **B1** | 11.605005 | 0.000000 | `>` | +11.605005 | no |
| **B2** | 0.340720 | 0.300000 | `>=` | **+0.040720** | no |
| **B3** | 3.000000 | 9.000000 | `<` | +6.000000 | no |
| **B4** | 6.057427 | 7.230750 | `<=` | +1.173323 | no |
| **A1** | 11.605005 | 0.000000 | `>` | +11.605005 | no |
| **A2** | 0.340720 | 0.500000 | `>=` | **−0.159280** | ❌ **FAILS** |
| **A3max** | 0.659280 | 0.700000 | `<=` | +0.040720 | no |
| **A3count** | 3.000000 | 4.000000 | `<=` | +1.000000 | no |
| **A4** | 6.057427 | 1.807687 | `<=` | **−4.249739** | ❌ **FAILS** |

**Two independent failures, and both are reported because a single-criterion C would read as a near
miss:** A2 on the state distributions, A4 on the normalised return, and A4 misses by a **factor of 3.4**.

**Non-firing criteria, ranked by relative distance** — the full ranking rather than one winner:
`A3max 0.040720 · B2 0.040720 · A3count 0.062500 · B1 0.308115 · A1 0.308115 · B4 0.324537 · B3 0.375000`.

⚠️ **B2 clears by only 0.040720 — the closest call in the table**, as the coordinator asked to see.
**A3max sits at the same distance, and that is structural rather than coincidence:** both extrema come
from the **same feature**, `lane_waiting_vehicle_count@road_1_0_1_1 → road_1_0_1_0`, on which
`OVL + KS = 1.0` exactly, and the two thresholds `0.30` and `0.70` also sum to 1.0. A lone
`nearest_non_firing` would have hidden one of them behind a float tie-break, so the artifact now emits
the whole ranking. *(Reported at 6 dp: `2·M = 7.230750`, which the ruling quoted as 7.2308 — a display
rounding, immaterial to every comparison.)*

### 3.5 What Branch C means, and what it does not

> **C3 is neither licensed nor descoped. CUT 3 of the 2026-08-14 sequencing ruling does NOT fire.
> P5 is next regardless.**

⚠️ **"Unclear" must not be read as "proceed".** The artifact carries that sentence in
`branch.operational_meaning`, beside the branch, so a hasty reader meets it at the same moment.

### 3.6 The ρ half, unaffected by the lane defect because ATT is network-level

| cell | ATT at horizon (A1) | sd | `vehicle_count` at horizon (A5) | distinct episodes |
|---|---|---|---|---|
| cityflow maxpressure | 247.7509 | 0.0000 | 208.00 | 1 / 5 |
| cityflow fixedtime | 285.4154 | 0.0000 | 155.00 | 1 / 5 |
| cityflow random | 421.5868 | 15.6703 | 157.00 | 5 / 5 |
| sumo maxpressure | 360.0205 | 18.6757 | 255.20 | 5 / 5 |
| sumo fixedtime | 371.6255 | 3.3798 | 176.20 | 5 / 5 |
| sumo random | 483.8784 | 11.8919 | 164.80 | 5 / 5 |

`Δ_cityflow = 37.6645`, `Δ_sumo = 11.6050` — **MaxPressure beats fixed-time in both backends**, so the
registered normalisation is well-posed and B1 does not fire. `ρ(random) = −3.615375` (CityFlow) against
`−9.672802` (SUMO): same sign, gap `6.057427`.

**No raw travel time is compared across backends anywhere.** The ATT column is per (backend, policy) so
the *within-backend* arithmetic is checkable; every cross-backend statement is about ρ or a distribution.

**Independent cross-check:** `cityflow maxpressure` ATT at the horizon is **247.7509**, reproducing
`PROJECT_PLAN` P8.0's registered A1 anchor of **247.75**. CityFlow `random` is 421.5868 ± 15.6703
against the registered 413.53; amendment **A2** records that `collect.py`'s random policy is a different
realisation from `experiments.runner._baseline_chooser`, so a difference is expected and the anchor sits
inside the spread. **I did not treat that as a match.**

### 3.7 §5.4 delivered: the action vocabulary DOES transfer, with its control

Owed by the plan and listed as undelivered in the PARTIAL packet; deliverable now that the alignment is
understood. For each green action index, the set of incoming lanes it releases in each backend, the
SUMO set translated through the movement-paired key:

**All 8 actions release the same physical lanes in both backends.** CityFlow file phases 1–8 map onto
SUMO file phases 0, 2, 4, 6, 8, 10, 12, 14.

| action | 0 | 1 | 2 | 3 | 4 | 5 | 6 | 7 |
|---|---|---|---|---|---|---|---|---|
| agree | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ |

⚠️ **Its control, without which 8/8 would be worth nothing: under the VOID identity key only 4/8 agree**
— and the four that agree anyway are exactly the actions releasing **both** lanes of a single road,
which are symmetric under a within-road swap. So the table has discriminating power and it is the
translation that supplies it.

**This is a positive C3 result and it is worth stating plainly:** an action index means the same thing
in both backends. That is a transfer prerequisite nobody had verified, and it holds. It also
corroborates the lane correspondence from a different field of the same files (`phase_roadlink_mapping`
+ `roadlink_lanes`, rather than `type`/`dir`) — a consistency check rather than a fully independent
derivation, and stated as such.

## 4. The vType binding (`BRIEF_21` §2), with its positive control

Live, this session:

```
$ .venv/bin/python -m offline.parity --write
parity contract v1.0; flow.json disagreements: 0
shipped  ...bc-tyc_18041610_1h.rou.xml:        vehicles=2021 with type=0    vTypes=['pkw']      bound=False
parity   ...bc-tyc_18041610_1h_parity.rou.xml: vehicles=2021 with type=2021 vTypes=['cf_parity'] bound=True
```

**`bound=False` on the shipped file is the positive control the brief requires**, encoded as
`test_shipped_file_is_not_bound_positive_control` so it can never silently pass.

The bound type carries `BRIEF_04` §3's full contract, **not** a copy of the shipped `pkw`:
`accel=2.0 decel=4.5 length=5.0 maxSpeed=11.11 minGap=2.5 speedFactor=1.0 tau=2.0 width=2.0`, id
`cf_parity`. `sigma` stays native and is declared unmatchable alongside CityFlow's
`usualPosAcc`/`usualNegAcc`. `flow_json_disagreements` returns `[]` — the committed table equals the
scenario's own `flow.json`, checked with `==` on floats.

**Horizon margin, per the coordinator's ruling:** `sumo_end=4000`, `env_horizon=3600`, margin `400 s`,
and **every one of the 15 SUMO episodes reached a final `sim_time` of exactly 3600.0** — so the
backend-asymmetric truncation the ruling names did not occur, measured rather than assumed.

---

## 5. `DEFERRED` 18 — CLOSED on SUMO, with both controls

| field | value |
|---|---|
| metrics A / B | 1 metric / 3 metrics, key sets verified different |
| rows compared | 361 |
| `np.array_equal` | **True** |
| max abs difference | **0.0** |
| ATT at horizon, A and B | 355.7984322508399 / 355.7984322508399 |
| **vacuity control** | identical metric sets are **refused** (`ValueError`), and the refusal is tested |
| **positive control** | a different-policy rollout gives `np.array_equal == False` (492.2801 vs 355.7984) |

Plus the structural half the empirical half cannot reach, and it changes what the row can conclude:
`envs/sumo_env.py:119-125` unions `average_travel_time` into `_metric_names` **unconditionally**, so
`metrics/sumo.py:77`'s `_track_completed` gate is always open and **"the metric is not requested" is
unreachable through `SumoEnv`**. The row's underlying worry is real — that gate genuinely exists on
SUMO and genuinely does not on CityFlow — but it cannot bite through this env. Reported as
`att_always_requested: true`.

**Also checked and clean, so it is not left as an unknown:** both backends define a waiting vehicle at
**0.1 m/s** — CityFlow `CityFlow/src/engine/engine.cpp:641` and SUMO's `getLastStepHaltingNumber`.
⚠️ The SUMO side rests on that function's *documented* threshold; I did not measure it.

---

## 6. `DEFERRED` 23 — reported, and it produced two findings, one inverted

**hangzhou 1×1 (the gate scenario).** Intersection order agrees — but there is exactly one
intersection, so the report is **true and uninformative**, and I am labelling it that way rather than
counting it as evidence. Phase counts differ: **CityFlow 9, SUMO 16**.

**cologne3 (the only paired multi-intersection scenario, which is why it was added).**

| backend | intersection order |
|---|---|
| CityFlow | `360082`, `360086`, `cluster_2415878664_254486231_359566_359576` |
| SUMO | `360082`, `360086`, `GS_cluster_2415878664_254486231_359566_359576` |

🚨 **The id SETS differ: the same physical intersection is `cluster_…` in CityFlow and `GS_cluster_…`
in SUMO.** So across backends **ID keying breaks while positional keying happens to work here** — the
opposite of the failure the row anticipated. That does not weaken ID keying *within* a backend, which
is what the corpus does and what the row is about; it means **neither ids nor positions are a valid
cross-backend intersection key**, which is a P7.3 prerequisite nobody had written down.

**Lane order.** Differs on hangzhou (same set of 8, both orders recorded) and on all three cologne3
intersections (same set for two of three). **The lane-keying mutant is now LIVE**, and §3.2 shows it is
worse than a permutation: the ids are not merely ordered differently, they denote different lanes.

---

## 7. Tests — REAL OUTPUT

Red first, in both code commits, and each failure reaches the real API surface rather than one shared
`ModuleNotFoundError` (signature-only skeletons raising `NotImplementedError`):

```
$ pytest tests/test_parity_vtype.py -q      →  21 failed, 1 passed in 0.49s
$ pytest tests/test_transfer_gate.py -q     →  33 failed, 2 passed in 0.39s
```
The passing ones use only merged code (the fixed-time cycle identity; the permutation fixture's own
non-vacuity; the threshold constants).

Green (after the fix round, which added 8 tests to the gate file):
```
$ pytest tests/test_parity_vtype.py -q      →  22 passed in 0.39s
$ pytest tests/test_transfer_gate.py -q     →  52 passed, 3 warnings in 9.65s
```

Full suite, pinned with `OMP_NUM_THREADS=1 MKL_NUM_THREADS=1` and both corpus env vars set, run from
the committed tree:
```
$ OMP_NUM_THREADS=1 MKL_NUM_THREADS=1 RLTRAFFIC_CORPUS_V11=... RLTRAFFIC_CORPUS=... pytest -q
923 passed, 3 skipped, 15 warnings in 100.42s (0:01:40)
```
Before the fix round it was `915 passed, 3 skipped` (run twice on identical trees, 106.53s and
103.79s, same counts).
Branch point on `main` was **849 passed, 3 skipped in 100.58s** under the same pin, so this task adds
**74** tests and removes none. **The pin is stated because `DEFERRED` 41 requires it**; the suite was
not run unpinned in this worktree.

### 7.1 Mutations — 14 executed, 14 killed, every failure observed

| # | mutation | what failed |
|---|---|---|
| M1 | parity `tau` 2.0 → 1.0 | 7 tests; `ValueError: tau: declared 1.0 disagrees with its declared source headwayTime=2.0` |
| M2 | unbind 1 of 2021 vehicles | `AssertionError: assert 2020 == 2021` |
| M9 | emit vType attributes unsorted | byte-identical regeneration fails, diff shows the reordered element |
| M11 | renderer corrupts one departure time | `ValueError: the rendered route file changed a vehicle id, departure or route` |
| M3 | lane alignment positional not by id | compared `array([1,2,3])` against `array([300,400,500])` |
| M4 | KS `max` → `mean` | `assert 39 == 86` against the brute-force numerator; `assert 0.5 == 1.0` on the disjoint endpoint |
| M5 | overlap `min` → `max` | `assert 921 == 369`; `assert 2.0 == 0.0` on the disjoint endpoint |
| M6 | ρ numerator sign flip | `assert -1.0 == 1.0` on the MaxPressure anchor |
| M7 | branch B on ALL B criteria instead of ANY | `assert 'C' == 'B'` |
| M8 | delete the `DEFERRED` 18 vacuity refusal | `Failed: DID NOT RAISE ValueError` |
| M10 | drop the signed-distance orientation | `B3 ... signed_distance=-9.0, fired=False` — the margin stopped agreeing with the verdict |
| **M12** | **B2's MINIMUM overlap → a MEAN** | **`assert 'A' == 'B'`** |
| **M13** | **`compare_lane_features` silently ignores the correspondence** | **`assert 0.0 == 1.0`** |
| M14 | the green-action table skips the movement translation | `assert False is True` |

**Two are worth reading twice.** **M12**: replacing the minimum with a mean turned a single
catastrophic feature (OVL 0.29 among fifteen 0.90s) from branch **B** into branch **A** — the pooling
failure `BRIEF_21` §4 forbids, demonstrated rather than asserted. **M13**: making
`compare_lane_features` fall back to the identity key is **the exact defect that caused this fix
round**, and it is now under test rather than under discussion. Every mutation was reverted and the
reversion verified by `sha256sum -c` before the next one.

### 7.2 Regeneration and no-drift proofs

**Before the fix round:** the whole campaign was re-run from scratch after the diagnostic was added and
every registered block compared leaf by leaf against the first run — **657 leaves, 0 changed**, only
the two additive keys appearing. SUMO reproduces bit-exactly under its seeds.

**The fix round re-ran no simulation.** The 30 episodes on disk were reused via `--skip-collect`;
re-running identical computation to launder the registration order would be theatre. Re-executed: the
analysis, the two deterministic side-checks inside the driver, and the artifact write. Proved:

- **329 leaves** across `cells`, `rho`, `deferred_18`, `parity`, `horizon_margin`, `lane_alignment`:
  **0 changed** — and that includes `deferred_18`, whose three SUMO rollouts re-ran and reproduced
  identically.
- `deferred_23`: **78 common leaves, 0 changed**, 177 added, **all** under `green_action_lane_sets`.
- The **VOID** rows in the artifact are byte-identical to the original run's `per_feature`, so the
  superseded reading is preserved rather than recomputed.
- The **registered** rows are identical to the semantic table the earlier diagnostic had produced **by
  a different route** — an independent-route confirmation of the numbers the verdict rests on.
- Adding the identity-key control changed **0 leaves** outside `runtime` and added 84, all under it.

### 7.3 Guards — all three, NO arguments, counted from full output

| guard | exit | finding count | corpus |
|---|---|---|---|
| `claude_guard.sh` | **0** | 0 lines of output | the working tree via `git status --porcelain` |
| `check_english.sh` | 1 | **5** | the whole repo |
| `check_test_hygiene.sh` | 1 | **16** `TH006` | the whole `tests/` tree |

Both non-zero exits are **inherited and identical to the branch point**, measured there before any of
my work: the 5 English findings are `.claude/agents/master-coordinator.md:158`, `README.md:30`,
`docs/patches/claude_guard_g1.patch:32`, `docs/patches/claude_guard_hygiene.patch:4`,
`scripts/claude_guard.sh:47`; the 16 `TH006` split `8 test_cityflow_env_phase_constraints · 5
test_sumo_env_phase_constraints · 2 test_phase_control · 1 test_base_traffic_env`, exactly `DEFERRED`
45's four-times-confirmed baseline. **None is in a file this task creates or edits**, and both guards
return 0 when run on this task's files alone.

⚠️ **A counting trap I hit and corrected, in `DEFERRED` 45's own family.** My first count of the
English findings said **6**, because `grep -c ":[0-9]*$"` also matched the header line, which ends in
`English:`. The correct count is **5**, read from the full output. Recorded because that row exists
precisely for measurements eaten by a pipeline element.

---

## 8. Self-review checklist

- Contracts in `docs/CONTRACTS.md` honoured, including the non-standard env API? **Y** — C2's `info`
  shape, C6's T+1 observation rows (row 0 included deliberately), C8 untouched (no MAPPO checkpoint
  runs here), C9 honoured in every artifact and commit message.
- All tests actually executed and green? **Y** — 923 passed, 3 skipped, pasted above.
- Zero modifications to frozen files? **Y** — proved by `git diff --name-only main...HEAD` and by the
  guard's exit 0.
- Zero new dependencies? **Y** — numpy, stdlib, `traci`/`sumolib` (already installed and already used
  by `envs/sumo_env.py` and the existing SUMO tests).
- Every number in this packet produced by a command run in this session? **Y.**
- Every load-bearing quantity double-computed by an independent route? **Y** — KS and OVL against
  brute-force counters, asserted `==` on **integers**; ρ against a hand-averaged route; OVL against
  `1 − TV` as a third identity; the vehicle count by regex against the XML parser.
- Did I keep the registered sections registered? **Y** — §5 and §6 are unchanged since `cf1c950`,
  which precedes every number.
- Did the gate answer the question it was built to answer? **Y, after the ruling — it returned a valid
  Branch C.** ⚠️ But **N on the first attempt**, and that is the honest half: the instrument I
  registered was invalid, and it took a post-run check to notice. See §3.1.
- Did I re-derive a branch from a reading I chose after seeing results? **N** — the branch came from the
  coordinator's re-registration, and §3.2 discloses that all three readings pre-existed it.
- Is any threshold different from the one registered at `cf1c950`? **N. Not one.**

---

## 9. Deviations from the brief

1. **A third policy, `random`.** Approved in advance. Without it ρ is 0 and 1 by construction in both
   backends and Branch A's "ρ transfers sensibly" cannot fail.
2. **The parity type has a distinct id (`cf_parity`), not `pkw`.** Approved in advance. The mechanical
   check is strengthened, not relaxed: the id and the full attribute set are checked as well as the
   count.
3. **`<end>` = 4000 in the parity `.sumocfg`** (shipped 3600). Approved in advance; the margin and every
   episode's measured final `sim_time` are in the artifact.
4. **cologne3 added for `DEFERRED` 23 only.** Approved in advance. No gate number comes from it.
5. **`offline/flow_randomizer.py::render_sumo` was NOT modified.** `BRIEF_04` §4.2 required a parity
   path there; `BRIEF_21` supersedes §4 and its §6 fence excludes it. `offline/parity.py` is written so
   P7.3 can consume the contract rather than re-derive it.
6. **Only `bc-tyc` was bound.** The other three hangzhou 1×1 variants ship the same unbound `pkw`.
   `BRIEF_21` §6 says one scenario.
7. **A post-hoc diagnostic was added to the module after the registered run.** It selected nothing at
   the time, and 657 registered leaves were proved unchanged by it. Its movement-paired table was
   subsequently **re-registered by the coordinator** as the gate's alignment (§3.2), with the
   disclosure that it pre-existed the ruling.
8. **The registered §5.3 alignment was replaced after the run**, by coordinator ruling recorded in
   `docs/plans/p7.0.md` §11. **No threshold moved.** The superseded reading is preserved and labelled
   VOID rather than deleted.

## 10. Conflicts found between brief and repo (repo wins)

1. **`BRIEF_21` §2 versus `docs/notes/P7.0_vtype_investigation.md` and `BRIEF_04` §3** — resolved before
   any code, recorded in `docs/plans/p7.0.md` §3, and **corrected at source by the coordinator in
   `8aa4908`**. Implemented to the repo: the full parity contract including `tau = 2.0`, and
   `maxSpeed = 11.11` from `flow.json` rather than the vType's `11.111`.
2. **`BRIEF_04` §5 says `scenarios/` is "not yours"; `BRIEF_21` §2 says scenario files are not frozen
   and instructs writing bound copies under a new path.** `BRIEF_21` supersedes `BRIEF_04` except §3,
   and `scripts/claude_guard.sh:44` agrees that `scenarios/` is not frozen. No existing file was edited.
3. **`BRIEF_21` §3's `DEFERRED` 23 asks for the enumeration order "for the same scenario".** On the
   declared 1×1 the question is unanswerable. Raised at the plan gate and approved; cologne3 added.

## 11. Tests I changed, disclosed in full

**One test specification was corrected, before implementation and not after a failure.** The first
draft of `test_signed_distance_is_negative_exactly_when_a_criterion_fires` asserted
`fired == (signed_distance < 0)` for **all** criteria. **That is false at an exact boundary** for the
two criteria whose declared inequality fires *on* the threshold (`B1`: `Δ ≤ 0`; `B3`: `count ≥ 9`) and
for `A1`, which is `B1`'s complement: there `signed_distance == 0.0` while `fired is True`.

Diagnosis: my own specification bug, found while designing the implementation, with **no implementation
in existence** and therefore no failing test being accommodated. It was replaced by something
**stronger**, not weaker:

- `test_fired_is_recomputable_from_statistic_threshold_and_comparison` — recomputes `fired` from the
  row's own `statistic`, `threshold` and `comparison` using `operator`, over a 7-case sweep (≥50 rows).
  This covers the boundary exactly.
- `test_signed_distance_sign_agrees_with_fired_away_from_the_boundary` — the sign invariant as an
  implication, with the limitation written into its docstring.
- `test_the_boundary_firing_criteria_are_exactly_b1_b3_and_a1` — pins the exempt set so it cannot grow.

**No existing test was edited or deleted. No assertion was weakened.** Mutation M10 confirms the
replacement is live.

## 12. Limitations

- **One scenario, one intersection, nominal demand only.** No flow draws; `flow_draw` is recorded as
  absent. `D4`'s split governs corpus draws and this task collects no corpus.
- **CityFlow's deterministic cells are one distinct episode repeated five times** (sd exactly 0), so
  their effective sample is 361 rows against SUMO's 1805. Reported per cell as `n_distinct_episodes`;
  the KS noise floor (0.0452) is quoted from the pooled count and labelled a reading aid, never a test.
- **KS and OVL on autocorrelated time series.** Both are reported as statistics; no p-value is computed
  and none should be inferred.
- **The phase block is still not given a KS number** — widths are 9 and 16, so a positional entry
  would be meaningless, and a KS statistic on a categorical variable would need an invented ordering.
  §5.4's green-phase correspondence table **is now delivered** (§3.7) and replaces it with something
  better: a set comparison per action, with a control.
- **The movement-paired correspondence is only as good as the scenario files' `type`/`dir` attributes.**
  If both files were generated from a common source that got the turn assignment wrong in the same way,
  the correspondence would inherit that error. §3.7's action-vocabulary agreement is a consistency
  check against a *different field* of the same files, not an independent measurement of the geometry.
- **The `.sumocfg` `<end>` of 4000 is a deviation from the shipped 3600**, and while no vehicle departs
  after 3599, I did not verify that the *shipped* value would in fact have truncated.
- **SUMO's halting threshold** is taken from `getLastStepHaltingNumber`'s documentation, not measured.

## 13. Open questions for the coordinator

**Resolved by the ruling and closed:** whether §5.3 is re-registered (it is, as movement-paired), and
whether branch B stands (it does not; the reading is VOID). Remaining:

1. **The lane-index reversal is a hard prerequisite for P7.1 and P7.3, not a curiosity.** A
   CityFlow-trained model fed a SUMO state vector reads a left-turn queue where it learned a through
   queue, **even after sorting the lanes** — sorting fixes order, not denotation. That is an interface
   defect, not a dynamics gap, and P7.4's defence against *"the transfer gap is just interface
   mismatch"* depends on it being repaired before the transfer curve runs, not after.
2. **Cross-backend intersection keying has no valid key** (§6, cologne3's `GS_` prefix). P7.3 needs a
   declared mapping; `lane_semantic_correspondence` is the pattern but there is no movement-based
   analogue at intersection level. Worth a `DEFERRED` row.
3. **`Δ_sumo` is 31 % of `Δ_cityflow`** (11.6050 against 37.6645). The normalisation is well-posed in
   both, but SUMO's anchors span far less headroom, so **ρ is roughly three times more sensitive
   there** — which is part of why A4 misses by a factor of 3.4. Not a declared criterion and not
   treated as one; flagged because it bears directly on P7.2's calibration protocol.
4. **May the ratio `Δ_sumo / Δ_cityflow` be reported** as a diagnostic without breaching
   `PREREGISTRATION` §3.4's ban on cross-backend raw travel times? It is a dimensionless ratio of two
   within-backend differences. Still kept out of the decision rule and out of any performance claim.
   *(This question was in the plan before the run and is still open.)*
5. **`render_sumo` parity path** (`BRIEF_04` §4.2) is still owed before P7.3 collects any SUMO corpus.
6. **Only `bc-tyc` is bound.** The other three hangzhou 1×1 variants ship the same unbound `pkw` and
   will each need the parity treatment — `offline/parity.py` generalises, but the files do not exist.

## 14. AI-assistance record

1. **Tool and version:** Claude Code, Claude Opus 4.6, 2026-08-16.
2. **Authorship per file.** AI-authored in full: `docs/plans/p7.0.md`, `offline/parity.py`,
   `offline/transfer_gate.py`, `tests/test_parity_vtype.py`, `tests/test_transfer_gate.py`,
   `docs/returns/P7.0.md`. AI-generated from a committed generator, not hand-written:
   `scenarios/hangzhou_1x1_bc-tyc_18041610_1h_parity/*.rou.xml` and `*.sumocfg` (by
   `python -m offline.parity --write`), and `docs/data/p7_0_gate.json` (by
   `python -m offline.transfer_gate`). No human-authored file was edited.
3. **Human verification performed.** All runs were executed by the AI in the `rltraffic-p70`
   worktree: full suite `923 passed, 3 skipped` (pinned), both file-level suites red then green,
   **14 mutations executed with every failure observed and every reversion verified by
   `sha256sum -c`**, a **657-leaf regeneration diff at 0 changed**, and a **329-leaf no-drift diff at 0
   changed** across the fix round. Independently recomputed by the AI: KS and OVL against brute-force
   counters with `==` on integers, OVL against `1 − TV`, ρ against a hand-averaged route, the vehicle
   count by regex against the XML parser, and the CityFlow MaxPressure ATT against `PROJECT_PLAN`
   P8.0's registered 247.75 anchor.
   **Verified by the coordinator, first-hand and independently:** the §10.1 vType conflict (`flow.json`
   11.11 and 2.0, the note's +0.6 % / +49 %, `BRIEF_04` §3's ordering) → brief corrected at source in
   `8aa4908`; **the lane-index inversion, from the roadnet and `.net.xml` alone with no result in
   hand**; and **the Branch C arithmetic, computed before the ruling was issued** (B2 0.3407 ≥ 0.30,
   B3 3 < 9, B4 6.0574 < 7.2308, A2 short by 0.1593, A4 short against 1.8077). **The remaining numbers
   in this packet have not been re-run by a human.**
4. **Who made the research decisions.** The scenario, the two anchors and the ρ formula are
   **pre-registered** (`PREREGISTRATION` §3.4, §4, §9; `PROJECT_PLAN` §2.0c). The parity contract is a
   **coordinator ruling** (`BRIEF_04` §3, re-affirmed by `BRIEF_21` §2 as amended in `8aa4908`).
   The **branch criteria and thresholds** (`docs/plans/p7.0.md` §6.2) were **proposed by the AI and
   approved unchanged by the coordinator before any number existed** — `cf1c950` precedes the first
   run — and the coordinator added §6.3's margin requirement in the same exchange. The four design
   choices in §9.1–§9.4 were proposed by the AI at the plan gate and approved by the coordinator.
   **Finding the lane-index defect, refusing to re-derive a branch from it, and reporting PARTIAL were
   the AI's.** ⚠️ **Every decision that followed was the coordinator's, and this is the distinction a
   publisher asks about: voiding the by-id reading rather than calling it unfavourable, re-registering
   the movement-paired alignment, holding every threshold fixed, and ruling Branch C with both failures
   reported.** Recorded in `docs/plans/p7.0.md` §11. The AI implemented that ruling and added the
   identity-key control of §3.7 on its own initiative.

## 15. What the next task will assume about this one

- `offline/parity.py` holds the parity contract with each value's source named and is **the** place to
  read it from; `PARITY_CONTRACT_VERSION` is `"1.0"`.
- `scenarios/hangzhou_1x1_bc-tyc_18041610_1h_parity/` is a committed, regenerable, vType-bound SUMO
  scenario whose network is the shipped one by reference.
- **`DEFERRED` 18 is closed on SUMO** — the ATT column of a SUMO corpus at v1.1 is safe to trust, with
  the scope limit that the metric is always requested through `SumoEnv`.
- **`DEFERRED` 23 is answered and has grown**: within a backend ID keying stands, **across backends
  neither ids nor positions are a valid key**, and lane ids denote different physical lanes on
  hangzhou.
- **P7.1 must treat the lane-index convention as a first-class problem**, not a sorting detail.
- **The C3 branch is C**, and the artifact says what that means in `branch.operational_meaning`, beside
  the branch itself, so *"unclear"* cannot be skim-read as *"proceed"*. **C3 is neither licensed nor
  descoped; CUT 3 does not fire; P5 is next.**
- **The action vocabulary transfers** — all 8 green actions release the same physical lanes in both
  backends (§3.7). P7.1 and P7.3 may rely on that, and on nothing else about lane identity.
- **`docs/data/p7_0_gate.json` carries three readings.** `per_feature` is the registered one;
  `voided_per_feature` is VOID and labelled; `lane_convention_diagnostic.road_level` is a diagnostic.
  A consumer that reads `per_feature` alone gets the right numbers.
- **§6's checkbox in `PROJECT_PLAN` is deliberately left unticked** — `BRIEF_21` §7 reserves it for the
  merge commit.
