# RETURN PACKET — P8.4a (entered-count probe)
**Date:** 2026-08-28 · **Mode:** Claude Code · **Contracts:** v1

**Status: DONE**, with one item that is a coordinator ruling rather than work: **E2 fired and I have
not adjudicated it** (§6, §13.1).

* 10-draw probe: **39 cells, 1870 episodes, 39/39 reference checks exact** — run by me.
* Registered escalation at 100 draws: **14 cells, 6200 episodes, 14/14 reference checks exact** —
  ⚠️ **run by the author, not by me** (§4a.8). I verified it post hoc by independent recomputation
  and did not re-run it.
* Amendments A, C, D and E all landed; `DEFERRED` 61 is closed in both halves.

---

## 0. THE HEADLINE, AND IT ANSWERS THE AUTHOR'S QUESTION

> **DO THE OFFLINE-LEARNED POLICIES ADMIT FEWER VEHICLES THAN THE BASELINES?**
> **On the cells that carry P5.2's headline: NO — and on hz1x1's weakest tier they admit MORE.**

**`iql@random` on grid4x4 — the 189.89 cell the question named — admits 1.000000 of the vehicles its
draws create, exactly as its behaviour anchor does.** So does `dt_spatial@random`, `dt_nomix@random`
and `bc_top10_perix@random`; `bc@random` and `bc_top10@random` sit at 0.999955 and 0.999669.

⛔ **The correct statement, and it is the one `BRIEF_31` §6.3 prescribes: *the ATT gap is not
explained by differential admission on these cells.*** Not "the result is safe".

**hz1x1's `random` tier, where the censoring is real (34.5 % never admitted):**

| arm | `entered/created` | vs anchor | `att_ours` | `att_engine` |
|---|---|---|---|---|
| `behaviour@random` | 0.654786 | — | 429.4908 | 800.4062 |
| `iql@random` | **0.708657** | **+5.39 pp** | 394.5985 | 726.1082 |
| `dt@random` | **0.669056** | **+1.43 pp** | 421.5994 | 767.3606 |
| `bc@random` | 0.654279 | −0.05 pp | 430.6119 | 793.8574 |
| `bc_top10@random` | 0.649735 | −0.51 pp | 430.5435 | 800.9212 |

**`iql` and `dt` admit MORE than the policy that produced their data, so their ATT advantage there is
if anything understated** — they carry a larger and harder population. That is the direction
`BRIEF_31` §1 said would make the true gap *wider*.

---

## 1. Branch + diff stat

```
task/p8.4a-entered-probe

$ git diff --stat main...HEAD
docs/PROJECT_PLAN.md                     |      9 +
 docs/data/p8_4a_admission.json           |  45079 +++++++++
 docs/data/p8_4a_admission_escalated.json | 139575 ++++++++++++++++++++++++++++
 docs/plans/p8.4a.md                      |    472 +
 docs/returns/P8.4a.md                    |    729 +
 offline/admission_probe.py               |   2230 +
 offline/campaigns/p8_4a.sh               |    289 +
 offline/materialise_draws.py             |     59 +-
 tests/test_admission_probe.py            |   1047 +
 tests/test_materialise_draws.py          |    109 +
 10 files changed, 189588 insertions(+), 10 deletions(-)
```

**Zero frozen files touched.** Every path above is either new or is `docs/`. `scripts/claude_guard.sh
--frozen-only` → `exit=0`, run after every commit.

## 2. Files produced

- `offline/admission_probe.py` — the instrument: the R1–R5 identity, the arm/reference wiring, the
  registered scoring, the artifact and a four-subcommand CLI.
- `tests/test_admission_probe.py` — 49 tests (41 + 4 for the escalation grain + 4 for E4's
  working-directory check).
- `offline/campaigns/p8_4a.sh` — the campaign, with Gate −1's certificate check, a 39-cell capability
  pre-flight, cell-granular resume, and the `--escalate` phase 2.
- `docs/plans/p8.4a.md` — the plan (§1–10) plus §11, Amendment A's reception.
- `docs/data/p8_4a_admission.json` — the artifact: 1870 episodes, 39 cells at per-seed grain, both
  ATT definitions on every episode, the Gate −1 report, per-cell timing, E1/E2/E3 scored, **no
  verdict on the science**.
- `docs/data/p8_4a_admission_escalated.json` — the 100-draw grain: 14 cells, 6200 episodes.
  ⚠️ **Produced by the author's run, verified by me (§4a.8).**
- `docs/PROJECT_PLAN.md` — **§6: `P8.4a` and `P8.4b` boxes CREATED (they did not exist). Both left
  UNTICKED.** ⚠️ **The coordinator must tick `P8.4a` in the merge commit** — and see §9, because
  PARTIAL may mean it should not be ticked yet.
- `offline/materialise_draws.py` — **MODIFIED** (Amendments D2 and E3 / `DEFERRED` 61):
  `source_config`, `source_flow` and `source_roadnet` added to `_NON_IDENTITY_FIELDS`, plus three
  docstrings corrected. §4a.
- `tests/test_materialise_draws.py` — tests for D2/E3, both directions, parametrised over all three
  path fields and all three digest fields; **one test removed** by E3's ruling (§11.3).

## 3. Tests — REAL OUTPUT

```
$ OMP_NUM_THREADS=1 MKL_NUM_THREADS=1 RLTRAFFIC_CORPUS_V11=... RLTRAFFIC_DRAWS=... \
  .venv/bin/python -m pytest tests/test_admission_probe.py -q
.............................................                            [100%]
49 passed in 1.77s
```

Both amended files together, same environment:

```
$ ... -m pytest tests/test_admission_probe.py tests/test_materialise_draws.py -q
78 passed in 5.24s
```

Whole suite, pinned, from this worktree:

```
$ OMP_NUM_THREADS=1 MKL_NUM_THREADS=1 RLTRAFFIC_CORPUS_V11=... RLTRAFFIC_CORPUS=... \
  RLTRAFFIC_DRAWS=... .venv/bin/python -m pytest -q
FAILED tests/test_rtg_ablation.py::test_the_spread_table_uses_the_whole_declared_training_set_not_the_probe_subsample
1 failed, 1456 passed, 18 skipped, 15 warnings in 102.16s (0:01:42)
```

(1444 before the amendment work. +7 for Amendment D's six `tests/test_materialise_draws.py` tests,
one parametrised over three digest fields; then +5 net for Amendment E — four new E4 tests in
`tests/test_admission_probe.py`, two extra parametrisations of the D2 path test, minus the one test
E3's ruling retired (§11.3). The single failure is unchanged and is diagnosed in 3.1.)

### 3.1 The one failure is pre-existing and environmental, and here is the proof

```
E  FileNotFoundError: [Errno 2] No such file or directory: 'output/p4_dt/dt_seed101.pt'
   offline/rtg_ablation.py:887 in spread_table
```

`tests/test_rtg_ablation.py` reads a checkpoint by a **relative** path, so it depends on the working
directory holding `output/` — which is gitignored and exists only in the main tree.

- `git diff --name-only main...HEAD` contains **neither** `tests/test_rtg_ablation.py` **nor**
  `offline/rtg_ablation.py` (grep count: **0**).
- Re-run with CWD = the main tree and this branch's code on `PYTHONPATH`: **`1 passed in 2.23s`**.

**I did not touch it and I did not "fix" it.** It is a latent CWD dependency in a file outside this
brief's fence; §9 raises it.

### 3.2 The tests fail for the right reason before the code exists

First run against the signature-only skeleton: **38 failed, 3 passed**, and `--tb=line` showed
`NotImplementedError` at **14 distinct call sites** — no shared `ModuleNotFoundError`. The 3 that
passed are the inventory checks, which read constants and assert them against
`method_tier_grid.METHODS` / `tier_sweep.METHODS` / `DRAW_SPLITS` / `TRAINING_SEEDS`. The four
escalation tests added later were also run red first (`3 failed, 42 passed`, 2 distinct call sites).

### 3.3 Mutation testing — 11 mutations, 11 caught

Each applied to the committed source, suite re-run, then `git checkout --`. Baseline: 41 passed.

| # | mutation | result |
|---|---|---|
| M1 | R1 `include_waiting=True` → `False` | 1 failed |
| M2 | R5 bound `start <= horizon-1` → `<= horizon` | **3 failed** |
| M3 | R5 `startTime == endTime` assertion removed | 1 failed |
| M4 | R2 == R3 cross-check removed | 1 failed |
| M5 | R4 == R5 cross-check removed | 1 failed |
| M6 | reference `==` loosened to `math.isclose` | 1 failed |
| M7 | `Delta = max(...)` → `min(...)` | 1 failed |
| M8 | escalation trigger `deficit > 0` → `deficit > delta` | 1 failed |
| M9 | `ReferenceCheck.exact` drops the `n_compared > 0` guard | 1 failed |
| M10 | pooled ratio → mean of per-episode ratios | 1 failed |
| M11 | E3 scored on grid4x4 instead of hz1x1 | **2 failed** |

Tree verified clean (`git status --short` empty) after the reverts.

### 3.4 Double-computation of every headline quantity, from the artifact's raw rows only

Recomputed with no import of `admission_probe`:

```
episode rows: 1870
admission ratios recomputed from raw rows: 39 cells, 0 disagreements
identity violations across 1870 episodes: 0
hz1x1 att_ours vs docs/data/p4_6_grid.json: 1170 compared, 0 mismatches (== , not isclose)
draws whose created differs between cells: 0 of 20
```

The last line is the check `BRIEF_31` §3 implies by calling `created` "fixed per draw": across the 39
cells, 1870 episodes and 20 draws, **no draw's `created` ever differed between cells**.

---

## 4. Gate −1 — the draw restoration, reported before anything downstream

Amendment A1 required the five grid4x4 survivors regenerated **first**, byte-identity reported
explicitly, and `BLOCKED` on any failure.

| scenario | survivors | result | restored |
|---|---|---|---|
| **grid4x4** | 1000–1004 | **5/5 `kept`** (byte-identical) | 1005–1009 `written` |
| **hz1x1** | 1000–1009 | **10/10 `kept`** | none needed |

`kept` is granted by `materialise_draws._existing_conflict` only on byte equality of the rendered
files **and** field equality of the provenance record. `force` was never passed. A second grid4x4
invocation re-verified 1005–1009 as `kept`, so those five are byte-verified too. Per-draw
`flow_sha256` and `n_vehicles` are in `output/p8_4a/draw_restoration.json` and in the artifact.

🚨 **A near-miss worth recording: run from the TASK WORKTREE, Gate −1 reports
`cityflow.json differs byte-for-byte` on draws that are in fact correct.** `materialise_draws`
records `source_flow` / `source_roadnet` as absolute paths resolved against the process working
directory and compares them as **identity** fields. **That would have been a FALSE `BLOCKED` under
A1's own stop rule.** The campaign therefore runs with CWD = the main tree and the code supplied via
`PYTHONPATH` + `PYTHONSAFEPATH=1`; the reason is in the script header. **Measured, not reasoned:**
the worktree run raised `FileExistsError` and wrote nothing; the main-tree run returned all five
`kept`.

## 4a. Amendments C and D — the restore invocation, and DEFERRED 61 landed

### 4a.1 🚨 C1's "all ten survivors" is TRUE OF THE FILE and MISLEADING AS EVIDENCE

`draw_restoration.json` does record all ten of 1000–1009 as `kept`. **But only 1000–1004 were
survivors.** Draws **1005–1009 were written by me** in the first invocation and re-verified as `kept`
by the second, minutes later, in the same tree by the same code. Their provenance proves it:

| draws | `git_commit` | `git_dirty` | what `kept` tests |
|---|---|---|---|
| 1000–1004 | `29ab244` (P5.2 era) | **True** | **purity across worktrees and weeks** ✅ |
| 1005–1009 | `964d3b0` (this branch) | False | determinism within one session ⚠️ |

**C1 calls this "a live test of a load-bearing assumption". Counting my own freshly written draws as
survivors would double the apparent evidence.** The genuine cross-time survivor count is **five**.

⭐ **So I tested the assumption properly, by a stronger route than `kept`.** Regenerating all ten
grid4x4 draws **in memory from this worktree** and comparing bytes against the main tree:

```
flow.json (THE DEMAND) byte-identical across worktrees: 10/10   differing: 0
cityflow.json differing across worktrees:               10/10
```

**`(source, base_seed, draw_id) → demand` is a pure function across worktrees — measured, on all ten.**
T1's recoverability argument stands. The `cityflow.json` difference is entirely its absolute `dir`.

### 4a.2 🚨 D1's factual claim about my refusal is wrong, and it matters for D2's scope

D1 states: *"It did not return that [a byte-for-byte complaint]. It returned a provenance-FIELD
complaint, which is strictly later."* **The refusal I measured and reported was:**

```
FileExistsError: .../draw_1000 differs from the draw that would be materialised there:
cityflow.json differs byte-for-byte.
```

**That is the rendered-file layer. The provenance loop was never reached.** Re-measured today, same
message. D1's general point about `sorted()` + first-return short-circuit is correct and I have
written it into `_existing_conflict`'s docstring — but it is not what happened to me.

**There were two different refusals, at two different layers**, and `DEFERRED` 61 item (a) merges
them. Measured today, both directions:

| configuration | before D2 | after D2 |
|---|---|---|
| CWD = main tree, `--repo-root <absolute>` — **the author's failure** | `provenance.json field 'source_config' differs` | ✅ **no refusal** |
| CWD = task worktree — **my failure** | `cityflow.json differs byte-for-byte` | ❌ **still refuses** |

⚠️ **D2 fixes the author's refusal and does NOT fix mine.** The rendered `cityflow.json` embeds `dir`
as a CWD-resolved absolute path and is compared *before* any provenance field. **The
main-tree-CWD requirement therefore stands after this fix**; only the `--repo-root` constraint is
lifted. Written into the module docstring so the next reader is not misled.

### 4a.3 The fix, and the tests both ways

`_NON_IDENTITY_FIELDS` is now `{git_commit, git_dirty, source_config, source_roadnet}`. Six tests
added to `tests/test_materialise_draws.py`; **28 passed** in that file, no pre-existing test changed
or broken. Four mutations, each caught by the *right* test — and the harness asserts its pattern
matched, after an earlier mutation of mine silently no-opped and produced a false "caught":

| mutation | caught by |
|---|---|
| revert the exemption | `..._kept_when_a_source_PATH_differs...` + the pinning test |
| **also exempt `source_config_sha256`** | **`..._refused_when_a_source_SHA256_differs[source_config_sha256]`** |
| also exempt `source_flow` | `..._source_flow_is_still_an_identity_field...` |
| skip the rendered-file loop | `..._rendered_file_difference_still_refuses...` + a pre-existing test |

**The second row is D2's own requirement — "the second test is what makes the first safe" — executed.**

⚠️ **`source_flow` was NOT granted the exemption.** It is the same kind of CWD-resolved absolute path
with the same `_sha256` companion, so D2's rationale arguably covers it, but D2 names two fields and
I will not extend a ruling on merged code silently. The current behaviour is **pinned by a test** so
the gap is visible rather than assumed. See §13.

### 4a.5 Amendment E — `source_flow` ruled in, the worktree refusal made legible, and the restore

**E3 — `source_flow` extended into `_NON_IDENTITY_FIELDS`.** The set is now
`{git_commit, git_dirty, source_config, source_flow, source_roadnet}`. The "path differs → `kept`"
test is parametrised over all three path fields and mirrors the digest test exactly, and the pinning
test additionally asserts the **invariant that made the omission visible**: every exempted path has a
`*_sha256` twin, and no twin is exempted.

⚠️ **A test was REMOVED**, disclosed in full in §11.3: `test_source_flow_is_still_an_identity_field_
and_a_path_change_there_refuses` asserted the *opposite* of E3's ruling. It existed to pin the gap and
its own docstring said it "must be revisited with the ruling, not around it". It was deleted rather
than edited into agreement, with a comment in its place naming what replaced its coverage.

**E4 — `restore-draws` now names a wrong working directory.** `assert_cwd_renders_the_recorded_
scenario_dir` compares the scenario directory this CWD resolves to against the one recorded in an
existing draw's `cityflow.json`, and refuses **before any draw is built** with a message that states
the two directories and says in terms that the demand is unaffected. The embedded `dir` is **not**
normalised, per E4. When no requested draw exists yet there is nothing to disagree with and the check
is silent.

Four mutations, each caught by the right test, patterns asserted:

| mutation | caught by |
|---|---|
| drop `source_flow` from the set | `..._PATH_differs...[source_flow]` + the pinning test |
| **also exempt `source_flow_sha256`** | **`..._SHA256_differs[source_flow_sha256]`** |
| remove the CWD check from `restore_draws` | both E4 refusal tests |
| CWD check raises a non-refusing type | both E4 refusal tests |

### 4a.6 ⭐ E5 — the restore had ALREADY BEEN RUN, and that turned it into a stronger test

**Draws 1010–1099 were on disk before my invocation**, materialised `2026-08-29 00:23:44` from
`git_commit f321fc5` — this branch's tip at the time. **The author ran the restore with the
invocation from my previous reply.** My run was therefore not the restore but an **independent
re-verification of it**, from a different process at a different commit, and **all 100 draws came
back `kept`**.

**My run wrote no draw**: every draw directory retains its original mtime, and only
`scenarios/draws/` was touched, by the `.staging-*` directory `_commit` creates and removes even when
every plan is `kept`.

Then the strongest available test, extended from 10 draws to 100 — regenerate in memory **from this
worktree** and compare `flow.json` bytes against the main tree:

| group | origin | `flow.json` identical |
|---|---|---|
| 1000–1004 | P5.2 era, `29ab244`, `git_dirty: True` | **5/5** |
| 1005–1009 | written by me 2026-08-28, `964d3b0` | **5/5** |
| 1010–1099 | **written by the author 2026-08-29, `f321fc5`** | **90/90** |
| **total** | three different processes, three commits | **100/100, 0 differing** |

🚨 **Reported as three groups and never pooled, per E1.** The purity evidence that actually bears on
T1's recoverability argument is the **five** P5.2-era survivors plus this cross-worktree byte
comparison; the other 95 test determinism across processes, which is weaker but not nothing — and the
90 the author wrote are a genuine second-process reproduction rather than a same-session echo.

⚠️ **One caveat on the `kept` half of this evidence, stated because E3 landed in the same task:**
`kept` is now a slightly weaker signal than it was yesterday, because three path fields no longer
count toward identity. It is **not** weaker for the demand: rendered files are still compared
byte-for-byte, `flow.json` is a rendered file, and the 100/100 byte comparison above imports none of
that machinery.

### 4a.8 🚨 THE ESCALATION WAS RUN BY THE AUTHOR, NOT BY ME — and I nearly shipped it unexamined

`docs/data/p8_4a_admission_escalated.json` (4.1 MB, 6200 episodes) appeared in my worktree at
**15:48:55 today**, together with 14 `*_full.json` work files and an `ESCALATION_COMPLETE` marker.
**The author ran `p8_4a.sh --escalate`**, which writes into `--repo-root "$WORKTREE"`.

⚠️ **A `git add -A` of mine committed it before I had looked at it.** That is the failure mode
`CLAUDE.md` §2 names — reporting a number I did not produce — and I caught it only when the diff stat
showed 139,575 lines I could not account for. **Disclosed rather than quietly kept.** What I did
about it: verified it by the same independent route I used on the primary artifact, importing none of
the module under test:

```
admission ratios recomputed from raw rows: 14 cells, 0 disagreements
identity violations across 6200 episodes: 0
hz1x1 att_ours vs docs/data/p4_6_grid.json: 3200 compared, 0 mismatches (==)
draws whose created differs between cells: 0 of 200
draws per cell: [100]   seed-slot counts: {5 seeds: 12 cells, 1 seed: 2 cells}
reference checks exact: 14/14
```

**I did not run the escalation and I am not claiming to have.** The numbers below are the author's
run, verified by me.

### 4a.9 E1 AT 100 DRAWS — the escalation changed two verdicts, which is why the rule exists

| arm | `r_arm` | `r_beh` | deficit | Δ | n=10 | **n=100** |
|---|---|---|---|---|---|---|
| grid4x4 `bc_top10@mappo1000` | 0.970454 | 1.000000 | +0.029546 | 0.009596 | falsified | **falsified** |
| hz1x1 `bc_top10@fixedtime` | 0.890840 | 0.907449 | +0.016609 | 0.026875 | close | close |
| hz1x1 `bc_top10@random` | 0.648887 | 0.656212 | +0.007325 | 0.030341 | close | close |
| hz1x1 `bc@fixedtime` | 0.904997 | 0.907449 | +0.002452 | 0.002883 | close | close |
| grid4x4 `iql@mappo1000` | 0.999168 | 1.000000 | +0.000832 | 0.001145 | close | close |
| **grid4x4 `bc_top10@random`** | 0.999550 | 1.000000 | +0.000450 | 0.000354 | close | **→ FALSIFIED** |
| hz1x1 `bc@random` | 0.655833 | 0.656212 | +0.000379 | 0.030341 | close | close |
| grid4x4 `bc@random` | 0.999910 | 1.000000 | +0.000090 | 0.000218 | close | close |
| **hz1x1 `dt@maxpressure`** | 0.999913 | 0.999906 | **−0.000007** | 0.000055 | close | **→ HOLDS** |

⭐ **Two flips, in opposite directions, and both were invisible at n=10.** `dt@maxpressure`'s deficit
was noise — at 100 draws it admits *more* than its anchor (79 vehicles never entered out of 907,175).
`bc_top10@random` crossed its Δ. **This is precisely what the zero-threshold escalation rule was for,
and it earned its 3.44 h.**

⚠️ **Read the second flip at its magnitude: 299 of 663,785 vehicles, 0.045 %.** The registered rule
declares it falsified because Δ there is 0.000354, and **I am reporting the rule's output as the rule
defines it rather than softening it.** But a falsification of this size is a statement about the
rule's resolution, not about a policy blocking traffic, and the packet says so in both directions.

⚠️ **`e1.escalated_arms` in the escalated artifact lists 8 arms.** That is the rule mechanically
re-applied at the new grain — **it is NOT a further 1000-draw obligation.** The registered escalation
target is the full held-out pool and it has been reached.

### 4a.7 The restore is a REPAIR, not setup (C2)

**What was missing:** `cityflow_grid4x4` held-out draws 1005–1099 — 95 of the 100 P5.2 evaluated on.
**Why:** `scenarios/draws/` is gitignored and therefore per-worktree; retiring the P4.6/P4.7
worktrees deleted them. **This is the second instance of `DEFERRED` 55 by the same mechanism** —
P5.3a repaired hz1x1, this repairs grid4x4. **The restore byte-verified the five genuine survivors
rather than assuming them**, and §4a.1 goes further and verifies the demand itself across worktrees.

## 5. Gate 0 — timing, measured per cell

| cell | s/episode |
|---|---|
| hz1x1 `behaviour@maxpressure` | 0.693 |
| hz1x1 `bc@random` | 1.333 |
| hz1x1 `dt@random` (Gate-0 probe) | 1.746 |
| grid4x4 `bc@random` | 2.165 |
| grid4x4 `dt_spatial@random` (Gate-0 probe) | 3.440 |
| grid4x4 `behaviour@mappo1000` | 7.027 |

39 cells: **hz1x1 mean 1.254 s/ep (min 0.693, max 2.333); grid4x4 mean 2.669 (min 1.304, max
7.027)**. Summed per-episode cost of the 1870 episodes: **3372.8 s = 56.2 min**, against a pre-run
projection of 58 min and a worst case of 74. ⚠️ `BRIEF_31`'s 1.684 s/ep figure is superseded, in both
directions: hz1x1 is cheaper and grid4x4's MAPPO anchor is **4.2×** more expensive.

Every figure is full per-episode cost including the per-draw `torch.load` that `evaluate_arm`'s
contract puts in front of every draw. `seconds_rollout` isolating the simulation is stored per
episode. **P8.4b should cost from these, not from hz1x1 alone.**

## 6. The registered predictions, scored

### E1 — 23 holds · 8 close · **1 falsified** · 9 escalate

| arm | `r_arm` | `r_beh` | deficit | Δ | status |
|---|---|---|---|---|---|
| **grid4x4 `bc_top10@mappo1000`** | 0.967168 | 1.000000 | **0.032832** | 0.011806 | **FALSIFIED** |
| hz1x1 `bc_top10@fixedtime` | 0.885443 | 0.906925 | 0.021482 | 0.021890 | close |
| hz1x1 `bc_top10@random` | 0.649735 | 0.654786 | 0.005051 | 0.030326 | close |
| hz1x1 `bc@fixedtime` | 0.903033 | 0.906925 | 0.003893 | 0.007058 | close |
| grid4x4 `iql@mappo1000` | 0.999263 | 1.000000 | 0.000737 | 0.001053 | close |
| hz1x1 `bc@random` | 0.654279 | 0.654786 | 0.000507 | 0.030326 | close |
| grid4x4 `bc_top10@random` | 0.999669 | 1.000000 | 0.000331 | 0.000752 | close |
| grid4x4 `bc@random` | 0.999955 | 1.000000 | 0.000045 | 0.000226 | close |
| hz1x1 `dt@maxpressure` | 0.999978 | 1.000000 | 0.000022 | 0.000110 | close |

**E1 is falsified for `bc_top10@mappo1000` on grid4x4, and `BRIEF_31` §2 then requires that arm's ATT
advantage be reported as partly or wholly a selection artefact.** ⚠️ Context, offered as context and
not as an excuse: that is the arm P5.2 already records as collapsing (registered level 749.58 against
a behaviour 160.28), so "it blocks entry" and "it is a bad policy" are the same observation here.

### E2 — **FAILED on 1 arm of 12, and I am NOT reversing the registered inference**

`grid4x4/bc_top10@mappo1000` reads **0.967168** against a registered floor of 0.99, per seed
0.969168 / 0.962175 / 0.973981 / 0.963980 / 0.966536.

🚨 **E2 is the null control and its registered falsifier says a failure there "indicts the probe
before the science", registered "so it cannot be reversed later". It fired. I am reporting it as
fired and escalating the interpretation rather than explaining it away.** The evidence that bears on
whether it indicts the *instrument* — for the coordinator, not for me:

- **that exact cell's reference check is EXACT** — all 50 episodes reproduce the committed
  `att_horizon` in `output/p5_1/eval_bc_top10.json` bit for bit (that is the cell P5.2 reused at this
  tier — see C7), so the probe is demonstrably measuring the episode the campaign reported;
- the `mappo1000` **behaviour anchor** is at **exactly 1.000000** on both scenarios, which is the
  direct replication of T1's 100.0 %;
- the other 10 `mappo1000` arms are all **≥ 0.999263**.
- E2's own wording is *"`mappo1000`'s **arms** sit at ≈100 %"*, generalising from a measurement T1
  made of the `mappo1000` **behaviour policy** — arguably the same subset-to-population step
  `PREREGISTRATION` A5 made. **That reading favours me and is exactly why it is the coordinator's
  call and not mine.**

### E3 — HOLDS, and monotone

hz1x1 behaviour anchors, `entered/created`:

| tier | ratio | never entered |
|---|---|---|
| `mappo1000` | 1.000000 | 0 / 90680 |
| `mappo500` | 1.000000 | 0 / 90680 |
| `maxpressure` | 1.000000 | 0 / 18136 |
| `fixedtime` | 0.906925 | 1688 / 18136 |
| `random` | 0.654786 | 31304 / 90680 |

Rank order `mappo1000 > mappo500 > maxpressure > fixedtime > random` — **monotone**, and the
registered pair `r(mappo1000) > r(random)` holds. ⚠️ T1's 100.0 / 90.8 / 92.9 / 65.0 was measured on
**training** draws with the **corpus** behaviour policies; ours is held-out draws with the anchors
re-rolled, so the two are close but are not the same measurement.

### grid4x4 — its own row, as a measurement (Amendment A4)

| tier | behaviour ratio | never entered |
|---|---|---|
| `mappo1000` | 1.000000 | 0 / 66490 |
| `random` | 1.000000 | 0 / 66490 |

Across all 14 grid4x4 cells (700 episodes, 66,490 created vehicles per cell), `never_entered` by arm:
**`bc_top10@mappo1000` 2183 (3.28 %) is the sole exception; the next largest is `iql@mappo1000` at 49
(0.074 %), then `bc_top10@random` at 22 (0.033 %), and the remaining 11 cells are exactly 0.**
Reported as a measurement, ⛔ not as an exclusion.

## 7. A0's two effects, separated at population scale

Registered before the campaign from four exploratory episodes; now measured on 39 cells:

| cells | `att_ours − att_engine` |
|---|---|
| **24 fully admitted** (`r == 1.0`) | **+3.2469 … +7.1809** |
| **15 censored** (`r < 1.0`) | **−370.9154 … +6.9284** |

**When admission is complete, ours sits +3 to +7 s above the engine's on every one of 24 cells —
T1's M1 quantisation term, isolated and bounded. When admission is incomplete it falls as far as
−371 s — T1's B1 selection term.** The two do not overlap in sign or magnitude.

🚨 **A0's hz1x1-localisation hypothesis is CORROBORATED, and it remains a hypothesis.** Censoring
appears on hz1x1 at `fixedtime` (9.3 %) and `random` (34.5 %) and nowhere else on either scenario;
grid4x4 is at ≈100 % everywhere. **If it holds, the B1 exposure lives in P4.6/P4.7's hz1x1 ladder
rather than in P5.2's grid4x4 headline.** ⚠️ Two scenarios, ten draws, five seeds, one demand
randomiser — **it is a hypothesis to test, not a result**, and cologne and grid4x4's other tiers are
unmeasured.

## 8. Self-review checklist (Y/N)

- Contracts in `docs/CONTRACTS.md` honored, including the non-standard env API? **Y** — the rollout
  is `offline.horizon_metric.horizon_rollout`, reused unmodified; nothing here calls `env.step`.
- All tests actually executed and green? **Y for the 45 P8.4a tests. N for the whole suite** — 1
  pre-existing, CWD-dependent failure in `tests/test_rtg_ablation.py`, diagnosed in §3.1 and shown to
  pass from the main tree.
- Zero modifications to frozen files? **Y** — proved by the diff stat and by `claude_guard.sh
  --frozen-only` → `exit=0`.
- Zero new dependencies? **Y** — stdlib, numpy, torch, pytest.
- Every number in this packet produced by a command run in this session? **Y.**
- Tests written and run red before the implementation? **Y** — §3.2, with the call-site counts.
- Load-bearing assertions proved by mutation? **Y** — §3.3, 11/11 caught.
- Critical quantities double-computed by an independent route? **Y** — twice: inside the probe
  (R2 vs R3, R4 vs R5, per episode, `==` on ints) and again post hoc from the artifact's raw rows
  (§3.4).
- Filesystem-mutation barrier respected? **Y** — every write goes through
  `tier_sweep.assert_writable` with the corpus and **every sibling `output/*` directory** (16 of the
  17 present; only `output/p8_4a` is writable) passed as protected roots. **My writes landed in
  exactly two places: `output/p8_4a/` and `scenarios/draws/cityflow_grid4x4/draw_100[5-9]/`.**
  `output/p5_3b/run_campaign.sh` was never invoked and `task/p5.3b-nortg-campaign` was never touched.
  ⚠️ Honest limit: `output/p5_3b/` carries a 2026-08-28 directory mtime. I did not write there and it
  was in the protected set on every run, but another session holds that branch and I cannot account
  for its mtime from inside this one.
- Artifact free of a verdict on the science? **Y** — `assert_no_science_verdict` runs inside
  `admission_artifact`, layering this task's banned strings on top of
  `method_tier_grid.assert_no_verdicts`, and is tested both ways.

## 9. Deviations from the brief

1. **The escalation was run by the AUTHOR, not by me** (§4a.8) — 14 cells, 6200 episodes, at
   15:48:55 today, after I had built, tested and costed it at 3.44 h and handed it off per Amendment
   A2. I verified its artifact by independent recomputation but did not produce it. **Every number
   from it in this packet is labelled as the author's run.**
2. **The two draw grains are reported as two artifacts, not merged.** Scoring a 100-draw arm against
   a 10-draw anchor would put two denominators under one label; `cell_files` enforces the split and
   `admission_artifact` refuses a payload whose cells span two draw counts.
3. **`P8.4a`'s §6 box was CREATED, not just ticked** — it did not exist. Left unticked, for the
   coordinator's merge commit.
3b. **I committed an artifact I had not examined**, via `git add -A` (§4a.8). Caught from the diff
   stat, verified afterwards, and disclosed. The lesson is mine: `git add -A` in a worktree another
   process writes into is not a safe default.
4. **The campaign runs from the main tree** rather than the worktree, for the reason in §4.
5. **`offline/materialise_draws.py` was modified** — merged, reviewed code that P2.2/P4.x/P5.x all
   used. Not a frozen path, and directly ruled by Amendment D2. Landed with six tests and four
   mutations (§4a.3). **The grid4x4 draws 1010-1099 were NOT restored**: the user asked for the
   invocation, and running a 90-draw materialisation was not requested.

## 10. Conflicts found between brief and repo (repo wins)

| # | brief | repo | what I implemented |
|---|---|---|---|
| C1 | DoD names `att_per_step[-1]` | that is a **corpus** field; held-out evals store `att_horizon` | exact `==` against the committed `att_horizon`; **39/39 cells, 1870 episodes, 0 mismatches**. Accepted by A6 as stronger |
| C2 | 10 grid4x4 draws | only 1000–1004 existed | Gate −1 per A1; §4 |
| C3 | E3 names `mappo060` | not a P4.6 tier | re-registered by A6 over P4.6's five |
| C4 | `dt` listed for P5.2 | P5.2's DT arms are `dt_spatial`/`dt_nomix` | corrected by A6 |
| C5 | 1.684 s/ep | measured 0.693–7.027 | §5 |
| C6 | — | `horizon_rollout.final_completed` is NaN here | R3 reads `metrics._episode["completed"]` |

**C7, found during implementation and NOT in the brief — the one that would have produced plausible
wrong numbers.** `output/p5_2/checkpoints/grid4x4_mappo1000_{bc,bc_top10,iql}_seed*.pt` and
`output/p5_1/checkpoints/…` are **different files** (verified with `cmp`), and `dt_spatial` /
`dt_nomix` at that tier exist **only** under `p5_1/`. P5.2 reused P5.1's *cells* while retraining its
own *baselines*. Loading the P5.2 file would have produced a plausible number reproducing nothing.
The mapping is in `_method_checkpoint`'s docstring and the exact-reproduction check is what would
have caught it. Related: `tier_sweep.campaign_cell_manifest():384-394` records that
`eval_mappo1000_random.json` doubles as the grid4x4 `random` tier's behaviour anchor "because the env
settings are identical across all four tier manifests (verified)" — **this module re-verifies that
equality itself rather than inheriting a parenthetical `(verified)`.**

## 11. Tests I changed, disclosed in full

All three are tests **I wrote in this session**. In the two fixture cases the *implementation was
correct*; the third was retired by a coordinator ruling that inverted what it asserted. **No
assertion was weakened, and nothing was edited into agreement with code.**

1. **`test_score_e1_escalates_a_deficit_inside_delta_and_calls_it_close`** — I asserted
   `deficit == 0.02` but built the fixture with `entered=640`, which gives
   `0.65 − 0.64 = 0.01`. **My hand-arithmetic and my fixture disagreed; the code agreed with the
   fixture.** Verified by an independent recomputation before touching anything. Fixed by changing
   the fixture to `entered=630`, which makes the asserted `0.02` correct, and by writing the
   arithmetic into the docstring so a reader can check it. The assertions (`deficit`, `delta`,
   `status`, `escalate`) are unchanged.
2. **`test_source_flow_is_still_an_identity_field_and_a_path_change_there_refuses` — REMOVED, not
   edited.** I wrote it under Amendment D2 to pin the gap D2 left: `source_flow` was not exempted,
   and the test asserted that a path change there refuses. Its docstring said in terms that it
   "must be revisited with the ruling, not around it". **Amendment E3 ruled `source_flow` into the
   set, so what it asserted became the defect rather than the contract.** Editing it into agreement
   would have destroyed the record of why it existed, so it was deleted and a comment left in its
   place naming what replaced its coverage: the parametrised `..._PATH_differs...[source_flow]` case
   asserts the new behaviour on the same field, and `..._SHA256_differs[source_flow_sha256]` keeps
   the digest side. **Net coverage of `source_flow` went up, not down.**

3. **`test_analytic_created_matches_a_raw_engine`** — the engine config was wrong twice: CityFlow
   resolves `flowFile` against `dir` (so an absolute path failed with `cannot open flow file!`), and
   my minimal vehicle template lacked required fields (`width is required but missing`). Both are
   **fixture defects that stopped the test exercising the engine at all** — it was comparing my
   correct `3` against a silent `0`. Fixed by putting the roadnet and flow under one `tmp_path` and
   taking the vehicle template from the scenario's own `flow.json`. **The assertion — analytic R5 vs
   the engine's own pool — is unchanged, and it now passes at 3 == 3.**

## 12. Limitations

- **Two scenarios, one demand randomiser, one engine seed (1000).** Thirty of the 39 cells rest on
  ten draws; the nine arms that n could not settle were re-run at 100 and two changed verdict (§4a.9),
  which is a caution about the other thirty rather than a reassurance about them.
- **cologne1/cologne3 and grid4x4's `maxpressure`/`fixedtime` tiers are UNMEASURED**, as are P4.7's
  mixture tiers and every SUMO/MOSS backend. T1 flagged the same gap.
- **`att_engine` is read once, at the horizon.** No per-step trace of the engine metric exists, so
  nothing here bounds how the two definitions diverge *during* an episode.
- **The M1 quantisation term is bounded only on the 24 fully-admitted cells measured here** (+3.25 to
  +7.18 s). It is not a general bound.
- **E2's failure is reported, not adjudicated.** I deliberately did not decide whether it indicts the
  probe, because the registered direction of inference is not mine to reverse.
- The probe is **CityFlow-only by construction** and says so by raising; SUMO and MOSS have no
  insertion buffer of this shape.

## 13. Open questions for the coordinator

1. **🚨 E2 failed on `grid4x4/bc_top10@mappo1000` (0.967 < 0.99). Does that indict the probe, per the
   registered direction of inference, or is E2's "arms" generalisation from a behaviour-policy
   measurement itself the flaw?** §6 lays out the evidence both ways. **Nothing downstream should be
   read until this is ruled.**
2. ✅ **DONE — and it paid for itself.** The escalation flipped two verdicts that n=10 could not
   resolve: `hz1x1/dt@maxpressure` close → **holds**, `grid4x4/bc_top10@random` close → **falsified**
   (§4a.9). ⚠️ **One question it raises: `bc_top10@random`'s falsification is 299 vehicles in
   663,785 — 0.045 %.** The registered rule fires correctly and I have reported it as fired, but
   whether a deficit that size belongs in the paper as "falsified E1" is a reporting decision, and
   it is yours.
3. **`P8.4a`'s §6 box: tick it at merge, or leave it while the escalation is outstanding?** I created
   it unticked at `docs/PROJECT_PLAN.md:716`. The DoD says the merge commit ticks it, but the status
   is PARTIAL.
4. **Out of fence, found in passing:** `tests/test_rtg_ablation.py` reads
   `output/p4_dt/dt_seed101.pt` by a **relative** path, so it fails in any worktree without
   `output/`. Own task?
5. ✅ **CLOSED by Amendment E3** — `source_flow` is now in the set (§4a.5). Recorded here because the
   loop is the point: the gap was pinned by a test rather than silently closed, the coordinator ruled
   on it, and the ruling landed in the same task.
6. ✅ **CLOSED by Amendment E4** — `DEFERRED` 61 item (a)'s remaining half. The embedded `dir` is
   deliberately **not** normalised; `restore-draws` refuses first with a message naming the working
   directory. ⚠️ **The refusal is now legible, not absent**: re-materialising from another tree is
   still impossible, and that is by ruling.
7. **T1's M3 remains live in source** — `offline/policies/plan_replay.py:15` and `:43`. A5 queued it
   for P8.4b; noted so it is not lost. **This task's §7 measurement is now the evidence that settles
   which way round those docstrings should read.**
8. **P5.3b is still held.** This probe's answer bears on it; it is not mine to release.

## 14. AI-assistance record

1. **Tool and version.** Claude Code, Claude Opus 4.6, 2026-08-28, single session on branch
   `task/p8.4a-entered-probe`.
2. **Authorship per file.** AI-authored in full: `offline/admission_probe.py`,
   `tests/test_admission_probe.py`, `offline/campaigns/p8_4a.sh`, `docs/plans/p8.4a.md`,
   `docs/returns/P8.4a.md`. AI-generated by execution: `docs/data/p8_4a_admission.json`. AI-edited
   from a human original: `docs/PROJECT_PLAN.md` (§6, two new checklist lines). Read-only, unchanged:
   every frozen path, and `offline/{dt_gate,horizon_metric,method_tier_grid,tier_sweep,spatial_mixing,offline_baselines,materialise_draws}.py`.
3. **Human verification performed.** All by the AI author in this session, and stated as such: 45
   unit tests; the whole suite (1444 passed / 1 pre-existing failure diagnosed by re-running it from
   the main tree); 11 mutations applied to committed source and reverted; the R2-vs-R3 and
   R4-vs-R5 identities enforced with `==` on ints inside every one of 1870 episodes; every admission
   ratio, every identity and all 1170 hz1x1 `att_ours` values recomputed **from the artifact's raw
   rows with no import of the module under test**; Gate −1's byte-identity run twice. **No
   independent human re-ran anything. The `contract-reviewer` has NOT reviewed this branch.**
4. **Who made the research decisions.**
   - **Coordinator, `BRIEF_31` §§1–4 and Amendment A**: the question, the arms, the tiers, the ten
     draws, E1/E2/E3, E3's re-registration over P4.6's tiers (A6), grid4x4's flatness as a
     first-class measurement (A4), the Gate −1 stop rule (A1), the one-hour tmux threshold (A2).
   - **Author (this session), registered in `docs/plans/p8.4a.md` §4 at commit `323acce` BEFORE any
     campaign number existed and approved unchanged as Amendment A3**: the pooled population ratio,
     `Δ = max(spread_behaviour, spread_arm)`, the three status bands, and the escalation trigger at
     `deficit > 0`.
   - **Pre-registration**: `PREREGISTRATION` §3.1's entered-count safeguard — withdrawn by A4/A5 and
     vindicated here — and A1's horizon metric.
   - **Nothing was decided after seeing a number.** The plan was committed before the instrument
     existed; Amendment A was merged before the campaign ran; `docs/data/p8_4a_admission.json` was
     written by a script whose scoring code was frozen and mutation-tested beforehand.

## 15. ⭐ B4 — the argument this episode buys the paper (`BRIEF_31` §8, quoted as required)

> **`PREREGISTRATION` §3.1 registered a safeguard against exactly this failure — an entered-count
> co-report with a >5 % invalidation — in its own words: *"a metric over 'vehicles that entered' can
> still be gamed by a policy that prevents vehicles from entering."* Amendment A4 withdrew it on the
> ground that the metric's own definition already defends against this, which is a restatement of the
> vulnerability rather than a defence. Amendment A5 withdrew the remainder on the ground that the
> entered count is *"stable across policies on fixed demand (measured spread 4.1 %)"* — a spread
> measured WITHIN ONE POLICY FAMILY and stated OF ALL POLICIES. Across the four behaviour tiers it is
> 35.0 %. The safeguard was correct, its removal was not, and the error was found three weeks later by
> a ground-truth check the registration itself made it possible to specify.**
>
> ⚠️ **The coordinator recommended and approved both amendments. That is on the record and belongs in
> it.**

**This task is the safeguard executed, and its own numbers sharpen the case.** A5's claim was that
the entered count is stable across policies on fixed demand. Measured here on identical demand,
across hz1x1's five behaviour anchors: **1.000000 / 1.000000 / 1.000000 / 0.906925 / 0.654786 — a
spread of 34.5 pp.** The withdrawn >5 % invalidation would have voided the `random` and `fixedtime`
tiers' cross-policy comparisons outright. ⭐ **And the registration is what made the withdrawal
legible enough to catch: an unregistered project would have had no safeguard to remove and no record
of removing it.**

🔒 **No novelty is claimed anywhere in this packet or the artifact** (`BRIEF_31` §7).

## 16. What the next task will assume about this one

- **`docs/data/p8_4a_admission.json` is the admission record**, format `p8.4a-admission/1.0`, 1870
  episodes carrying `created`, `entered`, `never_entered`, `att_ours` **and** `att_engine`, at 10
  draws. **P8.4b may treat every `att_ours` in it as verified: all 1870 reproduce their committed
  values exactly.**
- **P8.4b's cost model should use §5's per-cell timings, not hz1x1 alone** — grid4x4's MAPPO anchor
  is 7.027 s/ep against hz1x1's 0.693, a **10.1×** spread the coordinator's hz1x1-calibrated
  estimate cannot see.
- **`offline.admission_probe.read_admission_at_horizon` and `created_from_flow` are reusable**: any
  replay that holds a live `CityFlowEnv` at its horizon can obtain both ATT definitions and an exact
  admission triple with no extra simulation.
- **The `_method_checkpoint` mapping (C7) is load-bearing** and should be reused rather than
  re-derived.
- **Whatever P8.4b concludes about hz1x1's `random` and `fixedtime` tiers, §7's separation says the
  correction there is dominated by B1 selection, and everywhere else by M1 quantisation at +3 to
  +7 s.**
- ⚠️ **Nine arms are unresolved at n=10 and E2 is unadjudicated.** Neither should be quoted as settled.
