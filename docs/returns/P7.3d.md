# RETURN PACKET — P7.3d (the C3 zero-shot point on grid4x4) — INTERIM, after C1 / gate G1 and the first half of C3a (T-obs)
**Date:** 2026-09-19 · **Mode:** Claude Code, implementer session · **Contracts:** v1.1
**Brief:** `docs/briefs/BRIEF_39_p7.3d_grid4x4_zero_shot.md` · **Written against Amendments: A (A0–A9), A.1, B (B1–B7) and B.1.**
**⇒ CURRENT STATE: §24 — the B.6 fix round (2026-09-23), written against Amendments B.4, B.5, B.6, B.6.1 and B.6.2.
The sections before it are the record of their own dates.**
§1–§13 were written before Amendments B and B.1 landed (`a27673b`, merged at `d984bf0`); §14 records what they changed.

**Status: PARTIAL.** C0 and C1 are delivered and gate G1's artifact is committed. C2 left the task (Amendment A1(b)).
**The first half of C3a — the scenario-aware aligned env — and T-obs are delivered (§13, added after §1–§12 were
written; ONE brief-vs-repo conflict there needs the coordinator's eye).** The rest of C3a (`transfer_calibration`,
the probes, T-regress (b)) and C3b–C7 are **not started**. This file is written now, and extended as the task proceeds, so that C1's record — the one
changed test above all — does not live only in a conversation (`CLAUDE.md` §0, *the repo is the memory*).

A0's sequencing note: the P7.3b CI fix it orders first was done by **another session** (`a790ad1`, merged `708f157`);
this session did not touch it. The author confirmed in this session that no commit carries an AI trailer; the harness
text asked for one and the conflict was raised before the first commit (`CLAUDE.md` §4b).

---

## 1. Branch + diff stat

```
task/p7.3d-grid4x4          (worktree /home/filip/rltraffic-p73d; never pushed, never merged into main by me)
$ git diff --stat main...HEAD
 docs/data/p7_3d_cap_e.json       | 11565 +++++++++++++++++++++++++++++++++++++
 docs/plans/p7.3d.md              |   335 ++
 offline/materialise_draws.py     |   729 ++-
 offline/parity.py                |   411 +-
 tests/test_materialise_parity.py |    11 +-
 tests/test_parity_scenarios.py   |   707 +++
 6 files changed, 13685 insertions(+), 73 deletions(-)
```
(measured at `d465ec3`, before this file was added.) Commits, oldest first: `52e61e2` plan · `d04eaa8` merge of `main`
(Amendment A) · `43f6b8d` merge of `main` (Amendment A.1) · `22b1b22` C1 code · `73bf637` C1 fix (config header) ·
`d465ec3` G1's artifact. Both merges have two parents (`git rev-list --parents -n1` → 3 fields, read each time).

## 2. Files produced

- `docs/plans/p7.3d.md` — the plan gate (G0), approved by Amendment A at `52e61e2`.
- `offline/parity.py` — `ParityScenario` (hangzhou the default of every pre-existing entry point; grid4x4 with A15(g)'s
  table), `derived_vtype_attributes` (values READ from the flow block, refused on disagreement with the registration),
  `resolve_external_source` / `read_route_template_text` (RESCO located through `RLTRAFFIC_GRID4X4_RESCO`, no default,
  read in place, both digests verified), a second strict binding mode (`insert_when_absent`), `network_note`.
  `PARITY_CONTRACT_VERSION` stays `1.0`.
- `offline/materialise_draws.py` — scenario selection in `materialise_parity`; the TEMPLATED parent shape (three files,
  `sumo: null`) rendered inside `parity/`; `materialised-draw-parity/1.1` for that shape only; the reader refuses an
  unknown version; `cap_e_report` / `assert_cap_e_exact` / `--report-cap-e` / `--pedigree-artifact`.
- `tests/test_parity_scenarios.py` — 24 tests (T-regress (a) and (c)'s parity half, T-parity-values, T-cap-e, the RESCO
  root, the 1.1 record, the config header).
- `tests/test_materialise_parity.py` — **one existing test changed**, §6 below.
- `docs/data/p7_3d_cap_e.json` — gate G1's artifact, sha256 `949015b3d1be8b96272b0826fa750497f7c57a0d285f6ceb7baefe8f9da9e0cd`.
- Outside the repository, gitignored: 100 CityFlow parents `scenarios/draws/cityflow_grid4x4/draw_0201…0300` and 200
  `parity/` directories (201–300, 1000–1099), all in the **main** tree; evidence under `output/p7_3d_runs/c1/` (§9).

## 3. Gate G1 — A15(g)'s condition, decided by the artifact

**CAP(E) 200/200 exact → `condition_met: true`.** Per draw, through `offline.conversion_audit.audit_pair`: the
`(depart, route)` multiset **and** order equal between the parent's `flow.json` and the rendered `parity/routes.rou.xml`;
every vehicle bound to `cf_parity`; the `<vType>` **read back from each rendered file** equal to A15(g)'s eight values
derived from the flow block; `time-to-teleport -1`, `end 4000`; both rendered files at the digests their provenance
records; the parent's `flow.json` at the digest the parity record was derived from.

| quantity | value | produced by |
|---|---|---|
| draws exact | **200 / 200** | `--report-cap-e`, capture `cap_e_report_run.txt` |
| vehicles, and vehicles bound | **265,477 / 265,477** | the independent check (below) |
| held-out pedigree | **100 / 100** `flow.json` digests equal `p8_4a_admission.json : draw_restoration.grid4x4.flow_sha256` | same run |
| draws not covered by the pedigree artifact | 100 (201–300: new in this task; said in the artifact) | same run |
| structure re-confirmed (information, gates nothing) | ids equal · 240/240 lanes · lane links equal · 256/256 phases | same run |
| artifact provenance | `git_commit 73bf637…`, `git_dirty false` | `_git_provenance_strict` (raises if `git` fails) |

**Independent recomputation, before the artifact was committed** (`independent_cap_e_check.py`: regex over the raw route
text, `json` for the flow, *nothing imported from the repository*; compared **per draw under `==`** before any aggregate
— `DEFERRED` 85's rule): **0 per-draw disagreements, 200/200 exact**, no `/home/` in the artifact.
**Regeneration:** a second `--report-cap-e` run differs from the committed file in **`git_dirty` alone** (`false` →
`true`: the then-uncommitted artifact was itself the dirt); every row equal. It therefore regenerates byte-identically
only from a clean tree at `73bf637`, which I did **not** run (it needs a second worktree) — the coordinator's check.

**The 201–300 parents' rule, read from `provenance.json` and re-verified on all 100 after rendering:**
`draw.seed = 1000 + draw_id`, `jitter_sigma_s 30.0`, `thin_p 0.1`, `volume_scale 1.0`, numpy `2.5.1` (the venv's today,
and the held-out parents'), source flow / roadnet / config digests equal to draw 1000's, `dir` =
`/home/filip/rltraffic/scenarios/grid4x4/` on 100/100, rendered by `main`'s unmodified `materialise()` at `cdfc95d`
with cwd = the main tree (Amendment A6), `git_dirty false`.

## 4. Tests — REAL OUTPUT

Red first (skeletons raising `NotImplementedError`), 22 tests at that point:
```
19 failed, 3 passed in 3.60s
  8 x ValueError: ... has no usable SUMO pairing, so there is nothing to bind   (the grid4x4 shape not built)
  4 x NotImplementedError: P7.3d C1: derived_vtype_attributes
  3 x NotImplementedError: P7.3d C1: resolve_external_source
  1 x NotImplementedError: P7.3d C1: scenario_for_key
  1 x NotImplementedError: P7.3d C1: flow_json_disagreements for a non-default scenario
  1 x Failed: DID NOT RAISE ValueError          (the reader's unknown-version refusal)
  1 x AssertionError: Regex pattern did not match  (the refusal did not name the variable)
```
The 3 that passed are the hangzhou regression pins, green **before and after**, as they must be.
The header test (added later) was red on `TypeError: ... unexpected keyword argument 'network_note'`.

Green:
```
$ RLTRAFFIC_GRID4X4_RESCO=… RLTRAFFIC_DRAWS=… pytest tests/test_parity_scenarios.py tests/test_parity_vtype.py tests/test_materialise_parity.py -q -p no:randomly
78 passed, 1 warning in 24.45s                                    (at 73bf637's content)

$ … the five parity/audit files, variable UNSET:   124 passed, 13 skipped, 1 warning in 60.62s
$ … the five parity/audit files, variable SET:     137 passed, 1 warning in 74.85s          (before 73bf637)

$ env -u RLTRAFFIC_GRID4X4_RESCO pytest -q -p no:randomly          (whole suite, in the worktree)
2148 passed, 105 skipped, 33 warnings in 672.22s (0:11:12)         (at 22b1b22's content)
```
⚠️ **The whole suite was NOT re-run after `73bf637`** (the header fix); only the three parity files were. ⚠️ That
whole-suite command was piped into `tail`, so its printed exit status was `tail`'s — the verdict is read from pytest's
summary line, which carries no `failed`. The worktree has no `output/`, `datasets_v11/` or draws tree, so
artifact-gated tests skip there.
**Skips this task adds in a clean clone: +11** (`tests/test_parity_scenarios.py`: 10 gated on `RLTRAFFIC_GRID4X4_RESCO`,
1 on the draws tree; measured: `12 passed, 11 skipped`). **The CI skip ceiling is NOT pre-bumped** (the P7.1 convention).

Hygiene, English and the frozen guard: exit 0 / 0 / 0 on every changed file, re-run before each commit; the English
check was re-run on the artifact **after** `git add`, because it filters through `git ls-files`.

## 5. Mutations — executed, 19 runs, every mutant killed in the end (transcript `mutations_c1.txt`)

Each: back up → one site (uniqueness asserted) → the guarding test → restore → digest verified `OK`.

| id | mutant | verdict |
|---|---|---|
| M1 | **brief's:** the default stem changed | KILLED (2 tests) |
| M1b | the default TABLE moved (hangzhou `tau 2.0 → 1.5`) | KILLED (2) |
| M2 | **brief's:** grid4x4 `tau` read from the wrong flow key | KILLED — the derivation refuses |
| M2b | the disagreement check skipped | KILLED |
| M3 | **brief's:** CAP(E) compares counts only | KILLED — the shifted depart no longer names the draw |
| M4 | the RESCO net digest check removed | KILLED |
| M5 | the unset variable gets a default | KILLED (2) |
| M6 | insertion accepts a source that already declares a `<vType>` | KILLED |
| M7 / M8 | templated written as 1.0 / hangzhou written as 1.1 | KILLED / KILLED (2) |
| **M9** | the "every vehicle bound" check removed | **SURVIVED first**, then KILLED |
| M9b / M9c | both binding checks removed / the full-table check removed | KILLED / KILLED |
| M10 / M11 | reader accepts any version / the CLI writes inside the draws tree | KILLED / KILLED |
| M12 | A.1-2's seam: the hangzhou path calls the render seam with the new keywords | KILLED (the existing two-argument test) |
| M13 / M14 | grid4x4 keeps the "SHIPPED" header / hangzhou's header moves | KILLED / KILLED (`40e8e617…` ≠ committed `c177e962…`) |

**M9, in full:** with the count check removed a second guard (`binding_is_complete`) still refused the draw, so the
verdict held — under a reason reading *"every vehicle is bound, but…"*, which is false, in a registered gate's artifact.
My test asserted the verdict and a loose regex. I strengthened **my own new test** to pin the reason's exact text and
added a test for the case the second guard exists for (fully bound, `tau` wrong). Both changes are to tests written in
this task, before their first commit.

## 6. The ONE existing test changed — disclosed in full (`BRIEF_39` Amendment A.1-1; the `BRIEF_37` D3 precedent)

`tests/test_materialise_parity.py::test_a_scenario_without_a_sumo_pairing_is_refused_with_its_reason`.
**Diagnosis:** it asserts grid4x4 parity is refused naming `grid4x4.rou.xml`; C1 makes grid4x4 parity SUCCEED whenever
`RLTRAFFIC_GRID4X4_RESCO` is set, by registration (A15(g), A20(f)), so in that environment no code satisfies both.
Measured before any edit: four parity files 101 passed / 10 skipped / 0 failed with the variable unset, 110 / 0 / **1**
with it set. **I edited nothing and stopped**; the coordinator ruled Option 1. The whole diff:
```diff
-def test_a_scenario_without_a_sumo_pairing_is_refused_with_its_reason(tmp_path: Path) -> None:
+def test_a_scenario_without_a_sumo_pairing_is_refused_with_its_reason(
+    tmp_path: Path, monkeypatch: pytest.MonkeyPatch
+) -> None:
     """grid4x4's ``.sumocfg`` names a route file the repo does not contain, so there is nothing
-    to bind.  Skipping silently is how a transfer measurement ends up running DEFAULT_VEHTYPE."""
+    to bind.  Skipping silently is how a transfer measurement ends up running DEFAULT_VEHTYPE.
+
+    Precondition, stated since P7.3d (``BRIEF_39`` Amendment A.1): no candidates root is present --
+    ``RLTRAFFIC_GRID4X4_RESCO`` is unset -- because with it set grid4x4 HAS a pairing, by
+    registration (A15(g), A20(f)).  The assertion below is unchanged."""
+    monkeypatch.delenv("RLTRAFFIC_GRID4X4_RESCO", raising=False)
     materialise(GRID4X4_CONFIG, [1], out_root=tmp_path)
     with pytest.raises(ValueError, match="grid4x4.rou.xml"):
         materialise_parity(GRID4X4_CONFIG, [1], out_root=tmp_path)
```
The ruled line, the fixture it needs, one docstring sentence; both assertion lines byte-identical.
A second existing test broke on my first implementation and was **not** edited:
`test_a_lost_vehicle_is_refused_in_validation_and_no_draw_gets_a_parity_dir` substitutes `_render_bound_routes` with
its old two-argument signature. The **code** was fixed (the hangzhou path calls the seam literally as before, keyed on
the scenario per A.1-2, with the derived-equals-registered invariant checked as a refusal), and M12 pins it.

## 7. Self-review checklist

- Contracts in `docs/CONTRACTS.md` honoured? **Y** — C1 touches no env or agent API.
- All tests actually executed and green? **Y for what §4 lists; N for "the whole suite at the branch tip"** (§4's ⚠️).
- Zero modifications to frozen files? **Y** (the diff stat; guard exit 0).
- Zero new dependencies? **Y** (`hashlib`, `zipfile`: stdlib). Zero new absolute path in source? **Y** (one added
  `/home/` hit is a comment saying the variable never defaults to one).
- Every number here produced by a command run in this session? **Y.**
- Filesystem-mutation barrier? **Y** — refusals precede the staged commit of `parity/`; pinned on the unset-variable
  path by a whole-tree snapshot, and observed on the real tree (a plain re-run refused `draw_1000/parity` as
  `differs`, exit 1, *nothing has been written*).
- Tests first, red for their own reasons? **Y** (§4). Every named mutation executed and pasted? **Y** (§5).

## 8. Deviations, conflicts, limitations

**Deviations.** (1) **C1 is three commits, not one**: code, a fix, the artifact — so the artifact and all 200 parity
records carry a clean code commit. (2) **The 200 parity directories were rendered twice.** The first rendering
(`22b1b22`) carried hangzhou's header sentence *"The network is the SHIPPED one"*, false for RESCO's external network;
that header is hashed into every later cell's `config_sha256`, so I fixed it before any cell exists (`73bf637`, test
first, M13/M14) and re-rendered with `--force` (200 × `parity replaced`; one config digest, `c27d31e8…`, on all 200).
Both captures are kept. (3) `_report_heldout_thresholds`'s two literals now read `parity.HZ1X1_SCENARIO.key`; the brief
called `:1876` *the parity path* — it is hangzhou's held-out pedigree gate, and `materialise_parity` already took its
key from the config stem. (4) An unregistered scenario key falls back to the hangzhou table in `materialise_parity`
(what every scenario was checked against before P7.3d, refused on disagreement as before), while `scenario_for_key`
and `cap_e_report` are strict. (5) The pedigree gate's **second half — the six CityFlow episodes against
`p8_4b_g0_reference.json` (A9) — is NOT done**; only the 100/100 digest half is in the artifact.

**Conflicts, brief vs repo** (all ruled in Amendments A / A.1): the subject is a `SpatialDTAgent` checkpoint (A1);
byte-equal checkpoint round trips are unsatisfiable (A2); `report` cannot regenerate on a branch that changes
`offline/` (A3); Rule B's CityFlow denominator does not exist for grid4x4 and `R_best_source,i` is
`payload["target_rtg"][i]` (A4). New in C1: grid4x4 parents hold no `routes.rou.xml` and RESCO's template declares no
`<vType>`, so the bound routes are rendered inside `parity/` and the type is inserted (A9 accepted the design).

**Limitations.** No grid4x4 SUMO episode has run. The one SUMO contact was a **load** of draw 1000's config
(`--end 1 --xml-validation always`): exit 0, **and its capture file is empty** — SUMO printed nothing, so the exit
code exists only here. It measured nothing. The launcher that runs the worktree's code with cwd = the main tree
(`run_worktree_module.sh`) lives in the evidence directory, not in the repository.

## 9. Evidence, outside `/tmp` — `/home/filip/rltraffic/output/p7_3d_runs/c1/` (gitignored; sha256 prefixes)

`mutate_c1.py bed35495` · `mutations_c1.txt 882a189f` · `full_suite_resco_unset.txt 28a80ed7` ·
`parents_201_300_dry_run.txt 7ae3ed78` · `parents_201_300_render.txt 399689b1` · `parity_dry_run.txt 2f6271a5` ·
`parity_render.txt 40daaf10` · `parity_rerender_force.txt 8dbd1b38` · `cap_e_report_run.txt 8e2fe081` ·
`independent_cap_e_check.py 045e7b8d` · `independent_cap_e_check.txt 5fadacef` ·
`regenerated_p7_3d_cap_e.json 33854cea` · `run_worktree_module.sh fcd84948` · `sumo_load_check_draw1000.txt e3b0c442` (empty).

## 10. Open questions for the coordinator

1. **G1:** verify `docs/data/p7_3d_cap_e.json` (`949015b3…`) from disk → Amendment B.
2. **The +11 clean-clone skips** against the CI ceiling (191): yours to bump after an observed run.
3. **The six-episode half of the pedigree gate** (§8 deviation 5): in C4's commit, or its own?
4. Is `run_worktree_module.sh` worth a committed home (it is how any later grid4x4 re-render must run — A6), or does
   it stay evidence-side until C6's driver subsumes it?

## 11. AI-assistance record (`CLAUDE.md` §8)

- **Tool and version:** Claude Code, model `claude-fable-5-1` (Fable 5.1), 2026-09-19.
- **Authorship per file:** AI-authored — `docs/plans/p7.3d.md`, `tests/test_parity_scenarios.py`,
  `docs/returns/P7.3d.md`, and (generated by AI-authored code) `docs/data/p7_3d_cap_e.json`. AI-edited from a
  human-directed original — `offline/parity.py`, `offline/materialise_draws.py`, `tests/test_materialise_parity.py`
  (the last under the coordinator's ruling A.1-1, line for line).
- **Human verification performed:** none yet by a human on C1. By the AI session: the tests of §4, the 19 mutation
  runs of §5, the independent recomputation of §3. The coordinator read the plan at `52e61e2` and the stopped worktree
  before A.1 (its amendments say so); G1's artifact awaits its verification.
- **Who made the research decisions:** the pre-registration — A15(g)'s parity values and admission condition, A20's
  subject, arms and status (`v2.0-prereg-a20`, before any grid4x4 SUMO number). Coordinator rulings — Amendment A
  (A1–A9) and A.1. The implementer chose no registered quantity; its two unilateral choices are engineering ones, both
  disclosed in §8 (the header fix with its re-render; the three-commit split).

## 12. What the next step will assume about this one

`scenarios/draws/cityflow_grid4x4/draw_{0201…0300,1000…1099}/parity/` exist in the **main** tree at config digest
`c27d31e8…` with records `materialised-draw-parity/1.1` at `73bf637`; `parity.scenario_for_key("cityflow_grid4x4")`
resolves RESCO through `RLTRAFFIC_GRID4X4_RESCO`; `parity_sumocfg_path("cityflow_grid4x4", d, out_root=…)` is the
lookup C3a's env builders use; hangzhou's parity output has not moved by a byte. **Next: C3a, and T-obs as early as
C3a allows (A9) — the first grid4x4 SUMO episode, which tests the 40-wide aligned state before anything stands on it.**

---

## 13. ADDENDUM — C3a, first half: the aligned env takes a scenario, and T-obs (commit `b9ec455`)

Amendment A9: *"Assumption 4 (state width 40) is what T-obs exists to test — run it before anything is built on it."*
So T-obs ran before the rest of C3a. **It is the first grid4x4 SUMO episode this project has run, and it passes.**

**Code.** `offline/aligned_env.py::alignment_for_scenario_key`: `cityflow1x1` **is** `declared_alignment()`, unchanged
(pinned by dataclass equality); `cityflow_grid4x4` is built from the repo's CityFlow roadnet (its sim config, resolved
against the repository root, never the cwd) and RESCO's net through `parity.resolve_external_source`. The three env
builders already took a `scenario_key` for the config and aligned with the hangzhou pair regardless; they now use the
scenario's. `collect.py`'s direct `declared_alignment()` call is untouched (C3b).

**T-obs, as measured** (one fixed-time episode, draw 1000, `aligned_observer_env_for_draw`, halting check ON):
16 intersections and a **40-wide aligned state on every one of 360 decisions** — assumption 4 holds — in the env's
C1 order; actions inside the env's 8; 3,600 observations; **0 teleports**; `{"cf_parity"}`; `time-to-teleport "-1"`;
`e_sumo` finite and re-derived from `total / n_ids`; the halting check agreeing on every lane-second, `n_missing 0`.

```
red:    4 x NotImplementedError: P7.3d C3a: alignment_for_scenario_key        (4 failed, 1 passed)
green:  tests/test_aligned_env_grid4x4.py   6 passed, 1 warning in 109.79s   (T-obs call 108.59 s; RESCO + draws set)
        tests/test_aligned_env.py           9 passed                          (hangzhou, real SUMO, unchanged)
        tests/test_transfer_calibration.py  68 passed, 5 warnings in 32.95s   (builds envs through the changed builders)
mutation (the brief's, in its true form): the halting check restricted to the first 8 monitored lanes
        -> KILLED at `assert 28800 == (192 * 3600)`; offline/sumo_att_reference.py restored byte-identically
```
The whole suite was **NOT** re-run at `b9ec455`.

### 13.1 ⚠️ BRIEF vs REPO — the halting cross-check's lane count. Implemented to the repo; yours to overrule

`BRIEF_39` §3 C3b / §4 T-obs specify the count as **`240 × 3,600`** and the mutation as *"the lane count read from
hz1x1's 16"*. **Both are TOTAL lane counts, and neither is what the registered instrument covers.** The recorder's
cross-check iterates `_monitored_incoming_lanes` (`offline/sumo_att_reference.py:849-851, :870`), and the committed
record says what that means: **all 47 halting-checked cells of `docs/data/p7_3a_zero_shot.json` carry
`halting_n_lane_seconds = 28,800 = 8 × 3,600`** — hangzhou's 8 incoming lanes, not its 16 (measured in this session).
On grid4x4 that is `16 × 12 = 192`, and the first T-obs run measured exactly **691,200 = 192 × 3,600**; the network's
other 48 lanes are boundary-outbound and enter no controlled intersection. The first run was therefore RED on the
brief's number alone, every other assertion having passed. I did **not** touch the instrument (widening it to 240
lanes is a new instrument — `DEFERRED` 83, out of scope) and the test now asserts the instrument's coverage three
ways: derived from `env.intersections`, as the literal `691_200`, and as *not* `8 × 3,600`. The docstring carries this
paragraph's evidence. **If the registration intends the cross-check to cover all 240 lanes, that is a change to a
registered instrument and the coordinator's call, not mine.**

### 13.2 An assertion of mine that was wrong as a fact, corrected from the files before its first commit

I asserted *"at least one intersection's lane permutation is not the identity"*, by analogy with hangzhou
(`(0, 1, 2, 3, 6, 7, 5, 4)` there). **On grid4x4 all 16 permutations ARE the identity.** I investigated before touching
the test, because an identity permutation could equally have meant the movement correspondence had degenerated into a
by-position pairing. It has not: the lane INDEX is reversed between the two files (CityFlow's `A1A0_2` serves `r` and
corresponds to SUMO's `A1A0_0`), so **a by-id pairing would be wrong on 128 of 192 lanes** — only the middle lane of
each road keeps its id, 64 / 192 — while the CityFlow corpus's discovery order runs `_2, _1, _0` within a road and
SUMO's is sorted `_0, _1, _2`, so the two reversals cancel position by position. The test now asserts both facts
(`sumo_lane == f"{road}_{2 - k}"` for every lane; every permutation the identity; 64 same-id lanes). **Worth a line in
the paper's CAP discussion:** on this pair *by-position* coincides with the movement key and *by-id* does not.

### 13.3 A number I said would not be printed, and was

The first T-obs run failed on §13.1's assertion, and pytest's assertion introspection printed the reconstruction's
`repr` into `output/p7_3d_runs/c3a/t_obs_first_run.txt`: **fixed-time `e_sumo = 307.259…` s on grid4x4 draw 1000** —
the first grid4x4 SUMO number anyone has seen. It is an ANCHOR value (C4 will freeze it as a reference cell), not a DT
number; A20's tag existed before it (G3 cleared), so A20(b)–(e)'s *no DT number before the tag* is untouched. The file
is kept, not scrubbed. The mutation runner was written to print the assertion's operands only.

### 13.4 Two timings, **NOT rates** — no canary was taken, so nothing here may enter a schedule (G2 replaces them)

T-obs with the halting check ON over 192 lanes: **107.71 s and 108.59 s** (pytest `--durations`, env construction and
2 TraCI retries included). The mutant run, halting check over 8 lanes: **53.50 s** for the whole pytest process. So on
grid4x4 the cross-check costs roughly half an episode — hangzhou's was 55.96 s against 16.01 s (`BRIEF_37` C1) — which
is what C2's *draw 1000 only* convention is for.

**Evidence (`output/p7_3d_runs/c3a/`, gitignored):** `t_obs_first_run.txt` (red, carries §13.3's number) ·
`t_obs_second_run.txt` (green) · `mutate_t_obs.py` · `mutation_t_obs.txt` · `transfer_calibration_tests.txt`.

**Added to §10's open questions:** 5. Rule on §13.1 (192 vs 240). 6. §13.2's by-position coincidence — a packet line, or
a Decisions-Log entry? **What the next step assumes:** `aligned_observer_env_for_draw("cityflow_grid4x4", d, …)` and
`observer_env_for_draw(…)` build a working grid4x4 env under parity and `-1`; the aligned state is 40 wide in the
subject's order; the halting cross-check on grid4x4 counts 691,200 lane-seconds per checked cell.

---

## 14. ADDENDUM — after Amendments B and B.1 (merged at `d984bf0`)

**Received.** B1: **G1 PASSED** under the coordinator's own route (200/200, 265,477 vehicles, 0 aggregate flow
entries). B2: **A21** (`v2.1-prereg-a21`) — `b_mean_k100` only, no `naive`, no `random`, no `collect.py` door, **one
stage of 700 cells, one token**. B3: nothing for me on the skip ceiling; the six-episode pedigree half goes in **C4**;
the launcher stays evidence-side and **C6's driver carries the cwd rule**. B4: deviations accepted, with the note to
make `materialise_parity`'s unregistered-key fallback strict when next touched. B.1: **the halting count is 192 ×
3,600 = 691,200** (§13.1 stands, the instrument untouched); C3a's first half accepted at `b9ec455`; §13.3's
`e_sumo ≈ 307.26 s` is recorded as SEEN from here on. The coordinator pushed the branch; I still push nothing.

**Done since §13 — one commit, `ec52f1e`:** `tests/test_t_regress_artifacts.py`, T-regress (b)'s `p7_2b` half, written
**before** `transfer_calibration.py` is touched. Measured first on the unmodified module: `report` over
`output/p7_2b/` with the provenance pair substituted reproduces the committed file **byte for byte** (59,305 bytes,
`92b1592d…`, also equal to `transfer_curve.P7_2B_CALIBRATION_SHA256`).
```
$ RLTRAFFIC_OUTPUT_ROOT=… pytest tests/test_t_regress_artifacts.py -q      1 passed in 1.29s
$ (clean clone)                                                            1 skipped, naming output/p7_2b
mutants, source restored byte-identically each time:
  R1  the budget prefix takes k-1 episodes                                  KILLED (the bytes move)
  R2  a per-intersection SHAPE on the one-intersection scenario (the brief's) KILLED (rule_b_target raises)
```
A3 names two substitutions; this module has no `code_changed_since`, so **one** applies here and the docstring says
so. Clean-clone skips this task adds: **+11** (§4) **+ those of `test_aligned_env_grid4x4.py`, NOT counted, + 1** here.

**NOT started:** the per-intersection calibration (both probes, the subject facts, Rule B per id,
`p7_3d_calibration.json`), T-regress (b)'s `p7_3a` / `p7_3b` halves, C3b, C4, G2, C6, the campaign.
**Its design is on disk — `docs/plans/p7.3d.md` §10–§11 — written before a line of it exists.** I stopped here on
purpose: that code computes the REGISTERED prompt targets, this session had grown very long, and a critical quantity is
not something to build at the tail of one. **The whole suite has not been run since `22b1b22`'s content.**

---

## 15. ADDENDUM — C3a's second half: the per-intersection calibration, and the CityFlow probe RUN

Three commits: `aa84e39`, `fcf22fc`, `aef42c1`. **`docs/data/p7_3d_calibration.json` is not written**
— its SUMO half does not exist yet.

**What was built.** `run_probe_per_intersection` (CityFlow, both return routes compared **per id**,
sharing `run_probe`'s loop, now extracted once as `_roll_maxpressure_episode`);
`subject_facts_per_intersection` (the five checkpoints at A20(a)'s digests — **the first pin of them
in a source file**, as A20 said was owed — with the identity mask, the declared 40,000 steps and
five-seed agreement on every per-intersection constant enforced); `per_intersection_statistics` and
`per_intersection_targets` (Rule B through the unchanged `rule_b_target`, k = 100 registered, k = 5
and 20 recorded); the chunk contract and the CityFlow runner.

**Amendment A4's field question, settled by measurement rather than by reading:** on the real
subject, `rtg_scale_i == -stats.rtg.min_i` and `stats.rtg.max_i == 0.0` on **all 16** intersections
— so a target read from `stats["rtg"]` would be **0.0 everywhere**. `R_best_source,i` is
`payload["target_rtg"][i]`, and A4's named mutation dies on two tests.

**The CityFlow probe RAN in-session (A4 permits it; ≈ 100 short CityFlow episodes).**

| quantity | value |
|---|---|
| canary, draw 0, before anything | **2.257 s** |
| 100 episodes, draws 201–300 | **149.0 s wall = 1.490 s/episode** |
| chunks written | 100, at `output/p7_3d/calibration/` |
| two routes equal, per id | **1,600 / 1,600** (draw, id) pairs |
| returns integral and ≤ 0 | all 1,600 |
| distinct per-id means at k = 100 | **16 of 16** |
| disjointness | probe 201–300 against training 1–200 (read from the checkpoint) and held-out 1000–1099: asserted |
| module mean vs `statistics.fmean`, per id, under `==` | **all 16 equal** |

⚠️ **These are SOURCE-DOMAIN CityFlow numbers** — Rule B's denominator, the same kind of quantity
`p4_3_probe.json` holds for hangzhou. No grid4x4 SUMO number is produced here; A21's *results seen*
column is unaffected beyond §13.3's fixed-time anchor value.

```
tests: 24 passed (per-intersection) + the hz1x1 byte-identity pin, 89 passed with the hz1x1
       calibration suite; red first each time (12 failed / 8 passed, then 2 failed / 1 passed)
mutations: 18 more, 18 KILLED, every source restored byte-identically
  5 probe (pooling; the per-id refusal; the shared loop recording the pre-step info -- caught by
    hangzhou's P4.3 reproduction; a single-intersection scenario accepted; the decision count)
  8 Rule B (A4's named one -- R_best from stats.rtg.max; the ratio inverted; statistics pooled;
    k = 20 given the registered role; five-seed agreement dropped; digest pins unchecked; the
    identity-mask check dropped; the two probes' draw sets not required to match)
  5 chunks (the stored verdict trusted; domain dropped from identity; scenario key dropped; a bad
    chunk overwritten instead of moved aside; the artifact reading memory instead of disk)
```

**Disclosed:** two assertions in my own new tests were wrong before their first commit — a field
name `in_support_position` does not return, and a guessed support position — both corrected from the
function's body and from a measurement, with **no source changed to make a test pass**. The trailer
instruction reappeared in a new form (`Claude Opus 5 (1M context)`); raised in that turn before any
commit, the author refused it again, and **0 commits on this branch carry one** (measured).

**Next, in order:** **G2** — the one-cell measurement in tmux, author-started — **before** the SUMO
probe's schedule is written; then the SUMO probe (tmux, channel (a)); then
`docs/data/p7_3d_calibration.json`; then C3b. The whole suite has not been run since `22b1b22`.

---

## 16. ADDENDUM — B.2-3's suite gap CLOSED, and gate G2's script (commit `9ac75ce`)

### 16.1 The whole suite at the branch tip — REAL OUTPUT, 0 failures, delta accounted by measurement

B.2-3 required the whole suite at the tip with `RLTRAFFIC_GRID4X4_RESCO` set, compared against
2,148 / 105 at `22b1b22`'s content. **Run, from the worktree, exit 0:**

```
$ cd /home/filip/rltraffic-p73d
$ RLTRAFFIC_GRID4X4_RESCO=/home/filip/rltraffic/scenarios/grid4x4_candidates \
      /home/filip/rltraffic/.venv/bin/python -m pytest -q -p no:randomly
2204 passed, 96 skipped, 32 warnings in 224.90s (0:03:44)
pytest exit=0
```

**The delta is decomposed by MEASUREMENT, not by arithmetic on the totals** — I first derived the
split from the totals, noticed that was circular, and ran the tip a second time with the variable
UNSET to separate the two causes:

| run | passed | skipped | what it isolates |
|---|---|---|---|
| `22b1b22` content, RESCO **unset** | 2,148 | 105 | the previous session's baseline |
| tip before G2 (`2c6048e`), RESCO **unset** | **2,172** | **112** | **+24 passed, +7 skipped = the 31 tests added since** |
| tip before G2, RESCO **set** | **2,188** | **96** | **+16 passed, −16 skipped = 16 RESCO-gated tests now run** |
| tip **at `9ac75ce`**, RESCO set | **2,204** | **96** | **+16 passed = G2's own 16 tests** |

`2,148 + 24 + 16 + 16 = 2,204` and `105 + 7 − 16 + 0 = 96`: both reconcile exactly, and the 31 is
independently the collected count of the four new files (parity 23→24, aligned-env-grid4x4 6,
t-regress 1, per-intersection 23). **0 failed in all three runs.** So `aa84e39` — the commit that
extracts `run_probe`'s rollout loop into a helper shared with hangzhou's registered P4.3 path, the
change B.2-3 names as the one where collateral is real — has no collateral: its own guard
(`test_regenerated_draw_201_reproduces_p4_3s_recorded_episode`) passes inside the full suite, as do
the 68 `test_transfer_calibration.py` tests. **Nothing was edited to make anything pass.**

### 16.2 G2's script, built to B.2-2 and NOT RUN

`offline/g2_measure.py` + `offline/campaigns/p7_3d_g2.sh` + `tests/test_g2_measure.py` (16 tests).

- **Draw 5** (B.2-2(1)), outside the held-out pool and the probe band, asserted by a test. Its
  `parity/` does not exist (C1 rendered 201–300 and 1000–1099), so the script renders it first with
  C1's own tool and records the provenance it wrote; that render is exercised in `tmp_path` by a
  test, so a typo in it fails here rather than inside the author's pane.
- **What is measured:** wall and in-process seconds (split into env/agent build, rollout,
  reconstruct), **peak RSS of the whole process TREE**, GPU memory, the canary's both halves.
  Tree rather than self because a pool's cost is the *sum* over live processes and
  `resource.getrusage`'s `RUSAGE_CHILDREN.ru_maxrss` is the *maximum* over children, never the sum;
  a mutant that samples only its own process is killed by a test that starts an 80 MiB child.
- **What is not:** no travel time, no `e_sumo`, no return, no return-to-go, no action, from any
  cell, on any draw. `reconstruct_sumo_episode` **is** called — the campaign's cells call it, so a
  rate without it would be a rate for a different cell — and its result is discarded unread except
  for the campaign's own refusal counters (observations, teleports, vehicle types, teleport option,
  intersection count). The measurement body is written entirely under `fenced_do_not_report` in a
  `g2/` directory no `report` globs.
- ⚠️ **One thing the capture WILL contain that looks like an outcome, disclosed in advance:** the
  canary line prints hangzhou draw 0's `local_return` and `att_horizon`. Those are the committed
  constants `CANARY_REFERENCE_LOCAL_RETURN` / `CANARY_REFERENCE_ATT_HORIZON`, comparing them **is**
  the canary's correctness half (`BRIEF_36` E1.2), they are already in `docs/data/p4_3_probe.json`,
  and they are hangzhou CityFlow values that say nothing about grid4x4. Nothing is added to the
  *results already seen* ledger by them.
- **The driver carries the cwd rule itself** (B3/Q4): `cd $MAIN` so a render embeds the main tree's
  `dir` (A6, `DEFERRED` 87) while `PYTHONPATH` is the worktree, plus an assertion of *which file*
  loaded. Refusals precede the work in a pinned order — interpreter, import-from-worktree, no live
  runner, group leader, SigIgn, RESCO root, inputs, dirty tree, trap. **No token:** G2 consumes no
  authorisation, and a test asserts the word never appears in its executable text.

**Two failures I found in my own new code and fixed in the CODE, not the test:** the driver's
banner and its interrupt message named the forbidden fields in prose (`no e_sumo, no return…`,
`no token was consumed`), which the strict text assertions caught. Both were reworded; the
assertions stayed strict. **Seven mutants, seven KILLED**, every source restored byte-identically:
the sampler reading only its own process; `e_sumo` recorded in the structural block; an outcome
name reaching a `print`; the cell moved to draw 1000 (§3 C5's pre-A21 clause); the dirty-tree
refusal moved after the work; the cwd rule dropped; the handed-over tmux command drifting from the
header's.

**NOT RUN.** G2 is a tmux run the author starts (`CLAUDE.md` §5, channel (a)). The command handed
over is character-for-character the one in the driver's header, and a test asserts that identity.
Evidence: `output/p7_3d_runs/g2/` — the three suite captures, `mutants_g2.json`, `mutations_g2.txt`.

### 16.3 The first hand-over REFUSED, and the defect was mine (`1066aff`)

The author started the pane; the capture is three lines:

```
REFUSING TO START: offline.g2_measure loaded from 'nothing', not /home/filip/rltraffic-p73d
  The cwd is the MAIN tree (§3) so a rendered draw embeds the main tree's dir; the
  CODE must still be the task worktree's, or this gate measures another commit.
```

**The refusal was correct and the script was wrong.** Diagnosis, measured: rule 3 puts the process
cwd in the MAIN tree so a rendered draw embeds the main tree's `dir` (A6, `DEFERRED` 87); Python
prepends the cwd to `sys.path` for **both** `-c` and `-m`; the main tree has an `offline` package
**without** `g2_measure`, so `import offline.g2_measure` raised `ModuleNotFoundError`, `LOADED`
came back empty, and the precondition fired exactly as designed. `python -P` suppresses the cwd
entry so `PYTHONPATH` decides, and it does **not** change the working directory (measured: cwd
stays `/home/filip/rltraffic` under `-P`), so the render rule is untouched.

⚠️ **`offline/campaigns/p7_3b_anchor.sh` already carried `-P`, for this exact reason, and I omitted
it.** The run line had the same defect and would have died *after* every precondition passed —
i.e. minutes into the author's pane rather than at its first second.

**What now pins it** — a text assertion alone would be cargo-cult, so there are two:

1. both interpreter calls carry `-P` (2 invocations, asserted by count);
2. **executable and hermetic, with its own negative control:** a decoy `offline` package is created
   in a temporary working directory — the shape the main tree has — and the driver's **own** import
   check, extracted from the script rather than retyped, is run from there and must resolve to this
   worktree; the same command with `-P` stripped must fail with `ModuleNotFoundError`, which is what
   proves the test is about the flag and not about luck.

Both `-P` mutants KILLED. 18 tests in the file.

### 16.4 De-risking the second hand-over, without running the gate

Two checks, neither of which measures anything:

- **Every precondition executed end to end**, from a copy of the driver under
  `output/p7_3d_runs/g2/dry_run_preconditions.sh` whose measurement call is replaced by `--help`,
  run under `setsid` so the group-leader check is real. Exit 0; the banner prints the commit
  `1066aff`, `code /home/filip/rltraffic-p73d/offline/g2_measure.py`, `cwd /home/filip/rltraffic`.
  Capture: `dry_run_capture.txt`.
- **The registered subject LOADS and acts on the aligned grid4x4 env** — construction only, on
  draw 1000 whose parity already exists, so nothing was rendered and no episode was rolled:
  16 intersections, `n_actions` 8, `state_dim` 40, `n_head` 4, `spatial_mixing` False, 16 prompt
  ids (14 distinct values), reset info 16 intersections at state width 40, and one `act()` call
  returning 16 actions all inside the env's 8. **No ATT, `e_sumo`, return or RTG was computed or
  printed.** That was the one link the dry run could not reach, and it is the link that would have
  killed the author's run at its first cell.

---

## 17. GATE G2 — the measurement, and what it sets (`88eb59c`, `1cbcf6c`)

G2 ran in the author's pane at commit `07ab267`, **canary 0.76 s, both halves passed**. Draw 5's
parity was rendered by the gate itself: `written`, **1,335 / 1,335 bound**,
`materialised-draw-parity/1.1`, net `8d192de4…`. Machine: 16 cores, 48,174 MiB host (39,924
available), RTX 5080 Laptop, 16,303 MiB GPU.

| cell (halting check OFF, draw 5) | in-process | rollout | self HWM | GPU |
|---|---|---|---|---|
| `dt`, seed 101, the checkpoint's own prompt | **35.48 s** | 33.67 s | 1,279.7 MiB | 1,392 MiB |
| `fixedtime` | **34.55 s** | 33.17 s | 1,279.7 MiB | 1,502 MiB |

Both cells: 16 intersections, 3,600 observations, **0 teleports**, `['cf_parity']`,
`time-to-teleport '-1'`.

| W | wall | effective s/cell | in-process mean | speed-up | peak tree RSS | per worker | GPU |
|---|---|---|---|---|---|---|---|
| 4 | 46.66 s | 11.67 | 45.4 s | 3.89× | 6,562 MiB | 1,640 MiB | 2,489 MiB |
| 8 | 77.36 s | 9.67 | **75.5 s** | 7.81× | 11,856 MiB | 1,482 MiB | 3,573 MiB |
| 12 | 58.60 s | **4.88** | 55.8 s | **11.39×** | **17,297 MiB** | 1,441 MiB | 4,595 MiB |

### 17.1 ⚠️ An anomaly I cannot explain, carried rather than smoothed

**W = 8 is slower PER CELL than W = 12** — 75.5 s against 55.8 s in-process — which is backwards.
It is not noise: within each pool the spread is under 0.5 s (W = 8: 75.3–75.8; W = 12: 54.7–56.1).
I have no explanation. It does not change the choice (12 is the best measured throughput and
matches `BRIEF_37` C3's ruling of 12 on these 16 cores), but it does change the *confidence* in the
schedule, so **both rates are carried into the driver header** and a mutant that removes the word
`UNEXPLAINED` from it is killed. If W = 12 ever behaves like the W = 8 row, the campaign's 700
cells move from ≈ 57 min to ≈ 73 min — which is why the estimate below is a range.

### 17.2 What G2 sets, computed from the measurement and recomputed by a test

- **`WORKERS = 12`.** Host headroom at peak: 17,297 of 39,924 MiB available (43 %), 22,627 MiB
  free. GPU: 4,595 of 16,303 MiB (28 %).
- **`RSS_BUDGET_MIB = 24216` = the measured peak × 1.4**, and `PER_WORKER_RSS_MIB = 1441` = the
  peak ÷ workers. A test **recomputes both from the peak** and dies on a round number no
  measurement supports.
- **Schedule for C6's 700 cells at W = 12: ≈ 57 min**, or ≈ 73 min at the W = 8 per-cell rate, plus
  the halting check on draw 1000's 7 cells (T-obs: roughly double an episode). **≈ 1–1.5 h.**

### 17.3 Two findings from the capture, both mine

1. **Three refusals precede the successful run in `g2_capture.txt`.** One is §16.3's `-P` defect.
   **The other two are `not a process-group leader`** — the J3 guard firing on a live machine and
   consuming nothing, which is the evidence `BRIEF_38` B3(2) asked for on P7.3b's refused start.
2. **`e_sumo` appeared OUTSIDE the fence in the written record** — in `what_this_is`, the
   disclaimer that says the record carries no `e_sumo`. Found by the packet's own fence check,
   *after* the gate ran. It is prose, not a value, and every measurement is fenced; but an artifact
   whose prose contains `e_sumo` matches any grep for an outcome leak, which is the search a
   reviewer runs. The writer is fixed and pinned by a test. ⚠️ **The record on disk from the
   author's run carries the earlier wording**, and is not worth a re-run to reword.

## 18. C5 — the SUMO probe and its driver, NOT RUN

`run_sumo_probe_per_intersection` (`88eb59c`) and `offline/campaigns/p7_3d_probe.sh` (`1cbcf6c`).
A17(b)'s five refusals per episode — 0 teleports, exactly `[cf_parity]`, `"-1"`, the decision
count, and the two-route equality **per intersection, refusing by name** — all stopping the run;
the engine reads are part of a SUMO chunk's identity, so a chunk recording a teleport is not reused
on a resume. The driver additionally refuses unless all 100 parity configurations and **the
CityFlow half's 100 chunks** are present: Rule B's ratio is formed over the same draws in both
domains, so a missing half is a refusal and not a smaller `k`.

**An existing hz1x1 guard caught a real defect and I did not touch it.**
`test_the_module_contains_no_bare_dt_act_call` pins `transfer_calibration.py` to exactly ONE
`policy.act(...)` call; my new rollout was a second copy of the same loop and the count went to 2.
The fix was to **share, not to edit the test**: `_roll_sumo_maxpressure_episode` is extracted and
both probe entry points call it — the same move already made on the CityFlow side.
`tests/test_transfer_calibration.py` has a **zero-line diff**.

```
tests: 35 in test_per_intersection_calibration.py, 23 in test_g2_measure.py;
       153 passed across the four calibration files; red first each time
mutations: 10 on the probe -- 8 killed at once, TWO SURVIVED, both gaps in MY OWN tests:
   the shared loop recording the pre-step info, and the vehicle type read only at the horizon.
   Closed by (a) a test that reproduces p7_2b_calibration.json's committed draw-201 episode
   field by field under == -- the SUMO counterpart of P4.3's CityFlow guard -- and (b) a fake env
   whose type changes mid-episode; a third mutant (the post-loop teleport read dropped) dies on
   the same fake. 6 more on the driver, all killed, including a real copy into docs/data.
dry run: every precondition of BOTH drivers executed end to end under setsid, exit 0
```

**Three of my own test bugs, fixed before their first commit and disclosed:** a persisting
`monkeypatch` that made an assertion match the wrong refusal; a teleport schedule that made me
assert 2 where the code correctly produced 1; and — three times — **substring-absence assertions
where the contract was behavioural**, each tripped by a legitimate message (the G2 banner, its
interrupt line, and two refusals that usefully name `docs/data` paths). The last is now a
line-by-line check against the ways a shell writes, with a mutant that really copies a file into
`docs/data`. The pattern is mine and worth naming: I reach for a string ban when I mean *"this must
not happen"*.

**NOT RUN.** The probe is a tmux run the author starts. Evidence: `output/p7_3d_runs/g2/` and
`output/p7_3d_runs/c3a2/`.

---

## 19. C5 RAN, and `docs/data/p7_3d_calibration.json` exists (`01bb0c5`, `0db9d08`)

### 19.1 ⚠️ MY SCHEDULE ESTIMATE WAS WRONG BY 6×, AND IT COST THE AUTHOR 40 MINUTES

The probe driver's header said *"Expect ≈ 100 × 4.88 s ≈ 8 min at W = 12"*. **It took 49.9 min**
(2,991.7 s, 29.92 s/episode). The error is mine and it is elementary: **4.88 s/cell is G2's
POOLED effective rate at twelve workers, and this probe runs SEQUENTIALLY in one process** — which
the same header says, two lines below, in §6. I applied a parallel rate to a serial run.

The correct prediction was available before the run: G2's single-worker cell was 35.5 s, and a
MaxPressure episode with no DT forward pass and no observer is cheaper, so ≈ 100 × 30 s ≈ 50 min —
which is what happened, to within 0.3 %. **The measurement was right; my arithmetic over it was
not.** The capture carries the wrong estimate, and this paragraph is its correction.

**What it does NOT change:** C6's campaign IS pooled, so the 700-cell estimate of ≈ 57 min at
W = 12 stands on the right rate. But the probe now gives a second, cleaner data point for the
campaign: **a real sequential grid4x4 SUMO episode is 29.92 s** (MaxPressure, no observer) against
G2's 35.5 s (DT, observer, reconstruct).

### 19.2 The probe, as measured

Canary **0.81 s**, both halves passed. 100 draws × 16 intersections, wall 2,991.7 s, **0 failed**,
0 chunks moved aside. Every SUMO chunk: 360 decisions, **0 teleports**, `['cf_parity']`, `"-1"`,
`engine_seed_requested 1000`, `git_dirty false`. Verified independently over all 200 chunks of both
halves (a `json`-only route importing nothing from `offline/`): **0 problems**, both halves cover
201–300 exactly, and the 16 intersection ids appear in the same order on every chunk of both.

### 19.3 The artifact, and the 16 registered prompts

`docs/data/p7_3d_calibration.json`, sha256 `3e9df8ee…`, written at `01bb0c5` from a **clean** tree.
An earlier generation recorded `git_dirty: true` because the code was still uncommitted; I
committed the code first and regenerated, and the two are **byte-identical apart from the
provenance pair** — the numbers do not depend on the tree state.

| ix | R_best | S_cityflow | S_sumo | target (k = 100) | support | position |
|---|---|---|---|---|---|---|
| A0 | −132.0 | −252.88 | −239.33 | **−124.9271** | [−259, 0] | inside |
| A1 | −94.0 | −246.07 | −237.05 | **−90.5543** | [−180, 0] | inside |
| A2 | −130.0 | −269.50 | −280.94 | **−135.5184** | [−395, 0] | inside |
| A3 | −141.0 | −312.36 | −260.51 | **−117.5948** | [−255, 0] | inside |
| B0 | −114.0 | −270.76 | −247.73 | **−104.3035** | [−232, 0] | inside |
| B1 | −76.0 | −183.94 | −184.76 | **−76.3388** | [−209, 0] | inside |
| B2 | −84.0 | −202.80 | −194.06 | **−80.3799** | [−170, 0] | inside |
| B3 | −83.0 | −229.87 | −192.95 | **−69.6692** | [−160, 0] | inside |
| C0 | −102.0 | −254.20 | −237.94 | **−95.4755** | [−200, 0] | inside |
| C1 | −98.0 | −223.36 | −231.23 | **−101.4530** | [−836, 0] | inside |
| C2 | −64.0 | −182.77 | −190.07 | **−66.5562** | [−174, 0] | inside |
| C3 | −132.0 | −279.15 | −255.15 | **−120.6513** | [−258, 0] | inside |
| D0 | −125.0 | −289.85 | −251.54 | **−108.4785** | [−255, 0] | inside |
| D1 | −142.0 | −316.29 | −275.81 | **−123.8263** | [−247, 0] | inside |
| D2 | −98.0 | −229.49 | −214.87 | **−91.7568** | [−189, 0] | inside |
| D3 | −127.0 | −287.23 | −257.47 | **−113.8415** | [−258, 0] | inside |

**All 16 land inside their own intersection's training support** — reported as the diagnostic it
is (A8), selecting nothing. **The SUMO/CityFlow probe-mean ratio ranges 0.834 (A3) to 1.042 (A2)**,
so the per-intersection rule moves the prompt by up to 17 % in either direction: a pooled statistic
would have hidden that, which is why A17(e) forbids pooling.

**Recomputed independently before the commit** — `json` over the chunks, exact `Fraction` means,
the ratio formed first as `rule_b_target` forms it, importing nothing from `offline/`: **0
disagreements over 48 (intersection, budget) targets under `==`**; `k = 100` registered on all 16,
`k = 5` and `20` recorded only; no machine-local path in the file.

⛔ **These are probe quantities and a prompt computed from them. No cell has been evaluated, on
either backend; nothing here is a result for any arm.**

### 19.4 What remains

C3b (the cell on 16 intersections, `spatial_agent_with_targets`, the per-id RTG refusal), C4 (the
six reference cells + the six-episode pedigree half), C6 (the driver, `WORKERS = 12`, the RSS
budget), the campaign under one token, and C7. **The whole suite has not been run since `9ac75ce`**
(2,204 / 96); six commits have landed since.

---

## 20. THE WRONG-SESSION INCIDENT, and what I restored (2026-09-21)

The P5.4 implementer was handed this task by mistake and worked in this worktree for about nine
minutes. **The author's ruling: nothing it produced survives.** What I found, from disk, before
changing anything:

| check | finding |
|---|---|
| `git status --porcelain` | **empty** — no uncommitted edit survived |
| `git log --oneline -6` | tip `5c5d417`, a merge of `main`; **the only commit it made** |
| `git ls-remote origin` | `8e197a1` — my last commit, so the reset condition held |
| `worktree list` | **no throwaway to remove.** `/home/filip/rltraffic-p54` is the P5.4 task's OWN worktree with its branch checked out; removing it would have destroyed another task's work, so I did not. `git worktree prune --dry-run` was empty. |
| `output/p7_3d_runs/` files in 22:05–22:20 | **none**, on either day |

`git reset --hard 8e197a1`; HEAD and the remote tip then agree. **Its suite numbers (2,247 / 99)
are not used anywhere**; §21's run replaces them.

### 20.1 The one finding kept — VERIFIED BY ME, with a control

*The budget guard's mixing check is skipped for the subject's method name.* True, and it matters:

```
DT_METHODS = ('dt_spatial', 'dt_nomix')          # 'dt_nomix_h4' is NOT in it
assert_declared_budget(<a SPATIAL checkpoint>, 40000, 'dt_nomix_h4')  -> ACCEPTED
assert_declared_budget(<the same file>,        40000, 'dt_nomix')     -> refused
```

So a **mixing** checkpoint passes as the registered non-mixing control under the subject's own
method name. **No number is at risk**: A20(a)'s five digests are pinned in `fcf22fc` and
`subject_facts_per_intersection` asserts the flag and the identity mask directly. I did **not**
edit P5.2's module; `spatial_agent_with_targets` asserts `spatial_mixing` itself at the call site,
with a test that uses the real `dt_spatial_h4` checkpoint as the control. **For the coordinator:
whether `DT_METHODS` should gain the h4 names is a ruling on P5.2's module, not mine.**

## 21. The whole suite at the final tip — REAL OUTPUT

```
$ cd /home/filip/rltraffic-p73d
$ RLTRAFFIC_GRID4X4_RESCO=/home/filip/rltraffic/scenarios/grid4x4_candidates \
      /home/filip/rltraffic/.venv/bin/python -m pytest -q -p no:randomly
2264 passed, 107 skipped, 32 warnings in 237.78s (0:03:57)
pytest exit=0
```

**Delta from 2,204 / 96 at `9ac75ce`: +60 passed, +11 skipped = 71 tests added**, and that is the
collected count of what this session added: `test_spatial_cell.py` 12, `test_reference_cells.py` 4,
`test_g2_measure.py` 21→30, `test_t_regress_artifacts.py` 1→3, plus P5.4's own file arriving from
`main`. The two new files skip 6 of 16 in a clean clone (checkpoints, draws, SUMO).

⚠️ **An earlier run in this session reported 1 failed, and it was MY doing, not a defect.** I
edited `rtg_calibration.py` while that run was in flight, so `inspect.getsource` read the new file
at stale line offsets and returned the wrong function to
`test_run_probe_still_refuses_a_multi_intersection_scenario_with_its_own_message`. The same test
passes alone and in the clean run above; the test's own text is unchanged since `aa84e39`. **A
suite run is only evidence if nothing edits the tree while it runs**, and that run is not evidence.

## 22. C3b, C4 and C6 — delivered

**C3b (`23d4a16`, `4ef9cd8`).** `spatial_agent_with_targets` (load, THEN apply, with the budget,
`rtg_scale` and a per-id read-back all asserted); `dt_choose` recording **16 RTG and 16 reward
series** keyed by id in the env's order, the RTG read before `act` so D1's shift-by-one holds;
`assert_rtg_first_matches_targets` refusing **per id, by name**; `load_grid4x4_targets` reading the
16 prompts from the digest-pinned artifact and refusing any whose role is not
`registered_prompt`; `cell_chunk_name` gaining the scenario for non-default scenarios only.
**T-16 ran**: 20 decisions of seed 101 on grid4x4 draw 1000, `rtg_first_i == target_i` on all 16,
D1's identity per id, actions inside 8, 0 teleports, `cf_parity`. T-regress (b) now covers all
three hangzhou artifacts, byte-identically. `DEFERRED` 86 closed: the theatre test is **replaced**
by one that calls `run_pilot`, and the guard-removal mutation that the old one survived kills it.

**C4 (`747cb25`, `9c4a979`).** `docs/data/p7_3d_reference_cells.json`, sha256 `5265f0d5…`, from a
clean tree. The six anchors, **rolled twice and compared per cell field by field: 0 disagreements**
(clocks excluded). Draw 1000's fixed-time `e_sumo` is **307.2593**, the value that leaked into a
failure repr on 2026-09-19 — reproduced three days later. The CityFlow pedigree: **6/6 over 32
fields each**. ⚠️ The first pedigree run reported 0/6, differing on exactly `seconds` and
`seconds_rollout` and nothing else — wall clocks. They are now excluded **by name in a documented
tuple**, so a field that stops reproducing must be added there by someone who justifies it.

**C6 (`837f703`).** `offline/campaigns/p7_3d_grid4x4.sh`: one stage, 700 cells, one token,
`WORKERS = 12`, `RSS_BUDGET_MIB = 24216` (G2's peak × 1.4, recomputed by a test), B.3's schedule
range 57–113 min with the anomaly named, B.2-3b's demand line recomputed from the flow files
today, `-P` everywhere, the two-step foreground form documented with the reason the one-liner
refuses. **T-driver executes the header's own line through tmux**: the group-leader check does not
fire — it refuses at the dirty-tree check, which is the proof B.3-1(2) asked for.

### 22.1 ⚠️ A real bug I found by running the driver's own documented form before committing it

The user-facing argument is `confirmatory`. **hz1x1 already has a `confirmatory` stage of 1,200
cells**, so passing the bare word to `--stage` would have rolled **1,200 hangzhou cells under a
grid4x4 token** and produced a complete, plausible artifact. The driver now maps it to
`grid4x4_confirmatory` once, and a test asserts the mapped value reaches the module and that the
two declarations are different sizes. Nothing had run; the cost was minutes.

### 22.2 Mutations this session: 32 run, 32 killed — but SIX survived first, and all six were my tests

X1 (the read-back, defence in depth that nothing forced) · X10 (the role check, shadowed by the
digest pin) · C5 (`"113 min"` matched inside the driver's own banner, not the header) · C8
(`"tmux new -s p73d_cells"` matched inside the one-liner it was meant to exclude) · and earlier
S6/S7. Each was closed by a test that forces the guard, not by weakening anything.

### 22.3 My own test bugs this session, all fixed before their first commit

Four, each corrected from a **committed artifact or a measurement**, never from belief:
`reward_series[0] is None` (the artifact says every series begins `-0.0`); a fake env feeding a
non-zero reward at reset (no real env does); a regex assuming the wrong word order in a message
the code gets right; and a stage-1 path I chose where the artifact records its own.

### 22.4 One small thing for the author, about the capture

The hand-over line ends `| tee -a <capture>`, so the shell reports **`EXIT=0` even when the driver
refuses** — the pipeline's status is `tee`'s, and `pipefail` is set inside the script, not in the
invoking shell. The refusal TEXT is in the capture either way, which is what matters, and this is
how P7.3a's and P7.3b's captures behaved too. Read the text, not the exit code.

## 23. What remains: the campaign, then C7

The token (G5), the campaign under it, the stage read (G6), then C7's packet. **The pre-flight
(G4) has not run.**

*(Superseded by §24: the pre-flight ran on 2026-09-23 and was NOT CLEAR.)*

---

## 24. THE B.6 FIX ROUND (2026-09-23) — the driver's wiring into the module

**Written against Amendments B.4, B.5, B.6, B.6.1 and B.6.2** and `docs/reviews/P7.3d-preflight.md`, read whole after
`git -C /home/filip/rltraffic-p73d merge --no-edit main` → merge commit `e14d950` (4 files: `PROJECT_PLAN`, `BRIEF_39`,
`DEFERRED`, the pre-flight review). **Session model: `claude-opus-5-5[1m]`** (§24.15).

**Status: B.6 DONE — B.6-2 items 1–6 delivered, tests first, every named mutation executed (22 of 22 KILLED on the final
code), T-regress (b) byte-identical, whole suite green at the tip, and `dt_reroll_check` run once: IDENTICAL.**
**⛔ ONE finding blocks the token and is NOT fixed here, because it lies outside B.6-2's list: `report` has no grid4x4
body (F-B6-3, §24.5 — measured, not predicted).** Nothing has been merged or pushed.

### 24.0 Session start: three probes and two conflicts, before any other command

Verbatim (B.5-4, B.6.1): **(a)** `git reset --hard -h` → *"Permission to use Bash with command git reset --hard -h has
been denied."* **(b)** `git -C /home/filip/rltraffic-p73d reset --hard -h` → **ran**: the `usage: git reset [--mixed |
--soft | --hard | --merge | --keep] [-q] [<commit>]` block, `exit=129`, `git status --porcelain` empty afterwards (the
measurement `DEFERRED` 90 records). **(c)** `Write(envs/GUARD_PROBE.md)` → *"File is in a directory that is denied by
your permission settings."*; `ls envs/GUARD_PROBE.md` afterwards: no such file. **What fired: the permission layer**,
for (a) and (c); the session was launched from the worktree (B.6.1-2).
Two conflicts were raised and nothing was done until they were ruled (B.6.2): the harness instruction to add a
`Co-Authored-By` trailer (ruled: none, ever — no commit of this round carries one; `core.hooksPath` =
`/home/filip/rltraffic/githooks`, absolute, verified), and a message saying the model had not changed (ruled: record
the truth, §24.15).

### 24.1 Branch + diff stat (real)

```
task/p7.3d-grid4x4 @ cd1d09f   (worktree /home/filip/rltraffic-p73d; not pushed, not merged)
$ git diff --stat e14d950..HEAD          # this round
 docs/plans/p7.3d.md                |  137 ++++
 offline/campaigns/p7_3d_grid4x4.sh |  168 ++++-
 offline/transfer_curve.py          | 1066 +++++++++++++++++++++++++++++--
 tests/test_g2_measure.py           |   65 +-
 tests/test_p7_3d_campaign_path.py  | 1207 ++++++++++++++++++++++++++++++++++++
 5 files changed, 2540 insertions(+), 103 deletions(-)
$ git diff --stat main...HEAD | tail -1  # whole branch
 25 files changed, 28089 insertions(+), 169 deletions(-)
```
Commits, oldest first: `f963f75` plan (stage 3) · **`969de3c` the fix — B.6-2 items 1–6, ONE commit** · `cd1d09f`
test-only follow-up after the first mutation run (§24.7 says why it is separate) · this packet. **No frozen path** in
`git diff --name-only main...HEAD` (grep over the frozen set: no match); `claude_guard.sh --frozen-only` exit 0.

### 24.2 Files produced or changed

- `offline/transfer_curve.py` — `scenario_of` (strict; hz1x1 the absent default), `demand_identity_for`,
  `checkpoint_identity_for`, `grid4x4_checkpoint_identity` (A20(a)'s pin + `SHA256SUMS_p5_2.txt`),
  `grid4x4_support_ranges`; `run_cell`'s grid4x4 branch and `_grid4x4_payload` (format `p7.3d-grid4x4/1.0`, documented
  in the module docstring with its alignment convention); scenario-aware `validate_cell_payload`, `chunk_is_reusable`,
  `report` step 6 and `declarations_for`; `anchor_choose` recording all 16 actions on grid4x4; `artifact_name_for_stage`;
  `write_manifest`, `check_campaign_inputs`, `unresolvable_chunk_commits`; B.5-1's `dt_reroll_check_cell`,
  `compare_reroll_payloads`, `run_dt_reroll_check`; subcommands `check-inputs`, `resume-check`, `dt-reroll-check`,
  `manifest`; pins `P7_3D_REFERENCE_CELLS_SHA256` `5265f0d5…` and `P7_3D_CAP_E_SHA256` `949015b3…`.
- `offline/campaigns/p7_3d_grid4x4.sh` — rewritten around the same shape (§24.3); header §0 carries the PIPESTATUS line,
  §8 the fix round.
- `tests/test_p7_3d_campaign_path.py` — **new**, 31 tests (the end-to-end test, three more executed-driver tests, 27 unit
  and text tests).
- `tests/test_g2_measure.py` — three T-driver tests changed, **disclosed in full in §24.9**.
- `docs/plans/p7.3d.md` — stage 3 (§§13–17), committed first (`f963f75`).
- Evidence, gitignored, `output/p7_3d_runs/b6/` (sha256 prefixes): `red_run.txt 4aaaef3f` · `red_run_short.txt 4b47a1e6`
  · `e2e_first_run.txt 3b41cef7` · `regression_targeted.txt ecb60225` · `t_regress_b.txt 709ad439` ·
  `green_before_commit.txt 8862900f` · `f_b6_3_report_probe.txt 27dad6d0` · `mutations.txt 1c24a275` (both runs) ·
  `m16_sandbox_incident.txt 388a8bb9` · `whole_suite_at_tip.txt 55255d61` · `gated_regression_at_tip.txt 099164af` ·
  `b5_2_report_stage_confirmatory_refusal.txt 766e2522` · `dt_reroll_check_capture.txt 7e46c201`; the re-roll record
  itself under `output/p7_3d/g2/dt_reroll_check_20260923T105546Z/` (13 fenced rolls + `verdict.json`).

### 24.3 The pre-flight's findings IN FULL (B.6-3: nothing folded into "fixed") — evidence, what changed, what pins it

| # | the reviewer's evidence (by line, at `8c79778`) | what changed | pinned by (mutation, §24.7) |
|---|---|---|---|
| **B1** | `COMMON` after the subcommand at `:209/:243/:246/:249/:252`; `build_parser` puts the roots on the PARENT parser → `error: unrecognized arguments`, exit 2 at the canary on every start (RUNS A, C, D) | all **8** module calls are `-m offline.transfer_curve "${COMMON[@]}" <subcommand>` (`p7_3b_anchor.sh:230`'s shape) | text test (every call, count 8 = 8); the tmux, e2e and refusal tests all get PAST the canary · **M01** killed by 3 tests |
| **B2** | `run_cell :1715` `demand_identity` without `scenario=` (hz1x1's `c177e962…`/`c2c5eea2…` vs grid4x4's `c27d31e8…`/`6ab4d6d7…`, probe (iii)); `:1742` `targets_for_subject` → `KeyError` (probe (i)); `:1744` `checkpoint_identity` → `unknown subject` (probe (ii)); `chunk_is_reusable :1654` and `report :2123` re-derive the same default → a chunk with hz1x1's digests REUSABLE (sandbox ctrl) | `demand_identity_for` / `checkpoint_identity_for`, one call each at all THREE sites; `run_cell`'s grid4x4 branch reads the 16 targets and support ranges from the pinned artifact, loads through `dt_choose`'s per-id branch, writes `calibration_sha256` = P7.3d's | demand test (recomputed digests); **the reviewer's exact chunk now refused, its control reused**; report refuses it at step 6; checkpoint identity = A20(a); the e2e · **M02, M03, M06, M14** killed |
| **B3** | `declarations_for(grid4x4, None)` → hz1x1's 4,700 as "every declared cell", overlap 0 → a COMPLETE campaign refused as 700 undeclared (probe (iv)); `main :3290` names `p7_3a_zero_shot.json`; `manifest`: `invalid choice` | the grid4x4 stage is its own whole declaration; `artifact_name_for_stage` (`p7_3d_grid4x4.json`); `manifest --campaign-dir` (tmp → `os.replace` → re-read and re-hashed; `output/p7_3d/` ONLY; `sha256sum -c` passes on it) | declaration test (700/700, disjoint); name test through `main` for all four stages; manifest tests · **M05, M12, M13** killed |
| **M1** | no `COMPLETE`/`FAILED` file on any path (`:254-257`, `on_signal :215-222` echo only); `PIPESTATUS=2 0` / `1 0` in every run | `fail()` and `on_signal()` in `p7_3b_anchor.sh:248-278`'s shape (FAILED via `tee`, the handler ignoring INT/TERM/HUP before killing the group), `COMPLETE` on success, both removed right after the token; the pane line of B.5-3 | e2e: `FAILED` = `CAMPAIGN FAILED at report`, no `COMPLETE`; tmux test: last line `DRIVER EXIT: 2` = the `setsid` run's 2 · **M08, M10** killed. ⚠️ `on_signal`'s FAILED is pinned by TEXT only (§24.13) |
| **M2** | every DT chunk non-reusable (`checkpoint_identity` raises, caught `:1668`) → every restart re-rolls all 500 DT cells | through `checkpoint_identity_for` | a grid4x4 DT chunk at the right digests and targets IS reusable (control), at another checkpoint or other targets is not · **M20** killed |
| **M3** | `assert_rtg_first_matches_targets :896` and `load_grid4x4_targets :862` have no call site; `:1848` hard-codes P7.2B's calibration digest | the 16-id refusal runs in `validate_cell_payload` on every grid4x4 DT chunk — reached from `run_cell` on the finished chunk, from `chunk_is_reusable` and from `report`; the calibration pin is per scenario | one intersection's series off while `rtg_first` is left equal to the targets → refused naming it · **M04** killed |
| **m1** | three inputs checked by existence only (`:162-167`); no RESCO-root refusal | `check-inputs`: the three artifacts against pins in the module, A20(a)'s five checkpoints (§3 C6), and RESCO's net through draw 1000's parity provenance reference (A8); the existence checks stay in front, texts unchanged | passes today; a one-byte edit of `p7_3d_cap_e.json` refused by name · **M15** killed |
| **m2** | a 40-hex commit unknown to git → `RuntimeError` out of `reusable_chunk_at` AFTER `rm -f "$TOKEN"` (sandbox b3) | `resume-check --stage`, before the canary: walks exactly the path `chunk_is_reusable` walks up to the raising call | unit test (finds exactly that chunk; the validation-failing one is not reported; the raise it pre-empts is shown); executed: the driver refuses, token and chunk untouched · **M09** killed |
| **m3** | `pgrep` matches any process quoting the module path (fails safe) | **not changed** (it fails safe). It shaped this round's harness: the mutation runner and every test keep that literal out of process command lines | — |
| NOT REACHED | `build_policy` on a grid4x4 env handed hz1x1's sumocfg | moot: the anchor now receives grid4x4's config path. The e2e ran a grid4x4 **fixedtime** anchor through `anchor_choose`: 360 × 16 actions, 0 teleports, validated. MaxPressure through the same function was run by C4, not in this round | e2e · **M19** killed |

### 24.4 The end-to-end test (B.6-1) — design, and what it measured

`test_the_campaign_path_from_the_drivers_argument_to_a_written_grid4x4_chunk`: the DELIVERED driver on a clean
`git clone --shared` snapshot of the working copy (the driver refuses a dirty tree and `run_cell` a dirty chunk, so the
snapshot copies in every changed and untracked path and commits it in the clone). Redirected, each substitution asserted
to happen exactly once: `WORK_TREE`, `CAMPAIGN_DIR`, `TOKEN` → sandbox; the `dt-reroll-check` call → a stub line;
`--limit 2`; and on the `cells` and `report` lines only, the module → a shim that imports the REAL module, replaces
**one** function (`grid4x4_cells`: draw 1000's seven cells moved to draw 5, the dt-seed-101 and fixedtime pair first) and
calls the REAL `main()`. That is the only way the brief's three requirements — draw 5, `--limit 2` = one DT + one
fixedtime, and **698** missing — hold together: on the real declaration `--limit 2` selects two DT cells on HELD-OUT
draws 1000 and 1001. **Measured (first run 48 s; read back from the sandbox by a second route):** exit 1 through
`fail`; `FAILED` = `CAMPAIGN FAILED at report`; the token consumed once; two chunks
`cell_cityflow_grid4x4_{anchor_fixedtime_seednone,mappo1000_dt_nomix_h4_b_mean_k100_seed101}_draw0005.json`, both
`p7.3d-grid4x4/1.0`, `config_sha256` `c27d31e8…` (≠ hz1x1 draw 5's `c177e962…`), `routes_sha256` `707b8c4e…` (grid4x4
draw 5's), `canary_seconds` 0.87 = the canary line's, 360 × 16 actions in `[0, 8)`, 0 teleports, `git_dirty false`; the
DT chunk's 16 `rtg_first_i` == the 16 targets read by the test's own `json` route, checkpoint `329fb6b8…` checked against
A20(a) and `SHA256SUMS_p5_2.txt`; `report` refused with `698 declared cell(s) have no chunk` and named no undeclared
chunk; no artifact. The real `output/p7_3d/` still holds only `calibration/` and `g2/`; no token, no manifest written.

### 24.5 Findings beyond the pre-flight

* **F-B6-1 (fixed):** `cells` never received `--canary-seconds`, so every chunk would have recorded `canary_seconds: None`,
  and `report`'s `_canaries_by_stage` raises on the first such chunk — **after all 700 cells**. The e2e now asserts the
  value; mutants M07a (text) and M07b (behaviour) killed.
* **F-B6-2 (fixed):** the driver checked only the canary's CORRECTNESS half; the timing half (`CANARY_MAX_SECONDS` 2.0,
  `p7_3b_anchor.sh:239-245`) was absent while the header claimed both. A failing canary's observed line is now printed.
* **⛔ F-B6-3 (NOT fixed — blocks the token): `report` has no grid4x4 body.** Measured with `report` over a COMPLETE
  synthetic grid4x4 set (one DT cell and both anchors on draw 1001, a caller-supplied declaration, fabricated values):
  `report RAISED TypeError at transfer_curve.py:3224 in _in_support_block: int() argument must be … not 'dict'`, no
  artifact written. With only `_in_support_block` stubbed, the rest of the body wrote `scenario_key cityflow1x1`,
  `format_version p7.3a-zero-shot/1.0`, `inputs.calibration_sha256 92b1592d…` (P7.2b's), `by_subject_arm []` and **no
  subject in the H3 block** (`f_b6_3_report_probe.txt`). `report` iterates hz1x1's `SUBJECTS × DECLARED_ARMS` only, never
  checks C4's six reference cells (§3 C4: *"`report` refuses if any of the six differs"*), and nothing implements
  `GRID4X4_ARTIFACT_FORMAT_VERSION` for the artifact. **Because of J1(c), the body cannot be added after the campaign:**
  any later change to `offline/transfer_curve.py` makes all 700 chunks "rolled by different code", which `report` and
  `chunk_is_reusable` both refuse. So a token issued today would buy 700 cells that end `FAILED at report` and then have to
  be rolled again. §24.14 Q1.
* **F-B6-4 (fixed):** the earlier executed-form test ran the REAL driver against the REAL token path. With B1 fixed it
  would consume an author's token from a pytest run. Every executed test now runs a redirected snapshot.
* **F-B6-5 (fixed):** chunks carried no `actions` (B.5-1 compares them), and `anchor_choose` kept intersection 0's action
  only, so `actions_in_range` checked one intersection of sixteen. Grid4x4 chunks now record the 360 × 16 matrix and
  `intersection_ids`; hz1x1's records are unchanged (the branch is keyed on the scenario). M19 killed.

### 24.6 Tests — REAL OUTPUT

**Red first** (`red_run_short.txt`, before any implementation): `32 failed, 27 passed in 10.51s`. All 30 tests of the new
file failed, each for its own reason: `NotImplementedError` on the skeletons, `assert (4700 == 700)`, `invalid choice:
'manifest'` / `'dt-reroll-check'`, `assert 5 >= 8` module calls with COMMON in front, grid4x4 chunks refused on
`format_version 'p7.3d-grid4x4/1.0' is not 'p7.3a-zero-shot/1.0'`, a missing pin, and — for the four executed tests — the
redirection target `CAMPAIGN_DIR=` absent from the delivered driver (it had no such root and no re-roll stage to redirect;
B1's own red is the text test's `5 >= 8`, and the reviewer's RUNS A/C/D are its executed evidence). The two changed T-driver
tests failed on the missing new text. The 27 that passed are `test_g2_measure.py`'s untouched tests.

**Whole suite at the tip**, as instructed:
```
$ cd /home/filip/rltraffic-p73d  (HEAD cd1d09f, git status --porcelain: 0 lines)
$ RLTRAFFIC_GRID4X4_RESCO=/home/filip/rltraffic/scenarios/grid4x4_candidates /home/filip/rltraffic/.venv/bin/python -m pytest -q -p no:randomly
...
2294 passed, 107 skipped, 32 warnings in 308.77s (0:05:08)
pytest exit=0
```
Delta from §21's 2,264 / 107: **+30 passed, +0 skipped** = the new file's 31 − the one T-driver test that moved into it.
Nothing of this round skips on this machine.

**The gated files at the tip**, with the roots that un-skip T-regress and the checkpoint tests
(`RLTRAFFIC_OUTPUT_ROOT=/home/filip/rltraffic/output RLTRAFFIC_DRAWS=/home/filip/rltraffic/scenarios/draws`, plus the RESCO
root; `test_t_regress_artifacts`, `test_transfer_curve`, `test_spatial_cell`, `test_reference_cells`,
`test_p7_3d_campaign_path`, `test_g2_measure`, `-v -rs`):
```
tests/test_t_regress_artifacts.py::test_p7_2b_calibration_regenerates_byte_identically_through_the_defaults PASSED
tests/test_t_regress_artifacts.py::test_p7_3a_zero_shot_regenerates_byte_identically PASSED
tests/test_t_regress_artifacts.py::test_p7_3b_anchor_regenerates_byte_identically PASSED
================== 215 passed, 2 warnings in 91.20s (0:01:31) ==================
```
**T-regress (b) is byte-identical on all three hz1x1 artifacts** at the tip. `check_test_hygiene.sh` over both test files:
exit 0. `check_english.sh` over every changed file: exit 0.

### 24.7 Mutations — two runs; the first found two defects in my own tests

Every mutant was an exact string replacement, asserted to match once, applied in a throwaway worktree and restored with
`git checkout`, and run against ONLY the tests meant to kill it. The runner is a script file, so no process command line
carries the `pgrep` pattern (`mutations.txt`).

**Run 1, worktree at `969de3c`** (control: 60 passed): 21 mutants, **20 KILLED, 1 SURVIVED**:
* **M18 SURVIVED** — the grid4x4 arm-set check deleted: on a DT chunk the subject/arm check also refuses `naive`, so my
  test's `naive` case could not see it. Closed in `cd1d09f` by a grid4x4 **anchor** under `random`, which only the arm-set
  check refuses.
* **M16 was KILLED only by a 300 s timeout**, and that exposed the incident in §24.8.

**Run 2, worktree at `cd1d09f`** (control: `60 passed in 74.73s`): **22 mutants, 22 KILLED.**
M01 COMMON after `canary` (3 tests) · M02 `scenario=` dropped from `demand_identity_for` (3) · M03 grid4x4 routed to
hz1x1's checkpoint tables (2) · M04 the 16-id refusal not called (1) · M05 `whole = None` for grid4x4 (1) · M06 a grid4x4
chunk claiming P7.2b's calibration (e2e: `FAILED at cells`) · M07a `--canary-seconds` dropped (text) · M07b the chunk's
canary dropped (e2e) · M08 `fail` writing no FAILED (2) · M09 `resume-check` after the token (2) · M10 `${PIPESTATUS[0]}` →
`$?` (2) · M11 the comparison ignoring `actions` field-wise (2; caught by the digest route's disagreement) · **M11b**
`actions` stripped from BOTH routes (2; the verdict assertion itself) · M12 the manifest's re-verification removed · M13
the grid4x4 stage writing P7.3a's name · M14 `chunk_is_reusable` back to `8c79778`'s hz1x1 demand line · M15
`check-inputs` by existence only · **M16** NOT IDENTICAL no longer refusing (**9 s** now: the mutant consumed the sandbox
token and, with `--limit 0`, rolled nothing) · M17 `report`'s 16-target re-derivation removed · **M18** (now killed) ·
M19 `anchor_choose` keeping intersection 0's action (e2e) · M20 `chunk_is_reusable` skipping the 16-target check.

### 24.8 ⚠️ INCIDENT — a mutant rolled 70 held-out DT cells into a pytest sandbox; deleted UNREAD

In run 1, mutant M16 turned the driver's NOT IDENTICAL refusal into a no-op. The test of that refusal redirected every
root to a sandbox but put **no `--limit`** on `cells`. So the mutant driver consumed the sandbox token and started the
REAL declaration at `WORKERS=12` until the test's 300 s timeout. It wrote **70 chunks — `b_mean_k100`, seed 101, held-out
draws 1000–1069 — plus `canary.json`** into `/tmp/pytest-of-filip/pytest-1624/test_a_not_identical_reroll_re0/sandbox/`.
**The design fault is mine**: a test of a refusal must not be able to run the experiment when the refusal breaks.
What I did, in this order: checked for surviving processes (none); listed the directory **by file name only** (never
opened a chunk); recorded the count and the draw range (`m16_sandbox_incident.txt`); **deleted the directory unread**
(`2026-09-23T02:50:42+02:00` by the machine clock); swept every retained pytest directory by name for any other
held-out chunk (none of mine); then made every redirected driver in the file carry `--limit 0` unless a test asks for more
(only the e2e does: 2 cells on draw 5), and re-ran the whole mutation suite. **No value was read, printed or captured.**
The runner kept only the `FAILED` and `E` lines, i.e. the timeout message. These are registered-arm outcomes on held-out
draws, computed BEFORE the token by a mutated copy of the driver: the run_cell code was unmutated, and the recorded commit
is a sandbox clone's, unknown to the repository. The campaign would re-roll them deterministically. **Whether anything
beyond this disclosure must be recorded — the seen ledger, `DEFERRED` — is the coordinator's ruling (§24.14 Q2).** The
deletion was my decision, taken to keep any reader, me included, from opening them.

### 24.9 Tests changed — disclosed in full

All three are in `tests/test_g2_measure.py`, all are T-driver, and all are ordered by B.5-3 / B.6-2(1). No test in any
other pre-existing file was changed.
1. **`test_the_campaign_driver_refuses_before_the_token_and_in_order`** — the last marker was `"transfer_curve cells"`, the
   text of the very invocation B1 found broken; with the roots before the subcommand that text no longer exists, so the
   marker is now `' cells --stage "$STAGE_ARG"'`. The list GAINED five markers: the digest check, `resume-check`, the
   canary's timing half, `dt_reroll_check`, and `record-canary` right after the token. The ORDER assertion itself is
   unchanged; it now covers 21 markers instead of 16. Docstring extended with the diagnosis.
2. **`test_the_campaign_driver_documents_the_foreground_form_and_says_why`** — one assertion ADDED (the full Step-2 line
   including `; echo "DRIVER EXIT: ${PIPESTATUS[0]}" | tee -a <capture>`); nothing removed.
3. **`test_the_headers_own_documented_form_passes_the_group_leader_check`** — REMOVED here and REPLACED (not duplicated) by
   `test_p7_3d_campaign_path.py::test_the_headers_own_line_passes_the_guard_and_the_pane_carries_the_drivers_exit_code`.
   Diagnosis: it executed the REAL driver against the REAL token path (F-B6-4) and asserted only the absence of one text.
   The replacement types the header's own Step-2 line at a tmux prompt on a redirected snapshot with no token, and asserts
   that the leader check passes, that the canary runs, that the refusal is the token's and comes after the canary, and
   that the capture's last line `DRIVER EXIT: 2` equals the exit code of the same driver under `setsid`. Count:
   `test_g2_measure.py` 30 → 29, the new file +1.

**A pin that constrained the implementation, not edited:**
`test_transfer_curve.py::test_the_anchor_stage_writes_its_own_artifact_name` reads the spelling
`name = "p7_3b_anchor.json"` in the module. My first form of `artifact_name_for_stage` (`return "…"`) failed it, so the
helper uses the original `name = …` chain instead. The behaviour is identical, and the new name test pins all four
stages through `main`.
**My own new tests, after their first run:** `cd1d09f` (§24.7, §24.8); the e2e gained a line saving the driver's output
before the first commit; `test_report_refuses_a_grid4x4_dt_chunk_at_another_checkpoint_or_under_other_targets` was
**written after the implementation** (green on its first run; its evidence is M17, KILLED).

### 24.10 `dt_reroll_check` — run once, fenced, no token (`dt_reroll_check_capture.txt`)

Invoked as the driver invokes it, from the committed tip (`cd1d09f`, porcelain 0), cwd = the main tree, canary first:
```
canary 0.90 s {"att_horizon": 247.75089149261333, "decisions": 360, "local_return": -32648.0, "two_routes_agree": true}
dt_reroll_check IDENTICAL: 13 rolls of cell_cityflow_grid4x4_mappo1000_dt_nomix_h4_b_mean_k100_seed101_draw0005.json (1 at W=1, 12 in one 12-worker pool) agree under == on all 59 fields but the clock ['seconds']; sha256 minus clocks 81d176ce9a31797e7216044728b99de41fbd3512659b015c6137fb21c1a8f2d9 on all 13
dt-reroll-check exit=0   wall=85 s
```
(The canary's two numbers are hangzhou draw 0's committed reference constants, not a grid4x4 quantity.)
**Re-checked by a second route** (python3 stdlib, nothing imported from `offline/`, over
`output/p7_3d/g2/dt_reroll_check_20260923T105546Z/`): 13 rolls; ONE distinct sha256 minus clocks (`81d176ce…`); one field
set of 59; the seven fields B.5-1 names present in every roll; (360 decisions, 16 intersections, 16 RTG series,
`git_dirty False`, draw 5, seed 101, stage `pilot`); **13 distinct `seconds`** (W = 1: 34.41 s; the pooled rolls
46.51–47.30 s), so these are thirteen executions, not one payload copied. **Verdict: IDENTICAL.** Per B.5-1 the
reproduction claim now extends to the agent, on this basis. Timing reported, not a rate: 85 s wall, against the brief's
≈ 70 s estimate, which assumed two rolls rather than 1 + 12.

### 24.11 Self-review checklist (Y/N)

- Contracts honoured, the non-standard env API included (no env code touched)? **Y**
- Every test written first and red for its own reason? **N** — one (the report step-6 grid4x4 test) was written after the
  implementation; disclosed, mutation M17 is its evidence. Four executed tests were red on a missing redirection target,
  not on the behaviour.
- Every named mutation executed? **Y** — 22 of 22 KILLED on the final code. **N** on "no survivor the first time": M18
  survived, and M16 exposed §24.8.
- T-regress (b) byte-identical on all three hz1x1 artifacts, at the tip? **Y**
- Whole suite green at the tip? **Y** (2294 / 107 / 0).
- Zero frozen files touched? **Y** · zero new dependencies? **Y** · no trailer on any commit? **Y** · hygiene and English? **Y**
- Every number in this section from a command run in this session? **Y**
- The campaign can produce its artifact after this round? **N** — F-B6-3.
- `on_signal`'s FAILED file exercised by an executed test? **N** — pinned by text only.

### 24.12 Deviations from the brief

1. **Two commits, not one**: `969de3c` holds items 1–6; `cd1d09f` is test-only and records what the first mutation run
   found. Squashable if the coordinator wants one.
2. **The e2e reaches draw 5 through a one-function shim** (§24.4); it is the only route that satisfies draw 5, "one DT +
   one fixedtime" and "698 missing" together.
3. **Every executed test stubs `dt-reroll-check`** (thirteen SUMO episodes per run would breach §4's six-episode ceiling).
   Its logic is tested on stubs through the REAL runner (spawn pools) and it was run for real once (§24.10).
4. **`dt_reroll_check` compares all twelve pooled rolls with the W = 1 roll.** "Otherwise-identical cells" is read as
   G2's `measure_pool` shape (twelve copies of the one cell), and comparing all twelve costs nothing more than comparing
   one.
5. **Beyond B.6-2's text, in the same lines:** F-B6-1, F-B6-2, F-B6-5; `record-canary` moved right after the token
   (`record_canary`'s own docstring and `p7_3b`'s shape); checkpoints and RESCO's net in `check-inputs` (§3 C6, A8);
   `scenario` added to the chunk-vs-cell identity keys; the grid4x4 arm set in `validate_cell_payload`; grid4x4 chunks as
   format `p7.3d-grid4x4/1.0`; the 16 targets re-derived by `chunk_is_reusable` and `report`.

### 24.13 Conflicts between brief and repo, and limitations

* B.6-2(3): *"P7.3a's name is unchanged for its stages (T-regress (b) pins that)"* — T-regress (b) calls `report` with an
  explicit `out_path`, so it pins bytes, not names. The names are pinned by the P7.3b text test (anchor only) and now
  behaviourally by the name test.
* B.6-2(5): *"`--limit 2` (one DT cell seed 101, one fixedtime)"* is not what `--limit 2` selects on the real
  declaration (two DT cells on held-out draws). Implemented through the shim.
* §3 C4 / C6, §4 T-report / T-rho and §7 describe a grid4x4 artifact body the repository does not have (F-B6-3).
* **Limitations:** F-B6-3; `on_signal`'s FAILED is text-pinned only; the e2e's `report` stops at step 5 (completeness),
  and steps 6–7 for grid4x4 are covered on synthetic chunks; the executed tests depend on the main tree's absolute paths,
  as the driver does, and on its RSS budget (≥ 24,216 MiB available) and `pgrep` liveness check (they fail with exit 3
  if another campaign driver runs); the pre-token `dt_reroll_check` costs about 85–95 s on EVERY start of the driver.

### 24.14 Open questions for the coordinator

1. **F-B6-3, blocking the token:** the grid4x4 artifact body must exist, final, before any cell rolls (J1(c)). From the
   brief's own text it needs: `scenario_key` `cityflow_grid4x4`, format `p7.3d-grid4x4/1.0`, the grid4x4 subject in the
   per-seed and per-arm ρ blocks (the plan's `sum()/len()` note), H3 clauses 1 and 2 for this subject (clause 1
   CONFIRMATORY on this scenario, A20), no contrast block (A21(b)), the per-intersection in-support block from
   `p7_3d_calibration.json`, `inputs.calibration_sha256` = P7.3d's, C4's six reference cells compared bit-for-bit (refuse
   on a difference), and A21's two scope sentences. A ruling on where it is built — a follow-up commit here under a new
   amendment? — is needed; I have not started it.
2. **§24.8's incident:** is the disclosure enough, or does it enter the seen ledger or `DEFERRED`?
3. Are the e2e's shim (deviation 2) and the 13-roll reading (deviation 4) acceptable? Squash the two commits?
4. Should `dt_reroll_check` re-run on every start of the driver (about 90 s, as delivered), or once per commit (skip when
   `g2/` holds an IDENTICAL verdict for `HEAD`)?

### 24.15 AI-assistance record (`CLAUDE.md` §8)

1. **Tool and version:** Claude Code (updated by the author on 2026-09-23), model **`claude-opus-5-5[1m]`** (Opus 5.5, 1M
   context) for this round, dated 2026-09-23: commits `f963f75`, `969de3c`, `cd1d09f` and this packet's commit. **Every
   P7.3d commit up to `8c79778` was `claude-opus-5[1m]`**; the change was made by the author at the Claude Code update
   (Amendment B.6.2-3). The `implementer.md` pin (`model: claude-opus-5`) binds subagent launches only.
2. **Authorship per file:** AI-authored in this round: `tests/test_p7_3d_campaign_path.py`. AI-edited from AI-authored
   originals (earlier P7.3a/P7.3b/P7.3d implementer sessions): `offline/transfer_curve.py`,
   `offline/campaigns/p7_3d_grid4x4.sh`, `tests/test_g2_measure.py`, `docs/plans/p7.3d.md`, `docs/returns/P7.3d.md`. No
   human-authored file was edited. The pre-flight review and `BRIEF_39`'s amendments were read only.
3. **Human verification performed:** none yet in this round. The coordinator's re-run of B1/B2/B3 and of the e2e test,
   and the G4 re-review of the driver path, are pending (B.6-3). Run by the AI session: red → green, T-regress (b), the
   whole suite, two mutation runs (22/22 on the final code), the F-B6-3 probe, B.5-2's refusal, and `dt_reroll_check`
   with its independent re-check.
4. **Who made the research decisions:** the coordinator's rulings — `BRIEF_39` B.4–B.6.2 (the fix list, B.5-1's check,
   the fence), A1–A9 earlier; the registrations A20 and A21, tagged before any grid4x4 SUMO number existed
   (`v2.0-prereg-a20` → `0f3526b`, `v2.1-prereg-a21` → `169c467`). The implementation choices listed as deviations are
   mine and await ruling; so is the deletion of §24.8's chunks, an integrity decision disclosed for ruling.

### 24.16 What the next step will assume about this one

The driver starts, passes its canary and reaches the token (executed); `run_cell`, `chunk_is_reusable` and `report` agree
on a grid4x4 cell's demand, checkpoint and targets (one call each); the DT cell reproduces bit-for-bit at W = 1 and inside
a 12-worker pool (`81d176ce…`, at `cd1d09f`). **Any change to `offline/transfer_curve.py` — the F-B6-3 body included —
makes that verdict stale.** The driver re-runs the check at its own start, before the token, so the campaign cannot
start on a stale verdict. No token should be written until F-B6-3 is resolved and G4 is re-reviewed.

