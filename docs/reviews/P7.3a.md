# MERGE REVIEWS — P7.3a (branch `task/p7.3a-zero-shot`, tip `8945530`)

Two sequential reviewers, as `BRIEF_37` §5.5 requires: **A** the code and its mutations, **B** the artifact and the packet recomputed from the raw cells. Saved by the coordinator from each reviewer's report; both worked read-only in their own throwaway trees and wrote nothing outside their scratch directories.

---

## Reviewer A — code and mutations (2026-09-17/18)

**VERDICT: PASS-WITH-FINDINGS — 0 blocking · 5 major · 5 minor. Every finding is a MISSING PIN, not a defect in the delivered numbers; for each, the reviewer checked the artifact to see whether that channel could have fired in this run, and none did.**

Measured by the reviewer: whole suite **2,080 passed / 94 skipped**, exit 0; `tests/test_transfer_curve.py` 105 passed with **zero skips** (the real-SUMO T5 and the executed-driver test both ran); frozen guard, English and hygiene all 0; `git diff --name-only main...8945530` outside `offline/ tests/ docs/` empty — no frozen file, no new dependency. **39 mutations**, 27 killed, 10 survived, 2 equivalent, each diffed, run, restored and the tree confirmed clean.

Independent verifications the reviewer performed rather than taking from the packet: **A17(f) re-run against the real 100-draw corpus → `A17(f) 100/100`**, artifact sha `92b1592d…`, draw 201 `-23938.0 == -23938.0`, `engine_seed_drawn 437485271` equal; **every one of the 4,700 cells records `engine_seed_drawn 437485271`**, the probe's own drawn seed; B2's citation equals the committed stage-1 digest; `code_changed_since('8bf7eae')` is `[]` at the tip, so the chunks stay reusable and the artifact regenerates; teleports ≠ 0 on 0/4700, vtypes `{('cf_parity',)}`, tt-option `{'-1'}`, halting-checked 47 with 0 disagreements, `rtg_first != target` on 0/4000; and four driver pre-token refusals executed in a redirected copy (no token → exit 2 with nothing created; SIGINT-ignored → exit 2; non-leader → exit 2; bogus stage → exit 2). Contracts C1/C2 clean where this code touches the API.

### Major (all: correct code, no test would notice the defect)
1. **`transfer_curve.py:1104` — the engine seed handed to `horizon_rollout` is pinned by nothing.** `ENGINE_SEED → ENGINE_SEED + 1` leaves 173 tests passing; the chunk records the constant, not the call, so a wrong seed would publish as 1000. *Did not fire*: all 4,700 cells carry the probe's drawn seed.
2. **`transfer_curve.py:925` — the `rtg_first == target_rtg` refusal has no test** (`if False:` → 105 passed). It is the only guard that the prompt took effect. *Did not fire*: 0/4000.
3. **`transfer_curve.py:1746` — `_paired_block`'s ATT key mapping is unpinned** (forcing `att_env` → 105 passed). The registered H3 test row IS the paired comparison. *Did not fire*: the two definitions are genuinely distinct in the artifact.
4. **`transfer_curve.py:1128-1130` — the in-support diagnostic's bounds can be swapped silently** (105 passed). *Did not fire*: the artifact reproduces E4's declared asymmetry (`a_q1.0` 15.5 on `mappo1000` vs 316.1 on `mix50`).
5. **`transfer_curve.py:2067 — `run_stage`'s resume-by-content is unpinned at that level**: replacing it with `path.exists()` — the `p5_3b.sh` `[ -f ]` defect its own docstring cites — leaves 105 passing. Mitigated because `report` re-derives every check, so the consequence is a refusal, not a number. *Did not fire*: both stages ran with `n_reused: 0`.

### Minor
6. the published interval's orientation (`ci95_low`/`ci95_high`) is untested; 7. `random`'s policy seed reaching `build_policy` is untested (*did not fire*: draw 1000's five random cells differ); 8. the driver's `report --stage confirmatory` flag is untested (loud at runtime); 9. the chunk-vs-declaration identity omits `stage`; 10. the corpus carries no `SHA256SUMS` (inherited platform behaviour, already recorded on `main` at `96a001b`; A17(f)'s bit-for-bit returns are the stronger integrity evidence); 11. the T9 family pins one spelling per invariant (a re-spelled token move survives; the literal one dies).

### Process finding
**Amendment K is not in the brief.** Its rulings — the denominator diagnostic, the exactly-zero env-ATT exclusion, artifacts outside the run worktree, the executed dirty-tree test — are implemented and tested, but they live only in the `283276b` commit message and the `PROJECT_PLAN` Decisions Log, so a reader of `BRIEF_37` alone cannot find where the `rho_att_env: null` path was registered. (Coordinator: that follows from the author's standing instruction that findings after the J round go to him as paragraphs and are ruled in the Decisions Log rather than as amendments; a cross-reference is added to the brief.)

### Tests judged load-bearing
T5 (real SUMO episode, kwargs spy on every `act`, the RTG identity re-derived by raw arithmetic and by cumsum, the helper asserted second because it is weaker); T1/T2 in `test_transfer_calibration.py` (one real collection, killed the D2 survivor and an off-by-one seed under the reviewer's own hand); T8's artifact test (per-draw seed mean recomputed by a route that does not touch the aggregation code, exact binary fractions); the executed dirty-tree test (the only thing that kills the `if false` mutant); the AST test forbidding a bare `agent.act`; `test_g1_the_real_checkpoints_match_their_committed_records_today`, run unskipped against the real `output/`.

### Tests judged theatre, or weaker than they read
`test_h7_the_interval_fact_is_not_the_point_estimate_fact` (hand-feeds the interval into `_h3_block`); `test_an_unusable_chunk_is_moved_aside…` (calls `move_aside` directly, pins none of `run_stage`'s decision); `test_run_stage_skips_every_cell…` (every fixture chunk is reusable); the T9 family (text over comment-free source, one spelling per invariant); `test_report_regenerates_byte_identically…` (same process, same inputs, twice).

### Not verified by A, and why
The artifact's arithmetic (B's half, explicitly); `report` and `cells` end-to-end through the driver on real data (absolute limits — no token consumed, the pool never run); the driver's post-token half, verified as text only; J5's timing figures, read from the header; ~600 of 2,880 lines of the main test module read line-by-line, the rest judged by mutation response; the packet grepped for falsifiable claims, not read whole; Amendment K's text, reconstructed from the commit message.

---

## Reviewer B — the artifact and the packet, recomputed from the raw cells (2026-09-18)

**VERDICT: PASS-WITH-FINDINGS — 0 blocking · 3 major (ALL in the packet; none touches a published number) · 9 minor.**

**The numbers are real.** The committed artifact regenerates from the 4,700 chunks with exactly two differing leaves — the commit it records (`8bf7eae` against the reviewer's HEAD) and one machine-local path — and is byte-identical after substituting only those two strings, key order included. Every published quantity was recomputed by the reviewer's own numpy/stdlib route; `offline.transfer_curve` was imported only to enumerate the declared cell set.

Population checks, not samples: per-cell ρ on all 9,400 (subject × arm × definition × draw) values **bitwise identical**; `by_subject_arm` mean/std/ci95/low/high/n over 8 × 2 × 6 to **1.78e-15**; the 40 seed rows likewise; fixed-time exactly 0.0 and MaxPressure exactly 1.0 on every draw, with 8,788 of 9,400 ρ values outside [0, 1] and **nothing clipped**; all **32** paired blocks × 9 fields to 5.68e-14, wins/losses/ties exact; the contrast to 8.88e-16; the in-support totals exactly; the denominator diagnostic exactly, `excluded_draws` empty and 100 of 100 draws used; **B2's 1,200 stage-1 rows with 0 differing**; the cited stage-1 digest identical; the caveat present and byte-identical on both `att_env` H3 clauses and absent from the primary's; all 10 checkpoint digests; all 100 draws' demand digests; `rtg_first == target` on 4,000 of 4,000; A17(f)'s log max |difference| **0.0**; the manifest's 4,710 entries verifying (`sha256sum -c`, exit 0). **The artifact interprets nothing**: no `holds`, no verdict word, the contrast exploratory and never promoted, the third clause named absent.

### Major (packet only)
1. **A REFUSED START IS MISSING FROM THE PACKET.** `output/p7_3a_runs/stage2_driver_capture.txt` records `canary 6.42 s … REFUSING TO START: canary 6.42 s exceeds 2.0 s -- the machine is throttled`, then a second attempt at 0.80 s that took the token. `6.42` occurs 0 times in the packet, which `BRIEF_37` §8 requires to carry *the canary of every run*, and A11 exists to label a rate with the machine state that produced it — a reading 3.2× the threshold about 14 minutes before stage 2's rate was measured is precisely that datum. **Confirmed by the coordinator from the capture file.**
2. **Stage 1's driver clock is absent** (`2907 s` in `stage1_driver_capture.txt`, 0 occurrences in the packet); §6's DoD requires both clocks per stage. Confirmed.
3. **The final-state suite claim has no transcript at the final state**: the packet's only `2080 passed / 94 skipped` output is §17.4's K-round run at `283276b`, before the campaign. The claim is TRUE — reviewer A measured `2080 passed / 94 skipped` at `8945530` independently — but the packet must cite a run at the state it claims. Confirmed.

### Minor
The comparative in §19.2 is reversed (17.83 / 19.12 s are **below** the 37.06 s plan-gate figure, not above); the manifest line arithmetic says 4,709 and *six* logs where the file has 4,710 and seven; 3 of 8 in-support rows are reported; **`actions_in_range` is not published in the artifact** although `BRIEF_37` §3.5 lists it per cell, and it is not re-derivable because the actions are not published either; **the artifact states no alignment convention for `reward_series`**, which starts at 0.0 and omits the last decision's reward (`episode_reward − sum` is never zero: min −302, median −28), against `CLAUDE.md` §3's rule for on-disk formats; `rtg_advanced_every_decision` is `true` on 4,000 of 4,000 while the strict `==` identity holds on 3,901 (worst residual 9.09e-13), so the flag uses a tolerance its description does not state; the artifact carries machine-local absolute paths beside the durable digests; `random`'s ρ is aggregated nowhere in either committed file; and §19.6's prose uses "holds / does NOT hold" where the artifact deliberately does not (recorded as a tension, the packet does add *"No verdict is drawn here."*).

### Not verified by B, and why
The Wilcoxon p-values (no scipy; taken as given, but confirmed non-constant and consistent with the win/loss pattern); the test suite at the final state (would have written into read-only trees — reviewer A ran it instead); literal byte-identity at `8bf7eae` (authorised to detach only at the tip); that the chunks are correct *as simulations* (no simulator run, no cell re-rolled); the 100 collected episodes beyond `a17f.log`; anything in reviewer A's territory. Sampling stated: everything is a population check except checkpoint files hashed on disk (4 of 10; the rest matched via chunk fields and the committed pins).
