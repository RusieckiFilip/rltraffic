# PRE-FLIGHT REVIEW — P5.3b (`task/p5.3b-nortg-campaign` @ `39a4eef`)

**Date:** 2026-08-27 · **Reviewer:** `contract-reviewer`, fresh context, read-only
**Verdict: CLEAR TO RUN — no blockers, 6 MAJOR, 8 MINOR.** ⚠️ **The coordinator has nonetheless HELD
the campaign pending a short fix round (`BRIEF_30` AMENDMENT C); see the ruling there.**

⭐ **The first run of the PRE-FLIGHT REVIEW step, ruled into `PROJECT_PLAN` §7 the same day. It paid
for itself: `M5` would have written a knowably FALSE `git_commit` into the artifact's
`measurement_git_commits`, and no later gate would have caught it.**

**Scope, as ruled:** *can this run destroy or corrupt anything*, and *does the resume logic do what it
claims*. Statistics, scoring and analysis explicitly out — no numbers exist yet.

---

## Method

Read `output/p5_3b/run_campaign.sh`, `offline/nortg_campaign.py` (1658 lines), `offline/dt_gate.py`'s
diff, the plan and the brief with Amendments A/B. **Ran into a `/tmp` sandbox:** 64 tests, a full real
`gate1` (300 CityFlow episodes, ~10 min), a real `train`, a real `evaluate`, two `probe`s and a full
`report`. **Snapshotted all 827 files under `output/` by mtime+size before starting; `diff` after every
run: IDENTICAL.**

## BLOCKERS — none

**No path writes, moves, renames or deletes anything in any merged campaign directory, `datasets_v11/`
or `scenarios/draws/`.** Ten writes and one deletion in the whole campaign, enumerated with line
numbers; nine of ten are fenced by `assert_writable` and eight of ten are atomic.
**The fence is real, not stated** (`nortg_campaign.py:273-292`): it `.resolve()`s first and refuses
`output/p4_6`, `p4_dt/dt_seed101.pt`, `output/checkpoints`, `p5_3a`, the relative form and
`output/p5_3b/../p4_6/x.json`, **while allowing `output/p5_3b/checkpoints/` — both the prefix trap and
the bare-component trap are handled.** `--checkpoint-dir output/p4_6/checkpoints train` refuses in
8.1 s, **before any gradient step and before any write.**

## MAJOR

- **M5 🚨 THE ONE THAT DECIDED THE RULING — four existing chunks carry a `git_commit` that could not
  have produced one of them.** All four record `f115b7ce…`, but `eval_mappo1000_seed101.json` contains
  `cell.seed`, added by `39a4eef`. **Coordinator-verified: `f115b7c` defines `nortg_cell_record` ZERO
  times, `39a4eef` once, and the chunk's mtime 22:27:08 sits between the two commits (21:40:31 /
  22:31:32).** The chunk was written by a **dirty tree**, `runtime_provenance` (`dt_gate.py:636`) has
  **no dirtiness check**, and the resume would carry `f115b7c` into `measurement_git_commits` — the
  exact field `DEFERRED` 39 exists to make trustworthy.
- **M1 — `_run_probe` does not verify the checkpoint digest, though `_run_evaluate` does** (`:1475`
  vs `:1526-1563`). Demonstrated: a chunk pointing at the wrong seed's file → `evaluate` **refused**,
  `probe` **exited 0** and wrote its chunk. Gate 3 certifies arm validity for a file the campaign
  cannot prove is the one it evaluated.
- **M2 — the `probe` stage does not ENFORCE Gate 3; `report` does, two stages later.** `_run_probe`
  computes `worst = max(flip_rate)`, **prints it, and writes the chunk regardless.** Demonstrated
  against the committed *conditioned* checkpoint: `rtg_mode conditioned  max flip_rate 0.004722`,
  **exit 0.** ⚠️ **And because the driver skips a tier whose `probe_$TIER.json` exists, a bad probe
  chunk is CACHED and reused on every restart** — the only signal arrives at the end.
- **M3 — no chunk is self-describing.** `_run_evaluate` checks the seed but never
  `training["tier"] == args.tier`. Demonstrated: `train_mappo1000.json` copied to `train_mix50.json`
  → evaluated a `mappo1000` checkpoint and wrote `arm: dt_nortg@mix50`, exit 0, all downstream
  assertions passing.
- **M4 — a fail-fast in the evaluation fan-out orphans the other four jobs.** `run_campaign.sh:86-95`
  `exit 1`s on the first failure while background children keep running and writing. **An immediate
  restart gives 8–10 concurrent evaluations on one GPU**, outside the script's own stated protocol.
  No corruption (writes are atomic and per-seed); the load-independence claim does not survive.
- **M6 — no post-run re-verification of the reused manifests**, which `BRIEF_30` §8 and the plan §9
  both require. The script writes `SHA256SUMS_p5_3b.txt` and never runs `sha256sum -c`.
  **Baseline measured for the operator: all nine manifests verify, 0 failures.**

## MINOR

Checkpoint writes are not atomic (`dt_gate.py:837`), bounded because a mid-train kill leaves no chunk
· one unfenced write (`:1644`, the git-tracked artifact) · the fence covers directories, not siblings
(`output/SHA256SUMS_*.txt` is writable; no live path reaches it) · `$WORK/COMPLETE` is never cleared
at start · `probe_cell` ignores `--draws-root` (same value here only because the driver `cd`s) ·
`tests/test_p5_3b_artifact.py` is red until the artifact exists, by design ·
⚠️ **the plan §9 disclosure is FACTUALLY WRONG** — it claims CityFlow rollouts rewrite
`output/roadnet.json` and `output/replay.txt`; **coordinator-verified: all 100 held-out draw configs
carry `saveReplay: False` and `replayLogFile: None`**, and the reviewer's real rollouts left both
mtimes untouched. *(Not a contradiction of P5.3a's reviewer, who observed the **suite** — which uses
different configs — rewriting them. Both measurements are right about different things.)*

## Verified positively — the resume claim holds for every state constructible

⭐ **P5.2's O4 defect is NOT repeated.** `_run_train` trains all five seeds and writes the **complete**
column once, atomically, after the loop. **No read-modify-write anywhere in the campaign.**
**A partial file never counts as done:** SIGKILL mid-`write_json_atomic` left the target
**byte-identical**, the leak named `.p4-*.json.tmp` and not matched by the resume glob.
**Every bad chunk failed loudly before the expensive work:** truncated JSON, missing seed, truncated
`.pt`, digest mismatch. **Validate-then-write holds at Gate 2** — a failing control wrote no
`control.json`, so a resume re-runs it rather than caching a failure. **Gate 1b cannot "compare
nothing"**: a shortened re-roll was refused — *"100 committed episodes, not 3; 'found no differences'
must never be 'compared nothing'"*. **No hour-2 typo**: every flag and subcommand matches
`build_parser`, every downstream path resolves, and the whole `report` stage ran green on a
structurally complete chunk set. **Gate 2's committed side matches the constants exactly** — 8-key
config, no `rtg_mode`, digest equal to `CONTROL_COMMITTED_DIGEST`. Frozen guard exit 0.

## What the reviewer could NOT verify

Gate 2's real 40,000-step digest reproduction (the campaign's own first ~4 minutes, fails cheap) ·
retrain determinism after an interrupted training · five-way concurrent evaluation under real load ·
whether the other three chunks came from a clean tree (**no behavioural tell exists for them**) ·
tmux/SIGHUP semantics for M4 · full-disk behaviour · the full suite (only the three P5.3b files were
run) · **anything scientific — out of scope by construction.**
