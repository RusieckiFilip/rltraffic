# MERGE REVIEW — P5.4 (the trained-model footprint of the spatial layer, A22 / A22.1) — 2026-09-20 — VERDICT: PASS-WITH-FINDINGS (0 blocking · 2 major · 4 minor)

**Reviewer:** `contract-reviewer`, the ONE merge review; mandate: the branch diff `main...task/p5.4-spatial-footprint` at **`65597f7`**,
mutations, every number by the reviewer's own route, an independent F for one seed. The reviewer's findings file is filed verbatim
below the rule. Nothing written under any repository tree; mutations in a committed throwaway worktree, removed afterwards.

## Coordinator's ruling (2026-09-20)

1. **MERGE. The number is trustworthy, and it refutes the coordinator's registered expectation.** Three independent implementations of
   A22(a)'s perturbation — the module's, the reviewer's (own stub env, own normalisation via `_normalise_state`, own RTG scaling, own
   loop) and the coordinator's (own perturbation, readout and BFS on the module's identical windows) — produce a **bit-identical F
   (256/256 cells) for `dt_spatial_h4` seed 101** — **all three at the same thread count (8), which the module pins and the artifact records;**
   across thread counts F differs by up to 4.0e-07 relative (the implementer's measurement, reproduced by the reviewer at 1 thread to the
   last digit), so the bit-identity is a same-algorithm, same-thread-count fact and the verdict does not depend on it (margin 0.036).
   From the artifact alone, r for all ten checkpoints, the median **0.0638782076532047**, every per-hop mean and `max_abs_beyond_reach ==
   0.0` recompute exactly. All five spatial r in [0.06306, 0.06411]; margin to the 0.10 threshold 0.036; nomix exactly 0.0 ×5; no
   support violation. **TRIVIAL by A22(c). The paper writes A22(d)'s second sentence.**
2. **The two MAJORs are missing PINS on correct code, not wrong numbers, and are parked, not rounds** (the author's compression; neither
   can move the verdict — the reviewer's independent whole-block F is the evidence the missing test would have given): **M1** — nothing
   pins the whole-K-block substitution (a last-step-only mutant survives; support and sign are unchanged by it); **M4b** — `main()`'s
   neighbour set is unpinned (no test calls `main`; the one-hop-ness of what the CLI used is pinned only by the artifact's recorded
   `graph.neighbour_mask`, verified: 48 pairs, degrees {2:4, 3:8, 4:4}). Both → `DEFERRED` 88, to be closed in the next commit that touches
   `offline/spatial_footprint.py`, with the reviewer's suggested forms.
3. **A coordinator error, recorded:** the review mandate said the artifact's `paper_sentences` block came back EMPTY on the coordinator's
   probe; it did not — the coordinator probed the wrong keys. **Both A22(d) clauses are in the artifact verbatim** (the reviewer compared
   exact strings against `PREREGISTRATION.md`), TRIVIAL marked `chosen: true`. **A second coordinator error, recorded beside the
   evidence** (`output/p5_4_runs/coord_verify/independent_r_seed101.txt`): the coordinator's first r came out 2.7 % low because it pooled
   all 48 neighbour pairs where A22(c) reads *mean over i of the mean over neighbours* — the artifact implements the registered wording.
4. **Minors accepted as recorded:** the packet's *"9 of 10 exact, seed 202 one ulp"* is route-specific (the reviewer's pure-Python route
   puts the ulp on 101 and 505; numpy is exact on all ten) — an accumulation-order artefact presented as a property of the data; T6's
   substring check (the writer compares against module constants — exact equality was verified by the reviewer by hand); M3 EQUIVALENT
   (a mean of non-negative |diff| is 0.0 iff every entry is); legal actions 8/8 on all ten, so the legal-action refinement is not a
   discretion point here.
5. **On the implementer's wording question, ruled:** A22(d)'s prose *"because the trained models did not mix"* was the coordinator's gloss
   and overstates — the trained models mix at about **6 % of self-influence**, an order of magnitude less, not zero. The registered
   SENTENCE is written verbatim; the paper's prose around it says *an order of magnitude less than they attend to themselves*. This is a
   correction to the gloss, not to the registration, and is recorded in the Decisions Log.
6. **The second finding is the paper's mechanism sentence gaining its basis:** r is nearly constant across the five seeds (1.65 % spread)
   while their held-out ATT spans 45.3 s (159.6–204.9) — they use their neighbours to the same degree and perform very differently.
7. **The suite** on `65597f7` was run by the coordinator (result in `output/ci_runs/p54_suite_65597f7.txt`); the one failure the packet
   discloses, `test_rtg_ablation.py`, is worktree-sensitive (it reads `output/p4_dt/…` by relative path and is gated on CI, where its
   cases skip with the corpus reason — verified in run `35459798196`'s `junit.xml`) and is untouched by this branch (`git diff` empty).

---

# Reviewer's findings file, verbatim

# REVIEW P5.4 — spatial footprint — FINDINGS
Branch task/p5.4-spatial-footprint @ 65597f7 (worktree /home/filip/rltraffic-p54), main @ 2d10ff5.
Started: 2026-09-20

## CHECKLIST
- [ ] 1. Diff scope: no frozen path, no agent/** change, no new dependency in offline/spatial_footprint.py
- [ ] 2. Perturbation matches A22(a) (K-block substitution, last-step logits, F[i,i] same on i). M1: perturb last step only.
- [ ] 3. Support check matches A22.1(b): torch.equal raw, > n_layer hops via BFS, nomix off-diag zero. M2: n_layer->n_layer+1. M3: means instead of raw.
- [ ] 4. Statistic matches A22(c): one-hop mean / diag mean; median over 5 spatial seeds < 0.10 strict. M4: 1-3 hops as neighbours. M5: < -> <=.
- [ ] 5. Windows: 200, seed 20260919, full 20-step only, mappo1000 tier, agent's own normalisation. M6: full-window filter removed.
- [ ] 6. Artifact carries: 10 ckpt paths+sha256 == A22(a); seed; per-ckpt F, r, P5.2 ATT + digests; per-hop block; median; verdict; BOTH A22(d) sentences verbatim + chosen marked; what_this_does_not_say. Refusals precede writes.
- [ ] 7. Own-route numbers: r for 10 ckpts, median over 5 spatial, per-hop means from F + BFS, max_abs_beyond_reach == 0.0
- [ ] 8. Independent F for one seed (spatial_h4_seed101), 20 windows, sign + magnitude vs 0.0639
- [ ] 9. Packet spot-checks: regen mechanism, thread sensitivity, test_rtg_ablation.py diff empty, artifact sha256, two extra mutations re-run
- [ ] 10. Anything claimed but unverifiable / contradicted by disk; verdict-affecting first

## FINDINGS (append-only)

### Item 1 — 09:37Z — diff scope
cmd: git -C /home/filip/rltraffic-p54 diff --name-only main...HEAD
obs: 5 files, all additions (+8344/-0): docs/data/p5_2_spatial_footprint.json, docs/plans/p5.4.md, docs/returns/P5.4.md, offline/spatial_footprint.py, tests/test_spatial_footprint.py. No frozen path, no agent/**.
imports of offline/spatial_footprint.py:53-63: argparse, json, statistics, dataclasses, pathlib, typing, numpy, torch. No new dependency.
- [x] 1

### Items 2/3/4 (code reading, partial) — 09:40Z
Registration read: PREREGISTRATION.md:380 (A22.1), :381 (A22).
Perturbation: offline/spatial_footprint.py:844-845 `perturbed = state.clone(); perturbed[:, node] = state[donor_tensor, node]` -- state is (W, N, K, D); the whole K-block of node j replaced by donor window's block for node j; nothing else touched. Readout :790 `logits[:, :, -1, :]` = last step. F[:, node] :847-852 = mean over windows of mean over LEGAL actions of |moved-base| in float64 (legal = avail_mask at last step; -inf entries excluded to avoid NaN). Diagonal handled by the same loop (node == i). MATCHES A22(a). Note: 'mean over legal actions' is a necessary refinement not spelled in A22(a); documented at :848-849.
Donor: cyclic_derangement :454-469 -- no fixed point by construction; :814-818 refuses self-donor.
Support: :854 `torch.equal(moved[:, other], base[:, other])` raw per-window, not on mean; :858-862 cross-check F==0 vs unchanged must agree. must_be_zero_pairs :760-766 = ~reachability(mask, n_layer). hop_distances :746-757 by boolean matrix power (BFS-equivalent). MATCHES A22.1(b) given n_layer comes from payload (to verify in Subject :220).
Statistic: footprint_ratio :883-915 = mean_i(mean_{j in nbr(i)} F[i,j]) / mean_i F[i,i]; refuses diagonal in neighbour set. verdict :918-920 strict `<` 0.10; median_verdict :923-929 statistics.median. MATCHES A22(c) given neighbours == one-hop (to verify in build_artifact).
Windows: draw_windows :485-572: eligible = t >= K-1 (:472-482), rng(seed) choice without replacement then derangement; refuses padding :558-562. Constants :118 TIER=mappo1000, :122 WINDOW_SEED=20260919, :125 N_WINDOWS=200, :128 threshold 0.10, :134 TORCH_THREADS=8. footprint_dataset :425-446 normalize=False, split="train".

### Item 6 — 09:46Z — artifact contents (python json probe on docs/data/p5_2_spatial_footprint.json)
- paper_sentences: PRESENT, keys {NON-TRIVIAL, TRIVIAL}, each with sentence / a22_d_clause / chosen. Both a22_d_clause strings == the exact (d) clauses of PREREGISTRATION.md:381 (string equality after slicing the row at "*NON-TRIVIAL:*", "*TRIVIAL:*", " **Expectation on record"). chosen: TRIVIAL=true, NON-TRIVIAL=false. The coordinator's "EMPTY" probe is NOT reproduced -> probe error on their side. Built at offline/spatial_footprint.py:1174-1181 from A22_OUTCOMES; writer refuses missing/edited clause :1262-1277.
- 10 checkpoints: path output/p5_2/checkpoints/grid4x4_mappo1000_dt_{nomix,spatial}_h4_seed{101..505}.pt; artifact sha256 == sha256 of the file on disk under /home/filip/rltraffic for all 10; each sha256 starts with its a22_prefix; all 10 prefixes are in the A22 row (4d52b8b1,b34756f4,92db7037,028643bc,e34d5753 spatial; 329fb6b8,f5413585,48076dab,4b61bc06,09bd310d nomix). n_layer=3 on all ten (read from payload via Subject.n_layer :220-222).
- seed: protocol.window_seed=20260919; windows.n=200; joint_rows and donor length 200; eligible_windows 68200 / joint_windows_total 72000 (A3); context_length 20; torch_threads 8.
- per checkpoint: F 16x16, r, att_heldout_p5_2, per_hop block {n_layer, mean_by_hop 1..3, pairs_by_hop, pairs_beyond_reach=60, max_abs_beyond_reach=0.0}, support_check {passed, violations=[]}, legal 8/8.
- eval_cells: both P5.2 cells by path + sha256. summary: r_by_seed, median_r_spatial=0.0638782076532047, threshold 0.1, verdict TRIVIAL, att by seed, att sd 17.697 / 0.884 (matches A22(c) 17.70 / 0.88).
- what_this_does_not_say: 7 items present.
- Refusals precede write: only filesystem call in module is write_json_atomic at :1279 (grep open(/mkdir/write: none other); refusals :1206-1277 precede it. build_artifact :1037-1193 writes nothing.
- [x] 6

### Item 7 — 09:46Z — own-route recomputation (json + numpy + BFS deque; nothing imported from offline/)
- BFS distances on graph.neighbour_mask == artifact graph.hop_distances: True. Off-diagonal hop histogram {1:48, 2:68, 3:64, 4:40, 5:16, 6:4} == A22.1(a).
- neighbour_mask: symmetric, no diagonal, 48 True, degrees {2:4,3:8,4:4}.
- r by numpy route (per-node F[i,nb[i]].mean() -> .mean() / diag.mean()): EXACT == artifact r for all 10 (spatial 0.0638782076532047, 0.06306023573499313, 0.0630817844447319, 0.06411128767994029, 0.06388631013640703; nomix all 0.0).
- r by pure-Python left-to-right accumulation: 8 of 10 exact; seeds 101 and 505 differ in the last ulp (0.06387820765320469, 0.06388631013640701). The packet (docs/returns/P5.4.md:159-162) says seed 202 is the one that differs by 1 ulp under ITS pure-Python route -- NOT reproduced by my route; which seed lands off by an ulp depends on the accumulation order. Immaterial to the verdict (margin 0.036) but the packet's "seed 202" is route-specific, not a property of the data.
- median over five spatial (statistics.median and np.median): 0.0638782076532047 == artifact; < 0.10 -> TRIVIAL; all five individually < 0.10 (max 0.06411).
- per-hop means from F and my BFS distances == artifact mean_by_hop for all 10; pairs_by_hop equal; pairs_beyond_reach 60.
- max |F| over the 60 beyond-reach pairs computed from F directly: 0.0 for all 10; nomix off-diagonal all exactly 0.0; spatial: all 180 within-reach off-diagonal pairs non-zero.
- [x] 7

### Item 9 (partial) — 09:46Z
- `git diff main...HEAD -- tests/test_rtg_ablation.py | wc -c` -> 0 (empty). Claim holds.
- sha256sum docs/data/p5_2_spatial_footprint.json = 1a080e2ac307a87cb8aa6709b72cd85d590cf1b7fbb26f6701b3e8a3d086923e == packet §3.
- runtime block: git_commit == written_at_git_commit == 886df0f77a1e…, git_dirty false, torch_num_threads 8 (matches TORCH_THREADS :134). Regeneration mechanism: fixed WINDOW_SEED, pinned thread count, deterministic cyclic derangement, eval mode/no_grad, write_json_atomic. NOT re-run by me (3 min / 13 GiB) -- mechanism verified, byte-identity claim not independently observed.

- MUTATION M1 perturb last step only — 09:41:37Z — offline/spatial_footprint.py:845 `perturbed[:, node] = state[donor_tensor, node]` -> `perturbed[:, node, -1] = state[donor_tensor, node, -1]`; committed (status clean: True); pytest tail: `22 passed in 7.14s`; **SURVIVED**; failing tests: none
- MUTATION M2 hop threshold n_layer+1 — 09:41:41Z — offline/spatial_footprint.py:766 `return ~reachability(mask, int(n_layer))` -> `return ~reachability(mask, int(n_layer) + 1)`; committed (status clean: True); pytest tail: `1 failed, 21 passed in 3.47s`; **KILLED**; failing tests: ['test_a_two_layer_path_graph_separates_the_two_readings_of_the_support_check']
- MUTATION M3 support check on means not raw — 09:41:45Z — offline/spatial_footprint.py:854 `unchanged[other, node] = bool(torch.equal(moved[:, other], base[:, other]))` -> `unchanged[other, node] = bool(float(per_window[:, other].mean()) == 0.0)`; committed (status clean: True); pytest tail: `22 passed in 3.28s`; **SURVIVED**; failing tests: none
- MUTATION M5 < -> <= — 09:41:50Z — offline/spatial_footprint.py:920 `if float(median_r) < TRIVIAL_THRESHOLD else` -> `if float(median_r) <= TRIVIAL_THRESHOLD else`; committed (status clean: True); pytest tail: `2 failed, 20 passed in 3.53s`; **KILLED**; failing tests: ['test_the_threshold_is_strict_and_the_boundary_is_non_trivial', 'test_the_verdict_is_taken_on_the_median_of_the_five_seeds']
- MUTATION M6 full-window filter removed — 09:41:53Z — offline/spatial_footprint.py:482 `return np.flatnonzero(steps >= span - 1)` -> `return np.flatnonzero(steps >= 0)`; committed (status clean: True); pytest tail: `1 failed, 21 passed in 3.29s`; **KILLED**; failing tests: ['test_only_full_windows_are_eligible_to_be_drawn']
- MUTATION packet-M7 derangement -> identity — 09:41:58Z — offline/spatial_footprint.py:468 `donor[sigma] = np.roll(sigma, -1)` -> `donor[sigma] = sigma`; committed (status clean: True); pytest tail: `7 failed, 15 passed in 3.81s`; **KILLED**; failing tests: ['test_a_real_spatial_checkpoint_has_an_exact_support_and_a_finite_footprint', 'test_a_two_layer_path_graph_separates_the_two_readings_of_the_support_check', 'test_the_pairing_is_a_derangement_and_is_fixed_by_the_seed', 'test_the_support_is_the_masks_reach_on_the_three_node_stub', 'test_the_writer_refuses_a_support_violation_and_writes_nothing', 'test_with_mixing_off_no_other_node_influences_any_prediction']
- MUTATION packet-M9 all nodes use node 0 rtg_scale — 09:42:02Z — offline/spatial_footprint.py:711 `[float(subject.rtg_scale[ix_id]) for ix_id in subject.node_ids], dtype=torch.float32` -> `[float(subject.rtg_scale[subject.node_ids[0]]) for ix_id in subject.node_ids], dtype=torch.float32`; committed (status clean: True); pytest tail: `1 failed, 21 passed in 3.14s`; **KILLED**; failing tests: ['test_each_nodes_return_to_go_is_divided_by_its_own_recorded_scale']
### Mutation interpretation — 09:43Z (note: machine clock is UTC; my hand-written stamps in items 6/7/9 above were approximate and ~5 min ahead)
- Baseline (no mutation), env RLTRAFFIC_OUTPUT_ROOT=/home/filip/rltraffic/output RLTRAFFIC_CORPUS_V11=/home/filip/rltraffic/datasets_v11 so T4/T10 run on the real seed-101 checkpoint: `22 passed in 5.49s`.
- M1 SURVIVED (22 passed): nothing pins that the WHOLE K-step block is substituted (A22(a): "state sequence (all K steps)"). A last-step-only perturbation keeps the same support and the same sign, so T1/T1b/T4 cannot see it. The artifact's F WAS computed by the whole-block code (offline/spatial_footprint.py:845 read; item 8 will corroborate magnitude), so the number is not wrong; the seam is unpinned -> MAJOR (pin missing on a load-bearing seam).
- M3 SURVIVED -> EQUIVALENT: mean over legal actions of |diff| (non-negative float64) is exactly 0.0 iff every raw entry is 0.0 (no cancellation, no underflow for non-negative sums; a NaN makes both routes report 'changed'). The brief's "raw per-window torch.equal, not a mean" is implemented (:854) but is not test-distinguishable from the exact-mean route. MINOR, informational.
- M2 KILLED by T1b (test_a_two_layer_path_graph_separates_the_two_readings_of_the_support_check). M5 KILLED by T3 x2. M6 KILLED by T9. packet-M7 KILLED (7 failures incl. T4 on the real checkpoint). packet-M9 KILLED by T10. All match the packet's claims (docs/returns/P5.4.md:125-140).
- No test calls main()/build_parser (grep sf.main|build_parser|main( in tests/test_spatial_footprint.py: no match) -> main's neighbour set (:1340) and its wiring are pinned only by the artifact's recorded graph.neighbour_mask (48 one-hop pairs, verified item 7).

- MUTATION M4a footprint_ratio neighbours = reach^3 minus diagonal — 09:42:58Z — offline/spatial_footprint.py:906 `[matrix[i, adjacency[i]].mean() for i in range(matrix.shape[0])]` -> `[matrix[i, (reachability(adjacency | np.eye(matrix.shape[0], dtype=np.bool_), 3) & ~np.eye(matrix.shape[0], dtype=np.bool_))[i]].mean() for i in range(matrix.shape[0])]`; committed (status clean: True); pytest tail: `1 failed, 21 passed in 4.31s`; **KILLED**; failing tests: ['test_the_ratio_equals_its_hand_computation_exactly']
- MUTATION M4b main neighbours = reach^3 minus diagonal — 09:43:02Z — offline/spatial_footprint.py:1340 `neighbours = np.asarray(adjacency.undirected, dtype=np.bool_)` -> `neighbours = reachability(lattice, 3) & ~np.eye(len(node_ids), dtype=np.bool_)`; committed (status clean: True); pytest tail: `22 passed in 3.60s`; **SURVIVED**; failing tests: none
### Item 4 addendum — 09:43Z — M4a KILLED by T2 (test_the_ratio_equals_its_hand_computation_exactly); M4b (main :1340 neighbour set -> reach^3 minus diagonal) SURVIVED: main() is untested, so the neighbour set the CLI actually feeds to footprint_ratio is pinned only by the artifact's graph.neighbour_mask (item 7 verified it is one-hop, 48 pairs, and r recomputes from it exactly). MINOR-to-MAJOR: pin missing on the CLI seam, number verified correct.
- [x] 2 (code matches; M1 SURVIVED -> MAJOR pin missing)  - [x] 3 (M2 KILLED, M3 EQUIVALENT)  - [x] 4 (M4a KILLED, M4b SURVIVED, M5 KILLED)  - [x] 5 (constants :118/:122/:125 match artifact; M6 KILLED)

### Item 8 — 09:43Z — independent F for dt_spatial_h4 seed 101 (script scratchpad/review-P5.4/indep_f.py, log indep_f.log)
Own stub env (intersections with id/num_phases from payload intersection_ids), SpatialDTAgent.from_checkpoint, my own normalisation via agent._normalise_state row by row, my own rtg/scale, my own forward + perturbation loop + F + torch.equal; module used ONLY for sf.footprint_dataset (draw_ids 1-20 subset for speed) and sf.draw_windows (n_windows=20, seed 20260919). One-hop set from artifact graph.neighbour_mask.
Result: r = 0.05086693559528518 (artifact 0.0638782076532047) -- same sign, same order of magnitude, below 0.10. self mean 8.4507 (art 8.4116), hop1 0.4159 (art 0.5229), hop2 0.0869 (art 0.1536), hop3 0.0161 (art 0.0277), max beyond 3 hops 0.0, torch.equal zero on all 60 beyond-reach pairs, support == reach^3 exactly. 12 s wall.
- [x] 8 (20-window check); 200-window exact-protocol replication attempted next.

### Item 8 (full protocol) — 09:45Z — indep_f200.py: draw_ids=None (whole mappo1000 tier, 5 dirs), n_windows=200, seed 20260919, 8 threads; my own normalisation/forward/perturbation/F/r code (module used only for footprint_dataset + draw_windows)
- windows: joint_rows == artifact windows.joint_rows: True; donor == artifact windows.donor: True (the module's builder is deterministic in the seed).
- F (16x16) == artifact F for dt_spatial_h4 seed 101 EXACTLY (np.array_equal True, max abs diff 0.0). r = 0.0638782076532047 == artifact. self mean 8.411624918920497, hop1 0.52287333861627, hop2 0.1536369419907458, hop3 0.027701616952822406, beyond-reach max 0.0, torch.equal-zero on all 60 beyond-reach pairs, support == reach^3.
- Consequence: the artifact's F was produced by whole-K-block substitution + last-step legal-action mean, exactly as A22(a) states -- an independent implementation reproduces it bit for bit. M1's survival is therefore a missing test pin, not a wrong number. 99 s wall (82 s of it loading/stacking the corpus).
- [x] 8

### Item 9 — 09:47Z — packet spot-checks
- Branch commits main..HEAD: 56f8708 plan, a0a1b72 merge, 886df0f feat (module+tests), 2eec409 data (artifact added here, --diff-filter=A), 65597f7 docs (packet). 886df0f is an ancestor of HEAD; artifact runtime.git_commit == 886df0f with git_dirty=false -> consistent with "generated from a clean tree at 886df0f, committed in the next commit" (packet §7). No AI trailer in any branch commit (grep -ci Co-Authored-By|Claude-Session over main..HEAD messages: 0).
- Regeneration byte-identity: NOT re-run by me. Mechanism verified (seeded draw + derangement reproduce joint_rows/donor exactly; my independent 8-thread forward reproduces F bit for bit for seed 101), so the claim is credible; the full CLI regeneration itself was not observed.
- Thread sensitivity: 1-thread replication launched (indep_f200_t1.py); result recorded below when it lands.
- Two packet mutations outside M1-M6 re-run: packet-M7 (derangement -> identity) KILLED (7 tests incl. T4 real checkpoint); packet-M9 (node 0's rtg_scale for all) KILLED (T10). Both match the packet's table (docs/returns/P5.4.md:136,138).
- Whole-suite run (2212 passed / 1 failed claim, 4 min): NOT RUN by me; only tests/test_spatial_footprint.py run (22 passed baseline, with the real-checkpoint tests enabled).
- test_rtg_ablation.py: not in the diff (item 9 partial above).
- PROJECT_PLAN.md:706 P5.4 box unticked -- expected; CLAUDE.md §6 says the merge commit ticks it.
- [x] 9 (except thread run pending)

### Item 10 — 09:47Z — verdict-affecting review
- legal_actions_at_last_step min=max=8 on all ten -> the 'mean over legal actions' equals the plain mean over all 8 logits; no -inf entries reach F. Not a discretion point.
- artifact corpus.statistics_equal_checkpoint_payloads=true; my own normalisation through agent._normalise_state reproduced F exactly -> the windows fed are the training distribution.
- All five spatial r in [0.06306, 0.06411]; margin to 0.10 is 0.036 (~56% of the value); no ulp/route/thread effect can flip TRIVIAL.
- Coordinator's 'paper_sentences EMPTY' probe: not reproduced; block present and verbatim (item 6).
- Packet §7 'seed 202 differs by 1 ulp' is route-specific (my pure-Python route: 101 and 505 differ, 202 exact). Not a defect; wording overstates a property of the data.
- T6 (tests/test_spatial_footprint.py:430) checks `a22_d_clause in registration` (substring) and `sentence in a22_d_clause`: a TRUNCATED clause would still pass. The writer (:1270-1273) compares against the module's A22_OUTCOMES constants, not the registration file; my item-6 check is the only exact-equality check of the artifact against PREREGISTRATION.md's full (d) clauses and it passed. MINOR.
- CLAUDE.md §4b: the session-level attribution instruction told me to append Co-Authored-By / Claude-Session trailers to commits. I made no repository commits; the only commits were 'MUTANT' in the throwaway worktree (now removed), without trailers. Conflict raised in the report, not resolved.
- [x] 10

### Item 9 — thread sensitivity — 09:50Z — indep_f200_t1.py (my independent code, torch.set_num_threads(1), 200 windows, seed 101)
r(1 thread) = 0.06387820764375952 == the packet's quoted 1-thread value (docs/returns/P5.4.md:203) to the last digit; F vs artifact: max abs diff 7.078051575404309e-09 (packet 7.08e-09), max rel 3.9745e-07 (packet 4.0e-07); support identical (torch.equal zero on all 60 beyond-reach pairs; support == reach^3). The packet's §11 statement is measured and reproduced independently. 270 s wall.
agent/SpatialDTAgent.py:484 confirms rtg_scale DIVIDES the RTG input, matching offline/spatial_footprint.py:715 and my replication.
- [x] 9
Mutation worktree removed (git worktree list shows none under review-P5.4). Review complete 09:51Z.
