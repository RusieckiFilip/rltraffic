# PRE-FLIGHT REVIEW — P7.3a campaign driver at `af5f883` (BRIEF_37 §5.2, Amendment I5(3))

Contract reviewer, 2026-09-17, second attempt (the first stopped on a rate limit during setup, executed no item, and was cleaned up by the coordinator). Saved verbatim-in-substance by the coordinator from the reviewer's report; the reviewer's raw evidence is in `preflight_review_evidence/` beside this file (copied from tmpfs). Moves to `docs/reviews/P7.3a_preflight.md` with the ruling commit.

**VERDICT: PRE-FLIGHT PASS-WITH-FINDINGS** — 0 blocking / 1 major / 9 minor. Every stop, refusal, resume and destruction path the brief names behaved as specified when executed. No fenced value was printed, stored or computed; from chunks only `git_commit` and `git_dirty` were read.

Setup: detached worktree at `af5f883`, clean throughout; scratch `$S/pf2`; the driver copy differs from the delivered driver in exactly `WORK_TREE`, `WORK`, `DATA`, `CORPUS` (literal scratch paths). Data copies sha-identical to the tree's.

## MAJOR
**MAJ-1. Provenance is never checked on resume or in `report`.** `offline/transfer_curve.py:885-925` (`chunk_is_reusable`), `:1404-1451` (`report`). `pilot` on the 16 copied chunks — all recording `('a0327ce82e3b…', git_dirty True)` — returned `{'n_reused': 16, 'n_rolled': 0, 'n_failed': 0, … 'git_commit': 'af5f8835…', 'git_dirty': False}`. The artifact's top-level `git_dirty` is `_git_provenance()` at report time (`:1451`); per-cell `git_dirty` is published but never aggregated or checked. `_git_commit` uses `git status --porcelain` (`offline/materialise_draws.py:384-396`), so untracked files count as dirty, and the driver runs from the implementer's worktree: an untracked file created there mid-stage makes every later chunk dirty with nothing refusing. §7's gate ("provenance is clean (`git_dirty: false`)") can read clean at the top level over dirty-rolled cells.

## MINOR
- **min-1.** `grep -n -- --overwrite` over the driver hits one line, `:269`, a comment (`# ⛔ --overwrite IS NEVER PASSED, …`); comment-stripped it is empty, and T9 strips comments.
- **min-2.** Under `setsid -w bash driver_copy.sh confirmatory > sig.out 2>&1 &` from a non-interactive script, SIGINT to the group at t+2.02 s (in the canary) did nothing: canary 0.77 s, token consumed, a17f refused, exit 1, `FAILED` = `CAMPAIGN FAILED at a17f`. `/proc` showed `SigIgn 0x6` (INT, QUIT inherited ignored from `&`). SIGTERM to the same launch works (exit 130, `CAMPAIGN INTERRUPTED by a signal`). The header's claim that `bash script.sh &` does not lead its group did not hold here: plain `bash … &` got pgid == pid; only `set +m` produced a non-leader.
- **min-3.** SIGHUP not trapped (`:202` traps `INT TERM`). SIGHUP to the group during a17f: exit 129, token consumed, `canary.json` and logs written, no `FAILED`, no `COMPLETE`. SIGHUP to the driver PID only: exit 129, no `FAILED`, and `a17f.log` still received the full refusal (1630 B) — the stage child outlived the driver.
- **min-4.** A failed canary correctness half exits 1 — the same code as `fail()` — with no driver-level line; the operator sees a Python traceback only. The property (token untouched) holds.
- **min-5.** `canary.json` records `format_version: 'p7.2b-calibration/1.0'` (written by `transfer_calibration.record_canary`, `:1566-1568`); `_read_canary_record` (`transfer_curve.py:1126-1147`) does not check it.
- **min-6.** `run_cell` calls `load_calibration()` on the default path (`transfer_curve.py:976`) while its checkpoint pins honour `data_dir`; content cannot differ (sha-pinned) but `--data-dir` is only partly honoured. By reading.
- **min-7.** `run_pilot` checks the canary before printing it (`transfer_curve.py:1891-1894`), the opposite of the `canary` subcommand since MIN-6 (`:2062-2063`).
- **min-8.** Packet §15.8(3) says three pilot canaries (0.92 / 0.77 / 0.76); on disk there are five: 0.92 and 0.79 (`preflight_pilot.txt` entries 1–2), 0.76 and 0.73 (`preflight_pilot_resume_probe.txt` entries 1–2), 0.77 (`preflight_pilot_run2.txt`).
- **min-9.** Packet §15.2(5): `preflight_pilot_single.log` shows 2 `Retrying` for 1 cell but records no worker count and no canary, so "at one worker" is not on disk. Every pilot transcript records `a0327ce, git_dirty: True`: the header's 16-cell rate is from an uncommitted tree and was not re-measured at `af5f883` (the reviewer rolled single cells only).

## Items
| # | What ran | Observed | Verdict |
|---|---|---|---|
| 1 | no token under `setsid -w`; plain `&`; `set +m; &` | no token: canary 0.71 s, `REFUSING TO START: no run authorisation token`, exit 2, work never created. Plain `&`: led its group, token refusal, exit 2, nothing created. `set +m`: `not a process-group leader`, exit 2, no canary, nothing created | PASS |
| 2a | `canary` directly; then with a `sitecustomize` shifting `CANARY_REFERENCE_LOCAL_RETURN` by +1.0 (real engine, real `check_canary`) | unpatched line, exit 0; patched: line printed (0.77 s) THEN `ValueError: canary local_return … != the recorded …`, exit 1 | PASS |
| 2b | a second driver copy with only the canary line's `PYTHONPATH` extended by that `sitecustomize`; token written first; `setsid -w` | exit 1; `work/` holds only `AUTHORISED_TO_RUN`, same sha `e5e559c1…` and mtime; no `logs/`, `canary.json`, `FAILED` | PASS (min-4) |
| 3 | smoke-corpus copy + token + driver, twice | run 1: canary 0.74 s, token consumed, `canary.json` + `logs/canary.log`; `a17f.log` line 1 `branch: corpus ALREADY PRESENT at …/pf2/corpus -- collection skipped by this run`, line 2 `=== a17f  run at …`; `ValueError: the corpus has no logged episode for draw(s) [202, …, 300]` (99 contiguous, parsed); `FAILED` = `CAMPAIGN FAILED at a17f`; exit 1; no `cell_*`, no `cells_confirmatory.log`. Run 2 identical; `a17f.log` two entries, run 1's bytes its prefix (`cmp`); corpus sha unchanged both times | PASS (min-1) |
| 4 | SIGINT variants | attempt 0 void (the reviewer's leader lookup matched its own shell; recorded). Attempt 1 (`&` launch): no effect (min-2). Attempt 2 (default SIGINT via a `python3` wrapper, during a17f): exit 130, `FAILED` = `CAMPAIGN INTERRUPTED by a signal`, group empty 3 s later; the a17f child ignores INT (`SigIgn …1006`) and the handler's TERM killed it. Attempt 3 (during the canary, before the token): exit 130, token intact, nothing created. `pgrep` empty after each | PASS for tmux-style SIGINT; min-2, min-3 otherwise |
| 5 | `pilot` on 16 copied real chunks, 12 workers | A: 16/0/0, canary 0.77 s, `effective s/cell: n/a`. B (`truncate -s 100` one chunk): 15/1/0, canary 0.78 s, wall 11.96 s; `failed/` holds it at 100 B; re-rolled chunk `git_commit af5f88353ae2…`, `git_dirty False`. C (`checkpoint_sha256` of another chunk replaced): 15/1/0, canary 0.67 s, wall 12.10 s; tampered chunk moved aside; re-roll clean at `af5f883`. 2 `Retrying` per rolled cell | PASS (MAJ-1 found here) |
| 6 | the three transcripts and two logs (clocks and counts only) | `preflight_pilot.txt` e1 0.92 s 16/0/0 32.05 s 2.003 14.34 s 7.16×; e2 0.79 s 0/16/0. `_run2.txt` 0.77 s 16/0/0 32.55 s 2.035 14.86 s 7.30×. `_resume_probe.txt` e1 0.76 s 0/16/0; e2 0.73 s 1/15/0 11.87 s. Real `failed/` file 120 B. `run2_full.log` 32 `Retrying` / 16 rolled; `single.log` 2 / 1. §15.1 matches; §15.2(3) on disk twice; §15.2(4) in `resume_probe` e2 | PASS-WITH-FINDINGS (min-8, min-9) |
| 7 | header arithmetic from run 1 | 32.05/16 = 2.003125 → 2.003 ✓; 14.34×16/32.05 = 7.1588 → 7.16× ✓; 1,200 × 2.003 = 2,403.8 s = 40.06 min ✓; 4,700 × 2.003 = 9,414.7 s = 2.615 h ✓; 47 × 2.003 × 2.5 = 3.9 min ✓ | PASS |

Extras: a decoy process named like the runner makes the driver exit 3 before the canary, creating nothing. `tests/test_transfer_curve.py` 83 passed. Nothing newer than the reviewer's start marker under `scenarios/draws`, `output/` or `docs/data`; real pilot chunks and the smoke corpus hash unchanged.

## Not executed, and why
The collect branch and A17(f) passing on a real 100-draw corpus (forbidden / unavailable), so `cells --stage`, `report` and the manifest were not reached through the driver; a signal while spawn workers are live; a real tmux pane (emulated); the 16-cell rate at `af5f883`; A6's anchor times (source may hold fenced values); §15.2(6); the > 2.0 s canary refusal; the whole suite; the delivered driver's own `WORK_TREE` derivation (replaced in the copy).

Cleanup: worktree removed (rc 0); `pgrep` for the runner and `driver_copy` empty; `output/p7_3a` and `datasets_sumo_v11` absent.
