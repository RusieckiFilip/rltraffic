# REVIEW — P3, the offline dataset loader

**Date:** 2026-08-07 · **Reviewer:** `contract-reviewer`, independent read-only session
**Subject:** `task/p3-dataset-loader` @ `6549c73`+ in `/home/filip/rltraffic-p3`, diff vs `main`
**Commissioned by:** the user, with two extra checks specified by name (§7 P3.4 review gate)

**REVIEWER'S VERDICT: PASS-WITH-FIXES.** Blocking per the reviewer: P3-01, P3-02, P3-03.
**COORDINATOR'S RULING: five blocking, not three** — P3-04 and P3-10 are promoted, reasons below.

**Evidence base:** 31 mutations executed in a `/tmp` scratch tree; 46/46 green in the worktree;
389 passed with corpus, 383 + 6 skipped without; hygiene and English exit 0. The reviewer reports
**no number it did not produce itself this session.**

---

## 0. Harness integrity — checked first, and it corrected a repo document

The reviewer asserted module provenance before trusting any mutation, via a `pytest_configure`
hook that hard-exits unless every `offline.*` module resolves under the scratch root. Every figure
below was produced under that assertion.

It also found that the `sys.meta_path` trap **does not apply to `offline`**. The coordinator then
settled the mechanism directly, because two sessions had given contradictory accounts and one of
them was written into `docs/patches/README.md` as fact:

```
sys.meta_path:  [0] DistutilsMetaFinder  [1] BuiltinImporter  [2] FrozenImporter
                [3] PathFinder           [4] _EditableFinder
editable MAPPING: ['agent','algorithms','envs','experiments','metrics','rewards','states','utils']
  offline covered -> False        agent covered -> True
scratch copy of `agent` wins under cwd        -> /tmp/mp_probe/agent/__init__.py
scratch copy of `agent` wins under PYTHONPATH -> /tmp/mp_probe/agent/__init__.py
```

**`_EditableFinder` is LAST, after `PathFinder`, so it does not outrank `sys.path`, and
`PYTHONPATH` does work.** What P2.6 hit was `sys.path` **ordering** — the repo root ahead of the
scratch dir — not finder precedence. ⚠️ **The false mechanism is written into
`docs/patches/README.md` on `task/p2.6-format-v11`, not on `main`, so it is P2.6's to correct on
its own branch before merge** — recorded here because a wrong mechanism in a README is acted on
later by someone who cannot re-derive it. The defensive rule is unchanged and right either way:
**assert `__file__` after import; never infer which file ran from how you set the path.**

---

## 1. Blocking findings

| id | severity | file | what |
|---|---|---|---|
| **P3-01** | BLOCKER | `offline/dataset.py:609-614` | The **D3 leakage guard has zero test coverage.** Two mutants that let the loader fit normalisation statistics on held-out or nominal data survive the entire suite (`M_stats_fit_on_any_split`, `O_require_stats_fits_lazily`: 40 passed / 6 passed each). The behaviour ships **correct** — a `split="heldout"` dataset does raise on `.stats` and on `ds[0]` — but nothing asserts it. This is the one registered ruling whose enforcement is invisible to the suite, and the failure it guards is test-set leakage in its quietest form. |
| **P3-02** | BLOCKER | `offline/dataset.py:857-864` | **`save_stats` atomicity is untested; a fully non-atomic writer is green.** Replacing `mkstemp` + `os.replace` with a bare `open(dest,"w")` gives 40/6 passed. The existing barrier test covers only type validation and directory non-creation, and its `match=` is satisfied by `open()`'s own `FileNotFoundError`. The filesystem-mutation barrier has shipped broken **twice** in this project (P1 NB2, P2.0). |
| **P3-03** | BLOCKER | `tests/test_offline_dataset.py:642-647` | **`test_mixed_format_versions_are_rejected` cannot fail for its stated reason.** Deleting the guard (`if len(versions) > 1:` → `if False:`) leaves 40/6 passed: the fixture pairs v1.0 with v1.1, v1.1 is unsupported today, so the loader raises anyway and both messages contain `"format version"`. **It gets worse after the merge gate** — once v1.1 is supported the fall-through message *also* matches. The test keeps passing at exactly the moment the guard's only real job begins. |
| **P3-04** | MAJOR → **BLOCKING (coordinator)** | `tests/test_offline_dataset_corpus.py` | **The corpus RTG check certifies far more weakly than the packet claims.** It uses `context_length=8` and inspects only the item at max `t`, so it compares **8 of 360** rows on 20 streams. Under the off-by-one, grid4x4's weakest stream discriminates on **2 of 8 rows** with a minimum per-stream max-abs-Δ of **1.0**. Population scan: **4242/25600 grid4x4 streams (16.6%)** and 186/4800 cologne3 streams are entirely blind on their last-8 window. The check holds **by a property of the data, not by design** — a smaller `context_length`, a different `dirs[0]` or a single-stream check makes it blind. Return Packet line 174 ("whole-array assertion … never a sampled row") is true of the synthetic test and **false of the corpus test**. |
| **P3-10** | MINOR → **BLOCKING (coordinator)** | `offline/dataset.py:252` | **The zero-std guard is load-bearing on the real corpus and untested.** Mutant `AF` (`np.where(std>0,std,1.0)` → `std`) survives 40/6. Measured constant features in real fitted statistics: **191/640 grid4x4 std entries, 11/60 cologne3, 1/25 hz1x1 are exactly 0** — removing the guard makes every normalised state for those features `0/0 = NaN`. The synthetic fixture's states are never constant, so no current test can reach the branch. |

### Why P3-04 and P3-10 are promoted

**P3-04:** the user commissioned this review with the instruction *"if any scenario's margin is
thin, say so; a check that certifies weakly is worse than one that certifies nothing, because it
looks like evidence."* A margin of 2 rows out of 8 at Δ=1.0, surviving on a data property we do
not control, is thin. This is the discriminating-power rule (§7, 2026-08-07) applied to the single
most important test in the project.

**P3-10:** severity should track consequence, not code size. The consequence is **NaN in every
normalised state for 30% of grid4x4's features**, arriving silently at P4 training time. Sixteen
one-line regressions in this project have cost more than any large one.

---

## 2. Check (1) — RTG by falsification, per scenario, with margins

The reviewer applied the shift itself (`out[t] = total` before instead of after `total += r[t]`).
**Red on every scenario**, so the check works — with the caveat above:

| scenario | streams | rows compared | rows differing | weakest stream | min per-stream max abs Δ |
|---|---|---|---|---|---|
| cityflow1x1 | 1 | 8 | 8/8 = 100.0% | 8/8 | 115.0 |
| cityflow_cologne3 | 3 | 24 | 21/24 = 87.5% | 6/8 | 4.0 |
| cityflow_grid4x4 | 16 | 128 | 75/128 = 58.6% | **2/8** | **1.0** |

**Blindness re-measured on the population** (4800 episodes, 32000 streams, 11.52M rows) against
the packet's 138-episode sample:

| scenario | packet (138 eps) | reviewer (4800 eps) |
|---|---|---|
| cityflow1x1 | 0.83 % | 0.84 % |
| cityflow_cologne3 | 25.31 % | 25.65 % |
| cityflow_grid4x4 | **37.63 %** | **34.35 %** |

grid4x4 is off by 3.3 points, and it is the figure used to argue the check is weak (P3-12, MINOR).
The packet **states its sample**, so this is disclosed rather than hidden. `blind streams 0/920`
generalises correctly: **0/32000**. Verified not to appear in `PROJECT_PLAN.md`, so no plan
correction is owed — the packet's own figure is updated to the population value.

## 3. Check (2) — the ID-keying blind spot, on the population

Confirmed, and **stronger than the packet claimed**. All 4800 `.npz` opened:

```
cityflow1x1       ix_ids already SORTED 1600/1600   distinct orders seen: 1
cityflow_cologne3 ix_ids already SORTED 1600/1600   distinct orders seen: 1
cityflow_grid4x4  ix_ids already SORTED 1600/1600   distinct orders seen: 1
```

Positional-keying mutants kill 12 and 11 synthetic tests respectively and **0 corpus tests**.

**P3-05 (MAJOR), coverage stated plainly:** ID keying has **zero coverage from real data** and
rests entirely on one hand-written two-intersection fixture. When a SUMO corpus arrives for C3,
nothing in the suite would notice positional pairing, and the failure mode is **silent
whole-intersection trajectory swapping**. Note the id→array mapping is actually created in
`offline/trajectory_logger.py:262-272`, *outside this diff* — `dataset.py` is a pass-through, and
the fixture tests the composite. **Not fixable within P3**; parked as a C3 prerequisite
(`DEFERRED.md`), because the right regression test needs a corpus whose `ix_ids` are unsorted.

---

## 4. Non-blocking findings — fix before merge, cheap

| id | file | ruling |
|---|---|---|
| P3-06 | `dataset.py:143-147, 386-407` | `flow_draw = -1` corpora are unloadable under every split, and the message talks about draw 0 while rejecting draw −1. Not live (corpus is draws 1–200) but it is exactly the route a nominal-baseline collection takes. **Refuse `-1` explicitly, with a message naming the absent-draw sentinel.** |
| P3-07 | `dataset.py:509-517` | "raise, never skip" is conditional on `draw_ids is None`; with an explicit list, out-of-split episodes in the directory are `continue`d. **No leak** — requested ids are range-checked — but the module docstring states it flatly and is untrue on that path. **Fix the docstring and pin the real behaviour with a test.** |
| P3-08 | `tests/…:598` | `match="0"` discriminates nothing; under mutant `K` the test passed via a *different* error. Violates the 2026-08-07 `match=` ruling. **Make it name the condition.** `check_test_hygiene.sh` exits 0 because TH006 only checks that `match=` is present — a known limit of the grep, not a bypass. |
| P3-11 | `dataset.py:698-701` | RTG quantiles have no second-route check; mutant `AI` (`method="linear"` → `"lower"`) survives. The reviewer recomputed them independently on real data and they **agree exactly**, so the code is right today — but D3 added them precisely so P4.3 need not compute them from a second code path. **Pin them.** |
| P3-09 | Return Packet | Four "validation beyond the brief" claims are status-claims with no effect-test (mutants `AA`, `Z`, `AC`, `AL`, plus `AB`, `AD`, `AE`, `AG`, `AH` all survive). Per "verify by effect, not status", **either test them or stop asserting them.** Ruling: correct the packet wording now; the tests follow in P2.4, whose job this is. |
| P3-12 | Return Packet | Update grid4x4 blindness to the population value **34.35 %**. |
| P3-13 | branch | 4 commits behind `main`, including the two rules several findings apply. **Rebase before merge.** |

---

## 5. What is clean — checked, not assumed

**C6 alignment verified end-to-end through the loader by a route sharing no code with it**, on
`cf_hz1x1__fixedtime` draw 1:

```
r_t == -sum(lane_waiting[t+1])   exact rows: 360/360   (misaligned variant: 32/360)
loader state row t == pre-decision row : True      == POST-step row : False
loader rtg == suffix-sum from t        : True      == suffix from t+1: False
```

Index `t` really holds `(s_t, a_t, r_t)`; observation row `T` is never emitted. Zero frozen-file
edits, zero new dependencies. `.groups` separates cologne3's three shapes and a straddling batch
fails loudly. Determinism genuine (same-seed identical, different-seed different, shuffle ≠
no-shuffle). `avail_mask` all-True reproduced at **32000/32000 streams, 0 excluding the taken
action** — the packet's population claim holds exactly. The float64 RTG accumulation is an
*equivalent* mutant today (integer rewards, |rtg| ≤ ~75k): defensive, not a defect.

## 6. What the reviewer could not verify

The packet's `343 passed` baseline at the merge base (arithmetically consistent with its own
389 − 46, but not run); the v1.1 positive path (known merge gate); multi-worker `DataLoader`
determinism; the memory claims; whether any corpus with draw 0 or held-out draws exists; and
state mean/std pooling order on the real corpus (the `math.fsum` cross-check is fixture-only,
which the packet discloses).

---

## 7. Merge conditions

P3 merges when **P3-01, P3-02, P3-03, P3-04, P3-10** are fixed with each shown red under the
mutation it exists to catch; **P3-06, P3-07, P3-08, P3-11** are corrected; the packet is fixed for
**P3-09 and P3-12**; the branch is rebased (**P3-13**); and the pre-existing gates hold — P2.6
merges first, then the positive v1.1 test.

**The verdict deserves stating precisely, because it is not a criticism of the science.** The
alignment convention and the RTG definition — the two things a wrong answer would silently
corrupt every number in the paper — were verified correct by an independent route on real data.
What failed review is the **test net around five guards**, every one of which currently behaves
correctly and none of which would tell us if it stopped.
