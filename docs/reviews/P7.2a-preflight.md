# PRE-FLIGHT REVIEW — P7.2a (`BRIEF_35`), destruction paths only

**Filed by the coordinator 2026-09-13 from the reviewer's findings file, verbatim below the rule.** Reviewer: `contract-reviewer`, one mandate, 15-minute box (20:34 → 20:43), 13 constructed states under a sandbox tree in the session scratchpad, whole-tree sha256 snapshots before and after each; pytest out of scope by instruction; the real `scenarios/draws/` trees and `output/` never listed or touched. Reviewed commit `ee14f15` (code identical to `b43ac39`) on `task/p7.2a-sumo-draws`.

**Verdict: CLEAR WITH CONDITIONS — blocking 0, major 1 (F1), minor 3 (R1, R2, R3).** The coordinator's ruling is `BRIEF_35` Amendment B: F1 and R3 are fixed with tests before the campaign; R1/R2 are accepted residuals (a double-fault class inherited verbatim from `_commit` on `main`) and are named in the docstring; CLEAR is logged only after the coordinator re-runs the reviewer's item 9a construction against the fixed commit.

---

# PRE-FLIGHT REVIEW — P7.2a (BRIEF_35), destruction paths ONLY
Worktree: /home/filip/rltraffic-p53b  branch task/p7.2a-sumo-draws  HEAD ee14f15
Started: 2026-09-13 (time box 15 min)

## CHECKLIST
[ ] 0. Sandbox built: 4 parents, 4 files each; snapshot `before` taken.
[ ] 1. Normal run `materialise_parity(CFG, [1,2,3,1000], out_root=ROOT)`: every `before` file byte-identical after; exactly 4 new `parity/` dirs with 3 files; no `.staging-*` left under ROOT.
[ ] 2. Re-run: all four `kept`; snapshot identical to after item 1.
[ ] 3. Differing parity: edit `draw_0002/parity/noteleport.sumocfg` (append a byte). Re-run WITHOUT force -> refused, and NOTHING changed anywhere (snapshot). Re-run WITH `force=True` -> only `draw_0002/parity/*` changed; all parent files byte-identical; the old parity dir is not left as an `aside__*` anywhere.
[ ] 4. Interrupted commit: monkeypatch `os.replace` (in `offline.materialise_draws`'s namespace) to raise on its SECOND call, run a fresh parity for draws [1,2,3] in a NEW sandbox root (parents first). Observe: exception propagates; no parent byte changed; no partial `parity/` dir remains for any draw (or state exactly what remains); no `.staging-*` left. Then remove the monkeypatch and re-run: succeeds.
[ ] 5. Interrupted commit with an existing parity dir: in the first root, plant a differing `draw_0003/parity/` file, then `force=True` with `os.replace` raising on the call that moves the NEW dir into place (i.e. after the aside move). Observe: the ORIGINAL `draw_0003/parity/` is restored byte-identically (compare sha256 of all three files against the pre-run snapshot).
[ ] 6. Target-path guard: call `_checked_parity_target` directly with (a) `ROOT/'cityflow1x1'/'draw_0001'` (the draw itself), (b) `ROOT/'cityflow1x1'/'draw_0001'/'parity'/'x'`, (c) a path outside ROOT ending in `/parity`, (d) `ROOT/'cityflow1x1'/'draw_1'/'parity'` (unpadded). All four must raise. Then simulate a wrong plan: call `_commit_parity([("written", built, ROOT/'cityflow1x1'/'draw_0001')], out_root=ROOT)` with any `_BuiltParity`-shaped object -> must raise BEFORE any rename; snapshot unchanged; no `.staging-*` left.
[ ] 7. Foreign subdirectory: create `draw_0001/junk/` and re-run -> refused naming `junk`; snapshot unchanged (the junk dir still there, nothing else). Remove it.
[ ] 8. A6 order: in a fresh root with parents [1,2] only, corrupt `draw_0001/flow.json` (append a byte) and request `materialise_parity(CFG, [1, 2, 201], out_root=...)` -> refused; assert `draw_0201` was NOT created and no `parity/` exists anywhere.
[ ] 9. Wrong out_root shapes: (a) `out_root` = a draw directory itself; (b) `out_root` = an empty temp dir; (c) `out_root` = a path that does not exist. State what each does and whether anything pre-existing changed.
[ ] 10. Worktree refusal: create `fakewt/.git` as a FILE and `fakewt/draws/`; `materialise_parity(CFG, [1], out_root=fakewt/draws)` -> refused; with `allow_worktree=True` -> proceeds. Confirm the real worktree path `/home/filip/rltraffic-p53b/scenarios/draws` WOULD be refused by calling `_linked_worktree_marker` on it (read-only path check, no write).
[ ] 11. Dry run on a root with a mix (existing parity, differing parity, missing parent): snapshot identical before/after; the reported states match the actual situation.
[ ] 12. Leftover staging: create an empty `ROOT/.staging-leftover/` directory, run the phase -> does anything treat it as a scenario or a draw? Does the run still succeed? Is it left alone?
[ ] 13. `verify_p4_3_probe`: OPTIONAL — materialise draw 201 into a fresh root and run `verify_p4_3_probe(CFG, [201], out_root=...)`; confirm it writes nothing under the root (snapshot) and returns a match.

## FINDINGS (appended incrementally)

### 0. Sandbox — 20:35
cmd: `materialise('configs/sim/cityflow1x1.json', [1,2,3,1000], out_root=ROOT)` from /home/filip/rltraffic-p53b (HEAD ee14f15, branch task/p7.2a-sumo-draws confirmed).
observed: 4 parents written (draw_0001/0002/0003/1000), each with exactly 4 files {cityflow.json, flow.json, provenance.json, routes.rou.xml}; snapshot = 21 entries (5 dirs + 16 files), saved to snap_before.json. flow sha256 draw_0001=1ca91035…, draw_1000=40d0b3cb….
[x] 0

### 1. Normal run — 20:36
cmd: `materialise_parity(CFG, [1,2,3,1000], out_root=ROOT)`; whole-tree sha256 diff vs snap_before.
observed: removed=[], changed=[]; added = exactly 4 `draw_NNNN/parity/` dirs x 3 files (noteleport.sumocfg, provenance.json, routes.rou.xml). All 16 parent files byte-identical. `rglob('.staging*')` = [], `rglob('*aside*')` = []. All four results action='written', parent_action='kept'. n_bound == n_vehicles for all four.
[x] 1

### 2. Re-run idempotence — 20:37
cmd: same call again; diff vs snap_after1.
observed: all four (action, parent_action, planned_action) = ('kept','kept','kept'); diff removed=[] added=[] changed=[]; no .staging*.
[x] 2

### 3. Differing parity — 20:41
cmd: appended b'\n' to draw_0002/parity/noteleport.sumocfg; run without force; run with force=True.
observed: (a) FileExistsError "the parity directory .../draw_0002/parity differs (refused): noteleport.sumocfg differs byte-for-byte"; snapshot diff = {} (nothing changed anywhere). (b) force=True: actions [(1,kept),(2,replaced),(3,kept),(1000,kept)]; diff changed = ['cityflow1x1/draw_0002/parity/noteleport.sumocfg'] ONLY; all 16 parent files byte-identical; no .staging*, no *aside* under ROOT. (materialise_draws.py:1431-1443 classification; :1288-1298 aside/replace)
[x] 3

### 4. Interrupted commit, 2nd os.replace raises, fresh root draws4 — 20:41
cmd: materialise(CFG,[1,2,3],out_root=draws4); monkeypatch md.os.replace raising on call #2; materialise_parity(CFG,[1,2,3]).
observed: calls: #1 staged draw_0001__parity->draw_0001/parity OK, #2 draw_0002 raised OSError; exception propagated; diff vs pre-run = {} (draw_0001/parity rolled back via rmtree in outer except :1300-1305); `rglob('parity')` = []; no .staging*. Re-run without patch: all three 'written', changed=[] removed=[] added=12.
[x] 4

### 5. Interrupted commit with existing parity (aside path) — 20:41
cmd: planted extra bytes in draw_0003/parity/routes.rou.xml; force=True for [3] with os.replace raising when src is the staged draw_0003__parity (i.e. after the aside move).
observed: 3 replace calls: parity->aside__draw_0003 (ok), staged->parity (injected OSError), aside->parity (restore, ok). Exception propagated. diff vs pre-run = {}; all three planted draw_0003/parity files sha256-identical to pre-run; no .staging*/aside left. (:1293-1298). Cleanup force run then restored canonical parity.
[x] 5

### 6. Target-path guard — 20:41 (on a copy draws6)
cmd: _checked_parity_target with 7 inputs; _commit_parity([("written", fake _BuiltParity, R6/cityflow1x1/draw_0001)], out_root=R6) with os.replace spied.
observed: (a) draw itself -> ValueError; (b) parity/x -> ValueError; (c) outside root -> ValueError "refusing to write" (from _checked_output_path); (d) draw_1 unpadded -> ValueError; control draw_0001/parity passes; extra: draw_10000 (5 digits) -> ValueError. Wrong plan: ValueError raised at :1267-1268 BEFORE mkdtemp — 0 os.replace calls, snapshot diff {}, no .staging*.
NOTE (minor, not a destruction path): `R6/draw_0001/parity` (no scenario level) PASSES the guard — the guard checks name + parent pattern + containment only, not depth. Unreachable through materialise_parity because parity_dir() always inserts scenario_key; recorded for completeness.
[x] 6

### 7. Foreign subdirectory — 20:38
cmd: mkdir draw_0001/junk; materialise_parity(CFG,[1,2,3,1000]).
observed: ValueError "draw 1: .../draw_0001 holds unexpected subdirectory ['junk']; refusing to derive parity artifacts..." (:1074-1080); snapshot diff {}; junk still present; after rmdir, tree identical to snap_after5.
[x] 7

### 8. A6 order — 20:38 (fresh root draws8)
cmd: parents [1,2]; append b'\n' to draw_0001/flow.json; materialise_parity(CFG,[1,2,201]).
observed: ValueError "draw 1: flow.json does not match the digest in its own provenance (a1e138d8ac71... against 1ca910355fee...)" (:1088-1095, phase 2a runs before phase 1 materialise at :1390); snapshot diff {}; draw_0201 NOT created; rglob('parity') = []; no .staging*.
[x] 8

### 9. Wrong out_root shapes — 20:38 (copies draws9a/9b/9c)
(a) out_root = R/cityflow1x1/draw_0001 (a draw dir itself): NO refusal. Result (1,'written', parent 'written'). Added 10 entries: a NESTED tree `draw_0001/cityflow1x1/draw_0001/{4 parent files, parity/3 files}` INSIDE the pre-existing draw directory. changed=[] removed=[] — no pre-existing byte altered. Follow-up normal run on R refused: "draw 1: .../draw_0001 holds unexpected subdirectory ['cityflow1x1']". => FINDING F1 (see below).
(b) out_root = empty dir: proceeds; creates parent + parity for draw 1 (7 files). Nothing pre-existing to change.
(c) out_root nonexistent: proceeds; creates the root, parent + parity (7 files). Nothing pre-existing to change.
[x] 9

### 10. Worktree refusal — 20:38
cmd: fakewt/.git as FILE, fakewt/draws/; materialise_parity(CFG,[1],out_root=fakewt/draws) with and without allow_worktree.
observed: _linked_worktree_marker(fakewt/draws) -> fakewt/.git; without flag: ValueError "refusing to materialise into .../fakewt/draws: it is inside a LINKED WORKTREE..." and tree empty after; with allow_worktree=True: (1,'written','written'), 7 files. Real paths (read-only): _linked_worktree_marker('/home/filip/rltraffic-p53b/scenarios/draws') -> /home/filip/rltraffic-p53b/.git (is_file True) => WOULD be refused; '/home/filip/rltraffic/scenarios/draws' -> None (main tree, allowed). Note: phase-1 materialise() (:1390) is not given allow_worktree and did NOT refuse under allow_worktree=True, so the parent creation in a worktree is gated only by materialise_parity's own check.
[x] 10

### FINDING F1 (from item 9a) — classification: MAJOR (borderline; loud, not silent)
`materialise_parity` (and phase-1 `materialise`, :1390) accept any `out_root`, including a draw directory. With out_root = <draw_NNNN>, the phase writes `<draw_NNNN>/cityflow1x1/draw_NNNN/...` inside a pre-existing draw. No pre-existing byte is altered or deleted, but the polluted draw then FAILS `_validate_parent_for_parity` (foreign subdirectory, :1074) on every later legitimate run until someone hand-deletes the nested tree — i.e. a later run refuses the draw rather than misreads it. A depth/shape check on out_root (e.g. refuse if out_root or an ancestor matches draw_\d{4}, or if out_root.name == 'parity') would close it. Inherited from materialise() on main (same out_root freedom), so not introduced by this diff; it becomes more consequential now that the tool's purpose is to write INSIDE existing draws.

### 11 (attempt 1, on `cp -r` copy draws11) — 20:41
cmd: cp -r draws draws11; edit draw_0002/parity/noteleport.sumocfg; rmtree draw_0003/parity; materialise_parity(CFG,[1,2,3,5],out_root=draws11,dry_run=True).
observed: FileExistsError from phase-1 materialise() (:1390 -> materialise :902): "draws11/cityflow1x1/draw_0001 differs from the draw that would be materialised there: cityflow.json differs byte-for-byte. Nothing has been written." Cause: the parent cityflow.json embeds the out_root path, so a COPIED tree is not "the same draw" to _existing_conflict (main's code). Sandbox artefact; also a useful observation: even in dry_run, a parent that would not re-render identically is refused before any parity plan is reported. Re-running item 11 on a freshly materialised root.

### 12. Leftover `.staging-leftover/` at ROOT — 20:41
cmd: mkdir ROOT/.staging-leftover; run [1,2,3,1000]; run [1,2,3,1000,4] (forces mkdtemp next to it).
observed: first run all 'kept', diff {}; second run draw 4 'written'/'written', added 9 entries all under draw_0004, changed=[] removed=[]; `.staging-leftover/` untouched (still there, empty) both times; no other .staging* left. Nothing treats it as a scenario or a draw. Removed it afterwards.
[x] 12

### 13. verify_p4_3_probe — 20:41 (fresh root draws13)
cmd: materialise(CFG,[201],out_root=draws13); verify_p4_3_probe(CFG,[201],out_root=draws13).
observed: returned [ProbeCheck(draw_id=201, matches=True, local_return=-20966.0, att_horizon=191.80088251516824, horizon_vehicle_count=116.0, decisions=360, differing=())]; snapshot diff {} — writes nothing under the root.
[x] 13

### 11. Dry run on a mixed root — 20:42 (fresh root draws11b)
cmd: parents+parity for [1,2,3]; append byte to draw_0002/parity/noteleport.sumocfg; rmtree draw_0003/parity; materialise_parity(CFG,[1,2,3,5],dry_run=True) then dry_run=True,force=True.
observed: reported (draw: action/parent_action/planned_action): 1 planned/kept/kept; 2 planned/kept/differs; 3 planned/kept/written; 5 planned/planned/written (n_vehicles=0, parent not there yet). Matches the actual situation exactly. Snapshot diff {} after both dry runs; draw_0005 NOT created; no .staging*. dry+force reports 2 as 'replaced' and still writes nothing.
Explanation of attempt 1: the copied cityflow.json is byte-identical to the original (diff empty) but embeds the root path ("dir": "/home/filip/rltraffic-p53b/scenarios/hangzhou_1x1_bc-tyc_18041610_1h/"), so a fresh render for out_root=draws11 differs and main's _existing_conflict refuses — correct behaviour.
[x] 11

## READ-ONLY OBSERVATIONS (not experimentally exercised — flagged as such)
- R1 (:1300-1305, inherited verbatim from main's _commit): outer rollback does `shutil.rmtree(target, ignore_errors=True)` then `os.replace(aside, target)`. If the rmtree leaves anything behind (permissions, concurrent writer), os.replace onto a non-empty dir raises INSIDE the except handler; remaining asides are not restored and `finally: rmtree(staging_root)` (:1308) then deletes them. Requires a second fault during rollback; not reachable in my sandbox (rmtree never partially fails there). Minor; pre-existing pattern.
- R2 (:1293-1298): if the restore `os.replace(aside, target)` itself fails, the original parity dir sits under staging_root and is deleted by the finally rmtree. Same double-fault class as R1. Minor; pre-existing pattern.
- R3 (item 6f): _checked_parity_target accepts `<root>/draw_0001/parity` (no scenario level). Unreachable via materialise_parity (parity_dir() always inserts scenario_key). Minor.

## SUMMARY OF PRE-EXISTING-BYTE OUTCOMES
Items 1,2,3,4,5,7,8,10,11,12,13: zero pre-existing bytes altered or deleted under any constructed path, including two injected mid-commit faults (one with an existing parity dir, restored sha256-identical). Item 9a: zero bytes altered, but a nested tree is written inside a pre-existing draw when out_root is a draw dir (F1).

VERDICT: CLEAR WITH CONDITIONS — blocking 0, major 1 (F1: out_root shape not guarded; pollutes but never alters a draw; inherited from main), minor 3 (R1, R2, R3)

### Correction to item 11 note — 20:43
The root-path-embedding key(s) in the parent cityflow.json are: flowFile, (the 'dir' key names the scenario directory, not the root). Explanation of the copy refusal unchanged: a copied parent embeds the ORIGINAL root, so a fresh render for a different out_root differs.
