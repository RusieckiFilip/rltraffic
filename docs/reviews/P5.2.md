# Independent review — P5.2 (`task/p5.2-tier-sweep`, `BRIEF_27`)

**Date:** 2026-08-23 · **Reviewer:** `contract-reviewer`, fresh context, read-only
**Branch reviewed:** `8f0085e` · **Verdict: FAIL — 2 blockers, 7 major, 10 minor**

⚠️ **Recorded on disk by the coordinator per `PROJECT_PLAN` §10's rule of 2026-08-14 — *an agent's
findings are not an artifact until they are on disk; a notification is a delivery mechanism, not a
record.* This file is the coordinator's faithful transcription of the reviewer's report; the wording is
condensed, the findings, file:line references and measurements are not.**

**Headline, in the reviewer's words:** *"The science is sound and reproduces exactly; the engineering
record has one undisclosed unmet requirement that caused real data loss, and the task's declared first
result runs through completely untested code."* · *"Neither changes a reported number today. Both would
change one tomorrow."*

**Coordinator's independent confirmation:** BL-1, BL-2, MJ-4, MJ-5 and MJ-6 were each re-verified from
the artifacts before this file was written. Every one reproduced.

---

## Method

Suite run by the reviewer: **1308 passed, 3 skipped in 116.88 s**, threads pinned, corpus env named —
reproducing the packet exactly. Guards, no arguments, full-output counts: **frozen 0 · English 6 ·
hygiene 16, none in a P5.2 file.**

**51 mutants executed** in an isolated `/tmp` symlink sandbox; the reviewed tree was never written.
**32 killed, 19 survived**, one of the survivors being a deliberate no-op control — so the harness was
shown to discriminate.

---

## BLOCKERS

### BL-1 — Amendment O4's REQUIRED fix is absent, undisclosed, and the defect fired, destroying provenance in an un-backed-up tree

`offline/tier_sweep.py:1726-1766` (`_run_train`), `:1825-1876` (`_run_train_baselines`).
`records` holds only the seeds trained **in this invocation**; the per-seed skip at `:1731` skips the
rest; `:1758` writes `"runs": records` — full overwrite, no merge.

`BRIEF_27` **O4** required, in the implementer's next commit: *"merge by `(tier, method, seed)` into any
existing record rather than overwriting it, and test the resume case — write 3 seeds, resume, assert
the file holds 5."* **Not implemented. `O4` appears zero times in the plan and the packet.**

**Measured on disk by the coordinator, 2026-08-23:**

| file | `runs` | should hold |
|---|---|---|
| `training_random_dt_spatial.json` | **1** (seed 202, path `..._seed202_replicate.pt`) | 5 |
| `training_random_dt_nomix.json` | **1** (same shape) | 5 |
| `training_{mappo1000,maxpressure,fixedtime,random}_baselines.json` | **0** each | 20 each |

⚠️ **It is also a J2(b) leak:** `REPLICATE_SUFFIX` covers the checkpoint name and the eval cell but
**not** the training-record path at `:1765`, so phase R's replicate wrote into phase B's file and
destroyed it. The packet's J2 "distinct artifact key" claim is true of two of three artifact classes.
🚨 **`output/` is gitignored with no backup, and the coordinator's own O4 note verified these files
intact on 2026-08-20 (*"baselines carries 20"*). They are not intact now.**
**Untested:** N08 (`"runs": []` unconditionally) and N09 (skip branch always taken) both **SURVIVED**.
**No reported number changes** — §D-2 reads durations from checkpoint mtimes.

### BL-2 — `score_stop_rule` — Q0's `d4`, the task's declared FIRST result — is untested; three mutants survive

`offline/tier_sweep.py:2057-2108`. **Zero references in `tests/test_tier_sweep.py`** (confirmed by the
coordinator: `grep -c` → 0). Only the pure predicate `stop_rule_verdict` is tested.

| mutant | change | result |
|---|---|---|
| M04 | sign flip → `nomix − spatial` | **SURVIVED**, 113 passed |
| M05 | seed filter → `pass` | **SURVIVED**, 113 passed |
| M06 | pairs each draw against the nomix **mean** | **SURVIVED**, 113 passed |

**A sign flip turns `d4 = +27.10, CONTINUE` into `−27.10, STOP` — the campaign would have refused the
ladder and reported a reversal that did not happen.** `_run_stop_rule`'s exit-3 branch is also
untested. **The number is right** — the reviewer recomputed `+27.1004, CI [+23.8150, +30.3858]` by a
separate path — *"but the code is the artifact that gets reused for the next sweep."*

---

## MAJOR

- **MJ-1** — E1's and I1/J1's `d1` composite blocks untested. `:2484-2505`, `:2376-2400`. **M33** (E1
  stops subtracting the published run) and **M32** (sign flip) both **SURVIVED**. `paired_replicate_report`
  itself is well covered (M01/M02/M03 killed). Values independently confirmed correct.
- **MJ-2** — **B4's size match on the TRAINING INPUT is untested.** `:1438-1450`, `:1436`. **M20** (the
  joint stack ignores the selection), **N14** (prompts calibrated on the unmatched set) and **N13** all
  **SURVIVED**. *"With M20 live, the two DT arms would train on 400 episodes while the four baselines
  trained on 200 — the exact confound B4 exists to prevent — and the declaration would still read 3200."*
- **MJ-3** — `bc_top10_perix`'s wiring untested. `:1798`. **N16** (`kept_perix = kept_global`)
  **SURVIVED**: the new arm becomes a copy of the global filter and **Q5 becomes a self-comparison**.
  The arms are demonstrably distinct in the data (−469.69 / −49.16), but both filters keep 320 streams
  **by design**, so the artifact alone cannot distinguish correct-and-different from same-twice.
- **MJ-4** — **The per-seed qualifier does not exist.** No `reverses_on_n_seeds` / `per_seed` anywhere
  in the module (coordinator confirmed: 0). Plan §3 D9 and §6 obligation 10 describe a generator
  qualifier with nothing to remove and no test. **Worse: the docstring claims the protocol is reused
  from `dt_gate._paired`, `wilcoxon_signed_rank` and `offline_baselines.paired_comparison` — none of
  which is imported or called.** No Wilcoxon, no rank-biserial. **It bites exactly where predicted: on
  `random` the per-seed sign reverses on seed 202 (+0.6926 against four negatives), and that qualifier
  reached the packet only because the coordinator computed it by hand.**
- **MJ-5** — **`fixedtime` has two exact ties and no declared tie-break rule**, so three reported
  integers are tie-break dependent. **Coordinator's independent enumeration over all valid orders:
  hard subset `maxpressure` 2..2 · `fixedtime` **2..4** · `random` 3..3.** Q2b's FAILED verdict is
  robust (max 9 < 12) and Q3's *"does not lead"* is robust (rank ≥ 2 always). ⛔ **But the coordinator's
  Gate 2 sentence — *"7 of 18, BELOW the 9 that coin-flipping gives"* — is NOT robust: the range is
  7..9 and 9 is exactly the coin-flip expectation.**
- **MJ-6** — `docs/plans/p5.2.md:861` was explicitly required to be corrected by Amendment P5 and was
  not. It still reads *"PHASE B maxpressure, then random"* — a **two-tier** phase B against a
  **four-tier** registration and a **three-tier** executed loop. Three commits landed after P5 issued.
- **MJ-7** — **G3's required counts do not exist.** The DoD requires the with/without CUBLAS
  comparison *"both counts reported"*; `G3` appears once in the packet, in the AI-assistance record,
  claiming the probe was run. **The two tensor-difference counts appear nowhere.**

## MINOR

**MN-1** Gate 0's aggregator failing branch untested (**M16** survived); the test named for it calls
`_arm_factory` directly, so `assert_factories_constructible` is never made to refuse ·
**MN-2** **N10** survived: `_arm_factory` ignoring `args.replicate` would make the replicate evaluate
phase B's checkpoint — **zero by construction — and `assert_independent_replicate` would not catch it,
because it compares checkpoint FILES, not the model a cell came from. Eval cells record no model
provenance, so J2's chain has no cell→checkpoint link** ·
**MN-3** M34 survived: dropping `seeds_tag` lets single-seed cells overwrite five-seed cells ·
**MN-4** N01/N02/N03 survived: dict-order dependence in `selected_episodes` ·
**MN-5** M09 survived: `ceil`→`floor` equivalent at n=200/node, so C4's registered `ceil(0.10·n)` is
unpinned ·
**MN-6** *"every mutation's failure pasted"* — 3 of 22 pasted; the rest summarised as counts ·
**MN-7** P6's required quote absent; `train_tier_dt:866,895` still uses `time.time()` not
`time.monotonic()` ·
**MN-8** Q3b under-reported: it **also holds on `random`** (`dt_nomix − behaviour = −5.0360`,
CI [−5.5005, −4.5716]) — conservative, but §4.5 requires every testable ordering ·
**MN-9** packet §2 quotes `11 files / 13494 insertions`; actual `12 / 13949` (stat taken 5 lines early) ·
**MN-10** packet says *"the four DT checkpoints"* at `fixedtime`; there are **ten**, all verified
distinct with correct flags and masks.

---

## Verified positively, by independent recomputation

The reviewer recomputed from raw `episodes` arrays with a script importing nothing from `tier_sweep.py`
or `dt_gate.py`, re-deriving CIs by hand:

1. **Q4 — all four values, CIs and per-seed agreement reproduce exactly**, including `random`'s
   reversing seed 202 at +0.6926.
2. **Q3** — ranks 2/6 · 3/6 · 3/6, and 1/6 at the seen `mappo1000`.
3. **Q1** — 15/19, with the identity of all four misses; Q1b anchors 0.0417 % / 0.0906 %.
4. **Q2** — 11 · 8 · 8; first places `bc` / `iql` / `iql`. **Q5** — −469.69 / −0.28 / −49.16.
5. **Gate 1** — 7/7 reused sha256s match at consumption; the re-rolled anchor is identical over
   500/500 shared keys, **0 of 1500 values differ**.
6. `assert-complete` **35/35**, zero `.partial`, 124 checkpoints.
7. **`output/p5_1/` is untouched** — newest mtime `2026-08-18 10:18`, before P5.2 started. **D1(a) held
   in practice as well as in code.**
8. Contracts: the non-standard env API is honoured through `horizon_rollout`; C1 action ordering
   enforced at `SpatialDTAgent.py:525`; **C9 zero occurrences**; no new dependencies.
9. **C6 alignment holds in the corpus**: `current_phase[t+1] == 2·action[t]` exactly.

### ⭐ The `fixedtime` exact zeros — CONFIRMED at a stronger level than the coordinator's check

The reviewer rolled draw 1000 under four arms in CityFlow and diffed the **full 360 × 16 action
matrix**: `dt_nomix` and `dt_spatial` reproduce the fixed-time controller **entry for entry, 0 of 5760
differing**, from two seeds and both architectures, across ten checkpoints with ten distinct digests.
`bc` differs on **5760/5760**.

⭐ **And the `bc` divergence has a checked, non-defective explanation:** `bc` emits the same 8-cycle
**shifted by one step**, because the corpus has exactly **one aliased state row** — `state[0] ==
state[1]` bit-identically (one-hot phase, no `time_in_phase` feature), followed by actions 0 and 1.
**A memoryless policy cannot represent that initial hold; a DT with a 20-step context and a timestep
embedding can, and does.** 336 distinct state rows, exactly 1 ambiguous.

---

## Tests the reviewer judged load-bearing

`test_assert_writable_*` (kills the string-prefix mutant via the `p5_1_backup` sibling) ·
**`test_the_new_trainer_reproduces_the_old_one_exactly_at_one_head` — "the strongest test in the file",
killing four independent alignment mutants** · `test_each_arm_of_the_2x2_gets_the_graph_its_name_promises`
· B3(b)'s three equality-check tests · **`test_comparing_a_checkpoint_with_itself_is_REFUSED_not_reported_as_zero`
— J2(c)'s failing branch, and it fires** · the truncated-cell and missing-replicate refusals ·
`test_a_duplicated_episode_does_not_make_a_short_cell_look_complete` (set identity, not a count) ·
the `episodes_of_seed` pairing tests · **the LR probe tests, with step 249's discriminating power
measured at >1e13 against step 499** · `test_the_registered_constants_appear_in_the_committed_plan` ·
`test_the_declared_ladder_att_matches_the_committed_ladder_artifact` ·
`test_every_behaviour_anchor_this_campaign_PRODUCES_has_a_factory[fixedtime]` · the four CUBLAS tests.

## Tests the reviewer judged theatre

- `test_the_factory_check_REFUSES_an_undeclared_arm` — its docstring claims discriminating power; it
  calls `_arm_factory` directly, so the Gate 0 check it names is never made to refuse (M16 proves it).
- `test_the_per_intersection_filter_keeps_the_same_TOTAL_as_the_global_one` — fixture counts are
  multiples of 10, so `ceil` and `floor` agree and M09 survives.
- The two stop-rule predicate tests — correct, but their **names** read as covering the stop rule while
  the scoring half is untested. *"A reader auditing 'is the stop rule tested?' would answer yes and be
  wrong."*

## What the reviewer could NOT verify

No training was re-run (a 53 h campaign is not re-runnable in review) · obligation 6's byte-identity
verified only through its test · the C1 CUDA control and the +10.3 % cost need a GPU and a fresh run ·
**G3's counts do not exist in any artifact, so there was nothing to check** · **no artifact proves the
`dt_nomix` replicate cell came from the replicate checkpoint** (MN-2) · the `fixedtime` rollout cannot
detect a node-order permutation, since all 16 nodes run in lockstep · no mutation against frozen files,
so `DEFERRED` 37's three kills are taken on report — **though the reviewer confirms its own runs used
no `-k` filter, the defect the implementer disclosed** · whether the `training_*` records were complete
at any intermediate moment, since they are overwritten in place with no history · the scientific
interpretation of Q4, *"which is not mine to adjudicate. I verified the numbers, not the sentence."*
