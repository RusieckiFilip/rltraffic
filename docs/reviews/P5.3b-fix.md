# Independent review — P5.3b-fix (`task/p5.3b-fix-round` @ `8cb41d3`, `BRIEF_33` + Amendments A–D)

**Filed by the coordinator, verbatim, 2026-09-11.** The reviewer may not create findings files; every mutation was restored and `cmp`-verified, both trees clean at exit. **Ruling: `BRIEF_33` Amendment E — fix-first, then merge.**

---

## REVIEW — P5.3b-fix (`BRIEF_33` + Amendments A–D)

**Tree:** `/home/filip/rltraffic-p53b`, `task/p5.3b-fix-round` @ `8cb41d3` (10 commits over `main`; `main` @ `f436c92` has since gained one docs-only commit `d36a268` not on the branch). **Date:** 2026-09-11. **Reviewer:** independent, read-only; every mutation restored with `git checkout -- <file>` and `cmp`-verified against `HEAD`; `git status --porcelain` empty in both trees at exit; the main tree's `output/` was only read.

**VERDICT: PASS-WITH-NOTES** — 0 blocking, 3 major, 9 minor.
**Mutations:** 21 code mutations: **16 KILLED / 5 SURVIVED** (1 of the 5 behaviourally equivalent). 15 data mutations on the shipped artifacts: **11 KILLED / 4 SURVIVED**. 6 pipeline perturbations (items 4, 5 and two probes): **6/6 REFUSED**, artifact absent after every refusal.

**Headline.** Every shipped number I could reach was recomputed by a stdlib-only route importing nothing from `offline/` and agrees to ≤ 3e-14. Three episodes re-rolled through the real `run` path reproduce the committed rows on all 41 non-timing fields, including the `47c0ff33…` digest on a collapsed draw. The three authorised test edits are the only pre-existing assertions changed. What is not right: the embedded `per_arm` block, the zero-deviation refusal and the eight-field spec are each protected by nothing — all three are the "guard exists, nothing pins it" shape this project has already named.

---

### BLOCKING
None.

### MAJOR

**MJ-1 — `comparisons.<tier>.definition_difference_decomposition.per_arm` is asserted by no test; a swapped or shifted per-arm block ships silently.**
`offline/nortg_campaign.py:2016-2045` (`_embedded_decomposition`) has no unit test. Mutating `:2025` to write `dt`'s means under `dt_nortg` and vice versa → **SURVIVED, 176 passed**. Data-side, on the shipped `docs/data/p5_3b_nortg.json`: `per_arm.dt_nortg.clock_origin += 1.0` with `total += 1.0` → **SURVIVED, 178 passed**; `per_arm.dt ↔ per_arm.dt_nortg` swapped on `mix50` → **SURVIVED, 178 passed**. `tests/test_p5_3b_artifact.py:339` checks only that each block's three terms sum to its own `total`, and the packet's §1.1 per-arm table (`−204.9774`, `−11.8063`, …) is exactly such a number. This is BL-1's shape one task later. My independent recomputation confirms the shipped values today (table below); nothing keeps them there. A reader of the paper would take the per-arm clock-origin split as tested when it is not.

**MJ-2 — the `deviation_c1 == deviation_c3c == decomposition_residual == 0.0` refusal (`BRIEF_33` §3.2) is not pinned; test 1's `deviation_*` parameters are killed by a different guard.**
Deleting the zero check at `offline/nortg_decomposition.py:836-838` → **SURVIVED, 176 passed**. `tests/test_nortg_decomposition.py:204` perturbs the *stored* `deviation_c3c` by 1e-9, which route two (`:822-834`, recomputation from constructor fields) catches with a message containing "deviation", so the `match=` regex passes for the wrong reason. I then constructed a *genuinely* non-zero `c3c` (`att_reference_metric_cadence += 0.25`, `deviation_c3c` stored consistently): unmutated code refuses only through the zero check ("1 episodes carry a non-zero deviation"); with M25 applied it would be accepted. The packet found this exact class for `att_engine_call` (§6.3.2, fixed at `tests/test_nortg_decomposition.py:620`) and did not extend it to the three deviations. (A genuine `c1 ≠ 0` is caught by route one — verified — so only `c3c` is exposed.)

**MJ-3 — the eight-field outcome-identity definition (`BRIEF_33` §0, §3.3.1) is unprotected in code and in the artifact.**
Dropping `completed_at_horizon` from `MECHANISM_FIELDS` (`offline/nortg_campaign.py:1754-1763`) → **SURVIVED, 176 passed**: `mechanism_outcome_identity` (`:1771`) has no unit test, and `tests/test_p5_3b_artifact.py:393` recomputes on eight fields but never asserts `shipped["fields"]` equals its own list. Data-side, popping a field from `mechanism.outcome_identity.fields` → **SURVIVED, 178 passed**. On this data the counts coincide on 7 fields, which is precisely why nothing notices. The paper sentence "identical on eight fields" would rest on a list the tests do not read.

### MINOR

- **mn-1** The header-level cell-identity check in `chunk_is_reusable` (`offline/nortg_decomposition.py:491-496`, pre-flight M1 / Amendment C1.2) is unpinned: deleting it → **SURVIVED**; only the per-row check (`:511-516`) is pinned (deleting that → KILLED at `tests/test_nortg_decomposition.py:687`). The M1 test builds chunks whose header *and* rows are for the other cell. A header-wrong/rows-right chunk is skipped by the rows-only predicate and then refused by `assert_chunks_complete`, which reads headers — the livelock C1.2 closed, in one sub-case.
- **mn-2** `BRIEF_33` §3.3's "`registered`, `exploratory`, `found_by` as the first three fields" cannot hold: `write_json_atomic` sorts keys (`offline/dt_gate.py:1135`); on disk `mechanism` begins with `action_identity`. Fields present, order not.
- **mn-3** §3.3(3)'s reading string of the permitted shape ("on N of 5 seeds … on the other seeds it does not collapse (att_engine A, B against dt's C)") is not shipped; there is no `mechanism.reading`. The only reading is `attractor.<tier>.reading` (`offline/nortg_campaign.py:1917`), whose template yields for `mappo1000`: *"the same fixed [0, 0, 0, 0, 0] of 100 draws on mappo1000 emit the byte-identical action sequence … regardless of observation or demand"* — a sentence that reads as a positive claim about a tier carrying the attractor on zero draws.
- **mn-4** `action_counts` histograms (`:1990`) cover `mix50` seeds 101 and 404 only; §3.3(2) asks for "the collapsed cells", i.e. 202 and 505 too.
- **mn-5** `diff_json_paths` (`offline/nortg_decomposition.py:1105`) emits no leaf for an empty list, so a path present only as `[]` in one payload is invisible to it. My walk: 722 differing leaves; the tool: 707 — the 15 are the empty `non_identical_draws` lists; **both report 0 outside the enumerated set**, and the tool exits 0. Also the driver's NEXT STEPS (`offline/campaigns/p5_3b_decomp.sh:258`) still recommends `--allow format_version predictions.Q1 comparisons mechanism runtime` — the blanket prefix the packet §5.2 says it tightened.
- **mn-6** `attractor.mix50.per_cell` and `n_cells_carrying_it` are unpinned (`per_cell.404: 99→98` → SURVIVED), though the same count is pinned through `action_identity.mix50_against_reference` (`404.n_identical: 99→98` → KILLED). Low weight.
- **mn-7** The A13(b)-naming `FileNotFoundError` at `offline/nortg_campaign.py:2662` is untested; disabling it → SURVIVED, but behaviourally equivalent (the following `read_text` raises the same class), so the artifact still cannot be built without the decomposition. Nothing drives `nortg_campaign._run_report` end-to-end in the suite.
- **mn-8** `main` has moved one docs-only commit (`d36a268`) past the branch; the merge must carry it.
- **mn-9** Packet §2 says "9 files changed"; `git diff --stat main...HEAD` shows 10 (the packet is the tenth). Trivial.

**Positive, worth recording:** the decomposition tie now guards BL-1's headline from a clone — `by_definition.att_engine.paired.mean_difference: −409.1450 → −300.0` → **KILLED, 2 failed** (`test_the_decomposition_explains_the_difference_the_artifact_itself_reports`, `test_every_contrast_reports_both_definitions_with_att_engine_primary`). The original review's identical mutation survived 17 tests.

---

### A. MJ-3 (Q1.holds)
`_q1_holds` (`offline/nortg_campaign.py:1598-1611`) tests the `False` branch first, then `True`, else `None` — §2.1 exactly. `Q1_HOLDS_RULE` (`:202`) matches the brief's text modulo the brief's own line wrap. Shipped artifact: `predictions.Q1.holds: null`, `holds_rule` present, both limbs carry `registered_tier`/`as_registered` (`true`/`true`). Test diffs: the only removed/changed pre-existing assertions are `tests/test_p5_3b_artifact.py:258`, `tests/test_nortg_campaign.py:1212`, `tests/test_nortg_campaign.py:932` — no other assertion removed; `:662-731` untouched and passing.

### D. Regeneration
Walked every path between `git show bd36a0a:docs/data/p5_3b_nortg.json` and the shipped file myself: 722 leaves differ; by top level `format_version` 1, `predictions` 6 (`Q1.holds`, `holds_rule`, `{largest,smallest}_limb.{registered_tier,as_registered}`), `comparisons` 69 (all under `<tier>.definition_difference_decomposition`), `mechanism` 643, `runtime` 3 (`git_commit`, `git_dirty`, `written_at_git_commit`). **0 outside §3.5's set; 0 paths removed; the only type change is `Q1.holds` bool→null.** `git_dirty: false`, format `p5.3b-nortg/1.2`.

### E. Mutation ledger (each applied alone; restored byte-identical)
| # | mutation | result | killed by |
|---|---|---|---|
| (1) | `_q1_holds` returns `None` before the `False` branch | **KILLED** | `test_q1_is_False_when_an_evidence_bearing_limb_is_displaced…` |
| (2) | delete `assert_decomposition_embedded(payload)` at `:2712` | **KILLED** | `test_the_report_actually_calls_the_decomposition_guard` |
| (3) | drop `committed_* == source` at `nortg_decomposition.py:731-736` | **KILLED** | `test_a_chunk_that_agrees_with_itself_but_not_with_the_committed_column…` |
| (4) | `term_clock_origin += 1e-9` in a synthetic chunk, via `decomposition_artifact` and via `main report` | **REFUSED ×2**, artifact absent | route one |
| (5) | `att_reference_entered_population += 1e-9`, both routes | **REFUSED ×2**, artifact absent | route two |
| (6) | `chunk_is_reusable` header check deleted / row check deleted | **SURVIVED** / **KILLED** | see mn-1 |
| (7) | `out_dir` unfenced at `:1275` | **KILLED, 4 failed** | `…refuses_to_write_the_artifact_into_another_campaigns_tree[*]` |
| (8) | `MECHANISM_FIELDS` 8→7 | **SURVIVED** | MJ-3 |
| (9) | `CONTRAST_TOLERANCE` 1e-9→1.0 / `DECOMPOSITION_TOLERANCE` 1e-9→1.0 | **KILLED / KILLED** | contrast-refusal test / embedded-block test |
| (10) | `n_distinct_action_sequences = len(digests)` at `:1848` | **KILLED** | `test_the_mechanism_counts_distinct_action_sequences_per_cell` |
| (11) | `"p5_3b"` added to the allow-list at `:155` | **KILLED, 3 failed** | fence tests |
| (12) | `holds_rule` blanked (`"holds_rule": ""`) | **KILLED** | `test_the_artifact_explains_its_own_None…` (my first attempt `"" or (…)` was inert and is not counted) |
| extra | accept 29 cells; recorder returns a copy; `as_registered` always True; route two deleted; `holds` always None; attractor = mode when not open-loop | **all KILLED** | |
| extra | zero-deviation check deleted; `per_arm` labels swapped; A13(b) `FileNotFoundError` disabled | **SURVIVED ×3** | MJ-2, MJ-1, mn-7 |
| data | decomposition JSON one byte; outcome `202.n_identical` 88→87; `n_distinct` 303 97→100; contrast shift; `Q1.holds` null→true; `registered` false→true; `n_draws_carrying_it` 284→283; headline −409→−300; `source.sha256`; `as_registered`; action `404.n_identical` | **11 KILLED** | |
| data | `per_arm` shift; `per_arm` swap; `fields` minus one; `per_cell.404` 99→98 | **4 SURVIVED** | MJ-1, MJ-3, mn-6 |

### F. Frozen set, deps, hygiene, counts
`git diff --name-only main...HEAD`: 10 paths, all under `docs/`, `offline/`, `tests/`. `claude_guard.sh --frozen-only` exit 0. New imports in the diff: stdlib, numpy, pytest, `offline.*`, `agent.utils.utils` only — no new dependency. `check_test_hygiene.sh` on the three new/changed test files: exit 0 (whole-repo run exits 1 on pre-existing TH006 lines in `test_base_traffic_env.py`, `test_cityflow_env_phase_constraints.py` — baseline, not this task). `check_english.sh` on all eight new/changed files: exit 0. Collected: worktree **1772** vs main **1699** (+73); per file 62 / 92 / 24.

### G. The packet's claims
- Full suite, worktree, packet's env: **`1 failed, 1737 passed, 34 skipped in 135.05s`**; the failure is `tests/test_rtg_ablation.py::test_the_spread_table_uses…` (I did not re-run it in the main tree). Three files with env: **178 passed**; without env 176 passed + 2 skipped.
- Manifests: `sha256sum -c` on all 12 — 10/44/125/112/48/221/10/39/61/36/18/111 OK, 0 non-OK, every exit 0, **TOTAL 835**.
- "12.5 min": last `dt_nortg` checkpoint mtime `random_dt_nortg_seed505.pt` **00:55:19**; `eval_mix50_seed*.json` mtimes **01:07:43–01:07:53** (12.4 min); `evaluate_seconds` 287.6–297.7 s. The packet's provenance account holds; `grep "12\.5"` on the driver: exit 1 (removed).
- Seed 303: **97** distinct sequences; repeats `2c9065e7…` {1007,1065}, `965b8a7a…` {1010,1033}, `bcdf1b81…` {1032,1061}; none is the attractor. Amendment D's "100" is wrong, the packet is right.
- Speed-up: smoke 1.930 / 1.997 → mean **1.9635 s/ep**, ratio to 1.85 = **1.0614**; `COMPLETE` = **1883 s**; 1883/3000 = **0.6277**; 1.964/0.6277 = **3.129**. Sum of per-cell seconds 9258.6 → 3.086 s/ep per worker; 3.086/0.628 = 4.91 — the double-count the packet correctly rejects.

### Independent recomputation (stdlib only, nothing imported from `offline/`)
| quantity | recomputed | reported | diff |
|---|---|---|---|
| mix50 Δpopulation (paired per draw) | 11.733663784556402 | 11.733663784556404 | 1.8e-15 |
| mix50 Δclock_origin | 202.16332848008048 | 202.16332848008045 | −2.8e-14 |
| mix50 Δcadence | 0.7722667849940557 | same | 0 |
| mix50 Δtotal | 214.66925904963094 | same | 0 |
| Σterms − (Δ_ours − Δ_engine) from `by_definition`, mix50 | 8.5e-14 (fmean) | identity_gap −5.7e-14 (np) | both < 1e-9 |
| mappo1000 pop / clock / cad / total | −1.4252e-05 / −0.00084020 / −0.0037006 / −0.0045551 | identical | gap 3.0e-15 |
| random, all four | 0.0 | 0.0 | 0 |
| clock / pop / cad share of mix50 total | 94.17 % / 5.47 % / 0.36 % | 94.2 / 5.5 / 0.4 | — |
| `dt@mix50` per arm | −0.0727 / −2.8140 / +4.9991 / +2.1124 | identical | 0 |
| `dt_nortg@mix50` per arm | −11.8063 / −204.9774 / +4.2268 / −212.5569 | identical | 0 |
| admission latency mean, `dt_nortg@mix50` seeds 101…505 | 29.63 / 300.53 / 43.42 / 325.28 / 326.03 s | 29.6 / 300.5 / 43.4 / 325.3 / 326.0 | — |
| per-episode `Σterm == att_ours − att_engine_call` (exact) | 3000/3000 | 3000 | — |
| `att_engine_call == committed`, `att_ours == committed` | 3000/3000 each | 3000 | — |
| `committed_*` == sources (`p5_3b_nortg.json`; 4000 `p8_4b_rederivation` cell files read) | 3000/3000, 0 unsourced | — | — |
| non-zero `c1`/`c3c`/`residual` | 0 | 0 | — |
| outcome identity mix50 vs `dt_nortg@random` 101 | 0/88/0/99/97; non-identical draw lists equal | same | 0 |
| `dt_nortg@random` seed invariance | 100 ×5 | same | 0 |
| action identity mix50 | 0/88/0/99/97; draw lists **equal to the outcome lists** | same | 0 |
| `dt@random` vs `dt_nortg@random` (action) | 100 ×5 | same | 0 |
| `n_distinct_action_sequences`, 30 cells | 30/30 match | — | 0 |
| attractor digest (from `random` rows) | `47c0ff33c38b…` unique; 284 mix50 draws; histogram `[43,48,40,43,43,49,41,53]`, 360 decisions, one histogram across all attractor rows | same | 0 |
| re-roll `dt_nortg@mix50` s404 d1000 / s101 d1000 / `dt@mappo1000` s101 d1000 | 41/41 non-timing fields identical (`att_engine_call` 748.2029784886928 / 98.82956425813569 / 102.08328736900165) | — | only `seconds`, `seconds_rollout` differ |
| decomposition provenance chunk digests vs `SHA256SUMS_p5_3b_decomp.txt` | 30/30 | — | — |
| embedded `source.sha256` vs file on disk | `bc77d39e…` both | — | 0 |

### What I could NOT verify
- The 3,000-episode run itself beyond 3 re-rolled episodes and the 10 smoke records; that the 15 `dt_nortg` checkpoints were trained on the tiers they name; GPU determinism under 5-way concurrency (the 3000/3000 reproduction is the evidence, not an independent check).
- The pre-existing `test_rtg_ablation` failure's "passes in the main tree" classification — not re-run by me.
- The whole-repo hygiene baseline count on `main` vs the branch (my background run had not finished); only the per-file result is observed.
- Any CI behaviour or the skip ceiling; the concurrent-load statement in `runtime.concurrent_load` (a wall clock carries no signature of load, as the artifact itself says).
- The coordinator's pre-review reading in Amendment D (I reproduced its numbers from the artifact, not its process).

Files reviewed: `/home/filip/rltraffic-p53b/offline/nortg_decomposition.py`, `/home/filip/rltraffic-p53b/offline/nortg_campaign.py` (diff), `/home/filip/rltraffic-p53b/offline/campaigns/p5_3b_decomp.sh`, `/home/filip/rltraffic-p53b/tests/test_nortg_decomposition.py`, `/home/filip/rltraffic-p53b/tests/test_nortg_campaign.py` (diff), `/home/filip/rltraffic-p53b/tests/test_p5_3b_artifact.py` (diff), `/home/filip/rltraffic-p53b/docs/data/p5_3b_decomposition.json`, `/home/filip/rltraffic-p53b/docs/data/p5_3b_nortg.json`, `/home/filip/rltraffic-p53b/docs/returns/P5.3b-fix.md`, `/home/filip/rltraffic-p53b/docs/plans/p5.3b-fix.md`. Scratch only under `/tmp/claude-1000/-home-filip-rltraffic/42cbf76f-5d6e-44ba-b684-496d2fe5804d/scratchpad/merge-review/`.
