# Pre-flight review — P5.3b-fix (`BRIEF_33`), the decomposition run

**Filed by the coordinator, verbatim, 2026-09-10.** The reviewer may not create findings files; its tree was clean at exit and both `output/` snapshots were identical before and after. **Ruling: `BRIEF_33` Amendment C.**

---

## PRE-FLIGHT REVIEW — P5.3b-fix (`BRIEF_33`), decomposition run

**Tree:** worktree `/home/filip/rltraffic-p53b`, branch `task/p5.3b-fix-round` @ `3adc45013c9181f5ddb12457237dcd2fa56fe1ea` · **Date:** 2026-09-10 · **Reviewer:** independent, read-only
**Verdict: CLEAR WITH CONDITIONS** — **BLOCKER 0 · MAJOR 5 · MINOR 7**. Every finding below was produced by execution in a sandbox unless marked *(by reading)*.

**Conditions (all operator-side, none needs a code change before the run):**
1. Start the driver from the worktree: `cd /home/filip/rltraffic-p53b` before `bash offline/campaigns/p5_3b_decomp.sh` (M2 — from any other cwd the token is burnt and `FAILED` is written before a single episode).
2. If the run is Ctrl-C'd, wait until `pgrep -f nortg_decomposition` is empty before writing a new token (M3 — the five python cells survive Ctrl-C and keep writing).
3. Know that `output/SHA256SUMS_p5_3b_decomp.txt` will list the two `smoke/` chunks and all 31 logs (m1); do not delete `smoke/` afterwards without re-issuing the manifest.
4. A restart *after* the first successful `report` refuses at the report stage until `docs/data/p5_3b_decomposition.json` is committed (m6) — the driver's NEXT STEPS already say to commit it.

---

### Method

- Read `docs/PROJECT_PLAN.md` §7 pre-flight rule, `BRIEF_33` §3.1/3.2/3.6 + Amendments A/B, the plan §4.1–4.6, the prior `docs/reviews/P5.3b-preflight.md`; then the driver (201 lines), `offline/nortg_decomposition.py` (1290 lines), the test file (736 lines), and the helpers it calls in `nortg_campaign.py`, `dt_gate.py`, `engine_att_reference.py`, `materialise_draws.py`.
- `git -C /home/filip/rltraffic-p53b diff --stat main...HEAD`: 6 files, all under `offline/`, `tests/`, `docs/` — **no frozen file touched**. The `nortg_campaign.py` diff adds **zero** filesystem-mutation calls (grep of added lines).
- Enumerated every mutation verb in the eight modules the run imports; the only writes reachable from this campaign are `work.mkdir` (`nortg_decomposition.py:1188`), `write_json_atomic` (`:1189`, `:1246` → `dt_gate.py:1123-1143`, mkstemp + `os.replace`), and the driver's `rm -f` token (`:102`), `mkdir -p` logs (`:107`), `rm -f FAILED COMPLETE` (`:108`), `tee FAILED/COMPLETE` (`:110`, `:185`), and `> SHA256SUMS_p5_3b_decomp.txt` (`:167`). `draw_config_path` is pure path arithmetic (`materialise_draws.py:384-392`); all three sampled real held-out draw configs (`draw_1000/1050/1099`) carry `saveReplay=False, replayLogFile=None, roadnetLogFile=None`.
- **Sandboxes** (all under `/tmp/claude-1000/-home-filip-rltraffic/42cbf76f-5d6e-44ba-b684-496d2fe5804d/scratchpad/preflight/`): `sb/` fence probes (29 paths, symlinks, `..`, relative, prefix collisions); `drv/` a copy of the driver differing from the real one **only in the three assignment lines** `MAIN=`, `WORK_TREE=`, `PY=` (diff shown, lines 68–70), driven by a stub interpreter and by the real interpreter; `sb_skip/` 15 bad-chunk states pushed through the real `main([... run ...])`; `sb_report/` 11 chunk-set states through the real `main([... report ...])`; `sig/` a Ctrl-C process-group experiment with the real interpreter.
- **Tests:** `.venv/bin/pytest tests/test_nortg_decomposition.py -q` → `42 passed in 1.76s`. The fence tests call `assert_decomposition_writable` directly, the barrier test drives `nortg_decomposition.main([... "report"])`, the skip tests call `chunk_is_reusable` — all production functions, no re-implementations.
- **Snapshot:** `find -printf '%p %s %T@'` over `/home/filip/rltraffic/output` (39,617 entries) and `/home/filip/rltraffic-p53b/output` (76 entries) before the first experiment and after the last: **both diffs IDENTICAL**. The real token file was never created (`absent` at the end); both git trees remain clean.

### BLOCKERS — none

No path in the driver or the module deletes, renames, truncates or overwrites anything in `output/p5_3b/`, `output/p8_4b_rederivation/`, `output/p4_dt/`, `output/p4_6/`, `output/p4_7/`, `datasets_v11/` or `scenarios/draws/`. The re-verification loop (`p5_3b_decomp.sh:172-182`) is `sha256sum -c` only — with a deliberately failing decoy manifest it wrote `FAILED` and exit 1, and the `ls` of `output/` before/after was identical. The barrier in `report` held on every constructed bad input (below) and a **pre-existing artifact at the destination was left byte-identical** on refusal.

### MAJOR

- **M1 — `run` skips a chunk for the WRONG cell under the right filename** (`offline/nortg_decomposition.py:468-487`, `:1151-1155`). Constructed `_chunk("dt_nortg","random",505)` saved as `decomp_dt_mix50_seed101.json` → `main([... run --method dt --tier mix50 --seed 101])` printed `complete and clean, skipping`, returned 0. Same for a wrong-seed chunk (`dt/mix50/202`). The predicate never compares `payload["method"|"tier"|"seed"]` to the invocation. Consequence, demonstrated at `report`: refused as `needs exactly 30 cells ... duplicated/missing`, artifact absent — so it **cannot reach the artifact**, but it is a **livelock**: every restart skips it again and the campaign fails at the end until someone deletes the file by hand. Borderline BLOCKER by the letter of the scope ("skip a bad chunk"); MAJOR by consequence because the barrier catches it. Only reachable by an out-of-band copy (the prior pre-flight's M3 shape).
- **M2 — the driver burns the token and writes `FAILED` before verifying the module is importable; no `cd`** (`p5_3b_decomp.sh:132`, header `:2` says "run from the task WORKTREE" but `:6` gives no `cd`; `p5_3b.sh:56` had `cd "$WORK_TREE"` and this skeleton dropped it). `offline` is importable **only from cwd** (`.venv` has no `offline` on `sys.path`: `cd /tmp && python -c "import offline"` → `ModuleNotFoundError`). Sandbox driver with the **real** interpreter: cwd `/tmp` → all five cell logs end `No module named 'offline'`; cwd `/home/filip/rltraffic` (main tree) → `No module named offline.nortg_decomposition`; both: token deleted, `logs/` created, `FAILED` written, exit 1. No data touched, but one token per mistake.
- **M3 — Ctrl-C does not stop the cells, and there is no lock against a second start.** SIGINT to the process group (what tmux Ctrl-C sends) killed the bash driver (exit 130) while the three real-interpreter children started with `&` **survived, ran to completion and wrote their files** (`sig/done_1..3`). In the real run: five cells keep rolling up to ~5 min after the operator thinks they stopped, no `FAILED` is written, and chunks appear later. A second driver start with a fresh token while a run is in progress was accepted (62 stub invocations = 2 × 31; both printed `CAMPAIGN COMPLETE`). Writes are atomic per cell so nothing corrupts — but the timing record (`timing.per_cell_seconds`, the load statement `_CONCURRENT_LOAD_NOTE` at `:1282`) would be false under a doubled load.
- **M4 — `--out-dir` is not fenced** (`:1124-1127`: the fence is applied to `work_dir` only; `:1246` writes `out_dir / "p5_3b_decomposition.json"`). Sandbox `report --out-dir <root>/output/p5_3b` **wrote `output/p5_3b/p5_3b_decomposition.json`** — into the predecessor's tree the module docstring (`:88`) says it "must never write". It creates a new file only (never overwrites a predecessor file, since none has that name) and the driver hardcodes `--out-dir $WORK_TREE/docs/data` (`:79`), so no live path hits it — but the fence's "every other component under `output/`" guarantee is not true for this path.
- **M5 — a truncated or empty chunk crashes `run` instead of being re-run** (`:1152` `json.loads` runs before the predicate). Half-of-a-valid-chunk → `JSONDecodeError: Unterminated string`; empty file → `JSONDecodeError: Expecting value`. The process exits non-zero, the driver writes `FAILED`, and a restart hits the same crash — the docstring's "a partial ... chunk is re-run and overwritten" (`:474-475`) is false for a *syntactically* partial file. Unreachable by the driver itself (mkstemp + `os.replace` at `dt_gate.py:1136-1140`; the leaked `.p4-*.json.tmp` from a mid-write kill is invisible to both `report` and the manifest's `find -name '*.json'` — verified: `(nothing)`), so it needs an external cause; still a livelock when it happens.

### MINOR

- **m1 — the manifest lists the smoke** (`p5_3b_decomp.sh:166-167`, recursive `find`). Sandbox run: 33 entries = 31 logs + `smoke/decomp_dt_mix50_seed101.json` + `smoke/decomp_dt_nortg_mix50_seed101.json`; the real smoke chunks record `git_commit 5000cc26`, not `3adc450`. Deleting `smoke/` later makes `sha256sum -c` fail on this campaign's own manifest. Also: the manifest step **overwrote** a pre-existing `SHA256SUMS_p5_3b_decomp.txt` (expected — it is this campaign's file) via a non-atomic `>` redirect.
- **m2 — the skip predicate ignores provenance fields**: a chunk with `tree.allow_dirty_used: true` was skipped; a chunk whose `checkpoint_sha256` is `f…f` was skipped. And `report` does not check chunk-level `format_version`, `is_complete` or `n_mismatches` — a stale-version (`0.9`) chunk, an `is_complete: false` chunk and an `n_mismatches: 1` chunk were each **accepted** at `report` when their rows were valid (it recounts from rows, which is the right source; noted so nobody reads the flags as enforced).
- **m3 — the fence does not see `datasets_v11/` or `scenarios/draws/`** (no `output` component): `--work-dir <root>/datasets_v11/x.json` and `<root>/scenarios/draws/x.json` are ALLOWED. Would create new chunk files there, never overwrite an existing one; driver hardcodes `--work-dir`. Likewise the fence allows `output` itself as `--work-dir` (`parts[:-1]`), but the per-chunk destination check at `:1148` then refuses `output/decomp_*.json` — no write.
- **m4 — `reap` (`:114-118`, `:144`) is effectively unreachable**: the loop at `:138` waits for all five pids before `failed` is examined, so the four other seeds always finish (and write) before the failing one is acted on. Benign; the comment at `:112-113` overstates what it does. Stub test: 5 invocations, `FAILED` written, `COMPLETE` absent.
- **m5 — resumability positives, for the record** (all executed through `main`): 99 rows with lying flags, 99 honest draws, `n_mismatches=1`, missing `n_mismatches`, stale version, `is_complete=false`, duplicated draw id → all printed `incomplete or mismatching, re-running` and entered the roll path (which then refused on the sandbox's missing checkpoint **before any write**). `report`: 29 chunks → `FileNotFoundError`, artifact absent; one 99-draw chunk → refused; one `att_ours + 1e-9` → refused `1 of 12000 equalities`; wrong-cell chunk → refused; 30 valid + junk in `smoke/` + a stray `decomp_dt_mix50_seed606.json` → accepted, smoke ignored (`report` reads exact names, `:1217`). Zero `.p4-*` leaks in every case.
- **m6 — a restart after a successful `report` refuses at `report`.** `_run_report` calls `assert_recordable_tree` (`:1211`) → `_git_tree_is_dirty` → `git status --porcelain` (`dt_gate.py:705`), which lists untracked files (`?? untracked.json` in a scratch repo); `docs/data/p5_3b_decomposition.json` is not gitignored (`git check-ignore` → not ignored). The driver passes no `--allow-dirty` (`:158`). So: restart → 30 skips → `report` raises `refusing to write a chunk from a tree that is modified` → `FAILED`, although the first run's artifact is valid on disk. *(by reading + the two demonstrations; not run end-to-end.)*
- **m7 — `smoke/` vs run chunks cannot be confused in either direction** (executed): `report` and `run` use the work-dir root only; the smoke chunks carry `is_complete: false`, `draws: [1000..1004]`. The brief's literal "must not exist at first start" is not met (plan §4.5 discloses it; A4 accepts it).

### Verified positively (executed)

Fence (`:193-217`): refused `p5_3b/`, `p5_3b/checkpoints/…`, `p8_4b_rederivation/`, `p4_dt/`, non-existent `p4_6/`, `SHA256SUMS_p5_3b.txt`, `SHA256SUMS_p5_3b_decomp.txt.bak`, prefix collisions `p5_3b_decomp2`/`p5_3b_decomp_x`/`p5_3b_decom`, `p5_3b_decomp/../p5_3b/`, `smoke/../../p8_4b_rederivation/`, relative `output/p5_3b/x`, a symlink outside pointing at `p5_3b`, a symlink **inside** `p5_3b_decomp/` pointing at `../p5_3b`, and `p5_3b_decomp/output/p5_3b/`; allowed exactly the root, chunks, `smoke/` and the manifest. Driver refusal without token: exit 2, no `logs/`, no stub invoked, existing files untouched. Token + pre-existing `FAILED`/`COMPLETE`: token deleted, both cleared, 30 cells + 1 report invoked, re-verify `OK`, `COMPLETE` written.

### What I could NOT verify

- **The real driver's refusal path against the real tree** — the tool's permission classifier blocked `bash offline/campaigns/p5_3b_decomp.sh` in the worktree; I ran the identical logic on a copy differing only in `MAIN`, `WORK_TREE`, `PY`.
- Any real CityFlow rollout under the observer, `_dt_factory`/`torch.load` side effects, GPU behaviour under 5 (or 10) concurrent cells, full-disk and tmux SIGHUP semantics — nothing rolled in this review.
- `assert_reused_dt_identity` and `rederived_dt_episodes` internals — merged, previously reviewed code; read for absence of writes only.
- The full test suite (only `tests/test_nortg_decomposition.py` was run).
- Anything scientific — out of scope by construction; no numbers exist yet.
