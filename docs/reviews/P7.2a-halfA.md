# MERGE REVIEW — P7.2a (`BRIEF_35` + Amendments A–C), HALF A: code and tests, by mutation

**Filed by the coordinator 2026-09-13 from the reviewer's findings file, verbatim below the rule.** Reviewer: `contract-reviewer`, one mandate, 15-minute box (~8 used), sandbox copy of `offline/` + `tests/` (the worktree never mutated; `diff -rq` identical afterwards), 11 mutations, the two simulator-gated tests deselected by mandate (their claims rest on the coordinator's own runs: the 83-pass in the worktree, gate 6's engine read-back, and two independent 100/100 band gates). Reviewed HEAD `220be2c` on `task/p7.2a-sumo-draws`.

**Verdict: PASS-WITH-NOTES — blocking 0, major 0, minor 2.** 9 of 11 mutations killed. Survivors: (M3) the `depart − offset` shift in `_parity_demand_audit` is vacuous at this scenario's `depart_offset = 0.0` — a scenario fence, not a defect in today's artifacts; (M9) `_existing_parity_conflict`'s provenance-field comparison is dead under the suite — identical rendered files with an edited record are reported `kept`. Both become tests in the fix round (`BRIEF_35` Amendment D).

---

# REVIEW P7.2a HALF A — mutation review of offline/materialise_draws.py parity phase + tests
Started: 2026-09-13 (see timestamps per item). Worktree /home/filip/rltraffic-p53b @ 220be2c, branch task/p7.2a-sumo-draws.
Sandbox: /tmp/claude-1000/-home-filip-rltraffic/2824e906-e46f-4c96-a603-40341737cf5b/scratchpad/review-p72a-halfA/sandbox
Deselected always: test_the_parity_type_and_the_teleport_regime_are_what_the_engine_actually_runs (SUMO), test_regenerated_draw_201_reproduces_p4_3s_recorded_episode (CityFlow).

## CHECKLIST
[x] 0. Sandbox built; baseline green count on the two files with the two deselections pasted.
[x] 1. MUTATION — in `_render_bound_routes`, return `text` unchanged (skip the binding). Which tests fail?
[x] 2. MUTATION — in `_build_parity`, drop the `if not audit["multiset_equal"]: raise` block. Which tests fail?
[x] 3. MUTATION — in `_parity_demand_audit`, remove the `- offset` shift. Which tests fail? (offset is 0.0 on this scenario)
[x] 4. MUTATION — in `_build_parity`, pass `None` as time_to_teleport to `render_parity_sumocfg_text`. Which FILE-level tests fail?
[x] 5. MUTATION — in `materialise_parity` phase 1, pass `force=force` to `materialise()`. Which test fails?
[x] 6. MUTATION — swap phases: call `materialise()` BEFORE validating existing parents. Which test fails?
[x] 7. MUTATION — in `_checked_out_root_shape`, delete the third clause (holds draw_NNNN children). Which test fails?
[x] 8. MUTATION — in `_checked_parity_target`, delete the depth check. Which test fails?
[x] 9. MUTATION — in `_existing_parity_conflict`, skip the provenance field comparison. Does any test fail?
[x] 10. MUTATION — in `_build_parity`, record parent.flow_sha256 as the ROUTES file digest (swap). Caught?
[x] 11. MUTATION — in `render_parity_sumocfg_text`, emit the A15(c) header sentence unconditionally. Which test fails?
[x] 12. READ: does the no-parent-byte test snapshot the WHOLE tree (rglob)? Does the dry-run test snapshot before AND after?
[x] 13. READ: verify_p4_3_probe — writes under out_root? Does _report_probe refuse --report inside out_root?
[x] 14. Restore sandbox to pristine; re-run the two files once; paste green count.

## FINDINGS (appended incrementally)

### [x] 0. Sandbox + baseline — 22:08:55
Sandbox: copied offline/ and tests/ from worktree @220be2c, symlinked configs scenarios docs utils envs agent algorithms metrics states rewards.py experiments pyproject.toml. Pristine copies in $S/pristine_offline, $S/pristine_tests.
CMD: PYTHONPATH=sandbox .venv/bin/pytest tests/test_materialise_parity.py tests/test_parity_vtype.py -q -p no:cacheprovider --deselect <SUMO test> --deselect <CityFlow test>
RESULT: `50 passed, 2 deselected in 18.28s`. NOTE: mandate expected 48 passed; the two files contain 52 tests, so 50 is the correct green baseline (both deselections matched: "2 deselected").

### [raw] MUTATION 1 — 22:10:30
EDIT: materialise_draws.py: 'return parity.render_parity_rou_text(text)' -> 'return text  # MUTANT 1'
CMD: pytest (two files, two deselections) from sandbox
RESULT: 16 failed, 34 passed, 2 deselected in 13.00s
  FAILED tests/test_materialise_parity.py::test_the_parity_phase_adds_a_subdirectory_and_touches_no_parent_byte
  FAILED tests/test_materialise_parity.py::test_every_vehicle_of_every_drawn_rendering_is_bound_to_the_parity_type
  FAILED tests/test_materialise_parity.py::test_the_generated_config_builds_the_env_spec_p4_3_recorded
  FAILED tests/test_materialise_parity.py::test_cap_e_holds_on_every_draw_and_the_record_says_so
  FAILED tests/test_materialise_parity.py::test_a_lost_vehicle_is_refused_in_validation_and_no_draw_gets_a_parity_dir
  FAILED tests/test_materialise_parity.py::test_a_differing_parity_dir_is_refused_and_only_force_replaces_it
  FAILED tests/test_materialise_parity.py::test_force_is_never_passed_to_the_parent_materialisation
  FAILED tests/test_materialise_parity.py::test_an_out_root_that_is_itself_a_draw_or_a_parity_directory_is_refused
  FAILED tests/test_materialise_parity.py::test_an_out_root_that_is_a_scenario_directory_is_refused
  FAILED tests/test_materialise_parity.py::test_an_out_root_inside_a_linked_worktree_is_refused_without_the_flag
  FAILED tests/test_materialise_parity.py::test_the_parity_record_carries_no_path_field_without_a_digest_twin
  FAILED tests/test_materialise_parity.py::test_the_generated_config_names_the_shipped_net_and_the_bound_routes
  FAILED tests/test_materialise_parity.py::test_draw_zero_is_equivalent_to_the_committed_teleport_free_scenario
  FAILED tests/test_materialise_parity.py::test_the_dry_run_writes_nothing_and_reports_each_state
  FAILED tests/test_materialise_parity.py::test_the_cli_writes_the_requested_parity_dirs_and_refuses_with_a_reason
  FAILED tests/test_materialise_parity.py::test_the_result_dataclass_reports_the_paths_it_wrote

### [raw] MUTATION 2 — 22:10:49
EDIT: materialise_draws.py: 'if not audit["multiset_equal"]:' -> 'if False and not audit["multiset_equal"]:  # MUTANT 2'
CMD: pytest (two files, two deselections) from sandbox
RESULT: 1 failed, 49 passed, 2 deselected in 17.90s
  FAILED tests/test_materialise_parity.py::test_a_lost_vehicle_is_refused_in_validation_and_no_draw_gets_a_parity_dir

### [raw] MUTATION 3 — 22:11:08
EDIT: materialise_draws.py: 'sumo = [(depart - offset, route) for depart, route in raw]' -> 'sumo = [(depart, route) for depart, route in raw]  # MUTANT 3'
CMD: pytest (two files, two deselections) from sandbox
RESULT: 50 passed, 2 deselected in 17.94s

### [raw] MUTATION 4 — 22:11:27
EDIT: materialise_draws.py: 'time_to_teleport=PARITY_TIME_TO_TELEPORT,\n    )' -> 'time_to_teleport=None,  # MUTANT 4\n    )'
CMD: pytest (two files, two deselections) from sandbox
RESULT: 2 failed, 48 passed, 2 deselected in 18.04s
  FAILED tests/test_materialise_parity.py::test_the_generated_config_names_the_shipped_net_and_the_bound_routes
  FAILED tests/test_materialise_parity.py::test_draw_zero_is_equivalent_to_the_committed_teleport_free_scenario

### [x] 1. CLASSIFICATION — KILLED (16 failed / 34 passed). materialise_draws.py:1062 `return parity.render_parity_rou_text(text)` -> `return text`.
Direct killers: test_every_vehicle_of_every_drawn_rendering_is_bound_to_the_parity_type (file-level Counter of type=cf_parity vs parent n_vehicles). The other 15 fail because `_build_parity` (materialise_draws.py:1212-1218) refuses an unbound rendering via `parity.binding_is_complete`, so every test calling materialise_parity() on a happy path raises. The runtime read-back test (engine_actually_runs) was DESELECTED per mandate; its claim is not observed here.

### [x] 2. CLASSIFICATION — KILLED (1 failed / 49 passed). materialise_draws.py:1223 multiset_equal raise disabled.
Only killer: test_a_lost_vehicle_is_refused_in_validation_and_no_draw_gets_a_parity_dir. NOTE the second guard at :1229 (`report.vehicle_count != audit["n_cityflow"]`) did NOT catch the planted lost vehicle either -> it raised nothing, so on this mutant the run wrote parity dirs for all five draws. Meaning: the two guards are redundant for the equal-count case only; a SAME-count demand change (swapped route, shifted depart) is caught solely by :1223. One test covers that line; it is load-bearing.

### [x] 3. CLASSIFICATION — SURVIVED. materialise_draws.py:1098 `- offset` removed. 50 passed, 2 deselected.
Reason: cityflow1x1's SUMO pairing has depart_offset == 0.0, so the shift is a no-op on every fixture. test_cap_e_holds_on_every_draw... (tests/test_materialise_parity.py:295-298) reads `offset` BACK from the record and shifts by it, so it recomputes the multiset with the same 0.0 and cannot see the omission. No test claims to cover a non-zero offset. Classification: MINOR (scenario fence) — a later non-zero-begin scenario (e.g. cologne, 25200) would ship with an untested shift; today's artifacts are unaffected. Suggest: a unit test of `_parity_demand_audit` on a synthetic flow/rou pair with depart_offset != 0.

### [x] 4. CLASSIFICATION — KILLED (2 failed / 48 passed). materialise_draws.py:1239 `time_to_teleport=PARITY_TIME_TO_TELEPORT` -> `None`.
Killers (file-level): test_the_generated_config_names_the_shipped_net_and_the_bound_routes (tests/...:695-696, parsed XML `.//time-to-teleport == "-1"`), test_draw_zero_is_equivalent_to_the_committed_teleport_free_scenario (:724-727, `_scenario()` dict compare against committed noteleport cfg). NOTE: test_the_parity_record_carries_no_path_field_without_a_digest_twin (:644) asserts the RECORD says time_to_teleport=-1, but the record is written from the constant not from the file, so it did NOT fail — record and file can disagree undetected by that test; the two tests above are what bind the file. Runtime read-back deselected.

### [raw] MUTATION 5 — 22:12:14
EDIT: materialise_draws.py: 'records = materialise(source, ids, out_root=root, force=False, dry_run=dry_run)' -> 'records = materialise(source, ids, out_root=root, force=force, dry_run=dry_run)  # MUTANT 5'
CMD: pytest (two files, two deselections) from sandbox
RESULT: 1 failed, 49 passed, 2 deselected in 17.82s
  FAILED tests/test_materialise_parity.py::test_force_is_never_passed_to_the_parent_materialisation

### [raw] MUTATION 6 — 22:12:34
EDIT: materialise() call moved before the `parents = {...}` validation of existing parents (materialise_draws.py:1455/1464)
CMD: pytest (two files, two deselections) from sandbox
RESULT: 3 failed, 47 passed, 2 deselected in 18.65s
  FAILED tests/test_materialise_parity.py::test_a_parent_whose_digest_disagrees_with_its_provenance_is_refused
  FAILED tests/test_materialise_parity.py::test_a_parent_already_marked_bound_is_refused
  FAILED tests/test_materialise_parity.py::test_an_existing_parents_refusal_prevents_a_new_parent_from_being_materialised

### [raw] MUTATION 7 — 22:12:53
EDIT: materialise_draws.py: 'if resolved.is_dir():\n        draws = sorted(' -> 'if False and resolved.is_dir():  # MUTANT 7\n        draws = sorted('
CMD: pytest (two files, two deselections) from sandbox
RESULT: 1 failed, 49 passed, 2 deselected in 18.27s
  FAILED tests/test_materialise_parity.py::test_an_out_root_that_is_a_scenario_directory_is_refused

### [raw] MUTATION 8 — 22:13:13
EDIT: materialise_draws.py: 'if resolved.parent.parent.parent != Path(out_root).resolve():' -> 'if False:  # MUTANT 8'
CMD: pytest (two files, two deselections) from sandbox
RESULT: 1 failed, 49 passed, 2 deselected in 18.84s
  FAILED tests/test_materialise_parity.py::test_the_commit_guard_rejects_every_target_shape_that_is_not_a_parity_dir

### [x] 5. CLASSIFICATION — KILLED (1 failed / 49 passed). materialise_draws.py:1464 `force=False` -> `force=force`.
Only killer: test_force_is_never_passed_to_the_parent_materialisation (spy on md.materialise, asserts kwargs["force"] is False). Load-bearing and exact.

### [x] 6. CLASSIFICATION — KILLED (3 failed / 47 passed). materialise() call moved to just before `parents = {...}` (between :1454 and :1455); original :1464 call removed.
Killers: test_an_existing_parents_refusal_prevents_a_new_parent_from_being_materialised (the A6 test: draw_0201 got created), PLUS test_a_parent_whose_digest_disagrees_with_its_provenance_is_refused and test_a_parent_already_marked_bound_is_refused — those two fail because materialise(force=False) now reaches the tampered parent first and raises FileExistsError (its own no-op conflict check) instead of the ValueError the tests match. So the phase order is pinned by three tests, one of them by construction (A6).

### [x] 7. CLASSIFICATION — KILLED (1 failed / 49 passed). materialise_draws.py:1003 third clause of _checked_out_root_shape disabled.
Only killer: test_an_out_root_that_is_a_scenario_directory_is_refused (match="scenario directory"; also has the positive control for a same-named root). Exact.

### [x] 8. CLASSIFICATION — KILLED (1 failed / 49 passed). materialise_draws.py:1045 depth check disabled.
Only killer: test_the_commit_guard_rejects_every_target_shape_that_is_not_a_parity_dir (direct unit test of _checked_parity_target, the two "depth" rows :433-434). NOTE: no END-TO-END test reaches the depth clause through materialise_parity (targets are built by parity_dir, always 4 levels) — acceptable, the unit test is exact and the clause is reachable only via a wrong out_root, which B1 refuses earlier.

### [raw] MUTATION 9 — 22:13:48
EDIT: materialise_draws.py: 'return f"{name} differs byte-for-byte"\n    try:' -> 'return f"{name} differs byte-for-byte"\n    return None  # MUTANT 9\n    try:'
CMD: pytest (two files, two deselections) from sandbox
RESULT: 50 passed, 2 deselected in 11.66s

### [raw] MUTATION 10 — 22:13:57
EDIT: materialise_draws.py: '"flow_sha256": _sha256_file(parent_dir / FLOW_FILENAME),\n            "routes_sha256": _sha256_file(parent_dir / SUMO_ROUTES_FILENAME),' -> '"flow_sha256": _sha256_file(parent_dir / SUMO_ROUTES_FILENAME),  # MUTANT 10\n            "routes_sha256": _sha256_file(parent_dir / FLOW_FILENAME),'
CMD: pytest (two files, two deselections) from sandbox
RESULT: 1 failed, 49 passed, 2 deselected in 8.35s
  FAILED tests/test_materialise_parity.py::test_the_parity_record_carries_no_path_field_without_a_digest_twin

### [raw] MUTATION 11 — 22:14:07
EDIT: parity.py: '"     the env, not this file, owns the episode length on both backends.",\n    ]' -> '"     the env, not this file, owns the episode length on both backends.",\n        "     Teleporting is DISABLED under A15(c): every SUMO measurement recorded after",  # MUTANT 11\n    ]'
CMD: pytest (two files, two deselections) from sandbox
RESULT: 1 failed, 49 passed, 2 deselected in 9.05s
  FAILED tests/test_parity_vtype.py::test_the_sumocfg_generator_default_is_byte_identical_to_the_committed_artifact

### [x] 9. CLASSIFICATION — SURVIVED. materialise_draws.py:1309 `return None` inserted before the provenance field comparison in _existing_parity_conflict. 50 passed, 2 deselected.
Consequence: an existing parity/ whose routes.rou.xml and noteleport.sumocfg are byte-identical but whose provenance.json differs in an identity field (parent.provenance_sha256 after a parent re-materialisation, net.sha256 after a shipped-net change, a hand-edited demand_audit or vtype_attributes) is reported `kept` instead of refused/`replaced`. The two existing "differs" tests (test_a_differing_parity_dir_is_refused_and_only_force_replaces_it :479 edits the sumocfg; the CLI test :815 edits the routes) both exercise the BYTE branch only; nothing edits parity/provenance.json. Classification: MINOR — the files the engine reads are still byte-verified, so no measurement changes; what is unverified is record staleness (the digest twins the A7 test insists on can go stale silently). A test writing a wrong `net.sha256` into parity/provenance.json and expecting FileExistsError would kill it.

### [x] 10. CLASSIFICATION — KILLED (1 failed / 49 passed). materialise_draws.py:1252-1253 parent.flow_sha256 <-> parent.routes_sha256 swapped.
Only killer: test_the_parity_record_carries_no_path_field_without_a_digest_twin (:647-651 compares the whole `parent` block against independently computed sha256 of the three parent files). Exact and independent (test uses its own hashlib helper, not the module's).

### [x] 11. CLASSIFICATION — KILLED (1 failed / 49 passed). parity.py:381 A15(c) header sentence appended unconditionally to `header`.
Only killer: tests/test_parity_vtype.py::test_the_sumocfg_generator_default_is_byte_identical_to_the_committed_artifact (:386 `generated == DECLARED_PARITY_SUMOCFG bytes`). The -1 path tests still passed (they parse XML / substring "A15(c)"), as expected. The P7.0 control is pinned.

### [x] 12. READ — whole-tree snapshot: YES; dry-run before AND after: YES. — 22:15
Helper tests/test_materialise_parity.py:78-91: `_tree_snapshot` = `{str(path.relative_to(base)): sha256 for path in sorted(base.rglob("*")) if path.is_file()}` — every file under the root, not an expected-files list. T1 (:124-125): `assert {name: digest for name, digest in after.items() if name in before} == before` — a deleted parent file makes the LHS dict smaller than `before` and fails; then `assert len(after) == len(before) + 9` pins that nothing else appeared. Second run (:139-143) snapshot-before / run / snapshot-equal + no `.staging*` leftover. Dry-run test (:765 snapshot, :771 equal after the Python call, :792 equal again after the CLI `--dry-run` call) — before and after, for both entry points. Caveat: snapshots key on files only; an EMPTY new directory would not be seen by `_tree_snapshot` (T1 does check the three parity dir listings explicitly; the dry-run test relies on `not draw_dir(201).exists()` for the parent, but an empty `parity/` dir created by a dry run would go unnoticed). Minor test-coverage note, not a code defect observed.

### [x] 13. READ — verify_p4_3_probe writes nothing itself; _report_probe refuses --report inside out_root. — 22:15
materialise_draws.py:1554-1618: reads the artifact JSON, builds `draw_config_path(...)` per draw, calls `offline.rtg_calibration.run_probe(...)`, compares five keys under `!=` per key -> `ProbeCheck`. No open()/write/mkdir in the function. `_report_probe` :1784-1791: `report_path = Path(args.report).resolve(); root = Path(args.out_root).resolve(); if report_path == root or report_path.is_relative_to(root): raise ValueError("--report ... is inside --out-root ...; this mode writes nothing under the draws tree")`; only then `report_path.parent.mkdir(...)` + `write_text`. Quoted check is present and resolves symlinks on both sides. NOT VERIFIED (out of read scope): whether `run_probe` / the CityFlow engine itself writes replay/log files next to the draw's CityFlow config (depends on the draw config's saveReplay flags) — the "writes nothing under out_root" claim rests on that, and I did not run the probe per mandate.

### [x] 14. RESTORE + FINAL GREEN — 22:15:35
Sandbox offline/ and tests/ replaced from pristine copies; `diff -rq` against the worktree (excluding __pycache__) -> "SANDBOX == WORKTREE". Re-run of the two files with the two deselections: `50 passed, 2 deselected in 9.64s`. Worktree `git status --porcelain` empty — nothing written in either repository.

## MUTATION SCORE
11 mutations: 9 KILLED (1,2,4,5,6,7,8,10,11), 2 SURVIVED (3, 9). Both survivors classified MINOR.

## VERDICT: PASS-WITH-NOTES — blocking 0, major 0, minor 2
Minor 1 (item 3): the `- depart_offset` shift in `_parity_demand_audit` (materialise_draws.py:1098) is untested — the only fixture scenario has offset 0.0 and the CAP(E) test reads the offset back from the record. Scenario fence: correct for today's artifacts, unprotected for a non-zero-begin scenario.
Minor 2 (item 9): `_existing_parity_conflict`'s provenance field comparison (materialise_draws.py:1309-1319) is dead under the suite — identical files + stale/edited parity/provenance.json is reported `kept`. Engine-read files are still byte-verified.
Notes (not defects observed): (a) item 4 — the record's `sumocfg.time_to_teleport` is written from the constant, never cross-checked against the emitted file by the A7 test; the file itself is bound by two other tests. (b) item 12 — `_tree_snapshot` is file-keyed; an empty stray directory would be invisible to it. (c) item 13 — whether `run_probe`/CityFlow writes replay files under the draw dir was not verified (out of read scope; probe not run per mandate). (d) baseline is 50 passed not 48: the two files contain 52 tests; test file is 851 lines not 797.
