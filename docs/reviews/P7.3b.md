# MERGE REVIEW — P7.3b (the full-retrain anchor, A18(a)) — 2026-09-19 — VERDICT: PASS-WITH-FINDINGS (0 blocking · 1 major · 4 minor)

**Reviewer:** `contract-reviewer`, the ONE merge review the author's compression of 2026-09-18 provides for; mandate: the
whole branch diff `main...task/p7.3b-anchor` at **`2f95e20`**, code + mutations + every published number recomputed by the
reviewer's own route (json + numpy, nothing imported from `offline/`), packet claims against disk. The reviewer's findings
file is filed **verbatim** below the rule; 10/10 checklist items reached; nothing written under any repository tree;
mutations in a detached throwaway worktree, removed afterwards.

## Coordinator's ruling (2026-09-19)

1. **MERGE.** Nothing in the diff could make a published number wrong or destroy data. Every number in the artifact was
   recomputed by the reviewer — the third independent route after the coordinator's (campaign read, §4) and the
   implementer's (packet §5) — and is exact: 1,000/1,000 per-cell ρ, 8/8 aggregates, the paired ATT blocks, the prompt by a
   fifth route from the raw `.npz`, the digest chain.
2. **MAJOR [3] CONFIRMED under the coordinator's own hand, and ruled a finding, not a round.** The test body of
   `test_the_pilot_transcript_survives_a_cell_that_has_no_training_seed` (`tests/test_transfer_curve.py:4053`) has **0 calls
   to `run_pilot`** — it re-implements the guarded expression and asserts the unguarded copy raises. Mutation M2 (the
   `is not None` guard at `offline/transfer_curve.py:2624` removed), re-run by the coordinator on a **committed, clean**
   mutant worktree with a pattern-free command line: the three relevant tests pass unmutated (3/3) **and pass mutated
   (3/3)**. The guard is fixed and it is NOT pinned; the driver header's *"pinned by a test"* and packet §10.1 overstate.
   It cannot affect a published number — the transcript is the pre-flight's rate record — so under the author's
   compression it goes to **`DEFERRED` 86** and to **`BRIEF_39` §4 T-pilot**, which replaces the theatre test for P7.3d's
   pilot rather than reopening this branch.
3. **A coordinator near-error, recorded to the same standard.** The coordinator's FIRST M2 re-run reported *2 failed /
   134 passed* — both failures driver-execution tests. Neither was a kill: the mutant worktree was **dirty** (an
   uncommitted edit), so the driver's dirty-tree refusal fired before the refusal the tests expect; and the shell's command
   line carried the literal `offline.transfer_curve`, which the driver's liveness `pgrep` matches — the exit-3 false-verdict
   mechanism `HANDOFF_2026-09-19.md` §7 names. The control run established both. Had the first run been recorded, the
   review would have carried a false KILLED and a false MAJOR-closed. Both traps are now written into `BRIEF_39` §4.
4. **Minors, accepted as recorded:** [6] the driver's stage set is a documented deviation accepted at the pre-flight;
   [5] M4a is EQUIVALENT (the `published_arms` refusal is unreachable after `validate_cell_payload`) and the reachable last
   refusal IS pinned (M4b killed); [8] the paired-ATT mean's *bitwise* agreement holds on the helper's summation route —
   the route-naming rule of `DEFERRED` 85 covers it; [7] is `DEFERRED` 85.
5. **What the reviewer could not verify was verified by the coordinator instead:** the byte-identical regeneration and the
   600 bit-identity comparisons against P7.3a (`docs/notes/P7.3b_CAMPAIGN_READ_2026-09-19.md` §2, §4), and the full suite
   **2,136 passed / 94 skipped** at `2f95e20` (coordinator's own run, `output/p7_3b_runs/coord_verify/suite_2f95e20_tail.txt`).
6. The reviewer raised the session's AI-trailer instruction as a conflict with `CLAUDE.md` §4b and made no commits — the
   rule applied as written.

---

# Reviewer's findings file, verbatim

# REVIEW P7.3b — FINDINGS (incremental)
Branch task/p7.3b-anchor @ 2f95e200d0f52b011eef2471cb7c1de5912d03fd, worktree /home/filip/rltraffic-p73b ; main @ 6ae38f7
Reviewer started: 2026-09-19

## CHECKLIST
- [x] 1. Diff scope: no frozen path touched; no new dependency in offline/anchor_training.py and transfer_curve.py diff
- [x] 2. Filesystem-mutation barrier in offline/anchor_training.py (writes only after digests/disjointness/two-route). MUTATION M1: corpus digest check no-op -> which test dies
- [x] 3. MUTATION M2: run_pilot transcript `int(cell["seed"])` unguarded -> which test dies
- [x] 4. MUTATION M3: validate_cell_payload without `stage` in identity -> which test dies; cell_chunk_name: is STAGE in the filename (code, not docstring)
- [x] 5. MUTATION M4: in `report`, move _write_json(target_path, artifact) above the final refusal -> does a test assert empty out-dir after refusal and die
- [x] 6. Driver offline/campaigns/p7_3b_anchor.sh: order canary->trap->token->mkdir/rm->record-canary->stages->manifest; no --overwrite; no [ -f ] chunk guards; tee -a. MUTATION M5: token before canary -> driver text test dies?
- [x] 7. NUMBERS by own route (numpy+json only): rho per cell e_sumo & att_env for 700 chunks vs artifact cells rows (==); per-seed means (sum/len), aggregate mean/std(ddof=1)/CI vs rho.by_subject_arm[anchor_k200] (==)
- [x] 8. Paired ATT vs fixedtime & maxpressure on e_sumo: mean diff, analytic CI, wins/losses/ties (==); Wilcoxon p order-of-magnitude (own impl) or state not done
- [x] 9. Packet spot-checks: (a) artifact digest 4cae233c...; (b) COMPLETE + n_failed 0; (c) capture first line = process-group refusal; (d) git_commit uniform, git_dirty false across 700; (e) two corpus SHA256SUMS verify; (f) packet §6 pytest count vs test delta b35216c..HEAD
- [x] 10. Claimed-but-unverified / contradicted-by-disk list; code-diff items that could make a NUMBER wrong (listed first)

## LOG (one entry per verified fact; item, timestamp, command/construction, observation, path:line)

### [1] 14:40 diff scope (partial — dependency grep pending)
cmd: git -C /home/filip/rltraffic-p73b diff --name-only main...HEAD
obs: 10 files: docs/data/p7_3b_anchor.json, docs/data/p7_3b_anchor_training.json, docs/notes/P7.3b_CORPUS_EVIDENCE.md, docs/plans/p7.3b.md, docs/returns/P7.3b.md, offline/anchor_training.py, offline/campaigns/p7_3b_anchor.sh, offline/transfer_curve.py, tests/test_anchor_training.py, tests/test_transfer_curve.py. NO frozen path. pgrep for live runner: empty (exit 1). HEAD=2f95e20 confirmed, main=6ae38f7 confirmed.
[1] 14:41 deps: grep imports offline/anchor_training.py (argparse, hashlib, json, dataclasses, pathlib, typing, numpy, torch, offline.*, agent.utils.utils) and transfer_curve.py diff (+only `from offline.transfer_calibration import SubjectFacts`). No dependency outside numpy/stdlib/torch. ITEM 1 DONE -> [x]
[read] 14:44 Read brief BRIEF_38 L1-98 + Amendment B (L127-225), PREREGISTRATION.md L380 (A19), packet docs/returns/P7.3b.md in full (644 lines). Packet claims to check in item 9/10: digest 4cae233c...; COMPLETE "CAMPAIGN COMPLETE (anchor) in 1296s"; n_failed 0; capture first line "REFUSING TO START: not a process-group leader..."; git_commit f13358eb14d37592d517c75b7cb6c39a75b05cfd uniform, git_dirty false; SHA256SUMS 101/101 both; 2136 passed = 2113 + 23 tests added b35216c..HEAD; rho e_sumo mean 0.9829792481916148 CI [0.9701604747851167, 0.9957980215981128]; att_env 0.9840242299939405 [0.921675243158202, 1.0463732168296789]; per-seed 0.9726532764576968/0.9858670548205469/0.9798954351423358/0.9926876589198256/0.9837928156176684; paired vs fixedtime -240.0355018418621 [-242.85279070459723,-237.21821297912697] 100/0/0 p=3.9559116088995575e-18; vs maxpressure +5.124633990863357 [1.930806615431012, 8.318461366295702] 37/63/0 p=0.003230327177826658.

### [2] 14:47 filesystem-mutation barrier, offline/anchor_training.py (read L156-212, 235-458, 464-556, 654-762)
obs: load_anchor_corpus performs NO write: digests re-verified L286-287 (verify_corpus_digests L171-210, refuses missing file/mismatch/zero entries) -> manifest draw ids L290-315 -> disjointness_record L318 -> loader L321 -> shape asserts L325-348 -> stack L354 -> two-route prompt L380 (_assert_prompt_routes_agree L384-408 raises on !=). main(): load L713 -> prompt L714 -> dry-run return L724-726 -> FIRST fs mutation is checkpoints.mkdir L730, after every validation; train_dt writes checkpoints L517-535; artifact written atomically tmp->replace L654-662 LAST (L756). No delete anywhere in the module. Barrier holds. Pending: confirm disjointness_record does not write; M1.
[2] 14:50 disjointness_record (offline/transfer_calibration.py) body grep for write/open/mkdir/unlink: none -> reads only.
[2] 14:50 MUTATION M1: `if actual != recorded.strip():` -> `if False and actual != ...` (offline/anchor_training.py:199) in mut worktree; cmd: pytest -q -p no:cacheprovider tests/test_anchor_training.py -> "1 failed, 24 passed in 16.12s"; FAILED test_a_corpus_whose_bytes_moved_since_its_digest_file_is_refused (tests/test_anchor_training.py:336, "DID NOT RAISE ValueError" at :350). KILLED. Mutation reverted (git checkout -- ., porcelain empty). ITEM 2 DONE -> [x]

### [3-6 code facts] 14:56 (reads: transfer_curve.py L345-349, L995-1054, L1925-1972, L2608-2629; driver whole 363 lines)
[3] run_pilot transcript guard: offline/transfer_curve.py:2624 `sorted({int(cell["seed"]) for cell in cells if cell["seed"] is not None})`, :2625 n_cells_without_a_training_seed. Candidate test tests/test_transfer_curve.py:4053 test_the_pilot_transcript_survives_a_cell_that_has_no_training_seed.
[4] validate_cell_payload identity loop offline/transfer_curve.py:1026 keys ("kind","subject","arm","seed","draw_id","stage") — stage IS in identity (only when cell is passed, :1017). cell_chunk_name :345-349 builds f"cell_{subject}_{arm}_seed{seed}_draw{draw:04d}.json" — STAGE NOT IN FILENAME (observed from code). Matches coordinator's record. Candidate test :4030 test_a_chunk_from_the_other_campaign_is_refused_by_stage_not_accepted_by_name.
[5] report: last refusals :1958-1970 (FENCED_KEY scan over serialised bytes; published_arms <= DECLARED_ARM_NAMES), then _write_json(target_path, artifact) :1971. Candidate tests :1014 test_t8_report_refuses_a_missing_declared_cell_and_writes_nothing, :1039 test_t8_report_refuses_one_broken_cell_and_writes_nothing, :1152 test_t8_a_test_reaches_the_LAST_refusal_before_the_write.
[6] driver offline/campaigns/p7_3b_anchor.sh order observed: interpreter :112 -> import :119 -> live-runner pgrep :127 -> group leader :139 -> SigIgn :149 -> stage arg :158 -> dirty tree :172 -> input presence :184-221 -> CANARY :229-245 (`tee -a /dev/stderr` :230, threshold :240) -> fail()/on_signal defs, `trap on_signal INT TERM HUP` :278 -> TOKEN :281-291 (rm -f :290) -> mkdir/rm FAILED,COMPLETE :294-295 -> canary.log append :300-301 -> record-canary :306 -> verify_inputs (anchor_training --dry-run) :337 -> cells :344 -> report :347 -> MANIFEST last, tmp+mv :353-357 -> COMPLETE :360. ORDER AS REQUIRED. `[ -f ]` occurrences :186,:195,:210,:216,:282 are configs/corpus files/artifact/checkpoints/token — none over cell_*.json chunks. `--overwrite`: to grep. NOTE (minor): brief §3.5 lists stages collect->recheck->train->cells->report; the driver ACCEPTS collect/recheck/train from disk (header §3, presence checks only + dry-run digest re-verify) rather than running them — documented deviation, accepted at pre-flight per packet.
[3] 15:02 MUTATION M2: offline/transfer_curve.py:2624 guard `if cell["seed"] is not None` REMOVED (unguarded int(cell["seed"])) in mut worktree; verified import resolves to mut tree (offline.transfer_curve.__file__ -> .../mut/offline/transfer_curve.py; editable install finder, not a fixed path). cmd: pytest -q -p no:cacheprovider tests/test_transfer_curve.py -> "136 passed, 1 warning in 9.94s". **SURVIVED.** Reason (read tests/test_transfer_curve.py:4053-4078): test_the_pilot_transcript_survives_a_cell_that_has_no_training_seed never calls run_pilot or the transcript builder; it re-implements the guarded expression INSIDE the test and asserts the unguarded one raises TypeError — it pins the test's own arithmetic, not the module's code. Driver header :81-84 says "Fixed, and pinned by a test" and packet §10.1 relies on it: the "pin" cannot fail for a regression in run_pilot. THEATRE. -> MAJOR (not a published number; the pilot rate is a pre-flight quantity). ITEM 3 DONE -> [x]
[4] 15:06 MUTATION M3: offline/transfer_curve.py:1026 identity tuple without "stage" in mut worktree. cmd: pytest -q -p no:cacheprovider tests/test_transfer_curve.py -> "1 failed, 135 passed"; FAILED test_a_chunk_from_the_other_campaign_is_refused_by_stage_not_accepted_by_name (tests/test_transfer_curve.py:4030). KILLED. cell_chunk_name (:345-349) confirmed from code: filename has NO stage; identity (:1026) carries it. ITEM 4 DONE -> [x]
[5] 15:12 MUTATION M4 (variant a): `_write_json(target_path, artifact)` moved from offline/transfer_curve.py:1971 to just above the published_arms refusal (:1965-1970), i.e. still AFTER the FENCED_KEY scan (:1958-1964). cmd: pytest -q -p no:cacheprovider tests/test_transfer_curve.py -> "136 passed". **SURVIVED.** Assessment pending: the published_arms refusal duplicates validate_cell_payload's per-cell arm check (:1033-1040) which report runs on every chunk first, so this refusal may be unreachable => likely EQUIVALENT. Running variant b (write above the FENCED_KEY scan) to determine whether the last REACHABLE refusal is pinned.
[9] 15:12 (a) `git show HEAD:docs/data/p7_3b_anchor.json | sha256sum` = 4cae233ce480a5596dc9a65ad53c1ec0cb20c17fa80008d1e1a13341703af1cc — matches packet §4 and Amendment B2. (b) output/p7_3b_anchor/COMPLETE = "CAMPAIGN COMPLETE (anchor) in 1296s"; no FAILED file, no failed/ dir; logs/cells_anchor.log JSON tail: n_declared 700, n_rolled 700, n_reused 0, n_failed 0 — matches packet §5.2. (c) head -1 output/p7_3b_runs/campaign_capture.txt = "REFUSING TO START: not a process-group leader; run in a tmux foreground pane" — matches §7. (e) sha256sum -c --quiet SHA256SUMS: hz1x1_sumo_maxpressure 101 entries exit 0; hz1x1_sumo_maxpressure_301_400 101 entries exit 0 — matches §5.8. (f) git diff b35216c HEAD --stat -- tests/: only tests/test_transfer_curve.py (+774/-1); `+def test_` count 23, `-def test_` 0; b35216c is an ancestor of HEAD. 2113+23=2136 consistent with packet §6. (d) pending.
[5] 15:20 MUTATION M4 (variant b): `_write_json` moved ABOVE the FENCED_KEY scan (:1957-1964) in mut worktree. cmd: pytest tests/test_transfer_curve.py -> "1 failed, 135 passed"; FAILED test_t8_a_test_reaches_the_LAST_refusal_before_the_write (tests/test_transfer_curve.py:1152; asserts `list(roots.out.iterdir()) == []` after the refusal, :1179). KILLED. So: the out-dir-empty-after-refusal assertion exists and bites for the FENCED_KEY scan; the published_arms refusal (:1965-1970) sitting between the scan and the write is NOT pinned (variant a survived) — equivalence check pending (is it reachable after per-cell validate_cell_payload?).
[9] 15:20 (d) own python over 700 chunks: git_commit set == ['f13358eb14d37592d517c75b7cb6c39a75b05cfd']; git_dirty set == ['False']; stage ['anchor']; engine_seed_drawn [437485271]; n_teleports [0]; composition fixedtime 100 / maxpressure 100 / dt 500 (subject anchor_k200, arm naive_in_domain, seeds 101,202,303,404,505), draws 1000-1099 on all three. Matches packet §5.1/§5.3. ITEM 9 DONE -> [x]
[7] 15:20 PARTIAL: own route (json only, no offline import): per-cell rho recomputed as (ft-arm)/(ft-mp) against same-draw anchors: e_sumo 500/500 ==, att_env 500/500 == artifact cells rows (matched by (seed, draw_id)) => 1000/1000 exact. Baseline rows carry rho 0.0/1.0 by construction. Aggregates pending (script re-run; my file name `numbers.py` shadowed stdlib `numbers` and broke numpy import — renamed).
[6] 15:27 MUTATION M5: driver token block (:280-291) moved to before the canary section (:224) in mut worktree (bash -n ok). cmd: pytest tests/test_transfer_curve.py -> "1 failed, 135 passed"; FAILED test_t9_the_anchor_driver_carries_every_invariant_the_campaign_shape_requires (tests/test_transfer_curve.py:3901), assertion :3930 `code.index("trap on_signal") < code.index('rm -f "$TOKEN"')` ("the trap must be installed before the token is consumed"). KILLED — by the trap<token pin; whether canary<token is pinned separately: reading the test body next. `--overwrite` appears only in the header comment :37 (never passed). `[ -f ]` only over configs/corpus files/artifact/checkpoints/token, none over chunks. `tee -a /dev/stderr` at :230. ITEM 6 DONE -> [x]
[5] 15:27 M4a equivalence: report runs validate_cell_payload over EVERY chunk at :1616 (all cell_*.json) and again with the declared cell at :1650; validate_cell_payload refuses an undeclared arm at :1033-1040; rows (:1838 "cells": rows) come from those validated chunks. The published_arms refusal :1965-1970 is therefore unreachable with delivered code => M4a classified EQUIVALENT (belt-and-braces), not a missing pin. The last REACHABLE refusal (FENCED_KEY scan) IS pinned by :1152 (M4b KILLED). ITEM 5 DONE -> [x]
[7] 15:27 aggregates (own route, numpy): per-seed means via sum()/len(): 10/10 == artifact by_subject_arm_seed (e_sumo & att_env); numpy mean differs in last ULP on 4/5 (e_sumo) and 1/5 (att_env) — packet's "3 of 5" is the same phenomenon, count differs by definition/route, immaterial. Aggregate e_sumo: mean 0.9829792481916148 std 0.06540190513519402 lo 0.9701604747851167 hi 0.9957980215981128 — == artifact on all four via all three per-draw routes (np.mean, sum/len, fsum/len). att_env: mean 0.9840242299939405 std 0.31810707569254276 lo 0.921675243158202 hi 1.0463732168296789 — == on all four. Exact matches: 1000 per-cell + 10 per-seed + 8 aggregate = 1018/1018. ITEM 7 DONE -> [x]
[8] 15:27 paired ATT e_sumo, own route (per draw: mean over 5 seeds of arm e_sumo via sum/5.0, minus anchor; CI 1.96*std(ddof=1)/sqrt(100)): vs fixedtime mean -240.0355018418621 lo -242.85279070459723 hi -237.21821297912697 wins/losses/ties 100/0/0 — ALL == artifact. vs maxpressure: wins/losses/ties 37/63/0 == artifact; mean 5.124633990863352 vs artifact 5.124633990863357; lo 1.930806615431008 vs 1.930806615431012; hi 8.318461366295695 vs 8.318461366295702 — NOT bitwise on my route (diff ~5e-15, last few ULP). Trying the helper's likely aggregation order next. Wilcoxon (own impl: zeros dropped, average ranks, tie-corrected normal approx, NO continuity correction, two-sided via erfc): vs fixedtime p=3.8966e-18 (artifact 3.9559e-18) W+=0 W-=5050 n=100; vs maxpressure p=3.2124e-03 (artifact 3.2303e-03) W+=3382 W-=1668. Same order of magnitude both; the residual is explained exactly by a 0.5 continuity correction in the helper (z=(1668+0.5-2525)/290.84 -> p=0.003230).
[8] 15:34 paired vs maxpressure: the artifact's mean_difference 5.124633990863357 and ci95_low 1.930806615431012 ARE reproduced bitwise by the route "np.mean over the 5 seeds (asc) of the arm's e_sumo, minus the anchor's e_sumo, then np.mean/std over 100 draws" (also with sum/len or fsum over draws). My first route (Python sum/5.0) differed in the last ULPs. => route difference, not a discrepancy; all packet §5.5 numbers reproduced (mean, lo, hi, wins/losses/ties, both anchors). Wilcoxon p order of magnitude agrees (1e-18 and 3e-3); the residual is consistent with a 0.5 continuity correction in the helper (inferred, helper source not read). ITEM 8 DONE -> [x]
[6] 15:34 driver test :3935 also asserts `code.index("CANARY_MAX_SECONDS") < code.index('rm -f "$TOKEN"')` — canary<token is pinned SEPARATELY from trap<token (:3930). :3950 asserts "--overwrite" not in comment-free code. Both spelled pins (reviewer A minor 11 acknowledged in the docstring :3904).
[10] 15:34 prompt/checkpoint pins observed in chunks: rtg_first == target_rtg == -20625.0 on 500/500 dt cells; training artifact docs/data/p7_3b_anchor_training.json sha256 edca65f96f060d77a8ef9620deaa2c4a76c4c77eae0f7a9e05c47587a06dd62a == every dt chunk's anchor_training_sha256; per seed 101..505 checkpoint sha256 in training artifact == sha256 of output/p7_3b_anchor/checkpoints/anchor_dt_seed{s}.pt on disk == every chunk's checkpoint_sha256 (5/5 all_equal=True). Training artifact target_rtg -20625.0, rtg_scale 25257.0.
[10] 15:40 prompt by a FIFTH route (raw npz ix0_local_reward, RTG via reversed np.cumsum, both corpus dirs, 200 episodes x 360 steps): max episode return -20625.0 == training artifact target_rtg; largest |RTG| 25257.0 == rtg_scale; mean return -23099.165 (packet: -23,099.17). Training artifact: n_episodes 200, state_dim 25, n_actions 8, declared_gradient_steps 40000, raise_to null, recipe {context 20, 3 layers, 1 head, d_model 128, dropout 0.1, batch 64, AdamW lr 1e-4 wd 1e-4, warmup 1000, grad clip 0.25} = A18(a) verbatim (recipe block read from the artifact, not from DTConfig source).
[10] 15:40 logs: train.log line 1 "=== commit b35216ca79737ed945b24655d0e19652708e00ab started 2026-09-18T21:10:58+02:00"; canary.log: 0.77 s (2026-09-18T21:10:58) and 0.87 s (2026-09-19T11:31:19), both halves identical values (att_horizon 247.75089149261333, two_routes_agree true); report_anchor.log "wrote .../artifacts/p7_3b_anchor.json with 700 cells". Artifact: git_commit f13358eb..., git_dirty False, stage anchor, n_chunks_outside_stage 0, cell_set_source declared_cells(), n_cells_declared 700, chunk_commits_by_stage {anchor: [f13358eb...]}; what_this_does_not_say carries the "k = 200 endpoint" sentence and the online-anchor limitation. tests/ references to p7_3b_anchor.json: exactly 3 (:3770 tmp out-path, :3891-3892 source text) as the packet §6 states.
[10] 15:40 "TRAINING COMPLETE in 1095s" (packet §5.2) grep over output/p7_3b_anchor and output/p7_3b_runs: /home/filip/rltraffic/output/p7_3b_runs/train_capture.txt /home/filip/rltraffic/output/p7_3b_runs/train_anchor.sh TRAINING COMPLETE in 1095s echo "TRAINING COMPLETE in ${ELAPSED}s" | tee "$WORK/COMPLETE" 
[10] 15:43 "TRAINING COMPLETE in 1095s" found in output/p7_3b_runs/train_capture.txt (the training driver's capture, not logs/train.log) — packet §5.2 claim verified.

### [10] SUMMARY
A. Code-diff items that could make a published NUMBER wrong: NONE FOUND. Every published anchor quantity was reproduced from the raw chunks by a route that imports nothing from offline/: 1000/1000 per-cell rho (both definitions), 10/10 per-seed means, 8/8 aggregate mean/std/CI values, paired ATT vs both anchors (mean, both CI bounds, wins/losses/ties), Wilcoxon p to order of magnitude; the prompt (-20625.0 / 25257.0) by a fifth route from raw npz; checkpoint and training-artifact digests closed chunk->artifact->disk.
B. Claimed but NOT verified by me (read from packet/coordinator, not re-run): byte-identical regeneration of the artifact (§5.7); 600 bit-identity comparisons of the 200 re-rolled denominators against P7.3a (§5.9); the full-suite count 2136/94 (I ran only tests/test_anchor_training.py [25 tests] and tests/test_transfer_curve.py [136 tests], mostly under mutation, no clean control run of test_anchor_training.py); the five-draw re-collection check, the disjointness JSON, A17(f) (§11); the pilot commit 5849d59's prose-only difference (dangling, unverifiable by anyone); the 13 hand-killed mutations of 2026-09-18 (excluded by the request); DTConfig source values (recipe read from the training artifact only); the Wilcoxon helper's continuity correction (inferred from the residual, source not read).
C. Contradicted by disk: NOTHING. Every spot-check (digest, COMPLETE, n_failed, capture first line, provenance uniformity, SHA256SUMS x2, test delta 23/0, canary lines, train commit b35216c, TRAINING COMPLETE 1095s) matches the packet.

## VERDICT: PASS-WITH-FINDINGS
BLOCKING: none.
MAJOR:
- [3] tests/test_transfer_curve.py:4053 test_the_pilot_transcript_survives_a_cell_that_has_no_training_seed is tautological: it re-implements the guarded set expression inside the test and never calls run_pilot; mutation M2 (offline/transfer_curve.py:2624 guard removed) SURVIVED the whole file (136 passed). The driver header (offline/campaigns/p7_3b_anchor.sh:81-84 "Fixed, and pinned by a test") and packet §10.1 overstate the pin. Cannot affect a published number (the transcript is the pre-flight's rate record), but the packet's claim that the defect is pinned is false as written.
MINOR:
- [6] Brief §3.5 lists stages collect -> re-collection check -> train -> cells -> report; the shipped driver runs presence checks + a dry-run digest re-verify for the first three and rolls only cells+report (header §3 documents this; accepted at pre-flight). Documented deviation, not a defect.
- [5] report's published_arms refusal (offline/transfer_curve.py:1965-1970) is unreachable with delivered code (every chunk passes validate_cell_payload :1616/:1650, which refuses undeclared arms at :1033) — M4a EQUIVALENT. Belt-and-braces; the reachable last refusal (FENCED_KEY scan) IS pinned (M4b KILLED by :1152).
- [8] Packet §5.5's "bitwise" agreement for the paired ATT holds only on the helper's aggregation route (np.mean over seeds of the arm's ATT, then minus the anchor); Python sum/5 differs in the last ULPs (5.124633990863352 vs 5.124633990863357). Not wrong; the route should be named next to the word "bitwise", as §13 already does for the per-seed mean.
- [7] The per-seed rho mean is the one aggregate computed with Python's sum (transfer_curve.py:1767 per packet) while every other mean uses numpy — already raised by the packet as open question §17.2; concur.
Checklist items not reached: none (1-10 all ticked). Mutation worktree removed; both repository trees `git status --porcelain` empty at the end.
