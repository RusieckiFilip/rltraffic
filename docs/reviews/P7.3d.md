# MERGE REVIEW — P7.3d (`BRIEF_39` gate G9) — 2026-09-25 — VERDICT: PASS-WITH-FINDINGS (0 blocking · 1 major · 7 minor)

**One review (A21(d)), run as two ≤ 15-minute mandates with findings files (§7's rule for long reviews):** mandate **A — the numbers**
(every published number recomputed from the 700 raw chunks by the reviewer's own route, nothing imported from `offline/`) and mandate **B
— the code and its mutations** (the critical path by reading; four named mutants and one of the reviewer's own). Both on the branch at
`4b0c4f0`. Both findings files are filed verbatim below the rule.

## Coordinator's ruling (2026-09-25)

1. **MERGE.** Nothing blocks. **A:** every published number reproduces — per cell 500 / 500 on both definitions, per draw, per seed, the
   registered mean / sd / CI, the paired ATT and the reviewer's own Wilcoxon, A23(d)'s robustness block, the collision record (exactly the
   two events), the 16 ρ_i with their diagnostics, every row of the packet's §28.6–§28.10, the 16 targets from the probe means, the
   in-support counts from the raw RTG series, and A21(b)'s and A23(d)'s sentences verbatim against the registration; last-ULP differences
   appear only on routes with another summation order and reconcile exactly with the artifact's. **B:** the critical path conforms (the
   scenario keying admits no crossover; A23's rule; `report`'s step 5b before any aggregate and any write; the reference-cell refusal before
   the write; ONE estimator function for the primary and the robustness block; the robustness set removed whole; per-intersection ρ a ratio
   of means); **B16, B05, B03b and B08 KILLED**; test hygiene clean; T-regress (b) byte-identical at the tip.
2. **B's MAJOR — A23(f)'s "exactly one event in each A23 cell" is untested** (the reviewer's mutant `!= 1` → `< 1` SURVIVED): **parked as
   `DEFERRED` 94** with its one-test fix. No published number depends on it: the code at `4b0c4f0` is correct, and the coordinator's G6 read
   confirmed exactly one event in each A23 cell by its own route. A test commit now would move `tests/` past the chunks' commit for no gain
   in the published record.
3. **A's four MINORs — wording and labelling; no number is wrong — recorded for the paper** in `docs/notes/P7.3d_CAMPAIGN_READ_2026-09-24.md`
   §6; the artifact's strings are final and are not rewritten:
   - **(A-1)** the co-reported `att_env` row of clause 1 carries the status *CONFIRMATORY*; the paper states clause 1 as confirmatory on
     `E_sumo`, A15's primary (A23(d): *that primary alone decides clause 1*), with `att_env` co-reported;
   - **(A-2)** **clause 3 on grid4x4 is NOT TESTED by registration** (A20(e), A21: no few-shot point and no anchor on this scenario).
     A19's reason for voiding it on hz1x1 — no gap to MaxPressure — does NOT hold here: clause 2's inequality holds, so the gap clause 3
     presupposes exists. The paper gives that reason, not A19's. **It bears on the author's pending decision about P7.3c: grid4x4 is the
     scenario on which H3's clause 3 is testable at all;**
   - **(A-3)** neither the artifact nor the packet names H3's multiplicity family or a Holm-adjusted p; at p = 3.96e-18 no Holm multiplier
     moves the decision; the paper names the family and reports the adjusted value;
   - **(A-4)** `what_this_does_not_say[2]`'s *"no model … trained on this scenario"* means the SUMO target domain — the subject WAS trained
     on grid4x4's CityFlow side (P5.2); the paper says *on this scenario's SUMO side*.
4. **B's three MINORs noted:** the same-step match is exact float equality on `time`, sound because the recorder stamps both lists with the
   same snapshot time (stated in the docstring, not asserted); A23(f)'s check counts events per cell rather than their identity, as
   B.8-2(4)(d) specifies — the identity was G6's check; B08 kills two of the three T-regress tests (still killed).
5. **B's deviation — mutants run UNCOMMITTED:** the harness's trailer instruction reached the reviewer too, so it raised it (`CLAUDE.md` §4b)
   and created no commit at all. It showed first that a dirty no-op control still passes (the fixture's `_snapshot_clone` commits the working
   copy inside its own clone). Accepted.
6. **Observations kept, neither a finding:** `episode_reward` differs from `sum_i local_return[i]` on 691 / 700 cells, by −14 to 0 — the
   global reward counts lanes no intersection's local reward counts; not a registered quantity, and the paper does not equate them. The CI
   is the registered normal form 1.96·s/√n, as on hz1x1; a t-quantile would widen it by about 1.2 %, and no decision moves. **A's final report adds:** the artifact averages seeds with Python 3.12's compensated `sum()` for ρ and with a
   plain left-to-right sum for the paired ATT, so the two blocks can differ by up to 2 ULP — `DEFERRED` 85's summation-order class, not an
   error; and mandate A ran well past its 15-minute box (its findings file was complete before its report).
7. **The coordinator's own addition, `DEFERRED` 95:** the grid4x4 artifact has no T-regress; it regenerated byte-identically at G6, but the
   suite does not pin it.

---

# Mandate A — THE NUMBERS — findings file, verbatim

# G9 mandate A (THE NUMBERS) -- P7.3d merge review -- findings record
Reviewer: independent (Claude agent), 2026-09-25. Read-only on repo; own route (stdlib + numpy + json), nothing from offline/.

## Checklist
- [x] 1. IDENTITY: committed artifact sha256 == campaign artifact == manifest line; manifest re-verifies (sha256sum -c); every chunk git_commit 10d8fe8... and git_dirty false.
- [x] 2. PER CELL under == first: own rho for 500 DT cells x 2 defs vs artifact cells rows (rho_e_sumo, rho_att_env).
- [x] 3. AGGREGATES: per-draw (100), per-seed (5), registered mean/sd/CI on both defs vs artifact rho and h3 blocks; ULP counts + reconciling summation order if last-bit differences.
- [x] 4. PAIRED ATT vs both anchors on both defs: n, means, mean diff + CI, lower/higher counts, own Wilcoxon p vs artifact.
- [x] 5. A23: robustness block (98 draws) every estimator; collision record exactly two events (fixedtime 1020, b_mean_k100 seed 303 draw 1042), no other cell.
- [x] 6. PER-INTERSECTION block: every rho_i, three mean returns, diagnostic fields, own route.
- [x] 7. PACKET TABLES (s28.6 targets/scales/in-support; s28.7; s28.9; s28.10 incl. probe ratio + delta-method SE with covariance, count "ten more than 4 SE below 1.0").
- [x] 8. REGISTERED READING: H3 clauses vs A20(e), A23(d), A19; A21(b) two scope sentences verbatim; any interpretation beyond registered inequalities.
- [x] 9. Anything else that could make a published number wrong.

## Findings (appended one per entry)

### [1] IDENTITY -- 2026-09-25 (entry 1)
- `git -C /home/filip/rltraffic-p73d rev-parse HEAD` -> 4b0c4f08eb0f97a95fbc0d43de8c4733825ab93a (the named commit); worktree clean.
- `sha256sum` committed docs/data/p7_3d_grid4x4.json AND output/p7_3d/artifacts/p7_3d_grid4x4.json -> both c63c371ff14d208d16b9fbfa6d3daa31975679c90b20b5a4b44fbed60760b0b7; SHA256SUMS_p7_3d.txt line 1 carries the same hash for p7_3d/artifacts/p7_3d_grid4x4.json.
- `cd output && sha256sum -c --quiet SHA256SUMS_p7_3d.txt` -> exit 0 (960 lines: 700 cells + COMPLETE/canary + artifact + 200 calibration + g2 checks).
- All 700 cell_*.json: git_commit 10d8fe8172cac067af504b9616199eadb524c39d, git_dirty False (700/700), format_version p7.3d-grid4x4/1.1 (700/700), stage grid4x4_confirmatory (700/700). Arms: b_mean_k100 x seeds 101/202/303/404/505 x 100 each, fixedtime 100, maxpressure 100. cells/ also holds COMPLETE and canary.json (not cells, expected).
- JUDGEMENT: PASS. No finding.
- [x] item 1 ticked.

### [2] PER CELL under == -- 2026-09-25 (entry 2)
- Script scratchpad/g9a_item2.py (own loader g9a_load.py; json+stdlib; keyed by (arm, seed, draw_id) from the chunk bodies, not filenames).
- rho_c = (ATT_ft,d - ATT_c) / (ATT_ft,d - ATT_mp,d) evaluated in that operation order, per definition.
- Result: e_sumo 500/500 `==` artifact `cells[].rho_e_sumo`; att_env 500/500 `==` `cells[].rho_att_env`; 0 !=, 0 null (no zero denominator: e_sumo den in [130.4935, 141.3502], att_env den in [130.4943, 141.3525]).
- Also: every non-rho field of all 700 artifact `cells` rows equals the same field in the raw chunk (0 mismatches, 0 missing) -> the artifact's cell table IS the chunks (minus series).
- Anchor rows carry rho 0.0 (fixedtime, 100) and 1.0 (maxpressure, 100) on both definitions -- consistent with the formula (fixed-time = 0, MaxPressure = 1 by construction, sec 3.4).
- JUDGEMENT: PASS. No finding.

### [3] AGGREGATES -- 2026-09-25 (entry 3)
- Script scratchpad/g9a_item3.py; three per-draw routes: Python-3.12 built-in sum over seeds 101..505 in order / 5 (sum() of floats is Neumaier-compensated since 3.12), math.fsum / 5, numpy mean.
- by_draw (100 draws x 2 defs): built-in-sum route and fsum route `==` 100/100 on e_sumo AND att_env; numpy.mean route == 84/100 (e_sumo), 81/100 (att_env), max 2 ULP. n_seeds = 5 on every draw.
- by_seed (5 seeds x 2 defs): built-in sum / 100 `==` on all 10; numpy.mean differs by 1-2 ULP on seeds 303/404/505. n_draws = n_draws_e_sumo = n_draws_att_env = 100 on every seed.
- registered_arm: with per-draw by compensated sum and then numpy mean / std(ddof=1) / 1.96*sd/sqrt(n) over the 100 per-draw values -> mean, std, ci95, ci95_low, ci95_high, n_draws ALL `==` on BOTH defs. (Pure-Python stats instead of numpy: e_sumo still all ==; att_env mean/low/high 1 ULP off. numpy per-draw route: e_sumo std/ci95 2 ULP off.) Reconciling order: per-draw = compensated (fsum-equivalent) seed sum /5; aggregate = numpy pairwise mean + std(ddof=1).
- Own values: e_sumo mean 0.8854642679270011, sd 0.06059162815347192 (ddof 1), CI [0.8735883088089206, 0.8973402270450815]; att_env mean 0.8854600302820783, sd 0.0605913027186118, CI [0.8735841349492304, 0.8973359256149263]; n = 100 each. Largest route disagreement anywhere: 2 ULP (~2e-16) -- immaterial, never touches any printed digit.
- h3 clauses (primary and robustness): mean_rho / ci95_low / ci95_high / n_draws `==` rho.registered_arm on the same definition; point_estimate_satisfies == (mean > 0), ci95_entirely_satisfies == (ci95_low > 0); clause-2 inequality mean < 1 and CI high < 1 hold on both defs.
- JUDGEMENT: PASS. No numeric finding. (Labelling note carried to item 8: the att_env clause-1 row's status string also says "CONFIRMATORY".)

### [4] PAIRED ATT vs both anchors, both defs -- 2026-09-25 (entry 4)
- Script scratchpad/g9a_paired.py: left = DT seed-mean ATT per draw, right = anchor ATT same draw, diff = left - right; mean/sd(ddof 1)/1.96*sd/sqrt(n); median; own Wilcoxon (zeros dropped, average ranks, tie term sum(t^3-t)/48, continuity 0.5, normal approx, p = erfc(|z|/sqrt 2)), rank-biserial (W+ - W-)/(W+ + W-).
- Primary (100 draws), all 4 comparisons: n_shared_draws 100, mean_left, mean_right, wins, losses, ties, rank_biserial, wilcoxon_p ALL `==` the artifact. Own Wilcoxon: no zero differences, no tied |d|; e_sumo & att_env vs fixedtime W+ = 0, W- = 5050; vs maxpressure W+ = 5050, W- = 0; z = -8.680051067684394; p = 3.9559116088995575e-18 `==` artifact on all 4 (the smallest p a 100-pair normal-approx test can give: the DT is lower than fixed-time on 100/100 draws and higher than MaxPressure on 100/100 draws, on both defs).
- mean_difference / ci95_low / ci95_high / median_difference: `==` when the DT seed mean is a plain left-to-right sum /5 (or numpy.mean of the 5); with a compensated seed sum they differ by 1-2 ULP. NOTE: the rho by_draw block reconciles only with the COMPENSATED seed sum (item 3) while paired_att reconciles only with the NAIVE seed sum -- the artifact uses two summation routes for "the seed mean"; <= 2 ULP (~3e-14 s on ATT), immaterial, recorded as a curiosity, not a finding.
- Values (e_sumo): DT 183.6173 vs ft 304.0605, diff -120.4432 [-122.0971.., -118.79..] per artifact; vs mp 168.0313, diff +15.5860 [+13.9682, +17.2038]. att_env: DT 183.5536, ft 303.9980, diff -120.4444 [-122.0869, -118.8019]; mp 167.9668, diff +15.5868 [+13.9690, +17.2046].
- Holm: the artifact reports no Holm-adjusted p; with p = 3.96e-18 any Holm multiplier over H3's family leaves the decision unchanged (<< 0.05). Not a numeric finding.
- JUDGEMENT: PASS.
- CORRECTION to entry 4 (my own transcription, not the artifact): the e_sumo vs fixedtime CI I typed from memory was wrong; the primary values printed from the artifact are e_sumo ft: diff -120.4432 [-122.0857, -118.8007], median -121.0292; e_sumo mp: +15.5860 [+13.9682, +17.2038], median 15.4380; att_env ft: -120.4444 [-122.0869, -118.8019], median -121.0289; att_env mp: +15.5868 [+13.9690, +17.2046], median 15.4392. All reproduced by my route (== or <= 2 ULP as stated above).

### [5] A23 robustness (98 draws) + collision record -- 2026-09-25 (entry 5)
- Script scratchpad/g9a_item5.py. draws_removed [1020, 1042]; n_draws 98; n_cells_removed 14 (own count of chunks on those draws: 14 = 5 DT + 2 anchors, x 2 draws).
- rho by_draw 98/98 `==` on both defs (and each row identical to the primary's row for that draw); by_seed 5/5 `==` on both defs, n 98; registered_arm mean/std/ci95/low/high/n `==` on both defs. Own: e_sumo 0.8853966302464391 [0.8733261706446781, 0.8974670898482001], sd 0.06096502740316784; att_env 0.8853923979445033 [0.873321982785876, 0.8974628131031306].
- Denominator diagnostics (primary 100 and robust 98, both defs): min/max `==`, n_non_positive 0, n_below_one_second 0, draw_ids_non_positive [] -- all reproduced.
- paired_att (4 comparisons): n 98, wins/losses/ties/rank_biserial/wilcoxon_p `==` (own W+/W- = 0/4851, z = -8.593174194274377, p = 8.459913539251515e-18); means/diffs/CIs/median == or <= 2 ULP (same seed-mean summation-route split as item 4).
- A23(d)'s three criteria: sign of mean rho same (+ both), CI excludes 0 same (both), Wilcoxon decision same (p 3.96e-18 vs 8.46e-18) -> robustness agrees on all three, on both defs. h3 in the robustness block equals its rho block (item 3 check).
- `a23_d` string = A23(d) text of PREREGISTRATION.md:380 with markdown bold stripped (textual equality checked in item 8). `not_recomputed` excludes per-intersection and in-support blocks as "on neither E_sumo nor att_env" -- consistent with A23(d)'s wording "on E_sumo and att_env".
- COLLISIONS from the raw chunks: exactly 2 cells with n_collisions > 0 or a non-empty collisions list: fixedtime draw 1020 (collider 628 on D0right0_0 at t 2299, victim 969) and b_mean_k100 seed 303 draw 1042 (collider 1126 on A0left0_0 at t 2628, victim 1098); both collider_fate "arrived_at_collision_step"; each exactly 1 teleport = the collider at the collision time, n_explained_teleports 1, n_unexplained 0. No other of the 700 cells has a collision or a teleport; 0 cells with vanished/pending/never-entered or n_created != n_entered. Artifact collisions block: 2 cells, 2 events, per_arm 500/1, 100/1, 100/0, event dicts equal to the chunk dicts plus arm/cell/draw/seed labels.
- JUDGEMENT: PASS.

### [6] PER-INTERSECTION rho_i -- 2026-09-25 (entry 6)
- Script scratchpad/g9a_item6.py. Rbar_dt,i = mean over 100 draws of (seed-sum/5 of local_return[i]); Rbar_ft,i, Rbar_mp,i = anchor means; rho_i = (Rbar_ft - Rbar_dt)/(Rbar_ft - Rbar_mp); gap_d = R_mp,d - R_ft,d; se = sd(ddof 1)/sqrt(100).
- intersection_ids order A0..D3 (sorted) identical in the artifact and in all 700 chunks; local_return == local_return_from_lanes on 700/700 cells (the two routes agree).
- All 16 rho_i and all 48 mean returns `==` the artifact with Python-3.12 compensated sums (numpy-mean route: <= 2 ULP). All diagnostic fields (denominator, mean_gap, mean_gap_se, min_gap, max_gap, n_draws, n_draws_mp_not_better = 0, draw_ids_mp_not_better = []) `==` for all 16. rho_null list empty, n 16.
- Own rho_i: A0 0.0874, A1 1.0417, A2 1.0245, A3 0.9833, B0 0.8989, B1 1.0095, B2 1.0375, B3 0.9093, C0 0.5547, C1 1.0398, C2 1.0389, C3 1.0335, D0 0.9655, D1 1.0477, D2 1.0445, D3 0.9260. Count in [0.90, 1.05]: A1 A2 A3 B1 B2 B3 C1 C2 C3 D0 D1 D2 D3 = 13 (B0 0.8989 is outside) -- agrees with the coordinator's corrected "THIRTEEN" (commit 2bdcbdb), not with the uncorrected "fourteen".
- Side observation (not a registered quantity): episode_reward != sum_i local_return[i] on 691/700 cells; episode_reward - sum is in [-14, 0] (means -4.5 ft, -3.8 mp, -5.1 DT). Consistent with the global reward counting lanes no intersection's local reward counts. rho_i uses local_return, as its definition states; nothing published uses episode_reward as far as grep of P7.3d.md shows (lines 1289, 1318 only, reference-cell context). Not a finding.
- JUDGEMENT: PASS.

### [7] PACKET TABLES -- progress note 2026-09-25 (entry 7a)
- Read P7.3d.md 2048-2241 (s28.6-s28.11). Sec 28.7's printed values already cross-checked against my recomputation in items 3-4 (mean/CI/sd/n on both defs, per-seed four-decimal values pending check, paired ATT, p 3.96e-18 at n=100 and 8.46e-18 at n=98). Remaining in item 7: s28.6 table (R_best, S_cf, S_sumo, target, scale, support, position, inside/above/below; "0 of 8,000 pairs"), s28.7 per-seed and "500/500, 441/500, 440/500", s28.9 table, s28.10 table (ratio, delta-method SE with covariance, count of ids more than 4 SE below 1.0, "rho_i - ratio" column, and the in-support share column).

### [7] s28.6 targets/scales/in-support -- 2026-09-25 (entry 7b)
- Script scratchpad/g9a_item7a.py (calibration sha256 3e9df8ee..., matches the chunks' calibration_sha256).
- Own S_cf,i / S_sumo,i = mean of probe_returns[engine][201..300][i] `==` probe_source_stat / probe_target_stat on all 16; own target = R_best * (S_sumo / S_cf) `==` budgets.k100.target on all 16 (the (R*S)/S order is 1 ULP off on 6 -- the recorded order is R*(S/S)); rtg_scale = -support_range[0] on all 16; every target inside [-scale, 0]; artifact in_support registered_target/support_range/target_position equal the calibration.
- Every printed cell of the s28.6 table (R_best, S_cf, S_sumo to 2 dp, target to 4 dp, scale, support, position, inside/above/below) matches my values for all 16 rows (checked row by row).
- Prompt read-back: target_rtg != k100 target in 0/8000 (chunk, id) pairs; rtg_first and rtg_series[0] != target in 0; chunk support_range != calibration in 0 -> the packet's "0 of 8,000" reproduced.
- In-support counts from the raw rtg_series (lo <= x <= hi inside; > hi above; < lo below): equal to every chunk's in_support_counts and n_decisions_in_support (0 mismatches) and to the artifact's in_support block for all 16 (n_decisions 180000, n_cells 500); strict vs inclusive bounds make no difference on any pair; below = 0 everywhere; above range 72,231 (D0) .. 88,318 (B3) as printed.
- JUDGEMENT: PASS.

### [7] s28.7, s28.9, s28.10 -- 2026-09-25 (entry 7c)
- Scripts scratchpad/g9a_item7b.py + an inline formatter over the artifact (values already reproduced from chunks in items 3-6).
- s28.7: rho table (+0.8855 [+0.8736, +0.8973], sd 0.0606, n 100, both defs), full-precision lines, per-seed 101 +0.8272 / 202 +0.8947 / 303 +0.8962 / 404 +0.8914 / 505 +0.9178 (att_env equal at 4 dp: true on all 5), denominator 130.49-141.35 with 0 non-positive / 0 below 1 s, paired-ATT table (all 4 rows: DT, anchor, diff [CI], 100/0 or 0/100, p 3.96e-18), p floors 3.9559116088995575e-18 (n 100) and 8.459913539251515e-18 (n 98) -- ALL match. Single cells from raw chunks: DT < ft in 500/500 on both defs; MP < DT in 441/500 (e_sumo) and 440/500 (att_env), 0 ties -- match.
- s28.9: robust +0.8854 [+0.8733, +0.8975] on both defs; paired -120.43 / 98-0 / 8.46e-18 and +15.60 / 0-98 (e_sumo); att_env -120.44 / +15.60; primary columns -- ALL match.
- s28.10 table: my own row-by-row reproduction (rho_i 3 dp, three means 2 dp, denominator 2 dp, probe ratio S_sumo/S_cf 4 dp, SE 4 dp, (r-1)/SE 1 dp) matches all 16 printed rows digit for digit. SE formula that reproduces them: SE = sqrt((s_y^2 - 2 r s_xy + r^2 s_x^2)/n)/|xbar|, ddof 1, n 100 paired probe draws (x = cityflow, y = sumo).
- s28.10 claims: 12 below 1.0 / 4 above -- yes; range 0.834 (A3, -16.6 %) to 1.042 (A2, +4.2 %) -- yes; sd of the 16 = 0.0678 (ddof 1; ddof 0 would be 0.066) -> "0.068" ok; median SE 0.0123 -> "0.012" ok; count more than 4 SE below 1.0 WITH covariance = 10 {A3, B3, D0, D1, D3, C3, B0, C0, D2, A0} -- "ten" CONFIRMED (B.3-4's "eleven" is wrong under this form, as the packet says); WITHOUT covariance = 9 -- "nine" confirmed; no-cov SEs larger by 0.9 % (B1) .. 21.1 % (D1), median 0.0133, same sign on all 16 -- confirmed; above 1.0: B1 +0.3, C1 +2.4, C2 +2.7, A2 +3.3 -- yes; B2 -3.0, A1 -2.8 -- yes; A0 / C0 ratio ranks from bottom 10th and 8th -- yes; rho_i of A3,B3,D0,D1,D3 = 0.983, 0.909, 0.966, 1.048, 0.926 -- yes; A0 DT = 92.7 % of ft, 5.7x MP (5.665) -- yes; denominators -1,279.05 .. -797.19 -- yes; 13 of 16 in [0.90, 1.05], highest D1 1.048 -- yes.
- Not verifiable from the chunks: s28.9's "labels are one step after SUMO's own stamps (2,298 and 2,627)" (SUMO's own collision stamps are not in the chunks). Listed under could-not-verify.
- JUDGEMENT: PASS -- every number in s28.6, s28.7, s28.9, s28.10 that I checked reproduces.

### [8] REGISTERED READING -- 2026-09-25 (entry 8a)
- Verbatim (python re over PREREGISTRATION.md lines 383/380 vs artifact strings): A21(b)(i) == what_this_does_not_say[0] TRUE; A21(b)(ii) == what_this_does_not_say[1] TRUE; A23(d) text (markdown bold stripped) == robustness.a23_d TRUE. arms.not_evaluated = a_q1.0, b_max_k100, naive, random (A20(b), A21(b)) -- matches s28.11.
- h3 block vs A20(e)/A23(d)/A19: primary_definition e_sumo; clause 1 `rho_sumo(b_mean_k100) > 0` CONFIRMATORY; clause 2 `< 1` "reported as the inequality it is; not scored"; clause 3 void (A19) -- matches A20(e)'s words.
- MINOR (labelling): h3.clauses[1] (att_env, clause 1) carries status "CONFIRMATORY on this scenario (A20(e) ...)" -- the same status string as the e_sumo row. A20(d)/A15 make att_env co-reported and A23(d) says "that primary alone decides clause 1". The row also carries the caveat "CO-REPORTED, NOT THE REGISTERED PRIMARY (A15)" and h3.primary_definition = e_sumo, so it is not a wrong number, but a reader copying the clause table could present two confirmatory clause-1 tests (a Holm-family question under sec 8). Evidence: artifact h3.clauses[1].status.
- MINOR (reason for clause 3): A19(a) voids clause 3 because its premise -- a positive gap to MaxPressure -- was false on hz1x1 (rho > 1). On grid4x4 that premise HOLDS (clause 2: rho 0.8855, CI entirely < 1). A20(e) (registered before the number) nonetheless says "clause 3 is void (A19)", and the artifact follows it but gives a different, accurate reason ("not tested on grid4x4 -- no few-shot point and no anchor exist"). The paper must not import A19's "premise false, cannot fail" reasoning to grid4x4; there the clause is UNTESTED by scope, not void by a false premise. Not a number error; a sentence hazard. Evidence: PREREGISTRATION.md:385 (A19(a)), :384 (A20(e)), artifact h3.clause_3.
- MINOR (sec 8 multiplicity): neither the artifact nor s28 reports a Holm-adjusted p or names the size of H3's family, although A23(d) names "sec 8's Wilcoxon-Holm decision" as one of its three criteria and s28.9 reports it as "the Wilcoxon decision". With p = 3.96e-18 (8.46e-18) no plausible family size changes the decision, so no number moves; the adjusted p is simply absent.

### [8] REGISTERED READING -- 2026-09-25 (entry 8b, closes item 8)
- h3.hypothesis equals the H3 wording quoted in A19(a) (ASCII dashes); h3.test_row equals sec 2's H3 row plus the unit.
- Interpretation scan of s28.8, s28.10, s28.13, s28.19 (read at P7.3d.md 2127-2226, 2286-2313, 2411-2437) and the artifact strings: s28.8 reports the inequalities with the artifact's own "reported_not_interpreted" sentence; s28.10 is labelled and written as descriptive; s28.19 says the paper "interprets neither beyond what H3's rows register". I found no sentence that scores clause 2, promotes a per-intersection number, or draws a causal claim.
- MINOR (wording): what_this_does_not_say[2] says "no model was fine-tuned and no anchor was trained on this scenario". The subject WAS trained on this scenario's CityFlow side (P5.2, scenario_key cityflow_grid4x4); the sentence means the SUMO target domain. As a paper sentence it can be misread as "no grid4x4-trained model". Evidence: artifact what_this_does_not_say[2].
- [x] item 8 ticked.

### [9] OTHER -- 2026-09-25 (entry 9)
- Pairing (sec 8 paired design): on every draw all 7 cells share engine_seed_requested (1000), engine_seed_drawn, routes_sha256, config_sha256, n_created, calibration_sha256, time_to_teleport_option (0 draws disagree); 100 distinct route files over the 100 draws; one engine seed (A20 "one engine seed" limit, A18(c)).
- Checkpoint sha256 prefixes per seed = A20(a)'s (329fb6b8, f5413585, 48076dab, 4b61bc06, 09bd310d), one checkpoint per seed across its 100 cells.
- Every cell: decisions 360, actions 360 x 16, actions_in_range; halting cross-check on the 7 cells of draw 1000, 0 disagreeing lane-seconds; rtg_advanced_every_decision true on all 8,000 DT (cell, id) pairs.
- ATT-pair internal identity (my own route): e_sumo == p_sumo and att_env == w_sumo == att_horizon on 700/700; e_sumo - att_env == mean_depart_delay to 4.8e-14 s on all 700 (the two registered definitions differ only by the pool-clock departure delay, 0.031-0.104 s) -- consistent with rho differing only in the 5th-6th decimal between the definitions.
- s28.19/s28.20 facts: artifact 4,564,660 bytes (stat) -- matches; SHA256SUMS_p7_3d.txt sha256 begins 1d7a7f89, 960 lines -- matches. reference_cells: all_reproduce true, n_checked 6, artifact sha 5265f0d5 == on-disk p7_3d_reference_cells.json. chunk_commits_by_stage = {grid4x4_confirmatory: [10d8fe8...]}; n_chunks_outside_stage 0.
- Estimator note (not a finding): the CI is the registered/pinned normal form 1.96*s/sqrt(n) (as on hz1x1); a t-quantile (1.984 at 99 df) would widen the half-width by ~1.2 % (0.01188 -> 0.01202); no decision moves.
- Coordinator's G6 note (read only after my numbers): rho, CIs, per-seed, paired values, 500/500, 441/500 (440 att_env), 13 in [0.90, 1.05] as corrected, the rho_i list -- all equal to mine.
- JUDGEMENT: nothing further that could make a published number wrong.
- [x] item 9 ticked.

## Summary (assembled from the entries above)
VERDICT: PASS-WITH-FINDINGS -- 0 BLOCKING, 0 MAJOR, 4 MINOR (all wording/labelling; no published number is wrong).
- MINOR-1: h3.clauses[1] (att_env) status string "CONFIRMATORY" (entry 8a).
- MINOR-2: clause 3 "void (A19)" on grid4x4 although A19's premise (no gap to MaxPressure) is false there; artifact's own reason is "not tested" (entry 8a).
- MINOR-3: no Holm-adjusted p / family size reported although A23(d) names the Wilcoxon-Holm decision (entry 8a).
- MINOR-4: what_this_does_not_say[2] "no model ... trained on this scenario" (entry 8b).
Could not verify: E_sumo/att_env from per-vehicle data (chunks carry aggregates only); local_return from per-lane data (only its two recorded routes agree); probe returns and R_best (taken from the calibration as recorded); SUMO's own collision stamps (+1 s label claim); hz1x1 numbers quoted in A19/A21(b)/the att_env caveat; attempt-1 blind comparison; code/mutations (mandate B); pytest not run (forbidden by the request).


---

# Mandate B — THE CODE AND ITS MUTATIONS — findings file, verbatim

# G9 mandate B -- P7.3d merge review: THE CODE AND ITS MUTATIONS
Branch task/p7.3d-grid4x4 @ 4b0c4f0 ; reviewer started 2026-09-25

## Checklist
- [x] 1. hz1x1 UNCHANGED: run tests/test_t_regress_artifacts.py (3 tests) at tip; read where each grid4x4 branch is keyed (scenario) and whether hz1x1 chunk could reach grid4x4 path or reverse.
- [x] 2a. CRITICAL PATH read: _validate_collision_record A23 rule (same-time collider/victim; unexplained refused; unregistered fate stop; vanished-no-collision refused)
- [x] 2b. report step 5b before any aggregate and any write
- [x] 2c. _grid4x4_estimates used by BOTH primary and robustness block
- [x] 2d. robustness set removes draws 1020/1042 WHOLE
- [x] 2e. per-intersection block ratio of means (B.7.2-1) + diagnostic
- [x] 2f. reference-cell check refuses before write
- [x] 3.0 MUTATIONS control: target tests unmutated all pass
- [x] 3a. B16 primary depends on events -> KILLED?
- [x] 3b. B05 robustness set from DT cells only -> KILLED?
- [x] 3c. B03b same-step match loosened to any step -> KILLED?
- [x] 3d. B08 hz1x1 path given new rule (T-regress must die) -> KILLED?
- [x] 3e. own mutant on weakest line -> KILLED?
- [x] 4. TEST HYGIENE: check_test_hygiene.sh on tests/test_p7_3d_collisions.py tests/test_p7_3d_campaign_path.py
- [x] 5. anything else that could make a published number wrong / uncovered refusal
- [x] 6. cleanup: throwaway worktree removed + pruned

## Findings (appended one per entry)

### F1 [item 1, T-regress] 2026-09-25
- cmd: rt.sh tests/test_t_regress_artifacts.py in throwaway worktree @4b0c4f0 (clean, offline imported from worktree)
- observed: `3 passed in 9.57s`, exit 0.
- judgement: hz1x1 artifacts regenerate byte-identically at tip (keying read still pending -> see F-next).

### F2 [item 1, keying] 2026-09-25
- read: offline/transfer_curve.py:1089-1103 `scenario_of` (absent/None -> hz1x1; unregistered key refuses); :2023-2024 version keyed on scenario; :2042-2048 `scenario` part of the chunk identity vs the cell; :2075-2083 grid -> `_validate_collision_record`, else hz1x1's `n_teleports != 0` refusal UNCHANGED.
- judgement: hz1x1 chunk offered to a grid4x4 cell refused on `scenario` (None != cityflow_grid4x4); grid4x4 chunk missing the key is refused on format_version (grid 1.1 != hz1x1 version). No crossover path found. Item 1 DONE.

### F3 [item 2a, _validate_collision_record] 2026-09-25
- read: offline/transfer_curve.py:1912-2000. Required fields; counts vs lists (n_collisions, n_teleports, explained+unexplained); same-step match RE-DERIVED at :1963-1968 (parties keyed by float(time), collider AND victim) and compared to the recorded split :1969; unexplained != 0 refuses :1974; fate outside COLLIDER_FATES refuses as a stop :1979-1987; vanished not party to ANY recorded collision refuses :1993-2000.
- judgement: matches A23(c)(i)-(iii) and B.8-2(3). Note (MINOR): same-step equality is exact float equality on `time` -- correct only because both lists are stamped with the same snapshot time by the recorder (see recorder read below).

### F4 [item 2b, report step 5b ordering] 2026-09-25
- read: offline/transfer_curve.py:2971-3015 (validate every chunk, completeness, extra/undeclared refusal, re-validate with cell identity, filter to the stage, THEN :3014-3015 `_assert_a23_collision_cells`), digests :3017+, rows :3095-3109, reference check :3114 before `_grid4x4_artifact` :3115 and `_publish_artifact` :3130.
- judgement: 5b precedes every aggregate and every write, as B.8-2(4)(d) requires. Reference-cell check precedes the aggregate+write (item 2f, confirmed by the test at tests/test_p7_3d_campaign_path.py:2330-2343 `assert not out.parent.exists()`).
- A23(f) check counts events PER CELL (:1901-1903) but only against the stage's filtered chunks and only the COUNT, not the identity of the event (lane/collider) -- identity is left to the coordinator's G6 read; consistent with B.8-2(4)(d)'s wording.

### F5 [items 2c, 2d, estimates shared + whole-draw removal] 2026-09-25
- read: offline/transfer_curve.py:4146-4269 `_grid4x4_estimates` (rho by_seed/by_draw, registered arm e_sumo/att_env stats, paired ATT vs both anchors, denominator diagnostic, env-ATT exclusions, h3); called by the primary at :4354 `_grid4x4_estimates(rows, anchors_by_draw)` and by the robustness block at :4318 on `kept_rows`/`kept_anchors`.
- read: :4315-4317 removes by draw_id from ALL rows (DT and anchor) AND from anchors_by_draw -> whole-draw removal; :4323 n_cells_removed = len(rows)-len(kept_rows). A23_ROBUSTNESS_DRAWS_REMOVED = (1020, 1042) at :533; A23(d) carried verbatim at :536-547 (a test compares to the registration).
- read: :549-553 A23_ROBUSTNESS_NOT_RECOMPUTED -- per-intersection rho and in-support NOT recomputed (ruled B.8.1-2 D5; both are on neither E_sumo nor att_env). Consistent with A23(d)'s "every estimator the primary reports, on E_sumo and att_env".
- control run (unmutated, clean tree): FULL + V3 + T-regress(3) -> `5 passed in 29.92s`, exit 0.
- judgement: 2c and 2d hold by reading; mutation evidence below.

### F6 [item 2e, per-intersection rho] 2026-09-25
- read: offline/transfer_curve.py:3901-3999. Per intersection: seed means WITHIN a draw first (:3964-3967), then mean over draws for arm, ft, mp over the SAME draws (:3968-3972); ONE rho(mean_arm, mean_ft, mean_mp) (:3980) -> ratio of means per B.7.2-1. Exactly-zero mean denominator -> rho None + reason (B.7.5-4). Diagnostic :3981-3999 (per-draw gaps mp-ft, count/ids with gap<=0, mean gap + SE, min/max, denominator). Unpaired draws refuse (:3949-3957); duplicate seed/anchor refuse.
- judgement: conforms to B.7.2-1/-2 and B.7.5. Exploratory, not in robustness block (ruled D5).

### F7 [item 2a cont., recorder seam + observe] 2026-09-25
- read: offline/sumo_att_reference.py:1020-1055 `_simulate`: per simulated second, after simulationStep and metrics.on_sim_step, ONE observe() call carries getStartingTeleportIDList, getCollisions (via collision_facts), getEndingTeleportIDList, getArrivedIDList of THAT step -- reads only. :664-706 observe(): teleport events and collision entries both stamped with the same `now` (=getTime() after the step), so validation's exact float equality on `time` (transfer_curve.py:1965-1967) is sound by construction. Fate: arrived_now -> arrived_at_collision_step; collider not in `starting` -> UNREGISTERED (validation stops); ending_now -> put_back; else None -> in_transit_at_horizon at collision_record (:821). Earlier-step colliders later seen arrived without put-back -> UNREGISTERED (:692-693).
- dirty no-op control (a comment appended to a def line in offline/transfer_curve.py, uncommitted): FULL + V3 + T-regress -> `5 passed in 29.74s`, exit 0. => the executed-driver test's _snapshot_clone (tests/test_p7_3d_campaign_path.py:1344-1370) copies working-copy edits into a committed clone; a dirty tree does NOT trip the driver's refusal for these targets. Mutants therefore run UNCOMMITTED (deviation from the request's constraint 3, taken because the harness trailer instruction conflicts with CLAUDE.md s4b(3) "before you commit anything"; raised in the report).

### F8 [item 3a, B16] 2026-09-25
- mutant: offline/transfer_curve.py:4159 `dt_rows = [... if row["kind"] == "dt" and not row.get("n_teleports")]` (runner_b8 fragment, matched once).
- target: FULL. observed: `1 failed in 12.89s` -- `AssertionError: (303, 'e_sumo')  assert 0.586489898989899 == (58.6875 / 100)` (the by-seed mean derived by the test from its own stubs).
- verdict: KILLED by a value assertion (not a refusal). Load-bearing.

### F9 [item 3b, B05] 2026-09-25
- mutant: transfer_curve.py:4316-4317 kept_rows keeps non-DT rows of removed draws; kept_anchors = dict(anchors_by_draw) (runner_b8 fragments, each matched once). target FULL.
- observed: `1 failed in 13.37s` -- `assert ([1020, 1042] == [1020, 1042] ... 10 == 14)` (draws_removed, n_cells_removed).
- verdict: KILLED, first on the removed-cell COUNT; the test also pins diagnostic n_draws == 98 and att_env n_draws_total == 98 (tests/test_p7_3d_campaign_path.py:2308-2311), so the anchor half alone would also die (read, not executed).

### F10 [item 3c, B03b] 2026-09-25
- mutant: transfer_curve.py:1967 same-step match -> any step. target V3.
- observed: `1 failed in 0.18s` -- `Failed: DID NOT RAISE ValueError` in test_a_teleport_whose_collision_is_in_another_step_refuses_even_when_the_counts_claim_otherwise.
- verdict: KILLED.

### F11 [item 3d, B08] 2026-09-25
- mutant: transfer_curve.py:2075 `if grid:` -> `if True:` (hz1x1 given the A23 rule). target T-regress.
- observed: `2 failed, 1 passed in 1.73s` -- `ValueError: cell_anchor_fixedtime_seednone_draw1000.json: a p7.3d-grid4x4/1.1 chunk records A23's collision record; it carries no [...]`; FAILED test_p7_3a_zero_shot_regenerates_byte_identically, test_p7_3b_anchor_regenerates_byte_identically.
- verdict: KILLED (by 2 of 3; the third artifact does not route through validate_cell_payload's teleport branch).

### F12 [item 3e, OWN1 -- SURVIVED] 2026-09-25
- why this line: tests/test_p7_3d_campaign_path.py:2352-2369 builds the A23(f) refusal variants: a third event in ANOTHER cell (maxpressure anchor of ENV_ZERO_DRAW), an event moved to another seed, no event. No variant puts a SECOND event inside one of A23's two cells.
- mutant: transfer_curve.py:1903 `events.get(name, 0) != 1` -> `events.get(name, 0) < 1` (an A23 cell may record 2+ events). matched once.
- target: FULL + all of tests/test_p7_3d_collisions.py. observed: `22 passed in 28.27s`, exit 0.
- verdict: SURVIVED. The code at HEAD is correct (`!= 1`), but the "exactly one event in each A23 cell" half of A23(f) / B.8-2(4)(d) is unprotected by any executed test in these files. Severity: MINOR for the published number (campaign already rolled; see F14 for the artifact check), MAJOR as a protection gap on a registered refusal.

### F13 [item 4, test hygiene] 2026-09-25
- cmd (cwd = throwaway worktree @4b0c4f0; script identical to main's per cmp): `bash /home/filip/rltraffic/scripts/check_test_hygiene.sh tests/test_p7_3d_collisions.py tests/test_p7_3d_campaign_path.py` -> no output, `hygiene-exit=0`.
- verdict: clean.

### F14 [item 5, did the OWN1 gap bite the real campaign?] 2026-09-25
- cmd: python reading ONLY the collisions/robustness metadata of docs/data/p7_3d_grid4x4.json at 4b0c4f0 (no e_sumo/att_env/return printed).
- observed: n_events 2, n_cells_with_collision 2; b_mean_k100: 1 event in ...seed303_draw1042.json; fixedtime: 1 event in ...fixedtime_seednone_draw1020.json; maxpressure 0; both fates arrived_at_collision_step; robustness draws_removed [1020, 1042], n_cells_removed 14, n_draws 98.
- judgement: the published artifact has exactly A23's two events, one per A23 cell -> the untested "exactly one per cell" branch (F12) did not affect the published number.
- also: no test file references _assert_a23_collision_cells / A23_COLLISION_CELLS directly (grep -rln over tests/ empty); the only coverage is FULL's three variants.

### F15 [item 5, chunk_is_reusable] 2026-09-25
- read: offline/transfer_curve.py:2293-2357: re-validates the payload against the cell (so the 1.1 version pin and A23's record apply to a reused chunk) and refuses reuse when code_changed_since(git_commit). No path found by which a 1.0 / attempt-1 chunk is reused.

### F16 [item 6, cleanup] 2026-09-25
- throwaway worktree clean (0 porcelain lines) at 4b0c4f0 before removal; `git worktree remove --force` + `git worktree prune`; `git worktree list | grep -c g9b_wt` -> 0; main tree status clean; branch tip still 4b0c4f0. No commit was created (mutants ran uncommitted; see F7).

## Mutation table
| mutant | target | result | failing assertion |
|---|---|---|---|
| CONTROL clean | FULL, V3, TR(3) | 5 passed | - |
| CONTROL dirty no-op | FULL, V3, TR(3) | 5 passed | - |
| B16 | FULL | KILLED | (303,'e_sumo') 0.586489898989899 == 58.6875/100 |
| B05 | FULL | KILLED | ([1020,1042], 10) == ([1020,1042], 14) |
| B03b | V3 | KILLED | DID NOT RAISE ValueError |
| B08 | TR | KILLED (2/3) | ValueError: ...carries no ['collisions', ...] |
| OWN1 (!=1 -> <1 at :1903) | FULL + test_p7_3d_collisions.py | SURVIVED | 22 passed |
