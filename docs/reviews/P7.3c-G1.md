# REVIEW — P7.3c gate G1: the corpus door (C1 `268fd8e`) and its driver (C2 `8c12b78`), branch at `1bba206` — 2026-09-25 — VERDICT: PASS-WITH-NOTES (0 blocking)

**Reviewer:** `contract-reviewer`, one mandate that was also the corpus run's pre-flight (`BRIEF_41` Amendment A, Q11); read-only; no
SUMO, no driver run; 15 minutes, checklist first (it ran ≈ 6 min over; nothing was stopped). The findings file is filed verbatim below
the rule. **The coordinator's ruling is `BRIEF_41` Amendment B:** the one MAJOR (return values in the capture) is a stated exception
for the corpus stage — MaxPressure's own behaviour on the probe band, already seen, and no evaluated arm's outcome; the MINORs are a fix
list due before the merge. **The items the reviewer could not verify were covered by the coordinator's own commands:** the 40 tests
green under the standard line with both SUMO episodes; four committed mutants killed after a clean control; the real corpus, run,
token and capture paths untouched by the executed driver tests.

---

# G1 FINDINGS -- P7.3c grid4x4 corpus door (C1 268fd8e) + driver (C2 8c12b78), branch task/p7.3c-grid4x4-fewshot @ 1bba206
Reviewer: independent read-only contract reviewer. Started 2026-09-25.

## CHECKLIST
- [ ] R1 Alignment by scenario: hz1x1 path resolves to same alignment object as before C1; T-regress (a) compares REAL freshly collected arrays with committed hz1x1 corpus episode; grid4x4 goes through alignment_for_scenario_key.
- [ ] R2 Dense counter: MaxPressure acts on raw info while logger records aligned view; SUMO draw sweeps build per-second observer env; teleports AND collisions counted on EVERY simulated second; non-zero count refuses BEFORE finalize_episode (nothing written, manifest not rewritten); "collision at second 7" fixture really exercises per-second read.
- [ ] R3 A17(f) gate: artifact sha256 checked before parsing; returns keyed by ix ID from each npz's own ix_ids; integrality before float64 sum; == against artifact; exactly draws 201-300 one episode each; engine_seed_drawn == 437485271 on all 100 (read from docs/data/p7_3d_grid4x4.json); scenario_id cityflow_grid4x4 with CityFlow ids; first mismatch named (draw, id), no return value printed.
- [ ] R4 Disjointness run before first episode and re-asserted by the gate.
- [ ] R5 Driver pre-flight: refusals before token; barrier (non-empty dir refused; never --overwrite; every rm/mv quoted); token position/consumption; both canary halves; WORK_TREE from script location + refuses implementer worktree; -P on every interpreter call; liveness check; tee -a from first line + set -euo pipefail + PIPESTATUS two-step tmux start; SHA256SUMS atomic after 100 episodes + re-verified; gate LAST; COMPLETE/FAILED marker; death at episode 57 (resume/refusal, recovery safety); nothing printed that is a return value.
- [ ] R6 Tests: no assertion that cannot fail; every skipif names its artifact (RLTRAFFIC_SUMO_CORPORA incl., Amendment A Q10); named mutants (10 + 8) each catch their claimed defect class.

## FINDINGS (append-only)

### [x] R1 -- Alignment by scenario (2026-09-25, ~T+7 min)
Commands: `git diff main...task/p7.3c-grid4x4-fewshot -- offline/collect.py`; `sed -n` of offline/aligned_env.py:196-240 (main, unchanged by the diff); `git show <branch>:tests/test_p7_3c_corpus_door.py` lines 120-205.
- OK: collect.py (branch):842 `_alignment = _alignment_for_door(scenario_key)` -> collect.py:652-662 -> `alignment_for_scenario_key(scenario_key)`; aligned_env.py `if scenario.external is None: return declared_alignment(metric_keys)` -- hz1x1 takes the SAME call as before C1 (metric_keys=() as before). Equality (not identity) is the right test because declared_alignment() builds a fresh object per call; test_p7_3c_corpus_door.py:125 `assert got == declared_alignment()`.
- OK: grid4x4 goes through alignment_for_scenario_key (RESCO net digest-verified via parity.resolve_external_source); door test :138-141 asserts != hangzhou and the 16 ids.
- OK (load-bearing, NOT a tautology): T-regress (a) test_p7_3c_corpus_door.py:144-167 runs the real door (`tc.collect_logged_probe_corpus(HZ1X1,[201])` -> collect.main -> observer env) and compares EVERY key of the committed `datasets_sumo_v11/hz1x1_sumo_maxpressure/ep000000_seed1000_draw201.npz` under dtype, shape and np.array_equal. Independent route (a committed file written by P7.3a's plain-env door).
- MINOR (behavioural change not covered by T-regress): before C1 the hz1x1 SUMO door called declared_alignment() unconditionally for ANY scenario key; now `parity.scenario_for_key(scenario_key)` runs first and refuses an unregistered key. Harmless for the corpus (key cityflow_grid4x4 / cityflow1x1 both registered) but it is a new refusal path on the shared door; no test pins it. Fix: none needed for G1; note in packet.
- NOT VERIFIED BY ME: T-regress (a) and T-17f need SUMO + RLTRAFFIC_SUMO_CORPORA; I did not run them (mandate forbids SUMO). The packet's claim that they ran green is unverified here.

### [x] R2 -- The dense counter (T+10 min)
Commands: branch collect.py read at 756-1046; offline/sumo_att_reference.py:955-1095 and 664-706 (main, unchanged); envs/sumo_env.py:221-231; door test 236-395.
- OK: policy on raw info, logger on aligned view -- collect.py(branch):997 `action = policy(info)`; :998 `logger.on_action(logged, action)`; :999-1001 `reward, terminated, truncated, info = env.step(action)` then `logged = ... align_for_log(info)`; policy built on the UNWRAPPED env at :982. Contract C1/C2 honoured (reset returns info only :987; step reward-first :999).
- OK: every SUMO draw builds the observer -- collect.py:973-977 `_make_counting_sumo_env(draw_spec) if draw_spec.backend == "sumo" else make_env(draw_spec)`. The observer's `_simulate` (sumo_att_reference.py:1020-1055) is the frozen loop (envs/sumo_env.py:221-231: step, then metrics.on_sim_step()) plus reads of getStartingTeleportIDList() AND getCollisions() after EVERY simulationStep(); SumoEnv has no other simulationStep call site (grep: only :227/:230), so no simulated second escapes the recorder. recorder.clear() at reset (:986-990) -- counts are per episode.
- OK: refusal before the write -- collect.py:742-753: record fields set, `events = dense_engine_events(env)`, `refuse_episode_with_engine_events(...)` raises, `logger.finalize_episode()` never reached; the exception propagates out of main (finally only closes the env, :1043-1044), so no .npz and no manifest rewrite. A plain env fails CLOSED (dense_engine_events raises when `recorder` is absent, :693-699).
- OK (load-bearing): the "second 7" fixture (door test :306-368) drives the REAL PerSecondSumoObserver._simulate over a scripted TraCI stub whose collision and teleport start exist ONLY at t=7; at the decision boundary t=10 the lists are empty. A once-per-decision read would give n_collisions 0 -> `assert events["n_collisions"] == 1 and events["n_teleports"] == 1` dies. The write-side assertion `out.iterdir() == []` follows a buffered, unfinalized episode, and the clean-episode control (:371-393) proves the same path DOES write -- so the refusal test is not vacuous.
- MINOR: the fixture exercises the observer class directly (object.__new__), not collect.main's choice of env. A mutant reverting :973-977 to make_env() is caught only by the SUMO-gated T-17f/T-regress, or at runtime by dense_engine_events' fail-closed refusal (the corpus could not be written silently). Acceptable.
- MINOR (operational risk, not correctness): the observer's recorder enforces invariants the corpus does not need, EVERY second (pool identity sumo_att_reference.py:649-660, double-arrival :638-642); a raise there on any of draws 203-300 (only 201-202 were run through the observer on this band, per the C2 header :62-70) kills the run with FAILED and forces the manual move-aside recovery. It fails loud, not silent. Fix: none required; the coordinator should know the risk exists.
- MINOR: refusal message (collect.py:716-722) reports the first event time only; fine, prints no return.

### [x] R3 -- A17(f)'s gate (T+13 min)
Commands: branch transfer_calibration.py read at 2398-2863 and 2990-3047; `sha256sum docs/data/p7_3d_calibration.json docs/data/p7_3d_grid4x4.json` -> 3e9df8eed4af2e42... and c63c371ff14d208d... (both EQUAL the pins: transfer_curve.py(branch):576 and transfer_calibration.py(branch):2410); scratchpad art.py over the two JSONs -> scenario_key 'cityflow_grid4x4', 16 ids 'A0'..., probe_returns.sumo = 100 draws 201..300, each a DICT keyed by id; zero-shot cells: engine_seed_drawn Counter({437485271: 700}), requested Counter({1000: 700}); calibration disjointness {training [1,200], held_out [1000,1099], probe [201,300]}.
- OK: sha256 before parse -- tc.py:2431-2441 `_pinned_json` hashes then `json.loads`; both references go through it (:2672-2673).
- OK: keyed by id from EACH npz's own ix_ids -- :2791 `logged_ids = [... payload["ix_ids"] ...]`, :2804 `position = logged_ids.index(ix)`; id-set equality refused otherwise (:2792-2797).
- OK: integrality BEFORE the float64 sum -- :2806-2814 (NaN also refused since NaN != rint(NaN)); `==` against `float(probe[str(draw)][ix])` (:2817-2818), no tolerance.
- OK: engine_seed_drawn read from the PINNED zero-shot artifact and required to be ONE value with requested 1000 (:2444-2459), compared per draw (:2771-2775); door test :526 checks it against the literal 437485271 (an independent route).
- OK: scenario_id refused unless == calibration["scenario_key"] ('cityflow_grid4x4') (:2686-2690); manifest scenario_id is `spec.id` (collect.py(branch):617).
- OK: one episode per draw (:2719-2722), manifest listing == files on disk (:2706-2712), missing/extra draws refused (:2724-2731).
- MAJOR (mandate criterion "no return value printed" violated): the mismatch message PRINTS return values -- tc.py:2833-2838
    `f"the first is draw {draw}, intersection {ix!r}: logged return {logged!r} against the probe's {expected!r} (difference {logged - expected!r})..."`
  and main prints every refusal message verbatim (:3044-3046 `print(f"transfer_calibration: {exc}", flush=True)`), which the driver tees into the capture (driver :306-308). The (draw, id) naming is correct. Mitigation: it is precedent (the hz1x1 gate on main does the same, tc.py hz1x1 gate body lines ~100-103 of that function), and the printed values are MaxPressure probe returns, not an evaluated arm's outcome. Fix: drop `logged`, `expected` and the difference from the message (keep draw, id, and the count of mismatching pairs); likewise the integrality message's `first: {float(rewards[off][0])!r}` (:2810) prints a reward value -- MINOR.
- MINOR: "exactly 201-300" is enforced by the DRIVER's constant (driver :82 `DRAWS_RANGE=(201 301)`, passed at :306), not by the gate: `corpus-gate --draws-range` accepts any half-open range (tc.py:2193-2196, :3031-3033) and the function never compares `requested` with GRID4X4_CORPUS_DRAWS. A gate run on `--draws-range 201 206` over a 5-episode corpus writes a passing record. Fix: in the CLI path (or the function) refuse `requested != list(GRID4X4_CORPUS_DRAWS)`, or record the band in the record and have G3 check it.
- MINOR: the gate trusts the manifest's recorded counts/grain string (:2746-2759) -- unavoidable (nothing to recount from), and the grain-string check does prove the new door wrote the record.

### [x] R4 -- Disjointness before the first episode, re-asserted by the gate (T+14 min)
Commands: tc.py(branch) 2529-2617, subject_facts_per_intersection pins (default GRID4X4_CHECKPOINT_SHA256, digest checked per seed before use), offline/rtg_calibration.py:290-310.
- OK: before the first episode -- driver :212 runs `corpus-preflight` (before the canary, the trap and the token; collect-corpus is :290) -> corpus_preflight_record (:2566) -> grid4x4_corpus_disjointness_record (:2594) -> assert_probe_draws_disjoint(requested, training_draw_ids=stats.draw_ids of the five DIGEST-PINNED checkpoints, held_out_draws=HELD_OUT_DRAWS) (:2547-2550). A preflight failure exits 2 before the token (:212-216).
- OK: re-asserted by the gate -- tc.py:2692-2703 from the pinned calibration artifact's own disjointness record, [low, high] expanded to the whole range (can only refuse more) and the held-out pool compared with HELD_OUT_DRAWS.
- MINOR: the check is a property of the DRIVER's ordering, not of `collect-corpus` / collect_logged_probe_corpus (tc.py:2504-2526), which run no disjointness check themselves; a hand-run `collect-corpus` would collect with none. Fix: call grid4x4_corpus_disjointness_record at the top of the collect-corpus stage too (cheap), or document that collect-corpus is driver-only.

### [x] R5 -- Driver pre-flight (T+19 min; over the 15-min cap, abbreviated)
Commands: `git show <branch>:offline/campaigns/p7_3c_corpus.sh | cat -n` (315 lines, read whole); grep of every $PY/python call.
- OK ordering: every `exit 2` refusal (:104-216 interpreter, implementer tree, -P import check, liveness, group leader, SigIgn, RESCO net, 100 parity configs, dirty tree, BARRIER :189-201, memory, corpus-preflight, canary correctness :220-225 and timing :227-232) precedes the trap (:235-257) and the token (:260-267). Nothing is created before `mkdir -p "$RUN_DIR"` (:270), after `rm -f "$TOKEN"` (:266).
- OK every rm/mv, quoted: `rm -f "$TOKEN"` (:266) touches only the token file; `mv SHA256SUMS.tmp SHA256SUMS` (:296) runs inside "$CORPUS" on the tmp file it just wrote. No other rm/mv/delete in the script; `--overwrite` never passed (tc.py:2491-2501 argv has none; TrajectoryLogger(overwrite=False) refuses a populated dir). Python-side delete: collect.py(branch):911 `_clear_stale_draw_files(flows_dir)` can only touch `<corpus>/flows/*draw*` which the barrier proved empty/absent.
- OK -P: all six interpreter calls carry -P (:118, :212, :220, :287, :290, :306); grep found no other.
- OK WORK_TREE from `${BASH_SOURCE[0]}` with `pwd -P` (:77); implementer tree refused (:110-115); code-origin check asserts both modules load from WORK_TREE (:118-134); DATA read from WORK_TREE's committed docs/data (:87).
- OK: set -euo pipefail (:73); header two-step start with `tee -a` and `${PIPESTATUS[0]}` (:14); SHA256SUMS atomic tmp->mv (:293-296), re-verified `sha256sum -c` (:297), 101-line count (:298-302); corpus-gate LAST (:306), its record written only after the function returns (tc.py:3031-3035, atomic _write_json); COMPLETE (:314) / FAILED via fail() on every `||`-guarded stage.
- OK death at episode 57: collect is one process; episodes 1-56 + a 56-episode manifest remain; `|| fail "collect-corpus"` writes FAILED; a restart is REFUSED by the barrier (both dirs non-empty) until a person moves both aside (documented :36-42, :197-198). No resume path exists, so none can mix two runs into one manifest. Recovery (move aside, new token) is non-destructive. Safe.
- MAJOR (mandate: "nothing printed that is a return value"): the run's capture WILL contain return values: (1) collect.py(branch):1024-1029 prints `return={episode_return:.3f}` for all 100 episodes and :1037-1041 `mean return {mean_return:.3f}` -- the header ADMITS this (:32-34) and argues the per-intersection returns are published, but what is printed is the GLOBAL scalar return (Utils.scalar_reward), not the published per-intersection quantity; (2) the canary line carries `local_return` of CityFlow draw 0 (tc.py canary facts; header :64 quotes -32648.0 / 247.75...), a fixed reference value; (3) the gate mismatch message (R3). None is an evaluated arm's outcome, so no blinding harm I can identify; the coordinator must either rule the exception or require suppression (e.g. collect_logged_probe_corpus redirecting collect.main's stdout to a file under RUN_DIR, and the gate message without values).
- MINOR (reasoned, NOT executed -- running the driver is forbidden): on Ctrl-C/HUP the whole foreground job gets the signal, so the OUTER `tee -a` dies too; on_signal's `echo ... | tee "$RUN_DIR/FAILED" >&2` then writes to a broken pipe: GNU tee creates FAILED at open but is SIGPIPE-killed writing stdout first, so FAILED is likely EMPTY, and the script's next `echo ... >&2` (:252) SIGPIPE-kills bash before `exit 130`; the capture lacks the interruption lines. Marker file still exists; no data effect. Fix: `tee -i -a` in the header's start line, or write FAILED with `printf ... > "$RUN_DIR/FAILED"` before any echo.
- MINOR: under `set -e`, unguarded commands after the token (:270 mkdir, :272-283 echoes, :298 wc, :314 tee) exit without FAILED; an `EXIT` trap keyed on a success flag would make "FAILED on every path" literal.
- MINOR: RUN_TREE (:79) is never enforced -- any tree except the implementer's is accepted, including the MAIN tree; and no check that WORK_TREE's HEAD is the reviewed commit (it is only printed, :273). Fix: refuse unless WORK_TREE == RUN_TREE, or compare HEAD with a pinned sha passed by the coordinator.
- MINOR: liveness pgrep (:136) runs once, early; acceptable (later python calls are the driver's own children).

### [x] R6 -- Tests (T+21 min; abbreviated)
Commands: grep of the packet's mutant table (docs/returns/P7.3c.md(branch):115-143), driver-test and door-test indexes. Pytest NOT RUN by me (over the time cap).
- OK skip naming: door gates name the sumo binary, traci, RLTRAFFIC_DRAWS, RLTRAFFIC_GRID4X4_RESCO, RLTRAFFIC_OUTPUT_ROOT and RLTRAFFIC_SUMO_CORPORA (door test :76-111, the Q10 variable at :96-100); driver test skips name the interpreter, setsid, the five checkpoints, the parity configs, RESCO, tmux (driver test :160-175, :309).
- Load-bearing: T-regress (a) (:144-167, a committed file vs a fresh run); T-17f (:175-204, an independent np.cumsum route by id); the second-7 refusal + its clean control (:352-393); the reversed-id gate test (:207-219); the engine-seed literal 437485271 (:524-526).
- MINOR (weak mutant): M7 (packet :129) is killed only ON THE MESSAGE -- the packet itself says the draw is still refused by the later probe-row check; the gate's disjointness re-assertion therefore protects nothing the probe-row check does not, on this artifact. Not theatre, but not a defect detector either.
- MINOR: D5 (`rm -rf "$CORPUS"`) is killed by a TEXT count of rm lines (test :119-125), i.e. a comment-free grep; an `rm` spelled via `find -delete` or Python would pass it. The executed barrier test (D1) is the real protection.
- NOT VERIFIED: that the executed driver tests' sandbox redirection (test :236-259) cannot reach the real /home/filip/rltraffic/output token or datasets path if the redirection text-substitution fails to match; the mutants' kills themselves (packet claims only; the coordinator re-runs them at G1 per Amendment A4).
