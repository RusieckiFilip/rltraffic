# Pre-flight review — P7.1 half A (`BRIEF_34` + A–D), PART 1 of 2: destruction and corruption paths — PARTIAL

**Filed by the coordinator, 2026-09-12.** The reviewer was killed by an API session limit at ~25 minutes (the third such death in one evening; the first two returned nothing). **This one left its incremental `FINDINGS.md` on disk under the §7 rule adopted the same evening** — 17 of 33 checklist items ticked, every one with a timestamp, the command and the observed result — and it is reproduced verbatim below. The open items were closed by the coordinator (section *Coordinator's completion*), with the independence caveat stated there.

Verdict for PART 1, ruled by the coordinator from the file and the completion: **CLEAR WITH CONDITIONS — 0 blocking, 0 major, 3 minor** (the replay scratch files; the unreachable interpreter check; the smoke-first stage order). Both real `output/` trees identical to the 20:47 baseline at the end; `output/p7_1` never created. Combined ruling with PART 2: `BRIEF_34` Amendment E.

---

## The reviewer's FINDINGS.md, verbatim

# PRE-FLIGHT REVIEW P7.1 PART 1 — FINDINGS (incremental)
Tree under review: /home/filip/rltraffic-p53b @ task/p7.1-alignment (sha to be confirmed)
Main tree (inputs): /home/filip/rltraffic
Date: 2026-09-11

## CHECKLIST
- [ ] 0a. Before-snapshot of /home/filip/rltraffic/output/ and /home/filip/rltraffic-p53b/output/ taken
- [ ] 0b. Cross-check against previous attempt's snap_*_before.txt (differences reported)
- [ ] 0c. Confirm worktree sha == 18ed5cb, branch task/p7.1-alignment
- [ ] 0d. Read PROJECT_PLAN 'PRE-FLIGHT REVIEW — mandatory' scope subsection
- [ ] 0e. Read BRIEF_34 Amendment D
- [ ] 1a. Fence assert_metric_freeze_writable: locate + read
- [ ] 1b. Fence probe >=20 paths (rejected set) in sandbox root containing literal 'output' dir
- [ ] 1c. Fence probe accepted set (output/p7_1/x.json, output/p7_1/smoke/x.json, output/SHA256SUMS_p7_1.txt)
- [ ] 1d. Fence probe symlink outside -> output/p7_0; symlink inside p7_1-shaped dir -> ../p7_0; relative paths
- [ ] 2a. Is --out-dir fenced? Is --output-root used for any WRITE? _work_dir default fenced?
- [ ] 2b. main([...]) with --work-dir <sandbox>/output/p7_0 on synthetic inputs: refused or not
- [ ] 2c. main([...]) with --out-dir <sandbox>/output/p7_0 on synthetic inputs: refused or not
- [ ] 3a. Driver p7_1_metric_freeze.sh read whole; guard order documented with line numbers
- [ ] 3b. Sandbox driver copy (diff shown) with stub PY: no token -> exit code, nothing created; snapshot identical
- [ ] 3c. Sandbox driver: wrong cwd -> refused before token; snapshot identical
- [ ] 3d. Sandbox driver: decoy process matching lock pattern -> refused before token; snapshot identical
- [ ] 3e. Sandbox driver: token + pre-existing FAILED -> token consumed, FAILED cleared, stages invoked in order
- [ ] 4a. sha256sum -c re-verification: can it write anything? decoy failing manifest -> FAILED written, nothing else touched
- [ ] 4b. Manifest step writes only SHA256SUMS_p7_1.txt (.tmp + mv)
- [ ] 5a. sumo_cmd options in envs/sumo_env.py lines 170-195; tempfile/mkdtemp/open( hits
- [ ] 5b. New noteleport .sumocfg: <output> elements (none expected)
- [ ] 5c. traci abnormal exit: kill a traci episode mid-run in sandbox work dir; list new files under scenario dir and cwd
- [ ] 6a. A4 timing-hz4x4: read _run_timing and _declared_config; can it write into scenarios/hangzhou_4x4...?
- [ ] 6b. Run timing with --work-dir under scratch once (<=60 s) and list what it wrote and where
- [ ] 7a. Mutation-verb grep of sumo_att_reference.py (open(, .write, write_json_atomic, mkdir, os.remove, unlink, rmtree, rename, replace, shutil, tempfile) — each hit listed
- [ ] 7b. write_json_atomic in dt_gate.py: mkstemp + os.replace confirmed
- [ ] 7c. Any open(..., 'w') outside write_json_atomic?
- [ ] 8a. report barrier: 1 of N chunks missing -> artifact path absent
- [ ] 8b. report barrier: chunk of wrong regime -> artifact absent
- [ ] 8c. report barrier: noteleport chunk with n_teleports: 1 -> artifact absent
- [ ] 9a. pytest tests/test_sumo_att_reference.py -q tail (real output)
- [ ] 9b. Fence and barrier tests call production functions (confirmed by reading)
- [ ] 10. FINAL snapshot of both real output/ trees == before-snapshot

## FINDINGS (append-only)

### 0a [x] 20:47:30 before-snapshot
cmd: find /home/filip/rltraffic/output -printf '%p\t%s\t%T@\n' | sort > snap_main_before.txt (39681 lines); same for /home/filip/rltraffic-p53b/output (76 lines).
output/p7_1 does NOT exist at start (ls: No such file or directory). output/p7_0 present; 13 SHA256SUMS_*.txt (count checked below).
### 0c [x] 20:47:30 worktree sha
git -C /home/filip/rltraffic-p53b rev-parse --short HEAD -> 18ed5cb ; branch task/p7.1-alignment ; git status --short -> clean (no output).
### 0b [x] 20:48:13 cross-check vs previous attempt's snapshots
Re-took snapshots in the previous attempt's format (%p\t%s\t%TY-%Tm-%Td %TH:%TM:%TS\t%y). diff prev/snap_main_output_before.txt vs now -> exit 0 (identical, 39681 lines). diff prev/snap_wt_output_before.txt vs now -> exit 0 (identical, 76 lines). Nothing changed in either output/ tree between attempts.
Inputs: output/p7_0 has 36 regular files in 6 arm dirs (find -type f | wc -l = 36). output/SHA256SUMS_*.txt count = 12 (task text said 13; the 12 present: p4_3 p4_4_p4_5 p4_6 p4_7 p5_1 p5_2 p5_3a p5_3b p5_3b_decomp p7_0 p8_3 p8_4a). MINOR discrepancy in the request text, not in the tree.
Manifest SHA256SUMS_p7_0.txt has 36 lines with paths relative to output/ (e.g. p7_0/cityflow__fixedtime/ep000000_seed1000.npz); sha256sum -c from repo root fails with 36x 'No such file' (cwd-relative), from output/ cwd: see next line.
sha256sum -c from output/ cwd: exit 0 (all 36 OK, p7_0 inputs intact at start) 20:48:24

### 0e [x] Amendment D read (BRIEF_34 lines 506-559). D4 requires: token consumed on start after cd+import check; start lock pgrep -f 'python.*offline\.sumo_att_reference'; trap killing process group; stages A1 obs+unobs, A1b, A2, A4, smoke, report; SHA256SUMS_p7_1.txt last, atomic, smoke/ included.
### 3a [x] driver read whole (offline/campaigns/p7_1_metric_freeze.sh, 309 lines). Guard order BY READING:
  L114 cd "$WORK_TREE" (set -e => exit if fails)
  L116-120 import check ($PY -c "import offline.sumo_att_reference") exit 2
  L123-126 interpreter -x check exit 2  [NOTE: unreachable-as-intended: L116 already invokes $PY; a missing $PY fails at L116 with the 'cannot import' message. Cosmetic.]
  L133-140 pgrep lock exit 3
  L143-147 A1b noteleport sumocfg present exit 2
  L151-154 output/p7_0 dir exists exit 2
  L155-159 ( cd $MAIN/output && sha256sum -c SHA256SUMS_p7_0.txt ) exit 2 — cwd is output/, matching the manifest's relative paths (verified: exit 0 from output/ cwd, exit 1 from repo root)
  L162-168 token present check exit 2
  L170 rm -f "$TOKEN"  <- FIRST mutation
  L175 mkdir -p "$LOGS" "$SMOKE" ; L176 rm -f FAILED COMPLETE  <- after token
  L178 fail() writes $WORK/FAILED via tee; L187-195 on_signal writes FAILED first then kills CELL_PID and group.
  Stage order: smoke (L226, FOREGROUND, not via run_cell -> CELL_PID unset; trap cannot kill it by pid, relies on group signal) -> A1 obs (L236) -> A1 unobs (L239) -> A1b (L246) -> A2 (L253) -> A4 (L260) -> report (L266) -> manifest tmp+mv (L273-276) -> re-verify ALL SHA256SUMS_*.txt (L281-291) -> COMPLETE (L294).
  Manifest: find p7_1 -type f (*.json|*.log) | sort | xargs sha256sum > SHA256SUMS_p7_1.txt.tmp && mv. Writes only those two names in $MAIN/output. Not through the Python fence (shell), but literal names.
  Re-verification L283 loops over EVERY SHA256SUMS_*.txt in output/ -> a pre-existing non-verifying manifest of an OLDER campaign would turn a good run into FAILED at the very end. Must pre-check all 12 (next).
  Deviation from D4's stage list: smoke runs FIRST, not after A4 (order in D4 may be a list, not a sequence) — informational.
### 4a-pre [x] 20:50:02 all 12 SHA256SUMS_*.txt verify from output/ cwd (p4_3 10, p4_4_p4_5 44, p4_6 125, p4_7 112, p5_1 48, p5_2 221, p5_3a 10, p5_3b 39, p5_3b_decomp 61, p7_0 36, p8_3 18, p8_4a 111 OK). The driver's final re-verification loop (L281-291) will not trip on a pre-existing manifest.

### 0d [x] PROJECT_PLAN L855-883 scope read: two questions (destroy/corrupt? resume/idempotence?), falsification method, statistics OUT.
### 1a [x] fence read: sumo_att_reference.py L1616-1640. `parts = Path(path).resolve().parts`; loop over parts[:-1]; refuse if part=='output' and parts[i+1] not in ('p7_1','SHA256SUMS_p7_1.txt'). resolve() => symlinks and '..' are normalised BEFORE matching (to be tested). The LAST component is never a `part`, so a path ENDING in 'output' passes _work_dir (destination fence then refuses 'output/<chunk>' — to be tested).
### 7a [x] mutation-verb grep of sumo_att_reference.py (open( .write write_json_atomic mkdir os.remove unlink rmtree rename replace shutil tempfile): hits ONLY at L2025 (str.replace on a filename, not fs), L2039/2100/2125 (import write_json_atomic), L2085 & L2117 (work.mkdir(parents=True, exist_ok=True), AFTER the fenced destination and AFTER the episode rolls), L2086/2118/2138 (write_json_atomic to fenced destination). No open(, no shutil, no rmtree, no unlink, no tempfile in this file.
### 7b [x] dt_gate.write_json_atomic L1123-1145: parent.is_dir() check (raises FileNotFoundError, creates nothing), tempfile.mkstemp(dir=parent, prefix='.p4-', suffix='.json.tmp'), write, os.replace(tmp, destination); on exception unlink tmp. Atomic rename in the same directory. CONFIRMED by reading.
### 5a [x] envs/sumo_env.py L176: sumo_cmd = [sumo, -c, cfg, --no-step-log true, --no-warnings true, --waiting-time-memory 100000, (--seed N)]. No --tripinfo-output/--summary/--log/--output-prefix. grep tempfile|mkdtemp|open( in sumo_env.py: NO hits (only L176). By reading.
### 5b [x] noteleport .sumocfg (29 lines, committed in 18ed5cb): elements input/time/processing only; NO <output>. diff vs parity .sumocfg: comment block + <processing><time-to-teleport value="-1"/></processing> only. route-files and net-file identical (referenced, not copied).
### 2a-read [x] parser L1955-1957: --output-root default 'output'; --work-dir default None -> _work_dir = fence(work_dir or output_root/p7_1) (L2008-2012, fenced); --out-dir default 'docs/data' used ONLY in _run_report L2127, fenced there. --output-root passed as output_root= to run_sumo_arm/run_cityflow_arm (to check READ-ONLY in L1155-1265/1405-1465). _run_cell: fence(work) L2041, fence(destination) L2045 BEFORE rolling; mkdir L2085 after rolling. _run_timing: same shape L2102-2103/2117. _run_report: fence(out_dir/p7_1_metric_freeze.json) L2127 then _work_dir L2128 BEFORE reading chunks. main() catches ValueError/FileNotFoundError/KeyError -> 'REFUSED:' exit 2.
### OPEN QUESTIONS raised by reading (to test): (i) does anything called by run_sumo_arm/run_cityflow_arm write (offline.parity regenerating .sumocfg? CityFlow saveReplay -> output/replay.txt, output/roadnet.json exist in the main tree snapshot!)? (ii) gudang/parity .sumocfg <output> elements?

### 1b/1c/1d [x] 21:33 fence probes EXECUTED (script fence_probe.py, sandbox sb_fence/ with literal output/ dir, symlinks created)
27 reject probes ALL refused (ValueError): output/p7_0/x, p5_3b/x, never/x, p7_10/x, p7_1x/x, SHA256SUMS_p7_0.txt, SHA256SUMS_p7_1.txt.bak, SHA256SUMS_p7_1.txt.tmp, p7_1/../p7_0/x, relative output/p7_0/x, ./output/p7_1/../p7_0/x, p7_1/../../output/p7_0/x, symlink OUTSIDE -> output/p7_0 (elsewhere/link_to_p7_0/x), symlink INSIDE p7_1 -> ../p7_0 (both the link itself and link/x), symlink INSIDE p7_1 -> REAL /home/filip/rltraffic/output/p7_0 (…/manifest.json), output/p7_1_metric_freeze.json, output/x.json, P7_1/x, 'p7_1 /x', ./p7_0, //p7_0, output/p7_0 and output/p5_3b as dirs, relative output/SHA256SUMS_p7_0.txt.
10 accept probes accepted: output/p7_1/x.json, output/p7_1/smoke/x.json, output/SHA256SUMS_p7_1.txt, output/p7_1, relative output/p7_1/x.json, symlink elsewhere->p7_1/x.json, docs/data/p7_1_metric_freeze.json, and ALSO: `<sb>/output` itself (last component never checked), `<sb>/scenarios/x` (no 'output' component: the fence guards output/ only, it is NOT a whitelist of roots), `<sb>/output/SHA256SUMS_p7_1.txt/x` (a DIRECTORY named like the manifest passes). None of the three is reachable from the driver's hardcoded paths -> MINOR.
Sandbox byte-identical after probes (find diff empty). unexpected: 0.
### DISCOVERY (to be verified next): worktree /home/filip/rltraffic-p53b/output/replay.txt (47387 B) and roadnet.json (1821 B) exist, mtime Sep 11 19:53 — CityFlow replay outputs written under <cwd>/output/ despite cityflow1x1.json saveReplay:false. Main tree has the same pair at Sep 10 18:34. envs/cityflow_env.py L94-101 resolves roadnetLogFile/replayLogFile relative to CWD and mkdirs their parent; L207-216 writes a roadnet json. A2 (run-cityflow) is a candidate UNFENCED writer under output/.

### 2b/2c [x] 21:34 main() EXECUTED with bad targets (sandbox sb_main: output/{p7_0,p5_3b}, docs/data; cwd=worktree, real interpreter). 8 invocations, ALL 'REFUSED:' exit 2, sandbox byte-identical after (find diff empty):
  --work-dir <sb>/output/p7_0 run-sumo -> REFUSED output/p7_0 ; --work-dir <sb>/output/p5_3b run-cityflow -> REFUSED ; --work-dir <sb>/output/p7_0 timing-hz4x4 -> REFUSED ; --out-dir <sb>/output/p7_0 report -> REFUSED (output/p7_0/p7_1_metric_freeze.json) ; --out-dir <sb>/output report -> REFUSED (output/p7_1_metric_freeze.json) ; --work-dir <sb>/output/p7_0 report -> REFUSED ; --work-dir <sb>/output run-sumo -> _work_dir passes but destination output/freeze_sumo_maxpressure.json REFUSED before rolling ; default work dir (no --work-dir) report -> 'holds no freeze_*.json chunk' exit 2 (nothing created).
  => --out-dir IS fenced (report only, its sole user); --work-dir default IS fenced; --output-root feeds _stored_reference (read) and the fenced default work dir only. run_sumo_arm/run_cityflow_arm L1155-1265/1405-1466 take output_root only for _stored_reference() and the never-opened sentinel_out_dir (BY READING).
### CityFlow replay files: cityflow1x1.json has saveReplay:false and NO roadnetLogFile/replayLogFile keys; envs/cityflow_env.py L97-102 only rewrites those keys when present, so the env passes none; yet <tree>/output/replay.txt + roadnet.json exist in BOTH trees (main Sep 10 18:34; worktree Sep 11 19:53, i.e. written by something run from the worktree cwd today, before my snapshot). Neither file is in any manifest (grep over all SHA256SUMS: none). Origin to be established empirically in a sandbox cwd (5c/A2 run).

### 3b-3e, 4a(ii) [x] 23:00 driver EXECUTED in sandbox sb_drv (copy differs from the real driver ONLY in L88 MAIN= and L89 WORK_TREE=; diff shown in run log; PY derives from MAIN -> stub bash script logging its argv). Sandbox has main/output/{p7_0,p5_3b,SHA256SUMS_p7_0.txt,SHA256SUMS_p5_3b.txt}, wt/scenarios/.../_noteleport.sumocfg.
  3b no token: exit 2, message names the mkdir+date command; snapshot IDENTICAL; output/p7_1 not created.
  3c(i) token present, import check fails (STUB_IMPORT_FAIL): exit 2 'cannot import ... This is what a wrong cwd looks like'; snapshot IDENTICAL, token still present.
  3c(ii) WORK_TREE missing: cd fails under set -e, exit 1 at L114; snapshot IDENTICAL; token still present.
  3d decoy process (argv0 'python -m offline.sumo_att_reference decoy', a sleep): exit 3, decoy listed with pid/etime; snapshot IDENTICAL; token still present. The driver did NOT match itself (3e passed with no decoy).
  4a(ii) corrupted SHA256SUMS_p7_0.txt (first hex digit changed): exit 2 'does not verify'; snapshot IDENTICAL; token still present.
  3e token + pre-existing FAILED + COMPLETE: exit 0. Token deleted (L170) BEFORE mkdir (L175) and rm FAILED/COMPLETE (L176). Stage order from the stub's argv log: smoke(run-sumo maxpressure --episodes 1) -> a1 fixedtime/maxpressure/random (run-sumo --regime parity --halting-episodes 1) -> a1 x3 --no-observer -> a1b x3 --regime noteleport -> a2 cityflow x3 -> timing-hz4x4 --episodes 1 -> report. 15 stub calls + 1 import check. Snapshot diff: -AUTHORISED_TO_RUN, -old FAILED, +logs/ (15 .log), +smoke/ (dir), +COMPLETE (new content), +output/SHA256SUMS_p7_1.txt (15 entries, logs only since the stub wrote no json). p7_0/, p5_3b/, SHA256SUMS_p7_0/p5_3b UNCHANGED (size+mtime). Re-verification loop reported p5_3b 1 OK, p7_0 1 OK, p7_1 15 OK.
  NOTE: the smoke stage runs FIRST, in the foreground, not via run_cell; D4 lists smoke after A4. Informational.
  NOTE: top-level COMMON carries --episodes 5 and timing-hz4x4 re-declares --episodes 1 (subparser dest wins; the stub log shows both flags). By reading argparse semantics; A4 will roll 1 episode.
### 4b [x] manifest step: in 3e the only file added at output/ level was SHA256SUMS_p7_1.txt (tmp+mv, L273-276); no .tmp left behind.

---

## Coordinator's completion of the open items (2026-09-12, 09:05–09:25)

⚠️ **Independence caveat:** the items below were executed by the coordinator, who wrote the brief. They are falsification runs with their commands and numbers, not readings; a future reviewer may repeat them from this record. Items 8a–8c and 9 were already established by PART 2 (`docs/reviews/P7.1-preflight-part2.md`) and are cross-referenced, not redone.

- **10 — final snapshot.** `find -printf '%p\t%s\t%T@\n' | sort` on both real `output/` trees against the reviewer's 20:47:30 baseline: main **39,681 = 39,681 lines, 0 diff lines**; worktree **76 = 76, 0 diff lines**; `output/p7_1` absent. Across three agent lifetimes, PART 2's experiments and the coordinator's own runs, nothing under either `output/` changed.
- **5c — traci abnormal exit.** One observed `run-sumo --arm maxpressure` episode started into a scratch `--work-dir` (cwd = worktree), `SIGTERM` to the Python process after 12 s: the `sumo -c … parity.sumocfg` child (pid listed before the kill) was gone afterwards; **the work dir stayed EMPTY** (the chunk is written only after the roll, through `write_json_atomic`); **no new file under either parity scenario directory, the gudang directory, or the cwd** (`find … -newer` diffs empty).
- **6a/6b — A4.** `timing-hz4x4` run once into the scratch work dir: **7 m 23 s wall, `seconds_per_episode 438.69`**, one file written — `timing_hz4x4_gudang.json` (2,095 B, `att_is_not_a_result` present, `arm random`, `n_created 2983`, `n_entered 2761`, `n_teleports 0`) — **nothing under `scenarios/`** (diff empty), worktree `git status` clean. By reading `_declared_config` / `_run_timing` (`sumo_att_reference.py:1922-2024`): the only write is that chunk under the fenced work dir. ⚠️ **The observer costs 8.6× on hz4x4** (51.0 s bare, coordinator's survey of 2026-09-11) — the driver header's ~1.5 min for A4 and its ~17 min total are wrong; measured rates give G3 ≈ 33 min.
- **The replay discovery.** `configs/sim/cityflow1x1.json` sets `saveReplay: false` and carries neither log key; `envs/cityflow_env.py:97-102` rewrites the keys only when present; `CityFlow/src/engine/engine.cpp:74-76` reads them and calls `setLogFile(dir + …)`. The two files at `<cwd>/output/replay.txt` (47 KB) and `roadnet.json` (1.8 KB) are **a known, thrice-documented behaviour** of every CityFlow rollout (`docs/reviews/P5.3a.md:33`, `P5.3b-preflight.md:73-74`, `BRIEF_30:425-426`): gitignored, in no manifest (grep of all 12 `SHA256SUMS_*.txt`: none), overwritten by every campaign since P4. A2 will rewrite the worktree's pair. **MINOR, recorded; not a fence defect** — the fence governs the module's writes, not the engine's scratch.
- **7c.** The reviewer's 7a grep found no `open(…, 'w')` outside `write_json_atomic`; confirmed by the same grep.
- **8a–8c, 9 → PART 2** (`P7.1-preflight-part2.md`): a missing chunk, a wrong-regime chunk and a `noteleport` chunk with `n_teleports: 1` are **NOT refused by `report` when run by hand** — PART 2's MAJOR 1 and MAJOR 2 — and are unreachable through the driver's stage order; tests `81 passed, 2 skipped` (`83` with the output root exported).

**Minor findings from the reviewer's file, ruled MINOR:** the interpreter `-x` check at driver `:123-126` is unreachable as intended (the import check at `:116` already invokes `$PY`) — cosmetic; the smoke stage runs first and in the foreground, so on `SIGINT` it dies with the group rather than by pid — PART 2 verified the trap still writes `FAILED` in that case; the fence accepts a path whose LAST component is `output` and any path without an `output` component — by design, and `report`'s only such target (`docs/data`) is the intended one.
