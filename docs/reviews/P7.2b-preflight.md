# PRE-FLIGHT REVIEW — P7.2b (`BRIEF_36` + Amendment A), destruction and resume paths only

**Filed by the coordinator 2026-09-14 from the reviewer's findings file, verbatim below the rule.** Reviewer: `contract-reviewer`, one mandate, 10-minute box, two real SUMO probe episodes (of three allowed) plus sixteen in-process `report()` constructions in scratch directories; read-only on the draws tree; the driver read in full and not executed (its execution half was denied by the reviewer's permission layer). Reviewed HEAD `8a9bd7e` on `task/p7.2b-calibration`.

**Verdict: CLEAR WITH CONDITIONS — blocking 0, major 0, minor 3.** The resume rule re-validates from content (a stored `two_routes_agree` is never trusted; a lying chunk is moved aside byte-identically and re-rolled deterministically — same `engine_seed_drawn`, same return); every `report` refusal precedes every write; the P4.3 sha is checked against the constant, the file and the chunks; the fence is a whitelist (12 keys) and no fenced or stray value reaches the artifact; nothing under `scenarios/draws/` is written. **The two items the reviewer could not verify were closed by the coordinator by execution the same evening:** the driver with no token runs the canary (0.77 s) and refuses with exit 2 before creating `output/p7_2b/` in either tree; the canary's CityFlow episode writes nothing under the draws tree (`saveReplay: false`; 1,442 files, none newer). The three minors are `BRIEF_36` Amendment B.

---

# PRE-FLIGHT REVIEW — P7.2b (BRIEF_36 + Amendment A), destruction and resume paths ONLY
Started: 2026-09-14 (time box 10 min)
Worktree: /home/filip/rltraffic-p53b  branch task/p7.2b-calibration  HEAD 8a9bd7e

## CHECKLIST
[x] 0. `python -m offline.transfer_calibration --help` and each subcommand's `--help`: which flags select the work dir, the out path and the draw set? Record them. Snapshot: count files under `/home/filip/rltraffic/scenarios/draws/cityflow1x1` (expect 1442) and sha256 of `draw_0201/parity/noteleport.sumocfg` and `draw_0201/parity/provenance.json` — record for the end-of-review comparison.
[ ] 1. ONE real probe episode into a scratch work dir (draw 201 or 5 — whichever the CLI allows; if only the band is allowed use 201). Record the chunk's keys, `two_routes_agree`, `n_teleports`, `vehicle_types_seen`, `format_version`, `engine_seed_drawn`, `canary_seconds` presence. This is SUMO episode 1 of 3.
[ ] 2. Resume rule by CONSTRUCTION, no simulator: copy the real chunk and plant (a) `local_return_from_lanes` ≠ `local_return`; (b) `n_teleports: 1`; (c) `vehicle_types_seen: ["DEFAULT_VEHTYPE"]`; (d) `format_version: "p7.2b-calibration/0.9"`; (e) `decisions: 359`; (f) a stored `two_routes_agree: true` with the returns actually unequal (the stored verdict must NOT be trusted). For each: `chunk_is_reusable(payload, draw_id=…)` → must be False; record which check caught it. Then, for ONE planted case, run the probe entry point on that draw with the planted chunk in place and confirm it is moved to `failed/` (not overwritten, not deleted) and a fresh chunk is rolled (SUMO episode 2 of 3); record the `failed/` file name and that the original planted bytes are inside it.
[ ] 3. `report` refusals by construction: a work dir with 99 chunks (copies of the real one with `draw_id` rewritten to 201–299 — or whatever `report` needs; if it demands exactly 100, plant 100 and then delete one) → refuses a missing draw and writes NOTHING at the out path; a duplicated draw (two files, same `draw_id`) → refuses; each planted case from item 2 (a)–(d) → refuses, naming the draw; snapshot the out path's parent before/after each refusal (nothing created, no `*.tmp` left).
[ ] 4. The P4.3 sha pin: compute sha256 of `/home/filip/rltraffic/docs/data/p4_3_probe.json` and compare to `P4_3_PROBE_SHA256`; then hand `report` a chunk set whose recorded sha differs from the constant → refused; and (read-only) confirm `report` compares the FILE's current sha as well as the chunks' recorded sha — quote the lines.
[ ] 5. THE FENCE: build a synthetic smoke chunk with every whitelisted field plus `fenced_do_not_report: {"att_horizon": 123.4, "episode_reward": -999.0, "rtg_last": -1.0, "rtg_series": [...]}`; run `report` (with a valid probe set) into a scratch out path; assert the serialised output contains NONE of the strings `123.4`, `-999.0`, `fenced_do_not_report`, `rtg_last`, `rtg_series`, `episode_reward`, `att_horizon` in the smoke block. Then plant an EXTRA non-whitelisted key at the top level of the smoke chunk (`sneaky_att: 123.4`) → assert it does not reach the output (whitelist, not blacklist). Quote `_SMOKE_PUBLISHED_FIELDS`.
[ ] 6. The driver, by READING (do not run it): confirm the order canary → token (line numbers); that the token is deleted on consumption; that `FAILED` is written by both `fail()` and the signal handler before the kill; that `SHA256SUMS_p7_2b.txt` is written via tmp + mv last; that no line references `scenarios/draws` in a write position; that logs use `>>`. Then ONE safe execution test: run the driver with NO token present and with `output/p7_2b` absent → it must exit non-zero BEFORE creating `output/p7_2b/` (verify the directory does not exist afterwards; if it does, that is a finding and you delete ONLY what the driver created in that run — record it). If the driver cannot be run safely without risk of writing under `output/`, skip the execution and say so.
[ ] 7. End-of-review snapshot: file count under `scenarios/draws/cityflow1x1` still 1442; the two sha256s from item 0 unchanged; `output/p7_2b` absent in BOTH trees.

## FINDINGS (appended incrementally)

### Item 0 — 22:38 — CLI flags + snapshot
- HEAD confirmed 8a9bd7e on task/p7.2b-calibration.
- Top-level flags (global, before the subcommand): `--draws-root`, `--output-root`, `--work-dir`, `--out-dir`, `--engine-seed`, `--canary-seconds`. Subcommands: probe (`--draws-range START END`), smoke (`--subject {mappo1000,mix50}`, `--draw-id`), report (no flags), canary.
- Work dir = `--work-dir`; out path = `--out-dir`; draw set = `probe --draws-range START END`.
- Snapshot: 1442 files under scenarios/draws/cityflow1x1 (as expected).
  sha256 draw_0201/parity/noteleport.sumocfg = c177e9625953e752d8c49f3df21f83715f93020d69cff43da8e39705611fbeda
  sha256 draw_0201/parity/provenance.json   = 8b707dd4ab21f6747737684eb179c40c0e1ce2cdc72d7510cbf6ccdebea276a6
- `output/p7_2b` absent in both trees at start.
[x] 0

### Reading facts — 22:40 — offline/transfer_calibration.py (ranges as instructed) + driver
- `chunk_is_reusable` (transfer_calibration.py:176-202) checks, in order: format_version==ARTIFACT_FORMAT_VERSION ("p7.2b-calibration/1.0", :89), draw_id match, decisions==EXPECTED_DECISIONS, `float(local_return) != float(local_return_from_lanes)` (:190, recomputed — the stored `two_routes_agree` is never read), n_teleports==0, vehicle_types_seen==[PARITY_VTYPE_ID], time_to_teleport_option, p4_3_probe_sha256==pin. Catches only (KeyError, TypeError, ValueError) (:200).
- `_move_aside` (:222-235): `path.replace(dest)` into `failed/`, suffix `.1`, `.2`… if dest exists — never overwrites.
- `run_sumo_probe` (:287-295): existing chunk → json.loads (JSONDecodeError → `{}`) → reusable? reuse : `_move_aside`. NOTE: a valid-JSON non-dict (e.g. `[]`) makes `payload.get` raise AttributeError, uncaught at :200 → traceback, chunk left in place (no destruction; MINOR).
- `run_sumo_probe` refuses at :276 if the P4.3 file's sha != pin, before any episode.
- Chunk write `_write_json` (:424-430): tmp + `replace` — atomic. Chunk stores `two_routes_agree: True` literal (:403) — written only after the `!=` refusal at :356.
- `report` (:923-1027): file sha vs pin FIRST (:928-932); `_validate_chunk` per chunk (:888-920) checks format_version, routes `!=`, n_teleports, vtypes, ttt option, decisions, chunk-recorded sha vs pin (:916). `_validate_chunk` itself checks NEITHER draw_id-vs-filename NOR duplicates/missing draws — that must live in `probe_returns_from_chunks` (:942, outside my ranges) — to be tested by construction in item 3.
- Smoke: whitelist comprehension `{field: payload[field] for field in _SMOKE_PUBLISHED_FIELDS}` (:959); serialised-string fence check `if FENCED_KEY in serialised: raise AssertionError` (:1021-1025) precedes `_write_json` (:1026).
- `_SMOKE_PUBLISHED_FIELDS` (:872-885) = ("subject","draw_id","checkpoint","target_rtg","decisions","rtg_first","rtg_advanced_every_decision","n_decisions_in_support","actions_in_range","vehicle_types_seen","time_to_teleport_option","seconds").
- Driver p7_2b_calibration.sh: canary :94-103 (refuse > 2.0 s, :98-102) → token check :106-113 → `rm -f "$TOKEN"` :115 → `mkdir -p "$LOGS"` :119 (first write). `fail()` :125 tees FAILED; `on_signal` :128-136 tees FAILED then TERM/KILL cell then `kill -- -$$`. Manifest :167-170 `> ...tmp && mv`. Logs `>>` :144,:146. `scenarios/draws` appears only as `$DRAWS` (read: `[ -f ]` :79, passed as `--draws-root` :49). NOTE: `trap` installed at :137, AFTER token consumption at :115 — a signal in :115-:137 consumes the token and leaves no FAILED marker (MINOR).
- Driver runs the CANARY (a CityFlow episode via `rtg_calibration.run_probe` on draw 0, :1072-1080) BEFORE the token; the canary is not a SUMO episode.

### Item 1 — 22:39 — one real SUMO probe episode, draw 201 (SUMO episode 1/3)
- Cmd: `cd /home/filip/rltraffic-p53b && PYTHONPATH=... python -P -m offline.transfer_calibration --work-dir $S/work1 --out-dir $S/out1 probe --draws-range 201 202` → exit 0, wall 12.3 s, stdout `probe: 1 draws, all two-route equal, 0 teleports`.
- Chunk `$S/work1/probe_draw_0201.json` (879 B) keys: att_horizon, canary_seconds, config_path, config_sha256, decisions, draw_id, engine_seed_drawn, engine_seed_requested, format_version, git_commit, git_dirty, horizon_vehicle_count, local_return, local_return_from_lanes, n_teleports, p4_3_probe_sha256, scenario_key, seconds, time_to_teleport_option, two_routes_agree, vehicle_types_seen.
- Values: two_routes_agree=true, local_return=local_return_from_lanes=-23938.0, n_teleports=0, vehicle_types_seen=["cf_parity"], format_version="p7.2b-calibration/1.0", engine_seed_requested=1000, engine_seed_drawn=437485271, canary_seconds=null (present; null when not passed), decisions=360, time_to_teleport_option="-1", config_sha256 == the sumocfg sha from item 0, git_commit=8a9bd7e…, git_dirty=false.
- After the episode: draws tree still 1442 files, both item-0 sha256s unchanged, `find scenarios/draws -newer FINDINGS.md` empty → the SUMO episode wrote nothing under scenarios/draws.
[x] 1

### Item 2 (construction half) — 22:41 — `chunk_is_reusable` on planted chunks, no simulator
- REAL chunk: reusable(draw_id=201)=True; reusable(draw_id=202)=False (draw_id check :186).
- (a) local_return_from_lanes = local_return+1.0 → False (caught :190 routes `!=`).
- (b) n_teleports=1 → False (:192).
- (c) vehicle_types_seen=["DEFAULT_VEHTYPE"] → False (:194).
- (d) format_version="p7.2b-calibration/0.9" → False (:184).
- (e) decisions=359 → False (:188).
- (f) stored two_routes_agree=true with local_return = from_lanes-0.5 → False (caught :190; the stored verdict is never consulted — confirmed by (h): deleting the `two_routes_agree` key from a clean chunk still yields True, so the key is not read at all).
- extra (g) p4_3_probe_sha256 = "0"*64 → False (:198). extra (i) NaN in both returns → False (NaN != NaN, conservative: re-rolls).
- MINOR: a syntactically valid non-object JSON chunk (`[]`) → `payload.get` raises AttributeError, not in the caught tuple at :200 → the probe crashes with a traceback instead of moving the file aside; the file is left untouched (no destruction). transfer_calibration.py:184,:200.

### Item 2 (execution half) — 22:40 — re-roll over a planted lying chunk (SUMO episode 2/3)
- Setup: `$S/work2/probe_draw_0201.json` = planted case (f) (sha 88b0f704…); pre-planted sentinel `$S/work2/failed/probe_draw_0201.json` (sha ead09788…) to exercise the no-overwrite suffix path.
- Cmd: probe --draws-range 201 202 --canary-seconds 0.9 → exit 0.
- Result tree: `failed/probe_draw_0201.json` sha ead09788… UNCHANGED (sentinel not overwritten); `failed/probe_draw_0201.1.json` sha 88b0f704… == the planted bytes (moved, not deleted, not overwritten); fresh `probe_draw_0201.json` sha 07ff53b7… with local_return=local_return_from_lanes=-23938.0, engine_seed_drawn=437485271 (identical to episode 1 → the re-roll is deterministic), canary_seconds=0.9 (the CLI flag is recorded). transfer_calibration.py:222-235,:287-295.
[x] 2

### Item 3 — 22:41 — `report` refusals by construction (in-process `tc.report(...)`, output_root = MAIN output, read-only)
- Harness: 100 chunks 201–300 copied from the real chunk with draw_id rewritten and both returns set to -23938-i (kept equal). Baseline → WROTE, 100 probe rows, out dir created only then.
- 99 chunks (250 deleted) → REFUSED `ValueError: the probe band has 1 missing draw(s) (first [250]); …` out dir before=absent after=absent.
- Duplicate: extra `probe_draw_0250b.json` with draw_id 250 → REFUSED `draw 250 appears in more than one chunk under …` nothing written.
- Filename/draw_id mismatch (`probe_draw_0300.json` holding draw_id 299) → REFUSED as duplicate 299; nothing written. (Missing-300 would also have caught it.)
- Planted at draw 237: (a) → `probe_draw_0237.json: the two routes disagree (-23938.0 against -23937.0)`; (b) → `1 teleport(s) under A15(c)'s teleport-free regime`; (c) → `the engine ran ['DEFAULT_VEHTYPE'], not ['cf_parity']`; (d) → `format_version 'p7.2b-calibration/0.9' is not 'p7.2b-calibration/1.0'`; (e) → `359 decisions, not 360`; (f) lying verdict → `the two routes disagree (-23938.5 against -23938.0)`; (g) sha → `records source statistics from sha256 '000…', not the pinned 'dc12…'`. Every refusal names the file (= the draw); out dir absent before and after each; no `*.tmp` anywhere under out3.
- Stray `probe_draw_0250.json.tmp` in the work dir → ignored by the glob (:935); report WROTE normally.
- `find /home/filip/rltraffic/output /home/filip/rltraffic/scenarios/draws -newer marker` → empty: report's reads of the MAIN checkpoints/draws wrote nothing there.
- All refusals are ValueError/AssertionError → caught by main() :1134 → exit 1 (the driver's `fail`).
[x] 3

### Item 4 — 22:41 — the P4.3 sha pin
- sha256(/home/filip/rltraffic/docs/data/p4_3_probe.json) = dc12f8b7e791bdf1a2097fc372a0e8918f67b40b8bf531f7bbde802f07b12a74 == P4_3_PROBE_SHA256 (:110). Same for the worktree copy (the module's `_REPO_ROOT` resolves to the worktree, :100,:103).
- Chunk with recorded sha "0"*64 → report REFUSED (see item 3 (g)); transfer_calibration.py:916-920 `if str(payload.get("p4_3_probe_sha256")) != P4_3_PROBE_SHA256: raise ValueError(...)`.
- FILE's current sha is compared FIRST in report, :928-932: `on_disk = _sha256_file(P4_3_PROBE_ARTIFACT); if on_disk != P4_3_PROBE_SHA256: raise ValueError(f"{P4_3_PROBE_ARTIFACT.name}: sha256 {on_disk} against the pinned {P4_3_PROBE_SHA256}")`. The probe stage also refuses on the file at :275-281 before any episode, so a chunk cannot be born with a wrong sha. Three-way agreement (constant/file/chunks) confirmed.
[x] 4

### Item 5 — 22:41 — THE FENCE
- `_SMOKE_PUBLISHED_FIELDS` (:872-885): ("subject","draw_id","checkpoint","target_rtg","decisions","rtg_first","rtg_advanced_every_decision","n_decisions_in_support","actions_in_range","vehicle_types_seen","time_to_teleport_option","seconds").
- Synthetic `smoke_mappo1000.json` with all 12 whitelisted fields + `fenced_do_not_report: {att_horizon:123.4, episode_reward:-999.0, rtg_last:-1.0, rtg_series:[1,2,3]}` + top-level `sneaky_att: 123.4`; valid 100-probe set → report WROTE (1 smoke row). Smoke block keys == exactly the 12 whitelisted. In the whole serialised artifact: '123.4' False, '-999.0' False, 'fenced_do_not_report' False, 'rtg_last' False, 'rtg_series' False, 'episode_reward' False, 'sneaky_att' False; 'att_horizon' absent from the smoke block (present only in probe rows, legitimately). Whitelist confirmed (:959), not a blacklist.
- Smoke chunk missing `rtg_first` → REFUSED naming the absent field (:953-958), nothing written.
- Smoke chunk whose `subject` string equals the fence key → REFUSED AssertionError (:1021-1025): the string-level check is over the whole serialisation, conservative.
[x] 5

### Item 6 — 22:42 — the driver (offline/campaigns/p7_2b_calibration.sh)
- By READING: order interpreter :53-57 → import :60-63 → lock :68-75 (`pgrep -f 'python.*offline\.transfer_calibration' | grep -vx -e "$$" -e "$PPID"`) → inputs :77-89 → CANARY :95-103 (refuses `c > 2.0`) → TOKEN :106-113 → `rm -f "$TOKEN"` :115 (deleted on consumption) → first write `mkdir -p "$LOGS"` :119. Canary precedes token: CONFIRMED.
- `fail()` :125 `tee "$WORK/FAILED"` then exit 1; `on_signal` :128-136 `tee "$WORK/FAILED"` BEFORE `kill -TERM/-KILL $CELL_PID` and `kill -- -$$`: CONFIRMED.
- Manifest :167-170: `> SHA256SUMS_p7_2b.txt.tmp && mv … SHA256SUMS_p7_2b.txt`, after all stages, followed only by re-verification :172-174 and COMPLETE :177: CONFIRMED last + atomic.
- Logs: `>> "$log"` at :144 and :146 (append): CONFIRMED.
- `scenarios/draws`: only `$DRAWS` :44, read in `[ -f "$cfg" ]` :79 and passed as `--draws-root` :49; no redirection/rm/mkdir/cp targets it: CONFIRMED. Python side: `parity_sumocfg_path` read + `_sha256_file(config_path)` read (:297,:413).
- MINOR: `trap on_signal INT TERM` is installed at :137, AFTER the token is consumed at :115 — a signal arriving in :115–:137 (sub-second window) consumes the token and leaves neither FAILED nor COMPLETE.
- MINOR: `kill -- -$$` :134 assumes the script leads its process group (true under tmux/setsid, silently a no-op otherwise, `|| true`).
- EXECUTION TEST (no token, `output/p7_2b` absent): NOT RUN — the Bash call was denied by the permission classifier; I did not attempt a workaround. Pre-state verified: `output/p7_2b` absent in both trees, no `output/SHA256SUMS_p7_2b.txt`. By reading, nothing between :53 and :113 creates a path under output/; the only pre-token side effect is the canary's CityFlow episode (:95), whose write behaviour lives in `rtg_calibration.run_probe` (outside my ranges) — UNVERIFIED here.
[x] 6 (reading half done; execution half NOT RUN, permission denied)

### Item 7 — 22:42 — end-of-review snapshot
- scenarios/draws/cityflow1x1: 1442 files (unchanged). draw_0201/parity/noteleport.sumocfg sha c177e962… unchanged; provenance.json sha 8b707dd4… unchanged. `find scenarios/draws, MAIN output/, worktree output/ -newer FINDINGS.md` → empty. `output/p7_2b` absent in BOTH trees. `git status --porcelain` clean in both trees. No leftover transfer_calibration processes. SUMO episodes used: 2 of 3.
[x] 7

## VERDICT: CLEAR WITH CONDITIONS — blocking 0, major 0, minor 3
Minor:
 M1 transfer_calibration.py:184/:200 — a valid-JSON non-object chunk (`[]`) raises AttributeError (not in the caught tuple) → probe traceback instead of move-aside; file untouched.
 M2 p7_2b_calibration.sh:115 vs :137 — the signal trap is installed after the token is consumed; a signal in that window burns the token with no FAILED marker.
 M3 p7_2b_calibration.sh:134 — `kill -- -$$` assumes process-group leadership (no-op otherwise).
Conditions (things I could not verify):
 C1 The no-token driver execution test (item 6) was NOT RUN — denied by the permission layer. Recommend the author runs `bash offline/campaigns/p7_2b_calibration.sh` once with no token and confirms exit 2 and no `output/p7_2b/` before writing the token.
 C2 The canary (`rtg_calibration.run_probe`, CityFlow draw 0) runs BEFORE the token; whether it writes anything (replay files etc.) was outside my reading ranges and unverified.
 C3 Missing/duplicate refusals were verified by construction only; the implementing lines live in `probe_returns_from_chunks` (:942), outside my ranges.
Finished: 22:43 (5.5 min of the 10-min box).
