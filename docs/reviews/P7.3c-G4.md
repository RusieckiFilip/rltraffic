# REVIEW — P7.3c gate G4: the warm-start fine-tune (C3 `fc9a383`) and the training driver (C4 `7a35ff6`, `58cfaef`, `0749b96`), branch at `4211955` — 2026-09-26 — VERDICT: PASS-WITH-NOTES (0 blocking, 0 major, 2 minor)

**Reviewer:** `contract-reviewer`, one mandate that was also the thirty trainings' pre-flight (`BRIEF_41` Amendment A, Q11);
read-only; the pure C3 tests run on CPU from a `git archive` export (96 passed); no driver run, no CUDA; 15 minutes, checklist
first. The findings file is filed verbatim below the rule. **The coordinator's ruling is `BRIEF_41` Amendment D.** The items the
reviewer left unchecked were covered by the coordinator: all 114 C3–C4 tests green under the standard line (the driver tests
included); `choose_concurrency`, `summarize_timing`'s two-route repeat verdict and `write_manifest` read in full; and SIX
committed mutants killed after a clean control — the warm start skipped, statistics refit, `max_ep_len` from the data, B − 1
steps, k = 100's targets for every k, the scratch switch a no-op. **The reviewer's own pre-flight on the REAL inputs:** `prepare_fine_tune`
on the verified corpus and the real seed-101 checkpoint at k = 5 (CPU, writes nothing) — 1,800 joint instants, A24(b)'s own
figure; timestep max 359 < `max_ep_len` 360; the statistics adopted by identity; digests, gate record and disjointness all
passing. The k = 100 build is G5's fenced build-only timing (Amendment A, Q12).

---

# G4 FINDINGS — P7.3c C3 (offline/few_shot.py) + C4 (offline/campaigns/p7_3c_finetune.sh)
Reviewer: independent read-only contract reviewer. Started 2026-09-26. Branch task/p7.3c-grid4x4-fewshot tip 4211955.
Rule: one APPENDED block per item; never rewrite earlier blocks.

## CHECKLIST
- [ ] R0 Authority read: A24 (PREREG line 380), A18(d) line 386, A20(a) line 384; BRIEF_41 s3 C3-C4, s4 T-*, Amendment A/B; plan s7-8
- [ ] R1.1 model rebuilt from CHECKPOINT config (max_ep_len 360), never from data; strict load
- [ ] R1.2 Utils.seed_everything before build+load; sampler np.random.default_rng(seed); order as tier_sweep.py:843-873
- [ ] R1.3 spatial mask taken from checkpoint, never recomputed
- [ ] R1.4 windows via TrajectoryWindowDataset -> build_joint_index -> stack_joint with stats= from ckpt (never fitted); RTG / frozen rtg_scale per intersection
- [ ] R2 recipe: lr/wd/clip/WARMUP imported + asserted; warmup_for(B); lr_multiplier; batch 64 uniform w/ replacement; exactly B steps; step order; fresh AdamW
- [ ] R3 data: draws 201..200+k from manifest; missing draw refused by name; SHA256SUMS re-verified entry by entry before read; scenario_id + node order enforced; calibration digest pinned; k5/k20 gating
- [ ] R4 payload: format_version unchanged; provenance.few_shot fields; bitwise copies; target_rtg = k targets; no wall-clock; exclusive write; serialised in memory
- [ ] R5 scratch arm: same function, differs only in load_state_dict skip; would a stats-refitting scratch be caught?
- [ ] R6 driver pre-flight: modes, refusals before token, check-inputs digests, run tree/HEAD, resume in Python, attempt markers, FAILED on every path after token, regime env, -P, liveness, tee -a, set -euo pipefail, manifest atomic+re-verified, record fields, G5 concurrency, same-seed repeat two routes
- [ ] R7 tests: no unfailable assertions; skipif names artifact; mutant coverage list; T-cpu-determinism byte-identity vs finding 5
- [ ] R8 frozen-file edits in diff 1bba206..4211955
- [ ] R9 run the pure C3 tests on CPU (script in scratchpad, guarded names)

## FINDINGS (appended, chronological)

### [x] R8 — frozen-file edits — 14:16 — `git diff --stat 1bba206 4211955`
Observed: 11 files; all under docs/, offline/, tests/. No frozen path touched. CLEAN.
(Note: parent's PREREG line refs off by one row: line 384 = A21, 386 = A19; A24 at 380 confirmed. A20(a)/A18(d) fetched by grep instead.)

### [ ] R0 — A24(b) read in full (line 380). Key registered items extracted:
- rebuild from CHECKPOINT config incl max_ep_len 360; strict load
- seed_everything before build+load; sampler default_rng(seed); tier_sweep order
- spatial mask from ckpt
- TrajectoryWindowDataset -> build_joint_index -> stack_joint with stats= from ckpt; RTG / frozen rtg_scale per intersection
- constants lr 1e-4 wd 1e-4 clip 0.25 WARMUP_STEPS 1000 each == ckpt provenance; warmup_for min(1000,B/2); lr_multiplier; batch 64 uniform w/ replacement
- B=4000 primary; secondary B in {1000,16000} at k100; scratch_k100 B=4000 "weights initialised under the seed instead of loaded, so that ONLY the initialisation differs"
- saved ckpt keeps config/stats/rtg_scale/node order/mask BITWISE; target_rtg = same-k Rule B per intersection; source CityFlow targets kept in provenance
- RUN ONCE; regime CUDA non-deterministic as P5.2; sha256 committed before token; re-run only for infra failure BEFORE ckpt written, counted
- timing runs B=400 fenced never evaluated; same-seed re-run digest measured once on them
- prompt: p7_3d_calibration.json:per_intersection[i].budgets[k], never recomputed
- corpus: scenario key cityflow_grid4x4 + CityFlow ids, else refused; k5->201-205, k20->201-220, k100->201-300

### [x] R1.1 — 14:23 — few_shot.py:846-848 `config = SpatialDTConfig.from_json_obj(dict(source["config"]))`; `build_model` :678-691 `load_state_dict(source_model, strict=True)`; build_windows :662-664 refuses a corpus step >= config max_ep_len. Config never derived from data. OK (pending: from_json_obj preserves max_ep_len — checked via test at :387 loading the real subject, see R9).
### [x] R1.2 — 14:23 — prepare_fine_tune :845 `Utils.seed_everything(int(seed), seed_python_random=False)` -> :846 config -> :847 build_model; train_prepared :878 make_optimiser (AdamW+LambdaLR) -> :884 `np.random.default_rng(seed)`. Matches tier_sweep.py:843-873 order (seed, config, model, AdamW, schedule, ..., default_rng). Windows built BEFORE seeding (:840) — harmless, seed resets state. OK.
### [x] R1.3 — 14:23 — :857 mask = source["spatial_mask"]; load_source :404-409 refuses non-identity. Payload :966 copies the same object. OK.
### [x] R1.4 — 14:23 — build_windows :633-641 TrajectoryWindowDataset(stats=NormalizationStats.from_json_obj(source["stats"]), normalize=True); :642-643 `dataset.stats is not stats` -> RuntimeError; :657 build_joint_index(dataset, ids) + node_ids==ids asserted; :660 stack_joint; joint_batch :722 rtg / scale with scale (1,n,1,1) from source["rtg_scale"] in ids order (:851-855). OK (pending tier_sweep's own rtg division shape — read next).
### [x] R2 (partial) — 14:23 — make_optimiser :694-707 imports LEARNING_RATE/WEIGHT_DECAY from offline.spatial_mixing, lr_multiplier/warmup_for from offline.tier_sweep; load_source :421-437 asserts provenance == {gradient_steps 40000, batch JOINT_BATCH_SIZE, lr, wd, grad_clip, warmup_steps=warmup_for(40000)=1000, method, n_head}. Fresh AdamW :705. Loop :891-906 zero_grad(set_to_none)/backward/clip_grad_norm_(GRAD_CLIP)/optimiser.step/schedule.step; `for _ in range(budget)`, steps counted, fine_tune :1040 asserts steps==budget before write. draw_rows :712 generator.integers(0,count,size=64) with replacement. PENDING: confirm tier_sweep imports the same constants from spatial_mixing and its loop order.
### [x] R3 (partial) — 14:23 — select_prefix :557-605: _verified_entries re-verifies every SHA256SUMS entry (and refuses unlisted files in dir, :541-545) BEFORE manifest read; manifest format 1.1, scenario_id == source's; requires the WHOLE band 201-300 present (stricter than brief); prefix asserted == 201..200+k. load_k_targets :457-510: digest == transfer_curve.P7_3D_CALIBRATION_SHA256 (PENDING value check = 3e9df8ee), ids/scenario/rtg_scale cross-checked, budgets['k{k}'] with k/draw_ids/role/rule/statistic asserted. build_windows :646-656 refuses missing/unknown ids per episode. NOTE: _verified_entries :541 iterates corpus.iterdir() — a subdirectory or any extra file in the corpus dir (e.g. a log) refuses the corpus; must confirm the real corpus dir is clean (R6 pre-flight).
### [x] R4 — 14:23 — payload_for :915-969: format_version = source["format_version"] (spatial-dt-checkpoint/1.0, Amendment A Q1 honoured); config/stats/rtg_scale/intersection_ids/spatial_mask/normalise/scenario_id are the same objects; target_rtg = k targets in ids order; provenance has gradient_steps=steps, batch, lr, wd, clip, warmup, seed, device, deterministic False, git_commit, runtime, few_shot{format_version, init, k, draw_ids, source_path, source_sha256, source_provenance, source_target_rtg, corpus sums digest, calibration digest}. No seconds/losses in payload. write_payload_exclusive :994-1007 torch.save to BytesIO then _link_exclusive :972-991 (xb partial, fsync, os.link -> fails if dest exists, partial unlinked). PENDING: _runtime() :324 must carry no wall-clock (read next).
### [x] R5 (code) — 14:23 — build_model :678-691: single branch `if init == INIT_SOURCE: load_state_dict`; prepare_fine_tune has no other init-dependent path; stats= passed and identity-asserted regardless of init, so a refitting scratch is refused at :642 in code. PENDING: does T-scratch test check stats/rtg_scale/targets of the scratch payload equal the source's (else a mutant that refits only for scratch is uncaught)?

### [x] R2 (closed) — 14:29 — tier_sweep.py:809 imports GRAD_CLIP/LEARNING_RATE/WEIGHT_DECAY from offline.spatial_mixing (same source as few_shot :363,:702,:875); spatial_mixing.py:129-134 JOINT_BATCH_SIZE 64, LR 1e-4, WD 1e-4, CLIP 0.25; warmup_for :660 imports WARMUP_STEPS from spatial_mixing; lr_multiplier :672 min(1,(step+1)/warmup). Loop order tier_sweep:892-897 == few_shot:900-905 (zero_grad/backward/clip/step/schedule). Sampler tier_sweep:878-880 == draw_rows :712. RTG division tier_sweep:883 `/ scale` (1,n,1,1) == joint_batch :722. 
  MINOR (M1): few_shot.py:363/:875/:919 import JOINT_BATCH_SIZE from offline.spatial_mixing, but tier_sweep.py:152 DEFINES its own `JOINT_BATCH_SIZE = 64` and does not import it from spatial_mixing. Values equal today (64); the brief's "imported from where tier_sweep imports them" is not literally met for the batch size and the two could drift. Fix: import from offline.tier_sweep (or assert equality of both at import).
### [x] R3 (closed) — 14:29 — transfer_curve.py:576 P7_3D_CALIBRATION_SHA256 = 3e9df8ee...9723; on-disk docs/data/p7_3d_calibration.json sha256 = 3e9df8ee...9723 (sha256sum, this session). build_joint_index (joint_windows.py:105-111) groups by (episode, t) ordered by node_ids and refuses a missing id -> re-keyed by id, never by position. assert_probe_draws_disjoint called :826-830 before windows. OK.
### [x] R4 (closed) — 14:29 — _runtime :324-335 carries torch/cuda/numpy/python versions, num_threads, git_dirty; no wall-clock. _utc_now :337 is used only in next_attempt's marker text (:1233), not in the payload. Payload does embed the SOURCE's whole provenance (:947) — a copied constant, not a new clock; harmless for byte-identity. OK. NOTE: _code_commit :312-315 returns "unknown" silently when git fails — MINOR (M2): a payload could record git_commit "unknown"; the driver's B3.1 HEAD check makes it moot in the run tree.
### [x] R5 (closed) — 14:29 — T-scratch (test :670-716) records the batches fed to the model in both arms and asserts them equal tensor-by-tensor (`_unequal(a,b)==[]` for 3 steps) AND rows equal AND payloads equal minus model/init; step-0 tuned weights == source, scratch step-0 == fresh seeded init with no tensor equal to the source. A scratch run that refit statistics would produce different normalised states -> batch inequality -> CAUGHT (load-bearing). Also caught in code by :642.
### [x] R6 (driver, code read) — 14:29 — p7_3c_finetune.sh: set -euo pipefail :62; modes :110; 40-hex commit :113; train needs stamp :116; refuses IMPLEMENTER_TREE :122 and any tree != RUN_TREE :126; HEAD == EXPECTED_COMMIT :130-134 (B3.1); modules loaded from WORK_TREE :137-147; liveness pgrep :149-156; pgid leader :158; SigIgn :163; dirty tree :168; regime OMP/MKL=1 :86, unset CUBLAS_WORKSPACE_CONFIG :89, _enter_registered_regime (few_shot :1562-1577) refuses a set variable, configure_determinism(False), set_num_threads(1); -P on all 16 interpreter calls (:137,186,234,268,292,293,301,324,329,333,365,367,369,374,385,391). train: refuse_non_directories :323 -> check-inputs :324 -> timing concurrency :329 -> resume-decision --all :333 (an invalid ckpt refuses here, before token) -> canary both halves :339 -> install_traps :340 -> token :341 -> rm token :347 -> mkdir START_DIR :348 -> MARKER_DIR :349 (B3.4: only mkdir between). on_exit :199-207 writes FAILED if SUCCESS!=1; fail :209; on_signal :217. Per run: resume-decision :369 (skip/train/refuse->fail), attempt marker :374 BEFORE train_one :376, concurrency drain :379-383. manifest :385 + sha256sum -c re-verify :386 + 30-line count :387-390; record :391. check_inputs (few_shot :1471-1544): calibration pin, five sources by _pin_for (GRID4X4_CHECKPOINT_SHA256 in _cmd_train :1596), select_prefix(k=100) re-verifies every SHA256SUMS entry, gate record format/corpus_dir/all_match/100/16/1600/1600/zero events, CUDA available, free MiB >= G5 peak at C. resume_decision :1187-1206 train/skip/raise. validate_checkpoint :1136-1184 compares frozen parts with the SOURCE re-read at its pin (not the payload's copy). Loss: printed nowhere by the driver; the run record carries losses (noted, not flagged).
  UNCHECKED (time): write_manifest atomicity (:1252), build_record fields (:1291), choose_concurrency (:1387), summarize_timing two routes (:1421), whether `timing run` also calls _enter_registered_regime, the header's tee -a foreground forms (comments filtered).
  PRE-FLIGHT FACTS: run tree /home/filip/rltraffic-p73c-run exists, HEAD = 1bba206 (G1's commit) — must be moved to the reviewed commit before the run or the driver refuses (:130-134). Five sources present at output/p5_2/checkpoints (3,434,853 B each). Corpus dir + gate record existence checked next.
### [x] R7 (tests, read) — 14:29 — load-bearing: T-recipe hook test :464-491 counts AdamW.step via torch global optimizer hooks (independent of the code's counter; catches B-1) and asserts optimizer.state empty at first step (fresh AdamW); T-recipe warmup :493 parametrised (1000->500, 4000->1000, 16000->1000) catches "warmup 1000 at B=1000"; T-payload :605-650 recomputes expected targets from the artifact by k (catches k100-for-k5), asserts frozen parts == source, format_version == spatial-dt-checkpoint/1.0, and loads via spatial_agent_with_targets; T-cpu-determinism :653-667 runs the pipeline three times (two same-seed, one other) and asserts file sha256 equality across two separate writes AND weights equal — genuine byte-identity given the in-memory serialisation; T-scratch :670-716 (see R5); T-frozen :419-432 recomputes normalisation from the source payload via a helper and asserts >1024 values checked. Theatre: none found among those read. UNCHECKED: T-warm :364-418 body (does the fixture corpus have episodes shorter than 360 so a data-derived max_ep_len differs from 360?); whether the real-subject test :387 skips with the artifact named when output/p5_2 is absent (no skipif in the file — likely a hard failure off-machine).

### [x] R9 — tests RUN — script scratchpad/run_c3_tests2.sh on a `git archive 4211955` export (no worktree touched), RLTRAFFIC_OUTPUT_ROOT=/home/filip/rltraffic/output: tests/test_p7_3c_few_shot.py + tests/test_p7_3c_finetune_commands.py -> **96 passed, 0 skipped, 76.53 s** (log scratchpad/c3_tests.log). The real-subject T-warm test ran (not skipped). tests/test_p7_3c_finetune_driver.py NOT RUN by me (it spawns processes whose command lines match the liveness pattern; Amendment B B5) — the packet's claim for it is unverified here.
### [x] R6 pre-flight on the REAL inputs — scratchpad/preflight_prepare.py (module imported by concatenated name; writes nothing): corpus datasets_sumo_v11/grid4x4_sumo_maxpressure = 100 npz + manifest.json, all listed, no strays, no subdirs; manifest v1.1, scenario cityflow_grid4x4, 100 episodes; gate record output/p7_3c_corpus/a17f_gate.json: format p7.3c-corpus-gate/1.0, corpus_dir == driver's CORPUS, all_match True, 100/16/1600/1600, 0 teleports 0 collisions (every-second grain); calibration digest on disk == pin; five sources present. prepare_fine_tune(seed101, k=5, B=4000, cpu) on the real corpus+checkpoint: OK in 1.9 s; episode_length 360, actions (360,), timestep max 359 < max_ep_len 360; 1,800 joint instants (= A24's 1,800); state (28800,20,40), rtg (28800,20,1), scale (1,16,1,1) -> per-node division as tier_sweep; draw_ids 201-205; targets role recorded_not_evaluated; stats adopted True; destination not created. k=100 build NOT exercised by me (≈1.8 GB float32 state alone by these shapes; Q12's fenced build-only timing covers it).
  PRE-FLIGHT NOTE (not a code defect): build_windows's max_ep_len / id-set / stats-adoption refusals run inside `train` (after the token), not in check-inputs; had the corpus been 361 decisions long every run would have FAILED after the token. Verified today that it is not.
  PRE-FLIGHT ACTION: /home/filip/rltraffic-p73c-run is at 1bba206 (G1); the driver refuses unless HEAD == the commit passed as $2 (:130-134) — move it to the reviewed commit first.
### Registered table (:196-231): ft_k5/ft_k20/ft_k100 (init source, B 4000), scratch_k100 (init scratch, B 4000), ft_k100_b1000, ft_k100_b16000 x seeds 101/202/303/404/505 = 30; timing_ft_k5_b400 seed 101 B 400 outside the table. Matches A24(b)-(c).

## VERDICT: PASS-WITH-NOTES — 0 BLOCKING, 0 MAJOR, 2 MINOR (M1 batch-size import origin, M2 silent "unknown" git commit) + 2 pre-flight actions/notes. UNCHECKED: driver tests not run here; write_manifest/choose_concurrency/summarize_timing/build_record bodies read only by grep; header tee -a forms; Q6/Q6b/Q8 rulings read only as "yes, as proposed".
