# PRE-FLIGHT RE-REVIEW — P7.3d campaign driver + report path (BRIEF_39 gate G4, second pass) — 2026-09-24 — VERDICT: CLEAR-WITH-CONDITIONS (0 blocking)

**Reviewer:** `contract-reviewer`, on the RUN worktree `/home/filip/rltraffic-p73d-run` detached and clean at **`ec5bf19`** — the tree the campaign
runs from. Mandate: falsify the closure of the first pass's three blockers by EXECUTION; the report body on a complete synthetic set; the
run-worktree rule executed in a tmux pane; the one-fixture rule; the same-quantity pin; `dt_reroll_check`. Six mutations (5 killed, 1
equivalent); `tests/test_p7_3d_campaign_path.py` 42/42 net. The findings file is filed verbatim below the rule. Nothing written under any
repository tree; sandboxes and the throwaway worktree removed.

## Coordinator's ruling (2026-09-24)

1. **The three first-pass blockers are CLOSED, by execution, on `ec5bf19`.** B1: `canary` with `COMMON` before the subcommand parses and runs
   (0.71 s, both halves); B2: a grid4x4 chunk carrying hz1x1's digests is REFUSED by `chunk_is_reusable`, `reusable_chunk_at` and `report`
   (which names the cell and both digests), with grid-digest controls accepted; B3: `manifest` exists, `declarations_for` gives 700/700 with
   zero name overlap against hz1x1's 4,700, `report` names `p7_3d_grid4x4.json`, and the bare stage word is refused before any write. The
   run-worktree rule executed under the header's own Step-2 line: tree derived from the script's location, the implementer-tree refusal in
   place, the group-leader guard passed, `DRIVER EXIT: 2` in the capture from the dirty-tree refusal the reviewer's own `sed` caused.
2. **The reviewer's MINOR on `per_intersection_rho` is B.7.5's finding, independently reached** — a mean of per-draw ratios, no
   per-intersection diagnostic, coinciding with a ratio of means only because the stub denominators are constant. It is not a request-wording
   question: B.7.2 is a registered ruling and it goes into B.7.4's commit (B.7.5-2). **Therefore this verdict CARRIES to the B.7.4 commit on
   every item except item 4's `per_intersection_rho` block, which the coordinator re-checks by hand on the new commit** (a ratio of means
   recomputed from synthetic rows whose denominators vary across draws).
3. **CONDITION adopted, into the token block:** the campaign starts on an otherwise IDLE machine, on mains — under the reviewer's own concurrent
   load the driver's canary read 2.01–2.09 s against 0.71 s alone, and the 2.0 s refusal has under a second of headroom on this machine.
4. **Minors, recorded for the packet:** mutation 8(a) EQUIVALENT (the comparison iterates the union of keys, so `actions` is compared whether or
   not the constant names it; the tests' presence-loop omits `"actions"` — one line, B.7.4's commit); `write_manifest` hashes every file under
   `output/p7_3d/`, which already holds `calibration/` and `g2/` — the real manifest lists them, the packet says so.
5. **Verified-good facts worth keeping:** `_git` runs in the module's own tree, so chunks record the RUN worktree's HEAD regardless of the
   driver's `cd` — J1(c) compares against the code that ran; `report` creates nothing on any refusal route observed.

---

# Reviewer's findings file, verbatim

# P7.3d pre-flight RE-REVIEW (BRIEF_39 G4, second pass) — FINDINGS
Run worktree: /home/filip/rltraffic-p73d-run @ ec5bf19 (to verify)
Started: 2026-09-23

## CHECKLIST
- [ ] 0. Worktree identity: detached, clean, HEAD == ec5bf19
- [ ] 1. B1 closed: COMMON before subcommand in every driver call; canary shape parses
- [ ] 2. B2 closed: run_cell grid4x4 branch (demand_identity scenario, load_grid4x4_targets, A20(a) checkpoint, assert_rtg_first_matches_targets called, calibration sha pinned); INVISIBLE-REUSE falsification with hz1x1 digests on a grid4x4 chunk -> refused/re-rolled
- [ ] 3. B3 closed: manifest --help; declarations_for(grid4x4_confirmatory, None) == 700; report names p7_3d_grid4x4.json; bare `--stage confirmatory` against grid4x4 chunks refuses, writes nothing
- [ ] 4. Report body (B.7): complete synthetic set -> written artifact fields (scenario_key, calibration sha, subject, arms == A21, H3 clause-1 inequality on b_mean_k100, A21(b) scope sentences verbatim vs PREREGISTRATION.md, in-support 16 ids, per_intersection_rho ratio-of-means recomputed by hand + denominator_diagnostic, six reference cells checked); MUTATION M-ref -> REFUSED, out-dir empty
- [ ] 5. Run-worktree rule (B.7.1-1): WORK_TREE from BASH_SOURCE; sandbox copy of driver run in detached tmux without token -> refusal chain recorded; text refuses /home/filip/rltraffic-p73d
- [ ] 6. Fixture (B.7-3 / DEFERRED 91): one fixture for roots/token/--limit in both test files; refusal tests assert no chunk written; MUTATION M-fix (--limit 0 -> none) in throwaway worktree, 120 s timeout
- [ ] 7. Same-quantity pin: per_intersection_local_returns two routes == on POST-step infos both cell kinds; MUTATION shift-by-one -> test dies; validate_cell_payload re-checks both keys on anchor AND DT chunks
- [ ] 8. dt_reroll_check: stage before token; compares actions + both series + outcomes, clocks excluded BY NAME; prints IDENTICAL/NOT IDENTICAL + digests; NOT IDENTICAL refuses token; MUTATION drop actions -> test dies
- [ ] 9. Anything else that could make a NUMBER wrong; packet §25 vs disk
- [ ] 10. Cleanup: tmux sessions, sandboxes, throwaway worktree

## FINDINGS (append-only, one per entry)

### [x] 0. Worktree identity — 22:18
cmd: git -C /home/filip/rltraffic-p73d-run rev-parse HEAD; status --porcelain; symbolic-ref -q HEAD
obs: HEAD=ec5bf19ddcfd73157c128f7003726f680a3456a4, porcelain empty (clean), symbolic-ref fails -> DETACHED. Matches request.
### 1 (partial). Driver call sites — grep 'offline.transfer_curve' offline/campaigns/p7_3d_grid4x4.sh
obs: lines 259 (check-inputs), 286 (resume-check), 296 (canary), 317 (dt-reroll-check, with --canary-seconds BEFORE subcommand, after COMMON), 383/386/389/392 (collect/report presumably) — all have "${COMMON[@]}" before the subcommand. Line 175 is an import check (-c), not a subcommand. Still to run: canary shape.

### 2 (reading). run_cell grid4x4 branch — 22:20
offline/transfer_curve.py:2206 grid = scenario_of(cell)==GRID4X4_SCENARIO_KEY; :2208 demand_identity_for(cell) (scenario-aware, :1047); :2214-2224 DT+grid -> load_grid4x4_targets, grid4x4_support_ranges, checkpoint_identity_for -> grid4x4_checkpoint_identity (:1130-1136, A20(a)); :2304 _grid4x4_payload(...). chunk_is_reusable :2135 re-derives via demand_identity_for(cell) and :2146 re-checks target_rtg against load_grid4x4_targets. STILL TO CONFIRM: assert_rtg_first_matches_targets call site and calibration_sha256 in _grid4x4_payload; then run the invisible-reuse falsification.
### 1 (reading). CLI parser — :4849-4855 roots are parent-parser options; --canary-seconds parent option; subcommands cells/report/canary/record-canary/check-inputs/resume-check/dt-reroll-check/manifest exist (:4857-4905). Canary path in main :4921-4929 calls measure_canary()+check_canary() only — no filesystem write from roots (to confirm in transfer_calibration).
### provenance digests read — hz1x1 draw 1000: cfg c177e9625953e752d8c49f3df21f83715f93020d69cff43da8e39705611fbeda, routes c2c5eea23627a1905e8f786defca53bb04ce00c0fa475130219e65bb36f9b8c6; grid4x4 draw 1000: cfg c27d31e831e1dc1b4b4c90c73e4d64ed4684990b45c075f47312081afbf9a2cd, routes 6ab4d6d749635be77d74548f2a78fb3d87ca9a014d536c30412019663d7166cc.

### [x] 1. B1 CLOSED — 22:21
cmd: cd /home/filip/rltraffic && PYTHONPATH=<run> python -P -m offline.transfer_curve --draws-root <real> --output-root <real> --work-dir <sb> --data-dir <run>/docs/data --out-dir <sb> canary
obs: "canary 0.71 s {att_horizon 247.75089149261333, decisions 360, local_return -32648.0, two_routes_agree true}" exit=0; no file written to sandbox. Parser :4849-4855 roots+--canary-seconds on parent; driver lines 259/286/296/317/383-392 all put "${COMMON[@]}" first. Canary itself (transfer_calibration.py:2177-2206) reads P4_3 probe artifact + draw 0 only; writes nothing.
### [x] 3 (a,b,c). B3 partially CLOSED — 22:21
obs: manifest --help exits 0, requires --campaign-dir (:4902-4905). declarations_for("grid4x4_confirmatory", None) -> whole 700 == stage 700, all cityflow_grid4x4, Counter dt/b_mean_k100 500, fixedtime 100, maxpressure 100 (:2679-2683). hz1x1 confirmatory still 4700/1200, anchor 700/700; name overlap grid vs hz whole = 0. artifact_name_for_stage: grid4x4_confirmatory -> p7_3d_grid4x4.json (:4415-4416), confirmatory -> p7_3a_zero_shot_stage1.json, anchor -> p7_3b_anchor.json, None -> p7_3a_zero_shot.json. main :5047-5057 uses it. PENDING: bare-word refusal (3d).
### 2 (reading, continued). _grid4x4_payload :2536 calibration_sha256 = P7_3D_CALIBRATION_SHA256; :2550 validate_cell_payload on finished payload; validate :2043 calls assert_rtg_first_matches_targets(series, targets) for grid DT; :1928-1947 re-checks local_return & local_return_from_lanes keys==ids and equality under != on EVERY grid chunk (anchor and DT); :1951-1956 calibration pin per scenario; :2031 16-id check targets vs intersection_ids. chunk_is_reusable :2135 scenario-aware demand. PENDING: executed falsification.

### [x] 2. B2 CLOSED (executed) — 22:24
construction: sandbox work dir; anchor fixedtime draw 1000 + DT seed101 draw 1000 from declared_cells("grid4x4_confirmatory"), payload via tests' _grid_payload with hz1x1 draw-1000 digests (c177e962…, c2c5eea2…) -> write_chunk -> chunk_is_reusable / reusable_chunk_at(cell=..., out_root=real draws, output_root=real output, data_dir=run docs/data)
obs: hz digests anchor -> False/False; hz digests DT -> False; CONTROL grid digests anchor -> True, DT -> True (check discriminates); DT with one target +1.0 -> False (:2146). reusable_chunk_at has 2 call sites in module. report() over the hz-digest work dir (1 chunk) refused with "698 declared cell(s) have no chunk" (completeness precedes digest step); out dir not created. NOTE: the digest-step refusal in report is still to be observed on a COMPLETE set (item 4 extra mutation).
### 3(d) first attempt — bare "report --stage confirmatory" against sandbox holding grid4x4 chunks (no canary.json): refused FileNotFoundError at _read_canary_record (:2586), exit 1, out-dir NOT created. Refusal fired for canary reason, not stage; redo with canary.json present.
### note: PREREGISTRATION.md lives at repo root (tests/_a21_items reads REPO_ROOT/PREREGISTRATION.md), not docs/.

### [x] 3. B3 CLOSED (3d executed) — 22:26
construction: sandbox work dir holding 2 grid4x4 chunks + canary.json; CLI "…COMMON… report --stage confirmatory" (bare word)
obs: refused ValueError "1200 declared cell(s) have no chunk (first: cell_anchor_fixedtime_seednone_draw1000.json …)" (:2788) exit=1; out-dir NOT created. The bare word selects hz1x1's 1,200-cell declaration and the completeness check refuses before any write. Driver maps confirmatory->grid4x4_confirmatory at :217 (STAGE_ARG), test_g2_measure:627 pins it.
### [x] 5. RUN-WORKTREE RULE (executed) — 22:26
construction: throwaway detached worktree at <scratch>/wt @ec5bf19; sed CAMPAIGN_DIR/TOKEN -> sandbox (tree now dirty); header Step-2 line with the two paths substituted, typed into a detached interactive tmux pane (tmux new-session -d; send-keys), no token.
obs (capture): "WORK_TREE <scratch>/wt (derived from this script's own location)"; no "implementer's worktree" refusal; no "loaded from" refusal (import check PASSED); no "not a process-group leader"; no SigIgn; stage OK; inputs/ckpts/parity present; check-inputs printed 9 digests "(the pin)" incl. calibration 3e9df8ee…, reference_cells 5265f0d5…, cap_e 949015b3…, 5 ckpts, RESCO net 8d192de4…; THEN fired: "REFUSING TO START: the worktree <wt> is DIRTY / M offline/campaigns/p7_3d_grid4x4.sh"; "DRIVER EXIT: 2" as capture's last line. Sandbox holds only capture.txt; nothing under /home/filip/rltraffic/output/p7_3d/ or p7_3d_runs/ created (listing shows only pre-existing calibration/, g2/ and the p7_3d_runs subdirs). Driver text: :123 WORK_TREE=$(cd "$(dirname "${BASH_SOURCE[0]}")/../.." && pwd -P); :124 IMPLEMENTER_TREE=/home/filip/rltraffic-p73d; :165-172 refuses when equal, exit 2, names RUN_TREE. Order: interpreter -> implementer-tree -> import -> live runner -> leader -> SigIgn -> stage -> inputs -> ckpts -> parity -> check-inputs -> dirty -> RSS -> resume-check -> canary -> reroll -> trap -> token (matches header §3).

### [x] 4. REPORT BODY (executed via CLI, driver's argument shape) — 22:34
construction: 700 stubs from tests' _complete_set(git_commit=HEAD ec5bf19) + _write_stubs + _canary_record; CLI "…COMMON… report --stage grid4x4_confirmatory", cwd=/home/filip/rltraffic, PYTHONPATH=run worktree
obs: exit 0, wrote sb4/out/p7_3d_grid4x4.json (700 cells). scenario_key=cityflow_grid4x4; format p7.3d-grid4x4/1.0; inputs.calibration_sha256 == P7_3D_CALIBRATION_SHA256 == sha256(docs/data/p7_3d_calibration.json); subject mappo1000_dt_nomix_h4; arms.evaluated {registered:[b_mean_k100], anchors:[fixedtime,maxpressure]}, not_evaluated {naive, random, b_max_k100, a_q1.0} (A21 set); h3 clause 1 inequality "rho_sumo(b_mean_k100) > 0" on e_sumo and att_env, status CONFIRMATORY…, point_estimate_satisfies present; clause 2 "< 1" with no scored keys; clause_3 "void (A19)…". what_this_does_not_say[:2] == A21(b)(i),(ii) extracted by MY regex from PREREGISTRATION.md:382 (verbatim, True) and == module A21_SCOPE_SENTENCES (:500). in_support.per_intersection has 16 keys == IDS. reference_cells n_checked 6, all_reproduce True, artifact_sha256 == sha256(p7_3d_reference_cells.json).
per_intersection_rho: 16 entries, keys {mean_rho, n_draws_used, n_draws_excluded, draw_ids_excluded}; hand recompute (my own loop, no module fn) rho_i[B1]=0.40625 n=100 excl 0 == artifact; rho_i[B2]=0.5277777777777778 n=90 excl 10 == artifact. DEFINITION AS CODED (:3695-3755): per-cell ratio -> seed mean within draw -> mean over usable draws (B.7.1-2's formula), i.e. MEAN OF PER-DRAW RATIOS, NOT a ratio of means (on B1 the two coincide only because the synthetic denominators are constant). NO per-intersection denominator_diagnostic key (neither per entry nor on the block) — only exclusion counts/ids; network-level denominator_diagnostic present under rho.definitions.{e_sumo,att_env}. B.7.1-2 requires exclusion+count only -> request wording mismatch, MINOR.
hand network rho_e_sumo mean 0.586875 == artifact rho.registered_arm.e_sumo.mean == h3 clause-1 e_sumo mean_rho.
MUTATION M-ref(a): data-dir COPY with reference cells[0].e_sumo += 1.0 -> exit 1 "…p7_3d_reference_cells.json has sha256 9bb23085…, not the pinned 5265f0d5…" (digest pin at load), out-dir absent -> KILLED (refusal at digest, before value comparison).
MUTATION M-ref(b): one chunk (maxpressure draw 1002) e_sumo += 0.5, real data-dir -> exit 1 "the reference cell(s) do not reproduce … maxpressure draw 1002 differs on ['e_sumo']" (:3646), out-dir absent -> KILLED.
EXTRA (item 2 closure in report): complete set with anchor fixedtime draw 1050 carrying hz1x1 draw-1050 digests -> exit 1 "cell_cityflow_grid4x4_anchor_fixedtime_seednone_draw1050.json: config_sha256 'c177e962…' is not draw 1050's 'c27d31e8…'"; out-dir absent -> report's digest step is scenario-aware and names the cell.
### 9 (partial). _git runs in _REPO_ROOT (module's own tree, :604-605), not cwd -> chunks record the run worktree's HEAD and code_changed_since compares against the run worktree's HEAD even though the driver cd's to MAIN. Confirmed by item 4 (stubs at ec5bf19 accepted from cwd=/home/filip/rltraffic whose HEAD is 8857e5e).

### [x] 6. FIXTURE (B.7-3) — 00:34
obs: tests/test_p7_3d_campaign_path.py: DriverSandbox (:1166) built only by fixture driver_sandbox (:1209); --limit appears only in the fixture's _substitute (:1240-1243) and its own assertion (:1278); TOKEN redirected at :1235-1238; 7 tests take driver_sandbox; the only driver executions are DriverSandbox.run (:1191, setsid) and the tmux test (:1401-1403, on sb.driver). Refusal tests assert no chunk: :1426-1427, :1441-1442, :1468-1469, :1486-1487 (assert_no_experiment_started + campaign dir absent). tests/test_g2_measure.py executes NO driver (no bash invocation; its Popen/run calls at :235/:265/:375/:382 are python -c experiments) -> text-only, nothing to take from a fixture.
MUTATION M-fix (throwaway worktree wt @ec5bf19): fixture line -> no --limit. test_the_fixture_builds_every_driver_with_sandbox_roots_and_limit_zero FAILED in 2.63 s at :1278 (AssertionError on '--limit 0'); 0 cell_*.json under basetemp; no driver executed -> KILLED without rolling anything.
### [x] 7. SAME-QUANTITY PIN — 00:34
obs: per_intersection_local_returns (:1282-1309) calls rtg_calibration.episode_return_two_routes over POST-STEP infos (tap.post_step, appended only in _StepTap.step :1804-1808; reset info excluded :1793-1795), refuses per intersection on !=. Called on both cell kinds (run_cell :2319, grid branch covers anchor and DT). validate_cell_payload :1928-1947 re-checks both keys (== ids, equality) on EVERY grid chunk incl. anchors; test :922-935 covers DT + anchor.
MUTATION (wt): lane route computed over post_step[:-1] (one-info shift on one route) -> test_the_per_intersection_return_is_the_post_step_sum_by_two_routes FAILED: "the two return routes disagree on intersection 'A' (-4.0 … against -3.0 …)" -> KILLED. NOT EXECUTED: a _StepTap mutant appending the PRE-step info (both routes shift together) is caught only by the e2e's real-episode assertion (packet §25.5 bullet 3); I did not run that mutant.
### [x] 8. dt_reroll_check — 00:34
obs: driver :317-325 runs dt-reroll-check BEFORE the trap (:350) and the token (:353-359); non-zero exit -> "REFUSING TO START: dt_reroll_check did not report IDENTICAL", exit 2, token untouched. main :5037 returns 2 unless verdict == IDENTICAL. compare_reroll_payloads :4635-4681 compares EVERY key of every roll vs roll 0 as canonical bytes, clocks excluded BY NAME (REROLL_CLOCK_FIELDS=("seconds",) :4605), REROLL_COMPARED_FIELDS (:4606-4608: actions, rtg_series, reward_series, e_sumo, att_env, episode_reward, n_teleports) enforces PRESENCE only; lines :4777-4788 print verdict + field NAMES + digests, no values.
MUTATION 8(a) drop "actions" from REROLL_COMPARED_FIELDS -> 4 reroll tests PASSED -> SURVIVED, but EQUIVALENT for detection: the comparison iterates set(reference)|set(other) (:4665), so an action difference is still NOT IDENTICAL; the constant only guards absence, and validate_cell_payload (:1917-1925) already requires actions on grid chunks. MINOR: the presence-loop at tests/:796 omits "actions".
MUTATION 8(b) "actions" excluded from the comparison (added to REROLL_CLOCK_FIELDS) -> test_the_comparison_excludes_the_clock_by_name_and_nothing_else FAILED and test_a_one_action_difference_in_one_pooled_roll_is_not_identical_and_prints_no_value FAILED -> KILLED.

### 9 (continued) — 00:35
- resume-check --stage grid4x4_confirmatory against the REAL --work-dir /home/filip/rltraffic/output/p7_3d/cells (absent): "every chunk on disk … resolvable", exit 0. cells/ and artifacts/ do not exist yet; TOKEN_confirmatory absent; MemAvailable 41,608 MiB > budget 24,216.
- run worktree log: ec5bf19 (docs, §25) <- 3fe5f2e (B.7 code, ONE commit) <- 6b88a37 (plan) <- e0816b5 merge main. Packet §25.1 matches.
- artifact size on my complete-set run 4,374,872 B vs packet §25.3 4,374,873 B: 1-byte difference consistent with canary.json seconds ("0.8" via _canary_record vs the driver's measured 2-decimal value); not a finding.
- real output/p7_3d/ already holds 216 files (calibration/, g2/ incl. dt_reroll_check_20260923T105546Z); write_manifest (:4440) hashes EVERY regular file under campaign_dir, so the real manifest will list them too (tests expect 702 in a sandbox). Cosmetic, not a number.
- FIRST RUN of tests/test_p7_3d_campaign_path.py (background, from the run worktree, WHILE my mutation pytest runs and git clones were executing concurrently): 4 failed, 38 passed in 58.19 s. ALL FOUR refused at the driver's TIMING half: "canary 2.01 / 2.05 / 2.09 s exceeds 2.0 s" (e2e :1307, tmux header :1416, not-identical reroll :1438, complete-set :1656). Standalone canary earlier: 0.71 s. Contention from my own concurrent runs -> re-running the four alone now.

### [x] 9. RE-RUN of the four driver tests ALONE — 00:38
obs: "4 passed, 38 deselected in 88.10s" (e2e through the real module on draw 5, tmux header form, NOT IDENTICAL refusal, complete synthetic set through the driver to COMPLETE). Canary lines in the captures 0.72-1.00 s. The e2e rolled exactly cell_cityflow_grid4x4_mappo1000_dt_nomix_h4_b_mean_k100_seed101_draw0005.json and cell_cityflow_grid4x4_anchor_fixedtime_seednone_draw0005.json into the sandbox (fenced draw 5). Net over both runs: 42/42 of tests/test_p7_3d_campaign_path.py observed passing; the 4 first-run failures were the driver's own 2.0 s timing refusal under my concurrent load. Run worktree status: 0 lines; /home/filip/rltraffic/output/p7_3d/ still holds only calibration/ and g2/.
### [x] 10. CLEANUP — 00:38
wt worktree removed + pruned (worktree list shows no scratch entry); my tmux session pf2_p73d killed; pre-existing sessions fix05/p53bfix/p71a4 (Aug/Sep) are NOT mine and were left alone; sandboxes sb1/sb2/sb4/sb5/mut-basetemp/pytest-basetemp/pytest-basetemp2 removed; FINDINGS.md and suite_campaign_path.txt kept in the scratchpad.
