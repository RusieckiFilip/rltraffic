# Independent review — P4.7 (the mixture tiers)

**Verdict: PASS-WITH-NOTES** — 2 MAJOR, 8 MINOR, 4 NOTE, **no blocker**
**Reviewed at:** `2021cc7` (`task/p4.7-mixture-tiers`) · **Run and written:** 2026-08-15
**Written BEFORE the fix round**, unlike `P4.6.md` — see that file's §0 for why that mattered.

---

## 1. Scope and corpora

`offline/mixture_tiers.py` (1318 lines), the `offline/method_tier_grid.py` delta, four new test files,
`offline/campaigns/p4_7_phase2.sh`, the plan, the packet, the full diff.
**Corpora named:** 12 mixture `eval_*.json` × 500 = **6,000** records · 18 phase-1 cells · 3 constructed
references (1,500 records) · 60 checkpoints · **112** files · `p4_7_grid.json` (**19,200** episode
records, 40 cells, 160 + 32 comparisons).
⚠️ **Every count from full output; no `tail` anywhere in this review** — the implementer made that
error twice in this task, so the reviewer's own method is stated.

**Suite RUN** in a `git archive` copy under the scratchpad, never in the worktree:
**844 passed, 3 skipped, 13 warnings in 100.83 s**, reproducing the packet exactly. **12 mutations
executed: 10 killed, 2 survived.**

## 2. MAJOR

### M1 — Q2's ordering is not robust to the training-seed dimension, and nothing reports that
Q2 HELD is a **conjunction**. Its **positivity** conjunct is robust — 15/15 per-seed advantages
positive, minimum **+9.63**. Its **ordering** conjunct rests on `mix33 (156.88) > mix50 (129.47)`, a
gap of **27.41 ATT that reverses on 2 of the 5 training seeds**:

```
mix33 − mix50 per seed: +24.47, −28.30, +114.46, −2.37, +28.77   mean 27.41  SE 24.06  t = 1.14
mix50 − mix67 per seed: +122.98, +108.07, +21.54, +109.65, +70.88 mean 86.62  SE 18.43  t = 4.70
```

**The companion CIs the packet prints are draw-level with the 5 seeds averaged into each unit**
(`offline/offline_baselines.py:2204`), so they carry **no information about the seed dimension**. The
convention is inherited and legitimate; **its blind spot is an order of magnitude larger here than
anywhere in phase 1** — between-seed sd of the `bc` cell is **18.1 / 57.8 / 29.8** on the mixtures
against **≤ 6.0** on every phase-1 cell (`bc@fixedtime` 0.60). `bc@mix50` seed 303 lands at 135.6 where
seed 404 lands at 285.4. **No between-seed quantity appears anywhere in the plan or packet.**
**Discriminating power: the distance between "held" and "failed" on that clause is one training seed.**

### M2 — two mutations in the report-assembly path survive the full 844-test suite
`mixture_grid_artifact`, `_p4_6_prediction_sidecar`, `_load_report_inputs`, `_comparison_objects` and
the `report` subcommand are called by **no test**.
- **Mutation J** — `assert_phase1_reproduces(payload, payload)`, i.e. **Gate P1 compares the artifact
  with itself and can never fail** → `844 passed, 3 skipped`.
- **Mutation H** — the P4.6 sidecar reads `payload["predictions"]` instead of `committed[...]`, so
  P4.6's recorded `NOT SCORABLE` silently becomes `FAILED` in both columns — **the precise rescue
  RULING 2 exists to prevent** → `844 passed, 3 skipped`.

**Not a blocker because the gate's actual result was verified independently:** **1,896** phase-1 leaves
(wider than the 1,370 the gate compares) equal `p4_6_grid.json`, **0 mismatches**. **Sixth sighting of
the guard family** (`DEFERRED` 33/42/44 + twice in P4.6).

## 3. MINOR

- **N1** Q2's tie clause checks **adjacent** pairs; the plan and the artifact's own `rule` string say
  *"any two"*. Conservative (cannot manufacture a HELD), no outcome moves, but the artifact
  **mis-describes its own implementation**.
- **N2** **Q2's confirmation is largely a measurement of BC, not of the filter.** %BC spans **1.10 ATT**
  across the three mixtures against BC's **115.13**, so the ordering is ~99 % determined by BC's
  dilution response. Q2 stays falsifiable; the packet must say what its evidence is *about*.
- **N3** 🚨 **A PRE-REGISTERED DISCLOSURE WAS NOT DELIVERED AND THE ARTIFACT ASSERTS IT WAS.** The plan
  registered that the per-tier count of draws in **both** components be reported, and the volume check
  called **weakened** rather than null if large. Measured: **39 / 48 / 44** (mix50's 200 streams collapse
  to **152** distinct draws). **The count is in no artifact, the word "weakened" appears nowhere, and
  the packet calls the demand half "a real null".** `mixture_tiers.py:764` claims it is reported —
  false as written. Its only test asserts a string contains `"set"` and cannot fail.
- **N4** the promised `docs/data/p4_7_prediction_sidecar.json` does not exist; content lives in
  `p4_7_grid.json → inherited_predictions` (correct, nothing rescued) but the substitution is not in the
  deviations list.
- **N5** packet headline says the DT is within **4.9 / 3.5 / 2.0** ATT of the leader; recomputed
  **3.7856 / 3.4673 / 2.0015**. **4.9601** is the DT's gap to the best cell *anywhere* — two definitions
  mixed. Conclusion unaffected; mix33 overstated by 31 %.
- **N6** the packet's self-review says *"it has not been launched yet"* — stale from the PARTIAL packet
  and contradicted by §1 and by `phase2.log`. **A mandatory checklist line that is false on its face.**
- **N7** `Q1.outcome: HELD` carries no verdict-level qualifier; the tautology disclosure sits three
  levels down. Q3 has the better pattern in the same file.
- **N8** `Q3.falsifies_r2: false` is unqualified at its own level — a reader concludes R2 survived a
  test that could not be run.
- **N9** count slips: **17 → 16** `match=` tokens across **6 → 5** pre-existing files; diff stat
  **619/210329 → 622/210332** (the commit that fixed it did not re-measure after its own edit).
  `BRIEF_19` §5.2's seed/draw-block report is in the plan and declaration, not the packet.

## 4. NOTE

- **N10** **"MADT" appears once on the branch** — `docs/plans/p4.7.md:9`, **inside the sentence
  declaring that it appears nowhere.** Self-negating; C9 rule 2 forbids the string in prose.
- **N11** the report-assembly wiring has no test at all (M2's structural half).
- **N12** *"every mutation executed and its failure pasted"* is partial — 6 failures pasted, 23 named by
  their killing test.
- **N13** the paired comparisons average 5 seeds into each per-draw unit while the constructed reference
  draws an independent composition per seed, so its unit is one fifth of the way to the forbidden
  expectation in variance. **Measured and resolved in the design's favour** — see §5.

## 5. What reproduced, by independent routes

- **12/12 cell means** by `math.fsum` from 6,000 raw records: **10 exact, worst 2.842e-14**. Every cell
  500 episodes, 500 distinct `(seed, draw)`, draws `1000..1099`.
- **Q2's three advantages** recomputed: `156.87765179739313 / 129.4712538814495 / 42.84650902296809`,
  identical to 2.8e-14. Companion CI widths to **7.1e-15**.
- **A8(a) holds, timestamped.** Plan §4 introduced at `7df6d97` **11:41:22**, byte-unchanged since;
  `mixture_tiers.py` last changed **15:38:07**; first checkpoint mtime **16:52:46**; `phase2.log` starts
  **16:50:51**. **The rule and its scorer both predate every mixture number.**
- **The constructed reference is clean on all five sub-checks.** 500/500 records equal the *assigned*
  component's stored triple exactly; the components never coincide (0/500 equal ATT), so the check
  discriminates and an expectation would fail 500/500. The RNG was **re-derived from the plan's prose
  alone** and matched all 15 (tier, seed) assignments. Means `322.2417 / 267.2651 / 212.2675`, bracketed,
  each within **0.0474 / 0.0321 / 0.0042** of its f-implied interpolation.
  **N13 measured: the forbidden expectation would report CIs 29.2× / 41.0× / 56.8× narrower on the
  `bc_top10` rows. The ruling's protection is real and material where it matters.**
- ⭐ **R3's falsification survives on THREE independent operationalisations of "how weak the data is".**
  (i) constructed references; (ii) the coordinator's **BC proxy** — `bc@mix33` 261.2149 vs
  `bc@fixedtime` 265.8322, IQL 3/4 vs 1/4; (iii) **the reviewer's own, using neither construction nor
  BC — mean training-set return from the committed declarations**: `fixedtime −31,328.1` (IQL 1/4)
  against `mix33 −28,833.3` (IQL 3/4), and `maxpressure −18,546.5` (1/4) against `mix67 −17,735.3`
  (3/4) — matched to **8 %** and **5 %** on corpus quality, ranks inverted.
  ⚠️ **But BC is a sound proxy only for the MEAN**: `bc@mix33` sd **158.06**, **51 %** of episodes below
  150 ATT; `bc@fixedtime` sd **8.91**, **0 %** below 150. **§1b's "equally cloneable to within 4.6173
  ATT" must read "equally cloneable ON AVERAGE, and radically differently per episode" — which is the
  amendment's own thesis.**
- **F4 — no truncated read reached a result.** Exhaustively enumerated: guard counts (re-run with no
  arguments: hygiene **16**, english **2**, both matching the packet's corrected numbers), 12 cell means,
  500 episodes/cell, 192/192 comparisons at `n_shared_draws = 100` with one distinct `draw_ids` tuple,
  Gate P1's 1,370 leaves, 60 runs × 40,000 steps, 66/100/134 expert streams, 39/48/44 both-component
  draws, 20/20 expert top deciles, 75 new tests. **All correct. The only residue is prose (N5, N9).**
- **F5** — no sentence in the packet or artifacts says the mixture settled what the filter selects.
  Q3's circularity **is** in the artifact as a per-tier boolean plus a note, so **P4.6's failure did not
  recur in kind**. Q1's tautology confirmed independently: components separated by **29,580 / 29,876 /
  29,580** return units, **0** random streams above the expert minimum, CLES **1.0**, top-20-by-return
  **20/20 expert** on all three.
- **Also:** frozen set 19 changed paths, **0** hits. A5 satisfied, `horizon_vehicle_count` co-reported
  on every cell including constructed ones. Chain of custody **112/112**, 0 orphans either direction, 60
  distinct checkpoint digests. P4.6's P2 re-scoring — **nothing rescued**, `NOT SCORABLE` preserved.
  Re-declaration reproduces P4.6's `mappo1000` and `random` entries **byte-for-byte**. `phase2.log` is
  genuinely a `tmux capture-pane`; the 15 per-tier logs match their artifacts' mtimes **to the
  nanosecond**. §6's box is `[ ]`.

## 6. Tests: load-bearing versus theatre

**Load-bearing:** `test_mixture_tiers.py` (real corpus, asserted from the built stream list);
`test_p4_7_constructed_reference.py::test_every_constructed_value_is_one_of_the_two_components` and
`::test_the_realised_composition_matches_the_recorded_assignment` (both kill the expectation build);
`test_p4_7_predictions.py`'s boundary set; `::test_gate_p1_refuses_a_difference_of_one_part_in_a_trillion`;
`test_rtg_calibration_guards.py:127` (kills a ×4.0 that P4.3's own 51-test suite still passes — **`DEFERRED` 44's closure is real**).

**Theatre:** `test_p4_7_predictions.py:391` asserts a string contains `"set"` and **licenses N3's false
claim**; `:118`'s `assert "threshold" not in ... or "no threshold" in ...` is a tautology.

## 7. What the reviewer could NOT verify

`DEFERRED` 43's measured totals (depend on an unstated heuristic and a plugin not in the diff; only the
17 token changes were checked, none weakens an assertion) · the "29 mutations, 29 killed" claim as a
whole (12 of the reviewer's own were run instead) · Gate D and Gate G **as executions** (artifacts and
transcripts read; the draws were not regenerated) · **whether the 12 cells' training is reproducible**
(~4 h of GPU; artifacts, manifest, 60 distinct digests and log/artifact mtime correspondence verified
instead) · `docs/reviews/P4.6.md` in full · whether `max(|min|,|max|)` is the *right* normaliser —
**a coordinator question, not a review finding**.
