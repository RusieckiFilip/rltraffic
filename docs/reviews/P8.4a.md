# Independent review — P8.4a (`task/p8.4a-entered-probe` @ `cada7cd`, `BRIEF_31` + amendments A–H)

**Date:** 2026-08-29 · **Reviewer:** `contract-reviewer`, fresh context, read-only
**Verdict: PASS-WITH-NOTES — 2 blocking, 4 major, 7 minor.**

> **Reviewer's headline:** *"Every headline number in the packet reproduced under independent
> recomputation. The `materialise_draws.py` change survived every attack I could construct. The
> defects I found are in **claims about** the work, not in the work's numbers."*

**Coordinator's independent confirmation:** BL-1, BL-2 and MJ-1 were each re-verified from the
artifacts and source before this file was written. All three reproduced.

## Method

`78 passed` on the two new test files · whole suite from the worktree `1 failed, 1449 passed, 25
skipped`, the failure being the known CWD-dependent `test_rtg_ablation.py` which **passes from the main
tree** (diagnosis confirmed) · frozen guard `exit=0` · English `exit=0` · hygiene **16, none in new
files** · **6 mutations of `_NON_IDENTITY_FIELDS` all caught** · **2 mutations of the E4 check both
caught** · **9 source mutations of `admission_probe.py`: 4 caught, 5 SURVIVED** · end-to-end roadnet
attack in a sandbox refused on `source_roadnet_sha256`.

---

## BLOCKERS

### BL-1 — the packet's §7 prose states the opposite of the task's own finding, in the paragraph that gates P5.3b's release

`docs/returns/P8.4a.md:606`: *"Censoring appears on hz1x1 at `fixedtime` (9.3 %) and `random` (34.5 %)
**and nowhere else on either scenario; grid4x4 is at ≈100 % everywhere.**"*

✅ **Coordinator-verified from `p8_4a_admission.json`: grid4x4 has FOUR censored cells of fourteen.**

| grid4x4 cell | never entered | admission |
|---|---|---|
| **`bc_top10@mappo1000`** | **2183** | **0.967168** |
| `iql@mappo1000` | 49 | 0.999263 |
| `bc_top10@random` | 22 | 0.999669 |
| `bc@random` | 3 | 0.999955 |

**The largest is the arm this task falsified E1 on, fired E2 on, and calls SUBSTANTIVE in §4a.9.**
⚠️ **The §7 TABLE directly above the sentence is correct (24 fully admitted / 15 censored); the PROSE
is not.** §7 is the paragraph that will be read when deciding whether to release the held P5.3b
campaign, and a reader of §7 alone concludes the grid4x4 exposure is nil.

### BL-2 — both committed artifacts record a `git_commit` from a DIFFERENT TASK'S BRANCH that contains none of the code

Both carry `git_commit = 53e995da…` with `measurement_git_commits: []`.
✅ **Coordinator-verified: `git cat-file -p 53e995d:offline/admission_probe.py` → the path does not
exist in that commit, and `git branch --contains 53e995d` → `task/p5.3b-nortg-campaign`.**

🚨 **The cause is mechanical and it is a direct consequence of a COORDINATOR RULING.** `runtime_provenance()`
runs `git rev-parse HEAD` in the process CWD, and `BRIEF_31` Amendment **A2** deliberately sent the CWD
to the main tree to defuse `DEFERRED` 61 — **and the main tree's HEAD is the P5.3b branch.** The fix I
approved created this. **Fourth sighting of the class** (P5.3b's M5, Amendment D4, `DEFERRED` 61, now
this), and worse than a dirty tree: the recorded commit belongs to another task and contains none of
the code. The `measurement_git_commits` reachability machinery is never fed by this caller, so it never
fires. **Nothing in the packet or the artifacts discloses any of it.**

**Neither blocker changes a measured number. Both would mislead a reader of the record.**

## MAJOR

- **MJ-1 — Amendment G3's claim is FALSE, and the claim was the coordinator's.** G3 praised the
  pinning test as *"generalises the fix from three named fields to a property"*. ✅ **Verified at
  `tests/test_materialise_draws.py:435-441`: the exact-set assertion pins `_NON_IDENTITY_FIELDS` to a
  five-element literal containing no `_sha256` name, so the following loop CANNOT FAIL. It is dead
  code.** It also iterates a hardcoded 3-tuple rather than the set, so a fourth path field is not
  checked, and it never asserts the twin *exists*. **The real protection is the exact-set pin plus the
  parametrised digest-refusal test — both load-bearing, both confirmed by mutation. The property is
  asserted nowhere.**
- **MJ-2 — the invariant does not hold over the record, and the class is not closed.**
  `provenance.json`'s still-identity `sumo` dict carries `sumocfg` and `template_rou` as **absolute,
  CWD-resolved paths with NO `*_sha256` twin at all.** So E3 closed three named instances, not the
  class, and `materialise_draws.py:208`'s docstring claim that *"every path field here has a `*_sha256`
  twin"* is true of the exempt set and **false of the record**. Not a regression — identity is stricter,
  never looser — and masked in practice by the `cityflow.json` byte comparison firing first.
- **MJ-3 — five mutations of `admission_probe.py` survive all 49 tests, and three contradict packet
  claims.** Deleting `assert_no_science_verdict` from `admission_artifact` → **0/49 fail**; the
  two-grain refusal → `if False` → **0/49**; the `created`-consistency refusal neutralised → **0/49**;
  E1's boundary `<=` → `<` → **0/49**; **E3's `holds = None` → `True` → 0/49 — the guard against the
  exact upgrade F5 and G forbade has no test.** `admission_artifact` is never named in the test file.
  **The untested-wiring family, recurring.** ⚠️ **No shipped number is affected — the reviewer verified
  both artifacts satisfy all three guarded properties.**
- **MJ-4 — §573's grid4x4 row is off by one cell:** *"the remaining 11 cells are exactly 0"* — there
  are 14 grid4x4 cells and **10** are zero; `bc@random` has 3, which the packet's own E1 table lists
  two paragraphs earlier.

## MINOR

**1** `assert_cwd_renders_the_recorded_scenario_dir` **returns on the FIRST existing draw** and never
checks the rest — reviewer constructed a heterogeneous-origin pool where it reports `matches=True` and
`restore_draws` then dies with the very message E4 exists to eliminate. **No false-refusal mode found.**
· **2** `p8_4a.sh:19-21` and packet §4 repeat the now-stale claim that the path fields are identity
fields — *"corrected later in §4a.2/§4a.5"*, but it is the same misreading D1 made · **3** §5's timing
table mixes Gate-0 probe figures with artifact figures under one undifferentiated column, and §5 is
declared P8.4b's cost-model input · **4** neither artifact records the operator, `git_dirty`, or a code
commit; the author-ran disclosure exists only in the packet · **5** `draw_restoration.restored` lists
`1005–1099` while every action is `kept` · **6** G2(c)'s bound is a **re-accounting** bound: §4a.10 says
*"if all 299 were **admitted** and counted"* while holding `T = att_ours × entered` fixed. **The
arithmetic and conclusion are right; the word "admitted" overreaches — the correct reading is
"counted"** · **7** internal count inconsistencies (49 vs 45 tests; 1456 vs 1444 passed; §4a.3 quotes
the pre-E3 four-element set).

## Verified positively by independent recomputation

**Admission ratios exact** — `bc_top10@mappo1000` 644173/663785 = 0.970454, `bc_top10@random`
663486/663785 = 0.999550, `hz1x1/behaviour@random` 595299/907175 = 0.656212; **all 39 + 14 cells
recomputed from episode rows, 0 disagreements.**
**Reference reproduction under `==`** — matched on `(arm, draw_id, seed)` handling the `seed: null`
anchors: **1870 compared / 0 mismatches / 39-39 cells**, and **6200 compared / 0 mismatches / 14-14
cells.** The "39/39" and "14/14 exact" claims are **true**.
**R1–R5 identities: 0 violations in 8,070 episodes.** **`created` against the flow files, entirely
outside the artifact: 200/200 draw directories match.**
**E1 scoring rebuilt from raw rows: 0 mismatches on both grains**; status counts 23/8/1 and 1/6/2;
escalate lists identical.
**G2(c) fully re-derived** — lower 350.152350 (−0.187455), tight upper 351.760233 (+1.420428), loose
351.767479, interval 1.607883; 425/500 episodes zero, max 20. ⭐ **And the tight bound IS a valid upper
bound: `H − c` is strictly decreasing in `c`, so the maximum of `Σ(H−c)` over any size-n subset is at
the n smallest creation times. The greedy argument is correct.**
**Cross-worktree demand purity independently rebuilt: `flow.json` byte-identical 100/100.**
**Draw provenance groups confirmed** — `1000–1004` → `29ab244` `git_dirty: True`, `1005–1009` →
`964d3b0`, `1010–1099` → `f321fc5`. **E1's correction of C1's "all ten" is warranted.**
**No verdict on the science** — a broad prose regex over both artifacts returns one hit, and it is
*"no conclusion about P5.2's headline beyond what E1 measures"*. **E3 is `holds: null` and was not
upgraded.**
⭐ **The `materialise_draws` change is sound: for every exempt path the content is still guarded, and
`source_roadnet` — which is NOT rendered, so its digest is the sole defence — was attacked end to end
in a sandbox and refused. The reviewer could not construct two draws differing in anything that matters
that are accepted as identical.**
**Pre-registration confirmed by history:** the scoring rules are in `docs/plans/p8.4a.md` at `323acce`
(16:56), **before** the implementation at `964d3b0` (18:19).

## Tests judged theatre

The `for path_field` loop (MJ-1) · **no test for `admission_artifact` at all**, so the verdict guard,
the two-grain refusal and the created-consistency refusal are decorative from the suite's point of view
· E3's `holds is None` branch and E2's `>= floor` inclusivity, both unpinned ·
`test_admission_spread_is_zero_for_a_single_seeded_arm`, true by construction of `max − min` over a
one-element dict.

## What the reviewer could NOT verify

**Only ONE episode was re-simulated** (the suite's fidelity gate); the other 8,069 rows were verified by
reproduction against committed references — a different and strong check, but not re-simulation ·
🚨 **`att_engine` has NO external reference — it is new data, nothing in the repo pins it, and A0's
decomposition, §7 and G2(c)'s second route all rest on it. T1 validated the engine-metric
reconstruction on hz1x1 ONLY; grid4x4's `att_engine` is unvalidated against any independent route** ·
who physically ran the escalation · whether the P5.1/P5.2 checkpoint mapping is right beyond
reproducing the committed numbers · `p8_4a.sh` end to end (read, not executed) · the 16 hygiene
findings' byte-level stability against `main` · cologne, P4.7 mixtures, SUMO/MOSS · **B2's per-node
hypothesis, correctly reported as owed.**

**Merge-gate item, not a defect:** `docs/PROJECT_PLAN.md:716`'s `P8.4a` box is created and **unticked**.
