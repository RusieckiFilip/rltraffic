# MERGE REVIEW — P7.2a (`BRIEF_35` + Amendments A–C), HALF B: the artifacts, recomputed from raw files, and the packet's §9 numbers

**Filed by the coordinator 2026-09-13 from the reviewer's findings file, verbatim below the rule.** Reviewer: `contract-reviewer`, one mandate, 12-minute box, read-only on `/home/filip/rltraffic/scenarios/draws/cityflow1x1/` (206 draws, 1,442 files hashed and parsed with `json`, `hashlib`, `xml.etree`, `Counter`; no `offline.*` import, no simulator), both band-gate reports, `docs/data/p4_3_probe.json`, and the packet's §9.2–§9.4.

**Verdict: PASS-WITH-NOTES — blocking 0, major 0, minor 1** (a non-literal quotation in §9.4: neither report has a top-level `differing` key; every row's `differing` is empty). Every recomputed number matches the packet: 206/206 parity route files bind every vehicle to the parity `<vType>` with the nine expected attributes (375,193 vehicles); 206/206 configs resolve to the shipped net (`679acffa…`), `begin 0`, `end 4000`, `time-to-teleport −1`; CAP(E) from raw files — multisets equal 206/206, document order equal 206/206; every digest twin verifies; one commit, `git_dirty` False ×206; pools 1/105/100; all 424 original parent files dated 2026-08-06 or 2026-08-25, none on 2026-09-13; both band-gate reports 100/100 with `observed` identical per draw and `expected` equal to `p4_3_probe.json` on all five keys. Two things it could not verify are answered by the coordinator's own runs: the coordinator's band-gate report is a genuine 681-second run (`real 681.24 s user 99.85 s sys 94.35 s`), and the tree digest's recipe is a fix-round wording item (Amendment D3).

---

# REVIEW P7.2a HALF B — artifacts recomputed from raw files
Started: 2026-09-13 (reviewer: independent, read-only on scenarios/draws/)

## CHECKLIST
[x] 1. Tree census, computed by you: number of draw dirs and their id ranges; files directly in each draw (must be exactly the 4 names, no other files, no subdirectory other than `parity`); parity dirs and their 3 file names; total files under cityflow1x1 (packet: 1442); draws with any extra file or directory anywhere (expect 0).
[x] 2. For ALL 206 parity `routes.rou.xml`, parsed with xml.etree: exactly one `<vType>`, its attributes equal the expected set exactly (as strings); every `<vehicle>` has `type="cf_parity"`; count vehicles; sum over 206 (packet: 375,193); the five sample counts (packet: 2021/1821/1804/1821/1816).
[x] 3. For ALL 206 parity `noteleport.sumocfg`, parsed: `./input/net-file` resolves (relative to the cfg's directory) to the shipped net path and that file's sha256 is one value across all 206 (record it, first 8 hex); `./input/route-files` == `routes.rou.xml`; `./time/begin` == 0; `./time/end` == 4000; `./processing/time-to-teleport` == "-1". Count any deviation.
[x] 4. CAP(E) recomputed from RAW files for ALL 206: `(startTime, tuple(route))` multiset from the parent `flow.json` vs `(float(depart), tuple(edges))` multiset from `parity/routes.rou.xml` (offset 0 for this scenario — state that you checked `./time/begin` of the shipped `.sumocfg` is 0). Count draws where the multisets differ (expect 0); count where document order also matches (index-aligned n/n); record the five sample n.
[x] 5. Digest audit for ALL 206: parent `provenance.json:files[name]` == sha256 of that parent file (4 per draw); parity `provenance.json:files[name]` == sha256 of that parity file; parity `parent.{flow,routes,provenance}_sha256` == sha256 of the parent's files; parity `net.sha256` == the shipped net's sha256; `git_commit` and `git_dirty` and `format_version` value sets across 206 (expect one commit `672a7bac615e…`, `{False}`, `{"materialised-draw-parity/1.0"}`); `sumocfg.time_to_teleport` == -1 ×206; `demand_audit.multiset_equal` True ×206 and `n_index_aligned_equal == n_cityflow == n_sumo` ×206; `n_bound == n_vehicles` ×206 and `n_vehicles` equals YOUR parsed vehicle count from item 2 for every draw.
[x] 6. Pools from the parity records: counts by `pool` (packet: nominal 1, training 105, held_out 100) and that `pool` agrees with the draw id (0 nominal; 1–999 training; 1000–1099 held_out).
[x] 7. The 106 original parents (0–5, 1000–1099): file mtimes — report min/max date of `flow.json` mtimes (the coordinator saw 2026-08-06 → 2026-08-25); any parent file with an mtime on 2026-09-13 is a finding.
[x] 8. Band gate reports: both JSONs exist; `n_draws`, `n_reproduced`, `differing` in each; per draw, are the `observed` dicts IDENTICAL between the implementer's and the coordinator's runs (100/100 expected — two independent runs of a deterministic engine); draw 201 and draw 300 values vs the packet's quoted numbers; and do the `expected` values in the report equal `docs/data/p4_3_probe.json:episodes[*]` for the same draw ids (recompute the mapping yourself from the artifact at `/home/filip/rltraffic/docs/data/p4_3_probe.json`).
[x] 9. Packet arithmetic: 424 + 1018 == 1442; 206×3 + 100×4 == 1018; 106×4 == 424; state whether every packet number in §9.2/§9.3/§9.4 you could recompute matches, and list any that does not.

## FINDINGS (appended incrementally)

### [1] Tree census — 22:18 — os.listdir/os.walk over /home/filip/rltraffic/scenarios/draws/cityflow1x1
- top-level entries 206, all `draw_NNNN` dirs, no other top-level entries; id ranges [(0,5),(201,300),(1000,1099)] = 6+100+100 = 206
- every draw dir: files == {flow.json, cityflow.json, routes.rou.xml, provenance.json}, subdirs == {parity}; parity: files == {routes.rou.xml, noteleport.sumocfg, provenance.json}, no subdirs
- parity dirs 206; total files (os.walk) 1442 — matches packet 1442; draws with extra/missing entries 0; symlinks 0
- RESULT: item 1 OK

### [2] Parity routes.rou.xml, all 206, xml.etree — 22:20
- every file: exactly one <vType>; attrib dict == {id cf_parity, accel 2.0, decel 4.5, length 5.0, maxSpeed 11.11, minGap 2.5, speedFactor 1.0, tau 2.0, width 2.0} as strings, exact-equality (0 deviations)
- every <vehicle> type == cf_parity (0 deviations); no other child tags
- sum of vehicles over 206 = 375193 — matches packet 375,193
- samples 0/201/300/1000/1099 = 2021/1821/1804/1821/1816 — matches packet
- per-draw counts saved to review dir veh_counts.json for item 5 cross-check
- RESULT: item 2 OK

### [3] Parity noteleport.sumocfg, all 206 — 22:20
- ./input/net-file value (one string, 206x): ../../../../hangzhou_1x1_bc-tyc_18041610_1h/hangzhou_1x1_bc-tyc_18041610_1h.net.xml; realpath resolves to the shipped net for 206/206
- sha256 of resolved net: one value 679acffa… (679acffab386f0fa87aa40ae904f0744e31c1db1be964f381cbacc5a350b3d9f) x206
- route-files == routes.rou.xml x206; begin raw "0", end raw "4000", time-to-teleport raw "-1" x206; deviations 0
- shipped hangzhou_1x1_bc-tyc_18041610_1h.sumocfg ./time/begin == 0 (checked; so depart offset 0 for item 4)
- RESULT: item 3 OK

### [4] CAP(E) recomputed from raw files, all 206 — 22:22 (json + xml.etree; no offline.materialise_draws import)
- parent flow.json entry keys: vehicle/route/interval/startTime/endTime; every entry startTime==endTime (draw0: 2021/2021), i.e. one vehicle per entry, so (startTime, tuple(route)) is the right key
- depart offset 0 confirmed: shipped .sumocfg ./time/begin == 0 (item 3)
- Counter((float(startTime),tuple(route))) vs Counter((float(depart),tuple(edges))): multiset-differ 0/206; document-order equal 206/206; n_cityflow==n_sumo in all 206
- sample n 0/201/300/1000/1099 = 2021/1821/1804/1821/1816 (both sides)
- RESULT: item 4 OK

### [5] Digest audit, all 206 — 22:23 (hashlib.sha256)
- parent provenance files[] digests: 0 mismatches (NOTE: parent files dict has 3 entries per draw, not 4 — provenance.json cannot contain its own hash; checklist expectation of "4" was mine, not a defect)
- parity provenance files[] digests (3 files): 0 mismatches
- parity parent.{flow,routes,provenance}_sha256 == sha256 of parent's files: 0 mismatches x206
- parity net.sha256 == shipped net sha 679acffa…: 206/206
- git_commit set {672a7bac615e575a00b69a7803da98f60d79f49a: 206}; git_dirty {False: 206}; format_version {materialised-draw-parity/1.0: 206}; sumocfg.time_to_teleport {-1: 206}
- demand_audit.multiset_equal True x206; n_index_aligned_equal==n_cityflow==n_sumo x206; n_bound==n_vehicles x206; n_vehicles == my parsed count (item 2) x206; n_sumo == my parsed count x206
- Context (not under review): ORIGINAL parent record draw_1000 carries git_commit 29ab2445…, git_dirty True — that is the August P4.x materialisation record, not the parity record; packet's "git_dirty False (206)" refers to parity records and is correct as stated
- RESULT: item 5 OK

### [6] Pools — 22:23
- parity pool counts {nominal 1, training 105, held_out 100} = 206 — matches packet; pool vs id rule (0 nominal, 1–999 training, 1000–1099 held_out): 0 mismatches
- RESULT: item 6 OK

### [7] mtimes of the 106 original parents — 22:25 (os.stat)
- original flow.json mtime min 2026-08-06 14:24, max 2026-08-25 15:13 (coordinator saw 08-06 -> 08-25: agrees)
- all 424 original parent files: mtime day histogram {2026-08-06: 44, 2026-08-25: 380}; ctime histogram identical; files with 2026-09-13 mtime: 0
- new parents 201–300: 400 files all mtime 2026-09-13; parity files: 618 all 2026-09-13 (= 206x3)
- parent provenance files[] names = {cityflow.json, flow.json, routes.rou.xml} (3, confirms item 5 note)
- RESULT: item 7 OK

### [8a] Band-gate reports — structure — 22:26
- /tmp/p72a_band_gate.json exists (51169 B); coord_band_gate.json exists (51169 B); both top-level keys = [draws, n_draws, n_reproduced]; n_draws 100, n_reproduced 100 in both
- MINOR (wording): packet §9.4 quotes "The report's own summary: n_draws 100, n_reproduced 100, differing []" — there is NO top-level `differing` key in either report; `differing` is a per-draw-row field. Immaterial to the claim but the quoted summary is not literally what the file contains.
- probe file docs/data/p4_3_probe.json: 100 episodes, each carries draw_id; per-draw comparison follows in [8b]

### [8b] Band-gate per-draw comparison — 22:28 (json; no simulator run)
- both reports: draws ids == {201..300} (100); observed dicts identical impl vs coord 100/100 (dict equality AND sorted-json repr equality); rows matches True 100/100 in both; rows with non-empty differing 0 in both; expected==observed 100/100 in both
- the two report FILES are byte-identical (sha256 prefix b4658faceef6 both, 51169 B). Consistent with two runs of a deterministic engine + deterministic writer; content alone cannot distinguish that from a copy. File mtimes (stat):
2026-09-13 21:32:33 /tmp/p72a_band_gate.json
2026-09-13 22:03:35 /tmp/claude-1000/-home-filip-rltraffic/2824e906-e46f-4c96-a603-40341737cf5b/scratchpad/preflight-p72a/coord_band_gate.json
- expected vs docs/data/p4_3_probe.json: probe episodes carry draw_id, draw_ids list == episodes order, ids == 201..300; expected == probe episode on all 5 keys 100/100; OBSERVED == probe episode 100/100 in both reports
- packet §9.4 quoted values: draw 201 observed = {att_horizon 191.80088251516824, decisions 360, horizon_vehicle_count 116.0, local_return -20966.0, local_return_from_lanes -20966.0} — matches packet; draw 300 observed = {local_return -14256.0, att_horizon 150.75875486381324, horizon_vehicle_count 90.0} (also decisions 360, lrfl -14256.0) — matches packet; probe episodes[0].draw_id == 201 as packet states; report last row draw_id 300 as packet states
- RESULT: item 8 OK apart from the MINOR wording in [8a]

### [9] Packet arithmetic — 22:28
- 424+1018==1442 True; 206*3+100*4==1018 True; 106*4==424 True; 1+105+100==206 True
- every §9.2/§9.3/§9.4 number I could recompute matches: 206 draws, 1442 files, 206 parity dirs, 424 pre-existing files (106x4 by census; 0 changed/deleted by mtime+digest evidence — the packet's pre-run digest 85157bbb… itself I could not recompute, no pre-run snapshot available to me), 1018 added, pools 1/105/100, one commit 672a7bac, dirty False x206, CAP(E) failures 0, n_bound!=n_vehicles 0, 375,193 vehicles, 2021/1821/1804/1821/1816, band 100/100, draw 201 & 300 values, draw_1000 record fields (n 1821, begin 0, end 4000, ttt -1, offset 0.0)
- numbers NOT matching: none. Wording deviation: "differing []" at report summary level (see [8a])
- NOT verified (out of scope / no simulator): the pre-run tree digest 85157bbb…, the post-run digest 21fadf83… (packet does not state the digest recipe; I did not reconstruct it), and that the band-gate reports were produced by two independent engine runs rather than one run copied

## VERDICT: PASS-WITH-NOTES — blocking 0, major 0, minor 1
