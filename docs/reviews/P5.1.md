# Independent review — P5.1 (spatial mixing on grid4x4)

**Verdict: PASS-WITH-NOTES** — 2 MAJOR, 6 MINOR, 4 NOTE, **no blocker**
**Reviewed at:** `4921da9` · **Written:** 2026-08-18, **before the fix round**

**Ran:** suite `1042 passed, 3 skipped in 121.53s`; P5.1 files alone `119 passed, 0 skipped`; guards
0 / 5 / 17 with none in P5.1 files, all counted from full output; **15 of its own mutations** — 9
killed, **4 genuine survivors**. Secured artifacts confirmed byte-identical to the worktree by
`diff -rq` before anything was read.

## MAJOR

**J1 — a stated ordering reverses on a seed and is unflagged, IN THE SECTION WRITTEN TO CATCH THAT.**
§6 claims *"measured here on every headline contrast"* and lists 6 rows; the artifact holds **21**
comparisons, **3** of which reverse per seed. §6 names two and omits **`dt_spatial` vs `bc`, which
reverses on seed 101** (`−14.05` against `+63.29 / +57.93 / +22.10 / +12.89`). ⚠️ **That pair fixes the
rank the packet reports twice: `dt_spatial` is 3/5 pooled but 2/5 on seed 101.**
🚨 **`docs/data/p5_1_grid.json` already carries `reverses_on_n_seeds: 1` for it — the datum was in hand
and did not reach the prose**, and `predictions.p2a.order` is a pooled ranking with no per-seed
annotation, so a consumer inherits "rank 3" as settled. **P4.7's M1 recurring inside the section
written to prevent it.** P2a's verdict is unaffected (2 ≠ 1 either).

**J2 — the inference-time readout position is untested; a one-step off-by-one survives the suite.**
`agent/SpatialDTAgent.py:775` reads `logits[0, :, -1]`. Changing it to `[0, :, -2]` leaves the full
suite green (the 2 failures are the no-`.git` scratch baseline, identical unmutated). Proven
non-equivalent: pristine `[[0,2,0],[1,1,1],[1,1,1],[2,2,2],[0,0,0]]` against mutated
`[[0,0,0],[0,2,0],[1,1,1],[1,1,1],[2,2,2]]` — **an exact one-step lag with a degenerate read at step 0.**
⚠️ **The shipped code is CORRECT** — the reviewer traced the alignment by hand. **The defect is the
missing guard, on the single most alignment-sensitive line in the new agent, which produced every
number in the primary contrast.**

## MINOR

- **m3 — the `bc_top10` "exact set identity" is one tied stream from being false.** Rebuilt from raw
  `.npz` importing nothing from the branch: counts reproduce **exactly** (`B2 84 · C2 77 · B3 66 ·
  B1 32 · A1 22 · D2 19 · C1 16 · C0 4`, zero from the other eight). **But 302 of 320 are strictly above
  the cut; 32 tie at −114.0 and fill the last 18 slots — and one tie belongs to B0, the 9th node by
  own-max.** Reverse-lexicographic tie-break gives **9** intersections. **The mechanism claim is
  untouched (302/320 from 8 nodes); "verified as a set identity" overstates it.** `top_return_streams`
  **confirmed unmodified** since `738884b` (P4.4).
- **m5 — three shipped guards have no test, all surviving the suite:** `spatial_mixing.py:253`'s
  `rtg_scale` branch (the packet's M4 covered only `target_rtg` and reads as if the guard were covered),
  and `joint_windows.py:164` and `:209`.
- **m7 — THE PER-SEED VARIANCE IS ATTRIBUTABLE AND THE PACKET STOPS ONE LINE SHORT.** `dt_nomix` per-seed
  means `157.88 / 157.99 / 157.76 / 157.74 / 157.87`, **sd 0.104**. `dt_spatial` `158.91 / 230.07 /
  226.26 / 190.49 / 181.34`, **sd 30.36**. **The 70× spread is entirely a property of the TREATMENT
  arm**, and its failure mode is **partial loss of control** — vehicles at horizon `16.2 / 133.9 /
  123.2 / 75.7 / 49.4` against the control's ~15 on every seed — **not uniform degradation.**
- **m4** the retracted sublayer claim survives verbatim in `tests/test_spatial_dt_agent.py:162-165` ·
  **m6** mutation count self-contradicts (20 vs 19) · **m8** `final_loss` is a single final batch and no
  loss curve exists; *unclaimed positive:* both arms use identical batches from identical initial
  weights, better controlled than the packet says.

## F1 — the seed-101 near-null is NOT structural, by three independent routes

**Training loss** `0.2058`, **2nd lowest** of `{0.1947…0.2227}` · **weight statistics** within ~1.5 %
(spatial Frobenius `10.7413` vs `10.8377–10.9481`) · **graph-dependence probe**: seed 101 flips
**48.83 %** of argmax actions when the graph is removed — **the HIGHEST** of the five (43.80–47.12 %).
> **"Seed 101 learned to ignore the graph" is REFUTED. The heterogeneity is UNEXPLAINED and must be
> reported as such** — and the reportable structural fact is **m7**, not the difference's spread.

## Other confirmations

`iql` vs `random` correctly fenced and independently confirmed: per-seed `−1.47 / +20.58 / −28.22 /
+34.83 / +51.65`, pooled `+15.4753`, **36/100 draws won** — **IQL genuinely loses to `random` on
grid4x4 while winning the weak half of the hz1x1 ladder.** ⚠️ `reverses_on_n_seeds` is defined as the
seed **minority**, not "seeds disagreeing with the pooled sign"; the two coincide here but the metric
would under-report where they diverge. **`dt_nomix` properly fenced** (F4); **A8(a) holds with margin**
(plan 00:30:10, last code 13:30:02, campaign 13:46:36, first checkpoint 14:44:23); all 7 cell means to
**1.14e-13**; MDE **5.0226**, **7.88×**; 25/25 checkpoints at 40,000; `PROJECT_PLAN` untouched by the
branch (15 × `A`).

**Theatre found:** a test asserting a literal against itself; a "declared constants" test that never
reads the plan; the sublayer-order `inspect.getsource` pin; and a collapse-criterion test that cannot
fail for the reason its name gives.

**Could not verify:** whether seed 101's near-null has *any* cause (three routes negative — "unexplained"
is a finding, not a proof of no cause) · C3's head-count confound (no `n_head=4` run exists) · Gate D's
byte-identity (not repeated) · GPU determinism of the 40,000-step runs · whether
`build_joint_index`'s incompleteness branch is reachable · the `p_value 3.956e-18` (verified the sign,
0/100 win count and rank-biserial `+1.0`, which is what the verdict rests on).
