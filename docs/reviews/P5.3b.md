# Independent review — P5.3b (`main` @ `bd36a0a`, `BRIEF_30` + Amendments A–E, `PREREGISTRATION` A11(b)/A11(d)/A13/Rule R)

**Date:** 2026-09-10 · **Reviewer:** `contract-reviewer`, fresh context, read-only, post-merge
**Verdict: PASS-WITH-NOTES — 2 blocking, 10 major, 12 minor. 38 mutations: 25 KILLED, 13 SURVIVED.**

**Filed by the coordinator**, verbatim: the reviewer may not create findings files. Its tree was clean at
exit; all four mutated files byte-identical to `main`.

> **Headline:** *The −409 is real.* Reproduced from the raw per-episode cells by a **stdlib-only route
> importing nothing from `offline/`**, then **14 episodes re-rolled through a fresh CUDA process, every
> one bit-identical** to the committed artifact. **The number is right, the pairing unit is the
> registered one, the null control's non-discrimination is real, and P8.4b's worst defect is not
> repeated.**
> *Two things are wrong.* **(1) The headline is protected by nothing** — replacing the whole `mix50`
> `att_engine` block (`−409.1450 → −0.1300`, a 3,147× reduction) left **161 tests passing**, the test
> file forward-references a guarding test **that does not exist**, and the `dt` half of the contrast
> **is not in git at all**. **(2) The mechanism the packet says it did not establish is sitting in the
> artifact\'s own A11(b) columns.**

## BLOCKING

### BL-1 — the paper\'s newest headline is asserted by zero tests, and half its input is not in the repo

| mutation to `docs/data/p5_3b_nortg.json` | result |
|---|---|
| `mix50` engine `mean_difference` `−409.1450 → −300.0` | **SURVIVED — 17 passed** |
| same `→ −0.1300` + CI + means + reference | **SURVIVED — 161 passed** |
| CI `→ [−500, −300]` (no longer brackets the mean) | **SURVIVED** |
| `abs_mean_difference 409.14 → 300.0` | **SURVIVED** |
| `reference_dt_cells.mix50.att_engine_mean → 200.0` | **SURVIVED** |
| `seeds_reversed 0 → 4` | **SURVIVED** |

`tests/test_p5_3b_artifact.py:159` says the primary half *"is recomputed in
`test_the_primary_definition_recomputes_from_the_reference_column`"*. **That test does not exist** — one
grep hit, the docstring itself. And it cannot be written from a clone: the `dt` per-episode `att_engine`
column lives only in gitignored `output/p8_4b_rederivation/`, with **no `SHA256SUMS_p8_4b*.txt`**.

✅ **Why this is not "the number is wrong":** `assert_rederived_matches_committed` requires every
re-derived `att_ours` to equal the committed grid under `==` — **0/500 mismatches on all three tiers,
`reproduces_committed` true on 1500/1500** — so `att_ours` acts as a per-episode checksum on episode
identity. Loosening it to 1.0 s is **KILLED**. And the reviewer\'s own re-roll closed the gap: **0
mismatches of 5** on `dt@mix50` seed 101, bit-identical to both the grid and P8.4b.

### BL-2 — the mechanism is in the artifact\'s own A11(b) columns, and A13(b)\'s decomposition is missing

**(a) The collapse is entry-blocking**, and D1 collected the number that says so:

| arm (mix50, 500 ep) | `att_engine` | `att_ours` | gap | `never_entered` | `created` |
|---|---|---|---|---|---|
| `dt` | 105.590 | 107.703 | −2.112 | **3.7** | 1814.3 |
| `dt_nortg` | 514.735 | 302.178 | **+212.557** | **373.8** | 1814.3 |

**(b) The `−409` vs `−194` difference IS the never-admitted population, to four decimals.**
`−409.1450 − (−194.4757) = −214.6693`; `212.557 − (−2.112) = 214.669`. **`PREREGISTRATION` A13(b) makes
the three-component decomposition a REQUIRED reported quantity for every report of the two definitions\'
difference. P5.3b reports the difference in three places and the decomposition nowhere.**

**(c) The collapsed arm is bit-identical to `dt@random`.** Across 8 measured fields:
`mix50@404` 99/100 draws · `mix50@505` 97/100 · `mix50@202` 88/100 · `mix50@101` 0/100 · `mix50@303`
0/100. Confirmed against a fresh simulator on three draws. **`mix50`\'s ablated arm did not get worse —
it fell into the SAME degenerate attractor the `random`-corpus DT already occupies, which is also why
`random` cannot discriminate: both its arms are already there.**

## MAJOR (abridged; full text in the reviewer\'s report)

**MJ-1** A11(b) is met on `episodes` and on **nothing else** — `cells`, `reference_dt_cells`,
`comparisons`, `gates.gate_1b.cells` carry no counts, and the packet\'s headline table is exactly such a
table · **MJ-2** `DEFERRED` 71\'s naming hazard is banned only in `reference_dt_cells`; `cells[*]` still
uses `att_horizon_mean` for an engine value · **MJ-3** `predictions.Q1.holds` is an unqualified `true`,
pinned by test, beside a limb marked `is_evidence: false` — and `random`\'s `abs_mean_difference` is the
**global minimum of a non-negative quantity**, so the "smallest" limb could not have been falsified by
any outcome · **MJ-4** Q1\'s ordering is the **reverse** of P5.3a\'s decision-time flip-rate ordering
(`mappo1000` 0.0036–0.0047 vs `mix50` 0.0013–0.0022), so the prompt\'s value on multi-modal data is a
**training-time** effect, not a decision-time one · **MJ-5** the CI carries **no seed-level
uncertainty**: half-width 15.038 over 100 draw-means against **≈391** over the 5 seed means · **MJ-6**
two docstrings and a plan row assert `rtg_ablation.py` is *"unmodified"*, three lines above the call
passing the parameter this task added · **MJ-7** `_run_report` is exempt from C2\'s dirty-tree refusal;
the artifact carries `git_dirty: true` · **MJ-8** the entire `gates` block is asserted by no test ·
**MJ-9** the evidence base existed **only in a git worktree** · **MJ-10** `assert_arms_are_distinct`
reading `att_ours` regardless of `definition` **SURVIVED** — P8.4b BL-1\'s shape, one task later.

## Independent recomputation

| `mix50`, `att_engine` | recomputed | reported |
|---|---|---|
| **`mean_difference`** | **`−409.14498481947595`** | **`−409.1449848194759`** (`5.7e−14`) |
| Wilcoxon `W+/W−` | `0 / 5050` | exact |
| `n_shared_draws` | `100` | `100` — **the registered unit; P8.4b\'s defect not repeated** |
| `seeds_reversed` | `0` | `0` |

**A5:** all three tiers have exactly 500 shared `(seed, draw)` keys, `set(dt) == set(dt_nortg)`.
**E4:** `random` identical on **500/500 across all 8 fields** — non-discrimination verified independently.
**Q3:** `15 cells, 180 values, max flip_rate 0.0` reproduced exactly — **but 15 of the 180 are the
`baseline` intervention compared against itself and are `0.0` for any model; the discriminating count is
165.**

## What the reviewer could NOT verify

That the 15 checkpoints were trained on the tiers they name (nothing retrained) · 295 of Gate 1b\'s 300
episodes · the packet\'s `probe_cell` inertness measurement (proved the stronger static property instead)
· that `output/p8_4b_rederivation/` came from the code on `main` · every timing number · **all CI
evidence — no network calls were made** · the hygiene baseline of 16 · whether the collapsed policy is
literally constant-action · whether anything downstream already quotes `−409.1450`.
⚠️ **Representativeness: `1697 passed, 2 skipped` here against CI\'s 144 skips — 142 tests that run on
this machine do not run on CI, INCLUDING EVERY GATE-2 TEST. On CI, this task\'s load-bearing test does
not execute.**
