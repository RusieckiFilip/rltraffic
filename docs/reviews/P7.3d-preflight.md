# PRE-FLIGHT REVIEW — P7.3d campaign driver (BRIEF_39 gate G4) — 2026-09-23 — VERDICT: NOT CLEAR (3 blocking · 3 major · 3 minor)

**Reviewer:** `contract-reviewer`, §7's pre-flight mandate (two questions: destruction, resume) plus B.3-1's execution of the header's
form; the branch `task/p7.3d-grid4x4` @ `8c79778`. Six executions of a redirected copy of the driver under tmux, read-only probes of the
exact calls the driver makes, one sandbox resume experiment; the real work dir and evidence dir untouched (verified by the coordinator:
zero porcelain lines in both trees after the review). The reviewer's findings file is filed verbatim below the rule.

## Coordinator's ruling (2026-09-23) — every blocker RE-RUN under the coordinator's own hand before this was written

1. **NOT CLEAR stands; no token; a FIX ROUND (`BRIEF_39` Amendment B.6, one commit, one re-review of the driver path).** The three blockers
   reproduce: **B1** — `python -m offline.transfer_curve canary --draws-root …` → `error: unrecognized arguments`, exit 2 (the parent parser
   owns those options; the driver's line 209 puts `"${COMMON[@]}"` AFTER the subcommand; `p7_3b_anchor.sh` puts it before), so the
   delivered driver refuses at its own canary on every machine. **B3** — `manifest` is not a subcommand (`invalid choice: 'manifest'`), and
   `report` names its output `p7_3a_zero_shot.json`, not the header's `p7_3d_grid4x4.json`; `declarations_for('grid4x4_confirmatory',
   None)` returns hz1x1's 4,700 names as "every declared cell", so a COMPLETE grid4x4 campaign would be refused as 700 undeclared chunks.
   **B2, the one that could make a number wrong:** `run_cell` has no grid4x4 branch — `:1715` `demand_identity(draw_id, out_root=…)` with
   no `scenario=` resolves to hz1x1's parity files (so the 200 anchor chunks would carry hz1x1's `config_sha256`/`routes_sha256`, and
   `chunk_is_reusable` and `report` re-derive the same default, so the mismatch is INVISIBLE to every check); `:1742` `targets_for_subject`
   and `:1744` `checkpoint_identity` raise `KeyError`/`ValueError` for the grid4x4 subject (so all 500 DT cells fail before any simulator
   starts); `load_grid4x4_targets` (`:862`) and `assert_rtg_first_matches_targets` (`:896`) — C3b's deliverables — have **no call site**.
2. **Why the suite and the packet were green over this.** T-16 exercised the spatial loader and the per-id refusal DIRECTLY, not through
   `run_cell`; T-driver's executed-form test asserted only the absence of the group-leader text, so a driver that can never start stays
   green; the implementer's own tmux test refused at the dirty-tree check, which precedes the canary. **This is §7's rule *a pin on a function
   does not pin its caller* at the scale of a whole task: every piece was tested where it was built and none through the path the campaign
   runs.** The pre-flight exists for exactly this, and it is the second time in this project (P8.4a, 2026-08-28) that executing the delivery
   path found what reading it could not.
3. **What is NOT a defect and stays:** the refusal order (every refusal precedes the token; the trap precedes the token; nothing is created
   before `record-canary`); the leader guard and the header's foreground form (measured: the prompt form passes it, the one-liner refuses);
   the token consumed once and only after the canary; no destruction path anywhere (`_write_json` tmp+replace, `move_aside` never
   overwrites, P7.3a's and P7.3b's directories named nowhere, `docs/data` unreachable); the stage mapping and its pin; the resume cases the
   reviewer constructed (truncated → re-roll; code-stale → re-roll; docs-only stale → reuse; hz1x1 stage → refused; teleport → refused; all
   moved aside, none deleted). **The driver's SHAPE is right; its wiring into the module is not.**
4. **The majors and minors go into the fix round** where they touch the same lines (M1 the `COMPLETE`/`FAILED` files; M2 follows from B2;
   M3 is B2's dead code) and otherwise into the packet (m1 digest checks on the three artifacts; m2 the `RuntimeError` after the token —
   the check moves before it; m3 fails safe).

---

# Reviewer's findings file, verbatim

# PRE-FLIGHT P7.3d (BRIEF_39 G4) — FINDINGS
Branch task/p7.3d-grid4x4 @ 8c79778, tree /home/filip/rltraffic-p73d (read-only)
Started: 2026-09-23

## CHECKLIST
- [ ] 1. ORDER — every refusal precedes token consumption and any mkdir/rm; trap before token; record-canary after token
- [ ] 2. DESTRUCTION PATHS — rm/mv/--overwrite/--force/rmtree/unlink/rename/os.remove/> in driver + 4 subcommands; cross-campaign safety (p7_3a, p7_3b_anchor); manifest overwrite; report -> docs/data
- [ ] 3. RESUME BY CONTENT — sandbox: (a) truncated (b) stale git_commit (c) stage=confirmatory offered to grid4x4_confirmatory (d) n_teleports=1 (e) wrong checkpoint_sha256 (f) one rtg_first != target; reuse / re-roll / move-aside / refuse; anything DELETED?
- [ ] 4. STAGE MAPPING — driver `confirmatory` -> --stage grid4x4_confirmatory; declared_cells counts 700 vs 1200; pinning test name
- [ ] 5. HEADER FORM EXECUTED (B.3-1) — tmux interactive bash, send Step-2 line, token absent -> passes leader check, creates nothing; one-liner form -> "not a process-group leader"
- [ ] 6. TOKEN — absent -> refuses, nothing created; present -> consumed once before work; second run refuses; path under output/p7_3d_runs/ not /tmp
- [ ] 7. PIPE HIDES EXIT CODE — COMPLETE/FAILED FILE marker in work dir? what is written on success / each failure path
- [ ] 8. MANIFEST — atomic (tmp then mv)? re-verify? directory scope
- [ ] 9. NUMBER-CORRECTNESS AT COLLECTION — engine seed, checkpoint digest per cell, 16 per-id rtg_first==target_i, n_teleports==0, cf_parity, "-1" — by line

## FINDINGS LOG

### [1] ORDER — 2026-09-23T00:47 — Read driver whole (offline/campaigns/p7_3d_grid4x4.sh, 258 lines, HEAD 8c79778 clean)
Observed order of refusals (all `exit 2/3`, all BEFORE token):
  interpreter :102-106 | cd MAIN + `-P` import-from-worktree :108-118 | live-runner pgrep :120-127 |
  group leader :129-136 | SigIgn :138-143 | stage arg :145-160 | inputs EXIST (-f only, NOT digest) :162-167 |
  5 checkpoints exist :169-176 | parity configs draws 1000-1099 :178-189 | dirty tree :191-198 |
  RSS budget :200-207 | canary (python) :209-213 | TRAP :215-223 | token check :225-229 | `rm -f "$TOKEN"` :230 |
  cells :243 | record-canary :246 | report :249 | manifest :252 | echo COMPLETE :255 (echo ONLY, no file)
  - Trap :223 precedes token :225-230. OK. record-canary :246 follows token. OK.
  - NO `mkdir` anywhere in the driver; NO `rm` other than :230 (the token). mkdir must live in Python; check whether `canary` (:209, pre-token) creates the work dir.
  - GAP vs checklist wording: driver has NO refusal for RESCO root (only default+export :94-95) and NO digest check of calibration/reference-cells (existence only :162-167). Whether Python checks these -> see item 9/3.
  - ⚠ item 7 preview: success path writes NO COMPLETE file (:254-257 are echoes). To verify in Python.
- [~] 1 (driver side verified; python-side mkdir in canary pending)

### [8-pre / 7-pre] MANIFEST SUBCOMMAND DOES NOT EXIST — 2026-09-23T00:52
cmd: `cd /home/filip/rltraffic && PYTHONPATH=/home/filip/rltraffic-p73d .venv/bin/python -P -m offline.transfer_curve manifest --help`
obs: `error: argument command: invalid choice: 'manifest' (choose from cells, pilot, a17f, report, canary, record-canary)` exit=2
  - offline/transfer_curve.py:3153-3186 build_parser: no "manifest" subparser; :3189-3301 main(): no manifest branch.
  - grep `SHA256SUMS_p7_3d|def manifest|add_parser("manifest"` across offline/*.py -> nothing.
  - Driver :252 `... offline.transfer_curve manifest "${COMMON[@]}"` will exit 2 under `set -e` AFTER cells(:243), record-canary(:246), report(:249) ran.
  - Consequence: header :23 "output/SHA256SUMS_p7_3d.txt rewritten last" is FALSE; :255 "CAMPAIGN COMPLETE" is never printed; driver exit 2 (masked by `| tee`).
  - Not destructive (nothing written by a failing argparse). Chunks stay on disk. -> MAJOR candidate (driver does not do what it claims; no manifest ever written).
### [report name] — main():3285-3290: stage grid4x4_confirmatory falls to `else` -> name = "p7_3a_zero_shot.json"
  - Driver :249 `report --stage grid4x4_confirmatory --out-dir $MAIN/output/p7_3d/artifacts` -> writes output/p7_3d/artifacts/p7_3a_zero_shot.json, NOT the header's p7_3d_grid4x4.json (:22, :256). Whether report() itself accepts the grid4x4 stage: pending read of report() body.
- [ ] 8 (subcommand absent; atomicity question moot) — pending report() read

### [4] STAGE MAPPING — 2026-09-23T00:58 — READ (driver :145-160, module :244, :286-342, tests)
  - driver :151 STAGE_ARG=grid4x4_confirmatory; :152-160 only `confirmatory` accepted; :244 `--stage "$STAGE_ARG"`, :250 same. module :244 STAGE_GRID4X4="grid4x4_confirmatory"; declared_cells(:301) -> grid4x4_cells() (:247-283: 5 seeds x 100 draws dt + 2 anchors x 100 = 700). declared_cells("confirmatory") -> hz1x1 slice (b_mean_k100 x 2 subj x 5 seeds x 100 + fixedtime+maxpressure x100 = 1,200).
  - Pin test: tests/test_g2_measure.py:627 test_the_driver_maps_its_argument_to_the_modules_own_stage_name (asserts STAGE_ARG text, every --stage line uses "$STAGE_ARG", len 700 vs 1200). Counts verified by execution below (item 3 probe).
- [x] 4 (counts to be re-confirmed by execution in the item-3 probe)

### [2-read] DESTRUCTION SITES (module) — 2026-09-23T00:58
  - _write_json :531-537: mkdir parents + tmp write + tmp.replace(target). Used by write_chunk :1600-1604, record_canary (transfer_calibration :2439), report's final write. OVERWRITES target if it exists (replace) — for report that is $ARTIFACTS/<name>; for record_canary $WORK/canary.json.
  - move_aside :1607-1621: source.replace(dest under failed/), suffix grows, never overwrites. Only in run_stage :2940-2941 for a chunk that is NOT reusable.
  - run_stage :2953 work.mkdir only if todo non-empty. report :2044-2051 refuses docs/data ONLY when cells is not None (caller-supplied) — with cells=None (driver path) there is NO docs/data guard in report(); the driver's --out-dir is $MAIN/output/p7_3d/artifacts so docs/data is not reached by configuration, not by refusal.
  - No rmtree/unlink/os.remove anywhere in the module. Driver: only `rm -f "$TOKEN"` :230.
  - Cross-campaign: WORK=$MAIN/output/p7_3d/cells, ARTIFACTS=$MAIN/output/p7_3d/artifacts — disjoint from output/p7_3a/ and output/p7_3b_anchor/; no path in the driver names them. No SHA256SUMS is written at all (manifest subcommand absent).

### [9-read] run_cell HAS NO grid4x4 BRANCH — 2026-09-23T00:58 — transfer_curve.py:1685-1872
  - :1715 demand_identity(draw_id, out_root=out_root) — NO scenario= (default SCENARIO_KEY = hz1x1); grid4x4-aware form exists at :980 but is not used here.
  - :1737-1742 target from load_calibration(P7_2B_CALIBRATION_NAME) + targets_for_subject(subject,...) — hz1x1 artifact; load_grid4x4_targets (:862) has NO call site in offline/*.py.
  - :1744 checkpoint_identity(GRID4X4_SUBJECT,...) -> CHECKPOINT_RECORD[subject] :714 / LOCAL_CHECKPOINT_MANIFEST[subject] :779 — keys to verify.
  - :1766 dt_choose(target_rtg=float) -> scalar branch :1331 (single-intersection agent), Mapping branch :1294-1329 (spatial) unreachable from run_cell; assert_rtg_first_matches_targets (:896) has NO call site.
  - :1848 calibration_sha256 hard-coded P7_2B_CALIBRATION_SHA256; validate :1510 pins the same -> a grid4x4 chunk would claim hz1x1's calibration.
  - :1769 anchor_choose(env, cell, config_path=demand["config_path"]) -> hz1x1 sumocfg handed to a grid4x4 env's anchor policy builder.
  -> Executing the pre-simulator part next to FALSIFY (no simulator needed).
### [8-read] report() for grid4x4: declarations_for(:1996) whole = None -> all_declared = declared_cells(None) = hz1x1's 4,700; grid4x4 names are prefixed cell_cityflow_grid4x4_* (:400-403) -> report :2088-2094 `extra` = every grid4x4 chunk -> refusal even on a complete campaign. To verify by execution.

### [9] EXECUTED — run_cell's pre-simulator calls for a grid4x4 cell — 2026-09-23T01:03
cmd: PYTHONPATH=/home/filip/rltraffic-p73d .venv/bin/python -P  (probe script; read-only; module loaded from the worktree)
  (i)  targets_for_subject(GRID4X4_SUBJECT, load_calibration(docs/data/p7_2b_calibration.json)) -> KeyError 'mappo1000_dt_nomix_h4' (artifact subjects: ['mappo1000','mix50']). This is run_cell :1737-1742 for EVERY one of the 500 DT cells -> _worker :2893 returns ok=False -> "FAILED KeyError" x500, no chunk written, no simulator started.
  (ii) checkpoint_identity('mappo1000_dt_nomix_h4', 101, output_root=MAIN/output, data_dir=WT/docs/data) -> ValueError "unknown subject 'mappo1000_dt_nomix_h4'; A17(c) registers ['mappo1000','mix50']". CHECKPOINT_RECORD/LOCAL_CHECKPOINT_MANIFEST keys: ['mappo1000','mix50','anchor_k200'] — the grid4x4 subject is registered in NEITHER (:485-490, :714).
  (iii) demand_identity(1000, out_root=MAIN/scenarios/draws) [run_cell :1715, chunk_is_reusable :1654, report :2123 — all WITHOUT scenario=] -> cityflow1x1/draw_1000/parity/noteleport.sumocfg cfg c177e962... routes c2c5eea2...; with scenario='cityflow_grid4x4' -> cityflow_grid4x4/draw_1000/... cfg c27d31e8... routes 6ab4d6d7.... DIFFERENT files, different digests. The 200 ANCHOR cells (kind=anchor) pass :1715 on hz1x1's files and reach env_for_cell :1749 (grid4x4 env via cell['scenario']) with anchor_choose :1769 handed hz1x1's sumocfg path; any chunk written records hz1x1's config_sha256/routes_sha256, which chunk_is_reusable and report then RE-DERIVE with the same default and find CONSISTENT -> invisible.
  (iv) declarations_for('grid4x4_confirmatory', None) -> all_declared 4,700 hz1x1 names, stage 700 grid4x4 names, overlap 0 (names are cell_cityflow_grid4x4_*). report :2088-2094 -> "700 chunk(s) are not declared cells of ANY stage" -> report REFUSES a COMPLETE grid4x4 campaign; driver dies at :249 (set -e). Even if it passed, main :3290 would name the file p7_3a_zero_shot.json.
  (v) run_stage :2932 identity carries no scenario; _worker :2890 run_cell(cell, **identity) — confirmed.
  (vi) constants: ENGINE_SEED=1000, EXPECTED_TIME_TO_TELEPORT='-1', PARITY_VTYPE_ID='cf_parity', EXPECTED_DECISIONS=360 — asserted per cell in validate_cell_payload :1540 (engine seed), :1494 ('-1'), :1499 (cf_parity), :1490 (n_teleports==0), :1503 (360 decisions). rtg_first==target_rtg :1579 compares the SCALAR fields run_cell writes (:1856-1857); the 16-id refusal assert_rtg_first_matches_targets :896 is NEVER CALLED. checkpoint digest per cell: chunk_is_reusable :1660-1667 and report :2132-2144 both via checkpoint_identity, which RAISES for the grid4x4 subject.
  counts by execution: declared_cells(STAGE_GRID4X4)=700, ('confirmatory')=1200, (None)=4700.
  => BLOCKING: at 8c79778 the campaign cannot roll a single DT cell, and its 200 anchor cells would carry the WRONG scenario's demand provenance. The token would be consumed (:230) for this.
- [x] 9 (all six quantities located by line; the grid4x4 wiring is absent)
- [x] 8 (manifest subcommand absent; report refuses the grid4x4 stage by declaration mismatch)

### [3] RESUME BY CONTENT — EXECUTED in sandbox work3 — 2026-09-23T01:08
cmd: synthetic payloads from grid4x4_cells() (anchor fixedtime draw1001; dt seed101 draw1001), HEAD 8c79778, through validate_cell_payload / reusable_chunk_at (= what run_stage :2937 calls) / move_aside (= run_stage :2940-2941).
  ctrl anchor chunk carrying HZ1X1's demand digests (what run_cell :1715 would record) -> REUSABLE True.  <- the wrong-scenario provenance is INVISIBLE to resume.
  (g) anchor chunk carrying the CORRECT grid4x4 digests -> False -> MOVED ASIDE, would be re-rolled.  <- resume predicate (:1654) is wired to hz1x1.
  (a) truncated file -> reusable False (json parse fails :1679-1681) -> moved aside to failed/, nothing deleted.
  (b1) git_commit = 23d4a16 (code differs) -> False -> moved aside (re-roll). (b2) git_commit = 837f703 (docs-only parent) -> True (J1(c), by design). (b3) 40-hex unknown to git -> RuntimeError PROPAGATES out of reusable_chunk_at (:1648-1651 deliberate) -> run_stage :2937 would crash the driver at :243, AFTER `rm -f $TOKEN` :230. Not destructive; token lost.
  (c) stage='confirmatory' offered to grid4x4_confirmatory -> validate refuses (:1475-1481 stage in identity) -> False -> moved aside.
  (d) n_teleports=1 -> validate refuses :1490 -> False -> moved aside.
  (e) DT control chunk -> False (checkpoint_identity raises "unknown subject" for GRID4X4_SUBJECT, caught :1668) — so EVERY grid4x4 DT chunk is non-reusable regardless of its sha; wrong sha -> False, indistinguishable. Resume for the 500 DT cells is BROKEN (every restart moves all DT chunks to failed/ and re-rolls).
  (f) per-id dict rtg_first with ONE intersection off -> refused by :1579 dict `!=` (incidental; the 16-id refusal :896 is not called); scalar mismatch -> refused.
  DELETIONS: none in any case (files before == after; move_aside suffixes .1 .2 ...). Sandbox tree: failed/ with 9 files.
  anchor_choose :1357-1392: config_path -> collect_style_args("sumo", arm, config_path, ...) :1381 -> build_policy(env, args) :1384. For a grid4x4 anchor cell run_cell hands HZ1X1's noteleport.sumocfg here (:1715 default scenario). Effect on the built policy NOT verified (build_policy in sumo_att_reference not read); provenance effect verified above.
- [x] 3

### [5][6][7] EXECUTED under tmux — 2026-09-23T00:56 — driver copy with ONLY WORK (:77) and TOKEN (:81) redirected (diff verified); capture redirected to sandbox
  RUN A (tmux new -d; send-keys the Step-2 line at the PROMPT; token absent): passes interpreter/import/pgrep/GROUP LEADER/SigIgn/stage/inputs/checkpoints/parity/dirty/RSS, then
      `python -m offline.transfer_curve: error: unrecognized arguments: --draws-root ... --out-dir ...` -> "REFUSING TO START: the canary failed. NOTHING has been consumed." PIPESTATUS=2 0. WORK not created. No "not a process-group leader" -> B.3-1 header form PASSES the leader check.
  RUN B (one-liner `tmux new -d -s X 'bash copy confirmatory 2>&1 | tee ...'`): "REFUSING TO START: not a process-group leader" exit 2, WORK not created -> the header's WHY is measured true.
  RUN C (prompt form, token PRESENT, copy C = A + ARTIFACTS redirected + `--limit 0` on cells): same argparse refusal at the canary; token STILL PRESENT afterwards (refusal precedes :225-230, correct); nothing created.
  RUN D (prompt form, token absent, second run): same refusal.
  ==> NEW BLOCKING/MAJOR: driver :98-99 COMMON=(--draws-root ... --out-dir ...) is appended AFTER the subcommand on EVERY call (:209 canary, :243-244 cells, :246-247 record-canary, :249-250 report, :252 manifest), but build_parser :3147-3151 defines those options on the PARENT parser -> argparse "unrecognized arguments" -> exit 2. The delivered driver can NEVER pass :209 -> it always refuses at the canary, on any machine, token or not. The token is never consumed and nothing is ever created (so question (a) is trivially safe at this commit).
  [7] PIPESTATUS=2 0 in every run: the driver's exit 2 is masked by tee's 0 under `bash driver | tee -a cap`. Driver writes NO COMPLETE/FAILED FILE: success path :254-257 echoes only; every refusal is `echo >&2; exit`; on_signal :215-222 echoes only. p7_3b_anchor.sh :251 `tee "$WORK/FAILED"`, :265, :360 `tee "$WORK/COMPLETE"` — the claimed shape (:4) writes both; this driver writes neither.
  Real dirs after all runs: output/p7_3d/ {calibration, g2} unchanged, output/p7_3d/cells absent, output/p7_3d_runs/ unchanged; no live runner; no leftover tmux sessions.
- [x] 5   - [x] 7   - [~] 6 (absent->refuses+creates nothing VERIFIED; consumption itself UNREACHABLE in the delivered driver — probing with a reviewer-corrected copy E next)

### [6-E attempt 1] 2026-09-23T00:57 — RUN E refused "cells from another run are still alive" exit 3: the driver's pgrep (:120) matched the REVIEWER's own harness shell, whose argv quoted the module's usage line. Not a driver defect (fails safe: refuses rather than runs); MINOR note: `pgrep -f` over the pattern matches ANY process whose command line quotes it (an editor, a grep, a tee'd note). Token untouched, nothing created. Re-running E from a script file with no matching text.

### [6][1][7] RUN E EXECUTED (reviewer-corrected copy: COMMON before the subcommand; --limit 0; WORK/TOKEN/ARTIFACTS in sandbox) — 2026-09-23T00:58
  canary passed (1.02 s, local_return -32648.0, decisions 360, two_routes_agree true) -> trap -> token CONSUMED (file gone) -> banner -> cells --stage grid4x4_confirmatory --limit 0: n_declared 0 -> record-canary wrote work/canary.json (this is the FIRST mkdir of WORK; nothing pre-token created WORK — item 1 python-side closed) -> report: ValueError "700 declared cell(s) have no chunk" exit 1 -> driver dies at :249; manifest (:252) never reached; no COMPLETE/FAILED file; PIPESTATUS=1 0 (tee masks).
  canary.json: git_commit 8c79778, dirty False, seconds 1.02, keys [facts, format_version, git_commit, git_dirty, line, seconds, threshold_seconds] — record-canary (:2437-2449) parses before it writes, atomic via _write_json.
  Real dirs: output/p7_3d/{calibration,g2} unchanged; cells/ and artifacts/ absent; p7_3d_runs/ unchanged.
  NOTE: this is a REVIEWER CONSTRUCTION; the delivered driver at 8c79778 cannot reach any of this (see [5][6][7] argparse finding).
- [x] 1

### [6] RUN E2 (copy E, token ABSENT, second run) — 2026-09-23T00:59
  canary passed 0.86 s -> "REFUSING TO START: no run token at <sandbox>/TOKEN_confirmatory ... Nothing has been consumed." exit 2 (PIPESTATUS=2 0); work/ unchanged (canary.json from run E untouched, nothing added).
  Token path in the delivered driver :81 = $MAIN/output/p7_3d_runs/TOKEN_confirmatory (under output/p7_3d_runs/, not /tmp). Consumption :230 exactly once, after trap :223 and before any work :243.
- [x] 6 (verified on the corrected copy; UNREACHABLE in the delivered driver)

### [2] DESTRUCTION PATHS — CLOSED — 2026-09-23T00:59
  Driver: the only rm is `rm -f "$TOKEN"` :230 (after every refusal; verified by A/C/D: refusal before it leaves the token in place). No mv, no truncating `>`; the capture is `tee -a` (append) by the header's line.
  Module: _write_json (tmp+replace) at write_chunk :1603, record_canary (calib :2439), report's final write; move_aside :1620 replace into failed/ with growing suffix; NO rmtree/unlink/os.remove. Deletions observed in the sandbox: none.
  Cross-campaign: WORK/ARTIFACTS are output/p7_3d/{cells,artifacts}; output/p7_3a/ and output/p7_3b_anchor/ are named nowhere in the driver; `--output-root $MAIN/output` is read-only (checkpoint hashing). No SHA256SUMS_* is written at all (no manifest subcommand) so no other campaign's manifest can be overwritten. report's out_path is --out-dir (=$ARTIFACTS); docs/data is reached by no configured path (report's docs/data refusal :2044-2051 applies only to caller-supplied cells, i.e. not on the driver path — a configuration guarantee, not a refusal).
- [x] 2

### CONTEXT — docs/returns/P7.3d.md:808-809 — 2026-09-23T01:00
  Packet: "T-driver executes the header's own line through tmux: the group-leader check does not fire — it refuses at the DIRTY-TREE check". The dirty-tree check is :191, BEFORE the canary :209. The implementer's execution therefore never reached the argparse failure; tests/test_g2_measure.py:592 asserts only the absence of the group-leader text, so it stays green with a driver that cannot pass its own canary. My runs used the CLEAN worktree and reached :209.
  Packet :842-843: "The pre-flight (G4) has not run." — consistent.

## VERDICT: NOT CLEAR
BLOCKING
  B1 driver :98-99 + :209/:243/:246/:249/:252 — COMMON options after the subcommand -> argparse exit 2 at the canary on every start; the delivered driver can never consume the token or roll a cell. (RUN A/C/D)
  B2 transfer_curve.py run_cell :1715/:1737-1746/:1766 — no grid4x4 branch: 500 DT cells KeyError (probe i), checkpoint_identity unknown subject (probe ii), 200 anchor cells pinned to hz1x1 demand identity (probe iii; ctrl vs (g) in sandbox) and reusable as such — a number wrong at collection, invisible to resume and report.
  B3 report for stage grid4x4_confirmatory refuses a complete campaign (declarations_for :1996 whole=None -> 4,700 hz1x1 names, overlap 0; probe iv) and would name p7_3a_zero_shot.json (main :3290); `manifest` subcommand does not exist (argparse) -> header :22-23 and :256 describe files that are never written.
MAJOR
  M1 no COMPLETE/FAILED file on any path (:254-257 echo only; on_signal :215-222 echo only) while p7_3b_anchor.sh :251/:265/:360 writes both; the pipe masks the exit (PIPESTATUS=2 0 / 1 0 in every run). Packet 22.4 says "read the text" — there is no machine-readable terminal marker at all.
  M2 resume for the 500 DT cells is unconditionally non-reusable (checkpoint_identity raises for GRID4X4_SUBJECT, caught :1668) -> every restart moves every DT chunk to failed/ and re-rolls (sandbox e-ctrl).
  M3 assert_rtg_first_matches_targets :896 (the 16-id refusal) has no call site; load_grid4x4_targets :862 has no call site; calibration_sha256 :1848 hard-codes P7_2B's pin for a grid4x4 cell.
MINOR
  m1 driver checks calibration/reference-cells/cap_e by EXISTENCE only (:162-167), not digest (the checklist wording says "by digest"); no RESCO-root refusal in the driver (:94-95 default+export only).
  m2 a chunk whose git_commit is 40-hex but unknown to git makes reusable_chunk_at PROPAGATE RuntimeError (:1648-1651 by design) -> driver dies at :243 after the token is consumed (sandbox b3).
  m3 pgrep :120 matches any process whose argv quotes the module path (fails safe).
VERIFIED GOOD: leader guard both forms (A/B); trap before token; token consumed once, after canary, before work (E); refusal-before-token leaves token (C); no deletions anywhere; move_aside never overwrites; record-canary parses-then-writes atomically; cross-campaign dirs disjoint; docs/data not written.
NOT REACHED: build_policy's use of the hz1x1 config path on a grid4x4 env (anchor_choose :1381-1384) — not executed (needs SUMO); whether the 200 anchor cells would even complete.
