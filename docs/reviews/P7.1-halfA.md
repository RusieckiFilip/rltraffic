# Independent review — P7.1, HALF A of 2 (`task/p7.1-alignment` @ `6be6603`; the SUMO ATT observer, the metric-freeze campaign and artifact, `P7.1_FREEZE.md` §"Metric")

**Filed by the coordinator, verbatim, 2026-09-12.** Written under the §7 incremental-findings rule (34 items ticked in the reviewer's `FINDINGS.md`); every mutation restored and `cmp`-verified; nothing written under `output/`. **Ruling: with HALF B, in `BRIEF_34` Amendment H.**

---

## REVIEW — P7.1, HALF A (SUMO ATT observer, metric-freeze campaign and artifact, FREEZE §"Metric")

**Tree:** `/home/filip/rltraffic-p53b`, branch `task/p7.1-alignment` @ `6be6603` (13 commits over `main` @ `a3a1713`). **Date:** 2026-09-12.
**VERDICT (half A): PASS-WITH-NOTES.** Blocking 0 / Major 2 / Minor 4.
**Mutations:** 9 source mutations executed (M1, M2, M3, M3b, M5, M6, M7, M8, M9) — **8 KILLED, 1 SURVIVED (M3b)**; plus 2 input perturbations through `freeze_artifact` (M4, M10) — both ACCEPTED, one of which (M10) is a finding.
**Reviewed:** `offline/sumo_att_reference.py`, `tests/test_sumo_att_reference.py`, `offline/campaigns/p7_1_metric_freeze.sh` (via tests), `docs/data/p7_1_metric_freeze.json`, `docs/notes/P7.1_FREEZE.md` §"Metric", `docs/returns/P7.1.md` §4/§7/§8/§11, the new `.sumocfg`, `output/p7_0` (36 npz), `output/p7_1` (12 chunks + smoke + timing). Ran: `pytest tests/test_sumo_att_reference.py -q` → **112 passed, 2 skipped in 4.64 s**; the 2 skips (`:1702`, `:1711`, no `output/p7_0` in the worktree) pass with `RLTRAFFIC_OUTPUT_ROOT=/home/filip/rltraffic/output` → **2 passed**. One SUMO episode re-rolled into scratch (51.67 s).
**Findings file:** `/tmp/claude-1000/-home-filip-rltraffic/42cbf76f-5d6e-44ba-b684-496d2fe5804d/scratchpad/review-p71-halfA/FINDINGS.md` (34 items ticked, 3 flagged; scripts `recompute_A.py`, `recompute_B1.py`, `recount_EGHI.py`, `mutate.py`, re-roll under `work/`).

### BLOCKING
None. Every number shipped in `docs/data/p7_1_metric_freeze.json` recomputes from its own rows with stdlib, reproduces P7.0's raw npz 45/45, and a fresh SUMO episode reproduces the campaign chunk bit-for-bit on 31 of 32 fields (the 32nd is wall time).

### MAJOR
1. **The hz4x4 timing chunk carries a false P7.0 comparison, and the A4 resume guard does not work on the artifact it shipped.** `output/p7_1/timing_hz4x4_gudang.json` rows[0] has `has_p7_0_reference: True`, `reproduces_p7_0: False`, `att_p7_0_stored = 492.2801208496094` — which is hz1x1 `sumo__random` ep0's P7.0 value (`freeze_sumo_random.json` rows[0] carries the same number); `reproduction: {n_equal 0/1, max_abs_difference_float64 156.92}`. Cause: `_run_timing` (`offline/sumo_att_reference.py:2361-2369`) calls `run_sumo_arm("random", …, regime="parity")` and `_stored_reference` (`:1381`) returns NaN only for `regime != "parity"` or a cell not in `P7_0_CELL_DIRS`, so a gudang episode is scored against a hangzhou-1x1 reference. Consequences: (a) the docstring at `:1066-1073` ("False for the hz4x4 timing episode… reported as `n_unverified`") is false on the shipped chunk; (b) `_timing_chunk_is_reusable(<real file>, episodes=1)` returns **False** (I called it), because `chunk_is_reusable:1739` re-derives the reproduction — so a driver restart would re-roll A4 every time, which is exactly what Amendment E1 item 4 (mn-1) was meant to stop; (c) `test_a4_skips_a_complete_timing_chunk_rather_than_re_rolling_seven_minutes` (`tests/test_sumo_att_reference.py:1448`) passes only because its fixture writes `att_p7_0_stored = nan`, which the runner never does for hz4x4 — a test that models the intended behaviour, not the code. No number in `docs/data` is affected (`_run_report:2402` globs `freeze_*.json` only), and `_run_timing` returns 0 regardless (`:2378`). A reader of the chunk would conclude SUMO failed to reproduce by 157 s.
2. **Amendment D3's population second route is not implemented as a check.** `BRIEF_34:536-539` registers the population term's independent route as "`n_pending_at_horizon == n_never_entered` and the denominator equal to the route file's `depart ≤ T` count". Neither equality is asserted anywhere in the module (grep of `n_pending_at_horizon`: `:360, :790, :986, :1258, :1357, :1491, :1981, :2106` — reporting only); `n_created` is `|due|` from the same `due_by` call (`:762`), not a second route. M10 (one `sumo__maxpressure` row perturbed through `freeze_artifact`): `n_pending_at_horizon = n_never_entered − 1` → ACCEPTED; `n_created = 2020` → ACCEPTED; `n_never_entered = n_created − n_entered + 5` → ACCEPTED. M3b (source: `:730` `due_by(end, step_length=…)` → `due_by(end)`, i.e. dropping the measured `(S)` offset from `E_sumo`'s population at the horizon) **SURVIVED** all 112 tests: the synthetic stream (`tests:95-157`) declares no vehicle in `(T−dt, T]`. On this scenario nothing is wrong — I recounted `n_pending == n_never_entered` on 30/30 SUMO rows and `n_created == n_entered + n_never_entered` on 45/45, and the route file's max `depart` is 3599 so the edge is empty — but the definition A15 (draft) freezes ("`intended_depart + dt ≤ T`", `FREEZE.md:176`) is enforced only inside `observe()` (`:543`, killed by M1), not where `E_sumo` is computed.

### MINOR
1. `FREEZE.md:123-124` quotes the admitted-pair gap as `6.057427`; recomputed from the artifact's float64 rows it is `6.0574250`. The quoted figure is P7.0's own `p7_0_gate.json` `/branch/rows[3]/statistic 6.057426806204541` (computed from float32-stored ATT). The column is labelled "P7.0 verdict", so it is not wrong, but the same row mixes a P7.0 float32-derived number with P7.1 float64 numbers without saying so.
2. `W == att_env` on SUMO is not asserted under `==` anywhere; `freeze_artifact:2048-2053` only counts `term_cadence == 0.0` and takes a max. M4 (one SUMO row `att_env += 1e-9`, still float32-reproducing) → ACCEPTED, `n_with_zero_cadence_term` 30 → 29, `max_abs_term_cadence` unchanged (CityFlow's 4.78 dominates). Consistent with Amendment A5 (measure, do not gate), but `FREEZE.md:98` "EXACTLY 0.0 on all 30" rests on a count, and a sub-ulp SUMO cadence would be invisible in `max_abs_term_cadence`.
3. `FREEZE.md:141-144, 153` and packet `:157` cite a **60.59 s idle re-measurement** and a hz4x4 re-run "bit-identical in float64 on every quantity"; neither run is on disk (`output/p7_1/` has one timing chunk, 50.306 s). Unverifiable from a clone.
4. Packet §7 M1 says "7 unit tests" kill the offset mutation; under `-x` I observed the first (`test_the_pool_identity_holds_at_every_second_of_the_synthetic_stream`) and did not count the rest.

### Recomputation table (stdlib `json` only; no `offline/` import)
| quantity | recomputed | reported | diff |
|---|---|---|---|
| 9 cells × {att_env, E, P, W} means | from `episodes[]` | `cells.*_mean` | max 2.27e-13 |
| terms P−E, W−P, att−W (45 rows) | vs `term_*` | — | 0.0 exact |
| `att_env − E == Σ stored terms` (`==`) | 0 failures / 45 | residual max 0.0 | — |
| clock-origin second route max | 5.684341886080802e-14 | 5.68e-14 (`FREEZE:111`) | 0 |
| ρ (`PREREGISTRATION:175`) all 3 backends × 2 defs | vs `rho` block | — | max 1.39e-13 |
| Δ cf/sumo/nt env | 37.6645 / 11.6050 / 7.4667 | same | ≤5.7e-14 |
| Δ cf/sumo/nt E | 235.1994 / 237.6252 / 228.3625 | same | 0 |
| M, 0.5·M (pool-clock) | 1.475016, 0.737508 | `FREEZE:123` | 0 |
| A4 gap parity / noteleport | 0.361009 / 0.315823 | `FREEZE:123,133` | 0 |
| admitted gap | 6.057425 | 6.057427 (P7.0's) | 2e-6 (Minor 1) |
| Δ ratio | 1.0103 / 0.3081 (=1/3.246) | 1.010× / 3.246× | 0 |
| P7.0 npz `att_per_step[-1]` (float32) vs `att_env` | **45/45** equal; max float64 1.4732623924373911e-05 | 45/45; 1.473e-05 | 0 |
| cadence: SUMO rows `== 0.0` | 30/30; both counters 0 on 30/30; CF 15/15 non-zero, 3.6743…4.7827 | same | 0 |
| halting | 6 × 28,800 = 172,800; max abs 0; 0 disagreeing; declared 0.1 == `metrics/sumo.py:10` | same | 0 |
| teleports | mp parity [13,15,14,14,13], 25 other rows 0; noteleport 15/15 zero | same | 0 |
| regimes | fixedtime & random `att_env` and `E` `==` across regimes; mp +4.138302683211066 env / +9.262741217219173 E | +4.1383 / +9.2627 | 0 |
| never-entered means | 572.0 / 253.6 / 891.0 / 257.2 | `FREEZE:87-91` | 0 |
| timing s/episode (12 cells) | `seconds/n` == stored, diff 0.00e+00 | packet `:154-156` | 0 |
| hz4x4 chunk | 50.30633 s | 50.31 | 0 |
| observed vs unobserved `att_env` | identical per arm 3/3 | "observer does not perturb" | 0 |
| re-rolled `sumo/maxpressure` ep0 vs chunk rows[0] | 31/32 fields `==` (E 442.3349826818407, P 452.5783874580067, W = att_env 355.7984322508399, 13 teleports, 235 pending = 235 never-entered, 2021 created, halting 28800/0/0) | — | only `seconds` |
| route file | 2021 `<vehicle>`, 0 flow/trip, unique ids, integer departs 0…3599; `depart ≤ 3600` = 2021 = `n_created` | 2021 | 0 |
| `.sumocfg` diff | XML delta is exactly `<processing><time-to-teleport value="-1"/></processing>` (+ comment); net and route files referenced, not copied | — | — |
| diff stat | 15 files, 23503 insertions; stat lines identical to packet §1 | same | 0 |

### Mutations (each restored with `git checkout --`, `cmp` against `HEAD:` identical)
M1 `:543` drop `− step_length` → KILLED (`test_the_pool_identity_holds_at_every_second_of_the_synthetic_stream`). M2 `:221` tolerance 1e-9→1e9 → KILLED (`test_the_artifact_refuses_a_clock_origin_that_fails_its_second_route`). M3 `:598` skip per-second identity → KILLED (`…catches_a_vehicle_that_falls_out_of_the_accounting`). **M3b `:730` drop `step_length` from `due_by` → SURVIVED (112 passed).** M5 `:1743-1744` → KILLED (`test_a_noteleport_chunk_that_teleported_is_not_reusable[n_teleports]`). M6 `:1931` → KILLED (`test_the_artifact_refuses_a_noteleport_chunk_that_teleported[n_teleports]`). M7 `:2411-2413` → KILLED (`test_report_refuses_an_incomplete_campaign_and_writes_nothing`). M8 `:1759-1762` → KILLED (`test_the_artifact_refuses_an_episode_that_does_not_reproduce_p7_0`). M9 `:2296` → KILLED (`test_a_failed_chunk_is_moved_aside_before_the_cell_is_re_rolled`, 1 failed / 111 passed). Packet's 15/15 claim: 4 of its 15 independently confirmed (its M1, M4/M5, M6, M12).

### Frozen set, deps, hygiene
`git diff --name-only main...HEAD`: 15 paths, all under `docs/`, `offline/`, `tests/`, `scenarios/…_parity/` (new file). Imports: stdlib + numpy, lazy repo-internal only; traci via the env. `check_test_hygiene.sh` on the three new test files: 0 `[TH0` (exit 0; the script's no-argument form blocks on stdin in this harness — not a branch issue). `check_english.sh` on the six half-A files: exit 0.

### What I could NOT verify
- The 60.59 s idle re-measurement and the "bit-identical float64" hz4x4 re-run (not on disk).
- The packet's "7 unit tests" for M1 and 11 of its 15 mutations (not re-executed).
- Reproduction below one float32 ulp (by construction of the A2 form; ~3e-5 at ATT 355).
- The `(S)` offset on any scenario other than hz1x1 (the route file has no vehicle in `(T−dt, T]`, so M3b's survival is a latent gap here, not a wrong number).
- The pool identity on the 29 campaign episodes I did not re-roll (verified on 1 re-roll + the artifact's `max_abs_depart_clock_deviation = 0.0` on all 30 rows).

### Exit state / disclosures
Worktree `git status --porcelain` empty; `offline/sumo_att_reference.py` `cmp`-identical to `HEAD`; main tree porcelain empty; `output/SHA256SUMS_p7_1.txt` 29/29 OK after my re-roll; `output/p7_1/NEVER_WRITTEN` absent; nothing written under `output/`. Housekeeping disclosure: while cleaning up my own hung hygiene job I killed PID 162321, a 7-day-old orphaned `until … pgrep -f check_test_hygiene …; sleep 5` monitor loop from another session (it matched my grep). It touched no repository state.
