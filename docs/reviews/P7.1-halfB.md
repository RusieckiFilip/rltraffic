# Independent review — P7.1, HALF B of 2 (`task/p7.1-alignment` @ `6be6603`; the alignment adapter, the paired-scenario table, the Conversion Audit Protocol tool and artifacts, `P7.1_FREEZE.md` §"Features")

**Filed by the coordinator, verbatim, 2026-09-12.** Written under the §7 incremental-findings rule; every mutation restored and `cmp`-verified; both trees clean at exit. **Ruling: `BRIEF_34` Amendment H — FIX-FIRST for both halves.**

---

## REVIEW — P7.1, HALF B (alignment adapter, paired-scenario table, Conversion Audit Protocol)

**Tree:** `/home/filip/rltraffic-p53b`, branch `task/p7.1-alignment` @ `6be6603` · main tree `/home/filip/rltraffic` @ `aa8c8a1`
**Date:** 2026-09-12
**VERDICT (half B): FAIL** — 1 blocking, 6 major, 6 minor
**Mutations:** 14 executed on half-B code, 9 killed, **5 survived** (M6, M8a, M9, M11, M12). The packet's "15 executed, 15 killed" (§7) are all half-A mutations; the author recorded none on `backend_alignment.py` or `conversion_audit.py`.
**Reviewed:** `offline/backend_alignment.py` (730 l), `offline/conversion_audit.py` (616 l), `tests/test_backend_alignment.py` (24 tests), `tests/test_conversion_audit.py` (14 tests), `docs/data/p7_1_{canonical_order,paired_scenarios,conversion_audit}.json`, `docs/notes/P7.1_FREEZE.md:196-332`, packet §5/§7/§8/§11, brief §0.3–0.4/§3/Amendments B–C, `PROJECT_PLAN` P11.5.
**Ran:** yes — `pytest tests/test_backend_alignment.py tests/test_conversion_audit.py -q -p no:randomly` → **35 passed, 3 skipped in 5.93 s** with `RLTRAFFIC_GRID4X4_RESCO` pointed at the candidates root; **14 passed in 1.58 s** for the audit file when pointed at the `…/environments/grid4x4` directory (the 3 skips were the grid4x4 tests, see MINOR-1). `check_test_hygiene.sh` TH0 count **0**; `check_english.sh` exit **0**. Both trees `git status --porcelain` empty at exit; both mutated files `cmp`-equal to HEAD.

### BLOCKING

**B-1. CAP criteria B and C in the paper table are computed by code with zero test coverage, and each criterion is only half-implemented relative to CAP's definition.**
- `conversion_audit.py:426-453` (B): only `max_abs_coordinate_residual`, a *set* comparison of speeds, and SUMO-only `lane_length_min/max`. CAP(B) (`PROJECT_PLAN.md:750`) requires "per-lane length and speed limit — exact or enumerated per lane with max abs difference". No CityFlow-vs-SUMO length comparison exists. My own recomputation (CityFlow road polyline length vs SUMO lane length, per lane): **max abs diff 10.4 m on hz1x1 (16 lanes), 27.2 m on gudang (240), 27.2 m on grid4x4 (240)** — a real, non-zero residual that the artifact does not enumerate.
- `conversion_audit.py:456` + `:145-175` (C): direction *counts* and extras only. CAP(C) requires "the (in-lane → out-lane) connection set under the movement correspondence equals CityFlow's laneLinks exactly … any MISSING one a FAIL". Not computed. My own check: CityFlow `laneLinks` == SUMO tl-connections under lane-index reversal on all three pairs (16/16, 576/576, 576/576, zero missing) — so the shipped conclusion happens to hold, but the artifact carries no evidence for it.
- Mutations **M9** (netOffset ignored), **M12** (speed sets → count equality) and **M11** (extras never enumerated) all **SURVIVE** with 14/14 passing. The `0.0 over 5/32/32`, `11.111 vs 11.11` and `4 / 16 / none` cells of FREEZE §10 and packet §5.3 are unprotected numbers.
→ A reader of the paper would take "CAP B and C pass" from a table that never computed B's length half or C's equality half and that no test guards.

### MAJOR

**M-1. `align_info` silently drops a lane it cannot place, contrary to its own contract.** `backend_alignment.py:423-433` `_looks_like_an_outgoing_lane` is a *name-prefix* test. Probe on the real hz1x1 alignment: a dict entry `road_1_1_99_0` (no such road; prefix of the known intersection) is **accepted and dropped** without error; `road_9_9_9_0` is refused. Contradicts `:65` ("raises on any lane it cannot place") and `:395-398`. No test covers this class (my extra mutation M6b shows only the unknown-prefix class is tested).

**M-2. The hangzhou phase map is applied unconditionally; on a same-width pair it is wrong and nothing refuses.** `alignment_for_scenario` builds grid4x4 `A1` without refusal (cf/sumo phases 16/16, widths 40/40) and `align_info` maps SUMO phase 14 → CityFlow phase **8** (`:160-175`, `:340`, `:360-361`), where the same 16-phase program means 14. Also `n_actions` (`:279-281`, non-empty phases → **16** on grid4x4) disagrees with the env's rule (`envs/phase_control.py:21,472`, duration > 5 → 8). Not a shipped number today (scope is hz1x1/gudang), but grid4x4 is the C3 candidate A14 admits, and Amendment C(D) explicitly asked for the env's action count to be *stated* — it is not (grep of FREEZE/audit/packet: nothing).

**M-3. The three committed artifacts have no generating command on disk.** Whole-tree grep finds no caller of `audit_artifact(pairs)`, `canonical_order_artifact()` or `paired_scenario_table()` outside the modules and one `audit_artifact([])` test. The audit JSON's hand-written `notes` strings, the choice of pairs, and the absolute `/home/filip/rltraffic/scenarios/grid4x4_candidates/…` paths are not regenerable from a clone. `paired_scenario_table` also `rglob`s `scenarios/**` with no exclusion (`:515`): on a tree with the candidates clone present it returns **44** rows, not 20, and reads the CC BY-NC-SA files.

**M-4. Metric filtering is untested (M6 survived) and E's multiset equality has no negative control (M8a survived).** `aligned["metrics"] = dict(metrics)` passes 24/24; `multiset_equal := counts equal` passes 14/14. The "2983/2983 as a multiset" cell is protected only through the index-aligned count (M8b killed).

**M-5. §3.3 test 3 quotes P7.0's released-lane sets rather than deriving them.** `tests/test_backend_alignment.py:298-323` reads `cityflow_released_lanes` / `sumo_released_lanes_raw` from `p7_0_gate.json` and recounts 8/8, 4/8; it never calls `transfer_gate.green_action_lane_sets` or the network files, although FREEZE:232 and brief §0.4 say it does. I derived the sets from `roadnet.json` + `.net.xml` myself: **8/8 under the correspondence, 4/8 under identity, identity-agreeing actions = [4,5,6,7] = exactly the both-lanes-of-one-road actions; the gate's stored sets equal mine 8/8** — so the number is right, the test's provenance claim is not.

**M-6. Module docstrings state falsehoods the artifact itself contradicts.** `conversion_audit.py:46-47` and `:187-188`: the linkIndex→roadLink permutation "is [the identity] on grid4x4 and hz1x1". The artifact records hz1x1 `[7,6,4,5,2,3,0,1]`, `permutation_is_identity: false`, identity score 1/9; grid4x4 A1 `[3,4,5,6,7,8,0,1,2,9,10,11]`, identity 26/256. `backend_alignment.py:3-4` still reads "SKELETON. Every function below raises NotImplementedError" in delivered code (the same class the packet's D-10 flags in another file).

### MINOR

1. `tests/test_conversion_audit.py:28-40`: `RLTRAFFIC_GRID4X4_RESCO` is treated as the grid4x4 directory while the skip text says "the candidates directory"; the default is the hardcoded `/home/filip/rltraffic/...` path. The packet's "152 passed" ran the grid4x4 tests only via that machine-specific default.
2. `cityflow_phase_for_sumo_phase(16)` returns 9 (no refusal); `tests/test_backend_alignment.py:141-144` says "a 17th would silently become a 9th green" but tests only −1. `align_info` would then `IndexError` at `:361` — accidental protection.
3. Paired-table cologne3 row carries no connection-direction counts (row keys lack any `dirs` field), though brief §3.1 asks for "dirs by type" and FREEZE:264 asserts `t`/`L`/`R`. My own count from normal edges: t 39, L 1, R 1.
4. `clearance_characters` uses a `duration <= 5.0` heuristic (`conversion_audit.py:505-512`); reported not asserted, fine, but grid4x4's `G` is real (`A1` 3-s phase `sssrrrGGG…yyy…`).
5. E compares `(depart, route)`; Amendment B says `(id, depart, route)`. `flow.json` has no ids — acceptable, should be stated.
6. `test_the_audit_artifact_issues_no_verdict` is a string search over prose; `test_align_info_passes_the_untouched_fields…` is subsumed by the live test.

### Recomputation table (my code, `fractions.Fraction`, no `offline/` imports for A/E; `transfer_gate` imported only where check D allowed it)

| quantity | shipped | recomputed | equal |
|---|---|---|---|
| correspondence + discovery order from raw `roadLinks[].type` / `connection dir=` | canonical_order.json | same 8 pairs, same order, sumo_position `[0,1,2,3,6,7,5,4]`, 8/8 differ | yes |
| 16 `per_feature` rows KS & OVL (`==` on `float(Fraction)`) | p7_0_gate.json | **16/16** on arm maxpressure (fixedtime 0/16, random 0/16 → arm identified) ; min OVL `0.3407202216066482` | yes |
| identity pairing → `voided_per_feature` | 16 rows | **16/16**; min OVL `0.055401662049861494` | yes |
| green actions 8/8 corr, 4/8 identity, from files | packet §5.1 | 8/8, 4/8, identity set [4,5,6,7] | yes |
| `n_sumocfg` / `n_alignable` | 20 / 7 | 19 in main + 1 new on branch = 20; 7 reproduced (4 hz1x1, gudang 16/16, hetero 16/16, manhattan 196/196) | yes |
| cologne1 / cologne3 | 0/1, 16/19 ids eq · 0/3, 38/76 ids ≠ | same by CF-parser route (`metrics/sumo.py:62-66`) **and** by SUMO-net route; same messages | yes |
| A counts | 1/8/16 · 16/80/240 · 16/80/240 | same; grid4x4 entries carry both keys, 16 `gt_virtual` | yes |
| B residual with netOffset | 0.0 / 0.0 / 0.0 | 0.0 (300.0 / 800.0 / 0.0 **without** offset) | yes |
| B speeds | 11.11=11.11 · 11.111 vs 11.11 · 13.89 | same from both files | yes |
| B per-lane length CF vs SUMO | **not in artifact** | max abs diff 10.4 / 27.2 / 27.2 m | — |
| C extras | t:4 / t:16 / none | same; t carry no `tl` (0/4, 0/16) | yes |
| C laneLinks equality | **not in artifact** | equal under index reversal 16/16, 576/576, 576/576 | — |
| D sets / identity | 9/9,1 · 144/144,0 · 256/256,26 | same; gudang ix_1_1 perm `[10,11,9,6,7,8,3,4,5,2,0,1]`; grid A1 16/16, identity 3 | yes |
| D unsorted lists | 0/9, 16/144, 56/256 | same | yes |
| E demand | 2021 aligned · 2983 multiset, aligned 1 · 1473 aligned (zip) | same; `pkw` typed 0/2021, 0/2983; grid4x4 no vType | yes |
| F headers / sha256 | netconvert 1.13.0 ×2 with inputs; netedit 1.9.0 no `<input>` | same; sha prefixes 679acffa… / 5a74ada6… / 8d192de4… | yes |
| FREEZE:230 corpus claim | 577,600 rows / 1,600 eps / no 0 | `datasets_v11/cf_hz1x1*`: 1,600 eps, 577,600 rows, 0 zeros, max 8 | yes |
| FREEZE:226, :247 | 128/192; recipes | 128 (artifact sum); `phase_control.py:25`, `sumo_env.py:33-34` | yes |

### Tests I consider load-bearing
`test_p7_0s_registered_table_recomputes_through_the_adapter`, `…identity_permutation_reproduces_p7_0s_void_table`, `…permutation_is_the_hand_derived_inversion`, `…forced_to_the_identity…`, `test_align_info_rewrites…`, `test_align_info_refuses_rather_than_falling_back[5]`, `…does_not_mutate_its_input`, `…inert_on_every_decision_step_of_a_real_episode` (live SUMO), `…derived_sumo_lane_order_equals_the_live_envs` (live), `test_hz4x4_gudang_resolves…` (128), `test_cologne3…`; audit: `…link_index_permutation_is_derived…`, `…signal_check_has_discriminating_power`, `…frozen_parsers_filter`, `…demand_is_compared_as_a_multiset…`, `…grid4x4_demand…zip`, `…connection…does_not_declare…`, both provenance tests.

### Tests I consider theatre or weaker than their docstring
`test_the_green_actions_release_the_same_lanes…` (sets quoted from the gate, M-5); `test_the_phase_map_refuses_a_phase_outside_the_sumo_program` (−1 only, MINOR-2); `test_the_audit_artifact_issues_no_verdict` (prose search). Nothing tests criterion B, criterion C's extras, the metric filter, or a mismatching demand.

### What I could not verify
- Half-A claims quoted in §"Features": halting threshold "172,800 lane-seconds, 0 disagreements" (FREEZE:248), the A1 teleport numbers — out of half B's scope.
- The grid4x4 column from a clone: it depends on the gitignored, CC BY-NC-SA candidates tree (sha256 recorded); on a fresh clone the three grid4x4 tests skip.
- Byte-identical regeneration of the three JSON artifacts (M-3) — no command exists to run.
- The `.sumocfg` twin under `scenarios/` (half A) — only confirmed it is an added file, not an edit, and that `scenarios/` is outside the frozen set.

**FINDINGS.md:** `/tmp/claude-1000/-home-filip-rltraffic/42cbf76f-5d6e-44ba-b684-496d2fe5804d/scratchpad/review-p71-halfB/FINDINGS.md` (scripts `recompute16.py`, `check_c_d.py`, `check_e.py`, `probe_align.py`, `mutate.sh` alongside).
