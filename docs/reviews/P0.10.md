# Independent review — P0.10 (scoped: `offline/dt_gate.py` erfc numerics)

**Verdict: PASS-WITH-NOTES** — 2 MAJOR, 3 MINOR, 4 NOTE, **no blocker**
**Reviewed at:** `78cd3a9` (`task/p0.10-reproducibility`) · **Written:** 2026-08-17, **before the fix round**

**Oracle input bits, stated first as required:** `x.hex() == "0x1.c2c68c1f67a84p+1"` — byte-identical to
what `offline/dt_gate.py:341` passes, asserted via `Fraction(oracle_input) == Fraction(arg)` **before
any arithmetic ran.** *(This requirement existed because the coordinator's own verification fed two
140/160-digit methods the **exact real** `|z|/√2` instead, got agreement to ~48 figures, and was one
message from reverting a correct fix. See `PROJECT_PLAN` §7, 2026-08-17.)*

---

## 1. The verdict on the numerics: the implementer was right

The reviewer's oracle **shares nothing with either party** — no `decimal` module at all, pure integer
fixed-point **interval** arithmetic with directed rounding, π from **Euler's** `4(atan½+atan⅓)` against
the code's **Machin**, `math.isqrt` instead of `Decimal.sqrt`, and **the enclosure is asserted not to
straddle a rounding boundary, so the result is PROVEN correctly rounded rather than estimated.**

Enclosure width **1.26e-158** → `6.344854575852745511506396552225481771208e-07` → correctly-rounded
double `0x1.54a3046cb4c1dp-21` = **the committed value.** ✅ **The implementer's 18 stated digits and
their verdict on which libm was wrong are confirmed.**
**244 arguments swept, 0 mismatches**, including all **128 distinct committed erfc arguments** and the
subnormal region. **§15.3's soft spot 1 (the +45 guard-digit heuristic) is now checked and holds**, with
~23–28 spare digits at the `|x|→30` boundary.

## 2. MAJOR

### R1 — the landmine is NAME-BASED, and the reviewer bypassed it with the whole suite green
`tests/test_erfc_determinism.py:187` uses `monkeypatch.setattr(math, "erfc", landmine)`, which rebinds
the **module attribute** and **cannot see a reference bound earlier**. A complete revert to platform
libm written as an early binding — exactly what `from math import erfc` produces —

```python
_ERFC = math.erfc
G._normal_cdf = lambda z: 0.5 * _ERFC(-z / math.sqrt(2.0))
G._erfc_deterministic = _ERFC
```

gives **`997 passed, 3 skipped`, landmine intact, p-value path platform-dependent again.**
> **The property *"no platform libm in the p-value path"* is pinned against ONE SPELLING of the
> regression, not against the regression.** This answers §15.1's open question **in the direction the
> implementer feared: the mechanism test is necessary and NOT sufficient.**
**Discriminating power, quantified:** no *value* test can help — on this box **libm reproduces all 322
committed values exactly**, so the value is blind to the revert **by construction**.

### R2 — one test deletion reopens the bug, and CI's second libm is incidental and unpinned
Mutation K fails **exactly one** test; deselecting it gives **`996 passed, 3 skipped, 1 deselected`**.
And `.github/workflows/ci.yml:34` is a single `runs-on: ubuntu-latest` — **no matrix, no container.**
> **The empirical two-libm coverage (dev 2.43 vs runner 2.39) evaporates silently the day GitHub bumps
> the image, and NOTHING FAILS when it does.** A reader would conclude *"cross-machine determinism was
> verified"*, when what is verified is **that a monkeypatchable name is absent on one machine.**

## 3. MINOR and NOTE

- **R3** `_pi_at` (`offline/dt_gate.py:241`) uses `localcontext()`, which **copies the caller's whole
  context**. Its exit `if addend == 0` (`:253`) relies on underflow-to-zero, so under **`ROUND_UP` or
  `ROUND_05UP` it never terminates** — measured with `SIGALRM`, baseline 0.31 s, both **did not
  terminate in 8 s**; five `traps` settings raise instead. `tests/…:194` is titled *"the caller's
  decimal context cannot change the answer"* but varies **only `prec`**. **No caller in this repo sets
  a decimal context, so nothing on `main` is affected** — the test's title over-promises by three axes.
- **R4** `offline/dt_gate.py:309` still calls `math.log` in the precision heuristic — **a libm call in
  the value path**, the same class the change exists to replace. The guard-digit argument holds; a
  libm-free spelling was available and would have made the claim structural.
- **R5** the packet's §1 diffstat (**11 files / 2528**) was measured at `8dd38f8` and claims *"every
  commit after it touches only this file"* — **false**: the real diffstat is **13 files / 3101**, and
  four later commits touched `offline/dt_gate.py` and the test file. **The section whose purpose is
  *"zero frozen files, proved by the diff stat"* omits the critical-path file the packet exists to
  disclose.** §2 lists both, so nothing is substantively hidden.
- **R6** §11's performance figures are **warm-memo only**: cold **0.319 s** at x=3.52 and **0.419 s** at
  x=9 against the reported ~57 µs / ~8 ms — a four-order understatement of first-call cost, and it
  explains the suite's 98.8 → 112.9 s.
- **R7** `test_pi_is_computed_correctly` checks **16 of 65+ digits**, and `_pi_at` is a **shared premise
  between the code and the test's own "independent" oracle** (`:83`). Caught downstream by the
  committed anchors; the reviewer's oracle closes both gaps and confirmed `_pi_at` correct to
  **38 / 64 / 69 / 130 / 309** digits at prec 28 / 55 / 60 / 120 / 300.
- **R8** `DATA_DIR.glob("*.json")` (`:153`) is **non-recursive**; `docs/data/` has three subdirectories.
  Complete today (verified repo-wide), but a future artifact in a subdirectory escapes **without**
  tripping the `== 322` count guard.
- **R9** `offline/transfer_gate.py:1318` uses `** 0.5` (libm `pow`) and the result is committed to
  `docs/data/p7_0_gate.json`. Same defect class at negligible stakes; labelled *"a reading aid, never a
  test"*.

## 4. What reproduced, by independent routes

- **F2 — reachability by EXECUTION, not grep**, as instructed. The **43** non-erfc fields: 45 assertions
  recomputed through the real `hypergeometric_upper_tail` and permutation paths — **45 matched, 0
  mismatched, `_erfc_deterministic` called 0 times, landmine never fired.** The **322**:
  `_erfc_deterministic` called **exactly 322 times**, `math.erfc` **0 times**, **0 p-values moved**.
  *Scope limit stated: leaf functions with recorded inputs, not the full generation pipelines.*
- **F4 — no merged number moves, three routes:** `git diff main...HEAD -- docs/data/` → **0 lines**; all
  322 `(z, p_value)` pairs recomputed → **0 moved**; and the **merged P4.3 sidecar regenerated** —
  **51 leaves, 2 differences, both `runtime.git_commit` provenance hashes, all 21 numeric statistics
  identical.**
- ⭐ **A portability check nobody asked for:** blocked `_decimal`, re-ran on `_pydecimal` (verified
  switched, 30× slower on a control). `_pi_at(55)`, `_pi_at(120)`, 8 erfc arguments, 24 `_normal_cdf`
  values and all 322 pairs → **0 differences.** **The change is robust to the `decimal` BACKEND, not
  only to libm.**

## 5. F3 — which other determinism claims a second machine could falsify

**Criterion offered, and it is the useful part: a test comparing two runs INSIDE ONE PROCESS is immune;
a test comparing a live floating-point computation to a COMMITTED CONSTANT is exposed.**

1. **`offline/rtg_calibration.py:799` `gate_a_result`** — per-episode `==` between a **live DT +
   CityFlow re-run** and committed records. **Highest exposure in the repo**, and it is skipped both
   here and in CI, so it has **never run on a second machine.**
2. **`offline/rtg_calibration.py:1373`** — the guard that fired. Only its erfc step is hardened;
   `paired_comparison`'s numpy reductions remain SIMD-dispatch exposed.
3. **P4.3's byte-identical retrains** (exposed) and the **1-vs-16-thread invariance** (within-process,
   therefore safe).
4. **`tests/test_flow_randomizer.py`** `draw(0)` byte-identity and `episode_sha256` — depend on
   CityFlow's C++ floats; **CityFlow is not built in CI, so never cross-checked.**
5. The committed-anchor re-derivations in `test_p0_baseline_anchors`, `test_rederive_anchors`,
   `test_offline_baselines`.

⚠️ **Count correction to the coordinator's measurement:** the reviewer counts **27** determinism claims
in `PROJECT_PLAN.md` against the coordinator's 39 — different counting (matching lines versus matches).
**Both agree on the substantive point: `docs/CONTRACTS.md` has 0 cross-machine mentions and 0
bit-exactness claims, and the single cross-machine-adjacent sentence in the plan is about TIMING, not
values.** *(`PREREGISTRATION.md` is at the repo root, not under `docs/`.)*

## 6. Theatre found

`tests/test_erfc_determinism.py:130` (`!= RUNNERS_LIBM_VALUE`, implied by line 129 — cannot fail
independently) · `:225` (`values[-1] >= 0.0`, near-vacuous) · `:224` (`approx(2.0)` at rel 1e-6 where
the exact answer is 2.0 to the last bit, admitting a catastrophically wrong series).

## 7. What the reviewer could NOT verify

**The cross-machine property itself — it has one machine.** It verified the *mechanism* and the *value*;
it did not reproduce the glibc-2.39 runner evidence. Also: §15.1's "all 24 tests" figure (the file now
collects 35); the mutation round L/M/M2/N/O/P as executed (**though it independently re-derived and
confirmed the proof behind M**); the guard baselines and packaging/CI portions (out of scope by
instruction); and whether `docs/data` artifacts are reproducible end-to-end.
