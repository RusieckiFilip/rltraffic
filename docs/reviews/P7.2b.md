# MERGE REVIEW — P7.2b (target-domain SUMO probe and A17's cross-domain return-prompt calibration)

**Target:** branch `task/p7.2b-calibration` tip `5912f9e`; code tip `5dfaaa2` (everything after it is docs).
**Artifact under review:** `docs/data/p7_2b_calibration.json`, sha256 `92b1592de637cee187c56b988ce320611d89706c8f9f02c34fe7ebbe81658d86` (run 4).
**Date:** 2026-09-15. **Reviewers:** three independent `contract-reviewer` subagents, one per fragment, each in its own
detached worktree at `5912f9e`, each under PROJECT_PLAN section 7's findings-file rule.

> ⛔ **FENCE NOTICE.** Fragment 2 proved the fence holds by quoting the smoke chunks' fenced quantities. **Those values
> are deliberately absent from this document and must not be quoted in any committed file — not here, not in a packet,
> not in the paper — until P7.3's arm is defined.** They live only in `output/p7_2b/smoke_*.json` and in the reviewers'
> findings files, both gitignored. Publishing them from the document that certifies the fence would defeat Amendment A3
> through a side door. This notice exists because it nearly happened.

## Verdict

| fragment | scope | verdict |
|---|---|---|
| 1 | A16's door (`offline/aligned_env.py`) and the campaign driver | **PASS WITH FINDINGS** — **5 surviving mutants, 4 distinct findings** |
| 2 | the artifact recomputed from raw chunks and checkpoint payloads | **PASS** — no blocking findings |
| 3 | the arithmetic and refusal core of `offline/transfer_calibration.py` | **PASS WITH FINDINGS** — **2 surviving mutants, 2 distinct findings** |

> ⚠️ **CORRECTION, 2026-09-15, on the implementer's challenge — a counting error of the coordinator's, in the document whose purpose is accuracy.** This review's tables mark **SEVEN** rows SURVIVED (five in fragment 1, two in fragment 3), while the verdict table said *4* for fragment 1 and the ruling below said *six*. Both were wrong **as stated**, because both said *mutants* while counting *findings*: the door reviewer treated `reward + 1e-9` and `reward := 0.0` as one gap reported twice, and the coordinator copied its *4* into a table listing 5 rows, then added 4 + 2 for a *six* the tables never supported. **The right statement is 7 surviving mutants and 6 distinct findings.** `BRIEF_36` Amendment F's Definition of Done asked for six re-runs; **the coordinator ran all seven, and all seven are killed.** The implementer reported the discrepancy instead of quietly reconciling it, which is the behaviour this project wants and the reason it was caught at all.

**COORDINATOR RULING: FIX-FIRST, THEN MERGE.** One scoped round, `BRIEF_36` Amendment F. **Every item is test-side
(plus one docstring and two driver-text assertions): no byte of the artifact changes, no campaign is re-run, run 4
stands.** The shipped code is correct on every point three reviewers could falsify; what is weak is the test net, in
four places whose late-fix cost is a wrong number that nothing catches.

## An operational note that reflects on the process, not the task
All three reviewers were killed by a rate limit at 1-3 minutes on their first spawn. Their final messages read
*"I will begin by..."* — but their findings files already held real, load-bearing work, because section 7's rule forces a
checklist first and an append after every finding. **Two of the four door survivors and the whole artifact baseline were
established by agents whose own last words claimed they had not started.** Had the coordinator trusted those messages,
everything would have been re-run from zero. The successors continued from the first unticked item. The partials are
archived in `output/p7_2b_runs/review_partials_1929/`. This is the first time the rule has paid, and it paid in full.

## Fragment 2 — the artifact: PASS
Every number recomputed by the reviewer's own code from the raw chunks and the checkpoint payloads, compared under `==`,
never `allclose`.

| check | result |
|---|---|
| subjects, 7 fields x 2, from the 10 checkpoints (all 5 per tier agree) | `==` True |
| 16 targets via `rule_b_target` / `rule_a_target` / naive, fed with the reviewer's OWN statistics | `==` **16/16** |
| both registered prompts, `-7185.354721543778` and `-7431.0185327454665`, exactly one per subject | `==` True |
| 16 in-support verdicts against the split range AND `[-rtg_scale, -6]` | `==` 16/16; **both bounds agree on every one** |
| Rule A secondary quantiles, two independent routes | `==` 6/6 |
| disjointness, overlaps 0/0/0/0, union 300 | `==` True |
| smoke: 12 published fields against the raw chunks; counts 230 / 269 | `==` True |
| canary block against `canary.json` and the module constants | `==` True; `check_canary` did not raise |
| **regeneration** | 1532 leaf paths each side, 0 one-sided, **exactly one differing leaf, `/git_commit`** — the packet's claim confirmed |

**The fence held**, which was the FAIL condition: `fenced_do_not_report`, `episode_reward`, `rtg_last` and `rtg_series`
occur **0 times** in the committed artifact's bytes, and `att_horizon` occurs only in the 100 MaxPressure probe chunks and
in the canary block — no subject outcome anywhere. **Negative control the reviewer added:** perturbing `local_return` by
`+1.0` makes `check_canary` raise, so the canary check is not toothless.

## Fragment 1 — the door and the driver: PASS WITH FINDINGS

| mutation | result |
|---|---|
| phase map `2k -> k+1` becomes `2k -> k` | KILLED |
| swap two canonical lanes | KILLED |
| `step` returns the raw info | KILLED |
| remove the double-wrap refusal | **SURVIVED** |
| `reward + 1e-9` in the wrapper's `step` | **SURVIVED** |
| **`reward := 0.0` forever** | **SURVIVED** (62 passed) |
| `mkdir -p "$LOGS"` moved above the token check | **SURVIVED** |
| delete `set -euo pipefail` from the driver | **SURVIVED** |

**The finding that matters: a wrapper returning zero rewards forever passes the entire door and campaign suite.** The
delivered code is correct — the reward is forwarded unchanged at `offline/aligned_env.py:114-115` — but nothing pins it.
`tests/test_aligned_env.py:126-127` is the only place in the repo that builds a raw env and an `AlignedEnv` over it, and
it never calls `raw_env.step`; `:137-138` is a literal no-op; the tuple reward is asserted **by type only** at `:166`.

**Why P7.2b's numbers are nevertheless safe, traced by the reviewer rather than assumed:** the door's step tuple feeds
`offline/horizon_metric.py:97-98` into the smoke chunk's fenced `episode_reward`, while the campaign's headline ATT and
ratio path at `offline/transfer_calibration.py:442` **discards the tuple** (`_reward`). The S-ratio, both registered
prompts and every published number are immune. **The exposure is P7.3's**, where DT arms are evaluated on SUMO through
this door and the returns ARE the result.

**And the sharpest finding in the whole review: a test docstring asserts a comparison that does not exist.**
`tests/test_aligned_env.py:109` states *"One 20-step episode on the fenced draw 5, raw and wrapped compared step by
step."* `BRIEF_36` section 4's T1 requires exactly that — `reward`, `step`, `average_travel_time` and every global scalar
byte-identical between raw and wrapped. **None of those four comparisons exists.** This is the project's signature error
(a description standing in for an artifact) located inside a test file, where a future reader is most entitled to
believe it.

## Fragment 3 — the arithmetic and refusal core: PASS WITH FINDINGS

| mutation | result |
|---|---|
| swap the two statistics in the Rule B call | KILLED |
| `in_support_position` boundary `<` becomes `<=` | KILLED |
| `probe_draw_ids` window shifted by one | KILLED (two tests) |
| `chunk_is_reusable` accepts disagreeing routes | KILLED |
| `_validate_chunk` drops the teleport refusal | KILLED |
| add the fenced key to `_SMOKE_PUBLISHED_FIELDS` | KILLED |
| `run_smoke` `explore=False` becomes `explore=True` | KILLED |
| **`_write_json` moved above the fence assertion** | **SURVIVED** |
| **Rule A computes q0.9 while keeping the `q1.0` label** (the reviewer's own) | **SURVIVED** |

**T4's reuse requirement is satisfied, by grep:** `rule_a_target` and `rule_b_target` are imported from
`offline.rtg_calibration` at `offline/transfer_calibration.py:756-763` and called at `:773`, `:799`, `:822`. There is no
local redefinition.

**F1's target — the filesystem-mutation barrier, and this is the third instance of a class `CLAUDE.md` already records
as having appeared twice.** Five tests assert that a refusal leaves no file behind, but every one of them trips a refusal
*above* chunk assembly. **No test ever reaches the fence `AssertionError` at `offline/transfer_calibration.py:1259`** —
confirmed independently by the coordinator: the assertion's message `reached the artifact` appears in **zero** tests. The
shipped order is correct (`_write_json` at `:1264` follows the assertion), but a future reorder of two adjacent
statements publishes a zero-shot number into `docs/data/` with a green suite.

**F3's target — Rule A is arithmetically unpinned.** The only assertion touching it checks the *set of rule names*
(`tests/test_transfer_calibration.py:343`), not a number. This is **not** an equivalent mutant: on the real band,
q1.0 = `-20809.0` against q0.9 = `-22153.5`, a silent 1344.5-unit shift under an unchanged label, and the mutant's output
exactly duplicates the already-published `q0.9` secondary cell. The in-support verdicts happen not to flip — margins
saved it, not tests. Rule A is an ablation in P7.2b, so nothing published depends on it today; it is reported beside the
registered prompt and will be read as a comparison point.

**Defence in depth confirmed, better than the brief assumed:** widening the published-fields allow-list is caught by the
**runtime scan** at `:1259` rather than by the allow-list itself, and the test asserts over serialised bytes.

**Weaker than they look, recorded so no one leans on them:** `test_the_module_contains_no_bare_dt_act_call` is an AST
check on keyword *names* and would not have caught the `explore=True` mutation on its own (the kwargs spy does — which is
why both exist); the nesting assertion at `:384` is shift-invariant; `tests/test_transfer_calibration.py:1192`
(`text.index("canary")`) matches a header comment, and only `:1240`, over comment-free text, is load-bearing.

## What none of the three could verify
`rtg_advanced_every_decision: true` **is not re-derivable from anything published** — the per-decision rewards it is
compared against are stored nowhere, and only the fenced RTG series survives. Fragment 2 corroborated it circumstantially
(the series' shortfall equals exactly one trailing reward, which is what the shift-by-one rule at `:827-866` predicts):
**consistent, not proof.** Nothing published rests on it. P7.3 must log the per-decision reward series so the diagnostic
becomes re-derivable there. Also unverified: engine-level reproducibility of the 100 `local_return` values (regeneration
reused the chunks as given), and any consumer of the door outside `offline/`.

## Notes carried to the packet and the paper
1. **One sentence is owed in the paper.** The probe band 201-300 lies inside the training-corpus POOL band 1-999, while
   1000-1099 is the held-out reserve. It is disjoint from every draw actually trained on — verified 0/0/0/0 — but a
   referee will ask, and the artifact's assertion is not the paper's sentence.
2. `subjects[*].best_source_return` has **no key of that name** in the checkpoint payload; the carrier is
   `ck["target_rtg"]` and the values match. A naming trap for a future reader.
3. `in_support.margin` is stored **signed** (`-10818.0`); "below by 10818.0" is the magnitude.
4. `canary.git_commit` and `probe_chunks_canary.git_commit` legitimately differ; both are real ancestors of the tip and
   the artifact explains it in prose.

## Definition-of-Done items outside the reviewers' scope, checked by the coordinator
`main` is an ancestor of `5912f9e`; no code change after the confirmed code tip `5dfaaa2`; **no frozen file touched on
the whole branch**; the committed artifact is byte-identical to the archived `run4_92b1592d.json`; the English rule is
**clean for P7.2b** — the authoritative repo-wide sweep flags the same four pre-existing files on `main` and on the
branch and **not one P7.2b file** (all four are false positives of the kind the script's own TODO predicts, plus
illustrative quoted Polish in a frozen agent definition). ⚠️ **Separately: `scripts/check_english.sh`'s explicit-path
mode is defective and every per-file `exit 0` claim made through it is worthless** — see the Decisions Log of
2026-09-15 and `docs/patches/`. That defect is repo-wide, not P7.2b's, and does not block this merge.
