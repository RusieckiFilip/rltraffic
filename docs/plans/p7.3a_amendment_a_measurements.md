# P7.3a — A9, A6 and A2, measured before the pre-flight

**Branch:** `task/p7.3a-zero-shot` · **Against:** `BRIEF_37` Amendment A (A2, A6, A9, **A11**) and
Amendment B, both read before this was written.
**Transcript:** `output/p7_3a_runs/a9_a6_a2.txt`, sha256 `ef551b98…`; harnesses `a9_instrument.py`
(`687bf92d…`), `a6_rates.py` (`896768d5…`), `a6_pool_pilot.py` (`7a84d38e…`).

⚠️ **A11 BINDS EVERY RATE BELOW.** The canary read **0.97 s** (plan gate) and **0.90 s** (this
session, before A9) against P7.2b's 0.68–0.89 s. The author has since identified the cause as
**thermal** — blocked underside intake — and A11 rules that until a cooling-pad canary exists, every
rate is *a measurement under thermal constraint*, and every quotation says so. **These do.** A11 also
requires the A6 pilot to be re-run on the pad if the pad's canary differs by more than 10 % from this
pilot's basis (0.90 s); at ≤ 0.81 s that is a 10 %+ difference, so **plan for a re-run.**

---

## A9 — the instrument regenerates P7.1's frozen value: **PASS**

One observed MaxPressure episode, nominal teleport-free parity config
(`…_1h_parity_noteleport.sumocfg`), seed 1000, through the repo's own `run_sumo_arm` — the route
P7.1 used — against the frozen per-episode row.

| quantity | fresh | frozen (P7.1 ep0) | float32 `==` | float64 `==` | abs diff |
|---|---|---|---|---|---|
| `att_env` | 367.5816210045662 | 367.5816210045662 | **True** | **True** | 0.0 |
| `E_sumo` (`att_reference_created_population`) | 475.4507669470559 | 475.4507669470559 | **True** | **True** | 0.0 |
| `n_created` / `n_entered` / `n_never_entered` / `n_teleports` | 2021 / 1752 / 269 / 0 | 2021 / 1752 / 269 / 0 | — | equal | — |

**It reproduces bit-for-bit in float64 — stricter than P7.1's own registered convention**
(`np.float32(fresh) == np.float32(stored)`, detection floor one float32 ulp). **The observer's
change at `7efafa7` altered cost, not value. A9's stop condition is not met; P7.3a proceeds.**

### Three things found while doing it, each a correction to something on record

1. **`e_sumo` is not a key.** `cells.sumo_noteleport__maxpressure` has no field of that name. The
   carrier is `att_reference_created_population` in the `episodes` rows, and
   `att_created_population_mean` in the cell. Confirmed by re-deriving A13(b)'s identity:
   `att_env − E = population + clock_origin + cadence` → `364.15881576412613 + 97.304 = 461.463 =
   att_created_population_mean`. A naming trap for whoever writes the ρ code.
2. **P7.1 never re-verified the teleport-free cells.** `reproduction.per_cell` shows `n = 0`,
   `n_equal = 0`, `all_equal = null` for **all three** `sumo_noteleport__*` cells; the `n_verified:
   45` covers the other nine (9 × 5). So **A9 is the first reproduction these cells have ever had**,
   and the amendment's premise — that they were frozen-and-verified — was half true. Worth a line in
   the packet, because the noteleport regime is the one every P7.3 number uses.
3. **A5's residual is explained, and it is not a code change.** `run_sumo_arm`'s own docstring
   (`offline/sumo_att_reference.py:1195-1202`) records: *"the observer alone costs 14.63 s against
   the frozen env's 12.26 s (+19 %), while the **halting check** takes it to 42.83 s (3.49x) — it
   queries every vehicle's speed every second."* P7.1 ran `halting_episodes=1`, so its ep0 paid the
   check (42.461 s) and ep1–4 did not (13.0–13.5 s); the cell's `seconds_per_episode: 19.34` is the
   **mean of one checked and four unchecked episodes**. My plan-gate harness took
   `halting_check=True` by default on every cell. **The ×3 was the halting check all along.**

### A9b — the halting check is value-neutral, and that is now measured

Same episode with `halting_episodes=0`:

| | halting ON | halting OFF |
|---|---|---|
| `att_env`, `E_sumo`, all four counts | reproduce frozen exactly | **reproduce frozen exactly** |
| seconds | 55.96 | **16.01** |

**Identical values, 3.5× the cost.** The check compares the recorder's halting classification against
SUMO's own (`halting_n_lane_seconds 28800`, `halting_n_disagreeing_lane_seconds 0`,
`halting_max_abs_difference 0`); it answers a question about one threshold constant, and P7.1's own
docstring says it *"does not need every episode: one episode already contributes 28,800
lane-seconds."*

⛔ **I am not deciding this.** Running it on a subset is what P7.1 did and it is worth roughly half
the campaign — but it is an instrument setting on the registered route, so it is **Q6 for the
coordinator**, below.

---

## A6 — the anchor rates and the pool pilot

All on draw 5, fenced (wall time and counts only), observed, **unwrapped** (A2), `halting_check` ON
unless stated.

| arm | n | seconds |
|---|---|---|
| `fixedtime` | 2 | 35.83 / 41.48 — mean **38.65** |
| `random` | 2 | 53.60 / 48.89 — mean **51.24** |
| `maxpressure` (plan gate) | 2 | 39.81 / 49.50 — mean **44.66** |
| DT cell, aligned (plan gate / today) | 3 / 2 | 37.06 / **31.64** |
| DT cell, aligned, **halting OFF** | 2 | 12.59 / 12.35 — mean **12.47** (ratio **2.54×**) |

### The pool pilot — 16 observed DT cells on draw 5, `spawn`, one env per process

**All sixteen cells are draw 5.** A pilot on the held-out pool would *be* the experiment, run before
the token.

| workers | halting | wall (16 cells) | **effective s/cell** | in-process mean | speed-up |
|---|---|---|---|---|---|
| 8 | ON | 109.57 s | 6.848 | 53.97 s | **7.88×** |
| **12** | **ON** | **86.29 s** | **5.393** | 41.07 s | **7.61×** |
| 8 | OFF | 47.21 s | 2.950 | 22.58 s | 7.65× |

**The pool scales far better than the ×4.9 my plan assumed — 7.6–7.9× on 16 cores** — and A6's
instruction is that the campaign runs at whichever measured better: **12 workers**. Decisions per
cell 360 on all 16; teleports asserted 0 in every worker.

### The schedule, corrected twice — and my plan's ≈ 10 h was wrong in the pessimistic direction

| configuration | stage 1 (1,200 cells, Amdt B) | full campaign (4,700) |
|---|---|---|
| 8 workers, halting ON | ≈ 2.3 h | ≈ 8.9 h |
| **12 workers, halting ON** | **≈ 1.8 h** | **≈ 7.0 h** |
| 8 workers, halting OFF | ≈ 1.0 h | ≈ 3.9 h |
| 12 workers, halting OFF *(scaled by the measured 2.32×, not measured directly)* | ≈ 0.8 h | ≈ 3.0 h |

My plan said ≈ 49 h sequential / ≈ 10 h at 8 workers. **Both were too pessimistic**: the ×4.9 came
from *unobserved* episodes and the real pool gives 7.6–7.9×. The brief's original ≈ 4.8 h is closer
to the truth than my correction of it was. Stated plainly because I am the one who produced the
wrong figure.

---

## A2 — anchors unwrapped, and T7b

`tests/test_aligned_env.py::test_an_anchor_policy_cannot_be_driven_through_the_door`, SUMO- and
draw-gated (naming draw 5, per G2). It asserts the fact from two angles and carries its own control:

1. the aligned lane dict is **not** the SUMO lane dict, and **not a superset** of it — so there is
   something to protect;
2. MaxPressure through the wrapper raises `KeyError`, **and the key it raises on is a lane of the
   unwrapped env that did not survive the alignment** — so the failure is the door dropping an
   outgoing lane, not some unrelated breakage;
3. the **control runs first**: the same policy on the same unwrapped env does not raise. Without it
   the test would pass against an env that was simply broken.

**Mutation, in a throwaway detached worktree, removed afterwards:**

| mutant | outcome |
|---|---|
| **T7b-M1** — the door bypassed (`wrapped := make_env(spec)`) | **KILLED** |
| T7b-M2 — the unwrapped control deleted | survives, and *should*: deleting a guard cannot make a test fail. Its value is the false positive it prevents, which no mutation of this test can express. Recorded rather than dressed up as a kill. |

`tests/test_aligned_env.py` — **6 passed**. Hygiene and English exit 0.

**T7b has a second half that cannot be written yet:** the *explicit refusal* in the cell builder
(`offline/transfer_curve.py` §3.5) — "an anchor cell must not be constructed through `AlignedEnv`" —
lands with that module. What is delivered now is the arithmetic that makes the separation necessary.

---

## Open questions

6. **The halting check: on every cell, or on a subset as P7.1 did?** Value-neutral (A9b, bit-for-bit)
   and worth ≈ 4 h of the full campaign / ≈ 1 h of stage 1. P7.1's own precedent is
   `halting_episodes=1` per arm. **A ruling, not my call.** If it stays on, nothing changes except
   the clock.
7. **12 workers confirmed?** A6 says the campaign runs at whichever measured better; 12 beat 8 by
   1.27×. 16 cores, so 12 leaves headroom for the observer's own threads.
8. **A11's re-run.** This pilot's basis is a 0.90 s canary. If the pad's canary is ≤ 0.81 s that is
   > 10 %, so the pilot is re-run on the pad before the token. Assumed, and cheap (≈ 4 min).
9. **A9's finding 2** — the teleport-free cells were never in P7.1's reproduction set. Does the
   coordinator want that recorded as a correction to `p7_1_metric_freeze.json`'s standing, or only
   in this packet?

## What was NOT done

No production code was written for §3.0–3.6; this round is A9, A6, A2 only. `git diff` touches one
test file and this document. The campaign's work directory does not exist and nothing was written
under `output/p7_3a/`.
