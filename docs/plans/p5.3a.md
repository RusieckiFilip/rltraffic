# P5.3a — does the return prompt do anything at all?

**Task id:** `P5.3a` · **Branch:** `task/p5.3a-rtg-probe` · **Brief:** `docs/briefs/BRIEF_28_p5.3a_rtg_probe.md`
**Governing text:** the brief, **AMENDMENT A** (Gate 1) and **AMENDMENT B** (Gate 2).
A supersedes §4.2's `RtgSummary` cross-check (A6), §4.2/§6.4's δ **decision rule** (A7, withdrawn) and
§5's output fence (A2). B overrules this plan's original 5-tier probe (B1), corrects A4's *basis*
(B2), replaces A4's identity route (B3) and makes Gate 0 mandatory (B5).
**Compute:** no training. Forward passes on existing checkpoints, four committed-cell re-rolls (two
before the edit, two after), one short live rollout, and the regeneration of 95 lost flow draws.

---

## 1. What this plan commits to before any number exists

Everything in this section is registered now, on a branch with no results on it.

| # | Registered | Source |
|---|---|---|
| R1 | Headline statistic is **`flip_rate`**; `tvd` is required and reported beside it | brief §9 |
| R2 | The nine grid targets are P4.3's `DECLARED_GRID`, unchanged | brief §9 |
| R3 | Probe cell set: **the same EIGHT tiers as the tables × 5 seeds = 40 cells** — `fixedtime, mappo1000, mappo500, maxpressure, random, mix33, mix50, mix67` | **B1** (overrules this plan's original 5-tier registration) |
| R4 | Probe episode set per cell: **20 streams**, indices `0, 10, 20 … 190` of the tier's declared 200-stream training set in `StreamReturn.key` order | this plan, §6 |
| R5 | `fixedtime` will show **`flip_rate = 0.0` on every intervention**. A non-zero result indicts the **probe**, not the prompt. | brief A8 |
| R6 | `mappo1000`'s `grid_g5` (target `−5762.0`) **is** its own checkpoint target, so its `flip_rate` must be **exactly 0.0** — a live null control inside the campaign, at zero cost | this plan; confirmed B6 |
| R7 | Crosscheck cell: **`mappo1000`, seed 101, held-out draw 1000**, targets `0.0` and `−13000.0` | this plan, §7 |
| R8 | §6.1 cell 1 = **`mappo1000` seed 101** (`output/p4_dt/dt_seed101.pt`); cell 2 = **`random` seed 101** (`output/p4_6/checkpoints/random_dt_seed101.pt`) | brief A4 + §2 below |
| R9 | The artifact emits **no verdict**; the reading is the coordinator's and goes in the packet | brief §4.2 |
| R10 | **Row B is a result, not a table row**, and is reported beside the ramp prediction | **B2(ii)** |

---

## 2. ⭐ B2 — the measurement that decides what the spread axis means

The marginal `RtgSummary.std` is **mostly the within-episode ramp**. Verified independently by me on
2026-08-25 from the checkpoint-embedded `stats["rtg"]["cityflow1x1"]["intersection_1_1"]` at seed 101,
for **all eight** tiers. "ramp" is the standard deviation a *perfectly deterministic* RTG would have —
uniform over `[0, |target|]`, i.e. `|target| / (2·√3)` — divided by `rtg_scale`, which is what
`agent/DTAgent.py:596` feeds the network.

| tier | raw `std` | `rtg_scale` | target | **scaled** | ramp | **measured / ramp** | `count` |
|---|---|---|---|---|---|---|---|
| `mix67` | 13155.3172 | 40123 | −5959 | **0.3279** | 0.0429 | **7.65** | 216000 |
| `mix33` | 13155.3172 | 40223 | −5994 | **0.3271** | 0.0430 | **7.60** | 216000 |
| `mix50` | 13155.3172 | 40223 | −5959 | **0.3271** | 0.0428 | **7.65** | 216000 |
| `random` | 12149.7093 | 40294 | −38369 | **0.3015** | 0.2749 | **1.10** | 144000 |
| `fixedtime` | 9303.5481 | 33225 | −29707 | **0.2800** | 0.2581 | **1.08** | 72000 |
| `maxpressure` | 5837.9472 | 24115 | −13112 | **0.2421** | 0.1570 | **1.54** | 72000 |
| `mappo1000` | 2188.9709 | 9991 | −5762 | **0.2191** | 0.1665 | **1.32** | 72000 |
| `mappo500` | 2297.8980 | 11043 | −6362 | **0.2081** | 0.1663 | **1.25** | 72000 |

**`random` at 0.3015 and `mix50` at 0.3271 are nearly equal marginally and are not the same
quantity.** On the five single-policy tiers 65–93 % of the marginal spread is the deterministic decay
of RTG from target to zero across 360 steps — the same shape in every episode, carrying no
information about *which* episode this is. On the mixtures the ratio is 7.6–7.65, because their
`target_rtg` is an expert-like quantile while `rtg_scale` is set by the random component.

> 🆕 **AND A FINDING B2's TABLE DOES NOT CONTAIN, which I hit while reproducing it: all three mixture
> tiers carry the IDENTICAL committed `RtgSummary` — raw `std` 13155.3172, `count` 216000, at every
> seed.** `_fit_stats` (`offline/dataset.py:702`) runs over the whole loaded dataset, and all three
> mixtures load the **same six directories** (`mappo1000`×5 + `random`). `count = 216000 = 600 × 360`
> is the union of those dirs, **not** the 200-stream mixture. **So the committed `RtgSummary` does not
> describe a mixture tier at all**; the three differ only through `rtg_scale`. This is A6's correction
> arriving a second time and harder: the marginal statistic is not merely ramp-dominated for the
> mixtures, it is computed on the wrong population for them. **Test 26 asserts exactly this**, and the
> artifact names the population of every row.

**Consequences, all binding:**

1. **A4's cell-2 rule was stated on the wrong quantity** — "largest `RtgSummary.std`" selects partly
   on ramp amplitude. **The answer is unchanged: `random` ranks 1st raw and 1st scaled among the four
   tiers with checkpoints under `output/p4_6/checkpoints/`. Cell 2 stands.** Only the basis is corrected.
2. **Row B is promoted to the task's second result** (R10). It is the only quantity here that
   separates ramp from information, and nothing in this project has measured it.
3. **P5.3b is warned in this document:** a "narrow vs wide spread" axis chosen on the marginal
   statistic would pick `random` as its wide endpoint and measure a ramp. **The axis must be built on
   row B.**

---

## 3. ⭐ What §6.1 would FAIL to detect (required by A12)

§6.1 re-rolls two committed `dt` cells through the changed code and asserts bit-exact per-episode
ATT. Stated plainly, here is what a green §6.1 does **not** license anyone to believe.

1. **It exercises one branch of the new field and never the others.** The path it runs is
   `from_checkpoint → load → DTConfig.from_json_obj(<8-key payload>) → "conditioned" → forward`.
   It never executes `to_json_obj` emitting the key, `from_json_obj` on a **9-key** payload,
   `__post_init__`'s rejection branch, the zeroing substitution itself, or
   `DTAgent.__init__(rtg_mode=…)`. Those live or die on §6.2 and the unit tests **only**.
2. **It is blind to any logit change that does not cross a decision boundary.** ATT is a function of
   the action sequence, which is a function of `argmax`. An edit that perturbed every logit and never
   flipped an action would leave ATT bit-identical and this test green. That is the *exact*
   flip-rate-versus-TVD distinction the probe exists to draw, and it applies to the guard as much as
   to the finding.
3. **It is, more weakly, blind to an action change that does not move horizon ATT.** Two different
   action sequences can in principle produce the same scalar. Unlikely at float64; not excluded.
4. **It covers 2 of the 40 committed `dt` cells.** Seeds 202–505 and the tiers `fixedtime`,
   `maxpressure`, `mappo500`, `mix33/50/67` are **not** re-rolled by it.
5. **It says nothing about the non-DT arms** (`bc`, `bc_top10`, `iql`), which never construct a
   `DTConfig`, nor about `SpatialDTAgent` (`SpatialDTConfig` is an independent dataclass,
   `agent/SpatialDTAgent.py:124`). Their protection is the full suite, not this test.
6. **It is a statement about this machine.** Bit-exactness is asserted on this GPU, this torch, this
   numpy. It is evidence about *the edit* only because the same harness reproduces the same column
   **without** the edit — which is Gate 0, and why the two mutations are mandatory.
7. **It cannot address training-time relevance at all.** A prompt can shape weights and then sit
   unused. That is P5.3b (brief §1's table), not this.

---

## 4. Files, in the order they are created

| # | Path | Status | What it is |
|---|---|---|---|
| 1 | `docs/plans/p5.3a.md` | NEW | this file; first commit on the branch |
| 2 | `tests/test_dt_rtg_mode.py` | NEW | §4.1 mechanism + §6.2 arm validity; no simulator, no checkpoints |
| 3 | `tests/test_rtg_ablation.py` | NEW | §6.3 probe correctness + §6.4 tables + artifact hygiene |
| 4 | `tests/test_rtg_mode_identity.py` | NEW | §6.1, the load-bearing re-roll; gated, real checkpoints + CityFlow |
| 5 | `agent/DTAgent.py` | **EDITED** | the `rtg_mode` mechanism. **Not in the frozen set** (`CLAUDE.md` §1); §4.1 authorises the edit |
| 6 | `offline/rtg_ablation.py` | NEW | `probe` · `crosscheck` · `tables` · `report` |
| 7 | `docs/data/p5_3a_rtg_probe.json` | NEW artifact | the one committed artifact |
| 8 | `docs/returns/P5.3a.md` | NEW | Return Packet from `docs/returns/TEMPLATE.md` |

**Written but not committed** (gitignored): `output/p5_3a/` and
`scenarios/draws/cityflow1x1/draw_1005 … draw_1099`.

Tests 2–4 are written **and run red** before file 5 or 6 exists, against signature-only skeletons
raising `NotImplementedError`, so every test reaches the real API surface rather than one shared
`ModuleNotFoundError`.

---

## 5. Gates, in execution order — each one can stop the task

### Gate −1 · the draws are the draws P4.6 evaluated on (A1)

`scenarios/draws/cityflow1x1/` holds **11** directories (`draw_0000…0005`, `draw_1000…1004`); the
other 95 of the held-out pool were deleted with the `p46`/`p47` worktrees.

1. Regenerate `draw_1000…1004` into a **scratch root** (`output/p5_3a/draws_control/`).
2. Assert **byte-identity** of `flow.json` and `cityflow.json` against the five survivors, and
   equality of every `provenance.json` field except `git_commit` / `git_dirty`.
3. **If any differs → STOP, report `BLOCKED`.** Every downstream number would otherwise be measured
   on draws that are not P4.6's, undetectably.
4. Only then materialise the missing **95** into the shared `scenarios/draws/` (A1). **The surviving
   five are never overwritten** — `materialise_draws` keeps an identical draw and refuses a differing
   one.

### Gate 0 · the harness reproduces the committed column BEFORE the edit — **MANDATORY (B5)**

Re-roll cell 1 and cell 2 on **unmodified** `agent/DTAgent.py` and assert bit-exact per-episode ATT
against `p4_6_grid.json`.

> **B5's sharper form, which this plan did not originally make: if Gate 0 FAILS, §6.1 cannot be run
> at all** and the task needs a different instrument — something to discover in minutes, before the
> edit, rather than after. **And it is the reason the two mutations mean anything: without a pre-edit
> reproduction, "the mutation made it fail" and "this environment cannot reproduce the column" are
> the same observation.** This gate has the power to stop the task.

### Gate 1 · checkpoint identity — **both routes (B3)**

A4 said "exactly as P5.2's Gate 1 did"; B3 confirms that route does not exist —
`assert_reused_checkpoint_identity` has exactly one call site, `method_tier_grid.py:2269`, and P5.2
used its own manifest-based `assert_reused_digest`. Ruled: use both, they check different things.

- **(a) Cell 1** — `assert_reused_checkpoint_identity(p4_4_training, p4_gate, baselines_root=output/p4_4/checkpoints, dt_root=output/p4_dt)`, called directly. ⚠️ It verifies the five P4 DT
  checkpoints by **file sha256** (`method_tier_grid.py:1222`), not canonical digest —
  `p4_training.json` never carried one (`:1233-1235`). The canonical digest is *recorded* there, not
  *checked against a committed value*.
- **(b) Cell 2** — `canonical_digest_of` against `p4_6_training.json`'s committed
  `0b7c601ebf50efd873993735b808590b6ca9125741d544872fcc9f31bdda1d8f`, **plus** a
  `SHA256SUMS_p4_6.txt` check **at consumption**, in `assert_reused_digest`'s spirit: *a digest
  checked once is not a digest checked when used* (`BRIEF_27` B3(a)).
- **(c) Cell 1 cannot have (b)** — see §5.1 below.

#### 5.1 🚨 B4 — `output/p4_dt/` is in no integrity manifest, and it holds cell 1

Verified by me across all eight manifests, 2026-08-25: **`grep -c p4_dt` over
`output/SHA256SUMS_*.txt` returns `0`; so does `p4_probe`.** Coverage is `p5_2` 221 · `p4_6` 125 ·
`p4_7` 112 · `p5_1` 48 · `p4_4_p4_5` 44 · `p7_0` 36 · `p8_3` 18 · `p4_3` 10, and all ten `p4_3`
entries are `p4_3/eval_dt_g*.json` / `eval_dt_rule_a_k100.json` — **no checkpoints**.

`output/p4_dt/dt_seed*.pt` are the five models behind the pre-registered P4 gate, the models P4.3
swept across 13,000 RTG units, the reused `mappo1000` DT column in P4.6 and P4.7, and §6.1's cell 1.
They are gitignored, un-backed-up **and unmanifested**.
**Not P5.3a's to fix** — writing an integrity manifest for another task's merged output is outside
the fence; `DEFERRED` 56 records it. **The packet states the gap**: cell 1's identity rests on a file
sha256 (`DEFERRED` 29: filename-dependent) with no manifest behind it, so **the two cells are not
equally protected** and the packet must not let them read as if they were.

### Gate 2 · §6.1 after the edit, plus both mutations

Both cells reproduce bit-exactly. Then, separately and with the failure output pasted **in full**:

1. force `rtg_mode="zero"` unconditionally → §6.1 **must fail**;
2. make `from_json_obj` default to `"zero"` → §6.1 **must fail**.

Both must fail **on cell 2** (`random`). The packet reports whether they also failed on cell 1.
**If either survives on cell 2 → `BLOCKED`, and the test is not weakened.**

> A4's reason for two cells: `dt@fixedtime`'s δ is `−5.68e-16` and P5.2's reviewer found the grid4x4
> DT reproducing the fixed-time controller with 0 of 5760 actions differing. On such a cell, forcing
> `"zero"` could change nothing and mutation 1 would "pass" by surviving.

---

## 6. `agent/DTAgent.py` — the mechanism (§4.1, A3, A9)

- `DTConfig` gains `rtg_mode: str = "conditioned"`, values `{"conditioned", "zero"}`.
- `__post_init__` refuses anything else, **naming both** legal values.
- `to_json_obj()` **always** emits it.
- `from_json_obj()` **defaults it to `"conditioned"` when the key is absent** — so all 225 existing
  checkpoints load unchanged. **Its docstring states the asymmetry with
  `SpatialDTConfig.from_json_obj`, which hard-raises on an absent `spatial_mixing`
  (`agent/SpatialDTAgent.py:174-176`), names the 225 checkpoints, and says why the two are opposite**
  (A9): `SpatialDTConfig` was born with its flag and has no checkpoints predating it.
- `DecisionTransformer.forward` zeroes the RTG **after** the existing shape validation, so an
  ill-shaped `rtg` still raises before the substitution can hide it.
- `DTAgent.__init__` gains `rtg_mode: str = "conditioned"`, reaching `_config_template`.
- `DTAgent.from_checkpoint` passes `rtg_mode=config.rtg_mode` (A3). ⚠️ **The packet must say that this
  line's round-trip test passes with and without it** — `from_checkpoint` builds the model twice
  (`:465-466`/`:486`, then `:801`) and the second build replaces the first. It is a **regression
  guard, not a discriminating test**, and its docstring says so.

**Not touched:** `TOKENS_PER_STEP`, the 3-token interleaving, the state-token index at `:364`.

---

## 7. `offline/rtg_ablation.py` — `probe`

**Teacher-forced replay.** Logged states and logged actions are pushed through the model, so the
state sequence is **identical across interventions by construction** and any output difference is
attributable to the RTG alone.

- Windows are built with **`DTAgent`'s own `_window`** and a `_Context` advanced from the logged
  stream — the production inference layout (current action slot `PAD_ACTION`), not the loader's
  training layout. Normalisation goes through `DTAgent._normalise_state`, i.e. the checkpoint's own
  stats. Nothing is reimplemented.
- Baseline RTG is the inference-time construction: `target − cumulative logged reward`, then
  `/ rtg_scale` (`agent/DTAgent.py:596,662`), using the checkpoint's **own** recorded `target_rtg`.
- `avail_mask` is applied exactly as the model applies it, from the logged `avail` stream.

**Interventions — 12 configurations, fixed here, before any number:**

| key | intervention |
|---|---|
| `baseline` | the checkpoint's own `target_rtg` |
| `grid_g0 … grid_g8` | target replaced by each of `DECLARED_GRID`'s nine values |
| `zero` | RTG identically `0.0` in scaled units |
| `frozen` | RTG held at the target, never decremented |

⚠️ **`zero` is not `grid_g0`.** `grid_g0` sets the *target* to 0 and still decrements, giving
`−cumsum(r)`; `zero` feeds a constant 0. Confusing them would report the wrong quantity under the
right name.

**Reported per intervention, per (tier, seed):** `flip_rate` (headline), `tvd`,
`mean_abs_logit_delta` over legal actions, and `n_steps_compared`. **Never a bare pooled mean** —
per seed, plus the spread across the five (P5.1's lesson).

**Cell set (R3, B1):** **8 tiers × 5 seeds = 40 cells**; 20 streams per cell at indices `0,10,…,190`
of the declared 200-stream training set in `key` order. That stratifies across the five
behaviour-seed directories for the `mappo*` and mixture tiers instead of taking one directory's first
20. 20 × 360 = **7200 decision steps per cell**, so flip-rate resolution is 1/7200 ≈ 0.014 %.
Mixture training sets come from `training_streams(..., component_streams=…)` built from the public
`training_streams` of `mappo1000` and `random`; datasets are cached by directory tuple, so the three
mixtures load their six shared directories once.
**Device: the default resolution** (`Utils.resolve_device(None)` → CUDA here), matching
`method_tier_grid`'s `--device` default, so the probe measures the numerics the reported column was
produced with.

---

## 8. `crosscheck` — the second route, required

The probe measures sensitivity on **behaviour-policy states**, which is *not* the distribution the DT
visits when it drives. **The artifact states that limitation in words**, then bounds it:
`mappo1000`, seed 101, held-out draw 1000, two live `horizon_rollout` runs at targets `0.0` and
`−13000.0` (`DECLARED_GRID`'s endpoints), comparing the **full action sequences** and the two ATTs.
Actions are captured by wrapping the `choose_action` callable; `horizon_rollout` is not modified.

---

## 9. `tables` — Gate 0's two tables

### Table 1 — return / RTG spread, all **8** hz1x1 tiers

Three rows, and **A6 is why they are three and not two**: §4.2 asked for agreement between two
quantities that are not the same quantity.

| row | quantity | population | estimator |
|---|---|---|---|
| **A** | per-episode return: `mean, sd, IQR, min, max` | the tier's **declared 200-stream training set** (`training_streams`) | — |
| **B** ⭐ | **between-episode sd of the RTG at `t ∈ {0, 90, 180, 270}`, and pooled**, reported **beside the ramp prediction of §2** | the tier's **declared 200-stream training set** | — |
| **C** | `RtgSummary` `count/min/max/mean/std/quantiles`, **two routes** | the tier's **whole training split over `TierSpec.dirs`** — the population the loader fitted on | **`ddof=0`**, quantiles `RTG_QUANTILES`, `method="linear"` |

**Row C is the only cross-check.** Route 1 reads the committed `NormalizationStats` out of the
checkpoint payload (A5: **there is no other committed copy**, and the artifact says so). Route 2
recomputes it from the raw `.npz` reward arrays over **the same population and the same estimator**.
Rows A and B are separate rows and are **not** cross-checks of anything — asserting A against C would
fail for a correct implementation on `random` and on the mixtures.
**Row B is the quantity C4's hypothesis is about** and, per B2(ii), the task's second result: the
model sees an RTG at *every* step, so identifiability is about how much that value varies across
episodes at a given point in the episode. Every row is emitted in **raw and scaled** (`/ rtg_scale`)
units, because scaled is what the network receives.

### Table 2 — δ per tier: **measured, not a rule** (A7)

The δ **decision rule is withdrawn**. δ survives as a table and nothing more. Emitted per tier:
signed `mean_difference`, `ci95_low`, `ci95_high`, `ci95_half_width`, `wilcoxon`, `rank_biserial`,
`wins`/`losses`/`ties`, `n_shared_draws`, and the derived
**`behaviour_margin_degenerate = (ci95_low <= 0 <= ci95_high)`**.

Read from `behaviour_comparisons` in the committed `p4_6_grid.json` / `p4_7_grid.json`. Free checks,
measured by me 2026-08-25 and asserted by the tests:

- `mappo1000` δ is `−0.6262756469347375`; **`round(abs(δ), 4) == 0.6263`** (A6's registered value —
  A7 leaves it untouched; the sign is stored negative and that is stated);
- **exactly 2 of 8 tiers are `behaviour_margin_degenerate`** — `fixedtime` and `maxpressure`;
- the five tiers present in both grids carry **bit-identical** δ in each, so reading either is safe
  and the tests read both;
- a tier absent from **both** grids raises, **naming the tier**.

---

## 10. `report` — one artifact

`docs/data/p5_3a_rtg_probe.json`: probe results, both tables, the crosscheck, the limitation
sentence, and a `runtime` block with the **measurement-commit / written-at-commit split**
(`runtime_provenance` + `measurement_commits`, `DEFERRED` 39). **No verdict anywhere** — guarded by
`assert_no_verdicts` (`method_tier_grid.py:1838`), and that guard is itself tested.

---

## 11. Test list (one line each)

**`tests/test_dt_rtg_mode.py`**
1. `DTConfig` defaults `rtg_mode` to `"conditioned"`.
2. An illegal `rtg_mode` raises, and the message names **both** legal values.
3. `to_json_obj()` always emits `rtg_mode`; the payload has **9** keys.
4. `from_json_obj()` on an **8-key** payload (no `rtg_mode`) yields `"conditioned"` — the 225-checkpoint case.
5. `from_json_obj()` on a **9-key** payload round-trips `"zero"`.
6. An ill-shaped `rtg` still raises under `rtg_mode="zero"` — validation precedes substitution.
7. ⭐ §6.2a: a `"zero"` model gives **bit-identical** logits under two very different `target_rtg`.
8. ⭐ §6.2b **positive control**: a randomly initialised `"conditioned"` model gives **different** logits under the same two targets — without this, `flip_rate = 0` is uninterpretable.
9. `DTAgent.__init__(rtg_mode=…)` reaches `_config_template` and survives `_build_model`.
10. `save`/`load` round-trips `rtg_mode="zero"` and rebuilds a zeroing model (A3's **regression guard**; it passes with and without `from_checkpoint`'s new argument, and the docstring says so).
11. A `"zero"` model and a `"conditioned"` model fed a constant-zero RTG produce **identical** logits — the ablation is an information ablation with the architecture held exactly fixed.

**`tests/test_rtg_ablation.py`**
12. The baseline RTG sequence reproduces `DTAgent`'s own, recomputed by `np.cumsum` on the logged rewards — an independent route, exact equality.
13. `flip_rate` on an intervention identical to baseline is **exactly `0.0`**.
14. `flip_rate` on a fixture with a known number of differing steps is exactly `k/n`, with `k=3`, `n=7` so that `ceil`, `floor` and `n/2` all disagree (P5.2 **MN-5**).
15. `tvd` lies in `[0, 1]`, is `0.0` for identical distributions and **exactly `1.0`** for disjoint point masses.
16. `avail_mask` is applied: a fixture where masking changes the `argmax` changes the flip rate.
17. `DEFERRED` 21: every mask in this corpus is all-True, so masking must **not** change the probe's output on a real stream — asserted, not assumed by the code.
18. `frozen` differs from `baseline` when rewards are non-zero.
19. `frozen` **equals** `baseline` when every reward is `0` — the exact condition under which the documented hazard (`agent/DTAgent.py:560,628`) is harmless.
20. `zero` and `grid_g0` are **different** interventions on a non-zero-reward fixture.
21. Table 2: `mappo1000` δ is the committed float and `round(abs(δ),4) == 0.6263`.
22. Table 2: exactly **2 of 8** tiers are `behaviour_margin_degenerate`, and they are `fixedtime` and `maxpressure`.
23. Table 2: the five shared tiers carry bit-identical δ in `p4_6_grid.json` and `p4_7_grid.json`.
24. Table 2: a tier absent from both grids **raises, naming the tier** — no silent empty row.
25. Table 1 row C: the two routes to `RtgSummary` agree **exactly**, on the same population and estimator.
26. Table 1 row C: recomputing on the **wrong** population disagrees — the 200-stream subsample on `random`, and the mixture's own 200 streams against the 216000-value union the committed stats actually describe (§2's finding). This is what proves A6's correction is real rather than cosmetic.
27. `assert_no_verdicts` rejects a payload carrying a verdict, and the assembled artifact passes it.
28. The report refuses an undeclared intervention key — the grid may not grow after the fact.

**`tests/test_rtg_mode_identity.py`** (gated on the corpus/checkpoint env var; **it must actually run here and its real output goes in the packet**)
29. ⭐ **§6.1 cell 1** — `mappo1000` seed 101: identity via route (a), then **bit-exact** per-episode ATT on all 100 held-out draws. `==`, never `np.allclose`.
30. ⭐ **§6.1 cell 2** — `random` seed 101: identity via route (b), canonical digest **and** `SHA256SUMS_p4_6.txt` at consumption, then bit-exact per-episode ATT on all 100 draws.
31. The five surviving draws reproduce byte-identically from the generator (Gate −1, asserted as a test rather than only as a script).

---

## 12. Scope fence — what I am explicitly NOT doing

Copied from brief §3, as corrected by A2, A11, B1 and B7.

1. ⛔ **No training of any model.** No `train_dt`, no new checkpoints. P5.3b trains.
2. ⛔ **No `rtg_shuffled`, no `rtg_noise`, no second ablation mode.** One mode: `"zero"`.
3. ⛔ **No touching `TOKENS_PER_STEP` or the 3-token interleaving.**
4. ⛔ **No editing `offline/method_tier_grid.py`**, and in particular nothing added to `METHODS`.
5. ⛔ **No context-length K.** That is P5.3c.
6. ⛔ **No re-running or regenerating any P4.6 / P4.7 artifact.** They are read.
7. ⛔ **No SUMO, no cross-backend work.**
8. ⛔ **Not fixing `DEFERRED` 43's `match=` sites or P5.2's open minors MN-1/3/4/5.**
9. ⛔ **No δ decision rule** (A7). δ is a measured table; P5.3b's rule is not registered here.
10. ⛔ **Never write into, and never delete from**, `output/p4_3`, `output/p4_dt`, `output/p4_probe`,
    `output/p4_4`, `output/p4_5`, `output/p4_6`, `output/p4_7`, `output/p5_1`, `output/p5_2`,
    `output/p7_0`, `output/p8_3`, `output/checkpoints*` (A2's named list, which replaces the
    overbroad `output/p5_*` glob). **`output/p5_3a/` is created and is mine.**
11. ⛔ **No frozen file is touched.** `agent/DTAgent.py` is **not** frozen (`CLAUDE.md` §1) and §4.1
    authorises this edit; that authorisation is quoted in the Return Packet.
12. ⛔ **No integrity manifest is written for `output/p4_dt/`** (B4). The gap is stated in the packet
    and recorded as `DEFERRED` 56; writing another task's manifest is outside the fence.
13. ⛔ **Not touching CI, and not pre-bumping its ceiling.** **CI is green at `7ee606a`** — verified
    by me with `gh run list` (run `32846081637`, `success`), not from a commit message. The skip
    ceiling is **104** and P5.3a is expected to breach it: `re_measure_required_at.event` names "merge
    of P5.3 … or any task adding corpus-gated tests", and test 29/30/31 are exactly that. The
    protocol is `what_to_do`: **merge, let the gate go red, classify `junit.xml`'s `<skipped>`
    messages from `ci-artifacts-<leg>`, then commit the observed value with its breakdown. Do NOT
    pre-bump and do NOT widen with slack.** That is the coordinator's step, not mine.
14. ⛔ **The probe adds no tier beyond the eight** and no intervention beyond the twelve.

---

## 13. Verification obligations

- **Every mutation's failure output is pasted in full**, never summarised as a count (P5.2 **MN-6**:
  3 of 22 pasted).
- `scripts/check_test_hygiene.sh` with **no arguments** must still report **16** findings and **none
  in a file I created**. Baseline measured on this branch's parent, 2026-08-25: `exit=1`, 16, all
  `TH006`, in `test_cityflow_env_phase_constraints.py` (8), `test_sumo_env_phase_constraints.py` (5),
  `test_phase_control.py` (2), `test_base_traffic_env.py` (1).
- `bash scripts/check_english.sh` on every file created or edited.
- `output/SHA256SUMS_p4_3.txt`, `SHA256SUMS_p4_6.txt`, `SHA256SUMS_p4_7.txt` re-verified **after** the
  run, from `output/` itself, with the counts pasted. Expected `10/10`, `125/125`, `112/112`.
- Timing **measured**, per (tier, seed) for the probe and per cell for the re-rolls (Amendment R2
  forbids armchair estimates). No figure enters the packet that I did not produce by running a
  command.
- Whether the suite run was thread-pinned is reported (`DEFERRED` 41).
- `git diff --stat main...HEAD` shows **zero** frozen-file modifications.
- **The packet states the B4 gap** — cell 1 and cell 2 are not equally protected.

---

## 14. Questions asked at Gate 2, and how they were answered

1. **Probe tier set** — I registered 5 tiers and flagged it as my choice. **Overruled by B1: eight
   tiers × 5 seeds.** The reason is B2: `flip_rate` is only interpretable against row B, and a
   correlation over five points where the mixtures are the only structurally different case is a
   gesture rather than a measurement.
2. **"Exactly as P5.2's Gate 1 did"** — that route does not exist (B3). Both identity routes are now
   used, and B4 records that cell 1 cannot have the manifest one.
3. **Gate 0** — approved and **made mandatory** (B5), with the power to stop the task.
