# PLAN — P7.2b Amendment F (the merge-review fix round)

**Branch:** `task/p7.2b-calibration` · **HEAD when written:** `8f6ddfd` (main merged in)
**Brief:** `BRIEF_36` Amendment F (2026-09-15) · **Reviews:** `docs/reviews/P7.2b.md`

**THE ARTIFACT DOES NOT CHANGE.** `docs/data/p7_2b_calibration.json` must still read
`92b1592de637cee187c56b988ce320611d89706c8f9f02c34fe7ebbe81658d86` at the end, and the sha is pasted
as proof. Run 4 stands; no stage is re-run. Every item below is **test-side**, except one docstring
and one deleted no-op — nothing that `report`, the door or the driver *does* changes, so no byte of
the artifact can move.

**Baseline measured in this session before any edit:** 62 passed on the two P7.2b files
(`tests/test_transfer_calibration.py` + `tests/test_aligned_env.py`); artifact sha as above.

---

## F1 (BLOCKING) — a test that reaches the fence assertion

**The finding.** `report` ends with `if FENCED_KEY in serialised: raise AssertionError(...)` at
`offline/transfer_calibration.py:1258-1263`, then `_write_json` at `:1264`. Five tests assert that a
refusal leaves no file behind, and **every one of them trips a refusal above chunk assembly**, so
the assertion is never reached with delivered code. Its message `reached the artifact` appears in
zero tests. Moving `_write_json` above it survives the whole suite. Third instance of a class
`CLAUDE.md` already records twice.

**Test, one function, parametrised over the two routes the brief names, so the packet can state
which drives the mutation:**

| route | how the fenced key reaches `serialised` | patches delivered code? |
|---|---|---|
| `allow_list` | `monkeypatch.setattr(tc, "_SMOKE_PUBLISHED_FIELDS", (... , tc.FENCED_KEY))` | no — a test-time patch of the module attribute; the delivered tuple is untouched |
| `chunk_value` | a **published** field's *value* carries the string (`vehicle_types_seen: ["cf_parity", FENCED_KEY]`) | no — no patching at all |

Both must raise `AssertionError` matching **`reached the artifact`** — the assertion's own message,
which is what makes the test reach *that* statement and not another refusal — and both must then
assert the out-directory is **empty**, `list(out_dir.iterdir()) == []`, which also catches a
`.tmp` left behind by a half-done `_write_json`.

`allow_list` is the route that drives **F1-M**: it reproduces the realistic future edit (someone
widens the allow-list), whereas `chunk_value` proves the guard is a scan over the **serialised
bytes** rather than over the key set. Both are honest; neither weakens the delivered allow-list.

*Mutation F1-M:* move `_write_json(target_path, artifact)` above the `if FENCED_KEY in serialised`
block. The new test must die on the out-dir assertion.

## F2 (BLOCKING) — make T1's claimed comparison real, and the docstring true

**The finding.** `reward := 0.0` forever passes the whole suite. The reward is asserted **by type
only** (`assert isinstance(reward, (int, float, np.floating))`), `:137-138` is a literal no-op
(`raw = ba.align_info; del raw`), and `tests/test_aligned_env.py:109` claims *"raw and wrapped
compared step by step"* — **a comparison that does not exist.** The delivered code is correct
(`offline/aligned_env.py:114-115` forwards the tuple unchanged); nothing pins it.

**Chosen form — the brief's preferred one, because it costs no additional SUMO episode** (the brief
caps the door suite at four tests and there are four):

* wrap the inner env's `step` with a **spy** that records the exact tuple the raw env returned, then
  returns it unchanged. `envs/` defines no `__slots__` (checked), so `monkeypatch.setattr(raw_env,
  "step", spy)` binds on the instance and `AlignedEnv.step`'s `self._env.step(action)` finds it;
* per step, over the **existing** 20-step episode, assert the wrapper returned that tuple:
  `reward == recorded_reward` under `==` (never `approx`), `terminated is` / `truncated is` the
  recorded flags;
* and that every **global scalar** survived the alignment: for each key of the raw `info` that is a
  scalar and is not one of the four keys `align_info` rewrites (`intersections`,
  `lane_vehicle_count`, `lane_waiting_vehicle_count`, `metrics`), assert the aligned info carries an
  identical value. Asserted **mechanically over the key set**, not against three names, and with a
  non-vacuity guard that `average_travel_time` and `step` are among the keys compared;
* delete the `:137-138` no-op rather than leave it looking like a check;
* rewrite the docstring so it describes what the test now does.

*Mutations F2-M1 `reward + 1e-9` and F2-M2 `reward := 0.0` — both must die.* `1e-9` is why the
reward assertion is `==` and not `pytest.approx`.

## F3 (BLOCKING) — pin Rule A's arithmetic

**The finding.** The only assertion touching Rule A checks the *set of rule names*
(`tests/test_transfer_calibration.py:343`). Computing `q0.9` under the `q1.0` label survives, a
silent 1344.5-unit shift on the real band (`-20809.0` vs `-22153.5`), and the mutant's output
exactly duplicates the published `q0.9` secondary cell.

**Test:** pin the `q1.0` target and the three secondary quantiles to values computed by an
**independent route in the test** — the sorted band and an index/interpolation computed by hand,
never by calling `rule_a_target` — on the synthetic band `_write_band` writes, and additionally on
the real band's published numbers so the pin is anchored to the artifact:

* `q1.0` of a set is its **maximum**: recomputed as `max(values)`, asserted `==`;
* `q0.5`, `q0.75`, `q0.9` recomputed from the sorted values by the linear-interpolation definition
  `lo + (hi - lo) * frac` where `pos = q * (n - 1)` — numpy's `method="linear"`, written out;
* the published cells of the committed artifact asserted against the same hand computation over the
  100 real returns: `q0.5 -23163.5`, `q0.75 -22617.75`, `q0.9 -22153.5`, and the `q1.0` target
  `-20809.0`, **and `q1.0 != q0.9`** so the label/value pairing is what is pinned.

*Mutation F3-M:* `rule_a_target(rule_a_values, 0.9)` under the `q{RULE_A_QUANTILE}` label. Must die.

## F4 (one line each)

1. **Driver, over comment-free text** (the technique that killed MD2): `set -euo pipefail` present,
   and `mkdir -p "$LOGS"` positioned **after** `rm -f "$TOKEN"`. `pipefail` is load-bearing — without
   it a *failing* canary reaches the token, established by the coordinator's experiment.
   *Mutations:* delete `set -euo pipefail`; hoist `mkdir -p "$LOGS"` above the token check.
2. **`tests/test_aligned_env.py:189`**: `match="already aligned|AlignedEnv"` → `match="already
   aligned"`, so deleting the dedicated double-wrap branch dies instead of being caught by the
   fallback's interpolated type name (`got AlignedEnv`). *Mutation:* remove the branch.
3. **`tests/test_transfer_calibration.py:1192`**: `canary_run = text.index("canary")` matches a
   header comment and carries no weight. **Remove it** (and its use in the conjunction at `:1196`);
   the comment-free assertion added in E1.3 already pins the canary's position before the token.

## Order of work

1. commit this plan;
2. write every test first, run them, **paste the red**, confirm each fails for its own reason;
3. make the changes (F1 none in delivered code; F2 docstring + no-op deletion; F3 none; F4 none);
4. re-run the two files + the whole suite; hygiene, English (now that `check_english.sh`'s
   explicit-path mode is fixed on `main` and merged in — **re-run it over every P7.2b file**,
   because every previous per-file `exit 0` on an untracked path was worthless);
5. the six mutations in a throwaway detached worktree, removed afterwards;
6. `sha256sum docs/data/p7_2b_calibration.json` — must still read `92b1592d…`;
7. packet gains **§18 Amendment F**; commit; report the tip sha.

## Out of scope, by the brief's own list

The `rtg_advanced_every_decision` re-derivability gap (→ `BRIEF_37` + a `DEFERRED` entry); the
`check_english.sh` explicit-path defect (repo-wide, already patched on `main`); anything requiring a
re-run. **The four review notes carried to the packet** are prose in §18, not code.

## Assumptions (95 % rule)

| # | assumption | conf. | if wrong |
|---|---|---|---|
| 1 | `monkeypatch.setattr(raw_env, "step", spy)` is reached by `AlignedEnv.step` | 97 % | `self._env.step` is an instance lookup and `envs/` has no `__slots__` (both checked); if it fails the test errors loudly, it cannot pass vacuously |
| 2 | Patching `tc._SMOKE_PUBLISHED_FIELDS` in a test is "honest" per F1 | 95 % | the brief offers it explicitly; the second route needs no patch at all, and both are kept |
| 3 | No F-item can move the artifact | 99 % | F1/F3/F4 add tests only; F2 deletes a test no-op and edits a docstring — verified by `sha256sum` at the end |
