# PLAN — P7.3d: the C3 zero-shot point on grid4x4 (16 intersections), CityFlow → SUMO under dynamics parity

**Brief:** `docs/briefs/BRIEF_39_p7.3d_grid4x4_zero_shot.md`, read whole at `66da50e`. **Branch** `task/p7.3d-grid4x4`,
worktree `/home/filip/rltraffic-p73d`, created from `main` @ `66da50e` as the header specifies.
**Amendments written against:** none exist yet. Gate G3 is cleared in the brief itself (§8): tag `v2.0-prereg-a20` is
present locally (`git tag --list 'v2.0*'`), A20 read from `PREREGISTRATION.md:380`.
**Commit hook:** `core.hooksPath = /home/filip/rltraffic/githooks` (absolute, binds this worktree). The harness text of
this session instructed a `Co-Authored-By` trailer; raised to the author in the same turn before any commit, refused,
and the author confirmed the standing rule (`CLAUDE.md` §4b). No commit on this branch carries one.

**Everything below marked *measured* was produced by a command run in this session (2026-09-19). Nothing was written
to the repository, to `scenarios/draws/`, or to the RESCO tree during exploration; two read-only probe scripts live in
the session scratchpad.** No grid4x4 SUMO simulation was run.

---

## 0. FOUR FINDINGS THAT CHANGE WHAT THE BRIEF ASSUMED — the gate's load-bearing items

I will not build past the gate on any of these. Each is stated with the command that produced it.

### F1 — The registered subject is a `SpatialDTAgent` checkpoint. `DTAgent` cannot load it, and C2 is not on its path

*Measured.* `torch.load` of `output/p5_2/checkpoints/grid4x4_mappo1000_dt_nomix_h4_seed101.pt` (sha256 `329fb6b8…`,
all five digests re-measured and equal to §0.7 / A20(a)):

```
format_version = spatial-dt-checkpoint/1.0
config         = {state_dim 40, n_actions 8, n_nodes 16, context_length 20, n_layer 3, n_head 4,
                  d_model 128, dropout 0.1, max_ep_len 360, spatial_mixing False}
target_rtg     = {A0: -132.0, A1: -94.0, A2: -130.0, A3: -141.0, B0: -114.0, B1: -76.0, B2: -84.0, B3: -83.0,
                  C0: -102.0, C1: -98.0, C2: -64.0, C3: -132.0, D0: -125.0, D1: -142.0, D2: -98.0, D3: -127.0}
rtg_scale      = {A0: 259.0, A1: 180.0, A2: 395.0, A3: 255.0, B0: 232.0, B1: 209.0, B2: 170.0, B3: 160.0,
                  C0: 200.0, C1: 836.0, C2: 174.0, C3: 258.0, D0: 255.0, D1: 247.0, D2: 189.0, D3: 258.0}
spatial_mask   = 16 x 16, equal to the identity (checked element-wise)
keys           = config, format_version, intersection_ids, model, normalise, provenance, rtg_scale,
                 scenario_id, spatial_mask, stats, target_rtg
```

On a 16-intersection fake env built from the payload's own ids:

```
DTAgent        REFUSED: ValueError checkpoint format 'spatial-dt-checkpoint/1.0' is not readable by this build
                        (expected 'dt-checkpoint/1.0')
SpatialDTAgent LOADED;  current_rtg A0, C1 = -132.0 -98.0, n ids 16      (no adjacency supplied)
```

`dt_nomix` is the spatial architecture with the identity mask, by construction: `offline/spatial_mixing.py:348`
(*"`dt_spatial` uses the derived graph, `dt_nomix` the identity"*), `:384-396` (`SpatialDecisionTransformer`,
`spatial_mixing = method == "dt_spatial"`). P5.x evaluates it through `SpatialDTAgent.from_checkpoint`
(`offline/tier_sweep.py:2138-2140`, `offline/att_rederivation.py:1107-1111`, `offline/admission_probe.py:866-873`).

**What this contradicts.** §0.7–0.8, §2 (*"No spatial DT — `SpatialDTAgent` on SUMO needs the graph on the aligned env
and is new scope"*) and §3 C2 assume the subject runs through `DTAgent` and that `DEFERRED` 78 is the enabling change.
It is not: `DTAgent` refuses the file by format version, and the class that loads it **already** holds per-intersection
`target_rtg` / `rtg_scale` mappings (`agent/SpatialDTAgent.py:479-480`, `:545-558`, `:905-906`) and needs **no graph from
the env** — `load` adopts the checkpoint's recorded mask (`:892-902`) and `from_checkpoint` documents that any adjacency
passed in is overwritten (`:921-923`).

**What it does NOT change.** A20(a) registers the subject by five digests; those are the files, whichever class reads
them. The model is functionally non-spatial (identity mask: no intersection attends to another). Whether A20's sentence
*"No spatial DT — a different agent path on the aligned SUMO env"* needs a registration note is not mine to decide
(channel (b) if the coordinator thinks so).

**Proposed, pending the ruling (Q1):** the cell loads the subject through `SpatialDTAgent.from_checkpoint`; a sibling of
`rtg_calibration.agent_with_target` — `spatial_agent_with_targets(env, path, *, declared_gradient_steps, targets:
Mapping[str, float])` — applies the 16 targets **after** load with the same three guards `agent_with_target` has
(declared budget through `spatial_mixing.assert_declared_budget`; `rtg_scale` equal to the payload's **per id** under
`==`; `current_rtg()` equal to the targets **per id** under `==`, and a missing id refused by name). C2 as written
(`DTAgent` mapping) closes `DEFERRED` 78 for some future `dt-checkpoint/1.0` multi-intersection subject and serves no
cell of this task; I recommend it leaves P7.3d's scope, and I will build it if the coordinator rules it stays.

### F2 — T-78's byte-equality round trip is unsatisfiable on the UNMODIFIED code (measured), for both agent classes

*Measured*, no source edited: `from_checkpoint` → `save(<same file name>, provenance=payload["provenance"])` → compare.

```
DTAgent         dt_seed101.pt                             2,607,605 / 2,607,733 bytes   equal: False
DTAgent         mix50_dt_seed101.pt                       2,608,313 / 2,608,441 bytes   equal: False
SpatialDTAgent  grid4x4_mappo1000_dt_nomix_h4_seed101.pt  3,434,853 / 3,434,701 bytes   equal: False
                (payload level: every tensor torch.equal True; every other key == True)
```

The trainers wrote these files with their own `torch.save` call, not through `agent.save`, so the archive layout
differs while the content does not. Two further facts a byte test would trip on: P4-era payloads record
`intersection_ids: []` (so a saved agent built on any env differs on that key), and `load` does not retain
`provenance`. **Proposed (Q2):** T-78 asserts **payload-level exact equality** — every tensor under `torch.equal`,
every other key under `==`, the scalar payloads keeping `float` `target_rtg`/`rtg_scale` (type asserted), with
`intersection_ids` compared as recorded-or-empty and said so in the test. The coordinator's hygiene mutation
(`allclose` for `==`) applies unchanged. If F1's ruling removes C2, this test shrinks to the spatial loader's guard.

### F3 — T-regress (b) cannot run "via `report` through the defaults" on this branch, for two independent reasons

1. `report` re-derives J1(c) per chunk (`offline/transfer_curve.py:1657-1667`): `code_changed_since(commit)` lists every
   path outside `docs/` that differs from `HEAD` and **refuses**. This branch changes `offline/`, so every P7.3a/P7.3b
   chunk is refused by design. (`main` already refuses since A19 — `DEFERRED` 84.)
2. The artifact embeds `report`'s **own** `_git_provenance()` at write time (`:1948`). *Measured:* the committed files
   carry `8bf7eae…` (`p7_3a_zero_shot.json`), `f13358e…` (`p7_3b_anchor.json`), `ded56a0…` (`p7_2b_calibration.json`).
   A regeneration at any other commit differs in those bytes even when every number is identical.

**Proposed (Q3):** T-regress (b) calls the real `report` over the real work directories (`output/p7_3a/` — 4,704 files
on disk, `output/p7_3b_anchor/`, `output/p7_2b/`; `skipif` names each) into `tmp_path`, with **exactly two named
substitutions**, both already established patterns in `tests/test_transfer_curve.py` (`:1412`, `:2464-2489`):
`code_changed_since → []` and `_git_provenance → the committed artifact's recorded pair`. Then bytes `==` the
committed file. Everything else — every cell row, every ρ, every CI, key order — must be byte-identical, which is
what the brief's mutation (*a per-intersection branch taken on a one-intersection env*) attacks. J1(c) keeps its own
tests. The alternative I rejected: regenerating in the run worktrees at the recording commits tests the **old** code.

### F4 — Rule B's source-domain half does not exist for grid4x4, and `R_best_source,i` is not where the brief says

`target_i = R_best_source,i × S(R_probe_sumo,i) / S(R_probe_cityflow,i)` (A17(a), per intersection by A17(e)). On hz1x1
the denominator was **read** from `p4_3_probe.json`. *Measured:* no grid4x4 CityFlow probe exists in `docs/data/`, and
the draws 201–300 do not exist at all. C5 names only the SUMO probe; `rtg_calibration.run_probe` is in §0.6's seam
list, so I read the CityFlow probe as intended — but it is a registered quantity's input, so I ask (Q4).
And: §3 C3a / A17(e) say `R_best_source,i` comes from `stats["rtg"][scenario][i]`. *Measured:* that block holds
per-**window** RTG statistics (`A0: count 72000, min -259.0, max 0.0`), whose `max` is trivially 0. The repo's own
reading on hz1x1 (`transfer_calibration.py:561,586`) is `best_source_return = payload["target_rtg"]`, with `stats.rtg`
min/max used for the **support range** only (`:566-567`). I implement the repo's reading per intersection —
`R_best_source,i = payload["target_rtg"][i]` (−132.0 for A0), support `[stats.rtg.min, stats.rtg.max]` — and flag it.

---

## 1. The RESCO root, as resolved on this machine

`RLTRAFFIC_GRID4X4_RESCO` is **unset** in this session's shell (*measured*); it appears in no source module, only in
`tests/test_conversion_audit.py:37` and `tests/test_backend_alignment.py:659`. The candidates **root** on this machine is
`/home/filip/rltraffic/scenarios/grid4x4_candidates` (gitignored, in the main tree; the worktree has no copy).
Under it, `resco/resco_benchmark/environments/grid4x4/` — *measured, read-only*:

| file | sha256 | fact |
|---|---|---|
| `grid4x4.net.xml` | `8d192de462497c19…` | equals A15(g) / §0.1 |
| `grid4x4.zip` → `grid4x4_1.rou.xml` | member `2350dce7e086b8dd…` | 1,473 `<vehicle>`, **0 `<vType>`**, 0 `type=`; 1,400 members |
| `grid4x4.sumocfg` | `8ce3e8f2…` | `begin 0`, `end 3600`, names `grid4x4_1.rou.xml` |

No-default semantics kept: unset → the grid4x4 scenario is unresolvable, tests skip naming the variable, the CLI
refuses naming it. The main-tree path appears in no source file; it is passed in the environment by the caller.

---

## 2. The seams of §0.6, re-read from the code at `66da50e` (line numbers moved when P7.3b merged)

| seam | brief said | re-read |
|---|---|---|
| `dt_choose`'s single id | `transfer_curve.py:713` | **`:885`** (`def dt_choose` `:857`); also `actions` keeps element `[0]` only at `:902` and `:940` |
| `SCENARIO_KEY` | `:100`, 4 uses | `:100`; uses `:757`, `:819`, `:1823` |
| `n_actions = min(action_counts)` | `:1094` | inside `run_cell` (`:1236-1427`); re-read at C3b |
| `cell_chunk_name` | — | `:345-349`, no scenario in the name |
| J1(c) / provenance | — | `code_changed_since` `:515-526`; `report`'s use `:1657-1667`; `_git_provenance()` into the artifact `:1948` |
| `run_pilot`'s guard | `:2624` | `:2624` ✓ |
| `transfer_calibration.SCENARIO_KEY` | `:104`, 7 uses | `:104`; `:354`, `:367`, `:917`, `:1202`, `:1319` (+ `__all__` `:70`) |
| calibration single-intersection | `:414-421`, `:929` | `:415-421` (refusal + `[0]`), `:929` ✓ |
| `rtg_calibration` single-intersection | `:395-403` | `:396-403` ✓; `agent_with_target` `:597-636` is `DTAgent`-only (F1) |
| `aligned_env.DECLARED_SCENARIO` | `:55`, 4 uses | `:55`; `_declared_paths` `:58-67` takes both nets from `parity`'s hz1x1 constants; `declared_alignment` `:173-180` is called by all three env builders (`:211`, `:306`) and by `collect.py:714-717` |
| `parity._DECLARED_STEM` | `:109` | `:109` ✓; `PARITY_VTYPE` `:158-180` |
| `materialise_draws` literal | `:1876` | `:1876` and `:1879` (`_report_heldout_thresholds`) |

**Three seams the brief does not list, found by reading `materialise_parity` (`:1394-1560`):**
(a) `_sumo_pairing` (`:555-613`) reads the scenario's own `.sumocfg`; for grid4x4 it returns *"names route file
'grid4x4.rou.xml', which does not exist"* and `materialise_parity` raises at `:1433`. (b)
`_validate_parent_for_parity` (`:1124-1194`) requires **four** files and a non-null `sumo` record; *measured:* all 106
grid4x4 parents hold three files and `"sumo": null`. (c) `parity.render_parity_rou_text` (`:267-299`) requires
**exactly one** `<vType>` to replace; RESCO's member has none, and `FlowRandomizer.render_sumo`
(`flow_randomizer.py:492-549`) copies only what the template has. So grid4x4's bound rendering cannot be derived from
a parent `routes.rou.xml` (none exists, and none may be added: *nothing under an existing draw directory is
overwritten*); it is rendered **inside `parity/`** from the parent's `flow.json`.

---

## 3. Design, commit by commit

### C1 — `offline/parity.py` + `offline/materialise_draws.py`

- `ParityScenario` (frozen dataclass): `key` (the sim-config stem), `stem`, `flow_json`, `declared_vtype`
  (the REGISTERED table: hz1x1's existing `PARITY_VTYPE`; grid4x4's A15(g) eight), `net` and `route_source` as
  resolvers — hz1x1: the shipped files; grid4x4: `<RLTRAFFIC_GRID4X4_RESCO>/resco/…/grid4x4.net.xml` and
  `(grid4x4.zip, "grid4x4_1.rou.xml")` read with `zipfile` in place, each with its **pinned sha256** (`8d192de4…`,
  `2350dce7…`) checked before use. `scenario_for_key(key)` refuses an unknown key by name.
- **Derivation.** `derived_vtype(scenario, flow_json)`: for every attribute with a `source_key`, the emitted string is
  `repr(float(block[source_key]))` from `read_cityflow_vehicle_block`; `id` and `speedFactor` come from the ruling.
  `flow_json_disagreements(flow_json, scenario=HZ1X1)` compares derived against declared under float `==` and the
  caller refuses on any. *Measured:* hz1x1's seven derived strings are byte-equal to today's constants
  (`'2.0','4.5','5.0','11.11','2.5','2.0','2.0'`), and grid4x4 derives `accel 2.6, decel 4.5, length 5.0,
  maxSpeed 13.39, minGap 2.5, tau 1.5, width 1.8` — A15(g)'s values. So `PARITY_CONTRACT_VERSION` stays `1.0`
  **iff** T-regress (a) is green; otherwise it is bumped and the packet says why.
- `render_parity_rou_text(source_text, vtype=…)`: exactly one `<vType>` → replaced (today's path, bytes unchanged);
  **zero** → the parity `<vType>` is inserted as the first child of `<routes>`, textually, then `_verify_rendered_rou`
  as today; two or more → refused as today. Every existing signature keeps its hz1x1 default.
- `materialise_parity`: the scenario comes from `scenario_key_for_config(source)`. Parent validation gains a second
  legal shape, selected by the scenario and never guessed: *three files, `sumo: null`, the skip reason naming the
  missing template*. For that shape the unbound rendering is produced in scratch by `FlowRandomizer.render_sumo` over
  the parent's `flow.json` entries with RESCO's member (extracted to the OS temp scratch, never into any tree) as
  template and RESCO's `<begin>` as `depart_offset`, then bound. `provenance.json` keeps
  `materialised-draw-parity/1.0`'s keys; `parent.routes_sha256` becomes `null` with `parent.routes_absent_reason`,
  and a `route_template` block records the zip member's and the net's sha256. **Is an added key a format bump?** I
  read *"P7.2a's shape plus the RESCO member sha256"* as additive at `1.0`; flagged (Q5). `:1876/:1879` take the key
  from the args. **The tool refuses if `parity/` exists** for the grid4x4 shape unless identical (today's `kept`).
- **The 201–300 parents.** Seed rule *read from `provenance.json`, measured on draws 0, 5, 1000, 1001, 1099:*
  `draw.seed = randomizer.base_seed + draw_id` with `base_seed 1000` (draw 1000 → 2000, 1099 → 2099, 5 → 1005),
  `jitter_sigma_s 30.0`, `thin_p 0.1`, `volume_scale 1.0`, `numpy 2.5.1` — and the venv's numpy is `2.5.1` today
  (*measured*; NEP 19 makes that part of the rule). Rendered by the **unmodified** `materialise()` with
  `--draws-range 201 301`. **Where (Q6):** `cityflow.json` embeds an absolute `dir` resolved against the cwd. The
  existing grid4x4 parents embed `/home/filip/rltraffic/scenarios/grid4x4/` (*measured*); P7.3b's hz1x1 301–400 embed
  `/home/filip/rltraffic-p73b/…` (*measured*) and die with that worktree. I propose cwd = the main tree so the new
  parents match the existing ones and survive this worktree; the CityFlow probe of F4 reads those configs.
- **G1.** `python -m offline.materialise_draws --report-cap-e` (name TBD) → `docs/data/p7_3d_cap_e.json`: per draw
  (200) `audit_pair(...)["E_demand"]` — counts, `multiset_equal`, `n_index_aligned_equal`, `order_matches` — and the
  binding report (`n_bound == n_vehicles`, `{"cf_parity"}`, the derived attributes read **from the rendered file**),
  plus the pedigree block of §5. Any draw not exact → exit non-zero, `BLOCKED`. No simulation; in-session.

### C2 — per F1's ruling. If it stays: `DTAgent` takes `float | Mapping[str, float]` through `_resolve_per_node`'s shape, scalar payloads unchanged.

### C3a — `aligned_env.py` + `transfer_calibration.py` (+ `rtg_calibration.py`'s probe loop)

`alignment_for(scenario_key)` replaces the module-level constant pair (hz1x1 default; grid4x4 from
`scenarios/grid4x4/grid4x4_roadnet_red.json` + the RESCO net → `alignment_for_scenario`'s identity branch; canonical
width `2·12 + 16 = 40`, which is the checkpoint's `state_dim` — *measured from the payload*). `run_sumo_probe` and
`run_probe` record, per intersection id, `local_return` and `local_return_from_lanes` with
`episode_return_two_routes` applied **per id**; a one-intersection scenario keeps today's flat record (T-regress).
`p7_3d_calibration.json`: per id — `R_best_source,i`, `rtg_scale_i`, `S_sumo,i`, `S_cityflow,i` at `k ∈ {5, 20, 100}`,
`target_i` at `k = 100` (registered) with `k ∈ {5, 20}` recorded, the support range and in-support position.

### C3b — `transfer_curve.py` + `collect.py`

Scenario as a parameter (hz1x1 default). `dt_choose` records `{ix_id: series}` for RTG, reward and actions on a
multi-intersection env and **today's flat lists on a one-intersection env** — the single branch T-regress (b)'s
mutation attacks. **D1's shift-by-one, unchanged:** the RTG is read **before** `act`, so
`rtg[t] − rtg[t−1] == −reward[t−1]` per id, `reward[0]` is `None`, and `episode_reward ≠ sum(reward_series)` by design.
`validate_cell_payload` refuses `rtg_first_i != target_i` per id, naming the id. `cell_chunk_name` gains the scenario
for non-default scenarios only (existing hz1x1 names must not move: 4,704 + 700 chunks are keyed by them).
grid4x4 gets its own `ARTIFACT_FORMAT_VERSION` (`p7.3d-grid4x4/1.0`) rather than overloading P7.3a's. `DEFERRED` 85's
harmonisation is **not** done here: it would move P7.3a/P7.3b's last ULP and break T-regress; the grid4x4 artifact uses
`sum()/len()` for per-seed means, stated in the artifact.

### C4–C7 — as the brief orders them. Work root `output/p7_3d/`, runs and captures under `output/p7_3d_runs/` (never `/tmp`).

**Stage design (C6):** `confirmatory` = `b_mean_k100` × 5 × 100 + `fixedtime` × 100 + `maxpressure` × 100 = **700**;
`rest` = `naive` × 5 × 100 + `random` × 5 × 100 = **1,000**; halting check ON for every cell of draw 1000 (7 + 10).
Two tokens. Dedicated run worktree `/home/filip/rltraffic-p73d-run`, detached at the reviewed commit (J1(e)).
`WORKERS` and the RSS refusal budget are **unset until G2**; the driver reads them from G2's committed file.

---

## 4. The per-intersection prompt: how it is computed, and where each of the 16 `rtg_first_i` is read back

1. `R_best_source,i` ← `payload["target_rtg"][i]`, asserted identical across the five seeds (as `subject_facts` does).
2. `S_sumo,i`, `S_cityflow,i` ← means over intersection i's 100 probe returns (draws 201–300, `reset(seed=1000)`), each
   return computed by two routes that agree under `==` after an integrality check.
3. `target_i = rule_b_target(R_best_source,i, S_sumo,i, S_cityflow,i)` — the existing, association-pinned function.
4. `naive_i = R_best_source,i`. `rtg_scale_i` is never recalibrated (A17(a)).
5. The cell reads the 16 targets from the **digest-pinned** `p7_3d_calibration.json`, never recomputes them.
6. **Read-back, three places:** at load, `agent.current_rtg()[i] == target_i` for all 16; in the episode,
   `rtg_series[i][0] == target_i` recorded by `choose` before the first `act`; in `validate_cell_payload` and again in
   `report`, per id, by name. T-16's mutation (two targets swapped) must die at the first.

**Paired-CI helper:** `offline.offline_baselines.paired_comparison` (`offline_baselines.py:2199`), as P7.3a — analytic,
no resampling seed, Wilcoxon beside it.

---

## 5. The pedigree gate for the grid4x4 held-out draws — resolved from disk

**A committed per-draw record exists, and it is stronger than a value re-collection:**
`docs/data/p8_4a_admission.json : draw_restoration.grid4x4.flow_sha256` holds the sha256 of `flow.json` for **all 100**
draws 1000–1099, with `n_vehicles` beside it. *Measured today:* **100/100** files on disk hash to the committed
digests and **100/100** vehicle counts match. The demand file *is* the demand; this gate needs no simulation.
**Per-draw VALUES for a deterministic anchor arm exist for three draws only:** `docs/data/p8_4b_g0_reference.json`
(`behaviour@fixedtime`, `behaviour@maxpressure`, draws 1000–1002, CityFlow). All-100-draw values exist only for learned
arms (`bc_top10@mappo1000`, `iql@mappo1000` × 5 seeds in `p8_4a_admission_escalated.json`), which need P8.4a's harness.
**The gate I propose:** the 100/100 digest check, recorded in `p7_3d_cap_e.json` and re-asserted by a test, **plus**
the six CityFlow episodes of 1000–1002 reproduced bit-for-bit against `p8_4b_g0_reference.json` (≈ 20 s). P7.3b §3.2's
five-draw re-collection is therefore **not** needed. The 201–300 band is new: its pedigree is the seed rule of §3 and
byte-identical re-rendering, and `assert_probe_draws_disjoint` against the subject's training draws — *measured from
the payload:* `stats.draw_ids = 1…200` — and the held-out pool.

---

## 6. Tests — first, red for their own reasons (skeletons raising `NotImplementedError`), every named mutation executed

As §4 of the brief, with these specifics. **T-regress (a):** draw 1000 against the **committed**
`p7_3a_zero_shot.json : inputs.demand_by_draw["1000"]` (`c177e962…`, `c2c5eea2…` — *measured equal on disk today*);
draw 5 against its on-disk `parity/provenance.json` (gitignored; draw 5 is in no committed artifact — said in the
test's skip reason). **T-regress (b):** F3's form. **T-78:** F2's form, scope per F1. **T-16 / T-obs:** through the
spatial loader; ≤ 6 grid4x4 SUMO episodes in the suite, one shared module-scoped fixture for T-16 + T-obs where the
assertions allow it. **T-pilot** replaces `test_the_pilot_transcript_survives_a_cell_that_has_no_training_seed` with a
real `run_pilot` call; mutants committed in a throwaway worktree and run from a script file (the two harness traps).
A new guard test: the grid4x4 corpus manifest's recorded settings equal `COLLECT_SETTINGS` field by field
(*measured so far:* `local_reward_fn queue_length`, `global_reward_fn queue_length`, `delta_time 10`, `max_steps 360`
agree; `control_mode` and `state_features` NOT yet compared).

---

## 7. Assumptions (the 95 % rule)

| # | assumption | confidence | if wrong |
|---|---|---|---|
| 1 | CityFlow road ids equal SUMO edge ids on grid4x4, so `" ".join(entry["route"])` is a valid SUMO route | 97 % (A15(g) A + E exact, 1,473/1,473) | CAP(E) per draw fails → G1 `BLOCKED`, registered outcome |
| 2 | `startTime` is `int` in every grid4x4 draw, so `:.2f` is exact | measured on draws 0, 1000 and the source; 99 % | CAP(E) catches it per draw |
| 3 | The subject needs no graph from the env (F1) | measured on a fake env; 97 % on the real aligned env | T-16 fails at load, before any number |
| 4 | The aligned grid4x4 `state` is 40 wide in the corpus's order (identity phase map, movement-keyed lanes) | 90 % — never run | T-obs / T-16 refuse at the first `act` (`state width changed`) |
| 5 | The CityFlow probe on 201–300 is intended (F4) | 90 % → **asked, Q4** | the registered target has no denominator |
| 6 | Every `COLLECT_SETTINGS` field matches the grid4x4 corpus | 90 %, two fields unread → test in §6 | the RTG stream means something else; stop |
| 7 | 12 workers × (16-node DT + 240-lane SUMO + observer) fit in host and GPU memory | **unknown — G2 measures it** | fewer workers; `BLOCKED` if none fits |
| 8 | An added provenance key is additive at `materialised-draw-parity/1.0` | 80 % → **asked, Q5** | bump to `1.1` with a migration note |

---

## 8. Questions for the coordinator — I do not proceed past G0 on Q1–Q4

- **Q1 (F1).** The subject loads only through `SpatialDTAgent`. Confirm the cell uses it with a guarded per-id target
  override, and rule whether C2 (`DTAgent`, `DEFERRED` 78) stays in P7.3d. Does A20(a)'s *"a different agent path"*
  sentence need a note? (Possibly channel (b); yours to judge.)
- **Q2 (F2).** Byte-equality is unsatisfiable on unmodified code (three files measured). Accept payload-level exact
  equality (`torch.equal` + `==`, types asserted)?
- **Q3 (F3).** Accept T-regress (b) with the two named substitutions, or name the form you want.
- **Q4 (F4).** Confirm the CityFlow MaxPressure probe on grid4x4 201–300 (100 episodes, per intersection, same seed
  rule) as Rule B's denominator, recorded in `p7_3d_calibration.json`; and `R_best_source,i = payload["target_rtg"][i]`.
- **Q5.** Additive provenance keys at `1.0`, or a bump?
- **Q6.** Render the 201–300 parents with cwd = the main tree (matching the existing 106), using `main`'s unmodified
  `materialise()`? It writes 100 directories into the main tree's gitignored `scenarios/draws/cityflow_grid4x4/`.
- **Q7 (brief consistency, no action needed from me).** §3 C1 says G1's verdict arrives as *Amendment A* and C5 as
  *Amendment B*; §5's table says G0 → A, G1 → B, G4 → C, G6 → D. I read whatever letter is on `main`.
- **Q8.** `grid4x4`'s `noteleport.sumocfg` references the RESCO net by a relative path from the draws tree
  (`../../../../grid4x4_candidates/resco/…/grid4x4.net.xml`) — no copy, no absolute path. Confirm that is the intended
  reading of *"No RESCO file is copied into the tree"* (the temp-scratch extraction of the route template is outside
  every tree and deleted with the scratch directory).

---

## 9. What I have NOT done, and will not do before the gate opens

No source or test file touched. No draw rendered, no parity file written, nothing under `scenarios/` or `output/`
modified. No grid4x4 SUMO episode run; no DT cell's number exists. The whole suite: **NOT RUN** in this session.
A17(f)'s corpus-versus-probe gate does not arise in this task (nothing fine-tunes, no logged corpus) — the packet will
say so in one sentence, as §7 of the brief asks. Next action after this commit: `git merge --no-edit main`, read the
brief's amendments, and act on Amendment A — or wait, building nothing that F1–F4 could invalidate.

---

# STAGE 2 — after Amendments B and B.1 (2026-09-19, late): what A21 removed, and the design of C3a's second half

Written at `ec52f1e`, after G1 passed and T-obs ran, **before** any line of the per-intersection calibration exists —
so the next session starts from this file and not from a conversation. §0–§9 above are the plan G0 approved; where
they disagree with this stage, this stage and the amendments win.

## 10. What A21 (`v2.1-prereg-a21`) and Amendment B removed from §3 above

- **Arms:** `b_mean_k100` only. No `naive` arm, no `random` anchor on grid4x4 (A21(b)'s two scope sentences are the
  paper's). **One stage, `confirmatory`, 700 cells** = 500 DT + 100 `fixedtime` + 100 `maxpressure`; **one token**;
  the halting cross-check ON for the **7** cells of draw 1000, each counting **691,200** lane-seconds (B.1-1: 192
  monitored incoming lanes, not 240). G7 is gone; G9 is one merge review.
- **No `collect.py` SUMO door for grid4x4** — nothing is collected, so C3b touches `transfer_curve.py` only
  (plus the spatial loader's guard, A1/A2), and `collect.py`'s direct `declared_alignment()` call stays as it is.
- **C4 gains the six-episode half of the pedigree gate** (B3/Q3): CityFlow `behaviour@fixedtime` and
  `behaviour@maxpressure` on draws 1000–1002 reproduced bit-for-bit against `p8_4b_g0_reference.json`, or stop.
- **C6's driver carries the cwd rule itself** (B3/Q4): `cd $MAIN`, `PYTHONPATH=$WORK_TREE`, the RESCO root exported
  with the main-tree candidates dir as default, an assertion that the worktree's module loaded; T-driver asserts them.
- **B4's note:** the unregistered-key fallback inside `materialise_parity` becomes strict the next time that function
  is touched. **B.1-3:** fixed-time `e_sumo ≈ 307.26 s` on draw 1000 HAS BEEN SEEN; every later *results seen* says so.

## 11. C3a, second half — design (NOT built). Additive, so hangzhou cannot move

**Already pinned, first:** `tests/test_t_regress_artifacts.py` (`ec52f1e`) — `p7_2b_calibration.json` regenerates
byte-identically from `output/p7_2b/` (A3's form; this module needs the provenance substitution only). It must stay
green at every commit below; two mutants killed, the per-intersection-shape one among them.

**Principle.** `transfer_calibration.py` is P7.2b's module end to end (the P4.3 artifact pin, two `DTAgent` subjects,
the smoke, `report`). Threading a scenario through `report` would put every hangzhou byte at risk for no gain. So the
grid4x4 path is **new, separate functions in the same two files**, reusing the registered primitives unchanged:
`rtg_calibration.rule_b_target` (association pinned), `probe_statistic`, `episode_return_two_routes`,
`assert_probe_draws_disjoint`, `transfer_calibration.in_support_position`, `_write_json`, `_move_aside`.

| piece | where | what |
|---|---|---|
| the shared rollout | `rtg_calibration.py` | extract `run_probe`'s loop (`:406-417`) into a private helper returning `(post_step, samples, last_vehicle_count)`; `run_probe` calls it and is otherwise untouched (P4.3's gate, `verify_p4_3_probe`, reproduces draw 201 through it — that test is the guard) |
| CityFlow probe, per intersection (A4) | `rtg_calibration.py` | `run_probe_per_intersection(draw_ids, config_for_draw, env_settings, scenario_id, engine_seed)` → per draw `{ix_id: (from_rewards, from_lanes)}` for all 16 ids, each pair refused unless `==`; settings = the corpus's, asserted field by field against `COLLECT_SETTINGS` (assumption 6: `control_mode` and `state_features` still unread) |
| SUMO probe, per intersection | `transfer_calibration.py` | `run_sumo_probe_per_intersection(draw_ids, *, scenario_key, out_root, work_dir, engine_seed, canary_seconds)`; chunk format **`p7.3d-calibration/1.0`**, one chunk per draw, resumable by CONTENT (two routes re-compared per id, decisions 360, teleports 0, `[cf_parity]`, `"-1"`, config digest); A17(b)'s five refusals per episode, the two-route one per id naming the id; `_roll_one_episode`'s signature and its single-intersection refusal stay (tests substitute that seam) |
| subject facts, per intersection | `transfer_calibration.py` | from the five `grid4x4_mappo1000_dt_nomix_h4_seed*.pt` at A20's digests: `R_best_source,i = payload["target_rtg"][i]` (A4), `rtg_scale_i = payload["rtg_scale"][i]`, support `= stats["rtg"]["cityflow_grid4x4"][i]` min/max; all five seeds must agree per id under `==` or refuse naming the id |
| Rule B per intersection | `transfer_calibration.py` | `target_i(k) = rule_b_target(best_source_return=R_i, probe_source_stat=mean_cityflow_i(k), probe_target_stat=mean_sumo_i(k))`, nested prefixes 201…200+k for `k ∈ {5, 20, 100}`; **k = 100 registered, 5 and 20 recorded and never evaluated**; no pooling across ids (A17(e)) |
| the artifact | `docs/data/p7_3d_calibration.json` | per id: `R_best_source`, `rtg_scale`, both statistics at each k, `target` at each k with `role` (`registered_prompt` only at k = 100), the support range and in-support position; both probes' per-draw per-id returns; both engine-seed fields; the five checkpoint digests (the first COMMITTED pin of them — §0.7); the field each constant was read from and why (A4); `what_this_does_not_say`; refusals precede the write |

**Tests, first:** the per-id two-route refusal (one id's lane route perturbed → refused naming it); Rule B per id
double-computed in the test by raw arithmetic over the chunk returns (`R_i * (mean_sumo_i / mean_cityflow_i)`, the
ratio formed first, under `==`); the in-domain identity per id (equal statistics → `target_i == R_i` bit for bit);
nested prefixes; five-seed agreement refused on one perturbed id; disjointness against `stats.draw_ids` (1–200) and
1000–1099; a chunk of the wrong scenario refused; `report` refuses before it writes. *Mutations:* statistics pooled
across ids; `R_best` read from `stats["rtg"]["max"]` (= 0.0 → every target 0); the ratio inverted; k = 20 given the
registered role.

**Runs, in order:** (1) the **CityFlow** probe, 100 episodes on the 201–300 parents, `reset(seed=1000)` — in-session,
canary logged (A4); (2) **G2** — the one-cell measurement in tmux, author-started — BEFORE the SUMO probe's schedule
exists; (3) the **SUMO** probe, 100 episodes, tmux, channel (a). T-obs gives one data point that is **not a rate**
(108 s with the halting check on, 53 s for a run with it on 8 lanes; no canary).

---

## 12. C3a's second half — BUILT (2026-09-19/20), and what remains

Commits `aa84e39` (CityFlow probe per intersection), `fcf22fc` (subject facts + Rule B per
intersection), `aef42c1` (the chunk runner). The design of §11 was followed; two departures, both
smaller than the plan and both because the code already had a better seam:

1. `run_probe`'s loop is extracted as `_roll_maxpressure_episode` and **shared**, as §11 said; the
   per-intersection probe then sits behind its own seam `_roll_cityflow_probe_episode`, so its
   refusals are testable without an engine. hangzhou's P4.3 draw-201 reproduction is the guard, and
   it is what kills the mutant that records the pre-step info.
2. `p7_3d_calibration.json` is **not written yet**: its SUMO half does not exist. The two probe
   halves each write their own chunks (`probe_cityflow_draw_NNNN.json`, `probe_sumo_draw_NNNN.json`)
   and the artifact is assembled once both are on disk, from the chunks and not from memory.

**The CityFlow probe has RUN** (in-session, A4): 100 episodes on draws 201–300, canary **2.257 s**
on draw 0, wall **149.0 s = 1.490 s/episode**, 100 chunks at `output/p7_3d/calibration/`, every
return integral and ≤ 0, both routes equal per id on all 1,600 (draw, id) pairs, 16 distinct means.
Disjointness asserted against the subject's own training draws (1–200, read from the checkpoint) and
the held-out pool. **These are source-domain CityFlow numbers — the denominator of Rule B's ratio —
not a target-domain result.**

**Remaining for C3a:** the SUMO probe (`run_sumo_probe_per_intersection`, the same chunk contract,
the five engine reads per episode) and then `docs/data/p7_3d_calibration.json`. **G2 (the one-cell
measurement, tmux, author-started) comes BEFORE the SUMO probe's schedule is written**, and the SUMO
probe itself is a tmux run (channel (a)) — 100 grid4x4 SUMO episodes is not in-session work.

---

# STAGE 3 — the B.6 fix round (2026-09-23/24): the driver's WIRING into the module

Written against `BRIEF_39` Amendments **B.4, B.5, B.6, B.6.1, B.6.2** and `docs/reviews/P7.3d-preflight.md`, read whole
after `git merge --no-edit main` at `e14d950`. The session runs on **`claude-opus-5-5[1m]`** (B.6.2-3); every commit up to
`8c79778` was `claude-opus-5[1m]`. No commit carries a trailer (B.6.2-2); `core.hooksPath` is the absolute
`/home/filip/rltraffic/githooks`, verified this session.

**Probes (B.5-4, B.6.1), verbatim:** (a) `git reset --hard -h` → *"Permission to use Bash with command git reset --hard -h
has been denied."*; (b) `git -C /home/filip/rltraffic-p73d reset --hard -h` → **ran**, printed the usage block, `exit=129`,
`git status --porcelain` empty afterwards (a measurement, `DEFERRED` 90); (c) `Write(envs/GUARD_PROBE.md)` → *"File is in a
directory that is denied by your permission settings."*, `ls` afterwards: no such file. **Fired: the permission layer, for
(a) and (c).**

## 13. B.6-2 item by item — the design

1. **COMMON before every subcommand**, `p7_3b_anchor.sh:230` shape: `"$PY" -P -m offline.transfer_curve "${COMMON[@]}"
   <subcommand> …`. `cells` also receives `--canary-seconds "$CANARY"` (finding F-B6-1 below), exactly as p7_3b does.
2. **`run_cell` branches on `cell["scenario"]`** through two helpers used by ALL THREE sites — `run_cell`,
   `chunk_is_reusable`, `report` — so the demand identity and the checkpoint identity are re-derived by one call each:
   * `demand_identity_for(cell, out_root)` = `demand_identity(draw, out_root=…, scenario=scenario_of(cell))`;
   * `checkpoint_identity_for(cell, output_root, data_dir)` = hz1x1 → `checkpoint_identity` (unchanged); grid4x4 →
     `grid4x4_checkpoint_identity(seed)`: the file's sha256 against A20(a)'s pin
     `transfer_calibration.GRID4X4_CHECKPOINT_SHA256` (`fcf22fc`), plus `SHA256SUMS_p5_2.txt` where it lists the file
     (the same "a partial manifest is a gap" rule `checkpoint_identity` applies);
   * `scenario_of(cell)` is STRICT: absent → `cityflow1x1`; `cityflow_grid4x4` → itself; anything else refuses (A.1-2's
     lesson: no branch keyed on a coincidence, no unregistered key falling through to hz1x1's pins).
   The grid4x4 DT branch: targets from `load_grid4x4_targets(data_dir=…)`, support ranges per id from the SAME
   digest-pinned artifact, the subject through `dt_choose`'s Mapping branch (`spatial_agent_with_targets`),
   `assert_rtg_first_matches_targets` **called on the finished series against the loaded targets**,
   `calibration_sha256 = P7_3D_CALIBRATION_SHA256`. `chunk_is_reusable` and `report` additionally compare a grid4x4 DT
   chunk's `target_rtg` with the pinned artifact's 16 values. `validate_cell_payload` becomes scenario-aware: the
   calibration pin, the format version, `scenario` added to the chunk-versus-cell identity keys, the 16-id refusal and
   the per-id series lengths on every grid4x4 DT chunk, and grid4x4 arms restricted to A21's three.
3. **`declarations_for(STAGE_GRID4X4, None)` → (grid4x4's 700, grid4x4's 700)**; hz1x1's 4,700 unchanged for its stages.
   `artifact_name_for_stage(stage)` → `p7_3d_grid4x4.json` for the grid4x4 stage, P7.3a's/P7.3b's names unchanged, and
   `main` uses it. **`manifest --campaign-dir DIR`**: refuses unless `DIR` is a directory named `p7_3d` holding at least
   one file; hashes every regular file under it (sorted, paths relative to `DIR`'s parent, `sha256sum -c` format) into
   `<parent>/SHA256SUMS_p7_3d.txt.tmp`, `os.replace`s it into place, then re-reads the manifest and re-hashes every file,
   refusing on any difference or on a file set that moved.
4. **M1** — `fail()` / `on_signal()` in `p7_3b_anchor.sh:248-278`'s shape: every post-token failure writes `FAILED`, a
   signal writes `FAILED`, success writes `COMPLETE`; both removed right after the token. **m2** — `resume-check --stage`:
   every declared chunk on disk that would reach `code_changed_since` inside `chunk_is_reusable` (it parses and
   validates against its cell) has its commit resolved NOW; an unresolvable one refuses before the token, naming the
   chunk. **m1** — `check-inputs`: `p7_3d_calibration.json`, `p7_3d_reference_cells.json`, `p7_3d_cap_e.json` against
   pins in the module (two new pins, `5265f0d5…` and `949015b3…`, measured from the committed files), plus the five
   checkpoints against A20(a) (§3 C6's text) and the RESCO net against `parity.GRID4X4_SCENARIO`'s pin (A8's ruling). The
   existing existence checks stay in front, so their refusal texts are unchanged.
5. **The end-to-end test** — see §14.
6. **`dt-reroll-check`** (B.5-1) and the **PIPESTATUS line** (B.5-3, ending `| tee -a <capture>` per B.6-2(6)). The check
   rolls the fenced cell `{dt, mappo1000_dt_nomix_h4, b_mean_k100, seed 101, draw 5, cityflow_grid4x4, stage "pilot"}`
   through **`run_cell` itself** once in a spawn pool of ONE worker and 12 times in ONE 12-worker spawn pool — G2's
   `measure_pool` shape, "otherwise-identical cells" read as twelve copies of the same cell — and compares EVERY pooled
   chunk with the W = 1 chunk under `==`, excluding by name the one clock field `seconds`; the seven fields B.5-1 names
   must be present or the check refuses (a comparison over nothing is not a comparison). Thirteen chunks under
   `fenced_do_not_report` in `<g2>/dt_reroll_check_<UTC>/`, never overwritten, plus `verdict.json`. It prints ONE line:
   `IDENTICAL` / `NOT IDENTICAL`, the differing field NAMES, the sha256 of each chunk minus the clocks — no value. Exit 0 /
   2; a roll that failed raises (no verdict is invented). The driver runs it after the canary and before the trap,
   refuses the token on anything but exit 0, and echoes the line into the banner after the token.

## 14. The end-to-end test (B.6-1) — how the REAL driver reaches draw 5 without a production hole

**The clean tree.** The driver refuses a dirty tree and `run_cell` refuses `git_dirty` chunks, so the test builds what
`test_k_finding3` built: `git clone --shared --no-checkout` + `checkout HEAD`, then copies in every path the working tree
has changed (modified, added, deleted; untracked non-ignored), commits it IN THE CLONE, and writes the redirected
driver there. The clone is a byte-for-byte snapshot of the working copy, clean, and the code under test is the code on
disk — red before the fix, green after, independent of whether the developer's tree is committed.

**The redirection, each substitution asserted to happen exactly the stated number of times:** `WORK_TREE=` → the clone;
`CAMPAIGN_DIR=` → the sandbox (so WORK, ARTIFACTS, G2 follow); `TOKEN=` → the sandbox; the `dt-reroll-check` line → a
stub echoing a fixed line (the real check is a ≈ 95 s, 13-episode measurement, run once in-session, never in the suite);
`--limit 2` on `cells`; and on the `cells` and `report` lines ONLY, the module `offline.transfer_curve` → a four-line
shim in the sandbox that imports the REAL module, replaces `grid4x4_cells` with the real 700 cells with draw 1000's
seven moved to draw 5 and the (dt seed 101, fixedtime) pair first, and calls the REAL `main()`. So `--limit 2` rolls
exactly the fenced pair on draw 5 (outside both pools, B.2-2), the declaration still counts 700, and `report` must
refuse on completeness with **698** missing. (`sitecustomize` cannot do this: `-m` runs the module as `__main__`, a
different object from the one a startup hook would patch.)

**Asserted:** exit 1 (report's refusal through `fail`), `FAILED` = `CAMPAIGN FAILED at report`, no `COMPLETE`; the token
gone and consumed once; both chunks under the grid4x4 names; `config_sha256` = the grid4x4 draw-5 file's digest
(`c27d31e8…`, read by the test) and ≠ hz1x1 draw 5's (`c177e962…`); `routes_sha256` = grid4x4 draw 5's; the DT chunk's 16
`rtg_first_i` = the 16 targets read by the test's own `json` route, and `rtg_series[i][0]` likewise; `calibration_sha256`
= `p7_3d_calibration.json`'s digest by `hashlib`; `canary_seconds` = the canary line's seconds; 360 × 16 actions in
`[0, 8)`; 0 teleports; the report's refusal is the 698-missing one and names neither chunk; no artifact written.

**Other executed driver tests:** the header's Step-2 line through tmux on a redirected clone with NO token — the group
leader passes, the canary runs, the refusal is the token's, the capture's last line is `DRIVER EXIT: 2`, equal to the
exit code of the same driver run under `setsid` (B.5-3); a stubbed `NOT IDENTICAL` refuses with the token untouched; a
chunk with an unresolvable 40-hex commit refuses at `resume-check` with the token untouched.

## 15. Findings beyond the pre-flight (F-B6-n) — what they change

* **F-B6-1 (fixed here, load-bearing):** `cells` was never given `--canary-seconds`, so every chunk would carry
  `canary_seconds: None` and `report`'s `_canaries_by_stage` raises on the FIRST such chunk — after all 700 cells rolled.
* **F-B6-2 (fixed here):** the driver ran only the canary's CORRECTNESS half; the timing half (`CANARY_MAX_SECONDS` 2.0,
  `p7_3b_anchor.sh:239-245`) was absent while the header claimed both. Also: a failing canary's observed line was not
  printed (the value sat in `$CANARY_LINE`); now it is.
* **F-B6-3 (NOT fixed — outside B.6's list; blocks the token):** `report`'s body has no grid4x4 aggregation. It iterates
  hz1x1's `SUBJECTS` only (the grid4x4 subject reaches no ρ mean and no H3 clause), writes `scenario_key cityflow1x1`,
  `format_version p7.3a-zero-shot/1.0`, `calibration_sha256` P7.2b's and hz1x1's `deferred_56` text, never checks C4's six
  reference cells, and `_in_support_block` applies `int()` to a per-id dict. **Because of J1(c), adding the body after
  the campaign makes all 700 chunks "rolled by different code".** It must land before the token. Measured in-session
  on a synthetic grid4x4 set once B.6 is in; reported, not built.
* **F-B6-4 (fixed here):** the tmux executed-form test ran the REAL driver against the REAL token path; with B1 fixed it
  would consume an author's token from a pytest run. Every executed test now runs a redirected clone.
* **F-B6-5 (fixed here):** chunks carried no `actions` (B.5-1 compares them) and `anchor_choose` kept intersection 0's
  action only, so `actions_in_range` checked one intersection of sixteen. grid4x4 chunks now record the 360 × 16 matrix
  and `intersection_ids`; hz1x1's records are unchanged (keyed on the scenario, not on a count).
* **Chunk format:** a grid4x4 chunk is a new shape (per-id series, `scenario`, `actions`), so it carries
  `GRID4X4_ARTIFACT_FORMAT_VERSION` (`p7.3d-grid4x4/1.0`) and the module docstring states its fields and D1's alignment.

## 16. Tests changed (disclosed in the packet in full) and added

Changed, all in `tests/test_g2_measure.py`, all T-driver, all ordered by B.5-3 / B.6-2(1): the order test's marker
`"transfer_curve cells"` (the text B1's fix removes) becomes the new `cells` line, and the list GAINS the four new
pre-token steps; the header test GAINS the PIPESTATUS clause; the tmux test is REPLACED (not duplicated) by the
redirected, token-absent, exit-code-comparing form. Added: the end-to-end test; grid4x4 chunk reuse refused for hz1x1's
digests (the reviewer's sandbox case) with its correct-digest control; report refusing it at step 6; the checkpoint
identity against A20(a); `declarations_for` and the artifact name per stage; `manifest`; `resume-check`;
`check-inputs`; the reroll comparison, runner and CLI on stubs (a one-action difference → NOT IDENTICAL → exit 2).
**Mutations** (committed mutants in a throwaway worktree, driven from a script file whose text avoids the pgrep pattern):
COMMON after the subcommand; `scenario=` dropped from `demand_identity_for`; `checkpoint_identity_for` routing grid4x4 to
hz1x1's tables; `assert_rtg_first_matches_targets` not called; `declarations_for` whole = None for grid4x4;
`calibration_sha256` = P7.2B's; `--canary-seconds` dropped; `fail` not writing FAILED; `resume-check` after the token;
`${PIPESTATUS[0]}` → `$?`; the reroll comparison ignoring `actions`; the manifest's re-verify removed.

## 17. Assumptions (95 % rule)

| # | assumption | confidence | if wrong |
|---|---|---|---|
| 1 | The shim (declaration only; real module, real `main`) is what B.6-2(5)'s "redirected driver on draw 5 … 698 missing" requires | 85 % | a test design, revisable before any cell rolls; stated in the packet |
| 2 | "Otherwise-identical cells" = twelve copies of the SAME cell (G2's `measure_pool` shape); comparing all twelve is stronger than one | 90 % | the coordinator names another reading; one constant |
| 3 | `seconds` is the only clock in a chunk (`canary_seconds` is the value passed in, identical by construction) | 97 % (read from `run_cell`) | NOT IDENTICAL on a clock → a finding with the field named, never a loosened `==` |
| 4 | F-B6-3 is reported rather than built, because B.6 is a scoped fix round and nothing is irreversible before the token | 85 % | the coordinator orders it into a follow-up commit; no chunk exists to invalidate |
| 5 | Grid4x4 chunks carry `p7.3d-grid4x4/1.0` | 90 % | one constant, before any chunk exists |

