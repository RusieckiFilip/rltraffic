# PLAN — P7.3a, Amendment H fix round (H1–H8, one commit)

**Authority:** `BRIEF_37` Amendment **H** @ `772329b` · **Branch:** `task/p7.3a-zero-shot` (merged
`main` and pushed at the start of this session; origin was at `f6f57ec`, the packet's §1 implied
otherwise and H's opening paragraph is right).
**Scope fence:** H1–H8 and H9's two handed-over test weaknesses. **No pilot** (H10 Q4 puts F1's
pilot in the next session). Nothing that produces a number.

**Answered by H, so not re-opened here:** H0 confirms `(tier, method == "dt", seed: int)` as the
ruling (my open question 1). H10 confirms one canary per stage (Q2), the final `report` over all
cells citing the stage-1 sha (Q3), and that the implementer runs F1's pilot next session (Q4).

---

## The eight, each as: change · test · mutation

| # | change | test | mutation that must die |
|---|---|---|---|
| **H1** | `a17f` CLI passes `draw_ids=range(PROBE_DRAW_START_DEFAULT, PROBE_DRAW_END_DEFAULT)` = 201–300, asserts `n_checked == n_matching == 100`, prints `A17(f) 100/100`, and loads the calibration through `load_calibration` | a 99-draw synthetic corpus is refused naming the missing draw; a 100-draw synthetic with one mismatch is refused naming draw, both values and the difference; a moved artifact is refused | the draw set dropped (`draw_ids=None`) → the 99-draw test dies |
| **H2** | driver stage 1: `if [ -d "$CORPUS" ]` → `a17f` and skip collection; else collect → `a17f`. **Never `--overwrite`** | T9: `--overwrite` absent; `a17f` in BOTH branches and before any `cells --stage` | `--overwrite` added → dies; the skip branch's `a17f` deleted → dies |
| **H3** | — (the driver already has it; what is missing is the assertion) | T9 asserts `set -euo pipefail` exactly | `set -eu` → dies; the line deleted → dies |
| **H4** | — (the guard exists; its test never reached it) | the reviewer's fixture: DT on 1000 **and** 1001, anchors on **1001 only**, one shared draw, matching the guard's OWN message | the per-draw anchor guard removed → dies (today it writes an artifact with draw 1000's ρ normalised by 1001's anchors) |
| **H5** | — | T5 re-derives `rtg[t-1] − rtg[t] == reward_series[t-1]` for `t ≥ 2` by raw arithmetic, `rtg[1] − rtg[0] == 0`, `rtg[0] == target`; no helper | the helper is change-detection only, so a double-counted reward passes it: assert the arithmetic dies under a doubled reward series |
| **H6** | — | (a) draw 1000 with `halting_checked: False` refused; draw 1001 with `True` refused. (b) a fake tap whose final info differs from the rollout's last sample → refused | each refusal `if False:` → the new test dies |
| **H7** | `_h3_block`: `holds` → `point_estimate_satisfies`, plus `ci95_entirely_satisfies` (the whole analytic interval on the claimed side). No verdict word. Format version stays `1.0` (nothing recorded yet) | both keys present and mechanically correct on a constructed interval | `ci95_entirely_satisfies` computed from the point estimate → dies |
| **H8** | MIN-1 order pinned; MIN-3 action count from the env via `Utils.infer_action_counts`; MIN-5 `report --stage` filters by the stage's declared names and counts `n_chunks_outside_stage`; MIN-6 canary prints **then** checks (P7.2b's shape) | one test each | MIN-1: the shape check moved after the rollout → dies. MIN-3: `< 8` hardcoded → dies. MIN-5: stage-2 chunks present → stage-1 report still regenerates. MIN-6: check-then-print → dies |

## H9's two, handed over in the same round

* `"rest" in code` matches **restart** — replaced by the stage dispatch's own form.
* `test_t8_report_refuses_a_cell_whose_anchors_come_from_another_draw` is caught by **completeness**,
  not by the pairing guard: renamed to say so, and H4's fixture becomes the pairing test.

## Assumptions (95 % rule)

| # | assumption | conf. | basis / if wrong |
|---|---|---|---|
| 1 | the RTG identity holds exactly in float on a real episode | 97 % | the reviewer re-derived it on a real 20-decision episode (H5); rewards are vehicle counts, so integral. If it fails, `==` is not achievable and **that is a finding**, not a tolerance to widen |
| 2 | `Utils.infer_action_counts` falls back to `ix.num_phases` when there is no `action_space` | 99 % | read at `agent/utils/utils.py:13-29` |
| 3 | a synthetic corpus of `{flow_draw, ix0_local_reward}` npz + a `sumo_draws` manifest is what the gate reads | 98 % | read from `assert_logged_corpus_matches_probe`'s body, not from its docstring |
| 4 | `PROBE_DRAW_START_DEFAULT/END_DEFAULT` are 201/301 and half-open | 99 % | `transfer_calibration.py:1636-1637`, and H11 records the same reading of `--flow-draws-range` |
