# P7.3c — plan (gate G0): the few-shot curve on grid4x4 under A24 (`BRIEF_41`)

**Branch** `task/p7.3c-grid4x4-fewshot` · **worktree** `/home/filip/rltraffic-p73c` · **base** `main` at `019ad68` (session
start: `3882344`, *Already up to date*; the pre-gate `git merge --no-edit main` fast-forwarded to `019ad68`, whose only change
is `tests/test_g2_measure.py` — Amendment E, §0.3) · **authority** A24 (`PREREGISTRATION.md` §12, tag `v2.5-prereg-a24` →
`641e1da`) · **written against** `BRIEF_41` as issued, **no amendment yet** · 2026-09-25, implementer session (Claude Code,
Opus 5.5).

**What this file is.** C0 of `BRIEF_41` §3: the assumptions with their confidence, every seam I will touch with its
file:line (read in THIS session at `3882344`, not copied from the survey), the fine-tune's data route, the evaluation's new
branches, the tests of §4 mapped to commits, and every open question. **Nothing is built until Amendment A.** Line numbers
were read at `3882344` and hold at `019ad68` (between the two only `tests/test_g2_measure.py` changed).
`git log cdec88e..3882344 -- offline/ agent/ tests/ experiments/ envs/` is EMPTY, so the survey's code was the code I read;
every cited line was nonetheless re-read here.

**Results seen in this session:** none. I printed field NAMES, digests, configs, roles and draw windows only — no return,
target value, ATT or ρ of any cell or probe episode.

---

## 0. Before planning — the probe, the trailer, and a precondition that is NOT met

1. **Guard probe:** the Write tool on `envs/GUARD_PROBE.md` was **DENIED** (*"File is in a directory that is denied by your
   permission settings."*). The file does not exist and `git status --porcelain` was empty. That exercised the permission
   layer; the PostToolUse guard itself was not reached.
2. **Trailer:** the harness's attribution instruction (a `Co-Authored-By` line) conflicts with `CLAUDE.md` §4b; I raised it
   to the author before any commit, as §4b.3 orders. Ruling: *no co-authored*. `core.hooksPath` =
   `/home/filip/rltraffic/githooks` (absolute, from the main tree's config), `commit-msg` present and executable.
3. **`BRIEF_41`'s sequencing precondition was NOT met when this session started, and IS met at this commit.** The brief
   says *"`BRIEF_39` Amendment E … is done FIRST; this task starts once that commit exists."*
   - **When I started,** `task/p7.3d-ci-decoy` sat at `3882344` (0 commits ahead of `main`), and
     `git log --all -- tests/test_g2_measure.py` showed nothing after `bd4d457` (2026-09-24). The fix was in progress,
     uncommitted, in `/home/filip/rltraffic-p73d-ci` (`M tests/test_g2_measure.py`, 9+/2−). I explored and drafted anyway,
     because the author launched this session and ordered the plan committed as G0; the plan touches no code.
   - **At the pre-gate merge,** Amendment E's commit existed and was on `main`: `19e234c` (19:44, the test), merged at
     `019ad68` (19:56, two parents by `git rev-list --parents -n1`). That merge fast-forwarded this branch, so the plan is
     written on top of it. **Q0.**

## 1. What I read, and what I verified by running commands

**Read, in the brief's order:** `BRIEF_41` whole (331 lines); A24 whole; A17 (b), (e), (f); A18 (c), (d); A20; A21; A23;
`docs/notes/P7.3c_SURVEY_2026-09-25.md` whole; `BRIEF_39` §3–§5, B.3-1, B.5-3, B.7 through B.8.2, and Amendment E;
`DEFERRED` 93, 94, 95; `docs/reviews/P7.3d.md` whole; `CONTRACTS.md`; `BRIEF_37` J1–J4; `PROJECT_PLAN` §7 lines 770–945
in full and the pre-flight section's rules by heading (946–1784) — **not every line of 946–1784**.

| # | Verified | How, and the result |
|---|---|---|
| V1 | the three P7.3d artifacts' digests | `sha256sum`: `p7_3d_calibration.json` `3e9df8ee…9723`, `p7_3d_grid4x4.json` `c63c371f…b0b7`, `p7_3d_reference_cells.json` `5265f0d5…` |
| V2 | the subject, seed 101 | digest `329fb6b8…`; `torch.load(weights_only=True)` with ONE allowlisted class (`torch.torch_version.TorchVersion`); `spatial-dt-checkpoint/1.0`; config `state_dim 40, n_actions 8, n_nodes 16, context_length 20, n_layer 3, n_head 4, d_model 128, dropout 0.1, max_ep_len 360, spatial_mixing False`; `intersection_ids` A0…D3; `stats` split `train`, `stats_version 1.0`, `draw_ids` 1–200 (n 200), keyed `cityflow_grid4x4`; provenance `lr 1e-4, wd 1e-4, grad_clip 0.25, warmup_steps 1000, batch_size 64, gradient_steps 40000, device cuda, deterministic False, method dt_nomix_h4`; 66 tensors; mask the identity |
| V3 | the calibration artifact's shape | ids A0…D3; `probe_returns.sumo` 100 draws 201…300 × 16 floats; per intersection `budgets.k5` (`draw_ids [201, 205]`, `recorded_not_evaluated`), `k20` (`[201, 220]`, same role), `k100` (`[201, 300]`, `registered_prompt`), each with a `target` |
| V4 | the zero-shot record | 700 cells, ONE keyset of **56** fields. A P7.3d chunk has 68 keys. **Only in the record:** `rho_e_sumo`, `rho_att_env`. **Only in the chunk:** `actions`, `actions_in_range`, `anchor_training_sha256`, `collisions`, `format_version`, `local_return_from_lanes`, `n_collisions`, `n_explained_teleports`, `n_observations`, `n_unexplained_teleports`, `reward_series`, `rtg_series`, `teleports`, `vanished_ids` |
| V5 | the SUMO env's ids on grid4x4 | P7.3d's probe chunk, draw 201 (the PLAIN SUMO env): `intersection_ids` A0…D3 in env order; `engine_seed_drawn 437485271`; `n_teleports 0` (the sparse read) |
| V6 | collect's flow digest = the draws tree's | `FlowRandomizer(base_seed 1000; 30.0 / 0.1 / 1.0)` through `collect._cityflow_flow_source`, cwd the main tree, equals `draw_NNNN/flow.json`'s sha256 on draws 201, 250, 300 — **3 of 100 sampled, not all** |
| V7 | the machine | torch `2.11.0+cu128`, CUDA True, RTX 5080 Laptop 16,303 MiB (1,284 in use when read), deterministic algorithms OFF by default; `sumo` 1.27.1 and `netconvert` on PATH |
| V8 | hooks for an independent step count | `torch.optim.optimizer.register_optimizer_step_{pre,post}_hook` exist in this torch |

## 2. Findings that need a ruling BEFORE code (brief against repo, and readings of A24)

**F1 — a brief/repo conflict on C3's payload, load-bearing for EVERY trained cell.** C3 gives the fine-tuned payload *"a new
`format_version` (`few-shot-checkpoint/1.0`)"*. `SpatialDTAgent.load` refuses any top-level version other than
`spatial-dt-checkpoint/1.0` (`agent/SpatialDTAgent.py:857-862`), and the evaluation reaches it on every DT cell: `run_cell`
(`offline/transfer_curve.py:2459`) → `dt_choose` (`:1686-1748`) → `spatial_agent_with_targets`
(`offline/rtg_calibration.py:804-874`) → `SpatialDTAgent.from_checkpoint` (`:864`) → `load`. A payload under the new
top-level version could therefore never be evaluated without changing `agent/SpatialDTAgent.py`, which §2 forbids. The
WARM START needs no change: `SpatialDTConfig.from_json_obj` (`:171-191`), `SpatialDecisionTransformer(config)`
(`:270-289`) and `load_state_dict(strict=True)` read no version. **Proposal:** the payload keeps the top-level
`format_version: "spatial-dt-checkpoint/1.0"` — its layout IS that format, key for key — and carries the few-shot identity
in a versioned block, `provenance["few_shot"]["format_version"] = "few-shot-checkpoint/1.0"`. C5's guard requires that block
on every trained subject. No agent change. **Q1.**

**F2 — load-bearing for the stage-1 gate: the record carries two fields no chunk carries (V4).** *"Every field the record
carries"* includes `rho_e_sumo` and `rho_att_env`, which `report` computes (`transfer_curve.py:3106-3108`: the 54
`_GRID4X4_PUBLISHED_FIELDS` at `:3785-3799`, plus `_rho_pair` at `:2830-2853` against the same draw's anchors). A comparator
over raw chunks would refuse all 700 for a missing field. **Proposal:** the comparator runs once all 700 stage-1 chunks
exist. It builds each chunk's ROW through `report`'s own row code, extracted into one helper that both call, with ρ taken
against stage 1's re-rolled anchors of the same draw. It then compares row to record on the RECORD's keys minus the six
bookkeeping fields, under `==`. **Q2.**

**F3 — description only.** `BRIEF_41` §0.6 counts the record as eight named fields *"and 50 more"* (58). The artifact's
records carry **56** (V4). The six bookkeeping fields are all among them and nothing else depends on the count.

**F4 — where the "40,000-step branch" lives.** C5 says the check at `spatial_mixing.py:1118–1140` *"gains a branch"*.
`assert_declared_budget` (`offline/spatial_mixing.py:1118-1153`) already compares `provenance["gradient_steps"]` with
whatever `declared` its caller passes (`rtg_calibration.py:841`). The caller is `dt_choose`, whose default is 40,000
(`transfer_curve.py:1709-1711`), and `run_cell` never passes one (`:2459`). `spatial_mixing.py` is P5.2's module and outside
C5's named file. **Proposal:** the branch sits at C5's call site in `transfer_curve.py`. A trained subject's cell passes its
run's declared B to `dt_choose`. First, a new guard verifies from the payload (weights-only load) that its provenance names
a pinned 40,000-step source, i.e. A20(a)'s digest for THAT seed, or for `scratch_k100` the from-scratch switch, and that
B ∈ {1,000, 4,000, 16,000} equals the run's declared B. Only then is the agent built. `spatial_mixing.py` stays untouched,
and the zero-shot subject keeps the 40,000 default, so stage 1 is the P7.3d code path byte for byte. **Q3.**

**F5 — how A23's pins extend to P7.3c.** `_assert_a23_collision_cells` (`transfer_curve.py:1888-1909`) refuses a stage
unless it records EXACTLY A23's two events.

- **Proposal:** it applies to STAGE 1. The re-roll of P7.3d's set must record exactly those two, and the stage-1 gate
  compares `n_teleports` on all 700 in any case.
- On stages 2 and 3, A23(c) governs per cell through the unchanged `_validate_collision_record` (`:1912-2001`). A
  collision cell is kept, an unexplained teleport refuses the cell, and that refusal stops the campaign (A24(c)).
- The report's collisions block covers every P7.3c arm.
- A24(d)'s robustness set is DERIVED from the chunks — every draw on which any cell entering Δ₁₀₀ records a collision —
  not taken from A23's constant `A23_ROBUSTNESS_DRAWS_REMOVED`.

**Q4.**

**F6 — `DEFERRED` 93's scope.** The row names the PROBE's loop, `transfer_calibration.py:391-437`, whose sparse reads are at
`:414` and `:429`. P7.3c runs no probe; its SUMO episodes go through `collect.py`'s door, which C1 gives the dense counter.
**Proposal:** C1 closes 93 for the path P7.3c's episodes take, and `_roll_sumo_maxpressure_episode` is NOT changed.
Changing the probe's env class would move `run_sumo_probe` and `run_sumo_probe_per_intersection` with nothing in this task
re-verifying them. The coordinator then rescopes the row to what remains (a future re-run of `run_sumo_probe*`), or rules
that the probe loop changes too. **Q5.**

**F7 — `Utils.seed_everything`.** A24(b)(2) and C3 write `Utils.seed_everything(seed)`; `train_tier_dt` calls
`Utils.seed_everything(int(seed), seed_python_random=False)` (`offline/tier_sweep.py:843`; the helper at
`agent/utils/utils.py:38-44`). The trainer never draws from Python's `random`, so the two have the same effect.
**Proposal:** mirror `train_tier_dt` exactly ("the recipe unchanged where one exists") and record the call in the
provenance. **Q6.**

**F8 — STAGE B's start.** The header says *"(starts when the corpus is verified, G4)"*; the gates table has G3 = corpus
verified and G4 = the review of C3–C4 (which must therefore exist before G4). **Proposal:** I build C3–C4 after G1 while the
corpus runs, the way P7.3d built C6 during its probe. Their tests use synthetic corpora, the fine-tune never reads the real
corpus before G3, and nothing trains before G6's token. **Q7.**

## 3. Assumptions, with confidence

| # | Assumption | Conf. | Basis, and what falsifies it before it matters |
|---|---|---|---|
| A1 | The subject warm-starts with no change to `agent/SpatialDTAgent.py` | 99 % | V2 + the three constructors read (F1). T-warm on the real checkpoint |
| A2 | A trained payload is evaluable iff its top-level version is `spatial-dt-checkpoint/1.0` | 99 % | `SpatialDTAgent.py:857-862` read. A C3 test loads a trained payload through `spatial_agent_with_targets` on a fake env (§12) |
| A3 | Building corpus episodes on the OBSERVER env (`make_observer_sumo_env`) leaves SUMO's dynamics as the probe's PLAIN env had them | **90 %** | Its `_simulate` only READS after each `simulationStep()`, keeping `metrics.on_sim_step()` in the frozen env's position (`offline/sumo_att_reference.py:1020-1055`); B.8.2-2 measured that the added reads change nothing on the evaluation path. **Below 95 %, so it is MEASURED before it is relied on:** T-regress (a) (hz1x1 draw 201: arrays `==` the existing corpus), T-17f (grid4x4 draw 201: 16 returns `==` the artifact), then the A17(f) gate on all 1,600 |
| A4 | grid4x4's SUMO ids ARE the CityFlow ids (A0…D3) | 97 % | V5; `align_info` keeps intersection keys (survey S2-2, `offline/backend_alignment.py:419-423`). The gate and `build_joint_index` refuse otherwise |
| A5 | The logger's float32 reward sums equal the probe's float64 returns under `==` | 95 % | The reward is a vehicle count, so it is integral and exact in float32. `transfer_calibration.py:2265-2281` makes this argument for hz1x1, integrality checked first; T-17f and the gate falsify it |
| A6 | Stage 1 re-rolls P7.3d's 700 records bit for bit if the zero-shot path is untouched | 90 % | A23(f)'s blind comparison (698 / 698) and B.7.6-3's IDENTICAL over 13 rolls. This is exactly what stage 1 exists to test, and C5 is written to leave that path byte-identical |
| A7 | The k-targets are A24(b)'s same-k Rule B targets, read and never recomputed | 99 % | V3 |
| A8 | ≈ 122 ms per joint step on this GPU, so B = 4,000 ≈ 8 min | 85 % | P5.2's committed D-2; the fine-tune batch has the subject's shape. **G5 measures it**; the k = 100 window build is not covered by P5.2's figure (Q12) |
| A9 | The logger writes nothing before `finalize_episode()` | 99 % | `offline/trajectory_logger.py:805-843` read; callbacks only buffer |
| A10 | The corpus is ≈ 1–1.5 h sequential | 70 % | The probe's PLAIN env ran 29.9 s per episode alone; the observer costs "about 60 %" (`transfer_calibration.py:1024-1025`). **T-17f's one episode, timed with its canary, becomes the header's basis** |

## 4. The seams, re-verified at `3882344`

| Commit | File:line | What it is today | What changes |
|---|---|---|---|
| C1 | `offline/collect.py:712-742` | `align_for_log` built from `declared_alignment()`, UNCONDITIONALLY (`:714`, `:717`) | `alignment_for_scenario_key(scenario_key)` (`offline/aligned_env.py:196-238`; hz1x1 returns `declared_alignment()` itself, `:219-220`) |
| C1 | `offline/collect.py:846` | `env = make_env(draw_spec)` per draw | SUMO draw sweeps: `make_observer_sumo_env(draw_config, draw_spec.settings, halting_check=False)` (`offline/sumo_att_reference.py:1061-1095`; `SETTING_DEFAULTS` carries `obs_norm`, `experiments/config.py:61`) |
| C1 | `offline/collect.py:876-891` | reads type, option and engine seed at the horizon; NO teleport or collision count; then `finalize_episode()` | the dense counts from `env.recorder` (`n_teleport_events` `:758-761`, `collision_record()` `:811-831`) recorded per draw; an event REFUSES before `finalize_episode()` — nothing written. Stale comment at `:879` corrected |
| C1 | `offline/transfer_calibration.py:2252-2366` | `assert_logged_corpus_matches_probe`: hz1x1-only, reads `ix0_local_reward` (`:2320`) | UNCHANGED; a grid4x4 twin keyed by ID beside it |
| C1 | `offline/transfer_calibration.py:582-718`, `:1277-1281`; `offline/rtg_calibration.py:287-310` | `subject_facts_per_intersection` (digest-checks the five, reads `stats.draw_ids`); `assert_probe_draws_disjoint` | reused: a grid4x4 corpus disjointness record, run before the first episode |
| C1 | `offline/transfer_gate.py:1028-1038`; `offline/sumo_att_reference.py:1232-1270` | `COLLECT_SETTINGS`, the probe's env settings, parsed by `collect`'s own parser | reused: the corpus is collected with exactly these, through `collect.main(argv)` (the pattern at `transfer_gate.py:1058-1071`) |
| C3 | `offline/tier_sweep.py:660-678` | `warmup_for` (min(1000, max(1, B // 2))) and `lr_multiplier` | imported, unchanged |
| C3 | `offline/spatial_mixing.py:129-134` | `JOINT_BATCH_SIZE 64`, `LEARNING_RATE 1e-4`, `WEIGHT_DECAY 1e-4`, `WARMUP_STEPS 1_000`, `GRAD_CLIP 0.25` | imported from where `train_tier_dt` imports them (`tier_sweep.py:809`), asserted `==` the source's provenance |
| C3 | `offline/tier_sweep.py:765-948` | `train_tier_dt`: seeds (`:843`), config FROM THE DATA incl. `max_ep_len` (`:844-852`), fresh model (`:853`), AdamW + LambdaLR (`:854-860`), per-node scale (`:867-871`), sampler (`:873`), loop (`:877-898`), save (`:907-938`) | NOT called and NOT changed; the fine-tune reproduces its loop with the four A24(b) differences (§7) |
| C3 | `offline/tier_sweep.py:690-757` | `configure_determinism`, `assert_process_regime` | reused: the non-deterministic regime, asserted |
| C3 | `offline/dataset.py:456-669` | `TrajectoryWindowDataset(..., split, draw_ids, stats, normalize)`; supplied `stats` are adopted, never fitted (`:663-669`); a requested draw absent refuses (`:576-581`) | reused as is |
| C3 | `offline/dataset.py:265-287`, `:329-373`, `:390-404` | `normalize_state` (float32), `NormalizationStats.from_json_obj`, `_returns_to_go` | reused; T-frozen recomputes both by its own route |
| C3 | `offline/joint_windows.py:105-183`, `:186-217` | `build_joint_index(dataset, node_ids)` groups by `item_meta` in the CALLER's order and refuses missing, extra and incomplete; `stack_joint` | reused with `node_ids = payload["intersection_ids"]` |
| C3 | `agent/DTAgent.py:214`; `offline/dt_gate.py:205`, `:587` | `action_loss`, `mean_ci95`, `stack_dataset` | reused |
| C5 | `offline/transfer_curve.py:303-401` | `STAGE_GRID4X4`, `grid4x4_cells()` (700), `declared_cells` | P7.3c's three stages BESIDE them; nothing of P7.3d's changes |
| C5 | `offline/transfer_curve.py:451-462` | `cell_chunk_name`: no stage in the name | unchanged — stage 1's names equal P7.3d's, so P7.3c uses its OWN work dir, and the stage in the identity (`:2042-2048`) separates the two |
| C5 | `offline/transfer_curve.py:618-620`, `:2049-2074` | `DECLARED_ARM_NAMES` excludes `b_mean_k5` and `b_mean_k20`; the grid4x4 arm set is one subject under `b_mean_k100` plus two anchors | a branch keyed on the P7.3c stages admits A24(a)'s four pairs there and nowhere else |
| C5 | `offline/transfer_curve.py:1010-1030` | `load_grid4x4_targets`: k100 only, and refuses any role but `registered_prompt` | unchanged; a k-parameterised twin whose role check admits `recorded_not_evaluated` for the four pairs only |
| C5 | `offline/transfer_curve.py:1119-1199` | `grid4x4_checkpoint_identity` (A20(a)'s pins); `checkpoint_identity_for` refuses every grid4x4 subject but the zero-shot one | a branch for P7.3c's trained subjects, digest-checked against the committed training record |
| C5 | `offline/transfer_curve.py:2358-2603` | `run_cell`'s grid4x4 DT branch (`:2405-2415`); `dt_choose` without a budget (`:2459`) | P7.3c branch: targets per the arm's k, identity per subject, the F4 guard, B passed; zero-shot cells unchanged |
| C5 | `offline/transfer_curve.py:2293-2343` | `chunk_is_reusable` re-derives the k100 targets (`:2337-2340`) | re-derives the cell's own arm's targets |
| C5 | `offline/transfer_curve.py:2856-2884`, `:4773-4790`, `:5394`, `:5418`, `:5431` | `declarations_for`, `artifact_name_for_stage`, the three `--stage` choices | the P7.3c declaration, `p7_3c_grid4x4.json`, the new stage names |
| C6 | `offline/transfer_curve.py:2887-3130` | `report`'s grid4x4 path; body `_grid4x4_artifact` (`:4331-4426`) through `_grid4x4_estimates` (`:4146-4269`) | P7.3d's path byte-identical (`DEFERRED` 95 pins it); P7.3c's body is NEW, beside it |
| C6 | `offline/transfer_curve.py:3489-3501`, `:3880-3898` | `_seed_means_by_draw`, `_definition_stats` (`mean_ci95`) | reused for ρ̄ per arm, exactly P7.3d's estimator |
| C6 | `tests/test_t_regress_artifacts.py:84-144`; `tests/test_p7_3d_campaign_path.py:1882-2031`, `:2345-2369` | `_regenerates` with A3's two substitutions; `_complete_set`, `_write_stubs`, `_a23_record` and the three A23(f) variants | IMPORTED by two NEW tests (`DEFERRED` 95 and 94); the existing files are not edited |

## 5. C1 — the corpus door and A17(f)'s gate

**(i) The alignment by scenario.** `collect.py:714-717`: `declared_alignment()` → `alignment_for_scenario_key(scenario_key)`,
where `scenario_key` is the sim-config stem (`:643`). For hz1x1 the lookup returns `declared_alignment()` — the same call and
the same two constants. T-regress (a) asserts `alignment_for_scenario_key("cityflow1x1") == declared_alignment()` (a frozen
dataclass, so `==` compares fields). `alignment_provenance` (`:722-739`) keeps its shape for both scenarios. grid4x4 needs
`RLTRAFFIC_GRID4X4_RESCO` (`aligned_env.py:232`).

**(ii) Policy on raw info, logger on the aligned view — kept, as it is today** (`:865-870`). The scenario key and the ids
come from `--env-config configs/sim/cityflow_grid4x4.json`, so `run_metadata.scenario_id` is `cityflow_grid4x4`
(`_run_metadata`, `:606`). The logger's ids come from `env.intersections`, which are A0…D3 (V5). A corpus under another key
or other ids is refused by the gate (iv) and again by the fine-tune (§7).

**(iii) `DEFERRED` 93's fix — the env's per-step hook, not SUMO's statistics file.** For a SUMO draw sweep the door builds
each draw's env with `make_observer_sumo_env(draw_config, draw_spec.settings, halting_check=False)`. That builds
`PerSecondSumoObserver`, whose `_simulate` reads `getStartingTeleportIDList()` and `getCollisions()` after EVERY
`simulationStep()`, keeping `metrics.on_sim_step()` where the frozen env puts it (`sumo_att_reference.py:1020-1055`;
`envs/sumo_env.py:229-231`).

- **Why this route:** it is the one A23's evaluation cells already use. It changes no file under `envs/`. The alternative,
  `--statistic-output`, can only reach SUMO by patching `traci.start`, as the coordinator's one-off re-verify did
  (`output/p7_3d_runs/probe_reverify_20260924/probe_reverify.py`), and that is not a production route.
- **The count and the refusal:** after the episode, `dense_engine_events(env)` reads `recorder.n_teleport_events` and
  `recorder.collision_record()` and records `n_teleports` and `n_collisions` (with a grain label) in the draw's `sumo_draws`
  entry.
- Either count non-zero → `ValueError` naming the draw and both counts, **before `logger.finalize_episode()`** (A9). Nothing
  of that episode is written, the manifest is not rewritten, and the run exits non-zero.
- The recorder's own per-second refusals (the pool identity, `:648-661`) also stop the run, as a finding.

**(iv) A17(f)'s gate, per intersection, keyed by ID:** `assert_logged_corpus_matches_probe_per_intersection(corpus_dir,
artifact_path, *, draw_ids=range(201, 301))`, beside the hz1x1 function. Its steps, in order:

1. The artifact's sha256 must equal the pin before it is parsed. That pin is `transfer_curve.P7_3D_CALIBRATION_SHA256`,
   imported lazily; one pin, never retyped.
2. The manifest must be `1.1` with `scenario_id == "cityflow_grid4x4"`, and every `sumo_draws` entry must record dense
   `n_teleports == 0`, `n_collisions == 0`, `vehicle_types_seen == ["cf_parity"]` and `time_to_teleport_option == "-1"`.
3. There must be exactly one episode per draw, and the draw set must be exactly the requested one.
4. Per episode, the npz's `ix_ids` set must equal the artifact's. **For each id, its index is looked up in THAT npz's
   `ix_ids`**, never taken from a position, and `ix{i}_local_reward` is checked for integrality first. Then
   `float(np.sum(rewards.astype(np.float64)))` must `==` `probe_returns.sumo[str(draw)][id]`.
5. Any mismatch refuses, naming the FIRST draw and intersection and the count.
6. The function returns `{n_draws 100, n_checked 1600, n_matching, all_match, artifact_sha256, rows}` and writes nothing;
   the driver writes it (C2).
7. `engine_seed_drawn` is recorded per draw and all 100 must be equal. **Q14:** whether the gitignored probe chunks should
   also be consulted.

**(v) Disjointness before the first episode:** `grid4x4_corpus_disjointness_record(draw_ids, *, output_root)`. It calls
`subject_facts_per_intersection` (which digest-checks A20(a)'s five and reads their `stats.draw_ids`) and then
`assert_probe_draws_disjoint(draws, training_draw_ids=facts.training_draw_ids, held_out_draws=HELD_OUT_DRAWS)`. The C2 driver
runs it before the token, the gate re-runs it, and the fine-tune asserts it again. **Q21:** whether it must also live inside
`collect.py`, which is scenario-agnostic today.

**The corpus is collected by one function, so the settings cannot drift:** `collect_logged_probe_corpus(scenario_key,
draw_ids, out_dir, draws_root)` builds the argv
`--backend sumo --env-config configs/sim/<key>.json --policy maxpressure --flow-draws-range 201 301 --episodes 1 --base-seed
1000 --out-dir … --draws-root … *COLLECT_SETTINGS` and calls `collect.main(argv)`. It never passes `--overwrite`. T-17f calls
the same function for draw 201.

**Files:** `offline/collect.py`, `offline/transfer_calibration.py` (the functions above plus three subcommands on its existing
CLI, `build_parser` `:2143`, `main` `:2453`: `corpus-disjointness`, `collect-corpus`, `corpus-gate`), and
`tests/test_p7_3c_corpus_door.py`. The layout is C6 v1.1 (unchanged format): one directory
`datasets_sumo_v11/grid4x4_sumo_maxpressure/` holding 100 `.npz`, `manifest.json` and `SHA256SUMS` (101 lines, `sha256sum`'s
format, as hz1x1's).

## 6. C2 — the corpus driver (`offline/campaigns/p7_3c_corpus.sh`)

**Shape:** `p7_3d_grid4x4.sh`, the corrected one, rather than `p7_3d_probe.sh`, which still hardcodes `WORK_TREE`
(`p7_3d_probe.sh:56`) and documents the one-line tmux form (`:8`).

**Header:** the two-step foreground start (B.3-1) with the `PIPESTATUS` echo (B.5-3), from a DETACHED run worktree
`/home/filip/rltraffic-p73c-run` that the coordinator creates at the reviewed, pushed commit (J1(e)).

**Order — every refusal before the token:**

1. The interpreter.
2. `WORK_TREE` derived from `${BASH_SOURCE[0]}` with `pwd -P`, refused when it IS `/home/filip/rltraffic-p73c`.
3. `cd` to the main tree, then the `-P` import check of `offline.transfer_calibration` and `offline.collect` from `WORK_TREE`.
4. Liveness: `pgrep -f 'python.*offline\.(collect|transfer_calibration|transfer_curve|few_shot|g2_measure)'` → exit 3.
5. Group leader, then SigIgn.
6. The RESCO net, the 100 parity configs, and the committed calibration artifact BY DIGEST.
7. The dirty tree.
8. **The corpus directory must not exist, or must be empty** (the barrier; never `--overwrite`).
9. Free memory.
10. `corpus-disjointness` (§5(v)).
11. The canary, both halves (the 2.0 s timing half in the shell).
12. Trap, then the token `output/p7_3c_runs/TOKEN_corpus`, then `record-canary` into `output/p7_3c_corpus/canary.json`.

**After the token:**

1. `collect-corpus`.
2. `SHA256SUMS` written atomically (tmp → `mv`) over the 100 `.npz` and `manifest.json`, then re-verified with `sha256sum -c`.
3. **`corpus-gate` as the LAST stage** → `output/p7_3c_corpus/a17f_gate.json`, printing `A17(f) 1600/1600 MATCH` or
   refusing.
4. The `COMPLETE` or `FAILED` marker in `output/p7_3c_corpus/`.

It prints no return. Capture: `tee -a output/p7_3c_runs/corpus_capture.txt` from the first line; `set -euo pipefail`; `-P` on
every interpreter call.

**`WORKERS` — I argue for ONE (sequential), against `BRIEF_41`'s default of 12.**
- `collect.py` is one process by design: one `TrajectoryLogger` whose manifest is rewritten in full, atomically, on every
  episode (*"one `out_dir` belongs to one process"*, `trajectory_logger.py:398`).
- A pool would need a merge step writing one manifest from several runs — new code on the corpus path, which T-regress (a)
  would have to cover.
- The cost is ≈ 1–1.5 h (A10), which makes it a run over an hour; **Q11** asks whether G1 counts as its pre-flight.
- The schedule in the header is T-17f's measured episode time with its canary, never an estimate.

## 7. C3 — the warm-start fine-tune (`offline/few_shot.py`, new)

**The route, every step validated before any work, and the only write last (the barrier).**

1. **Arguments.**
   - The REGISTERED table has exactly thirty runs: `ft` × k ∈ {5, 20, 100} × B 4,000 (15); `scratch` × k 100 × B 4,000 (5);
     `ft` × k 100 × B ∈ {1,000, 16,000} (10). Seeds 101…505. The regime is CUDA, non-deterministic.
   - The FENCED timing run is `ft` k 5, B **400**, seed 101, under a `fenced_timing/` directory only.
   - The core function is generic, so tests can call it on CPU with small B. The CLI's registered path accepts only the
     table and refuses a device other than `cuda`.
   - The destination must NOT exist and must not be under `fenced_timing/` for a registered run (or must be under it for a
     timing run). Its parent must exist; nothing creates it.
2. **The source.**
   - The file's sha256 must equal A20(a)'s pin for that seed (`transfer_calibration.GRID4X4_CHECKPOINT_SHA256`, `:543-549`).
   - It is loaded with `torch.load(weights_only=True)` under `torch.serialization.safe_globals([TorchVersion])`.
   - Checks: `spatial-dt-checkpoint/1.0`; `spatial_mixing` False; the mask is the identity; `gradient_steps` 40,000.
   - **`learning_rate`, `weight_decay` and `grad_clip` must `==` the imported constants, and `batch_size` must `==` 64**, or
     the run is refused.
3. **The targets.**
   - The calibration artifact's sha256 must be pinned, then `target_rtg[ix] = per_intersection[ix].budgets[f"k{k}"].target`
     for all 16.
   - Each budget's `draw_ids` must equal `[201, 200 + k]`.
   - Roles: k100 `registered_prompt`; k5 and k20 `recorded_not_evaluated`, admitted HERE, for these checkpoints only
     (A24(b)).
4. **The prefix, from the manifests.** `select_prefix(corpus_dir, k)`:
   - `SHA256SUMS` re-verified entry by entry, with the file set exactly 100 `.npz` + `manifest.json`.
   - Manifest `1.1`, and `scenario_id ==` the source's `scenario_id`.
   - It returns exactly `range(201, 201 + k)` from the manifest's `flow_draw` values, one episode each. A missing draw is
     refused BY NAME; draws beyond the prefix are ignored.
   - Then disjointness against the source's `stats.draw_ids` and the held-out pool.
5. **The windows.**
   - `TrajectoryWindowDataset([corpus_dir], context_length=config.context_length, split="train", draw_ids=prefix,
     stats=NormalizationStats.from_json_obj(source["stats"]), normalize=True)`. The CHECKPOINT's statistics are supplied, so
     nothing is fitted (`dataset.py:663-669`).
   - There must be one `(state_dim, n_actions)` group equal to the config's, and the loaded episodes' draws must be exactly
     the prefix.
   - `index = build_joint_index(dataset, source["intersection_ids"])` puts the corpus in the CHECKPOINT's node order: a
     different on-disk order is re-keyed by id, and a missing or extra id is refused (**T-order's decision: re-key**).
   - Then `stack_joint`.
   - The largest RTG timestep must be `< config.max_ep_len`.
6. **The model.**
   - `Utils.seed_everything(seed, seed_python_random=False)` (F7).
   - `config = SpatialDTConfig.from_json_obj(source["config"])` — **`max_ep_len` 360 from the payload, never from the data**.
   - `model = SpatialDecisionTransformer(config).to(device)`.
   - `init == "source"`: `model.load_state_dict(source["model"], strict=True)`. `init == "scratch"`: nothing — the weights are
     the fresh initialisation under the seed, which is the SAME construction, so the torch RNG is in the same state in both
     arms after it.
7. **The recipe, unchanged where P5.2 has one.**
   - A FRESH `AdamW(lr=LEARNING_RATE, weight_decay=WEIGHT_DECAY)`.
   - `LambdaLR(lambda s: lr_multiplier(s, warmup_for(B)))` — 500 at B = 1,000, and 1,000 at B = 4,000 and 16,000.
   - `clip_grad_norm_(GRAD_CLIP)`.
   - `generator = np.random.default_rng(seed)`; rows `generator.integers(0, count, size=64)` — uniform with replacement over
     joint instants.
   - Per step: `rtg / scale` with `scale[n] = source["rtg_scale"][node_ids[n]]` (FROZEN, checkpoint order), the mask
     `source["spatial_mask"]` (never recomputed), and `action_loss`. The `train_tier_dt` step order: `zero_grad(set_to_none)`,
     `backward`, clip, `step`, `schedule.step()`.
   - **Exactly B steps**, counted in the loop and asserted `== B` before the save.
8. **The payload** (F1), written once through `write_payload_exclusive` (tmp → `os.link` → unlink, which refuses an existing
   destination):
   - **Copied from the source:** `format_version: "spatial-dt-checkpoint/1.0"`, `config`, `stats`, `rtg_scale`,
     `intersection_ids`, `spatial_mask`, `normalise` and `scenario_id` — the same Python objects read from the source,
     unchanged.
   - **New:** `model` is the fine-tuned CPU state dict, and `target_rtg` is the k targets.
   - **`provenance`:**
     - `few_shot` — `format_version few-shot-checkpoint/1.0`, `run`, `arm`, `init` ("source" or "scratch", THE SWITCH), `k`,
       `draw_ids`, the target rule and role.
     - The source — its path and sha256, `source_seed`, `source_gradient_steps 40000`, the source's own `provenance`, and the
       source's CityFlow `target_rtg` (kept, A24(b)).
     - The corpus: `corpus_dir` and the sha256 of its `SHA256SUMS`; the `calibration_sha256`.
     - The recipe: `gradient_steps B` (what `assert_declared_budget` reads), `batch_size 64`, `learning_rate`, `weight_decay`,
       `grad_clip`, `warmup_steps`, the sampler's description, `seed`, the seeding call.
     - The model and machine: `method dt_nomix_h4`, `n_head 4`, `spatial_mixing False`, `device`, `deterministic False`, the
       git commit, and the runtime versions.
     - **No wall-clock value in the payload**, so that two same-seed runs can agree byte for byte. The seconds and the loss
       trace go to the training record.
9. **No evaluation and no selection:** the payload after exactly B steps is the only one written.

**The frozen quantities — where each comes from, and the test that proves it frozen:**

| Frozen | Source | Test / mutation |
|---|---|---|
| weights at step 0 (fine-tune) | `source["model"]`, strict | T-warm / skip `load_state_dict` |
| `max_ep_len` 360 | `source["config"]` | T-warm / derived from a corpus truncated to 300 decisions |
| state mean, std | `source["stats"]`, through `stats=` | T-frozen / `stats=None` (refit) |
| `rtg_scale[ix]` | `source["rtg_scale"]`, checkpoint order | T-frozen / refit from the corpus |
| node order, mask | `source["intersection_ids"]`, `["spatial_mask"]` | T-order / windows in the corpus's order |
| recipe constants | imported, asserted `==` the provenance | T-recipe / B − 1 steps; warm-up 1,000 at B = 1,000 |
| target per k | the calibration artifact's `k{k}` | T-payload / k100's targets for k5 |

## 8. C4 — the training driver (`offline/campaigns/p7_3c_finetune.sh`) and G5's fenced timing

**Two modes.** `timing` (G5, no token, fenced) and `train` (G6's token, the thirty). The shape, the run worktree, `-P`,
liveness, the group leader, SigIgn, the dirty tree, `set -euo pipefail` and `tee -a` are all as §6.

**Inputs, checked by digest:** the calibration artifact; the five sources (A20(a)); the corpus's `SHA256SUMS` (`sha256sum -c`)
and G3's gate record; `torch.cuda.is_available()`; free GPU memory against G5's measured peak; the canary, both halves (it is
a SUMO canary: host health, not a GPU rate — `PROJECT_PLAN` §8's 2026-09-19 row). Then trap, token
(`output/p7_3c_runs/TOKEN_finetune`), `record-canary`, and the runs.

**Resume, decided in Python, never by `[ -f ]`:**
- `few_shot resume-decision --run R` returns *skip* for a checkpoint that exists AND validates (frozen parts `==` the
  source's, B, the k targets, the switch).
- It returns *train* when the checkpoint is absent.
- It REFUSES when the checkpoint exists but does not validate — never overwritten; a human moves it aside.
- Before each run the driver writes an attempt marker (`output/p7_3c_training/attempts/R.<n>`). A run found with a marker and
  no checkpoint is a re-run of an infrastructure failure, **counted on disk** as well as in the capture.

**After the thirty:** `few_shot manifest` writes `output/SHA256SUMS_p7_3c_finetune.txt` atomically and re-verifies it.
`few_shot record` writes `output/p7_3c_training/p7_3c_finetune.json`, format `p7.3c-finetune-record/1.0`. The record carries:
- per run: name, arm, init, k, B, seed, checkpoint path and sha256, weights-only digest (`tier_sweep.canonical_state_dict_digest`,
  `:1234`), source sha256, the frozen-part checks as booleans, steps, warm-up, loop seconds, final loss, attempts and re-runs;
- the corpus and calibration digests;
- G5's timing record.

It prints no outcome.

**G5 — the concurrency rule, FIXED NOW, before any measurement:**
1. The fenced run (k 5, seed 101, B 400) runs ALONE; then 2 at once; then 3 at once; then ONE same-seed repeat of the single
   run, alone.
2. Recorded for each: ms/step (loop seconds / 400); peak GPU memory per process (`torch.cuda.max_memory_allocated`) and for the
   device (`nvidia-smi`, sampled); the slowdown relative to alone.
3. The repeat's verdict comes by TWO routes: the file sha256, and `canonical_state_dict_digest` (weights only).
4. **Proposed addition (Q12):** one fenced BUILD-ONLY measurement of the k = 100 windows (no step), because P5.2's 122 ms/step
   covers the loop and not the build.
5. **The driver's concurrency C** is the C ∈ {1, 2, 3} with the largest aggregate throughput (C / ms-per-step at C) whose
   measured device peak is ≤ 80 % of 16,303 MiB; a tie goes to the smaller C.
6. The numbers go into the header, this plan and the packet, each with its canary and date.
7. The timing checkpoints stay under `output/p7_3c_training/fenced_timing/<UTC>/`; the evaluation path refuses that directory
   (C5).
8. **Q22:** G5 says *"you, in tmux"* — I start that pane myself for these few minutes unless the author prefers to.

**Files:** the driver, plus `few_shot.py`'s driver-facing subcommands (`timing`, `resume-decision`, `manifest`, `record`,
`check-inputs`), plus `tests/test_p7_3c_finetune_driver.py`. The committed record is **Q9**.

## 9. C5 — P7.3c's three stages, beside P7.3d's (`offline/transfer_curve.py`)

**Constants:**
- The stages: `STAGE_P7_3C_REPRODUCE = "p7_3c_reproduce"`, `STAGE_P7_3C_PRIMARY = "p7_3c_primary"`,
  `STAGE_P7_3C_CONTROLS = "p7_3c_controls"`, and a report selector `STAGE_P7_3C = "p7_3c"` meaning the whole declaration.
- The pins: `P7_3D_GRID4X4_SHA256 = "c63c371f…"` (full hex); after G7, `P7_3C_FINETUNE_SHA256`.
- `G = 0.11453573207299894` and `G_ATT = 0.11453996971792169`, literal constants, never recomputed.
- The A24 arm table. Its subjects are **Q8**:

| A24 arm | stage | subject (cell) | arm (prompt) | checkpoint | declared B |
|---|---|---|---|---|---|
| `zs_k100` (= ρ₀'s arm) | reproduce | `mappo1000_dt_nomix_h4` | `b_mean_k100` | A20(a)'s five | 40,000 |
| `ft_k5` · `ft_k20` · `ft_k100` | primary | `ft_k5` · `ft_k20` · `ft_k100` | `b_mean_k5` · `k20` · `k100` | training record | 4,000 |
| `zs_k5` · `zs_k20` | controls | `mappo1000_dt_nomix_h4` | `b_mean_k5` · `b_mean_k20` | A20(a)'s five | 40,000 |
| `scratch_k100` | controls | `scratch_k100` | `b_mean_k100` | training record | 4,000 |
| `ft_k100` at B = 1,000 · 16,000 | controls | `ft_k100_b1000` · `ft_k100_b16000` | `b_mean_k100` | training record | 1,000 · 16,000 |

**The declaration, 4,700 cells:**
- Stage 1: P7.3d's 700 re-labelled with stage `p7_3c_reproduce`, i.e. 500 DT and 200 anchors.
- Stage 2: 1,500 DT cells, no anchors — ρ uses stage 1's anchors of the same draw.
- Stage 3: 2,500 DT cells.
- Every cell carries `scenario cityflow_grid4x4`.
- `declared_cells`, `declarations_for` (whole = all 4,700), `artifact_name_for_stage` (`p7_3c_grid4x4.json`) and the three
  `--stage` choices gain the new names.
- P7.3d's `grid4x4_cells()` and its stage are untouched.

**The admission branch — one predicate, used by all four sites:** `validate_cell_payload` (`:2049-2074`), `run_cell`
(`:2405-2415`), `chunk_is_reusable` (`:2337-2340`) and `report`. On a P7.3c stage:
- A DT `(subject, arm)` must be in the table FOR THAT STAGE.
- `b_mean_k5` and `b_mean_k20` are admitted only for `(ft_k5, b_mean_k5)`, `(ft_k20, b_mean_k20)` and `(zero-shot, b_mean_k5 /
  b_mean_k20)` — A24(a)'s four, on P7.3c's stages and nowhere else.
- Anchors appear only in stage 1.

On P7.3d's stage everything is refused exactly as today.

**Targets:** `load_grid4x4_targets_for(prompt_arm, *, subject, stage, data_dir)` reads `k{k}` from the pinned artifact. The role
check admits `recorded_not_evaluated` only for the four pairs. `load_grid4x4_targets` itself is unchanged.

**The trained identity:** `p7_3c_trained_checkpoint_identity(subject, seed, *, output_root, data_dir)`.
- Paths come ONLY from the committed training record, and any path under `fenced_timing/` is refused (T-driver's last clause).
- The file's sha256 must `==` the record's, and it must be listed identically in `output/SHA256SUMS_p7_3c_finetune.txt` when
  that manifest exists.
- Then F4's provenance guard: the weights-only load; the `few_shot` block; `init`; source sha256 `==` A20(a)'s pin for the seed
  (for `init == "source"`) or `init == "scratch"`; `gradient_steps == B`, the run's declared B; `k` equal to the arm's k; the
  payload's `target_rtg ==` the k targets.
- `run_cell` then calls `dt_choose(..., target_rtg=k_targets, declared_gradient_steps=B)`.
- The zero-shot subject's cells take P7.3d's path, unchanged.
- The chunk format stays `p7.3d-grid4x4/1.1`, because the record shape is identical and the stage separates the campaigns.
  The artifact gets `p7.3c-grid4x4/1.0`. **Q8.**

**The stage-1 gate:** `stage1_reproduction_check(work_dir, data_dir)`, CLI `stage1-check`.
1. The zero-shot artifact's sha256 must `==` `P7_3D_GRID4X4_SHA256` FIRST.
2. All 700 stage-1 cells must exist, each validated.
3. Each chunk's row is built as F2 says.
4. The artifact's records are keyed by `(kind, subject, arm, seed, draw_id)`.
5. For every one of the 700, EVERY key of the RECORD except `git_commit`, `stage`, `seconds`, `canary_seconds`, `checkpoint`
   and `sha256_checked_against` is compared under `==`. A key the row lacks refuses by name.
6. The output is one line — `stage1_check REPRODUCED 700/700`, or `NOT REPRODUCED n` with the first cell and the differing
   field NAMES (never a value) — and exit 0 or 2.

## 10. C6 — P7.3c's report body

`report --stage p7_3c` → `output/p7_3c/artifacts/p7_3c_grid4x4.json`, format `p7.3c-grid4x4/1.0`. **Each numbered step is a
refusal before ANY write:**

1. A caller-supplied cell set may not write `docs/data/` (the existing guard, `:2935-2945`).
2. The zero-shot artifact's sha256 `==` `c63c371f…`, then the calibration pin, then the training-record pin.
3. `canary.json`, re-checked (`_read_canary_record`).
4. Every chunk validated; name == content; completeness against the 4,700 (A24(c): **no estimator on a partial set**); an
   undeclared chunk refused; a chunk whose stage is P7.3d's refused by the cell identity.
5. **All 700 stage-1 rows reproduce under the six-field rule** — the same function as C5's gate.
6. Stage 1 records exactly A23's two events (F5).
7. The digests from disk: demand per draw; checkpoint per `(subject, seed)` through the one identity function — a trained
   chunk whose digest is not in the training record refuses; the targets per the arm's k; the support ranges.
8. Pairing per draw: anchors from stage 1; ρ per cell (`_rho_pair`).

**Then the estimators, A24(d)–(e) exactly:**
- **ρ̄ per arm** on `E_sumo` and `att_env`: per-draw five-seed means (`_seed_means_by_draw`), then `_definition_stats`
  (`mean_ci95`, n = 100).
- **ρ₀(d) is read from the artifact's `rho.by_draw`**, never from the chunks.
- **Δ_k, paired BY DRAW ID:** per-draw differences over the draws present in BOTH series; a draw in one and not the other
  refuses; `mean_ci95` over the differences in draw-id order gives Δ, lo = Δ − ci, hi = Δ + ci, all in float64.
- **The verdict partition:** (i) lo > 0 and Δ₁₀₀ ≥ G/2; (ii) lo > 0 and Δ₁₀₀ < G/2; (iii) lo ≤ 0 ≤ hi; (iv) hi < 0. G is the
  literal. The output is the label and A24(d)'s sentence for that outcome, with X% [CI] filled from the closure fraction.
- **Beside the verdict, never deciding it:**
  - the closure fraction Δ₁₀₀ / G with the CI [lo / G, hi / G];
  - the same computation on `att_env` with G_att;
  - the five per-seed Δ₁₀₀ (Q6b), their CIs, the between-seed SD (ddof 1), and the count of seeds meeting each of (i)'s two
    conditions;
  - **A23(d)'s robustness:** every draw on which any cell entering Δ₁₀₀ records a collision is removed WHOLE — `ft_k100`,
    stage 1's `b_mean_k100` and both anchors; Δ₁₀₀, its CI and the verdict are recomputed by the SAME function; whether the
    outcome changes is recorded.
- **The adaptation effects** ρ̄(`ft_k`) − ρ̄(`zs_k`) paired by draw; at k = 100 that IS Δ₁₀₀, computed once and so stated.
- **Δ_transfer** = ρ̄(`ft_k100`) − ρ̄(`scratch_k100`), paired.
- **A24(e)'s five adjacent steps**, each paired per draw and refuted iff hi < 0:
  - the total effect across 0→5, 5→20, 20→100;
  - the adaptation effect across 5→20, 20→100;
  - the sentence *"five steps, each at 95 % without correction, read jointly"*.
- **The budget secondary, labelled and apart:** ρ̄ at B = 1,000 and B = 16,000, each one's Δ against ρ₀, and its paired
  difference against B = 4,000.
- **The in-support diagnostic** of every trained arm's k targets, per intersection, against BOTH the source's training range
  (`support_range`) AND the fine-tune corpus's episode-return range over draws 201…200+k (**Q13**). The chunks' per-decision
  counts against the source range are kept as P7.3d's.
- **The collisions block**, per arm.
- **`what_this_does_not_say`:** A24(f) (exploratory; no whole-H3 verdict), A24(g) (scope, and what each omission costs), and
  A24(h) (hz1x1 untouched).
- Then `_publish_artifact` (the fenced-key scan, the atomic write).

**Due in this commit:**
- **`DEFERRED` 95** — `tests/test_p7_3c_regress.py::test_p7_3d_grid4x4_regenerates_byte_identically` imports `_regenerates`
  and runs `stage="grid4x4_confirmatory"` over `output/p7_3d/cells`. Mutant: one aggregate of `_grid4x4_estimates` perturbed →
  it must die.
- **`DEFERRED` 94** — a variant with TWO events in the fixed-time draw-1020 cell, built from the imported `_complete_set` →
  `report` refuses and nothing is written. Mutant: `!= 1` → `< 1` at `transfer_curve.py:1903` → it must die.
- The three hz1x1 T-regress tests stay byte-identical.

## 11. C7 — the campaign driver (`offline/campaigns/p7_3c_grid4x4.sh`)

P7.3d's driver's shape (`offline/campaigns/p7_3d_grid4x4.sh`).

**Before the token:**
- `check-inputs`: the committed artifacts by digest, including the zero-shot artifact; A20(a)'s five; the thirty against the
  committed training record, frozen parts included; RESCO's net.
- The dirty tree; P7.3d's G2 RSS budget (24,216 MiB — the architecture is the same, and a different measurement is a
  finding); `resume-check`; the canary, both halves.
- **`dt-reroll-check` and `reference-reroll-check`, kept from P7.3d's shape (Q18)**, fenced under `output/p7_3c/g2/`.

**Then:** trap → the token (`output/p7_3c_runs/TOKEN_campaign`, ONE for all three stages) → `record-canary` →
`cells --stage p7_3c_reproduce` → **`stage1-check`: automatic, with no human step; anything but REPRODUCED writes `FAILED` and
stops before stage 2's first cell** → `cells --stage p7_3c_primary` → `cells --stage p7_3c_controls`, whatever stage 2 shows →
`report --stage p7_3c` → `manifest` (`output/SHA256SUMS_p7_3c.txt`, over `output/p7_3c/` only) → `COMPLETE`.

`WORKERS` 12. It prints no outcome of any cell.

## 12. C8 — the packet and the artifacts (after G10)

- `docs/data/p7_3c_grid4x4.json`, copied BY HAND with its measured digest.
- `docs/data/p7_3c_finetune.json` (**Q9**: committed by the coordinator at G7).
- `docs/returns/P7.3c.md` per `BRIEF_41` §7, including the AI-assistance record and the amendments written against, by
  letter.

Then *"P7.3c done"*.

## 13. The tests of §4, mapped to commits — every one red first, for its own reason

| Test | Commit | What it pins | Mutation(s), each executed and pasted |
|---|---|---|---|
| T-regress (a) | C1 | `alignment_for_scenario_key("cityflow1x1") == declared_alignment()`; hz1x1 draw 201 through the CHANGED door into `tmp_path` → every array `==` `hz1x1_sumo_maxpressure/ep000000_seed1000_draw201.npz` (SUMO, draws and corpus gated) | the lookup returns hz1x1's alignment for every key → T-17f dies |
| T-17f | C1 | grid4x4 draw 201 through `collect_logged_probe_corpus` → the per-id gate `==` the artifact on all 16; dense `n_teleports 0`, `n_collisions 0` (SUMO, RESCO, draws gated). **Plus** a synthetic corpus (no SUMO) whose `ix_ids` run D3…A0 with the artifact's returns placed BY ID → passes; one return off by 1 → refuses naming the draw and the id. **Plus** the stub fixture (the `object.__new__` observer + a scripted connection, `tests/test_p7_3d_collisions.py:263-361`'s pattern): a collision at second 7 of a 10-second decision → `dense_engine_events` counts 1 → the door refuses and NO `.npz` is written | the gate keyed by index → the reversed-id corpus dies; the counter read once per decision (one `getCollisions()` at the boundary) → the second-7 fixture dies |
| T-driver (corpus) | C2 | comment-free text: the canary before the token, `record-canary` after, `-P` on every `"$PY"`, `tee -a` in the usage, `set -euo pipefail`, the refusal over a non-empty corpus dir — EXECUTED on a sandbox copy with no token, asserting exit 2 and nothing written; the gate is the last stage | the non-empty-dir refusal removed → the executed test dies (the missing token still stops it: B.7-3's rule) |
| T-warm | C3 | real subject (checkpoint gated): before any step, the state dict equals the source's under `torch.equal`, tensor by tensor; `max_ep_len` 360 on a synthetic corpus of 300-decision episodes | skip `load_state_dict`; `max_ep_len` from the data |
| T-frozen | C3 | the batch the model sees (through `joint_batch`, the loop's own seam) against `(raw − mean) / std` computed in float32 from the `.npz` with the CHECKPOINT's arrays, per intersection, bitwise; RTG `==` the reversed `np.cumsum` of the stored rewards / `rtg_scale[ix]` | `stats=None`; `rtg_scale` refit |
| T-prefix | C3 | `select_prefix` on a 100-episode synthetic corpus of 3-decision episodes: k 5 / 20 / 100 → exactly 201–205 / 201–220 / 201–300; draw 203 missing → refused BY NAME; 206 present stays out of k = 5 | `range(201, 201 + k + 1)` |
| T-recipe | C3 | exactly B steps, counted by a GLOBAL optimizer post-hook; `warmup_for` 500 / 1,000 / 1,000; the LambdaLR lr at steps 0, w − 1 and B − 1 for B = 1,000, 4,000 and 16,000 on a one-parameter optimizer; the optimizer state empty at step 0 (pre-hook); a source whose provenance `lr` differs → refused | B − 1 steps; warm-up 1,000 at B = 1,000 |
| T-order | C3 | a corpus logged with ids in REVERSED order → the joint columns follow the checkpoint's order (checked through `item_meta`); one id absent → refused | windows in the corpus's order |
| T-payload | C3 | `config`, `stats`, `rtg_scale`, `intersection_ids`, `spatial_mask` `==` the source's; `target_rtg[ix] ==` the JSON's `k{k}` target for every ix, read by the test; every provenance field §7 names; **and the payload LOADS through `spatial_agent_with_targets` on a 16-id fake env** (F1) | k100's targets written for k5 |
| T-cpu-determinism | C3 | synthetic source, B = 20, CPU: the same seed twice gives identical state dicts; a different seed does not | — |
| T-scratch | C3 | the same seed, `ft` against `scratch`: at step 0 the scratch weights ≠ the source's and `==` a fresh model under `seed_everything(seed, seed_python_random=False)`; stacked windows, statistics, `rtg_scale`, target, the drawn rows (recorded through the sampler seam) and the recipe all `==`; the payloads differ ONLY in `model` and `provenance.few_shot.init` | scratch still loads the source; scratch refits statistics |
| T-driver (training) | C4 | the text clauses; the resume decision (a valid checkpoint → skip; an invalid one → refuse; absent → train; a marker without a checkpoint → counted as a re-run); `fenced_timing/` refused by C5's identity | each refusal removed → dies |
| T-stage1 | C5 | on committed-record fixtures: a row differing ONLY in the six fields passes; one differing in `e_sumo`, `n_teleports`, `checkpoint_sha256`, `engine_seed_drawn` or `local_return` refuses and names it; a row MISSING a record key refuses | `stage` compared; the loop over the ROW's keys |
| admission | C5 | the four pairs admitted on P7.3c's stages only; `(ft_k100, b_mean_k5)` refused; `b_mean_k5` on P7.3d's stage refused; the 4,700 declared (700 / 1,500 / 2,500); trained identity: a digest not in the record, a path under `fenced_timing/`, B ≠ the declared B, and a source digest ≠ A20(a)'s — each refused | each check removed → dies |
| T-verdict | C6 | synthetic per-draw differences at every boundary: Δ = G/2 exactly with lo > 0 → (i); lo = 0.0 → (iii); hi = 0.0 → (iii); hi < 0 → (iv); G and G_att read from the constants | `>=` → `>`; `lo > 0` → `lo >= 0`; `hi < 0` → `hi <= 0`; G recomputed → a fixture with another ρ₀ mean dies |
| T-perseed | C6 | the five per-seed Δ₁₀₀ recomputed by the test's own route (per-seed means over draws of cell-level differences) `==` the report's; a fixture with exactly three seeds above G/2 → count 3 | — |
| T-pairing | C6 | permuting one series by draw id changes nothing; a draw present in one series only refuses | zip by position |
| T-report | C6 | refusals precede every write: the zero-shot artifact at another digest; ONE stage-1 chunk not reproducing; ONE stage-2/3 cell absent; a trained chunk at a digest outside the record; a chunk whose stage is P7.3d's; no `canary.json` | the write moved above the last refusal |
| T-regress (b) / `DEFERRED` 95, 94 | C6 | §10 | §10's two |
| T-driver (campaign) | C7 | the text clauses (one token, three stages in order, `stage1-check` between 1 and 2, no outcome printed); **executed on a fixture:** one mismatching chunk among 700 → `FAILED`, no stage-2 chunk written | the gate's exit ignored |

**Suite discipline:**
- One grid4x4 SUMO episode in the whole suite (T-17f), under `BRIEF_41`'s cap of four; one hz1x1 episode (T-regress (a)).
- Every gate `skipif` names its artifact.
- Mutants are COMMITTED in a throwaway worktree, and pytest runs from a script file that does not carry the liveness pattern
  (`BRIEF_41` §4's two traps).
- Evidence goes under `output/p7_3c_runs/`, never `/tmp`.
- The standard line is B.7.3-5's three variables, plus **Q10**'s fourth.
- `scripts/check_test_hygiene.sh` runs on every test file and `scripts/check_english.sh` on everything, each falsified against
  a known-bad input first (§7, 2026-09-15).
- ⚠️ Every gated test adds a CI skip. The ceiling in `.github/ci/ci_baseline.json` is the coordinator's route, not mine (**Q19**).

## 14. Open questions for Amendment A (each with the answer I propose)

- **Q0** — Amendment E's commit did not exist when this session began and is merged now (`019ad68`, §0.3). *Proposal:* this plan stands as G0, written with that commit in its base; I build nothing before Amendment A.
- **Q1** (F1) — the top-level `format_version` stays `spatial-dt-checkpoint/1.0`, with the few-shot identity in `provenance.few_shot`. *Proposal: yes.*
- **Q2** (F2) — the stage-1 comparator builds rows through `report`'s row helper, with ρ from stage 1's anchors. *Proposal: yes.*
- **Q3** (F4) — the budget branch sits at `transfer_curve.py`'s call site; `spatial_mixing.py` is untouched. *Proposal: yes.*
- **Q4** (F5) — A23(f)'s two-event pin applies to stage 1 only; stages 2–3 record and keep, and an unexplained teleport stops the campaign; the robustness set comes from the chunks. *Proposal: yes.*
- **Q5** (F6) — `DEFERRED` 93 is closed for the corpus door; the probe loop is unchanged and the row is rescoped. *Proposal: yes.*
- **Q6** (F7) — `seed_everything(seed, seed_python_random=False)`, as `train_tier_dt`. **Q6b** — per-seed Δ₁₀₀,s = mean over d of [ρ(ft_k100, s, d) − ρ(zs_k100, s, d)], pairing seed with seed (the fine-tune of seed s starts from seed s), with ρ(zs, s, d) read from the ARTIFACT's cell records; SD ddof 1. *Proposal: both yes.*
- **Q7** (F8) — C3–C4 built after G1, while the corpus runs. *Proposal: yes.*
- **Q8** — cell subjects `ft_k5`, `ft_k20`, `ft_k100`, `scratch_k100`, `ft_k100_b1000`, `ft_k100_b16000`; the zero-shot subject keeps its name; P7.3c's chunks keep `p7.3d-grid4x4/1.1`; the artifact is `p7.3c-grid4x4/1.0`. *Proposal: yes.*
- **Q9** — the training driver writes `output/p7_3c_training/p7_3c_finetune.json`; at G7 the coordinator verifies it and commits it on `main` as `docs/data/p7_3c_finetune.json`; C5 pins its sha256 and resolves every trained subject from it. *Proposal: yes.*
- **Q10** — tests may not add absolute paths, so the hz1x1 corpus for T-regress (a) is found through a NEW variable, `RLTRAFFIC_SUMO_CORPORA` (unset → skip, naming it). *Proposal:* add it to the standard suite line.
- **Q11** — the corpus runs sequentially (one process), not at 12 workers (§6), for ≈ 1–1.5 h. *Proposal:* G1 serves as that run's pre-flight (it reviews the barrier), and G4 as the trainings' (run-once, resume).
- **Q12** — G5 adds one fenced BUILD-ONLY timing of the k = 100 windows, and the concurrency rule of §8 is fixed now. *Proposal: yes.* If the build is long (> 5 min), a ruling on one process per k that builds once and trains its seeds in turn.
- **Q13** — the fine-tune corpus's per-intersection return range is read from `p7_3d_calibration.json:probe_returns.sumo` over 201…200+k, which the A17(f) gate proves equal to the corpus on all 1,600. *Proposal: yes.*
- **Q14** — the gate records `engine_seed_drawn` per draw and requires all 100 equal, without consulting the gitignored probe chunks. *Proposal: yes.*
- **Q18** — C7 keeps P7.3d's two pre-token checks (`dt-reroll-check`, `reference-reroll-check`), ≈ 4–5 min, before the token. *Proposal: yes.*
- **Q19** — every gated test raises CI's skip count; the baseline is the coordinator's registered route. *Proposal:* noted in each packet section; I do not touch `.github/`.
- **Q20** — no real roll of a `b_mean_k5` / `b_mean_k20` prompt or of a trained checkpoint happens outside the campaign's token, because A24(a) says *"in P7.3c's stages only, and nowhere else"*. The trained payload's load path is proven on a fake env (T-payload); the admission and identity branches are proven on stubs. *Proposal: yes.*
- **Q21** — the disjointness check lives in `transfer_calibration.py`, is run by C2 before the token, and is re-asserted by the gate and the fine-tune; `collect.py` stays scenario-agnostic. *Proposal: yes.*
- **Q22** — G5's fenced timing, a few minutes: I start its tmux pane myself unless the author prefers to. *Proposal: yes.*

## 15. After Amendment A

`git merge --no-edit main`; re-read `BRIEF_41` whole; then C1's tests first (red, for their own reasons, pasted), then C1's
code, the named mutations, and the whole suite with the standard line. Stop at G1 and wait for Amendment B, building C3–C4
meanwhile if Q7 is ruled yes.

*Authorship: this plan was written by the implementer session (Claude Code, Opus 5.5) on 2026-09-25. The research decisions
it applies are A24's (tag `v2.5-prereg-a24`) and `BRIEF_41`'s; every "Proposal" above waits for the coordinator's ruling.*
