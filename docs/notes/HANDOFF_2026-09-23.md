# HANDOFF — 2026-09-23/24, written for a fresh or compacted Master session, BEFORE the P7.3d token

> **Updated 2026-09-24 (afternoon) by the fresh coordinator session, at the `BRIEF_39` B.7.6 commit: B.7.4 VERIFIED at `4383699`, the run
> worktree re-created there, both pre-token stages executed for real by the coordinator, the token block ISSUED. §1 and §2 read as of that
> commit; the pre-token timings in §2.4 are measured.**
>
> ⛔ **Updated again 2026-09-24 (evening), same session, now `claude-opus-5-5[1m]`: CAMPAIGN ATTEMPT 1 FAILED at `cells` — 698 / 700 valid,
> two cells refused for one SUMO COLLISION-teleport each, which `time-to-teleport -1` does not disable. Read, diagnosed and checked blind in
> `docs/notes/P7.3d_ATTEMPT1_READ_2026-09-24.md`; **A23 PROPOSED there, awaiting the author.** §1, §2, §2.5(c), §4 and §5 are corrected as of
> that commit.**
>
> 🔒 **And again, the same evening: A23 REGISTERED on the author's approval as written (`v2.4-prereg-a23` → `6e91acb`, chain verified from
> the remote); `BRIEF_39` Amendment B.8 ISSUED. Next: the implementer's ONE commit (B.8), then G8a–G8e and G6 — §2 item 4.**
>
> ✅ **Later the same evening: B.8 DELIVERED (`75ff1ac`, packet §27 at `10d8fe8`); G8a, G8c PASSED and G8d DONE (B.8.1, B.8.2); the RUN
> WORKTREE is at `10d8fe8`; attempt 1 lives at `output/p7_3d_runs/attempt1_cells/`; THE ATTEMPT-2 TOKEN BLOCK IS ISSUED. G6 starts with
> step (0): `python output/p7_3d_runs/coordinator_g8c/g6_step0_blind_compare.py` (exit 0 or STOP, A23(f)).**
>
> ⭐⭐ **And the same night: ATTEMPT 2 COMPLETE (700 / 700, `DRIVER EXIT: 0`) and G6 PASSED (`BRIEF_39` Amendment D;
> `docs/notes/P7.3d_CAMPAIGN_READ_2026-09-24.md`): ρ_sumo(`b_mean_k100`, `E_sumo`) = +0.8855 [+0.8736, +0.8973] — H3 clause 1 holds, clause 2's
> inequality holds, clause 3 void. NEXT: C7 (the implementer's packet, the artifact copied by hand to `docs/data/`), then ONE merge review —
> §2 items 5 and 6.**

**Read this INSTEAD of a chat summary.** Supersedes `HANDOFF_2026-09-19.md` (accurate on its date). Read order: this file → `CLAUDE.md` §4b
(no AI trailer, ever — raised again under three new wordings since 09-19; the author reconfirmed each time) → `PROJECT_PLAN` §7's rules dated
2026-09-19 to 2026-09-24 (relay-nothing-but-four-things; complete copy-pasteable actions; launch from the worktree; a merge is verified by its
parent count; no pipe on a chain-stopping command; the exit code comes from `PIPESTATUS`, never from `tee`) → `BRIEF_39` whole, **Amendments
A through B.7.6 in order** → `docs/reviews/P7.3d-preflight.md` (NOT CLEAR, three blockers, all re-run by the coordinator) →
`docs/reviews/P7.3d-preflight-2.md` (CLEAR-WITH-CONDITIONS on `ec5bf19`; carried to `4383699` by B.7.6) → the packet
`docs/returns/P7.3d.md` §24–26 (the two fix rounds and B.7.4, written by the implementer).
⚠️ **Re-check every sha before acting:** `git rev-parse main origin/main task/p7.3d-grid4x4 origin/task/p7.3d-grid4x4`,
`git -C /home/filip/rltraffic-p73d-run rev-parse HEAD`, `git worktree list`, `gh run list --branch main --limit 1`,
`ls /home/filip/rltraffic/output/p7_3d/cells | wc -l` (700 = attempt 1's 698 chunks + `canary.json` + `FAILED`, until A23's re-run moves
it out of `output/p7_3d/`), `test -f output/p7_3d_runs/TOKEN_confirmatory`.

## 1. Where things stand (verified by commands, 2026-09-24 ~13:40 — re-check every sha with the commands above)
- **`main` = `origin/main`** at the commit carrying this update (its parent `bd3551b`, plan v2.28, was CI-green — run `35929692740`, 23 min); plan **v2.30**; ceiling **193**; CI green on `75eca09` (run `35994275139`).
- **P7.3b CLOSED** (merged `732f378`, CI fix `708f157`, ceiling 191 applied by the author). **P5.4 CLOSED** (merged `a80006b`: the spatial
  footprint is **TRIVIAL**, r = 0.0639 against 0.10 — A22's expectation FAILED; the paper writes *adding the cross-intersection attention path
  destabilises training*; ceiling 193 applied by the author).
- **Registrations since 09-19:** A19 (`v1.9`, H3 clause 3 void; the few-shot curve as interference), A20 (`v2.0`, grid4x4 subject/arms), A21
  (`v2.1`, confirmatory only: `b_mean_k100` + two anchors, 700 cells), A22 (`v2.2`, the footprint), A22.1 (`v2.3`, the three-hop
  correction) and **A23 (`v2.4`, 2026-09-24: SUMO's collision teleport — a collision is recorded and its episode kept under §8, any
  teleport no same-step collision explains still refuses, a robustness check without draws 1020 and 1042)**. Tags `v2.0-prereg-a20 …
  v2.4-prereg-a23`, all verified from the remote.
- ⛔ **P7.3d: CAMPAIGN ATTEMPT 1 RAN AND FAILED AT `cells` (2026-09-24, 14:21–15:14).** 698 of 700 cells rolled and valid; fixed-time on draw
  1020 and `b_mean_k100` seed 303 on draw 1042 REFUSED, each with one teleport — a SUMO COLLISION at a merge onto an exit lane, resolved by
  SUMO's default `collision.action = teleport` (diagnosed from SUMO's own output; `output/p7_3d_runs/attempt1_diag/`). The token is
  CONSUMED; `output/p7_3d/cells` holds the 698 + `canary.json` + `FAILED`, digest-pinned in `output/p7_3d_runs/attempt1_manifest/`. G6's
  instrument checks on the 698 pass, run blind. **A23 is PROPOSED (the note's §5): record every collision, keep the episode under §8,
  refuse only jam teleports, a registered robustness check — REVIEWED once (`docs/reviews/A23-proposal.md`, four blocking
  corrections adopted) — REGISTERED on the author's approval as written (`v2.4-prereg-a23` → `6e91acb`).** The review also found both SUMO probes checked `n_teleports` one second
  in ten; all 200 probe episodes re-rolled every second the same day: clean, every registered probe return reproduced. Status before attempt 1, kept for the record:
- **P7.3d WAS IN FLIGHT, BEFORE THE TOKEN.** Branch `task/p7.3d-grid4x4` at **`4383699`** = `origin/task/p7.3d-grid4x4` (`bd4d457` the B.7.4
  code, `4383699` the packet's §26; pushed by the coordinator — the implementer never pushes). Run worktree **`/home/filip/rltraffic-p73d-run`,
  detached at `4383699`, clean** (re-created 2026-09-24 13:20; identical to `bd4d457` outside `docs/`). **The campaign work dir
  `output/p7_3d/cells` is EMPTY; no token exists; no grid4x4 evaluation cell has ever been rolled for real** except fenced draw-5 cells and
  six frozen reference cells (C4) — the coordinator's two pre-token runs of 2026-09-24 rolled exactly those (B.7.6-2, B.7.6-3).
- **Delivered and verified from disk:** C1 (per-scenario parity; CAP(E) 200/200 → A15(g) MET, Amendment B), C3a (per-intersection Rule B;
  `p7_3d_calibration.json`, 48 targets reproduced by the coordinator to one ULP), C3b (the spatial loader, 16-id prompt), C4 (six reference
  cells rolled twice, 0 disagreements; pedigree 6/6), C5 (G2 timing; the SUMO probe, 100 episodes), C6 (the driver) — then **two fix rounds**
  after the pre-flight found the driver could not start and `run_cell` had no grid4x4 branch (B.6), and `report` had no grid4x4 body (B.7).
  then **B.7.4** (`bd4d457`: ρ_i as a ratio of means with its diagnostic, B.7.5's null rule, the pre-token `reference_reroll_check`, 25/25
  mutants), VERIFIED by the coordinator from the run worktree at `4383699` (`BRIEF_39` B.7.6: the pin 51 green, the per-intersection block
  equal under `==` by an independent route, M01b and R02 re-run and killed). **Both pre-token stages executed for real at the run commit:
  `reference_reroll_check` 6 / 6 MATCH (143 s), `dt_reroll_check IDENTICAL` 13 / 13 (114 s, 61 fields, hash `90d212d6…`)** — records under
  `output/p7_3d_runs/coordinator_*`; the driver re-runs both itself before the token.

## 2. Open items, in order — nothing else is in flight
1. ✅ **DONE 2026-09-24 — B.7.4's commit** (`bd4d457`, packet §26 at `4383699`): B.7.2-1/B.7.2-2 with B.7.5's rulings, the pre-token
   `reference_reroll_check`, B.7's evidence in `output/p7_3d_runs/b7/`; 25/25 mutants killed before the commit; suite 2,336 / 85 / 0.
2. ✅ **DONE 2026-09-24 (`BRIEF_39` B.7.6-1) —** pushed; the run worktree re-created detached at `4383699`; from it the three B.6 commands
   (pass) and the pin (**51 / 51**); B.7.5-2's hand re-check of the per-intersection block by an independent route (**equal under `==`,
   16 / 16**); M01b and R02 re-run on committed mutants, control first, both killed.
3. ✅ **DONE — the G4 verdict CARRIES to `4383699`** (B.7.6-5); its condition — start on an IDLE machine, on mains — is in the token block.
   **Beyond the plan: both pre-token stages executed for real by the coordinator at the run commit** — `reference_reroll_check` **6 / 6 MATCH,
   143 s**; `dt_reroll_check` **IDENTICAL 13 / 13, 114 s** (B.7.6-2, B.7.6-3). The driver re-runs both before the token; expect the pre-token
   chain to take **≈ 4–5 min** and the capture to carry ` Retrying in 1 seconds` lines (TraCI connect chatter) and `sumo_utils.py:56`
   deprecation warnings before each verdict line — neither is a finding.
4. ⛔ **The token — USED 2026-09-24 14:21: ATTEMPT 1 FAILED at `cells`** (698 / 700; the note's §1–§2). **Next, in order:** (i) ✅ **DONE** — A23
   registered and tagged (`v2.4-prereg-a23` → `6e91acb`, tag object `1fc1f8e`, chain verified from a fresh clone); (ii) `BRIEF_39` B.8
   ISSUED — the implementer's one commit (the collision record, the refusal of unexplained teleports only, format 1.1, the report's
   `collisions` block and the 98-draw robustness block, the two-event refusal, T-regress, mutations); ✅ G8a PASSED (B.8.1, plan at
   `62aab7c`: D4 a stop not an exclusion, D8's toy test extended to settle A23(c)(iii)'s in-transit clause); ✅ G8b (`75ff1ac`, §27 at
   `10d8fe8`); ✅ G8c PASSED and ✅ G8d DONE (B.8.2 — the pin 87 green, the two collision cells KEPT, both pre-token checks real, the reads
   shown not to change the simulation, B01/B07 killed; attempt 1 renamed to `output/p7_3d_runs/attempt1_cells/`); (iv) the attempt-2 token
   block ISSUED — `tmux kill-session -t p73d_cells` FIRST (attempt 1's pane holds the name), then the same two steps; (iii) the coordinator verifies from the run worktree, rolls the two known collision cells once with every value fenced,
   MOVES attempt 1's `cells/` out of `output/p7_3d/` (a rename, nothing deleted), re-creates the run worktree; (iv) a NEW token block, the
   same two-step start; ≈ 55 min. *The attempt-1 token block, kept for reference:* Written by the author: `date -Is > /home/filip/rltraffic/output/p7_3d_runs/TOKEN_confirmatory`. Then the
   TWO-STEP start (B.3-1; the one-liner form refuses — measured): Step 1 `tmux new -s p73d_cells`; Step 2, typed at the pane's prompt:
   `bash /home/filip/rltraffic-p73d-run/offline/campaigns/p7_3d_grid4x4.sh confirmatory 2>&1 | tee -a /home/filip/rltraffic/output/p7_3d_runs/campaign_capture.txt; echo "DRIVER EXIT: ${PIPESTATUS[0]}" | tee -a /home/filip/rltraffic/output/p7_3d_runs/campaign_capture.txt`.
   **Read the last lines and the `COMPLETE`/`FAILED` file, never the exit code of `tee`.** Schedule 57–113 min (G2's W=12 and W=8 rates; the
   W=8 row is unexplained).
5. **G6 — the coordinator reads the campaign from disk, CAPTURE FIRST** (the 09-18 rule). What it MUST check, in this order:
   **(0) — after attempt 2, BEFORE everything else (A23(f)):** attempt 1's 698 valid chunks (now `output/p7_3d_runs/attempt1_cells/`)
   against attempt 2's same cells under `==` on every shared field but `git_commit`, `format_version`, `seconds`, `canary_seconds`
   (B.8-3), booleans only, and exactly A23's two events — the script is `output/p7_3d_runs/coordinator_g8c/g6_step0_blind_compare.py`;
   any difference stops the read.
   (a) `campaign_capture.txt` from its first line: every refusal before the successful start (the group-leader guard firing on a one-liner
   attempt is expected and consumes nothing); the canary line, both halves, at reference (~0.9 s; refuse above 2.0); `dt_reroll_check
   IDENTICAL`; six `MATCH` lines; the token consumed exactly once; `CAMPAIGN COMPLETE`; `DRIVER EXIT: 0`.
   (b) `output/p7_3d/cells`: **exactly 700 chunks** = 500 `b_mean_k100` (5 seeds × draws 1000–1099) + 100 `fixedtime` + 100 `maxpressure`,
   every one at the run worktree's commit with `git_dirty false`, `scenario cityflow_grid4x4`, `config_sha256` equal to that draw's grid4x4
   parity digest (never hz1x1's `c177e962…`), 0 JAM teleports and every collision recorded (A23, if registered), `cf_parity`, `"-1"`, 360 decisions, `engine_seed_drawn` one value; the halting
   cross-check ON for exactly the 7 cells of draw 1000 with 0 disagreements; `local_return == local_return_from_lanes` on all 16 ids on all 700.
   (c) On the 500 DT cells: `rtg_first_i == target_i` for all 16 against `p7_3d_calibration.json`; `calibration_sha256 ==
   P7_3D_CALIBRATION_SHA256`; `checkpoint_sha256` in A20(a)'s five; the RTG identity **in the agent's own association** — `rtg[t] == target_i − (left-to-right float64
   sum of reward_series[i][0..t−1])` (`SpatialDTAgent.act :745–746`), exact on attempt 1. ⚠️ **Corrected 2026-09-24:** the form this item
   used to prescribe, `rtg[t] − rtg[t−1] == −reward[t−1]`, fails by ≤ 0.5 ULP on 608 steps in 373 chunks — a false alarm (the
   coordinator's error). **Print pass/fail COUNTS only, never where a check fails** (the note's §4).
   (d) The six reference cells in the campaign reproduce `p7_3d_reference_cells.json` bit-for-bit on the 17 fields (`report` refuses otherwise).
   (e) `p7_3d_grid4x4.json` regenerates byte-identically from the run worktree; ρ recomputed by an INDEPENDENT route from the raw chunks —
   **per cell under `==` FIRST, then aggregates** (`DEFERRED` 85; a one-ULP corruption of one cell leaves every aggregate unchanged); per-seed
   means by `sum()/len()` (the artifact's route); per-intersection ρ as a RATIO OF MEANS with its denominator diagnostic (B.7.2).
   (f) What the number MEANS is fixed in advance: H3 clause 1 is confirmatory on this scenario (`ρ_sumo(b_mean_k100) > 0`), clause 2 reported
   as an inequality, clause 3 void (A19); A21(b)'s two scope sentences verbatim; the probe-ratio observation (B.3-4) is read against the
   per-intersection return and is descriptive. **Nothing in the artifact interprets; keep it that way.**
6. **The packet** (C7), ONE merge review (A21), merge with §6's box ticked, both refs pushed, the ceiling by the registered route (observed,
   never predicted — the run will be red by the gate alone; `output/ci_runs/skips.py` and `build_ceiling_patch.py` are the tools, falsified
   on the 189 run), handed to the author as one command.

## 3. Rules added this cycle that a fresh session must apply without being told
- **Relay nothing but four things** (a token, a registration-changing ruling, a finding that could make a number wrong, a finished task); a brief
  goes ONCE, whole, with its gates; an implementer that stops reaches the coordinator through ONE WORD from the author. (§7, 09-19.)
- **Every `THINGS YOU NEED TO DO` item is complete and copy-pasteable**; a command that does not exist yet is said so plus the message that produces it. (09-20.)
- **A merge is verified by its parent count**; `git merge` on a clean index; no chain-stopping command through a pipe. (09-19, after `3c9f546`.)
- **Implementer sessions launch from their worktree** (`cd <worktree> && claude`); the guard inspects its own cwd's toplevel. (09-23.)
- **The `-C` hole is measured** (`DEFERRED` 90): `git -C <path> reset --hard` walks around the deny prefix; the merge-time diff is the line that held.
- **A refusal test must not be able to run the experiment** (`DEFERRED` 91): one fixture supplies roots, token and `--limit 0`.
- **Per cell under `==` before any aggregate** (`DEFERRED` 85). **Per-intersection ρ is a ratio of means with a denominator diagnostic** (B.7.2).

## 4. What a fresh session must distrust, from this cycle's own errors
- **The coordinator's brief was wrong three times where the implementer opened the artifact:** the subject's checkpoint format (spatial, not
  `DTAgent`), the halting coverage (192 monitored incoming lanes, not 240), the receptive field (three layers → three hops, A22.1). Open the file.
- **The coordinator's B.7.2-3 pinned the anchors' return to `sum(reward_series[i])`** — one step short by D1's own shift, the same error the
  coordinator made on 09-19 and had already logged. The implementer's post-step 360-sum on both cell kinds is the right quantity (B.7.3).
- **`3c9f546` on `main` claims a merge and is not one** (one parent). Parent count, not message.
- **A suite run can HANG** (10 h, 40 bytes); block on it with `timeout` and `-v`.
- **A pipe hides the exit code**; a header can document an invocation its own guard refuses (measured: the tmux one-liner is not a
  process-group leader); a dry-run under `setsid` tests a third invocation. Execute the delivered form.
- **A pin tested on stubs is not a pin on the simulation** (B.7.4): the anchor branch changed and no real anchor rolled until the pre-token check.
- **`time-to-teleport -1` does NOT make SUMO teleport-free:** a collision still teleports the collider (default `collision.action`). The
  config's own comment and SUMO's help text describe the jam mechanism only — a description taken for the behaviour, by the tool itself.
- **An identity check written from memory of the convention fails by rounding** (G6(c), corrected): write it in the code's association.
- **A counter that reads a per-step list once per decision sees one step in ten** (the SUMO probe's teleport check, `DEFERRED`
  93): a check is only as dense as its read. The review found it; the coordinator's own note had called the condition *met*.
- **A check's failure PATTERN can leak an outcome** (the RTG rounding pattern revealed zero-crossings on ≥ 373 DT cells): counts only.
- **The coordinator's closing summary once reported a rule kept that was broken** (the trailer, 2026-09-24 afternoon). A summary of
  conduct is a description, not evidence: check `git log` and the turn itself.

## 5. Models, by date, for the AI-assistance record (the paper's disclosure is built from these)
- **Coordinator:** the agent definition `.claude/agents/master-coordinator.md` pins `model: claude-opus-5`, which binds subagent launches only.
  The interactive coordinator session ran as **`claude-opus-5[1m]`** on 2026-09-19 (the harness's own reminder named it), and as
  **`claude-fable-5-1`** (Fable 5.1) from the P7.3b anchor-campaign read of 2026-09-19 onward through 2026-09-24 — the model the harness
  reported for every ruling from A19 to B.7.4, including all pre-registration rows A19–A22.1. **Whichever model a future coordinator session
  runs on is recorded here by date when it first commits.** The fresh coordinator session of **2026-09-24 (afternoon)** — the one that
  verified B.7.4, wrote `BRIEF_39` B.7.6 and issued the token block — ran as **`claude-fable-5-1`** (Fable 5.1), the model the harness
  named for it. **From the G6 read of campaign attempt 1 (2026-09-24, ≈ 15:20) the same session ran as `claude-opus-5-5[1m]`** (Opus 5.5,
  1M context), switched by the author with `/model`; the attempt-1 note, A23's proposal and `BRIEF_39` B.7.7 are that model's.
- **The author's advisory chat** (claude.ai; reads the public GitHub remote, never writes; the author relayed its texts into the coordinator
  and implementer sessions, marked as its own): configured as **`claude-opus-5`** until 2026-09-23 00:48, and as **`claude-opus-5-5`** from
  then on. Its texts are advice the coordinator ruled on, never a ruling; where one is quoted, the brief says so.
- **Implementer (P7.3d):** `claude-opus-5[1m]` to `8c79778`; `claude-opus-5-5[1m]` from the B.6 fix round (`f963f75`) on (B.6.2, §25.10).
- **Reviewers:** `contract-reviewer` subagents, pinned by their agent definition; each review file names its mandate and date.
