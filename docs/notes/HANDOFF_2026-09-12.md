# HANDOFF — 2026-09-12, written for a fresh Master session

**Read this INSTEAD of a chat message.** Supersedes `HANDOFF_2026-09-10.md` (still accurate on its date;
its §5 carries a dated correction block). Read order: this file → `PROJECT_PLAN` §10 banner → §7's two
newest 🔒 rules (incremental findings; pre-flight) → `docs/notes/DEFERRED.md` rows 72–77 →
`docs/briefs/BRIEF_34_p7.1_alignment_and_metric_freeze.md` Amendments A–H → `PREREGISTRATION.md` A14.
⚠️ **Re-check every sha before acting: `git rev-parse main`, `git -C /home/filip/rltraffic-p53b log -1`.**

## 1. Where things stand (verified by commands on 2026-09-12)

- **P5.3b and its fix round are MERGED** (`9dd224e`, review PASS-WITH-NOTES, three surviving mutations
  killed by the coordinator). Result: the gap between the two ATT definitions on the ablation is 94 %
  boundary queueing of admitted vehicles; the collapsed DT is one open-loop 360-action sequence.
- **A14 is REGISTERED** (`c93c5f7`, tag `v1.4-prereg-a14`, sha256 `751c763b…`, verified from a fresh
  clone): the Conversion Audit Protocol (CAP) is the C3 admissibility rule; the NATIVE claim is withdrawn
  (hangzhou's SUMO nets were generated from CityFlow by LibSignal's toolchain); grid4x4 is a C3 candidate
  admitted iff CAP passes.
- **P7.1 (`BRIEF_34`) is on branch `task/p7.1-alignment`, worktree `/home/filip/rltraffic-p53b`,
  "H done" at `7efafa7`** — half A (SUMO ATT observer, campaign run 836 s, artifact
  `docs/data/p7_1_metric_freeze.json`) and half B (`align_info` adapter, paired-scenario table, CAP
  audit tool + `p7_1_conversion_audit.json`, freeze document `docs/notes/P7.1_FREEZE.md` with DRAFT
  amendments). Merge reviews filed: `docs/reviews/P7.1-halfA.md` (PASS-WITH-NOTES) and
  `P7.1-halfB.md` (FAIL → Amendment H fix round). **The coordinator's verification of H is what is
  in progress; see §3.**
- **The headline P7.1 measurement (pre-review until merge):** under the admitted ATT definitions the
  two backends' fixed-time–MaxPressure spreads are 37.7 vs 11.6 and ρ_random −3.62 vs −9.67 (P7.0's A4
  failure); under the pool-clock pair (`att_engine`, `E_sumo`) they are 235.2 vs 237.6 and −1.48 vs
  −1.11. A2 (state overlap) still fails, so P7.0's Branch C stands. SUMO's cadence term is exactly 0 on
  30/30; teleports occur only under MaxPressure (13–15/episode); hz1x1 SUMO costs 10.4–11.8 s/episode
  bare (`n = 5` per arm); **hz4x4 SUMO 50.3 s (campaign chunk) — the 438.7 s once logged was measured
  under load and is withdrawn (Amendment G).**
- **The paired set is 4 of 6, all one network:** cologne1 AND cologne3 are unalignable as the files stand
  (`DEFERRED` 74). grid4x4's SUMO-native original is RESCO's (`grid4x4_1.rou.xml` matches our flow.json
  1,473/1,473); it lives in the gitignored `scenarios/grid4x4_candidates/` (**CC BY-NC-SA — never
  commit it**; P2.3 now has a concrete case).

## 2. Rules added this week, and why

- §7 **INCREMENTAL FINDINGS** (2026-09-11): three reviewers died on the API session limit at ~25 min;
  two returned nothing. Every long spawn names a `FINDINGS.md`, writes its checklist within three tool
  calls, appends per finding; on death the coordinator files the partial and continues. **Spawn long
  agents SEQUENTIALLY, never two in parallel or beside heavy coordinator work — the session budget is a
  5-hour window (observed resets 20:40, 01:40) shared by everything in the session.** Patch for the two
  agent definitions: `docs/patches/agents_incremental_findings.patch` (apply on `main`).
- **A wall clock carries no signature of the load it was taken under** — broken by the coordinator on
  2026-09-12 (the 438.7 s). Read `user+sys` beside `real` before quoting a rate.

## 3. What is in flight and what to do next, in order

1. **Verify Amendment H on `7efafa7`** (coordinator, no third reviewer): the six surviving mutations
   (M3b; M6, M8a, M9, M11, M12) re-applied → all must fail a test; byte-identical regeneration of the
   three half-B artifacts and the freeze artifact from the new CLIs; the reviewers' own scripts
   (`/tmp/…/scratchpad/review-p71-halfB/check_c_d.py`, `recompute16.py`; `review-p71-halfA/recompute_A.py`)
   against the regenerated artifacts — if the scratchpad is gone, the numbers to reproduce are in the
   two review files.
2. **A4 re-roll** — a driver restart with a new token (`date -Is > /home/filip/rltraffic/output/p7_1/
   AUTHORISED_TO_RUN`; `cd /home/filip/rltraffic-p53b && bash offline/campaigns/p7_1_metric_freeze.sh`
   in a tmux FOREGROUND job; ~3 min): moves the false timing chunk to `failed/`, re-rolls, re-runs
   `report`, rewrites `SHA256SUMS_p7_1.txt`. The freeze artifact must regenerate byte-identically
   (`report` globs `freeze_*.json` only). Then a fresh implementer session updates the packet with the
   new chunk's values and commits.
3. **Merge** with §6's P7.1 box ticked, §1b's reading, the Decisions Log row; CI skip ceiling will move
   (SUMO- and output-gated tests) — registered protocol: classify `junit.xml`, commit the observed value.
4. **A15 (coordinator, fresh session, after the merge):** register from the freeze's DRAFT — the
   SUMO-side ρ definition (`E_sumo`, pool clock, all created) with the admitted pair beside it; the
   `noteleport` regime; P7.0's A4/B4 re-scored under the pool-clock pair with both verdicts and NO branch
   change; the paired set corrected (4 of 6, one network); grid4x4 admitted by CAP subject to its parity
   route file (`DEFERRED` 75). Tag `v1.5-prereg-a15`, verify from a fresh clone.
5. Then P7.2's brief (RTG calibration protocol — depends on the ρ definition just frozen) and
   `DEFERRED` 75 (the `render_sumo` parity path + `.sumocfg` generation) before P7.3.

## 4. What to distrust, including in this file

Every number here was read from an artifact on 2026-09-12; the P7.1 numbers are PRE-MERGE. The
coordinator's own errors this week, all in the reporting layer: a population claim from a 12-row sample
(caught before issue), a `100` that was `97` (caught by the implementer), three arm factories the campaign
never used, a schedule from an assumed speedup, and the load-contaminated 438.7 s. **Resolve every sha and
path before writing it; read the artifact, never the relay.**
