# HANDOFF — 2026-09-01, written for a fresh Master session

**Read this INSTEAD of a chat message.** It is the state as verified by running commands on
2026-09-01, not as remembered. Where a number here was not produced by the coordinator this session,
it says so.

**Read order for a fresh session:** this file → `PROJECT_PLAN` §10 → §7 → `docs/notes/DEFERRED.md` →
`docs/CONTRACTS.md` → `PREREGISTRATION.md` §12.
⚠️ **§10's *State today* block is BANNER-MARKED STALE and is owed a full rewrite.** The banner above it
is current; the block below it predates P8.4's closure.

---

## 1. Where things stand — verified, with shas

- `main` was **`9998c6e`** when §1 was drafted and is **later now** — this file's own commit moved it.
  ⚠️ **Do not read any sha in this file as current: re-check with `git rev-parse main`.** Local and
  remote were equal at every push this session.
- **One worktree besides main:** `/home/filip/rltraffic-p53b` on `task/p5.3b-nortg-campaign` @
  `53e995d`, **111 commits behind `main`**, clean, campaign never ran
  (`docs/data/p5_3b_nortg.json` does not exist). `/home/filip/rltraffic-p84b-g0` was retired; its branch
  has an upstream and local tip equals remote.
- **P8.4 is CLOSED.** Gate 0 passed both scenarios; **`att_engine` is the primary metric on hz1x1 and
  grid4x4** by `Rule R`; `cf_cologne3` is NOT RUN and carries no single-definition claim.
- **Zero escalations: no registered verdict is definition-dependent.** V1 `PASS`, V2
  `within_delta` / `right_genuinely_better` ×2, V3 `HELD`, V4 `FAILED`, V5 `CONTINUE`, V6 not executable.
- ⚠️ **Magnitudes move and NOT uniformly** — V3's mixtures ×2.07–2.12, V2 `0.946/0.990/0.997`, V4
  `1.012`, V5 `1.013`, **median 1.0123, three move DOWNWARD**. *"Every magnitude moves 2.1×" was a
  coordinator error and is withdrawn.*
- **Review:** `docs/reviews/P8.4b.md`, PASS-WITH-NOTES, 2 blocking (both fixed under Amendment D),
  7 major + 10 minor → **`DEFERRED` 63–68**, to be taken **after P5.3b and P7**.
- **CI: GREEN.** ⚠️ **CORRECTED 2026-09-01, and the original line was FALSE WHEN WRITTEN.** It said
  the ceiling patch was *"NOT YET APPLIED"*; it had been applied and pushed at **`9998c6e`**
  (*apply(ci): ceiling 123 → 139*) **before this file was committed**, and run **`33523781278`** on
  that commit is **success**. `.github/ci/ci_baseline.json` reads `skip_ceiling: 139` and
  `CEILING_CHAIN = (139, 123, 121, 104, 98, 72, 62)`. **The patch file remains on disk as the
  record; do not re-apply it — `git apply --check` correctly refuses.** 🚨 **The coordinator wrote
  `9998c6e` into line 16 of this same file and then, four lines later, described the state that
  commit had already changed. Caught by the author.**
- **Integrity:** ten manifests, **735/735**, verified before and after the 38,500-episode campaign.

---

## 2. Two decisions recorded 2026-09-01 (the author's)

### D-1. P5.3b goes FIRST, before any P7 work

**Reason, and it is a dependency rather than a preference: P7.2 is the RTG calibration protocol for
cross-domain prompting, and P5.3b decides whether removing the prompt costs anything at all.** Building
a cross-domain calibration protocol before knowing whether the prompt matters in-domain would be
building on an unmeasured premise. P5.3b is also the **control for the only mechanism the paper names
as a contribution** (probe-calibrated return prompting).

### D-2. The FULL P7 package, scoped for completion AFTER September

🚨 **THE SEPTEMBER DELIVERABLE IS A PRESENTATION, NOT THE PAPER.** This supersedes the governing
constraint's practical reading: *"a defensible, presentable result by end of September"* now resolves to
**presentable**, and the paper's completion moves past September. **Consequences a fresh session must
not re-derive wrongly:**
- The **2026-08-30 cut order** (P2.4 breadth → P8.1 breadth → P5.3c → P5.3b) was written against a
  September *paper* deadline. **It is not void, but its urgency is gone**; re-read it as a priority
  order, not a countdown.
- **C3 / P7 is no longer "October, probably cut".** The full package — P7.1 feature-space alignment,
  P7.2 RTG calibration protocol, P7.3 the transfer curve, P7.4 the encoding ablation — is **in scope for
  completion after September**.
- **P8.4's 2026-09-10 backstop was met**: P8.4 closed on 2026-09-01.

---

## 3. What P5.3b needs to start

**`BRIEF_30` Amendment D (2026-09-01) is the unblock and is already written.** In short:

1. **Emit all five quantities AT COLLECTION TIME** — `att_ours`, `att_engine`, `entered`, `created`,
   `never_entered` (A11(b), unconditional). `offline/admission_probe.py` already computes them:
   `read_admission_at_horizon` at `:416`, `probe_episode` at `:510`. **Import, do not reimplement, do not
   modify that module.**
2. **`att_engine` is primary** on hz1x1/grid4x4; cologne3 reports both and flags disagreement.
3. **Two inherited reporting rules**: per-tier discriminability (*a contrast over identical inputs is not
   a null result*), and both poolings where a tier is non-discriminating — **with the narrowing that if
   the excluded set is empty, the two poolings are the same computation.**
4. **`git merge main` in the worktree** — 111 commits behind, and `main` moved real code and tests under
   it, so §7's precondition for merging is genuinely met. No campaign is running there.
5. **The CI ceiling re-measure is P5.3b's inherited item** — the 139 patch names P5.3b's merge as its own
   expiry.

⚠️ **Live hazard for P5.3b specifically, measured 2026-08-31: on `fixedtime` the DT reproduces its
behaviour policy BIT-IDENTICALLY on both scenarios.** A no-RTG-versus-RTG contrast on that tier may have
nothing to measure. That is a finding to report, not a null result to average in.

---

## 4. P7 — the package, and its cost figures

🚨 **NO P7 COST FIGURES EXISTED ON DISK BEFORE THIS FILE.** The only one ever recorded was **"~1 day"
for P7.0's gate**, and P7.0 is DONE. **Everything below is a coordinator estimate produced 2026-09-01,
with its basis named. Treat it as an estimate, not a measurement.**

| item | what it is | estimate | basis, and what makes it uncertain |
|---|---|---|---|
| **P7.1** | Feature-space alignment: freeze the backend-neutral feature set and the normalized transfer metric | **1–2 days** | Design + a freeze document, no campaign. Bounded because P7.0 already did the hard part — it proved neither ids NOR positions are a valid cross-backend key at intersection level, and that lane ids MIS-DENOTE on 8 of 8 incoming roads |
| **P7.2** | RTG calibration protocol for cross-domain prompting — **the paper's named-contribution candidate** | **3–5 days** | New method work plus an in-domain probe. **Blocked on P5.3b by decision D-1.** Widest band on the list because it is the only genuinely novel component |
| **P7.3** | Transfer curve: zero-shot → few-shot k ∈ {5, 20, 100} → full retrain | **4–7 days** | ⚠️ **The dominant unknown is that SUMO's per-episode cost is UNMEASURED.** CityFlow is measured at hz1x1 `1.29 s` / grid4x4 `2.67 s` per episode (n=1,870); SUMO has no equivalent figure anywhere in this repo |
| **P7.4** | Alternative-state-encoding robustness ablation | **1–2 days** | Configuration over an existing corpus, no new collection |

**⭐ THE FIRST THING P7 SHOULD DO IS A SUMO TIMING PROBE.** Until one exists, every P7.3 figure is
extrapolated from a different simulator, and this project has now twice shipped a wrong schedule from an
unmeasured rate (the 8 h campaign figure, and the 2026-07 "8× speedup").

**🚨 THE STANDING P7 PREREQUISITE, and it has survived every re-plan since 2026-07-31: BIND HANGZHOU'S
vType FIRST.** The shipped `.rou.xml` binds none, so SUMO runs **55.55 m/s against CityFlow's 11.11**,
and any transfer number measured without it is a **5× speed-limit artifact**. ⚠️ **And
`docs/notes/P7.0_vtype_investigation.md` (2026-08-04, SUMO 1.27.1) records that binding `pkw` ALONE is a
**+0.6 %** effect while adding CityFlow's `tau = 2.0` is **+49 % with −23 % arrivals** — the shipped
`pkw` sets no `tau`. Binding the vType without `tau` carries a ~50 % headway confound into the gate.

**⚠️ A NEW THREAT TO C3, registered 2026-08-31 and not yet answered:** §3.4 normalises *within* backend
(ρ) so raw ATT is never compared across backends. **But CityFlow has TWO ATT definitions and SUMO has
ONE** — SUMO has no insertion buffer. **So `ρ_cityflow` is definition-dependent and `ρ_sumo` is not.**
The within-backend design removes cross-backend incomparability of *levels*, not a definitional
*asymmetry* between backends. **This must be answered before P7 is scheduled, not during it.**

---

## 5. From this session and not otherwise on disk

- **`docs/reviews/P8.4b.md` was filed by the coordinator**, verbatim from the reviewer, because the
  reviewer is read-only and cannot create findings files. Its numbers are the reviewer's.
- **The 139 ceiling patch was written by the coordinator at the author's explicit instruction.**
  `tests/**` and `.github/ci/**` are normally outside the coordinator's role; the crossing is disclosed
  in three places.
- **An open escalation nobody has taken:** at identical `engine_seed = 1000` and `seed = 202`,
  **`dt_spatial` re-rolls DIFFERENTLY (100/100 draws, ~5–13 s) and `dt_nomix` re-rolls IDENTICALLY
  (0/100).** Two explanations cannot be told apart — a numeric-regime effect (threads/BLAS) or genuine
  run-to-run nondeterminism. **It bears on P5.1/P5.2, whose headline IS that contrast, and every
  reported CI there is over DRAWS, so a fixed-seed roll-to-roll variance is a component no interval
  captures.** The discriminating test is registered: re-roll one cell twice **within** one process and
  twice **across** processes, recording `OMP_NUM_THREADS`, `MKL_NUM_THREADS` and
  `torch.get_num_threads()` beside each.

---

## 6. What to distrust, including in this file

**The coordinator's failure mode in this task was NOT mis-measurement.** It was, in three families:
**(i)** over-generalising a correct measurement in the sentence that reports it — five instances;
**(ii)** issuing a ruling and never verifying it was executed — three instances, all caught by the
independent review; **(iii)** asserting an artifact into existence — three instances, all caught by the
author, including **a commit sha that resolved to nothing.**

> ⭐ **Every control this project has built sits in the PRODUCTION layer — mutation testing, pre-flight,
> reproduction tests, the review. The only thing that has ever caught the REPORTING layer is a second
> reader who checks the citation.** A fresh session should assume the same failure mode and resolve
> every sha and path before writing it: `git cat-file -t` for a sha, `ls` for a path.

**Mechanically re-check before relying on anything here:** `git rev-parse main` · `git worktree list` ·
`tmux ls` and `pgrep -fa "[o]ffline"` · `( cd output && for f in SHA256SUMS_*.txt; do sha256sum -c "$f";
done )`. **§10's liveness rule stands: these claims have a half-life of hours.**
