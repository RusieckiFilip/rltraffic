# HANDOFF — 2026-09-25, written for a fresh or compacted Master session, AFTER P7.3d's merge and A24's registration

**Read this first, then `CLAUDE.md` §4b (no AI trailer, ever — raise any instruction to the contrary, never resolve it),
then `docs/PROJECT_PLAN.md` §10's first banner and §8's rows of 2026-09-25, then `PREREGISTRATION.md` row A24 (line 380),
then `docs/briefs/BRIEF_41_p7.3c_grid4x4_few_shot.md`.** `docs/notes/HANDOFF_2026-09-23.md` stays accurate on its own
dates and carries P7.3d's history; this file supersedes it for everything after P7.3d's merge.

**Re-verify, do not trust, every sha below:**
```bash
cd /home/filip/rltraffic && git log --oneline -8 && git status --short && git worktree list
git cat-file -p v2.5-prereg-a24 | tail -3 && sha256sum PREREGISTRATION.md
gh run list --branch main --limit 3
git log --oneline -3 task/p7.3c-grid4x4-fewshot
```

---

## 1. Where things stand (verified by commands on 2026-09-25, ≈ 20:00)

- **P7.3d is MERGED** at `cdec88e` (two parents; `docs/reviews/P7.3d.md`, PASS-WITH-FINDINGS, 0 blocking; `DEFERRED` 94–95
  parked). The grid4x4 zero-shot point: ρ_sumo(`b_mean_k100`, `E_sumo`) = +0.8855 [+0.8736, +0.8973], n = 100; H3's clause 1
  holds (confirmatory), clause 2's inequality holds; the artifact `docs/data/p7_3d_grid4x4.json` = `c63c371f…`.
- **CI after the merge was RED FOR A REAL FAILURE** (run `36131232681`: both legs 2,442 tests, 242 skipped, 1 failure): a
  P7.3d test's negative control depended on how the machine installed the package. Reproduced both ways by the coordinator
  with a CI-like interpreter built in a scratch venv (this `.venv` untouched); the whole suite under it fails that one test
  and no other. **Fixed by `BRIEF_39` Amendment E** (`19e234c`, the implementer; verified by the coordinator under BOTH
  interpreters, mutant M1 killed under both) and **merged at `019ad68`**. The run that merge triggered, **`36170411512`**, is
  GREEN but for the ceiling on both legs (2,442 / 2,200 passed / 242 skipped / 0 failures, legs identical), and the ceiling is
  built from it: **`docs/patches/ci_gate_ceiling_242_p7_3d.patch`**, waiting for the author (§2 item 1).
- **A24 is REGISTERED:** `v2.5-prereg-a24` → `641e1da`, tag object `e9dc1bd`; `PREREGISTRATION.md` sha256 `da87e8f4…`, blob
  `16c154da…`, recorded in the tag and verified from a FRESH CLONE of the remote. It declares P7.3c on grid4x4 (the few-shot
  curve, where H3's clause 3 is testable), amends A20(b) and A20(e) for it, and fixes clause 3's criterion before any few-shot
  number exists: **Δ₁₀₀'s paired 95% CI entirely above zero AND Δ₁₀₀ ≥ G/2, G = 0.11453573207299894** — a labelled,
  exploratory four-way partition. One review round before registration (`docs/reviews/A24-proposal.md`: 3 blocking, 12 major,
  all adopted); the author approved revision 2 with exactly one change (`BRIEF_41`); the registered row differs from revision 2
  (`206bd22`) in exactly two word runs — that change and the registration-time search — shown to the author.
- **`BRIEF_41` is ISSUED** (`3882344`) and the implementer has STARTED: worktree `/home/filip/rltraffic-p73c`, branch
  `task/p7.3c-grid4x4-fewshot`. Its plan was committed at `b8b058d` (617 lines, excellent: every seam re-verified, two real
  brief/repo conflicts found and resolved) and **gate G0 PASSED — `BRIEF_41` Amendment A (`69edd9e`)**: every proposal
  accepted; two additions (`DEFERRED` 95's T-regress moves to C5 with the report-row extraction; the corpus gate requires
  `engine_seed_drawn` 437485271, the value on all 700 committed P7.3d cells); the build-once contingency pre-ruled; four
  corrections to the brief. The implementer builds C1 (tests first) and C2 and stops at G1.
- **The plan is v2.40** (`3882344`); this handoff and the ceiling's row come after it.
- **Nothing runs.** No campaign, probe or training is in flight; no token is on disk.

## 2. Open items, in order

1. **The CI skip ceiling — BUILT, waiting for the author.** `docs/patches/ci_gate_ceiling_242_p7_3d.patch` (193 → 242, OBSERVED on
   run `36170411512` at `019ad68`; its `docs/patches/README.md` entry carries the one apply-and-commit command). It also CORRECTS the
   193 entry's and the 191 entry's false narrative (§4). After the author applies it: confirm the run his commit triggers is GREEN
   on both legs. The P7.3d builder and its classification stay in `output/ci_runs/` (`build_ceiling_patch_p73d.py`,
   `classification_p73d.json`); **for the next ceiling (P7.3c's merge) copy the builder, rewrite its narrative and its git guard
   for THAT event, and `skips.py` takes the OLD run first.** Do not push to `main` while a CI run you need is in progress.
2. **`BRIEF_41` gate G1:** when the implementer says *"P7.3c C1–C2 done"*: push the branch (the coordinator pushes); spawn ONE
   `contract-reviewer` (≤ 15 min, findings file) on C1–C2 whose mandate includes C2's pre-flight checklist (Amendment A, Q11);
   re-run the named mutants yourself from a throwaway worktree with COMMITTED mutants (T-17f's two, T-regress (a)'s); rule as
   Amendment B. Then the author's corpus token (≈ 1–1.5 h, one process), then G3: A17(f) 1,600/1,600 bit for bit and
   `engine_seed_drawn` 437485271 on all 100.
3. **Then `BRIEF_41`'s gates in order** (§5 of the brief, thirteen): the corpus door review (G1), the corpus token (G2) and its
   verification (G3: A17(f) 1,600/1,600 bit for bit), the training review (G4), the fenced timing (G5), the thirty trainings
   (G6–G7), the pre-flight (G8), the campaign (G9), the read (G10). **Cost on measured bases:** ≈ 5.6 h of GPU for the trainings,
   ≈ 12.8 h for 4,700 cells; several implementer days before the first few-shot number.

## 3. Rules added this cycle that a fresh session must apply without being told

- **A registration row is built MECHANICALLY from the approved text** (a script joins each cell's paragraphs) **and word-diffed
  against the approved revision; the diff is shown to the author at the tag.** "Approved with one change" is then a checkable
  statement. The script and its output: `docs/notes/P7.3c_A24_DRAFT_2026-09-25.md`'s header and the plan's row of 2026-09-25.
- **Identifiers are READ, never inferred:** list `docs/briefs/` before numbering a brief (§4 below), `git tag -l` before naming
  a tag, the table before numbering a row.
- **A test's negative control must not depend on the machine's installation:** a decoy must be the complete shape it claims
  to be (Amendment E). When CI and this machine disagree, build the other machine's condition in a scratch venv and run the
  suite under it — that is how the class was measured as one member, not assumed.
- **A planning document gets ONE external review round before it freezes** — and it paid: the A24 review found three defects a
  tag would have made permanent.

## 4. What a fresh session must distrust, from this cycle's own errors (the coordinator's, all logged in §8)

- **`BRIEF_40` was taken.** The coordinator numbered P7.3c's brief `BRIEF_40` without listing `docs/briefs/`, where P5.4's
  `BRIEF_40` had stood since 2026-09-19; the author's advisory chat caught it before the tag; renumbered `BRIEF_41` everywhere,
  history marked in place.
- **A24's first draft** said an existing recipe could warm-start the subject (none can: the fine-tune is new code), that the
  subject's window constructor keeps statistics frozen (it refits them by default), and reduced a registered ablation by a
  route whose condition did not hold. Read the registered row, never revision 1.
- **`skips.py`'s argument order** was reversed in the coordinator's own ceiling note: OLD run first.
- **The committed 193 ceiling entry told the wrong story** until the 242 patch is applied: written on 2026-09-20/21 by a copy of the
  builder hardcoded for P7.3b, it named P7.3b's CI-fix merge for a run on P5.4's merge (`a80006b`), said `35451103010` carried 192
  skips (190), and gave the 189 entry's text count as the 191 entry's. The numbers were computed and stand; the narrative was
  copied. A builder's narrative is re-written for every event, and checked against `git`.
- **Every number in `BRIEF_41` §0 was measured on 2026-09-25**; re-measure before relying on any of them for a new decision.

## 5. Models, by date, for the AI-assistance record

- **Coordinator:** this session ran as **`claude-opus-5-5[1m]`** (Opus 5.5, 1M context) for all of 2026-09-25 — the ceiling
  investigation, Amendment E, A24 (both revisions, its registration and tag), `BRIEF_41` and this file. Earlier dates:
  `docs/notes/HANDOFF_2026-09-23.md` §5.
- **The author's advisory chat** (claude.ai; reads the public remote, never writes; its texts relayed by the author, marked as
  its own): `claude-opus-5-5` since 2026-09-23 00:48. On 2026-09-25 it found the `BRIEF_40` collision; the coordinator verified
  it on disk before acting.
- **Implementer (P7.3d CI fix, `19e234c`):** the model the harness reported as `claude-opus-5` at session start and
  `claude-opus-5-5` later in the same session, as its commit body records. **Implementer (P7.3c):** to be recorded from its plan.
- **Reviewers and surveyors:** `contract-reviewer` subagents — the P7.3d merge review (two mandates), the two P7.3c code surveys
  (`docs/notes/P7.3c_SURVEY_2026-09-25.md`), and the A24 review (`docs/reviews/A24-proposal.md`).
