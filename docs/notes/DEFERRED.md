# Deferred items — the parked list

**Created 2026-08-06** at the user's request, because the pace is high enough that parked items stop
being visible and get rediscovered in September. **This file is the parked list; if something is not
here, it is not parked, it is forgotten.** Update it in the same turn as any deferral.

**Filter in force since 2026-08-06:** does it produce corpus data, or unblock something that does?
If neither, it waits. **September target:** C1 ladder + P4 guaranteed; P5 stretch.

---

## Does anything block P3 (dataset loader)?

**No.** P3 needs the corpus (exists, 4800 episodes), the format (v1.0, stable), and the C6 alignment
convention — **verified, but not by the identity this line used to cite.** *(Corrected 2026-08-07: the
"logged rewards reproduce from raw lane counts, misaligned control differs by 16" claim was measured on
hz1x1 only. C6 holds by a paired test, 138/138 episodes across all 69 dirs and 7 tiers, plus an exact
local-reward identity on 92/92 hz1x1+grid4x4 episodes. See the Decisions Log row of that date.)*
**P3 can start immediately** — and did, in a worktree, 2026-08-07.

Two items are *adjacent* to P3 but do not block it:
- **P2.4 corpus linter** — validates P3's input rather than gating it. Worth having first, not required.
- ~~**ATT re-collection** (`datasets_att/`)~~ — **that campaign DIED** (contract C8: every MAPPO tier
  raised). Superseded by **P2.6 / BRIEF_08**, which carries ATT as a first-class logger field at format
  v1.1 instead of as a metric. P3 codes against the v1.1 spec and gates the field on `format_version`.

---

## Parked, with why and what it costs to resume

| # | Item | Why parked | Cost to resume | Needed by |
|---|---|---|---|---|
| 1 | ~~**P3 loader brief**~~ **ISSUED 2026-08-06** as `BRIEF_09_p3_dataset_loader.md`. It was overdue by three turns — logged rather than quietly closed, because the parked list existing is what surfaced it. | — | — | done |
| 2 | **P2.4 corpus linter** | Promised across several turns, never written. The checks exist as ad-hoc scripts run on 2026-08-06 (hash uniqueness, draw disjointness, `base_seed`, reward double-compute, ladder, coverage). | ~1 day; content already specified by those scripts | Before P4 consumes data at scale |
| 3 | **Horizon re-evaluation of MAPPO checkpoints on held-out draws 1000–1099** | Needs the P8.0 horizon reader wired to `_load_agent`. | ~half a day + minutes of compute | Before the online MAPPO baseline is *reported* |
| 4 | **P7.0 / all of C3** (`BRIEF_04` written, unrun) | Off the September path; the plan already designates C3 droppable. Parity contract and brief are complete and waiting. | ~1 day for the gate | October |
| 5 | **Draw-cycling trainer + `mappo_dr`** (`BRIEF_07` §5) | Serves C2's 2×2, which is October. Carries the constraint that it must reproduce `_train_agent`'s returns bit-exactly on draw 0 or the 2×2 is confounded. | ~1 day + a training run | Before C2 |
| 6 | **`dqn` as a second learned algorithm** | Cheap credibility against "your MAPPO was weak". Already registered — config only. | one config + ~2 h training + ~15 min collection | Nice-to-have for September |
| 7 | **`dqn_pfrl` (RESCO parity)** → P11.7 | **Not a config**: `pfrl` is not installed and `experiments/registry.py` is frozen. Needs a new dependency *and* an authorised patch. | new dep + frozen patch + training | Journal version |
| 8 | **DTRL external anchor** → P11.6 | Strongest external comparison available; format adapter + their average-delay metric needs C3-grade care. | ~1 week | Journal version |
| 9 | **`classify_draw_pool` must refuse ids ≥ 1100** | Ruled 2026-08-06, not implemented. Nothing currently requests those ids. | ~10 lines + a test | Before anyone materialises outside the registered pools |
| 10 | **`collect.py` should derive draw id from provenance** | Currently moot: we use `collect.py`'s own draw mechanism, which records `flow_draw` correctly. Only bites if we point it at a materialised config. | ~half a day | Before collecting from `scenarios/draws/` |
| 11 | **P0.9 `check_english.sh` o-acute false positive** | Fires on every edit to `claude_guard.sh`. Patch route (permission-denied path). | ~1 h | Whenever the guard is next edited |
| 12 | **cologne3 peak-vehicle re-measurement** | The 2026-07-27 "532 of 536 still in network" figure is contradicted by a reproducible measurement (~42–50). Old figure unreproducible (session scratchpad). | ~10 min | Before cologne3's "unresolved state" caveat is cited |
| 13 | **cologne3 engine-seed stochasticity — mechanism unidentified** | Measured (σ=3.32 vs 0.0000 elsewhere); `laneChange` refuted. **Quantified 2026-08-07: 7.7 % of draws (3/39) diverge bit-wise under identical code and a deterministic policy, grid4x4 control 0/6; tier means stable to 0.080 %.** Now affects `compare_corpora`'s criterion, P2.4's duplicate detector, D4's held-out pool and A5's shared-draw rule — all swept the same day. Affects P8 CIs and P2.4's duplicate detector on that scenario. | ~half a day | Before P8 statistics |
| 14 | **Extended MAPPO training toward the convergence label** | Declined 2026-08-06: the criterion failed at B=1000 and the label buys nothing C1 needs, since tiers are by measured return. | ~4.5 h | October, optional |
| 15 | **Fixed-time per-phase green splits (Webster-style)** | Declared limitation; only cycle length is tuned. | ~1 day | Only if a reviewer presses |
| 16 | ~~**Master session restart** so the two new agent rules load~~ ✅ **CLOSED 2026-08-07.** Verified in the artifact, not assumed: `.claude/agents/master-coordinator.md` contains "Grep the plan before you escalate" ×1 and "THINGS YOU NEED TO DO" ×3, and the restarted session's own instructions carry both. The decision-line rule (`f9ab0ad`) is loaded too. | — | — | done |
| 18 | **`info["average_travel_time"]` is verified metric-set-independent on CityFlow ONLY** | P2.6 §0.1 measured 3 scenarios × 2 policies × 61 rows, 6/6 bit-identical — **all CityFlow**. The supporting source argument (`_run_step_hooks` sections 1–3 run unguarded) is a claim about `metrics/cityflow.py` and does not transfer. `sumo_env.py:316` emits the key, but whether the value is correct when the metric is **not requested** is unmeasured on SUMO. | ~1 h: one paired rollout, 1-metric vs 3-metric env, `np.array_equal` on the ATT sequence | **Before any SUMO collection at v1.1** — i.e. P7.0/C3. A wrong-but-plausible ATT column in the SUMO corpus is exactly the silent-number failure this repo exists to prevent |
| 22 | **RETRO-REVIEW P8.0 — the statistics harness merged unreviewed, and queue item 0 depends on it** | §7 names "statistics harness" in its critical-path list; `docs/reviews/` holds exactly one file (`P0.3-fix.md`). P2.5, P2.2-draws and P8.0 all merged after the 2026-08-03 `docs/reviews/` convention with no review filed. **P8.0's merge gate does not discriminate the thing it introduced:** reproducing `att_running_mean` bit-for-bit proves the pipeline is unchanged, not that the horizon *extraction* is right -- a reader taking `[-2]`, a max, or the last non-NaN passes it untouched. | ~half a day, after P3's review | **Before queue item 0's numbers are believed.** Settling the suspended cells with a wrong reader is worse than leaving them open |
| 23 | ✅ **ANSWERED by P7.0 (2026-08-16) — AND THE ANSWER IS WORSE THAN THE ROW ANTICIPATED, IN BOTH DIRECTIONS.** **The row asked whether CityFlow and SUMO enumerate intersections differently. P7.0 read both backends side by side for the first time and found: (a) on `cf_cologne3`, the third intersection is `cluster_…` in CityFlow and `GS_cluster_…` in SUMO — so ACROSS BACKENDS neither ids NOR positions are a valid intersection key**, which is the opposite of the failure this row anticipated and a **P7.3 prerequisite nobody had written down**; and **(b) the same class exists one level down, at LANE granularity, and it is worse because it is silent** — `CityFlow road_0_1_0 lane _0 = turn_left, lane _1 = go_straight` against `SUMO lane _0 = "s", lane _1 = "l"`, **exactly inverted on all four incoming roads**, verified by the coordinator from the network files alone. **8 of 8 lane ids mis-denote.** ⚠️ **That defect voided P7.0's registered branch criterion** — the by-lane-id table compared a left-turn lane against a through lane and returned `min OVL 0.0554`, which would have descoped C3 on an indexing convention. **The general rule is now in `PROJECT_PLAN` §7: any cross-system comparison must PROVE its pairing key from structure alone, before using it.** Original entry: **ID-keying has ZERO coverage from real data — a C3 prerequisite, not a P3 defect** | Verified on the population by the P3 reviewer: all 4800 episodes, `ix_ids` already sorted 1600/1600 per scenario, **1 distinct order each**. Positional-keying mutants kill 12 and 11 synthetic tests and **0 corpus tests**. So ID keying rests entirely on one two-intersection synthetic fixture. The mapping is actually built in `offline/trajectory_logger.py:262-272`, outside P3's diff. Failure mode is **silent whole-intersection trajectory swapping**. | ~1 h once a corpus with unsorted `ix_ids` exists | **Before any SUMO corpus is trusted (P7/C3)** — CityFlow and SUMO may enumerate intersections differently, which is the case the fixture stands in for |
| 24 | **P3's nine unverified loader-validation behaviours (P3-09) need effect-tests** | Mutants `AA`, `Z`, `AC`, `AL`, `AB`, `AD`, `AE`, `AG`, `AH` all survive the full suite: shape check, extra `.npz` absent from manifest, requested draw absent, manifest/episode version disagreement, missing file, `load_stats` version check, empty batch, stats width, missing `scenario_id`. Behaviour is correct; nothing pins it. Packet wording corrected at merge; the tests belong with the corpus validator. | ~half a day | P2.4, whose job corpus validation is |
| 25 | **P2.4's duplicate-`episode_sha256` detector MUST exempt cf_cologne3 by name** | Required by ruling `0398039` and by `PREREGISTRATION.md` §5 as amended the same day. **The guard does not exist yet** — `offline/corpus_format_check.py` checks versions and metric sets and has no duplicate detector, so this is not "handled" by anything currently merged. On cologne3, **non-duplication is no longer evidence of distinct draws** (7.7 % of draws diverge under identical code) **and duplication is not guaranteed for a repeated one**. Flagged by P2.6 rather than assumed away. | folded into P2.4 | **P2.4, and it must be in the brief explicitly** |
| 26 | **Automate the checklist drift check — as a TEST, not a guard patch (ruled 2026-08-07)** | The user proposed hooking `scripts/claude_guard.sh`. **Ruled the other way, with reasons:** the guard is frozen, so it needs a patch plus a human application for a check that touches no frozen file; every other repo-wide invariant in this project already lives in `tests/` (`test_offline_naming_guard.py`, `test_claude_guard.py`); and a test can carry a **positive control proving the scanner can fail**, which a shell clause cannot easily do — the lesson from the 2026-08-03 harness that returned BLOCKED for every path including its control. **Acceptance:** parse `docs/returns/*.md` on `main`, assert each has a ticked §6 box; **a positive control on synthetic input proving the scanner flags a fabricated drift**; and it must go **red** when a known-ticked box is un-ticked. Trade-off accepted and stated: a test fires on every suite run rather than on every tool call, so feedback is slower than the guard's — but nothing merges here without the suite. | ~20 lines + a test | **P2.4** (next task touching `tests/`); it closes a failure that has now occurred twice in opposite directions |
| 27 | **P2.6's test-strength defects (review D2-D15) — correct artifacts, unprotected** | Retro-review 2026-08-08. The **delivered corpus is confirmed correct** by a 69-manifest field-by-field diff, so these are test defects over right answers, not data defects. Worst: `recollect_v11`'s derivation loses **14 of 17 mutants** including dropping `--checkpoint`, the only field separating the five MAPPO seeds; the ladder's **JSON-writing path has zero tests**, so A5's `draw_ids` and co-report are enforced in the artifact but not on the path that made it; the co-report fixture is **constant in time**, so `[-1]`→`[0]` is invisible; and two tests (`..._refuses_without_the_backup_directory`, `..._before_running_anything`) **pass for the wrong reason**, proven. | ~1 day | **Before the campaign tooling is re-run or reused** — it is correct today and nothing would say if it stopped being |
| 28 | **Interleave seed and draw in any future collection campaign** | Review D16: the five MAPPO seeds occupy disjoint contiguous 40-draw blocks, so seed and demand are perfectly confounded and the seed variance component is not estimable. Costs nothing at collection time to interleave; unfixable afterwards without re-collecting. | free if done up front | Any future campaign; the held-out crossed design covers all *reported* comparisons meanwhile |
| 29 | **Canonical checkpoint digest — the forward fix that makes reproducibility testable at all** | A checkpoint's file hash depends on **both** its filename (the `torch.save` zip root) **and** its provenance block, so **no** claim of the form *"the model reproduces byte-identically"* is testable at file level — not this one, and not P5's, P7's, or the paper's reproducibility section. **Spec:** sha256 over the `state_dict` tensor bytes in **sorted key order**, filename- and provenance-independent; ~10 lines. Keep the file sha256 for what it *does* prove — **transport integrity**. | ~10 lines + a test | **Introduced by P4.4** (it produces BC/%BC checkpoints); cited by the reproducibility section thereafter |
| 30 | **Test the retired-phrase class, in the shape of `test_offline_naming_guard.py`** | A table of retired phrases, asserting every live occurrence in `docs/` sits within N lines of a dated correction marker or is in an explicit allowlist. **It would have caught all three of the consecutive sweep misses.** Same family as `DEFERRED` 26 (the checklist drift test) and could share its harness. | ~30 lines + a positive control | With row 26, whoever next touches `tests/` |
| 31 | ✅ **DISCHARGED by P4.3 (2026-08-13), marked here 2026-08-14 — the row sat DONE-BUT-UNMARKED for a day.** `docs/data/p4_3_p4_gate_effect_sizes.json` exists on disk (2,609 bytes) and §6's P4.3 entry records it, with `p4_gate.json` untouched as required. ⚠️ **The drift is the lesson: this file's own header says "if something is not here, it is not parked, it is forgotten" — and the converse failed. A row that is DONE but unmarked makes the parked list overstate the backlog, which is the same class as a stale §6 checkbox, and it put a discharged item into §10's queue.** Original entry: **Back-fill effect sizes into P4's artifact — §8 makes them mandatory and P4 reported none** | Flagged by the P4.4 implementer, correctly scoped as outside its fence. §8: *"Effect sizes are mandatory and reported next to every p-value."* `p4_gate.json` carries three Wilcoxon results with none. The paired per-draw differences are already in the artifact, so this is arithmetic over committed data, not a re-run. | ~1 h, no compute | **Before any P4 number enters the paper**; the gate verdict itself is unaffected |
| 32 | **P4.4 review F5 — the tautology that was found and fixed can be silently restored** | `_run_report`'s completeness derivation can be reverted to the exact tautology the packet's §6.4 discloses finding, with 58/58 green: **the fix has no test.** Second half: the baseline arms' expected seed set is derived from `training["runs"]` (data) while `madt`/`mappo1000` correctly use `TRAINING_SEEDS` (declaration), so a 3-seed method would pass the completeness check. | ~30 min + a test | Whoever next touches `offline/offline_baselines.py` — **P4.3 will** |
| 33 | **P4.4 review F6 — two working artifact guards with no regression protection** | The unbalanced-design cross-check (`abs(direct − paired) > 1e-9`) and the δ-rounding cross-check both survive being disabled, 58/58 green. The reviewer confirmed the first **does** fire when provoked (drop one `bc` episode → `ValueError: the recovered fraction for 'bc' disagrees between its two routes`), so these are right answers with no protection, not broken guards. | ~30 min | With row 32, same file |
| 34 | **P4.4 review F10 — `eval_{method}.json` records no `env_settings`** | The artifact's settings come from `report`'s own `--manifest`, so a different manifest on one invocation of a **chunked** campaign would be undetectable from the artifacts afterwards. Closed **empirically** for 16 cells across all three methods (bit-exact re-rolls), never **structurally**. Matters more for P4.3/P5, which will chunk harder. | ~20 min | Before the next chunked evaluation campaign |
| 35 | **P4.4 review F11 — AWR uses the pre-V-step `v_pred`; reference IQL uses the post-update `V`** | A one-gradient-step staleness at lr 1e-4, numerically negligible. It matters only to the claim *"published values, unchanged"*, which is load-bearing for the binding IQL sentence: an untuned-from-published baseline should be exactly that. **Either declare the deviation or align it** — and if aligned, it is a retrain, so it cannot be folded into a fix round. | declare: minutes · align: a retrain | **Before the IQL number enters the paper** |
| 36 | **P4.4 review F12 — BC/IQL checkpoint provenance omits `warmup_steps`, which the DT records** | `_schedule` applies it; only the record is asymmetric. Cosmetic, but the reproducibility section will compare these provenance blocks side by side. | ~10 min | P10.0 (release packaging) at the latest |
| 37 | **P4.4 review MU5 — feeding `act()` the wrong intersection's statistics is an EQUIVALENT mutant here and a LIVE defect at P5** | Survives 58/58 because every episode in this tier carries `ix0_*` only, so `intersection_ids[0] == ix_id` always. **The moment a multi-intersection corpus exists the mutation stops being equivalent and nothing catches it.** Same family as row 21 (masks never bind): a guard that cannot be exercised by the data it ships with. | ~1 h, needs a 2-intersection fixture | **P5.1's brief, explicitly** — not "when convenient" |
| 38 | **P4.5 — BC on MAPPO seeds 101+202 only, against %BC. NOT deferred: promoted to the §6 checklist the same turn, and listed here only so the parked list does not imply it was forgotten** | The decisive test of the mechanism P4.4 uncovered. It is out of `BRIEF_12`'s fence (§5 forbids any retraining) and must not creep in — hence the row: **an idea named in a brief's prose and nowhere else is an idea that does not happen.** Hypothesis registered before the run: if BC-on-best-two ≈ %BC's 103.16, the top-10 % filter's advantage is checkpoint selection; if %BC still wins, a within-policy episode-quality effect exists on top. | ~1 h compute + one session; `filter_stacked_to_streams` already takes an arbitrary stream set | **After `BRIEF_12` merges**, alongside or just before P4.3 |
| 39 | ✅ **DISCHARGED by P4.3 (2026-08-13) and PROVEN IN USE by P4.6 (2026-08-14); marked here 2026-08-14.** P4.3 shipped the split in `offline/dt_gate.py` and made it refuse to cite a commit unreachable from `HEAD`. **P4.6 is the first genuinely chunked campaign to rely on it, and the independent reviewer verified it end-to-end:** `runtime.measurement_git_commits` = `{0d3ccce, 13732f3, 1f35ea3, 801f28b}`, **exactly the union it enumerated from the 19 raw JSONs plus 4 inputs, all reachable, `unreachable_measurement_commits: []`.** The original defect is now impossible to reproduce: a tmux campaign spanning two days and four commits records all four rather than the one that happened to be `HEAD` at write time. Original entry: **A single `runtime.git_commit` cannot express a chunked campaign's provenance — split it into MEASUREMENT provenance and WRITTEN-AT commit** | `runtime_provenance()` records `git rev-parse HEAD` **at write time**, so (a) an artifact can never be committed at the commit it records — committing it moves HEAD, the same fixed point as a document hashing itself — and (b) it is already **wrong for a chunked campaign**: measured 2026-08-12, `output/p4_4/gate_a.json` carries `738884b` while the three `eval_*.json` carry `c13aaa9`, so the report's single `c13aaa9` means *"when the report was assembled"* and **no single commit produced all the measurements**. It also silently records `''` when run from a directory outside the repo. **Fix:** carry a frozen `measurement_git_commit` (or a per-input map) from the inputs, and record the writing commit separately. Same family as row 29 — an identity claim resting on a container property rather than on content. **Cost of not fixing it:** every regeneration overwrites real provenance with the provenance of the act of regenerating. | ~30 min + a test | **P4.3**, which writes new artifacts and can adopt the split from the start rather than migrating |
| 40 | ✅ **DISCHARGED 2026-08-12 — I read OffLight's Table II myself** (`arxiv.org/html/2411.06601v3`, after the PDF route failed: no `poppler-utils`, no `pymupdf`). **Every relayed number matched to the cent**, and the table gave two things nobody had flagged — see the Decisions Log row of 2026-08-12. Original entry follows. ⚠️ **Superseded claim about OffLight (arXiv 2411.06601) Table II — read the PDF before any of it enters a brief or the paper** | Relayed 2026-08-12: *"BC is the worst offline method in all nine cells"*, with figures 364.50/388.18/409.36 (Jinan), 382.59/393.38/408.46 (Hangzhou), 561.07/595.22/617.59 (Manhattan), losing to TD3+BC and CQL at 33/50/67 % expert; plus *"same reward, r = −Σ q_l"*, *"Hangzhou is 16 intersections"*, and *"no CIs or statistical tests in the tables"*. ⚠️ **I confirmed only the paper's existence, title, authors (Bokade & Jin) and date (10 Nov 2024, v3 Mar 2025) — the abstract page carries none of the table.** The claims are plausible and would make P4.6's registered prediction much stronger, because they supply the **other endpoint** of C1's curve from an independent group. **They are hearsay until someone opens the PDF.** | ~30 min reading | **Before P4.6's brief is written** — the prediction is only worth registering if its stated basis is true |
| 41 | **The TEST SUITE can deadlock unpinned — second sighting of the liveness class, first one inside `pytest`** | Reported by the P4.5 session 2026-08-12: `pytest -q` ran **8 minutes at ~0 % CPU with 35 threads in `futex_do_wait`** (read from `/proc/<pid>/wchan`) and was killed; with `OMP_NUM_THREADS=1 MKL_NUM_THREADS=1` it gave **612 passed, 1 skipped in 69.43 s** — the same 613 collected as P4.4's pinned run, so **the pin moves no outcome, only liveness**. It is **intermittent**: I ran the same suite unpinned twice the same day (76.09 s, 79.58 s) without a hang. First sighting was P4.4 §6.2, in the evaluation path. **Not fixed in P4.5** — a `conftest.py` pin changes every test run's threading and is a decision about test infrastructure, not about a result. **Interim rule, already binding: every reported suite run states whether it was pinned, and every background job sets the pin.** | ~1 h + a decision on `conftest.py` | Whoever next has a reason to touch test infrastructure; **or immediately if a third sighting costs someone an hour** |
| 42 | ✅ **DISCHARGED by P4.6 (2026-08-14), `BRIEF_17` §8.** Four guard branches addressed, **one proved unreachable by construction** rather than papered over, **6/6 mutations killed with every failure pasted** (commit `ea839eb`). Original entry: **P4.5 review N2 — four new guard branches survive being disabled with the suite green** | `assert_selection_design`'s composition-sum check, `select_arm_streams`'s pool-versus-declared-seeds check, and two `selection_artifact` refusals. The reviewer judges the first two **equivalent on today's data** and the last two **unable to fire on a complete campaign** — so these are right answers with no protection, not broken guards. **F6's class, recurring**, and it is the second time this family has been queued rather than fixed (`DEFERRED` 33 is P4.4's). ⚠️ **If it appears a third time, stop queueing it and fix the class**: the pattern is a guard whose failing branch no correct campaign ever reaches, which means only a deliberately malformed fixture can exercise it. | ~1 h with fixtures | Whoever next touches `offline/offline_baselines.py` — **P4.6 will** |
| 43 | 🟡 **STILL PARTIAL after a SECOND round in P4.7 (2026-08-15) — and the row's own enumeration is now known to be STALE.** P4.7 measured **77 fragile-shaped sites → 28 ambiguous → 11**, from **267 captured `raise` messages** rather than from this row's list, and lengthened **16 tokens across 5 pre-existing files** (a 17th is in a file P4.7 created). ⚠️ **The implementer reports that several tokens enumerated below NO LONGER EXIST**, so the list is a historical record and not a work queue — **re-measure before acting on it.** ⚠️ **Not verifiable by the reviewer and disclosed as such:** the totals depend on the implementer's own "fragile-shaped" heuristic and on a plugin that lived in `/tmp` and is not in the diff; only the 17 token changes were checked, **and none weakens an assertion.** Coordinator's own count 2026-08-15: **240 `match=` assertions** across `tests/*.py`, the most repeated tokens being `symmetric difference`, `not disjoint` and `incomplete campaign` at 3 each. Original entry, **partially discharged by P4.6 (2026-08-14), `BRIEF_17` §8 — the `offline_baselines.py` share only.** Measured rather than estimated, which was the instruction: **63 `match=` sites examined, 10 fragile, 8 lengthened, 4 unfixable by token** (two pairs of identical messages raised from two different functions — a real limit of the token approach, reported rather than forced). Commit `18a4d6d`. **The remainder of the enumerated 73 stands below, untouched.** ⚠️ **Do not treat this row as closed.** Original entry: **The fragile-text-match class across the rest of the suite — 73 sites, ENUMERATED here so the sweep cannot be lost** | Third sighting of the class (`PROJECT_PLAN` §7, 2026-08-12): a text match that succeeds or fails for a reason the author did not intend. Measured, not estimated: **136 `match=` assertions in the suite, 73 of the fragile shape** (single or two-word token, no alternation). `BRIEF_14` fixes the 21 in the two files P4.5 owns; **these are the remainder**, and the fix for each is the same measurement — grep the module under test for the token across every `raise` message, and change only those with a count above 1. ⚠️ **Not a mass rewrite: blind lengthening is the reflex that caused the problem.** Full enumeration: tests/test_base_traffic_env.py:113 match='second dimension'; tests/test_callable_reward_validation.py:28 match='metrics='; tests/test_dt_agent.py:473 match='reward'; tests/test_dt_agent.py:481 match='expected decision'; tests/test_dt_agent.py:487 match='n_actions'; tests/test_dt_agent.py:640 match='n_actions'; tests/test_dt_gate.py:192 match='paired'; tests/test_dt_gate.py:259 match='void'; tests/test_dt_gate.py:274 match='window'; tests/test_dt_gate.py:285 match='three'; tests/test_dt_gate.py:309 match='held-out'; tests/test_dt_gate.py:352 match='groups'; tests/test_dt_gate.py:429 match='gradient steps'; tests/test_dt_gate.py:456 match='one arm'; tests/test_dt_gate.py:480 match='declared budget'; tests/test_dt_gate.py:552 match='local_reward_fn'; tests/test_experiments_config.py:87 match='person_file'; tests/test_fixed_time_env_mapping.py:95 match='mapping mismatch'; tests/test_fixed_time_policy.py:127 match='signal plan'; tests/test_fixed_time_policy.py:267 match='ladder'; tests/test_flow_randomizer.py:259 match='aggregate'; tests/test_flow_randomizer.py:306 match='reproduce'; tests/test_flow_randomizer.py:331 match='aggregate'; tests/test_flow_randomizer.py:563 match='begin'; tests/test_flow_randomizer.py:865 match='cityflow'; tests/test_mappo_c8_metric_keys_guard.py:196 match='symmetric difference'; tests/test_mappo_c8_metric_keys_guard.py:273 match='global_metric_keys=None'; tests/test_mappo_c8_metric_keys_guard.py:336 match='symmetric difference'; tests/test_mappo_c8_metric_keys_guard.py:363 match='symmetric difference'; tests/test_materialise_draws.py:481 match='scenario key'; tests/test_migrate_mappo_checkpoints.py:206 match='global_feature_dim'; tests/test_migrate_mappo_checkpoints.py:224 match='already records'; tests/test_migrate_mappo_checkpoints.py:257 match='global_feature_dim'; tests/test_offline_baseline_agents.py:419 match='n_actions'; tests/test_offline_baseline_agents.py:421 match='n_actions'; tests/test_offline_baseline_agents.py:428 match='scenario_id'; tests/test_offline_baseline_agents.py:527 match='n_actions'; tests/test_offline_baseline_agents.py:545 match='statistics'; tests/test_offline_baselines.py:298 match='group'; tests/test_offline_baselines.py:306 match='constant'; tests/test_offline_baselines.py:373 match='shared draws'; tests/test_offline_baselines.py:419 match='zero'; tests/test_offline_baselines.py:452 match='incomplete'; tests/test_offline_baselines.py:462 match='not requested'; tests/test_offline_baselines.py:568 match='madt'; tests/test_offline_baselines.py:706 match='declared'; tests/test_offline_baselines.py:756 match='held-out'; tests/test_offline_baselines.py:1142 match='equivalence margin'; tests/test_offline_baselines.py:1271 match='not trustworthy'; tests/test_offline_baselines.py:1443 match='incomplete campaign'; tests/test_offline_baselines.py:1460 match='incomplete campaign'; tests/test_offline_baselines.py:1500 match='two routes'; tests/test_offline_dataset.py:655 match='held-out'; tests/test_offline_dataset.py:662 match='1000'; tests/test_offline_dataset.py:685 match='split'; tests/test_offline_dataset.py:690 match='context_length'; tests/test_offline_dataset.py:724 match='absent-draw sentinel'; tests/test_offline_dataset.py:738 match='flow_draw'; tests/test_offline_dataset.py:745 match='NaN'; tests/test_offline_dataset.py:1087 match='NormalizationStats'; tests/test_offline_selection.py:323 match='behaviour seeds'; tests/test_offline_selection.py:327 match='count'; tests/test_offline_selection.py:331 match='selector'; tests/test_offline_selection.py:338 match='unknown arm'; tests/test_offline_selection.py:446 match='held-out'; tests/test_offline_selection.py:483 match='composition'; tests/test_offline_selection.py:494 match='normalisation'; tests/test_p0_baseline_anchors.py:222 match='double-compute mismatch'; tests/test_p0_baseline_anchors.py:242 match='double-compute mismatch'; tests/test_phase_control.py:162 match='transition phases'; tests/test_rederive_anchors.py:18 match='historical'; tests/test_rederive_anchors.py:25 match='historical'; tests/test_trajectory_logger.py:580 match='average_travel_time' | ~2 h, measurement-driven | **P4.6** touches `offline_baselines.py`; the rest whenever a file is opened for another reason — and a test file must not be opened for this alone |
| 44 | ✅ **DISCHARGED by P4.7 (2026-08-15), and the closure was CONFIRMED BY THE INDEPENDENT REVIEWER rather than taken on report.** **The reviewer verified it the only way that counts: `tests/test_rtg_calibration_guards.py` (10 tests, present on `main`) kills the `ci95_half_width × 4.0` mutation that P4.3's own 51-test suite still passes green.** A test that catches what the original suite could not is the definition of closing this row. ⚠️ **But the family did not stay closed for one task: P4.7's reviewer found TWO NEW survivors in the report-assembly path** — Gate P1 made self-comparing, and the P4.6 sidecar rescue — **the sixth and seventh sightings**, both fixed in `BRIEF_20` by testing the **wiring** rather than the parts. ⚠️ **Two of the seven mutations in that round survived their own first pass**, because the implementer wrote the fields before the tests that hold them, and were closed by adding tests rather than by argument. **The durable lesson is the one this row was queued for three times: a guard's failing branch that no correct campaign reaches can only be exercised by a deliberately malformed fixture, so the fixture must be written at the same time as the guard, never after.** Original entry: ⚠️ **STILL OPEN for its own sites, but ITS TRIGGER FIRED AND WAS HONOURED (2026-08-14).** This row's condition — *"if the guard family appears a FOURTH time, stop queueing it"* — **fired during P4.6**: the independent reviewer ran four probes beyond the implementer's 22 and **all four survived with the suite green**, on quantities the paper quotes (P1's τ_b gap, whose `min`→`max` mutation **inverts the reported headline's sign**; `volume_check`'s `ddof`; the `behaviour_cells`↔training-draw distinction; the per-tier completeness refusal). **`BRIEF_18` §4 fixed all four in `method_tier_grid.py` and queued none** — six tests, 6/6 killed, every failure pasted. ⚠️ **What remains open is THIS ROW'S OWN sites, in P4.3's module** (`ci95_half_width` surviving ×4.0, Gate A's two refusals, `probe_artifact`'s disjointness, `evaluate_point`'s support-range check) — **they were not in P4.6's fence and were not touched.** ⚠️ **FIFTH sighting now recorded. The family has appeared in P4.4 (33), P4.5 (42), P4.3 (44) and twice inside P4.6 — including inside the module written by the task commissioned to close it.** Original entry: **P4.3 review F3 and F4 — an unprotected CI half-width and four working guards with no regression cover** | **F3:** the adjacent-step `ci95_half_width` survives being multiplied by 4.0 with 48/48 green, and its guarding assertion (`paired < marginal`) has **5.13x slack in the fixture against 1.72x on the real data** -- the fixture is 3x more forgiving than reality, which is its own lesson about fixture calibration. The printed half-widths (0.1169 ... 0.2070) therefore have no test behind them; the coordinator hand-recomputed the endpoint one (0.183682) and it is right. **F4:** Gate A's two refusals in `_run_report`, `probe_artifact`'s disjointness assertion and `evaluate_point`'s per-seed support-range check all survive deletion. **F6's class recurring for the third time** -- guards that exist, ran, and whose removal is invisible. | ~1 h with fixtures | **P7**, which extends this module; and if the guard family appears a FOURTH time, stop queueing it (`DEFERRED` 33, 42 and now 44 are the same shape) |
| 45 | **`scripts/check_test_hygiene.sh` EXITS 1 ON `main` — ~~17~~ **16** TH006 findings in four frozen-adjacent test files, and every task from here inherits a red guard** | ✅ **COUNT RE-CONFIRMED AT 16, FOUR INDEPENDENT TIMES (2026-08-14), after the P4.7 implementer reported 2.** Measured by me from **`main`'s tree** and again from the **`p47` worktree** — `8 test_cityflow_env_phase_constraints · 5 test_sumo_env_phase_constraints · 2 test_phase_control · 1 test_base_traffic_env` **both times**, with all four files verified identical between the trees. So the environment is **not** the explanation and this row is correct. ⚠️ **MECHANISM CORRECTED 2026-08-15 BY THE P4.7 IMPLEMENTER — MINE WAS WRONG.** I wrote here that the 2 came from a **path-argument invocation** scanning a subset (`scripts/check_test_hygiene.sh:33-46`). **Falsified by the implementer against its own saved output: the guard ran with NO arguments and emitted all 16; a `tail -12` showed the last 2, and the tail was reported as the total.** **So it was not a partial CORPUS — it was a partial VIEW of a complete run**, and `grep -c TH006` on the very file written at the time still returns 16. ⚠️ **The correction matters more than the count: I diagnosed a mechanism I had not measured and wrote it into a durable record, which is the same act this file exists to prevent.** **The real family is the one that has now bitten twice in this project in three days, both times through a pipeline element eating the measurement: `$?` after a pipe reading `tail`'s status, and now `tail` truncating a count that was then reported whole.** ⚠️ **COUNT PREVIOUSLY CORRECTED 2026-08-14 from 17 to 16, and that error was the coordinator's:** I verified the blob shas first-hand and **copied the count from the packet without running the grep**, in the same row where I wrote "verified first-hand" — the packet's own per-file enumeration summed to 16 all along. Independently counted twice since: the P4.6 reviewer got **16**, and my own recount got **16** (8 + 5 + 2 + 1). **A row that says "verified" must name WHICH claim was verified; this one verified identity and not arithmetic.** Found by the P4.6 implementer 2026-08-13, **after it corrected its own false `hygiene=0`** — it had measured `$?` following a pipeline, so it was reading `tail`'s status, not the guard's. **Blob shas verified first-hand by the coordinator:** `tests/test_cityflow_env_phase_constraints.py`, `tests/test_sumo_env_phase_constraints.py`, `tests/test_phase_control.py` and `tests/test_base_traffic_env.py` are **byte-identical between `main` and `task/p4.6-method-tier-grid`**, and the branch touches only its own four test files. **So the findings are pre-existing and P4.6 may merge with the guard red** — but that is the whole problem: **a guard that is red for reasons nobody expects to fix is a guard that stops being read**, which is how the `$?`-after-a-pipeline error went unnoticed in the first place. Either fix the 17, or make the guard's baseline explicit so a NEW finding is distinguishable from the inherited 17. ⚠️ `check_test_hygiene.sh` is a guard-level `FROZEN_EXCEPTIONS` entry and **denied at permission level**, so any change ships as a patch under `docs/patches/` (CLAUDE.md §1). | ~1 h to triage the 17; ~15 min for a baseline file | **Before the next task reads a guard verdict** — and it is the second time in this project that a status line was trusted where success and no-op are observationally identical (§7, 2026-08-07) |
| 46 | 🚨 **R2 CANNOT BE ANSWERED BY AN EXPERT+RANDOM MIXTURE, AND THE DESIGN THAT WOULD ANSWER IT IS MEASURED HERE** | **The Q1 defect was the coordinator's.** `BRIEF_19` §4 registered **Q1 — "≥ 90 % of %BC's kept top-decile streams come from the `mappo1000` dirs"** — as the direct test of R2's mechanism. **It was UNFALSIFIABLE BY CONSTRUCTION and the implementer caught it before the campaign finished.** Measured independently by the coordinator on 2026-08-15: `mappo1000` spans **[−9991, −5762]**, `random` spans **[−40294, −38369]** — a **+28,378** gap with **0 of 400 random streams above the expert minimum**, so a top decile taken by return is all-expert **with probability 1**. Q1's 18/20 floor was unreachable from below. ⚠️ **This is §7's 2026-08-07 rule — *a fixture that satisfies the assertion by construction is a tautology* — recurring one level up, at the level of a REGISTERED PREDICTION rather than a test fixture.** **What still carried information on the mixtures is A5's second axis**, which is exactly why A5 requires both: the kept decile concentrates on 3–4 of 5 expert seeds (`mix33 {101:8, 202:8, 505:4}`), i.e. **checkpoint selection — P4.5's effect — not composition selection.** ⭐ **THE FIX IS A DIFFERENT MIXTURE, NOT A DIFFERENT STATISTIC, and the corpus already contains it. Measured pairwise overlap (percentage of the weaker tier's streams above the stronger tier's minimum): `mappo1000+mappo500` 99.0 % (too similar to discriminate) · `mappo1000+mappo200` **43.5 %** · `mappo500+mappo200` 61.0 % · `maxpressure+mappo060` 20.0 % · `fixedtime+mappo060` 60.0 % · `mappo060+random` 100 % · `mappo1000+random` 0 % (what we ran).** **`mappo1000 + mappo200` at 43.5 % is the design: partial overlap means a return filter MUST discriminate and CAN fail, so the prediction becomes falsifiable in both directions.** Second candidate worth its own question: **`maxpressure + mappo060` at 20 %**, where the learned tier is *worse* on ATT (281.89 against 176.50), testing whether the filter tracks **return** or **quality** — which our ladder says are not the same ordering. | ~5 h, same machinery, no new collection | **Whenever R2 is answered — it is NOT answered by P4.7, and no paper sentence may say the mixture settled what the filter selects.** Journal version or a P4.8 if September allows |
| 47 | **CI's frozen-file guard is VACUOUS on a runner, and the non-vacuous version is a new enforcement mechanism** | P0.10 wires `scripts/claude_guard.sh --frozen-only` into `.github/workflows/ci.yml`, where it can only pass: the guard derives everything from `git status --porcelain`, which is clean on a fresh checkout. The version that would bite diffs the PR against `main` and matches the guard's own `FROZEN_PATTERNS` — but it would fire on legitimate *authorised* changes (AUTHORISATION A/B/C are all precedents), so it encodes policy, and an implementation session may not extend the guard surface that constrains it. **Coordinator ruling 2026-08-17 (`BRIEF_23` Q2): ship it vacuous and documented at the call site, park the real check here.** ⚠️ Whoever picks this up needs a rule for authorised changes first, otherwise the check's first true positive is a legitimate merge. The pattern list should be **read out of `claude_guard.sh`** rather than copied, or the two drift. | ~1 h once the authorised-change rule exists; the extraction itself is one `grep` | Before any external contributor can open a PR; not on the September path |
| 48 | 🚨 **`scripts/check_english.sh FILE` RETURNS A FALSE CLEAN, so the PostToolUse language hook has been checking far less than it appears to** | Found by P0.10 when CI (repo-wide mode) flagged a file the same guard had just cleared by path. **Two independent conditions, both measured 2026-08-17, git 2.53.0, reproduced in two worktrees.** **(a) UNTRACKED file, any depth -> exit 0** (the coordinator's diagnosis): line 47 builds the list with `git ls-files`, which lists tracked files only, and line 52 exits 0 on an empty list. This is the normal authoring order -- write, then check, then `git add` -- so it covers every new brief, review and packet. **(b) TRACKED file at depth >= 3 -> exit 0** (the implementer's diagnosis): combining a nested positive pathspec with the guard's own `EXCLUDES` drops the match. Measured: `git ls-files -- docs/notes/x.md` -> 1 file; adding `':!CityFlow/'` or `':!*/vendor/*'` -> 0 files; `docs/x.md` (depth 2) and `README.md` (depth 1) are unaffected. **Both diagnoses were right about different cases and each looked like a counterexample to the other**; the union is that a file is actually scanned only when it is tracked AND at depth <= 2. ⚠️ **The no-argument repo-wide run is CORRECT and unaffected**, which is why the recorded baseline of 5 has always been right and why CI catches what the hook misses. | ~30 min: drop the excludes when explicit paths are given (they exist to bound the repo-wide sweep, not a named file), plus a truth-table test over the four cases. `scripts/` is frozen: patch into `docs/patches/` + `git apply --check` + a human applies it (CLAUDE.md section 1). | **P0.9**, which already owns this script -- coordinator ruling 2026-08-17. Until then, trust only the no-argument run |
| 20 | **P2.4 inherits two corrected corpus checks, and must state its sample or run the population** | Ruled 2026-08-07. The alignment check is the **paired** form (aligned residual < misaligned, per episode), never the exact identity — which has 1.8× power on cologne3 and would be a test that certifies nothing. The exact form to assert is `Σ_j local_reward[t] == -Σ lane_waiting[t+1]`, on hz1x1 and grid4x4 only. Also: "every stored `avail_mask` is all-True" over all 4800 episodes. | folded into P2.4 | P2.4 |
| 21 | **Action masking never binds in the corpus — a P4/P5 claim constraint, not a task** | Measured 2026-08-07: all 460 mask streams sampled are all-True, because `acyclic` returns every green phase. A no-mask ablation on this corpus measures nothing, and the paper must not credit masked-action modelling. Masks bind under cyclic modes and P6 lane closures. | none — a constraint to honour | P4.1, P5.3, P6.1 |
| 19 | **What happens to `datasets/` (v1.0) once the v1.1 gate passes** | Undecided. BRIEF_08 says "do not delete"; after the six-tier bit-identity gate passes, v1.0 is redundant except as the equivalence reference for that gate. ~4 GiB. | a ruling, then minutes | After P2.6's validation gate, before P10.0's release packaging |
| 17 | **P2.4 needs a mixed-metric-key fixture for the C8 homogeneity check.** *(Row repaired 2026-08-07: it carried 4 cells in a 5-column table, so every cell after the first rendered one column to the left. Found by an awk field count split on the pipe character — the shape check to run after any structured edit, over the whole table rather than the row you touched. It caught a second defect immediately: the repair text originally quoted the awk command itself, and the literal pipe inside it broke the row again.)* | ✅ **Condition already satisfied — nothing to preserve.** The 2-vs-3 split from the aborted campaign is in **immutable git history at `7dc9928b7770~1`** (1249 files), and both files were read out of it on 2026-08-07 *without restoring anything*: `cf_hz1x1__random` carries 2 metric keys, `cf_hz1x1__maxpressure` carries 3. `datasets_att/` is already off disk (280 MB reclaimed) and `datasets*/` now prevents a recurrence. **Preferred fixture is synthetic anyway** — two minimal `.npz` with differing `metric_keys`, constructed in the test — because a regression fixture should not depend on scavenged wreckage. History is the fallback if a real-file fixture is wanted: `git show 7dc9928b7770~1:<path> > fixture.npz`. | none — evidence is permanent | P2.4 |

---

## Superseded tables to rebuild after `datasets_att/` lands

Both were computed on the **untuned** fixed-time controller and on `datasets/`:
- the **C1 ladder** (2026-08-06) — `fixedtime` rungs are stale on all three scenarios;
- the **state-coverage table** — `fixedtime` diversity was measured on `k=4`, now `k=6/1/3`.

The other six tiers are unaffected and must be bit-identical between the two corpora. **Mark clearly
which rungs come from the tuned run when rebuilding.**
