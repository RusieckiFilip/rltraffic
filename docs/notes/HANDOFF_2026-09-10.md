# HANDOFF — 2026-09-10, written for a fresh Master session

**Read this INSTEAD of a chat message.** Supersedes `HANDOFF_2026-09-01.md`, which is still accurate on
its own date and should be read for the P7 cost estimates and the failure-mode section.

**Read order:** this file → `PROJECT_PLAN` §10 → §7 → `docs/notes/DEFERRED.md` → `docs/CONTRACTS.md` →
`PREREGISTRATION.md` §12.
⚠️ **Re-check every sha and path before acting: `git rev-parse main`, `git worktree list`,
`gh run list`. §10's liveness rule stands — these claims have a half-life of hours.**

---

## 1. P5.3b IS MERGED — and it is the last piece of the paper's science

**Merged at `bd36a0a`**, §6's `P5.3b` box ticked in the merge commit (`P5.3` stays open because P5.3c
remains), CI **green** at `b790694` on both legs plus `libc-matrix`, zero frozen files touched.

**THE RESULT — primary metric `att_engine`, `dt − dt_nortg`:**

| tier | difference | CI | seeds reversing | arms distinct? |
|---|---|---|---|---|
| `mappo1000` | **+0.1226** | [−0.129, +0.374] | 2/5 | yes |
| `mix50` | **−409.15** | [−424.18, −394.11] | **0/5** | yes |
| `random` | +0.0000 | — | 5/5 | **NO — 500/500 identical** |

⭐ **The return prompt matters on multi-modal data and does essentially nothing on expert data.** First
direct evidence for the **named** contribution rather than for the architecture.

**Q1 is scored as TWO LIMBS and must stay that way:** *largest on `mix50`* is **established**; *smallest
on `random`* is **`satisfied_by_a_non_discriminating_tier` and is NOT evidence** — `random`'s two arms
are identical on 500/500 episodes, so its zero is an artefact of non-discrimination, not a null. **E4
was registered on 2026-09-09, the day before these numbers existed, and fired on exactly the tier it
named.** Without it, `+0.0000` would have been reported as the strongest possible null and it means the
opposite. **Q2 does not score (`holds: None`). Q3 holds.**

⛔ **NOT established and not to be claimed:** that `mix50`'s bimodality *causes* the gap. A broken fit
was excluded; a mechanism was not shown by the campaign. **(See §2 — the review found one.)**

---

## 2. The post-merge review, `docs/reviews/P5.3b.md`, recorded at `9ca670e`

**PASS-WITH-NOTES — 2 blocking, 10 major, 12 minor. 38 mutations, 13 SURVIVED.**

⭐ **The `−409` IS REAL, verified past any prior standard in this project:** recomputed by a stdlib-only
route importing nothing from `offline/` (`−409.14498481947595` against `−409.1449848194759`), **and 14
episodes re-rolled through a fresh CUDA process, every one bit-identical.** The pairing unit is the
registered per-draw one (`n_shared_draws = 100`) — **P8.4b's √5 defect is NOT repeated.** A5's shared
draws hold 500/500 on all three tiers.

### THE THREE OPEN ITEMS — the author has ruled these IN (see §3)

1. **BL-2(b) — we are in BREACH of a registered requirement.** `PREREGISTRATION` **A13(b)** makes the
   three-component decomposition (population + clock origin + cadence) a **required reported quantity**
   for *every* report of the two definitions' difference. **P5.3b reports the difference in three places
   and the decomposition nowhere.** And the arithmetic is already done: `−409.1450 − (−194.4757) =
   −214.6693`, against the arms' own engine-minus-ours gap of `212.557 − (−2.112) = 214.669`. **The
   engine metric's extra 215 ATT is entirely vehicles the ablated policy never admitted** —
   `never_entered` **373.8** against `dt`'s **3.7**, coordinator-verified.
   > ⚠️ **CORRECTED 2026-09-10 (later session), before acting on it.** The `never_entered` figures are
   > verified (3.74 re-measured from `output/p8_4b_rederivation/`). **The attribution is not:**
   > `−409.1450 − (−194.4757) = −214.669 = (−2.112) − 212.557` is the identity `Δ_engine − Δ_ours ≡
   > Δ(engine − ours)`, a tautology, not A13(b)'s decomposition, and the three terms cannot be computed
   > from the five A11(b) quantities at all — `P` and `W_running` need the per-second observer. Gate 0's
   > committed hz1x1 rows on the nearest analogue (`behaviour@random` draw 1000, 615 never entered) put
   > population at **−1.28** and clock origin at **−358.08**: the gap is the insertion-buffer wait of
   > vehicles that DID enter, not the never-admitted ones. **`BRIEF_33` §3.4 measures it on the actual
   > arms; Decisions Log 2026-09-10 records the error.**
2. **BL-2(c) — the paper's best sentence, and it is free.** The collapsed arm is **bit-identical to
   `dt@random`** on 88–100 % of draws for 3 of 5 seeds, confirmed against a fresh simulator. **So
   `mix50`'s ablated arm did not get WORSE — it fell into the SAME degenerate attractor the
   `random`-corpus DT already occupies, which is ALSO why `random` cannot discriminate: both its arms
   are already there.** One mechanism explaining two tiers, computable from committed bytes.
3. **MJ-3 — a coordinator ruling is HALF implemented.** The limbs are correct and refusal-gated, but
   `predictions.Q1.holds` is still an unqualified `true`, pinned by a test — **exactly the *"lets a
   reader keep the whole prediction"* outcome the 2026-09-10 ruling forbade.** `score_q2` sets
   `holds=None` under the same condition; `score_q1` does not. **It must be `None`.**

### BL-1 — DEFERRED, and the reason is structural

**Replacing the whole `mix50` `att_engine` block `−409.1450 → −0.1300` — a 3,147× reduction — left 161
tests passing.** `tests/test_p5_3b_artifact.py:159` forward-references the guarding test
`test_the_primary_definition_recomputes_from_the_reference_column`; **that test does not exist** (one
grep hit, the docstring itself).

🚨 **It CANNOT be written from a clone.** The `dt` half of the contrast — the per-episode `att_engine`
column — lives only in gitignored `output/p8_4b_rederivation/` (38,502 files) **with no
`SHA256SUMS_p8_4b*.txt`.** ⭐ **That is `DEFERRED` 56's class, SECOND BITE: a manifest that verifies
everything it lists is not evidence that everything is listed — and here the unlisted thing is the
column the headline rests on.** Closing BL-1 therefore requires **writing that manifest and shipping the
1500 `dt` reference rows into the artifact**, which is a different and larger job than the fix round.

### Also open, not in the fix round

**MJ-4 sharpens the claim and §6's entry currently overstates it:** Q1's ordering is the **reverse** of
P5.3a's decision-time flip-rate ordering (`mappo1000` 0.0036–0.0047 against `mix50` 0.0013–0.0022), so
**the prompt's value on multi-modal data is a TRAINING-time effect — mode selection — not a
decision-time one.** *"First direct evidence for the named contribution"* needs that qualification, and
it makes the claim sharper rather than weaker. · **MJ-5:** the reported CI carries **no seed-level
uncertainty** — half-width `15.038` over 100 draw-means against **≈391** over the 5 seed means — so
*"CI [−424, −394], 0/5 reversing"* reads as two reinforcing facts when 3 of 5 seeds are collapsed and 2
are not. · **MJ-10** is P8.4b's BL-1 recurring one task later (the discriminability record can be
computed from `att_ours` while declaring `att_engine`; inert on this data, verified). · **MJ-9 is
CLOSED** — see §4.

---

## 3. THE AUTHOR'S DECISION — do not re-litigate it

> **SHORT FIX ROUND for BL-2(b), BL-2(c) and MJ-3. BL-1 is DEFERRED.**

Decided 2026-09-10. **A fresh session writes that fix round; it does not re-open the scope question.**
The reasoning: BL-2(b) is a registered requirement we are in breach of, BL-2(c) is the paper's best
sentence and costs nothing, MJ-3 is one line reverting a ruling already made — and BL-1's fix is a
manifest plus 1500 rows, which is a separate job.

---

## 4. Done this session, so it is not redone

- **`output/p5_3b/` SECURED into the main tree.** It existed **only in the worktree** — 27 entries, 15
  checkpoints, the 39-line manifest — while the main tree held 3 stale files and no manifest. **One
  `git worktree prune` from destroying the paper's newest result.** Copied with `cp -rp`, **39/39
  verified in BOTH directions, mtimes preserved to the nanosecond.** The displaced stale files are at
  `output/p5_3b.stale_pre_secure/` and can be deleted.
- **Integrity: 735/735** across the ten `output/SHA256SUMS_*.txt`, unchanged across the campaign.
- **CI ceiling re-measured 139 → 144** and shipped as its own commit (`b790694`), classified by
  inspection of all 35 distinct messages, both legs identical.

---

## 5. P7.1's entry conditions, carried from P7.0

⚠️ **Recorded from the author on 2026-09-10; NOT re-verified this session. Check each before acting.**

1. **The lane-index inversion fix must land BEFORE P7.3.** P7.0 found `CityFlow road_0_1_0 lane _0 =
   turn_left, lane _1 = go_straight` against `SUMO lane _0 = "s", lane _1 = "l"` — **exactly inverted on
   all four incoming roads, 8 of 8 lane ids mis-denoting.** Its `min OVL = 0.0554` measured
   turning-versus-through traffic and would have descoped C3 on an indexing convention.
2. **P7.0's numbers may NOT enter the paper without independent review.**
3. **`hangzhou_4x4_gudang` is the only scenario with BOTH SUMO parity and coordination structure — and
   it has NO CORPUS.** So: **a one-episode SUMO timing probe before any commitment.** ⚠️ This compounds
   `HANDOFF_2026-09-01.md` §4's finding that **SUMO's per-episode cost is unmeasured anywhere in this
   repo**, which makes every P7.3 figure an extrapolation from a different simulator.
4. **Still standing from 2026-07-31: bind hangzhou's vType FIRST** — the shipped `.rou.xml` binds none,
   so SUMO runs **55.55 m/s against CityFlow's 11.11**. ⚠️ **And bind `tau` with it:**
   `docs/notes/P7.0_vtype_investigation.md` records that `pkw` alone is **+0.6 %** while adding
   CityFlow's `tau = 2.0` is **+49 % with −23 % arrivals**.
5. **Unanswered C3 threat (registered 2026-08-31):** CityFlow has **two** ATT definitions and SUMO has
   **one**, so `ρ_cityflow` is definition-dependent and `ρ_sumo` is not. **Answer before P7 is
   scheduled, not during it.**

---

## 6. What in `PROJECT_PLAN` is STALE

- 🚨 **§10's *State today* block predates P8.4's closure AND P5.3b's merge. It is banner-marked and owed
  a full rewrite.** It is the single most misleading section in the file today.
- **§10's *Immediate queue*** still lists P5.3b as held. It is merged.
- **§10's 2026-08-30 cut order** was written against a September **paper** deadline. Per
  `HANDOFF_2026-09-01.md` §2, the September deliverable is a **presentation**, so read it as a priority
  order and not a countdown.
- **§6's `P5.3b` entry** is ticked and correct, but its *"first direct evidence for the NAMED
  contribution"* needs MJ-4's training-time-versus-decision-time qualification.
- **§1b's readings** have not been revisited since P5.3b, which corroborates the C1 reading from the
  prompt side.
- ⚠️ **The Decisions Log is ~311 rows and grows several per session.** It is a log, not a summary; a
  fresh session should read §10 and this handoff, and query the log rather than read it.

---

## 7. What to distrust, including in this file

**The coordinator's failure modes in the last two weeks, all in the REPORTING layer, none a measurement
error:** over-generalising a correct measurement in the sentence reporting it · relying on a ruling
without verifying it was executed · **asserting an artifact into existence, including a commit sha that
resolved to nothing** · a condition that checks the wrong layer · quoting a constant without checking
what it measured, then accepting the explanation of why it was wrong without checking that either ·
giving a formula instead of the number it evaluates to.

> ⭐ **Every control this project has built sits in the PRODUCTION layer. The only thing that has ever
> caught the REPORTING layer is a second reader who checks the citation.** Resolve every sha and path
> before writing it: `git cat-file -t` for a sha, `ls` for a path. **And before any instruction that
> asks the author to DO something, re-run the check that establishes it still needs doing.**
> ⚠️ **A schedule handed to the author is a NUMBER, never a formula, and it names every term.**
> ⚠️ **Cite `<ref>:<path>:<line>`, never a bare line — the coordinator and the implementer share an
> object store and the author does not.**
