# HANDOFF — 2026-09-16 (end of session), written for a fresh Master session

**Read this INSTEAD of a chat message.** Supersedes `HANDOFF_2026-09-15.md` (accurate on its date). Read order:
this file → `CLAUDE.md` §4b (the trailer rule: raise it in the turn, never add it — it was raised three times this
session as the harness wording changed, and refused each time) → `PROJECT_PLAN` §7's newest 🔒 rules → `BRIEF_37`
whole, Amendments A–D last → `docs/plans/p7.3a.md` and `docs/plans/p7.3a_amendment_a_measurements.md` →
`docs/reviews/P7.2b.md`. ⚠️ **Re-check every sha before acting:** `git rev-parse main origin/main
task/p7.3a-zero-shot`, `git -C /home/filip/rltraffic-p73a status --porcelain`, `gh run list --branch main --limit 1`.

## 1. Where things stand (verified by commands on 2026-09-16, ~18:00)

- **`main` = `origin/main` = `3211203`**, plan v1.60, tree clean. **CI GREEN** at `dcc93dc` (run `35091499492`, every
  gate and guard; the skip ceiling is **178** since `51f92fa`); the six docs commits after `dcc93dc` did not change a
  test or a gating path. `scripts/check_english.sh`'s explicit-path mode is FIXED (`9edfc33`); per-file `exit 0`
  claims from before it are not evidence.
- **P7.2b is MERGED** (`ad7fa1a`; Amendment G `8c83fd8`), section 6 ticked: `docs/data/p7_2b_calibration.json` = run
  4, `92b1592d…`; registered prompts **−7185.354721543778** (`mappo1000`) and **−7431.0185327454665** (`mix50`); three
  independent merge reviews in `docs/reviews/P7.2b.md`. All four P7.2b runs, chunks, canary records and driver
  captures are archived in `output/p7_2b_runs/` (gitignored; `/tmp` is tmpfs here).
- **P7.3a is IN FLIGHT.** `BRIEF_37` issued (`6501b19`); P7.3 was SPLIT into P7.3a / P7.3b on the author's ruling
  (§6 sub-rows). Branch `task/p7.3a-zero-shot`, worktree `/home/filip/rltraffic-p73a`, **tip `e380ecb`** (no
  upstream yet — push it with `-u` before the merge, §7's rule). Plan approved at `baf1ca4`.
  **Built and verified by the coordinator from disk, in order:**
  - `d114850` — §3.3 `evaluate_arm(env_for_draw=…)` with the default branch verbatim (T3 byte-identity on committed
    P4 rows); §3.4 `aligned_observer_env_for_draw` + `AlignedEnv.recorder` refusing by name (a property raising
    `AttributeError` falls through to `__getattr__` — fixed); `halting_check` defaults False, pinned; C5 closed.
  - `196255c` — §3.0 `verify_against_artifact` + `--verify-heldout-thresholds`: **100/100 on draws 1000–1099**
    (`output/p7_3a_runs/deferred80_gate.txt`); `DEFERRED` 80 marked closed pending merge; the
    `episode_reward`/`local_return` mapping guarded on `global_reward_weight == 0.0` and refused otherwise.
  - `e380ecb` — §3.1 the SUMO draw wiring, **the door at the LOGGER boundary** (env unwrapped for MaxPressure, info
    aligned where the logger is fed — the implementer's correction of the brief, accepted as D1); §3.2 the A17(f)
    gate (integrality before `==`). **A17(f) holds under the coordinator's own recomputation** from
    `output/p7_3a_runs/collect_smoke/ep000000_seed1000_draw201.npz`: −23938.0 == the probe, all rewards integral,
    state (361, 25), format 1.1.
  **NOT yet built:** §3.5 `offline/transfer_curve.py` (the cell, the arms, ρ under both definitions, B2's
  `--stage confirmatory`, C2's halting convention, T5–T8) and §3.6 the driver (T9).

## 2. What Amendments A–D ruled, and why (all in `BRIEF_37`, all on `main`)

- **A (`6d4ae14`, plan gate, ten rulings):** anchors run UNWRAPPED (measured `KeyError` — A2); the paired CI is
  analytic `mean_ci95`, no resampling seed exists (the brief had presumed one — A3); `fixedtime` is in
  `collect.POLICIES` (A4); the observer's ×3 was not density (draw 5 is lighter than nominal) nor `libsumo` (A5);
  a 16-cell pool pilot at pre-flight (A6); **A9: the instrument must reproduce P7.1's frozen `E_sumo` before the
  campaign** because `sumo_att_reference.py` changed after P7.1's timing. **A11:** every rate is labelled *measured
  on a thermally constrained machine* until a cooling-pad canary exists.
- **B (`ed6bf35`, author's ruling): TWO DECLARED STAGES** — stage 1 the confirmatory `b_mean_k100` × both subjects
  × five seeds × 100 held-out draws plus the two ρ anchors (≈ 1,200 cells); stage 2 the three ablations + `random`
  (≈ 3,500), **UNCONDITIONAL — it runs whatever stage 1 shows.** `report --stage confirmatory` writes a stage-1
  artifact the final one cites by sha. **Declared before any number existed, for early existence and slack, NOT for a
  deadline** (the compute is one night; the author presents on the 26th). B3: nothing aimed outside the project
  carries an internal-process label such as *pre-review*; a slide states what the number is and what produced it.
- **C (`7b620ac`, nine rulings): A9 PASSES bit-for-bit** — the observer's change moved cost, not value. **The ×3 was
  the HALTING CROSS-CHECK** (`run_sumo_arm(halting_episodes=1)`, 3.49× by its own docstring, which P7.1 ran on the
  first episode only and the plan-gate harness ran on every cell). **C2: the check runs on a declared subset (every
  cell on draw 1000, 47 of 4,700), chunks record `halting_checked`, `report` refuses any disagreement.** 12 workers
  (pool scales 7.6–7.9× on 16 cores, not ×4.9). C5: fixed-time and random reproduced too — **P7.1 never reproduced
  its three teleport-free cells; A9/C5 were their first reproduction** (a correction to the freeze artifact's
  standing, recorded, artifact not edited). C6: `e_sumo` (reconstruction attribute) = `att_reference_created_population`
  (artifact key) — one quantity, two names. Schedule under C2: stage 1 ≈ 1 h, full ≈ 3–3.5 h at 12 workers.
- **D (`3211203`): T1 and T2 were done BY HAND, not as tests.** The coordinator's mutant bypassing the logger-side
  alignment (logging the raw 32-wide info) **SURVIVED** — no test references `align_for_log`, `ix0_state`, a 25-wide
  assertion, `alignment_provenance` or a SUMO sweep. **D2 requires T1 and T2 as tests sharing one real draw-201
  collection, T1 asserting −23938.0 and seed 437485271 directly as well as through the gate, T2 asserting width and
  provenance, and the bypass mutant killed — BEFORE §3.5 is built on the seam.** D3: the retired contract test
  accepted with its reason in the docstring. D4: T1-M2 (`==` → `abs < 1.0`) equivalent GIVEN the integrality guard.

## 3. Outstanding, in order

1. **T1 and T2 as tests (Amendment D2)** — the implementer's next commit; verify by re-running the bypass mutant.
2. §3.5 and §3.6, then the **pre-flight**: destruction/resume paths, the no-token driver, both canary halves, A17(f)'s
   stop, **A6's pool pilot RE-RUN ON THE COOLING PAD** (C4; the pilot's basis was a 0.90 s canary and the pad's will
   differ by more than 10 %) — the driver header's schedule is the pad pilot's rate.
3. **Stage-1 checkpoint** (100 logged episodes + A17(f) 100/100), read by the coordinator from disk before stage 2.
4. The token; stage 1 attended in tmux through a pipe and `tee -a`; `report --stage confirmatory`; the coordinator
   reads it. Stage 2 under a second token, unconditional. Packet; two sequential merge reviewers (code + mutations;
   the artifact recomputed from the raw cells); merge with §6's P7.3a box ticked; push BOTH refs; the ceiling by the
   registered route (`re_measure_required_at` names P7.3).
5. `BRIEF_38` (P7.3b) against A18 and P7.3a's *what P7.3b will assume*.
6. Retire the merged worktrees `/home/filip/rltraffic-p53b` and `/home/filip/rltraffic-cigate` (both branches merged
   and pushed; their only ignored entry is `output/`) — the author's action.

## 4. The thermal finding

The canary drifted 0.68 → 0.79 → 0.89 → 0.97 → **1.02 s** over 24 h on an idle machine on mains, then read **0.81 s**
with nothing changed but time: the laptop's underside intake is blocked (glass table). **The cooling-pad canary is
outstanding**; until it exists, the 37.06 s / 12.47 s cell rates, the ≈ 3× and the schedule are *measured under thermal
constraint* wherever quoted (A11). The driver re-runs the canary at every stage start, so each stage carries its own.

## 5. What is stale in `PROJECT_PLAN`

§10's *State today* and *Immediate queue* blocks are stale since 2026-08-29 and still owed a full rewrite; the banner
at the top of §10 is current and points here. §6's `P11.4` appears twice. §1 line 21 still carries the withdrawn
NATIVE wording with its dated correction beside it. The costing note `P7.3_COSTING_2026-09-14.md` prices the
zero-shot arm at the unobserved rate and assumes ×4.9 — superseded by the measurements in `docs/plans/p7.3a*.md`.

## 6. What a fresh session must distrust, from this cycle's own errors (each in the Decisions Log)

- **A behaviour verified by hand is not a test.** T1/T2 at `e380ecb`; `reward := 0.0` at P7.2b. Run the mutant.
- **A universal negative names its search** (*lost* written twice for files sitting in `/tmp`).
- **A failed command is one observation** (*offline* for a whole session; the coordinator pushes its own commits and
  reports the exact error).
- **A count in prose is checked against the table** (4 / six over seven rows).
- **A prediction written down is an obligation** (*expect five paths* → a red guard one push later).
- **A rate is a property of the machine state that produced it** — three schedule figures were each wrong in a
  different direction until measured; the pad canary is still outstanding.
- **A gate's cost and its value are different questions**, and only a regeneration under `==` separates them (A9).
- **The brief can be wrong and the code right**: *wrap the env* (§3.1) and *wrap every cell* (§3.5) were both
  corrected by a measured `KeyError`. When a brief conflicts with the running code, the code wins and the brief is
  amended with the evidence.
- **A follow-up push cancels an in-flight CI run**; `git merge -F -` reads no stdin; a `-P` interpreter needs
  `PYTHONPATH`; a mutator that refuses (two occurrences) is not a survivor.
- **Internal process words stay internal** (*pre-review* on a slide would qualify a result it does not qualify).
