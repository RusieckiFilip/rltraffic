# HANDOFF — 2026-09-16, written for a fresh Master session

**Read this INSTEAD of a chat message.** Supersedes `HANDOFF_2026-09-15.md` (accurate on its date; its §5 carries the
appended record of 2026-09-15/16 in order). Read order: this file → `CLAUDE.md` §4b → `PROJECT_PLAN` §7's newest 🔒
rules (five were added on 2026-09-15/16: one-place-is-not-a-record; a check that accepts a path list must prove it read
them; a pin on a function does not pin its caller; the coordinator pushes its own commits; the machine-health canary) →
`docs/briefs/BRIEF_37_p7.3a_zero_shot.md` → `docs/reviews/P7.2b.md` → `docs/returns/P7.2b.md` §17.
⚠️ **Re-check every sha before acting:** `git rev-parse main origin/main`, `gh run list --branch main --limit 1`.

## 1. Where things stand (verified by commands on 2026-09-16, ~12:00)

- **`main` = `origin/main` = `dcc93dc`. CI GREEN — run `35091499492`: all three jobs, every gate and guard, both legs
  `1876 passed, 178 skipped, 0 failures`.** Plan v1.53.
- **P7.2b is MERGED (`ad7fa1a`, 2026-09-15), section 6 ticked in the merge commit.** The first target-domain numbers
  are on `main`: `docs/data/p7_2b_calibration.json` = run 4, sha `92b1592d…`, provenance `ded56a0`, `git_dirty false`.
  Registered prompts **−7185.354721543778** (`mappo1000`) and **−7431.0185327454665** (`mix50`); `S = mean` at k = 100,
  ratio 1.2470; Rule A `q = 1.0` sits 10,818 below `mappo1000`'s trained range (an ablation). Three independent merge
  reviews (`docs/reviews/P7.2b.md`): the artifact PASSED a full recomputation under `==`; the door and the core PASSED
  WITH FINDINGS — seven surviving mutants, six findings, none a defect in shipped code — all closed test-side by
  `BRIEF_36` Amendment F with the artifact unmoved, and re-killed by the coordinator. Amendment G (`8c83fd8`, 2026-09-16)
  fixed the one P7.2b test with no `skipif` that had put CI red with a REAL failure; the coordinator's discriminating
  mutation (draw 5 present, draw 201 absent) reproduced CI's exact `FileNotFoundError` when the gate named the wrong draw.
- **The CI skip ceiling is 178** (`51f92fa`, the author's commit of `docs/patches/ci_gate_ceiling_178_p7_2b.patch`):
  classified on the registered route from run `35085215720` at `164b519`, both legs identical, direct diff against the
  158 run — 3 new messages carrying all 20 skips (+16 checkpoint-gated → `corpus_or_checkpoint` 92, +4
  parity-configuration-gated → `campaign_output` 24). `re_measure_required_at` now names P7.3's merge.
- **`scripts/check_english.sh` is FIXED on `main` (`9edfc33`, the author's commit of the coordinator's patch):** the
  explicit-path mode used to report clean on any untracked file and on tracked files under `.claude/`; it now checks
  paths as given, blocks on a missing path, and prints a loud NOTE when every path is excluded. **Every per-file
  `exit 0` quoted in a packet BEFORE `9edfc33` is not evidence.** The repo-wide sweep still lists four benign
  self-referential lines and CI's English guard baselines exactly those four.
- **`BRIEF_37` (P7.3a) is ISSUED** (`6501b19`): the zero-shot point of C3 (H3's first two clauses), the anchors on the
  held-out pool, `collect.py`'s SUMO draw wiring with A17(f)'s bit-for-bit gate, the env-factory `evaluate_arm`, the
  observed aligned env (`E_sumo` through the observer), a resumable 8-worker driver, `DEFERRED` 80's gate. P7.3 was
  SPLIT on the author's ruling; **`BRIEF_38` (P7.3b: fine-tune path, few-shot curve, anchor) is not yet written**, and
  the gate between them is mechanical and outcome-blind — *P7.3b runs whatever the number is* (`BRIEF_37` §7).
  **Costing corrected:** 4,700 OBSERVED SUMO episodes ≈ 23 h sequential / ≈ 4.8 h at the measured ×4.9 — the costing note
  priced the arm at the unobserved rate; the observed DT-driven rate is measured at the plan gate before any schedule.
- **Archives that exist only in gitignored `output/`:** `output/p7_2b_runs/` (all four P7.2b runs' artifacts, smoke
  chunks, canary records, driver captures, the three review partials, the G3 plugin, the ceiling generator) and
  `output/p7_2a_tmp_captures/`. `/tmp` is tmpfs on this machine — anything there is one reboot from gone.

## 2. What a fresh session must distrust, from this cycle's own errors (all in the Decisions Log)

- **A universal negative names its search.** "Lost / exists nowhere" was written twice for files that sat in `/tmp`;
  the correction came from asking the implementer for the exact launch command.
- **A failed command is one observation.** One `git push` failure at 14:41 became "offline" for a whole session and the
  push sat in the author's action block. The coordinator pushes its own commits and reports the exact error text.
- **A count stated in prose is checked against the table it summarises** (the review said 4 / six over seven rows).
- **A prediction written down is an obligation:** "expect five paths" in a README entry was the exact guard regression
  that turned CI red one push later. Run the guard gate locally, read it, then commit.
- **"The suite passes in a throwaway worktree" never implied "passes without the artifacts"** — `DEFERRED` 82's absolute
  roots make every gate read the main tree here; CI is the only environment where those gates fire, until re-rooted.
- **`git merge -F -` does not read stdin;** it printed an error, created nothing, and the following push said
  *Everything up-to-date*. Verify a merge by a two-parent HEAD, never by the command's summary.
- **A follow-up push cancels the in-flight CI run** (three times now, twice caused by the coordinator). Hold docs
  pushes until the run whose number you need has completed.

## 3. Next, in order

1. **P7.3a plan gate** — the implementer's `docs/plans/p7.3a.md` must carry the measured observed, aligned, DT-driven
   episode rate on draw 5 (mains, canary first), the collector's seeding path from the code, the A17(f) identity argued
   from both routes, the paired-CI helper named, the four-arm set restated. Coordinator rules; then pre-flight.
2. **Stage-1 checkpoint** (100 logged episodes, A17(f) 100/100) read by the coordinator from disk before stage 2.
3. Token; the campaign attended in tmux through a pipe and `tee -a`; packet; two merge reviewers; merge; the ceiling by
   the registered route (P7.3a adds gated tests).
4. `BRIEF_38` (P7.3b), written against A18 and P7.3a's *what P7.3b will assume*.
