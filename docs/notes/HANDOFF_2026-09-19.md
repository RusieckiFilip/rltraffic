# HANDOFF — 2026-09-19, written for a fresh Master session

**Read this INSTEAD of a chat message.** Supersedes `HANDOFF_2026-09-16.md` (accurate on its date). Read order:
this file → `CLAUDE.md` §4b (**the trailer rule: raise it in the turn, never add it** — it was raised again on
2026-09-17 under new wording and the author reconfirmed: *no AI trailer, ever, in any commit*) → `PROJECT_PLAN` §7's
newest 🔒 rules and §8's rows from 2026-09-17/18 → `BRIEF_38` whole (Amendment A last) → `docs/plans/p7.3b.md` →
`docs/reviews/P7.3a.md` (both merge reviews) → `BRIEF_37`'s closing pointer, which says where its post-J rulings live.
⚠️ **Re-check every sha before acting:** `git rev-parse main origin/main task/p7.3b-anchor`,
`git -C /home/filip/rltraffic-p73b-run rev-parse HEAD`, `git worktree list`, `gh run list --branch main --limit 1`.

## 1. Where things stand (verified by commands, 2026-09-19)

- **`main` = `origin/main` = `162544f`**, plan **v1.90**, tree clean, **CI GREEN** (the run on the pre-flight ruling
  commit), **skip ceiling 189** since `696ab10`; `re_measure_required_at` names **BRIEF_38's merge**.
- **P7.3a is MERGED** (`b480c7b`, §6's box ticked in the merge commit; branch tip `8630d5b`, merged). **The zero-shot
  point exists and the MADT beats MaxPressure on SUMO:** ρ_sumo(`b_mean_k100`, `E_sumo`) = **+1.8895 [+1.8610,
  +1.9180]** (`mappo1000`) and **+1.7264 [+1.6862, +1.7666]** (`mix50`) over 100 held-out draws × 5 seeds.
  **H3's first registered clause holds; its second (ρ < 1, worse than MaxPressure) is REFUTED** on both subjects and
  both ATT definitions. Artifacts on `main`: `docs/data/p7_3a_zero_shot.json` (`e7a0d290…`, 4,700 cells) and
  `…_stage1.json` (`57430734…`, 1,200). **Exploratory and not promoted:** the calibrated prompt buys nothing over the
  naive one (−0.0021 / −0.0179 on `E_sumo`), and `a_q1.0` collapses `mix50` to +0.52 while leaving `mappo1000`
  untouched — the inverse of what the in-support diagnostic predicted. `random` scores −1.300 / −3.604.
- **Two merge reviews, both PASS-WITH-FINDINGS, 0 blocking** (`docs/reviews/P7.3a.md`). Reviewer B recomputed **every**
  published number from the 4,700 raw chunks: per-cell ρ bitwise identical on 9,400 values, CIs to 1.78e-15, B2's 1,200
  rows, the artifact regenerating with only two expected leaves differing.
- **P7.3b is IN FLIGHT, through the pre-flight.** Branch `task/p7.3b-anchor`, tip **`f13358e`** (pushed), worktree
  `/home/filip/rltraffic-p73b`. **The campaign has NOT run.** Commits, in order:
  `8250d86` plan · `0c7d7fd` §2's five pins + Finding 4 · `b233a4a` §3.1+§3.2 corpus evidence · `b35216c` §3.3
  `offline/anchor_training.py` · `b8e6cb0` §3.4+§3.5 the anchor stage, `report`, the driver, plus the committed
  training artifact · `f13358e` the pre-flight pilot and the declaration pin.

## 2. What P7.3b has produced so far, and what was checked by the coordinator rather than read

- **The corpus is 200 episodes and is the first in this repository to carry its own digests.** Draws **301–400**
  rendered with `materialise_draws` unmodified; the gated 201–300 band UNDISTURBED (all 100 parity digests in P7.3a's
  corpus manifest still match disk). `SHA256SUMS` inside both directories — `def8fb30…` (201–300) and `93f180e0…`
  (301–400). **Amendment A2 passed:** the 201–300 digests equal the file the coordinator recorded before this task
  touched anything, 0 differing. **A17(f) re-run by the coordinator WITH the new file present: `100/100`, exit 0.**
  The band 301–400 has no probe to gate against, so its integrity is a **bit-for-bit re-collection of five declared
  draws** (301 −22676, 325 −23235, 350 −22255, 375 −21748, 400 −21063; `engine_seed_drawn 437485271` on every one).
- **The anchor is trained.** Five seeds × 40,000 steps, 1,095 s, artifact `docs/data/p7_3b_anchor_training.json`
  (`edca65f9…`) committed on the branch and byte-identical to the run's output. **All five checkpoint digests match
  their files.** Recipe A18(a) verbatim, `raise_to: null`, provenance clean at `b35216c`.
  **The prompt is `target_rtg = −20625.0`, `rtg_scale = 25257.0`** — recomputed by the coordinator from the raw
  episodes alone (`np.cumsum`), matching the module's two internal routes. **Amendment A5's collision is broken by the
  data:** the 201–300 half maxes at −20809.0, which is `a_q1.0`'s registered target for both subjects **by
  construction**; the new half contains a better episode. The proof that the anchor's prompt is its own remains the
  ROUTE test, never a value.
- **The pilot ran** on this task's own cells: canary 0.76 s, 4 fenced cells on draw 5, wall 19.53 s, in-process mean
  17.02 s, a resumed run reusing 4 of 4 with the rate correctly `null`. **The header states that the schedule is NOT
  700 × 4.883 s** — four cells cannot saturate twelve workers — and projects 17–27 min from P7.3a's twice-measured
  full-pool scaling. **The pilot found a defect reading could not have:** `run_pilot`'s transcript did
  `int(cell["seed"])` over every cell and an anchor cell has `seed: None`; all four cells rolled, then the summary
  raised `TypeError` and destroyed the rate. F1's sixteen cells were all `dt`, so nothing had met that seam for a
  whole task.

## 3. What §2's five pins and the stage-identity fix closed

**The five pins** (merge reviewer A's majors, all correct code that no test would have noticed changing) are in
`0c7d7fd`, and the coordinator re-killed every one: the **engine seed** actually handed to `horizon_rollout` (the
chunk records the constant, not the call); `validate_cell_payload`'s **`rtg_first == target_rtg`** refusal; the ATT
key **`_paired_block`** uses, which is the registered H3 test row; the **in-support bounds**; and `run_stage`'s
**resume-by-content**, whose removal reinstated the `p5_3b.sh` `[ -f ]` defect its own docstring cites. Finding 4's
fix is in the same commit — the all-excluded env-ATT path raised `TypeError` in `_h3_block` and `_contrast_block`
while a comment claimed it carried on; both guards and the comment are corrected.

**The stage-identity hazard, found by the implementer in `b8e6cb0` and confirmed by the coordinator.**
`cell_chunk_name` derives from `(subject, arm, seed, draw)` and **not** the stage, so the anchor's 200 re-rolled ρ
denominators carry **the same 200 filenames** as P7.3a's anchors. Nothing collides on disk because the work
directories differ — but `validate_cell_payload`'s identity omitted `stage`, so a P7.3a chunk offered to
`report --stage anchor` would have validated as one of the anchor's own. **`stage` is now part of that identity**
(measured: 200 shared names, zero overlap once stage is in the key). This closed merge reviewer A's minor 9, which was
outside `BRIEF_38` §2, and the implementer flagged the scope deviation rather than burying it. `declared_cells(None)`
is still **4,700** and `confirmatory + rest` still partition it element for element; the anchor's **700** are
reachable only by name and are now pinned by size and composition (mutations die on `500 == 700` and `300 == 700`).

## 4. Two rules this cycle added — both binding

1. **NO COMMIT IS AMENDED AFTER A CHUNK HAS BEEN ROLLED FROM IT.** The pilot's four chunks record **`5849d59`**, which
   was amended into `f13358e` and is now a **dangling object** `git gc` will delete. The rate still describes the
   shipped runner — the only `offline/transfer_curve.py` difference between those commits is one prose string inside
   `run_pilot`'s fenced record, no logic — and the packet says so. Evidence whose commit cannot be checked out has a
   shelf life.
2. **A stage's verification reads the DRIVER CAPTURE, not only the work directory's logs** (2026-09-18). P7.3a's
   stage 2 **refused a start at canary 6.42 s** — "the machine is throttled" — fourteen minutes before its rate was
   measured, and the coordinator missed it because `canary.log` by design records only starts that consumed a token.
   Merge reviewer B found it in `stage2_driver_capture.txt`.

Standing from earlier and still binding: **no evidence of a P7.3 run is written to `/tmp`** (tmpfs here); the campaign
runs from a **dedicated worktree at a pushed commit**, never the implementer's tree (J1(e)); long runs — collection,
training, campaigns — go to a **tmux session the author starts** (`CLAUDE.md` §5).

## 5. The campaign: run worktree, token, and what to read afterwards

`/home/filip/rltraffic-p73b-run` exists, **detached at `f13358e`, clean**. The driver derives its worktree from its
own location, so running the copy inside it points everything there; the launcher under `output/p7_3b_runs/` hardcodes
the implementer's tree and is therefore **not** used.

```bash
mkdir -p /home/filip/rltraffic/output/p7_3b_anchor && date -Is > /home/filip/rltraffic/output/p7_3b_anchor/AUTHORISED_TO_RUN
tmux new -s p73b_cells 'bash /home/filip/rltraffic-p73b-run/offline/campaigns/p7_3b_anchor.sh anchor 2>&1 | tee -a /home/filip/rltraffic/output/p7_3b_runs/campaign_capture.txt'
```

700 cells (500 anchor + 200 ρ denominators), 17–27 min expected. **Afterwards the coordinator reads the CAPTURE and
the logs**, then: 700/700 cells present and valid, every chunk at one commit with `git_dirty: false`, 0 teleports,
`cf_parity`, `"-1"`, 360 decisions, `rtg_first == target` on all 500, the artifact regenerating byte-identically, and
ρ recomputed from the raw chunks by an independent route.

## 6. What remains, in order

1. **The campaign**, then the coordinator's read from disk.
2. **The packet** `docs/returns/P7.3b.md` per `BRIEF_38` §8 — including the dangling-commit caveat (§4.1), the pilot's
   own rate with A11's label, and *what P7.3d will assume*.
3. **ONE merge review** (the author's compression, 2026-09-18): after it, findings go in the packet **with their
   caveats, not as amendments**; only something that could make a number wrong comes back as one paragraph for the
   author to rule.
4. **Merge with §6's box ticked**, both refs pushed, then **the CI skip ceiling re-measured by the registered route**
   — merge, let it go red, classify `junit.xml` from BOTH legs, diff against the 189 run, commit only the OBSERVED
   value. The route is documented in `docs/patches/README.md`; the last move (178 → 189) is the template.
5. **P7.3d, grid4x4** — authorised by the author, costed at ≈ 7.5–11 implementer days and ≈ 3–16 h of compute
   (`docs/notes/GRID4X4_ZERO_SHOT_COSTING_2026-09-17.md`). **P7.3c, the few-shot curve, is DEFERRED not cancelled.**

## 7. What a fresh session must distrust, from this cycle's own errors

- **A date asserted is not a date measured.** *When did P7.3a's chunks stop being reusable?* The brief said "this
  task's first code commit", the implementer said `696ab10`; both were wrong. `code_changed_since` says **`d92947e`**.
- **A claim about a file's content, written from memory, is the recurring error.** `BRIEF_37` named
  `SHA256SUMS_p4_6` as `mappo1000`'s digest pin; it lists none of those files, and every `SHA256SUMS_*` is gitignored.
  `BRIEF_38` pointed at "§10's outcome rows" and "§4's cap" as if they were its own sections.
- **A mutation verdict is only as good as its selector.** The coordinator's first K-round run reported KILLED for a
  mutant whose test failed with **exit 3** — the driver's live-runner refusal — because the coordinator's own suite
  was running concurrently. Check `pgrep -f 'python.*offline\.transfer_curve'` is empty first.
- **A test can pass while the guard it names is dead** (a refusal test matched a string that a later guard's message
  also contained), and **a bare `pytest.raises(TypeError)` is unfailable** — `check_test_hygiene.sh` caught one here.
- **A behaviour verified by hand is not a test**, and **a docstring's claim about WHICH refusal fires** is worth
  measuring: `run_pilot` said the undeclared-cell fence guards a pilot directory; the refusal that actually fires is
  completeness.
- **The registered direction of an exploratory contrast was not observed**, and **H3's second clause was refuted.**
  Both were fixed in writing before any target-domain number existed, which is exactly why they can be reported as
  they came out. Nothing in the artifact interprets; keep it that way.
