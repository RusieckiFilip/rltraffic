# P7.3d campaign, ATTEMPT 2 (2026-09-24) — read from disk by the coordinator (gate G6)

**Status: COMPLETE, 700 / 700, verified.** The confirmatory campaign ran from the run worktree `/home/filip/rltraffic-p73d-run` at
**`10d8fe8`** (B.8 in, A23 registered), started 21:11:47 and ended `CAMPAIGN COMPLETE (confirmatory) in 5896s`, `DRIVER EXIT: 0`. Every
check below was run by the coordinator on 2026-09-24 in the order `HANDOFF_2026-09-23.md` §2.5 fixes, the blind comparison FIRST. Model:
`claude-opus-5-5[1m]`. Evidence: `output/p7_3d_runs/coordinator_g6/` (every script and its output).

## 1. The headline — H3 on grid4x4, AS REGISTERED (A20(e); A23(d))

| quantity (registered subject `mappo1000_dt_nomix_h4`, arm `b_mean_k100`, 5 seeds × draws 1000–1099) | value |
|---|---|
| **ρ_sumo on `E_sumo` — the registered primary** | **+0.8855, 95 % CI [+0.8736, +0.8973]**, n = 100 draws |
| ρ_sumo on `att_env` — co-reported | +0.8855, 95 % CI [+0.8736, +0.8973], n = 100 |
| **clause 1**, `ρ_sumo(b_mean_k100) > 0` — CONFIRMATORY on this scenario | the inequality holds: point estimate AND the whole CI |
| **clause 2**, `ρ_sumo(b_mean_k100) < 1` — reported as the inequality it is, NOT scored | the inequality holds: point estimate AND the whole CI |
| clause 3 | void (A19): no few-shot point and no anchor on grid4x4 |
| paired `E_sumo`: the DT against fixed-time | 183.62 s vs 304.06 s, −120.44 s; Wilcoxon p = 3.96e-18 (the DT better on all 100 draws) |
| paired `E_sumo`: the DT against MaxPressure | 183.62 s vs 168.03 s, +15.59 s; Wilcoxon p = 3.96e-18 (MaxPressure better on all 100 draws) |
| per seed (`E_sumo`; `att_env` equal to four decimals) | 101: +0.8272 · 202: +0.8947 · 303: +0.8962 · 404: +0.8914 · 505: +0.9178 |
| **A23(d)'s robustness check** (draws 1020, 1042 removed whole; 98 draws) | +0.8854 [+0.8733, +0.8975]; paired −120.43 s and +15.60 s, p = 8.46e-18 each — it agrees with the primary on the sign of mean ρ, on the CI excluding 0 and on the Wilcoxon decision, so A23(d)'s disagreement sentence is not needed |
| A23's collision record | exactly the two registered events: fixed-time draw 1020 and the DT seed 303 draw 1042, one each; MaxPressure 0 |

A p-value of 3.96e-18 is the smallest the registered test (two-sided, normal approximation, n = 100) can produce: every paired difference
has the same sign — **counted directly: at the registered unit (the held-out draw, seeds averaged) the DT beats fixed-time on 100 / 100
draws and MaxPressure beats the DT on 100 / 100; at the level of single cells the DT beats fixed-time in 500 / 500 and MaxPressure beats
the DT in 441 / 500 (`E_sumo`; 440 on `att_env`)** — so 59 single seed-draw cells beat MaxPressure while no draw's seed mean does. **What the artifact says is arithmetic, not a verdict** (`reported_not_interpreted`); A21(b)'s two scope sentences are
carried verbatim first under `what_this_does_not_say`. **A fact for the paper, stated and not interpreted here:** on hz1x1 H3's clause 2
was REFUTED (ρ_sumo +1.8895 / +1.7264, the zero-shot DT better than MaxPressure; A19); on grid4x4 its inequality HOLDS (+0.8855: better
than fixed-time on every draw, worse than MaxPressure on every draw).

**Per-intersection ρ_i (EXPLORATORY and descriptive, A20(e); B.7.2's ratio of means; no CI):** A0 0.087 · A1 1.042 · A2 1.025 · A3 0.983
· B0 0.899 · B1 1.010 · B2 1.038 · B3 0.909 · C0 0.555 · C1 1.040 · C2 1.039 · C3 1.034 · D0 0.966 · D1 1.048 · D2 1.045 · D3 0.926. Every
denominator is large (−797 to −1,279 in the collection-reward return) and MaxPressure beats fixed-time at every intersection on every draw
(`n_draws_mp_not_better` 0 on all 16), so no ρ_i rests on a near-zero denominator. **Fourteen intersections sit in 0.90–1.05; A0 (0.087)
and C0 (0.555) carry the shortfall.** B.3-4's probe-ratio observation is to be read against these returns in the packet and the paper —
descriptively, as registered.

## 2. The read, item by item (`HANDOFF_2026-09-23.md` §2.5)

- **(0) FIRST — the blind comparison (A23(f), B.8-3):** attempt 1's 698 valid chunks against attempt 2's same cells, every shared field
  but `git_commit`, `format_version`, `seconds`, `canary_seconds` — **58 fields per chunk, 0 chunks differing**; the only cells new in
  attempt 2 are A23's two, each recording exactly one collision, and no other cell records any. **PASS** — no outcome was read before it.
  The re-run was not a second sample: it is the same experiment, bit for bit.
- **(a) The capture** (`output/p7_3d_runs/campaign_capture.txt`, attempt 2 at lines 4367–8726, sha256 `e37a5b9b…` at 8,726 lines): no
  refusal before the start; canary 0.82 s, both halves; `dt_reroll_check IDENTICAL` (13 rolls, 67 fields); six `reference_reroll_check
  MATCH` lines before the token and in the banner; **the token consumed exactly once**; commit `10d8fe8`, 12 workers, 40,187 MiB free; 0
  failed cells; the artifact written with 700 cells; the manifest re-verified (960 files); `CAMPAIGN COMPLETE`; `DRIVER EXIT: 0`.
- **(b) The 700 chunks** (stdlib, nothing from `offline/`; counts only): 500 `b_mean_k100` + 100 `fixedtime` + 100 `maxpressure`; every one
  at `10d8fe8`, clean, `cityflow_grid4x4`, stage `grid4x4_confirmatory`, format `1.1`; config and route digests equal to each draw's grid4x4
  parity provenance (never hz1x1's); teleports 0 except A23's two cells (1 each, explained), 0 unexplained, 0 vanished; `-1`; `cf_parity`;
  360 decisions; ONE engine seed over all 700; the canary; halting ON on exactly draw 1000's 7 cells with 0 disagreements over 691,200
  lane-seconds; the two return routes equal on all 16 ids; actions 360 × 16 in range; the two collision events equal to attempt 1's
  diagnosis (+1 s label), both `arrived_at_collision_step`.
- **(c) The 500 DT chunks:** the calibration digest pinned; each seed's checkpoint digest A20(a)'s; `target_rtg` and `rtg_first` equal to
  the registered k = 100 target on all 16 ids; **the RTG identity exact on 500 / 500** in the agent's own association.
- **(d) C4's six reference cells** in the campaign: MATCH on the 17 fields and the halting counts.
- **(e) The artifact regenerates BYTE-IDENTICALLY** from the run worktree (`report` re-run over the real work dir into scratch; 4,564,660
  bytes, sha256 `c63c371f…`, equal to the manifest's line; the work dir untouched; all 960 manifest lines re-verify). **ρ recomputed by an
  independent route from the raw chunks, per cell under `==` FIRST:** 500 / 500 cells equal on both definitions (no zero denominator),
  100 / 100 per-draw seed means, 10 / 10 per-seed means, the registered mean / sd / CI on both definitions, the paired means, the Wilcoxon
  p (an independent implementation, relative difference 0), and A23(d)'s robustness block per draw and in its stats — all equal. The
  per-intersection block: every ρ_i, mean return, gap and denominator equal under `==`; two `mean_gap_se` values (A1, C3) differ from the
  coordinator's stdlib route by exactly 1 ULP and equal the registered estimator's numpy route exactly — the summation-order class
  (`DEFERRED` 85), not a defect.
- **(f) What the numbers MEAN was fixed in advance** and §1 reports exactly that: clause 1 confirmatory and holding; clause 2 an inequality,
  holding, not scored; clause 3 void; A21(b)'s scope sentences verbatim; the per-intersection block and B.3-4's observation descriptive.

## 3. A timing note — no number depends on it

The cells stage took **5,887 s (98 min)** against attempt 1's 2,923 s, inside the header's 57–113 min range. Each cell got slower rather
than the pool running under-used: per-cell seconds median 60.2 (attempt 1: 48.8), upper quartile 126.0 (49.8), maximum 490.6 (169.8); the
sum over cells ÷ 12 workers is 97.7 min, the wall. The lower quartile (49.9 s) is attempt 1's speed, so the slowdown was intermittent and
machine-side (load or throttling during the run); the coordinator's own pre-token re-rolls at the same commit ran at attempt-1 speed.
Outcomes are bit-identical to attempt 1 on 698 cells (§2(0)).

## 4. What has been SEEN

Everything: this is the read. It happened after (0) passed, as A23(f) requires.

## 5. Next — `BRIEF_39` Amendment D

C7 (the implementer): the artifact copied BY HAND into `docs/data/p7_3d_grid4x4.json` with its measured digest (`c63c371f…`), the whole
suite after the copy, and the packet's last section (`BRIEF_39` §7 plus Amendment D's list) → *"P7.3d done"* → ONE merge review (A21) →
the merge with §6's box ticked → the CI skip ceiling by the registered route.
