# FROZEN ENVIRONMENT RECORD -- rltraffic, written by P0.10 (BRIEF_23) on 2026-08-17.
#
# WHAT THIS FILE IS: a record of the exact environment that produced the merged numbers in
# output/ and in docs/data/. It is evidence, not configuration.
#
# WHAT IT IS NOT: a supported matrix, and not an installer input. Two lines below cannot be
# resolved on another machine and are kept because removing them would delete provenance:
#   * `CityFlow @ file:///home/filip/rltraffic/CityFlow` -- built from the vendored tree
#     (cd CityFlow && pip install .), never published to an index;
#   * the `-e git+https://github.com/RusieckiFilip/rltraffic.git@<sha>#egg=zpp_traffic_control`
#     self-entry -- it records the commit the environment was installed from.
# To reconstruct an approximation, install the project with its exact pins from
# pyproject.toml and build CityFlow from CityFlow/.
#
# WHY IT EXISTS: the project reports differences of 5.68e-14, argues from 1-ulp float
# reduction order and proves 1-vs-16-thread invariance, while the environment producing
# those numbers was specified by lower bounds only (`numpy>=1.24.0`, `torch>=2.8.0`,
# `requires-python = ">=3.9"` against an actual 3.12.13). A referee who notices would be
# right to ask, and until this file existed the honest answer was that we could not
# reconstruct it.
#
# NOTHING WAS UPGRADED TO PRODUCE THIS FILE. The brief is explicit: the goal is to
# *describe* the environment that produced the results, not to change it -- an upgrade
# would invalidate every number in output/.
#
# MEASURED FROM THE LIVE VENV:
#   interpreter : Python 3.12.13 (main, May  6 2026, 18:46:41) [GCC 15.2.0]
#   venv        : /home/filip/rltraffic/.venv
#   pip         : 26.1.2
#   torch build : 2.11.0+cu128 (CUDA 12.8; nvidia-* wheels below arrive with it)
#   command     : /home/filip/rltraffic/.venv/bin/python -m pip freeze
#   kernel      : see below
#   uname       : Linux 6.18.33.1-microsoft-standard-WSL2
#
CityFlow @ file:///home/filip/rltraffic/CityFlow
cloudpickle==3.1.2
contourpy==1.3.3
coverage==7.15.0
cuda-bindings==12.9.4
cuda-pathfinder==1.2.2
cuda-toolkit==12.8.1
cycler==0.12.1
Farama-Notifications==0.0.6
filelock==3.29.0
fonttools==4.63.0
fsspec==2026.4.0
gymnasium==1.3.0
iniconfig==2.3.0
Jinja2==3.1.6
kiwisolver==1.5.0
MarkupSafe==3.0.3
matplotlib==3.11.0
mpmath==1.3.0
networkx==3.6.1
numpy==2.5.1
nvidia-cublas-cu12==12.8.4.1
nvidia-cuda-cupti-cu12==12.8.90
nvidia-cuda-nvrtc-cu12==12.8.93
nvidia-cuda-runtime-cu12==12.8.90
nvidia-cudnn-cu12==9.19.0.56
nvidia-cufft-cu12==11.3.3.83
nvidia-cufile-cu12==1.13.1.3
nvidia-curand-cu12==10.3.9.90
nvidia-cusolver-cu12==11.7.3.90
nvidia-cusparse-cu12==12.5.8.93
nvidia-cusparselt-cu12==0.7.1
nvidia-nccl-cu12==2.28.9
nvidia-nvjitlink-cu12==12.8.93
nvidia-nvshmem-cu12==3.4.5
nvidia-nvtx-cu12==12.8.90
packaging==26.2
pillow==12.3.0
pluggy==1.6.0
Pygments==2.20.0
pyparsing==3.3.2
pytest==9.1.1
pytest-cov==7.1.0
python-dateutil==2.9.0.post0
setuptools==78.1.0
six==1.17.0
sumolib==1.27.1
sympy==1.14.0
torch==2.11.0+cu128
traci==1.27.1
triton==3.6.0
typing_extensions==4.15.0
wheel==0.47.0
-e git+https://github.com/RusieckiFilip/rltraffic.git@f6da4fd733a87baec1afa7488f98a9d18127077a#egg=zpp_traffic_control
